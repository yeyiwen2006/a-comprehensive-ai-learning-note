---
title: "29.3 Titans与Nested Learning（论文）"
source_docx: "第4部分 大模型智能体与持续学习/29.基于梯度与超网络的持续学习/29.3 Titans与Nested Learning（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 29.3 Titans与Nested Learning（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、Titans

Google Research提出的Titans将模型视为一个具有不同层次记忆系统的智能体，通过对Delta Attention进行泛化，引入了一种能够在测试时改变权重、进行记忆的长短期神经记忆模块。

### （一）核心思想

Titans架构由三个相互关联的Hyper-heads组成，模拟了人类大脑处理信息的不同方式：

Transformer模块：类似于人类的短期记忆（工作记忆），负责处理当前数据流，通常使用窗口有限的注意力机制实现。

长期记忆模块：类似于人类的长期记忆，维护大小恒定的MLP，在推理过程中通过梯度下降不断更新其中的参数，从而“存储”长期的历史信息。

静态参数模块：训练后“冻结”的参数，存储全局知识和能力。

### （二）长期记忆模块的基本原理

和Kimi Delta Attention类似，计算新信息的Key经过记忆模块后的输出与Value之间的误差，作为“惊讶度”，损失函数为：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

刀忉 -IISTkt-

即梯度更新的幅度与误差成正比，误差大意味着过往信息中没有这部分内容，记忆模块通过梯度更新将其“记住”。

但Titans也作出了改进和泛化：

1.将单一矩阵（线性层）改为MLP，包含了非线性激活函数。多层 MLP 的非线性表达能力远强于线性矩阵，能把更复杂的历史抽象压缩进参数里。

2.采用了动量法，原来的MLP为M_t-1时，利用梯度的移动平均S_t更新MLP的参数：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Mt = （1 一 (t)Mt-1 + St

这样可以增加前后token之间语境的连贯性。

需要注意的是，对于第t个输入token（或段落，段落中的token并行经过记忆模块和推理，此时记忆模块记录上一段落及更早的记忆，即每段只用和原来之间的平均损失更新一次）检索时，输出一个由和输入段落同样多的embedding向量构成的序列（矩阵）：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 3 OCR 未识别出有效文字：未识别出有效文字。]

上面所述的更新过程均发生在这步计算完成之后，因为记忆模块的目的是“检索过去的信息”，不能让本token的Key和Value混入其中影响判断。

### （三）记忆模块融入模型架构的方式

Memory As a Context：将记忆检索结果作为上下文拼接，在处理长程依赖时表现最好。

Memory As a Gate：记忆与注意力分支并行并门控融合。

Memory As a Layer：记忆模块作为独立层顺序堆叠。效率最高但性能较差。这种顺序设计限制了模型同时利用注意力和神经记忆互补处理数据的能力，但优势在于能更好地适配FlashAttention等硬件加速技术。

### （四）为什么Titans无法像Kimi Delta Attention那样迅速投入业界应用？

1.必须保留计算图和显式执行反向传播

核心在于上述的梯度表达式。Kimi Delta Attention之所以不需要显式执行反向传播，是因为其隐状态更新公式可直接求出解析解，通过矩阵乘法和加法来更新隐状态：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

St = St-1 + 0 岳

从而我们不需要保留计算图。

但是Titans存在非线性激活函数，其梯度在跨越激活函数的过程中，无法简单地写成解析闭式解，因此在推理更新阶段，系统必须维护一个针对该MLP模块的局部计算图来执行梯度下降，还要保留优化器状态。这带来了巨大的计算开销，和现在业界的推理优化方式不兼容。

【注意】Titans在训练时直接舍弃二阶梯度项，即训练时直接由L对“当前时刻的瞬时权重”求梯度。主要计算开销在于推理时的反向传播。

2.Memory as a Context和Memory As a Gate无法用Flash Attention大规模并行

我们知道，当输入第t个token（或段落，段落中的token并行经过记忆模块和推理，此时记忆模块记录上一段落及更早的记忆，即每段只用和原来之间的平均损失更新一次）时，会先经过记忆模块：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 5 OCR 未识别出有效文字：未识别出有效文字。]

随后记忆模块更新：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Mt = （1 一 (t)Mt-1 + St

MAC：处理第2段q_2（x_2）就必须要先算出整个M_1，M_1需要通过x_1的值由梯度下降（无法写成显式解，即矩阵加法和乘法形式）才得到，必须一步一步串行。Flash Attention能在同一段上下文内部执行分块加速，但它无法跨段落融合。因为在算子层面，全局计算图被“权重更新”这一步骤物理性地切断了。

MAG：注意力分支中，对于一篇文章的各个段落（从1到t）可以完美使用Flash Attention。然而，由于最终输出需要注意力输出与记忆输出在每一位置进行门控融合，这限制了某些深度端到端的内核优化，因为数据必须从SRAM回写到HBM进行融合。

MAL：当所有段落都经过记忆层后，就完全同步了，可以直接用Flash Attention对整个文本的计算进行分块加速。

### （五）为什么Titans强于用RAG注入上下文？

1.压缩和抽象：简单地将文本块存入文件系统或向量数据库，检索出来的是原始数据。原始数据往往包含大量噪声，且没有经过“理解”和“压缩”。而Titans长期记忆模块通过梯度下降学习的是数据的抽象，它将成千上万个token的逻辑关系“浓缩”进神经网络的参数中。

2.“惊讶度”驱动：只有当输入的信息违反了模型的预期（即很“令人惊讶”）时，模型才会产生强烈的梯度去大幅修改权重。这种机制让模型能自主决定哪些信息是真正值得“刻骨铭心”的，哪些只是重复的废话。

3.表达能力：大多数外置记忆检索基于线性相似度，且把序列切成块存进文件系统，块与块之间的逻辑流动会断裂；Titans使用深度（≥2层）MLP，且运用动量法使得前后token更连贯。

4.解决“迷失在中间”和窗口限制：RAG 提取回来的上下文依然受限于Transformer的窗口大小，且模型往往会产生“迷失在中间”的现象，无法有效利用极长的上下文；Titans是在参数层面更新，可以轻松扩展到更长的上下文窗口，而传统的检索或简单的循环模型此时性能会剧烈下滑。

### （六）训练时一阶递归的时间复杂度

在同一个块内，通过固定初始权重，可以一次性并行算出所有token的梯度项总和，时间复杂度为O(1)。而在不同块间，训练时利用S_0算出S_N的时间复杂度是O(logN)而非O(N)。这是因为，我们注意到下一段对上一段的依赖关系：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

= 目 M 一诩 》 2。 假设损失函数是二次的： I();） 根据梯度下降， 更新公式为： Mt = M 1 一 qVl(Mt 一 1） 其中梯度 VI = 2 （M 仁 1 岳一 》 现在， 我们把梯度公式代入更新公式： Mt = Mt-1 一 77 · 2 （“ 一 1 一） 人有 = Mt- 关键的一步出现了！ 我们提取出 Mt-1： 0 一 2 一 1 岳 + 277 Mt = Mt-1(I 一 2 帕凼） + （2 tktT）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

= 目 M 一诩 》 2。 假设损失函数是二次的： I();） 根据梯度下降， 更新公式为： Mt = M 1 一 qVl(Mt 一 1） 其中梯度 VI = 2 （M 仁 1 岳一 》 现在， 我们把梯度公式代入更新公式： Mt = Mt-1 一 77 · 2 （“ 一 1 一） 人有 = Mt- 关键的一步出现了！ 我们提取出 Mt-1： 0 一 2 一 1 岳 + 277 Mt = Mt-1(I 一 2 帕凼） + （2 tktT）

也就是说，和Kimi Delta Attention一样，上式可看成对MLP模块M_t-1进行线性变换：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Titans 的动量更新公式为： St = St 一 1 + —OtVI 是已经预计算好的瞬时项。 这可以看作是一个线性变换： 其中 St = St 一 1 + bt （这里一， bt =） 我们可以定义一个元组算子 = （， (t), 它表示从状态一 1 到乯的转换逻辑。 如果我们有两个连续的转换 = (al, bl） 和 = (a2, (2), 它们的组合结果为： S2 = a2(a1So + bl） + b2 (a2a1)So + （a2 bl + b2）

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Titans 的动量更新公式为： St = St 一 1 + —OtVI 是已经预计算好的瞬时项。 这可以看作是一个线性变换： 其中 St = St 一 1 + bt （这里一， bt =） 我们可以定义一个元组算子 = （， (t), 它表示从状态一 1 到乯的转换逻辑。 如果我们有两个连续的转换 = (al, bl） 和 = (a2, (2), 它们的组合结果为： S2 = a2(a1So + bl） + b2 (a2a1)So + （a2 bl + b2）

利用S_0算出S_2，只需要经过以上两个算子的融合算子：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

(a201， a2 bl + b2 〗

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这种算子组合操作是满足结合律的： 一圇由于满足结合律， 我们就不必非得从左往右算： 1 ． 2 ． 3 ． 第一层 （并行）： 同时算出 （1 (2), （3 (4), （5 6） · · （消耗 1 个单位时间）。 第二层 （并行）： 将结果两两合并， 算出 （1 一 4）， （5 一 8） · 一 （再消耗 1 个单位时间）。 对数层级： 经过 log2 N 次合并， 你就能得到从起始点到任意位置的总转换算子。

## 二、Nested Learning

### （一）问题背景

无论是训练MLP还是运行Linear Attention，都可以被写成同一个通用的联想记忆优化目标：

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

neW = arg min 〈 Mapping(Key), ． + Regulariz at ion

MLP 训练是在低频（整个预训练数据集上、只训练一次）求解一个优化问题，将“输入x”映射到“梯度信号u”；Attention 推理是在高频（当前上下文窗口内、每次输入token时）求解一个优化问题，将“Key”线性回归映射到“Value”。也就是说，MLP的“训练”和 Attention 的“推理”，在数学形式上是完全一样的（都是在做梯度下降），区别仅仅在于它们的更新频率和数据来源不同。

现在的Transformer，本质是在外层MLP内部嵌入一个Attention层。进一步说，Transformer 实际上是一组并行的、频率不同的线性层，有的线性层（MLP）更新得很慢（由于预训练），有的线性层（Attention）更新得极快（在推理时实时更新）。

外层是一个极低频优化的MLP，它在预训练时注入通用知识，并永久记忆，但无法更新（更新频率为0）；内层则是一个高频优化的线性回归，它（在任务结束前）每输入一个token就更新（更新频率极高），但难以记忆（大模型在上下文处理时的“灾难性遗忘”）。于是我们想到两个改进：

1.线性回归毕竟比较弱，能不能把内部换成高频优化的MLP呢？

2.能否找一个中等的优化（更新）频率，作为外层与内层之间的“中间层”，隔一定量的token更新一次，既能更新知识，又不至于因过快更新导致遗忘，从而解决LLM在预训练结束后就无法学习新知识的问题？

（注：我们这里假设模型“在线学习”，即相当于Transformer中的前向推理过程一直不结束，模型持续学习新知识，不会出现KV Cache中的“清空显存”操作）

### （二）HOPE架构

结合上面所述，我们参考大脑中不同频率的电波，构造以下HOPE架构：

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

参数更新规则为： 三 0 ifi （30） (mod C(I)） 乥彳@然 t=i—C(1) （原理阐释与推导分析 〕 （31） 分形结构： HOPE 模型不再区分 " 训练阶段 " 和 “ 推理阶段 "。 它由多层 MLP 组成， 每一层的更新速度不同。 荪 （高频）： 可能每读一个 to ke n 就更新一次参数 （类似 RNN 的隐藏状态）。 艿 （中频）： 可能每读 16 个 token 更新一次。 （低频）： 可能每读 IM 个 toke n 更新一次 （接近传统的预训练）。 数学意义： 公式 （31） 定义了一个多时间尺度的在线梯度下降。 每个 MLP 都在压缩属于它那个时间窗口内的上下文信息。 这模仿了人类大脑： 只有极其重要或重复的信息 （通过慢速层） 才会变成长期记忆， 而原本的信息流 （快速层） 会被快速处理并遗

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

高频层 （类似海马体）： 迅速改变自己的结构 （参数 ), 捕捉当前的对话细节、 临时人名或数字。 如果这层参数不更新， 模型就会忘记前一句话说了什么。 中频层 （类似皮层整合）： 它的参数变化较慢， 负责提取段落级的主题、 逻辑流。 它忽略细枝末节， 只记住 " 大概发生了什么 "。 低频层 （类似长期知识）： 极其稳定， 几乎不怎么变， 存储核心的语言规则和世界知识。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 HOPE 架构中， 全连接层 (MLP) 是动态的。 它的 " 前向计算过程 " 包含了一个在线更新步骤 （即公式 30 一 31）。 这意味着， 当数据流经这个层时， 它确实是在实时求解一个微型的局部优化问题， 将当前的上下文压缩进参数里。 它求解的优化方程 （局部压缩目标）： 这个动态 MLP 试图最小化当前输入与其对应的 " 局部惊奇信号 " （梯度） 之间的 2 距离。 方程如下： 禺 + 1 = arg min 目@讫 “ 一 Vy £ （；） 腭 @忆： 278 ] 局部惊奇信号前向计算的本质： 在 HOPE 中， 前向计算不再只是一而是变成了： 1 ． 2 ． 求解优化： 根据当前输入， 求解上述方程， 得到更新后的权重 1 （通过公式 28 的解析解形式）。 应用计算： 使用新的 ; + 1 或其更新过程中的中间状态来进行计算。 这一步使得全连接层从 " 静态映射 " 变成了 " 正在运行的在线压缩算法 “。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对于 Attention 层： 前向计算： 求解高频优化问题 （方程： min 〈 “M丛叻）。 因为它的状态 （KV Cache) 在推理时实时更新。 对于标准 MLP 层 （静态）： 前向计算 = 应用低频优化问题的解 （方程： min E Cg10bal)。 它在推理时是冻结的， 代表了预训练阶段 “ 外层循环 " 的优化结果。 对于 HOPE MLP 层 （动态）： 前向计算： 求解中频优化问题 （方程： min 目@忆 “ 引 W 一可。 它通过在线梯度下降实时修改自己， 以压缩当前的上下文块

### （三）优化器选择

尽管如前面所述，我们可以训练一个神经网络作为优化器，但在 HOPE 架构中，每一层 MLP（即连续体记忆系统中的每一块）使用的是一种基于L2回归目标的改进型梯度下降优化器（也就是直接取了这个优化目标下的解析解，就不必用逼近法浪费算力了）：

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

T) 一 T/t+1VWtC(Wt;） 展开形式：

展开形式：

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

T) 一 T/t+1VWtC(Wt;） 展开形式：

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

min IIWct 一 £ （； 目 2 这里的目的是寻找一个， 使其对当前输入的预测输出， 尽可能接近当前的 " 局部惊奇信号 " （即梯度 VytC)。 公式结构解析： 衰减 / 去相关项 Wt(I 一的： 这一项源于 2 目标中对模长的惩罚。 它的作用是清除彬， 中与当前输入方向一致的旧分量 （即投影到零空间）， 从而防止由于数据相关性导致的权重累积或爆炸。 学习 / 写入项一 + IV £ （彬）： 这是标准的梯度下降项， 负责将新的误差信号 （惊奇信号） 写入权重中。

该公式是通过求解以下优化问题的单步梯度下降推导而来的：

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

min IIWct 一 £ （； 目 2 这里的目的是寻找一个， 使其对当前输入的预测输出， 尽可能接近当前的 " 局部惊奇信号 " （即梯度 VytC)。 公式结构解析： 衰减 / 去相关项 Wt(I 一的： 这一项源于 2 目标中对模长的惩罚。 它的作用是清除彬， 中与当前输入方向一致的旧分量 （即投影到零空间）， 从而防止由于数据相关性导致的权重累积或爆炸。 学习 / 写入项一 + IV £ （彬）： 这是标准的梯度下降项， 负责将新的误差信号 （惊奇信号） 写入权重中。

1.为什么用基于L2回归目标的优化器，不用向量点积相似度？

在向量检索（Retrieval）或静态特征比对中，我们通常更喜欢点积或余弦相似度；HOPE架构的优化场景（即在线更新参数）中，逻辑却不同，正是为了抑制“模长噪声”和“冗余信号”，才需要切换回L2回归。

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

检索场景 (Retrieval)： 我们在寻找相似的向量。 如果向量模长很大 （噪声）， 它可能会在欧氏距离上远离目标， 但在角度上很近。 所以我们用 Cosine/Dot Product 忽略模长。 优化场景 (Optimization, HOPE 的场景）： 我们在修改矩阵 WO · 在这里， “ 噪声 " 通常指的是数据流中的冗余相关性 (Data CorreIation)0 例如， 如果输入序列是连续的、 高度相似的 Token （如视频帧或重复文本 ), 单纯的点积更新会导致 W 在同一个方向上无限累加。

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

检索场景 (Retrieval)： 我们在寻找相似的向量。 如果向量模长很大 （噪声）， 它可能会在欧氏距离上远离目标， 但在角度上很近。 所以我们用 Cosine/Dot Product 忽略模长。 优化场景 (Optimization, HOPE 的场景）： 我们在修改矩阵 WO · 在这里， “ 噪声 " 通常指的是数据流中的冗余相关性 (Data CorreIation)0 例如， 如果输入序列是连续的、 高度相似的 Token （如视频帧或重复文本 ), 单纯的点积更新会导致 W 在同一个方向上无限累加。

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在在线学习中， 点积相似度目标 min 〈 VVc, 一实际上等价于最大化 〈。 问题： 只要彬的模长无限增大， 点积就会无限增大。 后果： 优化器没有 “ 停止条件 "。 只要和方向一致， W 就会拼命增长。 如果输入数据含有噪声 （模长忽大忽小 ), 或者数据高度相关 （连续输入类似的）， W 的权重值就会 * * 爆炸 (Explode) * * 或被高频出现的特征主导。 这就是最大的噪声： 权重的模长失控了。

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

HOPE 采用力 2 回归目标 min llWc 一腭， 实际上引入了两个关键机制来对抗噪声： A. 能量约束 （作为刹车系统） 力 2 目标不仅要求方向一致， 还要求模长匹配。 目标是让输出 VVc 这里的响应强度， 精确匹配惊奇信号的强度。 一旦 VVc 足够接近损失就接近 0， 更新就会停止。 · 抗噪原理： 即使输入 £ 持续重复 （高相关性噪声）， 模型也不会在同一个方向上反复更新， 因为它已经 " 学会 " 了 （彬 £）， 误差为 0。 这就防止了重复数据带来的过拟合。 B. 投影去相关 （作为滤波器） 这是最核心的数学魔法。 正如之前推导的， 力 2 目标导出了解析解中的衰减项： VVI 衅 1 = 彬，@ 一的一 77V £ @ 一 》） 的作用： 它是一个正交投影算子。 去噪过程： 它强制模型在更新之前， 先从权重中减去所有已经能被当前输入解释的部分。

拓展：在工程实践中，为什么这种权重模长失控的情况很少出现？

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

论文之所以强调这个问题， 是因为它针对的是在线 (Online)、 连续 (Continual) 且数据高度相关的场景， 而不是传统的离线 (Offline)、 批次化 (Batched) 训练场景。 以下是为什么在现有实践中 W 没有爆炸， 以及论文为何依然认为这是一个理论痛点的详细解释：

A.数据的独立性假设与现实的不符

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 数据的 “ 独立性 " 假设 vs · 现实目前的实际应用 (Offline Training): 我们在训练模型时， 通常会使用 Shuffle （打乱数据） 和 Batching （批处理）。 Shuffle 的作用： 它打破了数据的时间相关性。 和 + 1 在一个 Batch 里可能来自完全不同的文档。 因此， 梯度方向是随机的、 相互抵消的， 不会一直在同一个方向上累加。 结果： 满足了 I.I.D. （独立同分布） 假设， 简单的梯度下降也能工作得很好。 论文关注的场景 (OnIine/Nested Learning)： 论文关注的是 ln-context Learning 或 HOPE 架构的在线更新。 场景： 模型正在读一本书。 是 “ 哈利 "， + 1 是 ' 波特 “。 数据是高度相关的， 无法打乱。 后果： 如果在这里使用纯粹的梯度下降 （点积相似度 ), 因为 + 1， 梯度会指向同一个方向， 导致局部权重迅速过大。 论文的解决方案： 引入 @ 一 ccT) 这一项， 是为了在无法打乱数据的在线流式学习中， 手动去除数据的相关性。

B.现代优化的修改

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

权重衰减 (Weight Decay / L2 ReguIarization)： 我们在使用 AdamW 或 SGD 时， 参数。 置 weight_decay = 0 “ + /\IIW112 total 通常会设这在梯度中引入了 —2AW 项。 这实际上就是一个人为添加的 " 刹车 “， 强行把彬的模长压下来。 · 论文的观点： 论文通过力 2 回归目标推导出的公式 （28）， 是想说： 如果我们用正确的数学目标， 这种 “ 衰减 " 是自动出现的 （且是各向异性的 ), 而不需要人为去调 weight-decay 超自适应学习率 (Adam)： Adam 优化器会除以梯度的二阶矩 （方差） 厉。 如果梯度一直在同一个方向累积导致数值变大， 厉也会变大， 从而自动降低学习率。 这本身就是一种防止数值爆炸的机制。 · 论文的观点： 论文在 2 ． 3 节甚至证明了 Adam 本质上就是一种最佳的联想记忆模块。

2.为什么Linear Attention中，我们推导出解析解就是V*K^T，这里却不能用？

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们要解线性方程 MK =。 解析解 (The AnaIytical Truth)： 数学告诉我们要用伪逆： M* = 忄 = VKT (KKT)-。 注意这里有一个关键的项： (KKT) 一， 即协方差矩阵的逆。 它负责去相关 (De- correlation)o 你的假设 (The Hebbian/Attention Assumption)： 如果 K 是正交的 (Orthogonal)， 那么 KKT = （单位矩阵）。 此时， 解析解确实简化为： M* = T。 一了宀、 八这就是 Linear Attention 的做法： 它直接计算 E 省。 匕假了所有的都是独立的， 不需要计算那个昂贵的逆矩阵 (KKT) 一这也是为什么论文称其为 “ 点积相似度 " 优化一一它只最大化相似性， 不处理冗余。 现实情况 (The DeIta RuIe/HOPE Assumption)： 现实中的 Key (Token 的 Embedding) 通常高度相关， KKT 10 此时， 直接用 VKT 得到的模型是次优的， 因为它会重复计算相似特征， 导致 “ 过拟合 " 或方向偏差。 这就是 Delta Rule 的做法： AM = 一 M 左这个规则通过减去预测值 Mk， 实际上是在迭代地逼近那个协方差矩阵的逆。 它在更新时会自动 " 抵消 " 掉已经学过的方向， 从而正确处理数据相关性。

但正如前面所述，在连续输入的Token流中，各向量高度相关，直接用V*K^T得到的模型并非最优，因为它会重复计算相似特征，导致过拟合或方向偏差：

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们要解线性方程 MK =。 解析解 (The AnaIytical Truth)： 数学告诉我们要用伪逆： M* = 忄 = VKT (KKT)-。 注意这里有一个关键的项： (KKT) 一， 即协方差矩阵的逆。 它负责去相关 (De- correlation)o 你的假设 (The Hebbian/Attention Assumption)： 如果 K 是正交的 (Orthogonal)， 那么 KKT = （单位矩阵）。 此时， 解析解确实简化为： M* = T。 一了宀、 八这就是 Linear Attention 的做法： 它直接计算 E 省。 匕假了所有的都是独立的， 不需要计算那个昂贵的逆矩阵 (KKT) 一这也是为什么论文称其为 “ 点积相似度 " 优化一一它只最大化相似性， 不处理冗余。 现实情况 (The DeIta RuIe/HOPE Assumption)： 现实中的 Key (Token 的 Embedding) 通常高度相关， KKT 10 此时， 直接用 VKT 得到的模型是次优的， 因为它会重复计算相似特征， 导致 “ 过拟合 " 或方向偏差。 这就是 Delta Rule 的做法： AM = 一 M 左这个规则通过减去预测值 Mk， 实际上是在迭代地逼近那个协方差矩阵的逆。 它在更新时会自动 " 抵消 " 掉已经学过的方向， 从而正确处理数据相关性。

> [图片 31：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

A. 解释现状： Linear Attention 为什么是 Hebbian? 论文首先指出， 现有的 Linear Attention 架构 （如 Equation 13） 的更新规则 Mt = 1 +， 在数学上等价于优化 “ 点积相似度 " 这说明现有的线性 Attention 本质上是一个 * * " 懒惰 “ 的回归器 * * 在上， 而不去检查左是否已经被记住了。 B. 提出改进： HOPE 为什么要用 Delta Rule? 它假设数据正交， 直接把贴论文紧接着指出， 这种简单的 Hebbian 更新会导致记忆效率低下。 为了设计更强的模型 (HOPE), 作者引入了为 2 回归目标 (MSE Loss) 由此推导出的 Delta Rule （公式 28 中包含的 I 一 T 项）， 正是为了解决你提到的问题： 在迭代过程中实现去相关。 公式 （28） 衅 1 = Wt(I 一 ccT) + · · · 实际上就是在做一个在线的、 增量的矩阵求逆近似， 逼近你心中那个完美的解析解忄。

> [图片 32：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

算法 1 (Attention/Hebbian): 赌数据是正交的。 直接累加 VKTO 计算快， 但处理相关数据能力弱 （这也是为什么标准 Attention 需要 Softmax 来强制 " 稀疏化 / 伪正交化 “）。 算法 2 (HOPE/DeIta RuIe): 承认数据是相关的。 使用误差修正 （一 (K)KT0 这在数学上等价于逐步计算 (KKT)-I, 最终收敛到真正的最小二乘解。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- MLP 训练是在低频（整个预训练数据集上、只训练一次）求解一个优化问题，将“输入x”映射到“梯度信号u”；Attention 推理是在高频（当前上下文窗口内、每次输入token时）求解一个优化问题，将“Key”线性回归映射到“Value”。也就是说，MLP的“训练”和 Attention 的“推理”，在数学形式上是完全一样的（都是在做梯度下降），区别仅仅在于它们的更新频率和数据来源不同。
- 结合上面所述，我们参考大脑中不同频率的电波，构造以下HOPE架构：

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
