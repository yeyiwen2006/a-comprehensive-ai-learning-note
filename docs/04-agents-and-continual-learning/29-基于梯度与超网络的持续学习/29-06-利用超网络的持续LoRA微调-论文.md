---
title: "29.6 利用超网络的持续LoRA微调（论文）"
source_docx: "第4部分 大模型智能体与持续学习/29.基于梯度与超网络的持续学习/29.6 利用超网络的持续LoRA微调（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 29.6 利用超网络的持续LoRA微调（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、核心思想

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

目前， 为了让预训练的 LLM 适应特定的下游任务或新知识， 业界通常采用两种范式： 提示词工程 (Prompt Engineering) 和参数微调 （如 S FT 或 LoRA)。 但这两种方法均存在明显局限： · 提示词工程： 将大量上下文塞入 Prompt 中会消耗宝贵的上下文窗口容量， 并显著增加推理延迟。 参数微调： 训练开销庞大、 对超参数敏感， 且需要为每一个特定任务保存一套独立的模型权重， 造成巨大的存储负担。 " 超网络 （Hypernetwork） " 作为一种生成另一个网络权重的特殊网络， 为解决这一问题提供了理想方案： 它有望通过一次前向计算直接生成任务所需的适配器参数。 然而， LLM 参数空间极大， 过去的超网络受限于架构瓶颈 （例如仅复用小型多层感知机 MLP)， 表达能力非常有限， 始终无法成为 LLM 适配的实用且可扩展的解决方案。

具体来说，超网络的输入为模型输入，输出为LoRA模块的参数，即模型参数需要变化的量。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

SHINE 的核心突破在于： 仅需单次前向传播， 即可将复杂的、 多样化的自然语言上下文， 直接映射转化为高质量的 LLM LoRA 参数， 全程无需任何基于梯度的微调训练。 通过这种方法， 输入文本中的知识被瞬间 " 内化 " 到模型参数中， 大模型随后即可基于这些注入的参数进行多轮问答或推理， 无需在推理时再读取冗长的原始上下文。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

复用 LLM 作为上下文编码器： SHINE 没有从零开始训练一个庞大的外部编码器， 而是直接内联 (ln-context) 使用了预训练 LLM 自身的参数来压缩文本。 这意味着超网络直接继承了 LLM 的语言理解能力， 免去了海量的基础训练开销。

## 二、模型架构与工作流

### （一）记忆提取

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 输入拼接： 给定长度为 N 的上下文 Token 嵌入 X， 模型在其尾部追加一段长度为 M 的可学习初始记忆嵌入序列 Moo 其拼接后的输入序列可表示为： （： H 为 LLM 的 0 层维

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这个亻 0 是一组可学习的参数 (Learnable Memory Embeddings) 在超网络的 “ 元训练 (Meta-training)" 阶段， 与 M2P Transformer 以及 Meta Lo RA 的参数一起被梯度下降优化。 当训练完成进入推理阶段时， Mo 的数值就已经被完全固定下来了。 它就像是一种特殊的 “ 结构化 Prompt", 负责作为信息载体去吸收前文 X 的语义。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 ． 3 ． 4 ． 前向传播与提取： LLM 处理上述拼接序列后， 我们提取每一层 （第犭层） 输出的最后 M 个 Token 的隐藏状态作为该层的记忆状态为： 构建全局记忆张量： 将所有层的 Mt 堆叠起来， 形成一个全局记忆张量 M: M = stack( [MI， …， ML]) e 无瓶颈约束： 为了保证生成的参数有足够的信息容量， 记忆序列的长度 M 必须满足能够容纳生成的 LoRA 权重。 设生成的 Lo RA 秩为单层所有线性层的输入输出维度之和为刀， 则强制约束： rD 这样就保证了记忆状态的尺寸大于或等于 LoRA 参数的尺寸， 从根本上消除了信息瓶颈。

### （二）M2P Transformer

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

拿到全局记忆张量 M 后， 需要将其转化为目标 LoRA 参数。 如果直接把 M 展平为一个长度为 LM 的序列并使用标准 Transformer 处理， 自注意力计算的复杂度将高达 0 （M） 2）， 这在计算上是极其昂贵的。 因此， SHINE 引入了一个轻量级的稀疏注意力 M2P Transformero 具体原理与工作流： 1 ． 2 ． 位置编码 (Positional Encoding)： 为了让网络感知记忆状态所在的层级和 Token 顺序结构， 模型引入了可学习的层级位置嵌入尸 “ 和 Token 位置嵌入 P to 曰 = + player + P to e 稀疏交替自注意力 (Sparse AIternating Attention): M2P Transformer 交替执行列注意力 （层间） 和行注意力 (Token 间 ), 将时间复杂度大幅降至 0 M2 + M 2）。 奇数层 （列注意力 / 跨层混合）： 在不同层的相同位置 Token 之间进行双向自注意力计丫 = SelfAttn(Z 》）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 ． 这一步的理论意义极度关键： 它允许信息在层与层之间双向流动， 让深层的特征能够传递回浅层。 这在直觉上模仿了反向传播的过程 （浅层梯度的更新依赖于深层的梯度 ), 打破了传统基于隐藏状态逐层生成参数的 “ 盲目性 "。 偶数层 （行注意力 / 跨 Token 混合）： 在同一层的不同 Token 之间进行计算。 Y 《 = SeIfAttn(Z(.i.)） 非线性更新： 每次注意力计算后， 都使用一个两层的 MLP 对特征进行更新。 此外， 这里特意采用了 Post-Layernorm （后置层归一化） 而非目前 LLM 常用的 Pre-Layernorm, 因为记忆特征与最终的 LoRA 权重分布存在巨大鸿沟， Post-Layernorm 能让训练更稳定且收敛更快。

### （三）利用Transformer的输出得到LoRA参数

把前面Transformer的输出按它所在的层重新分配，然后对每一层分别按如下步骤重新用LoRA：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 展平为一维向量 · 首先， 将第层对应的记忆切片 M[i,：，： ] （形状为 M × 丑） 强行展平， 变成一个非常长的一维向量 e “。 你可以把想象成一条装满了几万个浮点数的长纸

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 ． 3 ． 4 ． 5 ． 初始化指针： 设定一个游标指针乯， 初始值为 0。 它代表 " 我们当前在这条纸带上用到了哪个位置 "。 分配矩阵： 假设我们现在要为该层的第一个模块 （比如 Query 投影层） 生成输入维度为、 秩为 r 的 LoRA 矩阵。 这个矩阵需要 · 个参数。 · 因此， 算法从的第个位置开始， 一直往后截取 0 r 个元素， 即 v[t： + · r]。 然后将这段一维数组变形 (Reshape) 成二维矩阵 e × 7 ．。 分配矩阵 B： 接着为该模块生成秩为 r、 输出维度为 0 的 LoRA 矩阵 BO 这个矩阵需要 r · 0 个参数。 算法紧接着上一次截取结束的位置 （也就是 + · r), 继续往后截取 · 0 个元素， 即 · 将这段一维数组变形为二维矩阵 B e ． × 0 更新指针： 生成完这一对 A 和 B 后， 前 · + r · 0 个数值就已经被用掉了。 此时更新指针， 把乯往前推： 乯艹 t + / · r + r · 0

这样，此后模型前向传播时用的就是新权重了。

### （四）LoRA是否跨会话保留？

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

不保留。 SHINE 的核心理念是 " 即插即用 "。 它的工作流是针对给定的上下文 ℃ ontext), 通过单次前向传播立刻生成一套对应的 LoRA 权重。 记忆状态 Mt 是 LLM 结合当前输入的文本和固定的 Mo 动态计算出来的隐状态。 如果开启了一个全新的会话或输入了全新的上下文， 模型会根据新的文本重新进行单次前向传播， 生成全新的记忆状态和对应的 LoRA 参数， 旧的记忆状态会被直接丢弃。 1

## 三、训练方法

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

阶段一： 预训练 (pretraining) 模型在无监督语料上进行自监督学习， 包含两个主要任务： 重建任务 (Reconstruction 丿： 让超网络把完整文本纟扁码进 Lo RA， 然后要求 LLM 仅依靠该 LoRA 一字不差地重建出原始上下文。 补全任务 (Completion 丿： 隐藏文本最后 ℃ ％ 到 30 ％ 的 Token， 要求生成的 LoRA 具备推理缺失信息的能力， 从而让 LLM 补全残缺部分。 阶段二： 指令微调 (lnstruction Fine-Tuning) 利用构建的上下文一问答对数据集 （如 MS MARCO MQA 等 ), 计算预测答案 Token 的交叉熵损失。 这一阶段旨在赋予生成的 LoRA 理解、 提炼复杂上下文并将其用于精准问答的能力。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
