---
title: "29.2 部分权重就地更新的TTT（论文）"
source_docx: "第4部分 大模型智能体与持续学习/29.基于梯度与超网络的持续学习/29.2 部分权重就地更新的TTT（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 29.2 部分权重就地更新的TTT（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、TTT-E2E

### （一）核心思想

对于长上下文的处理，在实际生产中，重要的不是一字不落地记住所有细节，而是对上下文建构理解，并将其内化。TTT-End to End（TTT-E2E）提出可以在长上下文处理中，通过在推理时不断用Next token prediction实现持续学习；在训练时，也把目标替换为“进行若干步更新后表现最好”的元学习目标，而不是“冻结参数时开箱即用的表现”，以防止参数漂移、对异常片段的过拟合等。

### （二）模型架构

在推理时，冻结嵌入层（Embedding）、归一化层和注意力层，只更新多层感知机（MLP）的权重，以避免外层循环的梯度不稳定。具体来说，只更新网络最后1/4的Transformer block中的第一个MLP层（为了防止TTT过程遗忘预训练知识，在被更新的block中，额外添加一个静态的第二MLP层作为预训练知识的“安全存储区”；前3/4的Transformer block起表征提取作用，故不动）。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 测试时优化： 内层循环 (lnner Loop) 在测试阶段， 模型接收到上下文后， 并非直接一次性输出， 而是先利用这些上下文进行自我更新。 基础架构： 模型基于标准的 Transformer, 但将全注意力替换为滑动窗口注意力 (Sliding- Window Attention, SWA), 窗口大小设为 = 8K。 + 1 Mini-Batch 更新规则： 为了提高并行性和稳定性， TTT 采用小批量 (Mini-batch) 梯度下一 1） 降， 批量大小设为 b = IK （必须满足 > b 以保留局部记忆）。 对于第个 mini-batch, 模型权重的更新公式为： + 1 其中 lt 是标准交叉熵损失函数 (W) 1 t=(i—l)b+l 为学习率。 + 2

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 训练时元学习： 外层循环 (Outer LOOP) 如果模型在预训练时没有为 " 测试时更新权重 " 做好准备， 直接进行 TTT 效果会很差。 因此， 在预训练阶段 （训练时 ), 作者使用元学习 (Meta-learning) 来寻找最优的初始权重 0。 + 2 · 端到端损失函数： 训练时的目标函数直接等于测试时经过 TTT 更新后的平均损失： T/b 犭 = 1 t=(i—l)b+l 梯度反传： 外层循环通过对上述损失求 Wo 的梯度 （即梯度的梯度 VC(Wo)) 来优化初始权重， 使得模型天生就具备 " 在测试时快速学习 " 的能力。

### （三）算法工作流

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 前向传播 (Forward pass)： 接收给定的上下文 to ke n。 亻苎号沿着向上的箭头逐层传递。 首先经过底部的 Embedding 层， 然后穿过前 3L / 4 个完全冻结的 Transformer 块 （仅包含滑动窗口注意力和 MLP) 顶层预测： 信号继续穿过顶部的 L/4 个 Transformer 块。 在这部分块中， 除了常规网络， 还包含一个专门用于 TTT 更新的额外 MLP 层。 最终在网络末端输出对下一个词的预测概率分布。 全局损失计算 (Global Loss)： 当系统接收到真实的下一个上下文 token 昀时， 在网络的绝对顶端计算标准的交叉熵损失 12。 误差反向传播 (Backward pass)： 图中的向下长箭头代表梯度的反向传播。 关键设计在于， 梯度仅穿过最后的 L/4 个网络块， 并在向下传递到 3L / 4 的边界处被截断。 状态更新 (State Update)： 利用截断的梯度， 使用梯度下降 （图中以在线更新 b = 1 为例） 更新这顶层 L/4 个块中的目标 MLP 层 （图中更新后的 MLP 变为蓝色） 循环与未来预测： 现在， 将昀作为新的输入向上前向传播。 当它经过蓝色的 MLP 时， 使用的是已经吸收了信息的新权重， 最终预测出未知的 （即力 3）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

TTT-E2E 优化的是标准的语言建模交叉熵损失： 彬） = CE(f@t-1; 彬）， 司

对比以Titans为代表的“K-V记忆”类Test time training方法：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 逐层前向流动： 输入经过 Embedding 后， 进入总共力个 Transformer 块的第 I 块， 得到局部输入南。 键值投影 (Key-Value projection)： 在块内部， 输入分别通过外层循环参数 OK 和厂进行线性投影， 生成当前的 Key 和 value。 局部代理预测与损失计算： 该层内的 MLP 尝试根据 Key （即 0） 来预测 value, 得到预测值。 随后， 将其与真实的 va | ue （即南） 对比， 计算出局部的重建损失 11 局部状态更新： 图中的短向下箭头表明， 梯度不会跨层流动。 误差仅在当前层内部反向传播， 更新该层内部特殊 MLP 的权重 （变为蓝色） 生成该层输出并传递： 当输入昀到达该层得到时， 使用投影后， 送入更新后的蓝色 MLP, 得到该层的最终输出， 传递给更上一层。 顶层预测： 经过所有层的局部更新与前向传递后， 在顶层通过 Softmax 预测 3。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

TTT-KVB 优化的通常是逐层独立的均方误差 (MSE) 损失：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

(a) Our main method (b) Prior work i.e ” TIT-KVB simplified MLP Softmax Embedding MLP MLP Softmax Embedding MLP Sliding wi ndow MLP SIiding window Embedding 1 1 4 S liding window MLP 3L 4 S liding window Embedding MLP Lx SIiding window Embedding Softmax Embedding M LP MLP SIiding window Embedding

### （四）如何避免在显存中实例化二阶Hessian矩阵？

事实上，元学习的表达式中确实出现了二阶Hessian矩阵：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

外环 （元参数更新）： MAML 的精髓在于， 它的元目标 (Meta-objective) 不是优化初始参数 O 在原始任务上的表现， 而是优化经过内环更新后的参数伊在该任务上的表现。 这实际上是在优化 " 适应能力 "。 外环的总体目标函数为所有采样任务更新后表现的期望： 方。忉@ 一乥方@） 一乥方 + av 河丆@）） 丆、 丆） 为了最大化元目标， 我们需要对初始参数 O 求导 （元梯度学习率为）： 0 0 + 0 乥方@） 应用链式法则， 这里的梯度 V 方@） 展开后将包含二阶导数 (Hessian 矩阵 ), 因为研本身就包含 0 的梯度： 这就是基于梯度的 Meta-RL 能有效学习的关键所在： 通过二阶优化计算， 外环强迫参数 0 走向那些使得内环梯度更新最有效率的空间。

但它是以和向量乘积的形式存在的，简化为考虑Hessain矩阵H和向量v乘积计算的问题：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

计算一阶梯度： 首先， 对损失函数 £ 进行一次常规的反向传播， 得到一阶梯度向量 g: g = voC(0) 此时， 显存开仅为 O(N), 存储梯度向量的大。 构造标量点积： 将梯度向量 g 与目标向量进行点积计算。 因为 v 是一个与 O 无关的常数向量， 这一步会得到一个纯标量 S： 此显存开为 0 （1）。 分求导法则： V9S S = gT 对标量求一阶导数 （梯度的梯度）： 接下来， 我们对标量 S 再次关于参数 O 求梯度。 根据微积 = V9(gTv) = （v 四） 与 = Hv 这一步实上是让框架在一次反向传播建立的计算图上， 再进行一次反向传播 (Reverse- over-reverse autodiff 丿。 显存开依然严格保持在 0 （N）。

这样的计算总开销约等于两次独立的前向+反向传播。

### （五）核心计算开销

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

虽然我们在单个时间步上避免了 0 （N2） 的显存爆炸， 但在 TTT- E 2 E 架构中， 系统必须在长达数十万的 Token 序列上展开计算图， 这就引爆了另一个维度的灾难。 1 · 随时间反向传播 (BPTT) 的状态膨胀在传统的 RNN 或全注意力 Transformer 中， 计算图随时间展开时， 层间传递的 " 隐藏状态 (Hidden State) " 通常是一个小维度的特征向量 （如维度为刀 1024）。 但在 TTT- E 2 E 的内层循环中， 模型是将自身的参数矩阵 W 作为随着时间演进的 " 隐藏状态 " 来进行持续学习的。 其内层更新规则构成了时间上的状态转移方程： VVZ = 彬 ; 一 1 一 V， 一 1） 外层循环要计算 VwoC， 必须通过链式法则穿过所有时间步 (mini-batch) 进行反向传播： DC DWo DWZ- DWJ-

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

内存灾难： 假设内层更新的 Mini-batch 大小为 b = IK, 那么在处理 T = 128K 的上下文时， 模型实际上要在内层循环中连续走 128 步梯度下降。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

虽然我们在单个时间步上避免了 0 （N2） 的显存爆炸， 但在 TTT- E 2 E 架构中， 系统必须在长达数十万的 Token 序列上展开计算图， 这就引爆了另一个维度的灾难。 1 · 随时间反向传播 (BPTT) 的状态膨胀在传统的 RNN 或全注意力 Transformer 中， 计算图随时间展开时， 层间传递的 " 隐藏状态 (Hidden State) " 通常是一个小维度的特征向量 （如维度为刀 1024）。 但在 TTT- E 2 E 的内层循环中， 模型是将自身的参数矩阵 W 作为随着时间演进的 " 隐藏状态 " 来进行持续学习的。 其内层更新规则构成了时间上的状态转移方程： VVZ = 彬 ; 一 1 一 V， 一 1） 外层循环要计算 VwoC， 必须通过链式法则穿过所有时间步 (mini-batch) 进行反向传播： DC DWo DWZ- DWJ-

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 致命的显存耗尽 （00M） 根据深度学习框架的默认机制， 为了在反向传播时能计算上述偏导数， 前向传播时必须在显存中保留所有中间时刻的激活状态。 在 TTT 中， 这就意味着框架试图把整个上下文时间步里更新过的无数个巨大的权重矩阵彬 1， W2， ·， 彬 h/b 全部塞进显存。 这会立刻导致显存溢出。 3 · “ 用时间换空间 " 的工程妥协 （梯度检查点） 为了解决显存耗尽的问题， 论文不得不采用 * * 沿时间轴的梯度检查点 (Gradient Checkpointing through time) * * 技术。 该技术的本质是： 在前向传播时不保存大部分中间的， 而只保存极少数的关键节点 ℃ heckpoints)o 当反向传播经过某个时间步发现缺少 VVt 时， 系统会从最近的 Checkpoint 重新运行前向传播和内部的梯度下降来临时算出一个。 这就导致了巨大的时间代价。 正如论文所揭示的， 随着上下文长度 T 的增加 （例如从 8K 扩展到 128KL 为了将显存控制在硬件允许的范围内， 必须将随时间的梯度检查点数量增加一个 log(T) 的因子。 这种反复抛弃又反复重算的机制， 使得计算时间呈非线性增长， 最终导致训练延迟成为 TTT-E2E 目前最手的瓶颈。

### （六）关于MoE

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

正如我们前面讨论的， TTT-E2E 能高效运行的核心在于它把 IK （℃ 24） 个 Token 打包成一个 Mini- Batch, 利用静态 MLP 计算统一的梯度 V 彬） 并一次性更新。 如果换成 MoE， 这 ℃ 24 个 Token 会被路由网络 (Router) 分发给不同的专家 （例如 Token 1 去专家 A， Token 2 去专家 B)。 这意味着在反向传播求导时： 专家 A 可能只收到了 150 个 Token 的梯度。 专家 B 可能收到了 300 个 Token 的梯度。 底层 CUDA 算子无法对这些极其不规则的张量进行 coalesced memory access （合并内存访问）。 在推理阶段执行这种高度动态分布的梯度累加和权重更新， 会直接导致 GPU 并行计算单元大量闲置， 让推理延迟飙升至不可用的地步。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

MoE 的核心是门控 (Gating) 和路由网络， 通常使用 Top-K 选择， 这是一个 * * 不可导 (Non- differentiable) * * 或包含严重梯度噪声的操作。 如果要在训练期让梯度穿过 TTT 的更新步骤， 再穿过 MoE 的硬路由机制， 其二阶导数的计算在数学上会极其不稳定， 极易导致梯度爆炸或消失， 模型根本无法收敛。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

然而， 如果我们在架构设计上进行妥协与解耦， TTT-E2E 和 MoE 完全可以强强联手。 在 TTT- E 2 E 的官方实现中， 为了保证速度， 网络前旦的层是被 * * 完全冻结 (Frozen) * * 的， 只有最后的层参与 TTT 梯度更新。 因此， 最优的结合方案是： · 前的冻结层使用 MoE 架构： 在这里发挥 MoE 的特长一一用极低的推理算力调用海量参数， 提取极其丰富的世界知识和高级语义表征。 由于这些层在推理期不需要计算梯度和更新参数， MoE 的路由机制完美适用， 不会带来任何额外的 Test-Time Training 负担。 后的 TTT 层使用稠密 (Dense) 标准 M L P： 在这里发挥 TTT- E 2 E 的特长一一利用全量静态架构接收前方 MoE 传递过来的高级特征， 进行规整的 Mini-Batch 反向传播， 将当前超长文本的具体细节 （如几十万字小说的特定情节） 高效地压缩进这块巨大的连续权重中。

### （七）关于推理和Agent能力

Next-token prediction本质上是一种无监督的行为克隆。如果在测试时，强行让模型去拟合一段带有噪声、逻辑跳跃或次优决策的外部Context，确实极易引发“灾难性遗忘”，破坏掉前期通过PPO、GRPO等强化学习算法精调出的长链条推理和任务执行策略。

一种方法时让模型在测试时不去拟合外部给定的 Prompt，而是先利用现有的RL能力生成（或基于其他方法搜索出）高质量的推理步骤，然后结合环境反馈或验证对其经过筛选，利用经过筛选和重述的自身高质量数据来进行对前文所述的MLP模块的TTT更新，这就更类似于后面会提到的自蒸馏方法。

## 二、In-Place TTT

### （一）现有测试时训练（TTT）方法的问题

1.架构不兼容：现有TTT方法往往需要设计全新的独立层来替代自注意力机制，这意味着必须从头开始进行高昂的预训练，无法直接利用现有的模型。

2.计算效率低下：传统的TTT需要逐个Token顺序更新状态，严重限制了现代GPU/TPU的并行计算吞吐量。

3.目标不契合：以往TTT的自监督目标通常是简单的“重建当前词”，这与自回归语言模型“预测下一个词”的终极目标不一致。

### （二）核心架构

为解决上述问题，字节Seed提出了In-Place Test-Time Training框架。该框架不引入全新的网络层，而是直接将Transformer架构中普遍存在的MLP块中的最后一个投影矩阵W_down征用为“快速权重”。这种“即插即用”的设计无需修改原始架构，保留了预训练权重的完整性，仅需极低的成本进行持续微调即可生效。

由于该方法作用于MLP层并与注意力机制互补，它允许以较大的分块为单位进行并行更新，极大地提升了在现代硬件上的吞吐量。

### （三）损失函数

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

作者通过以下公式构造了一个带有 “ 未来信息 " 的目标值： = Conv1D(Xo)W target

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

天 0： 这是输入 Token 最原始的词嵌入向量 (Token Embedding) Conv1D(•) （一维卷积）： 这是引入 “ 未来信息 " 的核心魔法。 通过控制卷积核 (Kernel) 的权重， 它可以像一个滑动窗口一样把后面的词拉取过来。 例如， 如果把卷积核在 “ 下一个词 " 的位置设为 1， 其他位置设为 0， 这个操作就能精准地把下一个 Token 的特征提取出来。 “@ （投影矩阵）： 这是一个可训练的矩阵， 负责将卷积提取到的未来特征映射到合适的维度空间中。

简单来说，我们希望神经网络最后一层的输出接近这个V值。神经网络最后一层的输出是上一层激活值Z经过权重W_down后得到的。这里预测的词可以是上下文中的任何词，无论由用户、环境输入还是模型生成。

计算损失函数的过程如下：

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

当模型处理第个数据块时： 1 ． 2 ． 3 ． 计算当前输出： 模型使用当前的快速权重 w(i) 来处理当前的激活值得到一个输出结 down 果 O[t 0 = z （巛） T 冂 [ 引生成对齐目标： 模型利用 Wtarget 计算出包含未来信息的标准答案 ]。 计算损失并更新： 论文定义损失函数为两者之间的负内积： 最小化这个损失函数， 等价于最大化 （彬 “） T 与 ] 之间的相似度 （内积）。 换句话说， 模型在强制修改 Wdown 内部的参数， 使得下一次同样的激活值 Z 进来时， 经过 VV( 投影出来的结果， 能够与包含未来预测信息的高度对齐。

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

正因为选用了最简单的内积作为损失函数， 它的梯度推导极其漂亮和简洁。 对快速权重的更新步骤直接变成了优雅的矩阵相乘和相加： down

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

问题显然存在： 随着处理的 Token 越来越多 （尤其是对于 128k 甚至更长的上下文 ), 这种无衰减的累加会导致快速权重 Wdown 的数值呈线性甚至指数级增长 (Unbounded growth)， 最终在推理时引发数值溢出或严重破坏模型原有的表征分布。 论文的解决方案： 强制梯度截断 (Clipping Mechanism) 为了防止状态爆炸， 作者在实际的推理代码中打了一个非常暴力的 “ 工程补丁 "。 在将更新量 AW(i) 加到权重上之前， 系统会计算其 Frobenius 范数目 A “ “ 目丆。 如果该范数超过了预设的阈值丆， 则会对其进行强制缩放： AWdown AW(i) 丆 · dOWn 目 A 明监目 F 在他们 4B 模型的实验中， 这个极其严格的阈值被设定为丆 = le 一 5。 通过这种极小阈值的强行截断， 论文在维持了内积计算的高吞吐量的同时， 人为压制了数值爆炸的风险， 以此替代了真正数学意义上 Delta Rule 的自稳定机制。

拓展：为什么不用交叉熵？

1.Softmax的计算开销

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

交叉熵的计算路径： 如果使用交叉熵， 模型必须把当前 dmodel （如 ℃ 24 或 2048 维） 的隐藏状态， 通过一个巨大的语言模型头 ()M Head)， 投影到整个词表空间 (Vocabulary Size, 本文中 500M 和 1 ． 5B 模型的词表大小为 32， 000）。 然后执行 Softmax, 计算概率分布， 求出误差， 再反向传播穿过 LM Head 回到 MLP 层。 TTT 的现实环境： TTT 是在 * * 推理阶段 (lnference Time) * * 动态运行的。 如果在推理时每处理一个 Chunk 都要做一次词表级别的反向传播， 其计算开销和显存占用将是灾难性的， 彻底丧失实时推理的可能。

2.梯度公式简洁，易于优化

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

非线性 vs · 线性： 交叉熵配合 Softmax 产生的是高度非线性的梯度， 需要依赖深度学习框架 （如 PyTorch) 的自动求导引擎去逐层计算雅可比矩阵。 内积的数学优雅： 当使用 £ = 一 〈 Z 彬。！ “， 〉 丆时， 损失函数对彬 “ 的亻扁导数具有极其简单的解析解。 其梯度更新规则直接退化为纯粹的矩阵乘法： down 这种没有激活函数、 没有链式法则的线性更新， 极大降低了计算复杂度， 使得在 GPU 算子层面的极致优化成为可能。

3.上下文并行的要求

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

并行计算的数学前提是 “ 结合律 (Associativity) "： 论文为了实现计算加速， 引入了上下文并行机制， 利用并行扫描 (Parallel Scan/Prefix Sum) 来同时处理多个文本块。 交叉熵打破结合律： 如果用交叉熵， 第个块的权重更新 AVVt 会极度非线性地依赖于前一个块更新后的权重一 1。 这迫使计算必须严格串行 （算完块 1 才能算块 2), 上下文并行将彻底瘫痪。 内积保全了结合律： 因为内积带来的更新规则是矩阵加法 (AWt = ViTzt)。 矩阵加法完美满足结合律， 所以各个 Chunk 可以独立、 并行地算出自己的增量 AVVt, 然后通过一个简单的累加操作 ()S = 」 A 丿） 即可无损合并。 这是内积损失带来的最大工程红利。

### （四）为什么对推理能力的影响不大？

1.架构层面

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

ln-Place TTT 并没有更新模型所有的权重， 而是做了一个非常精妙的隔离设计： · 冻结 “ 通用推理底座 "： 模型的核心自注意力机制 (Attention)、 以及 MLP 层中的 “ 和彬都被完全冻结， 作为 “ 慢权重 (Slow weights) " 保留。 这些庞大的参数矩阵承载了模型 gate 通过海量语料学到的常识逻辑和 Ag e nt 规划能力。 残差更新机制： 它仅将 MLP 的最后一个投影矩阵 Wdown 征用为可以就地更新 (ln-Place Update) 的 “ 快权重 (Fast Weights) "。 由于 Transformer 层天然包含残差连接 (Residual Connection)， 其输出形式可以抽象为： 丑 = Htn + MLP(Htn) 当 Wdown 发生改变时， 它实际上是在模型原有的庞大推理路径 (Htn 及其内部由慢权重主导的特征提取） 之上， 做了一个局部的增量特征叠加。 这保证了底层推理能力的绝对安全。

2.目标函数层面

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

论文特别指出， 在实际工程实现中， 这个目标函数并没有局限于单一的下一个 Token。 局部未来特征的融合： 通过一维卷积 Conv1D(•) 和映射矩阵 VVtarget 的可学习组合， 目标值实际上捕获的是一个局部的未来 Token 组合信息 (learned, localized combination of future tokens) 与 MTP 思想的同源性： 论文明确提到， 这种设计与近期在先进大模型 （如 DeepSeek-V3 采用的 Multi-Token Prediction 技术） 中被验证的思路高度一致。 强制模型在隐空间内一次性规划并预测未来的多个演化状态， 不仅不会削弱， 反而会倒逼模型进行更深度的前向隐式规划， 这对维持甚至增强 Ag e nt 的多步决策能力是大有裨益的。

### （五）预填充阶段的加速机制：因果填充

在Agent任务中，模型需要在预填充阶段从长上下文中学习。

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了打破逐词计算的性能瓶颈， ln-Place TTT 采用了上下文并行 ℃ ontext Parallelism)， 将极长的序列切分为多个块 ℃ hunk), 并让所有 Chunk 同时并行计算自己的参数更新量 AW。 这就带来了一个严重的问题： 理论上论文是为了 " 预测未来词 "， 如果在计算 Chunk 最后一个 Token 的目标值时， 卷积核向右 " 看 " 到了哪怕一个未来的词， 它就会跨界提取到 Chunki + 1 的数据。 一旦发生了跨块的数据依赖， Chunki 必须等待 Chunki + 1 的数据， 并行扫描算法 (Parallel Scan) 的基石就会崩塌。

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了确保对 Chunki 的更新量完全不包含未来的信息， 作者在生成目标值时对 ID 卷积强行应用了因果填充。 常规卷积的视野： 如果卷积核大小为 5 （即 = 5), 为了计算位置的特征， 通常会取一 2， 一 1， + 1 + 2 ]。 这显然包含了未来的佇串口 / 匚 0 因果填充的具体做法： 论文在附录中明确指出， 实现中采用了核大小为 5 且带有因果填充的深度一维卷积 (depthwise Conv1D)。 具体来说， 它会在序列的左侧 （历史方向） 手动填充 4 个 0。 这样一来， 在计算位置的特征时， 感受野被强行向左平移， 只能严格看向过去和现在： 一 4 一 3 一 2 一通过这种单向的填充， 每个 Chunk 的计算被严格隔离在自己的边界内。 这种隔离机制使得高度并行的扫描算法在数学上完全等价于严格的因果序列过程。

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

你可能会立刻察觉到一个逻辑矛盾： 理论分析中 (Theorem 1), 作者证明了最完美的 NTP （下一个词预测） 目标是直接拿 “ 下一个词的词嵌入 （场刀 = 丛 l) " 作为目标值。 但是因果填充把卷积核向右看的视野完全封死了， 它怎么还能提取未来信号？ 这正是这篇论文在工程实践上的精妙妥协： 1 ． 2 ． 3 ． 真正的 " 硬标签 （即确切的下一个词） " 确实无法在严密隔离的并行系统中被提前提取。 因此， 作者将实际应用中的目标值退化为一种局部的、 组合式的特征。 也就是说， 通过可训练的映射矩阵 Wtarget 和带有因果填充的 Conv1D， 模型实际上是在学习： 如何用快速权重将当前的激活值， 映射为一个由过去局部 Token 演化而来的高级平滑表征。 这种对当前及过去特征的高维重组， 虽然没有直接 " 偷看 " 下一个词， 但足以提取出比简单 “ 重建当前词 " 丰富得多的预测信号。

当然，解码阶段仍然逐词串行输出和更新权重：

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

AppIy: 模型直接使用当前维持的彬 “ 对这单个 Token 进行投影， 得到输出。 Update: 计算这单个 Token 带来的极小梯度增量 AW, 并将其累加到 VV( 灬中 （在这个过程中同样受到极小阈值丆 = le 一 5 的强制截断保护以维持数值稳定）。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
