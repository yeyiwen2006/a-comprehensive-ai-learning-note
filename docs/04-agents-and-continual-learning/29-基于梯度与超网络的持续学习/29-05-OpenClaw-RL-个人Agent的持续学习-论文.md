---
title: "29.5 OpenClaw-RL：个人Agent的持续学习（论文）"
source_docx: "第4部分 大模型智能体与持续学习/29.基于梯度与超网络的持续学习/29.5 OpenClaw-RL：个人Agent的持续学习（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 29.5 OpenClaw-RL：个人Agent的持续学习（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、核心思想

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

现有的智能体 RL 系统通常忽略了交互后产生的下一状态信号 (next-state signals) （如用户的回复、 工具输出、 环境的状态变化等 ), 仅仅将其作为生成下一步动作的上下文。 论文指出， 这种做法导致了两种极具价值的在线反馈信息被浪费： 评估性信号 (EvaIuative signals)： 下一状态隐式地对前一个动作进行了质量打分。 例如， 用户对智能体重新提问代表不满意， 测试用例通过代表成功， 而代码报错代表失败。 指导性信号 (Directive signals)： 除了单纯的打分， 下一状态通常还包含具体的修改建议。 例如， 详细的错误日志， 或者用户直接指出 " 你应该先检查文件再修改 "， 这在 Token 级别提供了明确的定向优化指导。

## 二、系统架构

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了实现在线、 实时的训练并避免单点阻塞， OpenClaw-RL 基于异步框架构建了 * * 完全解耦 (fully decoupled) * * 的系统架构。 它包含四个独立运行的异步循环： 1 ． 2 ． 3 ． 4 ． 策略服务 (policy Serving)： 由 SGLang 驱动， 负责不间断地为后续的用户请求或环境交互提供推理服务。 环境服务器 (Environment Servers)： 对于个人智能体， 环境即用户的本地设备， 通过加密 API 与 RL 服务器通信； 对于通用智能体， 环境部署在云服务上以支持大规模并行计算。 奖励裁判 (Reward Judging)： 利用 SGLang 或 API 部署过程奖励模型 (PRM), 对收集到的历史交互进行打分或提取文本提示。 策略训练 (policy Training)： 由 Megatron 引擎负责， 在后台处理样本队列并进行模型权重的平滑更新 (graceful weight update)， 全程不会中断前端的推理服务。

## 三、核心算法

### （一）针对评估性信号的二进制强化学习

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

该方法将分布广泛但粗粒度的评估性下一状态信号转化为标量的过程奖励。 工作流： 1 ． 2 ． 3 ． 4 ． 5 ． 智能体在状态下生成响应动作。 环境或用户反馈产生下一状态 + 1。 系统调用 PRM 模型， 依据 + 1 对的质量进行评估， 输出初步分数 e 通过多次独立并发查询 (m 次） 并进行多数投票 (Majority vote), 得出该轮次的最终评估奖励 0 小直接将最终奖励作为该步的优势函数 （= rfinal)， 并将其送入训练队列。 数学公式： 该方法采用标准的带有非对称裁剪边界的 PPO (Proximal Policy Optimization) 代理目标函数进行优化： 一 Et [min （At， clip （， 1 一 e, 1 + €high)At)] 其中， 丌 9 （老 St） 曰疋新旧策略的概率比率。 丌 d at St

### （二）针对引导性信号的蒸馏

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

该方法将包含丰富纠错信息的指导性信号， 转化为 Token 级别的定向监督梯度。 工作流 · 1 ． 2 ． 3 ． 4 ． 提取提示 (Hindsight Hint Extraction)： 裁判模型对和 st + 1 进行分析， 如果发现明确的优化建议 （即判定为 + 1 分）， 则提取出简明扼要 （1 一 3 句话） 的文本提示 (Hint)0 过滤与选择 (Quality Filtering)： 为了保证训练信号的纯度， 系统会过滤掉无效样本； 在多个有效 Hint 中， 选择长度最长、 信息量最大的一个。 构建增强上下文 (Enhanced Teacher Construction)： 将提取出的 Hint 以特定格式 （如 [user's h 土 n t / instruction]\n{hint}） 附加到上一轮的系统上下文中， 构造出一个包含了 " 事后诸葛亮 " 信息的增强状态蹈而 “。 计算 Token 级别优势： 让策略模型扮演 " 教师 "， 在知晓 5 “ / 灬艹 d 的情况下重新评估原始响应中每个 Token 的对数概率， 并与学生模型在原状态下的概率相减。 数学公式： Token 级别的优势计算公式为： 一 log （lst “ 4 = log Tteacher at Senhanced 如果 > 0， 说明该 Token 符合指导意见， 应被强化； 若 < 0， 说明该 Token 不符合要求， 应被抑制。

这里是在比较学生上下文和教师上下文下生成学生动作的概率，只是评价学生模型生成的token的好坏，并没有让教师生成新token并进行KL散度损失计算。这和在线自蒸馏并不一样。

为什么A_t取这个值：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在深度学习中， 让学生模型 （砌） 去学习教师模型 (Tteacher) 的行为， 最标准的方法是最小化它们分布之间的 KL 散度 (Kullback-Leibler Divergence)0 如果我们对 KL 散度求梯度 （即我们要更新学生模型参数 0 的方向）， 其核心项可以近似推导为： 一 logno(at)) · Vo log （V 刀五刀现在， 我们再看强化学习。 标准的策略梯度 (Policy Gradient) 算法更新参数 O 的基本公式是： VOCPG = 一 At · Vo log （

两式对应即得。值得注意的是，真实的KL散度梯度含有期望项，但是在实际的大模型工程中，对每个步骤都计算全词表的期望会导致计算量爆炸。考虑到a_t就是我们在这一步由学生模型策略真实采样生成的 Token，所以a_t本身就可以作为这个期望的一个无偏估计量。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

大语言模型的最后一层是没有被归一化的分数， 叫做 Logitso 当我们使用 Softmax 函数将 Logits 转化为概率时： log elogitj 乥对其取对数 (log)： log P@i) = logitt 你可以看到， 对数概率在本质上就是模型输出的 Logit 值 （线性偏移了一个常数）。 因此， log Tteacher 一 log@ 这个操作， 在物理意义上就是在计算两个模型输出的 Logits 的差值 （距离）。 如果教师的对数概率是一 2 （约 13 ％ 概率）， 学生的对数概率是一 5 （约 0 ． 6 ％ 概率）， 差值这个 + 3 的优势值告诉 PPO 算法： " 在这个 Token 上， 学生模型的 Logit 偏低了， 请在反向传播时， 给这个 Token 的 Logit 施加一个 + 3 大小的向上推力。 '

### （三）联合优化

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Binary RL 能够覆盖所有带有评分的交互轮次 （提供广度 ), 而 OPD 能够在用户或环境给出明确指导时提供高分辨率的逐 Token 纠正 （提供深度）。 由于两者共享相同的 P PO 损失函数架构， 论文提出通过直接加权两者的优势函数来进行联合优化： At = Wbinary “ ． + Wopd(lOg Tteacher at 5 曰访 ed 一 log 丌鼠

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
