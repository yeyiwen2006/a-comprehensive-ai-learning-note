---
title: "25.5 重排序与精炼"
source_docx: "第4部分 大模型智能体与持续学习/25.检索增强生成/25.5 重排序与精炼.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 25.5 重排序与精炼

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、重排序器

1.全量重排序

前面的稀疏和稠密检索可以让我们获得Top-K分数的文档（片段），但都主要集中在整个文档（片段）向量的整体语义上，为了速度牺牲了精度。为此，我们还需要进一步在token级别进行精细化调整，即用重排序器，在检索出的 Top-K（例如前100个）文档上进行排序。具体方法有：

（1）LLM评估法

（2）交叉编码器法

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

输入构造： 将查询 q 和候选文档 d 拼接成一个序列， 中间用分隔符隔开： lnput = [CLS] q [SEP] d æ CSEP] 全注意力交互 (Full Self-Attention): 将拼接后的序列输入 Transformero 由于 Self-Attention 机制， 查询中的每个 toke n 都能注意到文档中的每个 tokeno 这使得模型能理解复杂的逻辑关系 （如否定词 “ 不 "、 " 但是 “）。 评分输出： 通常取 [CLS] 标记的输出向量， 经过一个全连接层 (Linear Layer), 输出一个标量分数 5 e [ 0， 1 ] （通常经过 Sigmoid)0 Score(), d) = 00 铲 · Transformer (lnput） + b)

可以看到，这里并非Query对文档用互注意力（Bi-Encoder），而是Query和文档拼接后用自注意力打分（Cross-Encoder）。最后取Query [CLK]对应的全局注意力向量，追求极致的精度。（注：W为1*d_model维，和[CLS]token向量的d_model*1维相乘为标量）

为什么选择交叉编码器：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

双编码器 (Bi-Encoder): 流程： Query ． 分 Vector Vq, DOC ． 分 Vector。 痛点： Query 和 Doc 在生成向量时是互不可见的。 所有的语义必须被压缩到一个固定维度的向量中 (vector Bottleneck)0 例如， " 苹果 " 这个词， 在生成向量时必须同时隐含 “ 水果 " 和 " 公司 " 的含义， 非常模糊。 交叉编码器 (Cross-Encoder): 流程： 将 Query 和 Doc 拼接后送入模型。 优势： Query 和 Doc 在模型的第一层就开始交互。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

“ 互不可见 " 指的就是： 在生成的那一刻， 模型不知道 Q 是什么； 在生成的时刻， 模型不知道刀是什么。 它们是独立编码的。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设有一篇文档刀： “ 苹果发布了新财报， 股价大涨。 " 场景 A (Cross-Encoder, 无瓶颈）： Query: " 好吃的水果有哪些？ “ 拼接后模型看到 " 水果 " 和 " 股价 "， 直接打低分。 Query: " 科技公司动态 " + 拼接后模型看到 " 科技 " 和 " 股价 "， 打高分。 模型在交互中动态理解了语又。 场景 B (Bi-Encoder， 有瓶颈）： 文档刀必须被压缩成一个静态向量。 这个必须同时通过 768 个数字来表达 " 苹果 “、 “ 公司 "、 " 贝兼钱 “、 " 科技 " 等所有含义。 多义词灾难 · 如果过分强调 " 科技 "， 那遇到 Query " 苹果 （水果） " 时， 虽然语义不符， 但因为都含有 " App 丨 e " 这个词的嵌入信息， 向量距离可能还是很近 （产生幻觉 / 误召回）。 如果试图中庸， 既像水果又像科技， 那它可能离 " 纯粹的水果 " 和 " 纯粹的科技 “ 都有一段距离， 导致谁都搜不到它。 总结： Vector Bottleneck 指的是： 无论句子多长、 含义多丰富、 多歧义， 双编码器都强行将其压缩进一个固定大小的向量中。 由于缺乏查询时的上下文 ℃ ontext)， 这个向量只能是一个 " 模糊的平均值 "， 丢失了细粒度的语义匹配能力。

而如果用交叉编码器，模型在Embedding时可直接将Query和文档拼接，根据Query确定把文档的哪些内容放进Embedding。但缺点是这样文档的Embedding依赖于Embedding，必须在每次给出Query后都在线计算，而无法提前单独对文档进行Embedding操作。

2.chunk重排序

在数据量大、对延时要求高、算力相对不足等情形下，对所有的论文进行整个文档的token级计算并不现实，往往每篇论文只能取几个较小的chunk送进重排序器（如果是论文检索等场景，往往除了chunk之外还会加上摘要一并送入重排序器）。一般而言，切分chunk的方法有：

（1）按自然段/一定token数切分：每段或每一定token数为一个chunk，最简便，但有时会丢失一些上下文信息，或出现冗余信息。可以通过加入压缩后的上下文、与相邻chunk 20%重叠的方式一定程度上解决丢失上下文信息的问题，保证连续性。

（2）按句子切分再合并：先按句子切分，再计算每一句与下一句的embedding向量间的相似度。若高于阈值，则将下一句合并入该句所在chunk；否则二者分入不同chunk。这样可以得到若干chunk。

切分完毕后，计算Query与每个小chunk的embedding向量间的相似度，取每篇论文TopK的chunk，送进重排序器。

## 二、精炼器

检索到的文档往往包含大量无关噪声，直接输入生成器会导致Token浪费甚至产生幻觉。精炼器的目标是提高“信噪比”，故常用精炼器进行精炼后再加入上下文窗口。

精炼方法：

1.抽取式精炼：使用一个轻量级的打分模型（甚至可以是另一个 Cross-Encoder）对每个句子评分，判断其是否包含回答问题所需的关键信息。

2.摘要式精炼：让LLM或小型BERT模型对检索到的内容生成摘要。

3.LLMLingua压缩：用LLM预测对应token，越能被轻易预测，该token信息量（困惑度）越小，越应压缩。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Keep@t) = 一 log 尸@0 < D > 到

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
