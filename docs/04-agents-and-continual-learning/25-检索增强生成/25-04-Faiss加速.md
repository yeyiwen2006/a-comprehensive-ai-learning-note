---
title: "25.4 Faiss加速"
source_docx: "第4部分 大模型智能体与持续学习/25.检索增强生成/25.4 Faiss加速.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 25.4 Faiss加速

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

由于稠密检索需要遍历所有文档（片段）向量并进行逐个维度点乘，时间复杂度为O(n*d)。Faiss加速可以起到降低时间复杂度的作用。

## 一、查找次数的降低：K-means聚类和倒排索引（IVF, Inverted File Index）

IVF是常用的加速手段，其核心思想是“先粗筛，再精选”，类似于图书馆的分层检索。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

IVF 是最常用的加速手段。 其核心思想是 " 先粗筛， 再精选 “， 类似于图书馆的分层检索。 工作机制： 1 ． 2 ． 3 ． 聚类： 在训练阶段， 利用 K-means 算法将整个向量空间划分为个聚类 （称为 voronoi Cells)0 每个聚类中心被称为一个 Centroid0 建立倒排表： 每一个原始向量都会被归类到离它最近的一个或多个聚类中心下， 形成一个 " 倒排列表 "。 查询加速： 检索时， 查询向量 q 首先与个聚类中心对比， 选出最近的 nprobe 个聚类， 只在这几个聚类下的候选向量中进行精细计算。

改进算法：

1.倒排多索引（IMI, Inverted Multi-Index）

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这是一种通过 " 维度分解 " 实现的隐式多层聚类。 数学逻辑： 将 d 维向量切分为两半， 每半部分分别进行聚类 （假设各有个中心）。 层级效果： 通过这种组合， 实际上产生了 × 左 = 左 2 个虚拟桶。 优点： 只需要存储个中心点， 就能达到级别的细分粒度。 这在处理数十亿 (Billions) 级别的向量时非常强大。

2.层次聚类树（IndexHierarchicalNSG / Multi-level K-means）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在训练阶段， Faiss 可以通过多级 K-means 构建一棵树。 工作流： 1 ． 先将空间聚类成 100 个大类。 2 ． 在每个大类内部， 再聚类成 100 个子类。 检索： 像查字典一样， 从树根向下搜索。

## 二、查找次数降低的另一方法：HNSW（Hierarchical Navigable Small World，分层导航小世界）

1.基本思想

NSW（导航小世界）：在一个图结构中，如果每个节点都与附近的节点相连（短程边），且存在少量跨度较大的随机连接（长程边），则整个图具有“小世界”特性。搜索时通过贪心算法，从一个起始点开始，不断移动到离目标最近的邻居。在RAG的背景下，这里的每个“节点”是一个向量，每个向量都和离它较近的向量在图中相连。

HNSW的改进：NSW在处理大规模数据时，搜索复杂度会退化。HNSW通过引入类似B+树的分层结构，每个在某层出现的节点在它上面的层必然也出现，搜索时不断“下降”，每次下一层的节点个数约为上一层的M倍（在插入节点时由概率确定该节点出现的最上层是哪一层，上一层的概率为下一层的1/M）。

例如：最高层有6，中间层有369，底层有123456789（一个序号代表一个向量离Query的距离）。从最高层的6出发，下沉到中间层，贪心可在很短的时间（常数时间，这是“小世界”特性带来的）内找到最近的3，再下沉到底层，又可以在常数时间找到1。可以证明，此时检索时间复杂度近似O(logN)，构建这个层级结构的时间复杂度近似O(NlogN)。

2.检索工作流（O(logN)）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

阶段一： 高层快速路由 (Greedy Routing) 起始点 · 从最高层唯一的入口点开始。 贪心遍历： 在每一层， 只保留一个当前最优解。 不断跳向比当前更靠近 q 的邻居。 目标： 以极小的计算开销， 跨越空间中的 " 荒漠 "， 迅速到达 q 所在的 " 纟录洲 " 附近。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

A. 高层贪心路由原理： 在 0 到 Lm 层， 不使用复杂的队列， 只进行简单的贪心跳转。 为什么这么做？ 效率至上 · 咼层节点少， 误差容忍度高。 快速下降到 0 才是核心任务。 · 小世界特性： 根据 Watts-Strogatz 模型， 小世界网络中通过少量的长程边 (Long-range edges) 即可实现极短的路径长度。 高层边正是起到了长程边的作用。 B. 底层束搜索 (Beam Search with C & W Queues) 原理： 在力 0 层使用候选队列 C （小根堆） 和结果队列彬 （大根堆） 进行扩展。 为什么这么做？ 克服局部最优： 高维空间的向量分布非常复杂。 贪心搜索 ()f = 1) 很容易掉进 “ 局部陷阱 "。 通过增大 e Search， 算法可以同时沿着多个可能的方向探索。 计算与精度的平衡： 队列维护了目前最优秀的 efSearch 个候选者， 它实际上划定了一个 “ 动态搜索边界 "。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

阶段二： 底层精细搜索 (Beam Search at 力 0） 当下降到为 0 层时， 算法从贪心策略切换为基于两个优先级队列的束搜索 （类似于宽度优先搜索的变种）：

我们采用类似广度优先的算法，但不同的是，这里只把最小邻居（而非所有邻居）压入待访问队列C，使得算法具备一定的贪心性；BFS中的“访问”，也被改成了“加入结果队列W”：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

数据结构准备 · 候选队列 C (Candidate Set)： 小根堆， 存储 “ 待探索 " 的节点， 离 q 越近优先级越高。 结果队列 (Result Set)： 大根堆， 存储 “ 目前为止最接近 q" 的 efSearch 个节点。 循环迭代： 从 C 中弹出距离 q 最近的节点 co 获取 c 的所有邻居 eo 距离判定： 如果 （e， q) 小于 W 中最远节点的距离 （即大根堆堆顶 ), 则将 e 同时压入 C 和彬。 动态调整： 如果超过了 efSearch 大小， 弹出堆顶最远的占收敛条件： 当 C 为空， 或者 C 中最近的节点已经比彬中最远的节点还要远时， 搜索停止。

为什么这样选取收敛条件：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为什么这么做？ 图的单调性假设： 在导航小世界图中， 我们假设如果你当前能看到的最好机会 (C 的堆顶） 都比你已经握在心里的最差结果 （彬的堆顶） 还要差， 那么在图的更深处找到更好结果的概率已经微乎其微。 大幅减少计算量 · 这避免了对图进行全量遍历， 是 HNSW 实现亳秒级响应的核心。

3.构建层级结构工作流（O(NlogN)）

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第一步： 确定最大层级 I 每个节点在插入前， 都会被赋予一个最高可见层级 10 这通过泊松分布或指数分布随机生成： 一 1n(unif(0, 1）） · mLJ I 物理意义： 大部分节点只存在于底层 0）， 只有极少数幸运几能登上高层。 这构成了典型的 " 金字塔 " 结构。 第二步： 跨层寻找入口点系统从整个索引的全局入口点 (Enter Point, 预定义的最高层节点） 开始， 自顶向下搜索： 1 ． 2 ． 3 ． 在每一层执行贪婪搜索： 检查当前节点的邻居， 找到离 vnew 最近的一个邻居。 将该邻居作为下一层的起始点， 继续下沉。 注意： 在进入 I 层之前， 这个过程不建立连接， 只为了快速定位。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为什么这么做？ 快速收敛： 高层图相当于空间的 " 摘要 "。 在稀疏图中， 贪心搜索能以极少的计算量 （跨越式地） 快速逼近查询向量所在的局部区域。 消除冗余： 在高层没必要找 Top-K， 因为高层的目的是 “ 导航 " 而非 " 精检 "。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第三步： 逐层建立连接 (Layeri e [ 0， l]) 一旦进入 I 层及以下层级， 系统开始真正的 " 拉帮结派 "： 1 ． 2 ． 3 ． 确定候选者： 在当前层进行类似检索的搜索， 维护一个大小为 e C “ 5 c “ 的候选节占集合。 选择邻居 (M 个）： 从候选中选出 M 个点与 vnew 建立双向边。 启发式连接策略 (Heuristic)： 这是 HNSW 的关键。 系统不一定只选最近的 M 个点， 而是会考虑这些点是否 " 扎堆 "。 策略： 如果候选点离 Vnew 较近， 但已经和已选中的邻居 B 靠得太近， 系统可能会跳过， 转而选择一个稍微远一点但方向不同的占目的： 确保连接具有方向多样性， 避免搜索时陷入 " 死胡同 "。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为什么这么做？ 解决 “ 孤岛 " 效应： 如果只连最近的邻居， 节点会倾向于形成密集的簇 (Cluster), 而簇与簇之间缺乏连接。 一旦搜索进入一个簇， 就很难跳出来。 模拟相对邻域图 (Relative Neighborhood Graph): 这种策略保证了图的全局连通性。 即使搜索点离目标较远， 也能通过这些 " 跨度较大 " 的边快速横跨空间。

## 三、点积计算和向量存储复杂度的降低：乘积量化（PQ, Product Quantization）

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

子空间切分： 将 d 维向量切分为 m 个子向量， 每个子向量的维度为 d/mo 子空间量化： 对每个子空间的向量分别进行 K-means 聚类。 通常每个子空间聚成 * 28 = 256 个类。 这样每个子向量就可以用一个 8-bit 的索引 （0 一 255） 来表示。 编码压缩： 一个原先需要 d × 32 bits （浮点数） 的向量， 现在只需要 m × 8 bitso 压缩比可达 30 一 ℃ 0 倍。 非对称距离计算 (ADC)： 当查询向量 q 到来时， 系统会预先计算 q 的每个子向量到该子空间 256 个聚类中心的距离， 生成一个距离查找表 (Distance Look-up Table)0 D(), ～ 乥 LUTt [codet ]

这样我们不需要把Query和n个d维向量逐个点积，而是只需要和m*256个d/m维聚类中心向量点积，对于每个d维向量来说，它的每个子向量（只需要存聚类中心的ID）和Query的点积就是其聚类中心和Query的点积，只需要去距离查找表中查到每个子向量的点积再相加即可。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
