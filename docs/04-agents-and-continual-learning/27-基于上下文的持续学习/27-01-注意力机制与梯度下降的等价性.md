---
title: "27.1 注意力机制与梯度下降的等价性"
source_docx: "第4部分 大模型智能体与持续学习/27.基于上下文的持续学习/27.1 注意力机制与梯度下降的等价性.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 27.1 注意力机制与梯度下降的等价性

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

注意力模块Transformer的Attention层（包含残差流在内）整体上可以被看作是一种回归问题的梯度下降优化。

## 一、线性注意力的表达式

对于一组token（模型输入序列）{e1,...,en}和query（test）token etest，经过去除Softmax的注意力层（即线性注意力层，后面会探讨加上Softmax的情形）后，注意力的表达式为：

Outputtest = etest + P*V*KT*qtest （1）

其中输出加上自身是因为残差连接；P为输出投影矩阵。若我们将V视为(v1,...,vt)，K视为(k1,...,kt)，则V*KT=sigma(vi*kiT)，即：

Outputtest = etest + P*sigma(vi*kiT)*qtest （2）

用类似RNN的方式表述即为：

St = St-1 + vt*ktT

Outputt = inputt + P*St*qt

上式意味着，我们利用隐状态矩阵S构建了一个从K到V的映射，并获取在输入qt时的输出，作为残差相加。忽略投影P，这里的V实质上拟合的是“输出与输入之间的残差”。

## 二、构造线性模型，并证明等价性

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们正在进行 ln-Context Learning （上下文学习）， 输入是一系列示例 （演示数据）： ．， № 以及一个新的查询输入 “。 在 Transformer 中， 每个 Token 会将输入特征和标签拼接在一起。 我们定义上下文矩阵为天 = · ,CN]， 标签矩阵为丫 = 匆 1，，

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

输入 Token: 假设将输入特征觞和目标标签拼接成一个列向量作为 token， 即 ‰

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

引入一个由权重矩阵 W e 参数化的参考线性模型扒 = VVc, 以及一个包含输入样本和标签坊的训练数据集刀 = { （，） } 1。 学习的目标是最小化平方误差损失：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2N

此时梯度下降的表达式（动态模型的更新方式）为：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

AW = -nVwL(W) =

（3）

这里的W*xi-yi和前面的vi对应（而不是yi和vi对应），表示“按原始权重的输出（W原*xi）和应输出的值（标签yi）的差距”；xi和前面的ki对应；η/N和前面的P对应。这也就意味着，每次输入一批样本，W的权重就将变化 -P*sigma(vi*kiT)。

也就是说，这个权重矩阵W和前面讲的隐状态矩阵S不一样：W是隐式的，表示x到最终y的映射；S是显式的，表示k到v（残差W*x-y）的映射。△W和S成比例关系，W的一次梯度下降相当于运用了S中存储的所有上下文token的梯度。(W+△W)*xtest和inputt+St*qt相对应。

对于测试token：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

test etest

我们初始化yinit为-W原*xtest（后面会阐释为什么可以这么做）。通过构造WQ、WK、WV矩阵，使得：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

当前查询 to ke n j 的 Query 向量： 上下文 toke n 的 Key 向量： = W1€ei 上下文 token 的 Value 向量： 0 0 0 0 0 0 0 0 屿防 0 0 0

计算注意力：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

何 0）

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 9 OCR 未识别出有效文字：未识别出有效文字。]

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 10 OCR 未识别出有效文字：未识别出有效文字。]

这意味着对上下文中的W原*x-y进行“聚合”，可以视为得到的是W原*xtest-ytest。我们想要的就是这个上下文聚合后得到的y值预测ytest=(W原+△W)*xtest，从而上式的下面部分就可以视为W原*xtest-ytest=-△W*xtest，也就是“原有输出-应然输出”。而这恰好和前面△W的表达式对上了，因此：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

pVK qj = —AWæj

这意味着对于test token，（1）式等价于：

这条式子的下面部分取相反数后，也就得到参数变换后（按W原+△W）应该输出的值，即注意力模块的输出。

## 三、为什么（4）式有负号，明明ytest=W*xtest+△W*xtest，为什么这里是减去△W*xtest？

这需要从多轮优化的角度分析。大模型中往往是多个注意力层串行处理数据，每一轮都把输入xtest转变为输出ytest，它又作为新的xtest输入下一个注意力层。这等价于对一个线性层进行多轮权重更新：刚开始输入xtest时（权重W原）执行一次梯度（权重变为W原+△W）下降并输出ytest，它又作为新的xtest输入（其他xi和yi仍然取原来的值）此时权重已经变为W原+△W的模型，再结合上下文进行梯度下降、推理，以此类推。

以上模型确实是正确的等价，但我们知道，实际上Transformer的权重是不变的，因此我们试图将上述过程再做一个等价转换，让W不变，而上下文yi可变，更符合Transformer本身静态结构对应的表达式（即（1）式）。除了计算输出值（这一点我们已经有办法了）外，“当下的W值”唯一的“用处”就是用于梯度下降，而由（3）式可知，线性层梯度下降的幅度和残差W*xi-yi成正比，因此W从W原变为W原+△W，等价于上下文token的标签yi减少△W*xi，这时Loss不变，残差不变，优化状态不变。因此，我们可以假设多个线性层串行组成模型，每个线性层的参数均为W，改变上下文token的yi：

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

= 一 AVVxt

下一层的W不改变，但是这时候上下文token里输入的不是原始的yi，而是yinew，下一层再对yinew进行类似的处理，以此类推，就可以通过改变上下文来等价地实现改变权重。

但是这时候会出现一个问题：自注意力层不会区分上下文token和test token，我们为了保证残差不变，把yi减取去△W*xi，但是ytest=W原*xtest+△W*xtest，符号反了。于是我们把yinit取为-W原*xtest，减去△W*xtest后恰好为-W原*xtest-△W*xtest，乘以一个对角线全-1的输出投影矩阵即可。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Target: Dynamic ModeI EvoIution (Updating Weights W) Gradient Step Gradient Step Further Upd ated lnitial Model origin Updated ModeI AWI AW2 WI = W + AWI origin Achieved by. Model W2 = WI + AW2

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Data Fixed Layer (Weight Worigin) Layer 1 (Worigin) computed Fixed Layer (Weight Worigin) Layer 2 (Worigin) Data y ． AW1*x) [ AW2 computed KEY FORMULA: Fixed Layer (Weight W 酾） Layer 3 “ Data y ． AW1*x ． AW2*x) Final Output TEST TOKEN | NIT: origin Xtest Ynew = Yold - * x FINAL READOUT： Output = 刁 Yfinal

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

穿过第 1 层注意力层 （执行第 1 步梯度下降）： 第 1 层根据当前的上下文计算出梯度增量 AWIO 残差连接将其加上： 1， - 一 0， 穿过第 2 层注意力层 （执行第 2 步梯度下降）： 第 2 层读取到继续计算出新的梯度增量 A 彬 2。 残差连接再次将其加上： —()o + AWI + A 2） “ A 彬 2 “ 穿过第 K 层注意力层 （执行第步梯度下降）： 以此类推， 经过层后， 测试 Token 的值变成了：

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

厂 0 e “ + AWkCtest

这样，我们就把一个持续梯度下降的线性模型等价转化为了多个权重不变的线性层，数据在层间单向流动，每流过一个层，所有token（包括上下文token和test token）对应的y值都会-△W*x，每一个这样的层就和Transformer中的每一个注意力层一一对应。值得注意的是，这样的W原并非显式储存于Transformer的权重中，而是被隐式维护（虽然它也是不变的）：在前面的推理中，我们构造过：

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

、、 厂 0

也就是说这里它由WV决定。

总结：标准Transformer 无法在内部凭空变出一个真正的权重矩阵W存起来，只能靠修改数据流（残差）来“模拟”梯度下降，为了在多层Transformer中保证优化状态一致性，才采用了这种“别扭”的方式。

## 四、大模型中的实际情况

### （一）“搬运”机制和归纳头

前面，我们只是在理论上证明了“自注意力有能力做梯度下降”。我们假设每个上下文token都由输入特征和标签拼接而成，但显然现实世界中的文本往往不符合这一点，输入特征和标签一般会分布在不同token中以随机位置出现。当面对这种真实随机的序列时，Transformer的第一层（包括了Softmax）并没有直接去算梯度，而是学习到了一种数据预处理功能：对于每一个含有y的token，通过q和K的匹配分数，找到它对应的x所在的token（不一定在x旁边，也不一定只有一个token），把x“搬运”到y所在的token里，再经过后面的线性层“整理”把向量变成类似于前述命题所需的(xi,yi)拼接状态。

这个搬运机制，恰好与Anthropic 团队之前发现的、被认为对上下文学习至关重要的“归纳头”（Induction Heads）现象吻合。第一层做的是Induction Head的拷贝工作，为后面开始执行真正的梯度下降（也就是前面推导的逻辑）、学习上下文铺平道路。在这里，Softmax起到了至关重要的作用：“搬运”意味着当前位置的Token必须能够百分之百地把注意力集中在目标Token（有需要的对应x值）上，同时绝对无视序列中的其他所有Token，这只有“赢家通吃”的Softmax能做到，如果纯粹用线性，往往会把序列里所有Token的特征都搅拌在一起（糊成一团），无法通过排除其他token的干扰来提取出极其干净的、单一的x。

### （二）yinit取为-W原*xtest的合理性

而那个把yinit取为-W原*xtest的构造操作，在实际训练Transformer的时候，实际上只要我们将注意力权重初始化为一个非常小的值，就可以近似认为WV = 0，进而由前面的分析W原 = 0，-W原*xtest = 0，而真实模型的第一层（归纳头）也会学习到主动把yinit变换为接近0的值，在遇到test token时初始预测值就是0，随后通过层层残差连接去累加算出的梯度。

另外值得注意的是，这种模式往往只在部分层、（多头注意力的）部分头出现，后面也会阐述原因。

### （三）关于yinit不参与△W的计算

在上下文学习中，输入序列的形式为：

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

lnput =

我们在上下文（准确地说是test token的前文）中通过梯度下降学习一个线性回归模型，再用这个模型预测xtest对应的y值。显然，yinit不应该混入训练这个线性回归模型的数据，但自注意力层仍会“一视同仁”地将其纳入△W的计算。

一种处理方法是掩码，把注意力权重矩阵对角线上的值全部设为负无穷。这将会导致训练时注意力矩阵从“下三角矩阵”变成“空心下三角矩阵”，导致为常规“下三角矩阵”设计的高度优化的Flash Attention等算子失效。

但事实上往往无需这样操作，因为根据之前的推导：

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

= 酽 0 一

由前面的分析，-W原*xtest = 0，而真实模型的第一层（归纳头）也会学习到主动把yinit变换为接近0的值，也就意味着此时Vtest = 0。这样，即便注意力权重没有被强制归零，它乘以的值Vtest = 0，也不会被累加到结果中，不会影响△W的计算。

当然，真实的大模型中，显然不可能让所有test token的Value直接设为0，丢弃test token自身的信息。大模型有极其庞大的特征维度（子空间）和复杂的多头/多层分工机制，只有一部分层、一部分头是按这里所述运行的，以实现上下文学习功能；其他层、其他头中，test token的Value仍然会参与聚合，实现该token的语义处理等其他功能。真实模型不是全局丢弃了 Test Token 的 Value，而是把“提取语义”和“计算误差梯度”分给不同的注意力头去并行处理了。

值得一提的是，模型中的Attention Sink现象可能也有着类似的机制：

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Transformer 每一层的公式是： 输出 = 输入 + 注意力计算结果在深层网络中 （比如 ℃ 0 层的模型 ), 很多时候， 某几层可能觉得当前的特征 （输入） 已经足够完美了， 不需要再从上下文中补充任何额外信息。 此时， 最理想的情况就是什么都不做， 让特征顺着残差主干道原封不动地流到下一层。 也就是说， 模型希望： 注意力计算结果 =， 从而实现输出 = 输入 +。

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

但是， 注意力机制里有一个极其霸道的数学公式： Softmaxo Softmax 要求： 所有 Token 的注意力权重加起来， 必须 100 ％ 等于 1 · 0。

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

既然被逼着必须把 ℃ 0 ％ 的注意力分配出去， 而自己又不想改变特征， 大模型 （比如凵 ama, GPT) 在漫长的预训练中， 自己 " 顿悟 " 出了一个绝招： 1 ． 2 ． 找一个替罪羊 (SinkToken)： 模型通常会选中序列的第一个 Token （比如 < s >） 或者第一个标点符号。 把它的 value 变成 0： 模型在训练参数时， 偷偷把提取这个替罪羊 value 的权重矩阵学成一种特殊状态， 使得这个替罪羊的以 0。 3 ． 倾泻无用的注意力： 当模型在某一层 " 不想关注任何上下文 " 时， 它就强行把高达 99 ％ 的注意力分数 (Attention Score) 打给这个替罪羊。 见证奇迹的时刻到了： 这 99 ％ 的注意力权重， 乘上了替罪羊的。 0 · 99 × 0 = 0 于是， 整个注意力分支的计算结果变成了 0。 输出 = 输入 +。 模型成功地在满足了 Softmax 苛刻要求的同时， 完美地什么也没做， 保持了特征的纯洁。

### （四）真实运行中，哪些token的V会被置为零？

在真实的自回归序列中，输入不是拼好的(x, y)，而是每个x和y都分布在不同token中，如一个交替序列：x_1, y_1, x_2, y_2, ..., x_19, y_19, x_20。

当我们要生成y_19时：（这时y_19还没有产生，被初始化为0；test token为x_19）

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

浅层网络处理： 因为这是新输入的 token， 它还没有对应的 9。 它的隐藏层状态只能是昀 9， 0）。 深层网络 （梯度下降层）： 深层网络提取它的 value 向量。 根据公式 = Woc 一此时 9 = 铲 0 9 一 0 0。 结果： 它完美地扮演了 Test Token！ 它带着 9 = 0 去和前面的 KV Cache 算注意力， 输出预测结果 9。 物理沉淀： 第 19 位的 KV Cache 被永久固化， 其深度层的 V 永远是 0。

此时梯度下降用的是(x1,y1),(x2,y2),...,(x18,y18)。x19 token的V于是在KV Cache中被固化为0.

注意：这里的深层网络不是最终的输出层，只是部分隐藏层的部分头。最终输出层的Value向量并不是0，无需担心信息损失。

当我们要生成x_20时：（test token为y_19）

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

浅层网络处理 （复制机制启动）： 大模型的第 1 层 （通常是 Softmax 归纳头） 具有强大的时序视野。 当它处理处于第 20 位的 9 时， 它会向回看一眼第 19 位的昀 9， 并把 9 的特征 * * 复制 （拷贝） * * 到自己的隐藏层状态中。 深层网络 （梯度下降层）： 当数据流到深层时， 第 20 位的隐藏状态已经不再是单薄的 9， 而是被浅层网络组装成了完整的上下文对 （昀 9， 9）！ 根据公式， 深层网络在计算第 20 位的 value 向量时： 0 = Wo 昀 9 一 9 0 物理沉淀： 第 20 位的 KV Cache 被固化， 其深度层包含了一个非零的 0。

此时梯度下降用的还是(x1,y1),(x2,y2),...,(x18,y18)。（不能包括(x19,y19)，因为这是test token）

V_y19=W_0*x_19-y_19≠0.这一次计算后，KV Cache包含了一个非零的V_y19。

当我们要生成y_20时：（test token为x_20）

x_20作为Query去遍历过去的KV Cache。

此时梯度下降用的是(x1,y1),(x2,y2),...,(x18,y18),(x19,y19)。

对于x_19，它的V_19 = 0，乘法结果为0，直接被无视；对于y_19，它的V_y19=W_0*x_19-y_19≠0，提供了第19个样本的梯度信息。

### （五）Transformer内部只是朴素的梯度下降吗？

部分后续研究指出，给Transformer喂了条件数极差的数据（特征矩阵极度不平衡）的情况下，其性能远优于普通的梯度下降。根据对中间层输出的分析，Transformer可能在内部自动收敛成了一个类似于迭代牛顿法的收敛轨迹，从而能在极短的Few-shot样本下瞬间学会新任务。

## 五、含Softmax的情形

### （一）单头Softmax的困境：引入线性偏移

单层、单头的Softmax自注意力层无法完全匹配普通梯度下降的性能。其根本原因在于，Softmax的指数运算和归一化分母会在矩阵乘法中产生一个附加的误差项。这可以通过一阶泰勒展开近似说明。

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在自注意力机制中， Softmax 函数作用于注意力分数 （即 Query 和 Key 的内积）。 对于当前的查询 Token 与和第个上下文 Token 觞， 未归一化的注意力分数为： = 胡 ‰ = 彬五 Q 与 Zi 将包含所有 N 个上下文 Token 的分数代入 Softmax 函数， 第个位置的输出为： softmax(z)t

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

根据高等数学中的一阶泰勒展开公式， 当 z -+ 0 时， 指数函数可以近似为： 我们将这个近似代入 Softmax 的分子和分母中： · 产厂 kQ 与 1 + 铲 Q 与分子 · e N WKQXj。 丷 · 分母： Ek- ～ Ek-1(1 + 心与）

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们将所有 N 个 Token 的分子写成一个列向量： 一 1 1 + 1 铲五 Q 与 1 + CNWICQCj （中 1 是一个全为 7 的列向 1 气与 CNWKQCj 令分母的和为标量 SO 那么 Softmax 的输出向量可以写为： softmax(KTqj) 1 一 （1 + KTqj) =

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

softmax(KTqj) K ‰ + 1

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

物理含义： 这个 e 是一个常数偏移量 (Linear Offset)。 它意味着， 即便某些 Token 和当前 Query 亳无关系 （内积 Q 与 = 0）， Softmax 依然会强制分配给它们一个基础的权重 （即与。 这种 " 强制均分 “ 的特性， 污染了原本纯净的梯度方向， 导致单头 Softmax 注意力无法完美执行梯度下降。

即便对于内积为负数的token（对应可能是数学推导中的错误方向），权重也会变成正值。这显然不合理。

### （二）多头Softmax如何解决这一问题

前面提过，由于Softmax对于归纳头起着不可或缺的作用，我们无法通过直接去掉Softmax的方法来增强上下文学习能力。但既然偏移量1（即epsilon）为常数，那么多头Softmax可以通过不同头之间相减抵消来实现：

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设模型学习到了两个注意力头， 并且在最后的输出投影层 (Projection Layer) 中， 它将这两个头的权重学成了符号相反的形式， 即使得 PIVI —P2V20 为了简化表达， 论文假设这两个头提取误差的矩阵 PV 相同 （或被网络调整到一致）， 但在内部的注意力特征矩阵 WKQ 上学到了不同的特征空间。 我们将这两个头的注意力计算结果相减： Output PV · softmax()1 ql 孬） 一 PV · softmax()2 q2,j)

> [图片 31：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

将我们在第一部分得到的泰勒近似列向量代入上式 （这里论文做了一个合理假设， 即两个头经过 Softmax 的分母常数 S 近似相等， 并将其吸收到尸中） ． 1 + W1,KQCj Output 尸 1 + 1 铲 2 五 Q 与接下来进行极其简单的线性代数运算， 将对应行的元素相减 · 对于第行： （1 + 彬 1， 五 Q 与） 一 （1 + £ Ti 彬 2 Q 与） 1 一 1 + 铲 1， 五 Q 与一铲 2 KQCj = （WI Q 一彬 2 Q） 与看！ 常数项 1 （即那个讨厌的 6 偏移量） 在减法中完美归零了！

## 六、为什么Transformer作为一种事实上的元学习算法，仍不能很好地应对OOD问题？

### （一）特征提取的困难

我们在前面推导中的前提是，执行梯度下降的特征向量x，是经过大模型的Embedding和MLP层投影后的深层表示。注意力层可以看成实时梯度下降，即视为元学习；但Embedding和MLP层并不是如此。如果遇到OOD问题（比如人类从未见过的数据模态），预训练阶段的MLP根本没有为这种数据建立特征坐标系。OOD数据在输入网络后，被MLP映射成了一堆毫无意义的噪声向量，无法顺利提取数据的特征，进而后续的注意力层在执行y_new = y_old - △W*x 时，算出的梯度也就是一团乱码。

### （二）梯度下降的局限性

梯度下降算法本身的特点决定了，面对OOD任务时，大模型所谓的“学习”，并不是真正在寻找全局最优解，而是在它预训练时见过的“任务池”里，寻找一个和当前OOD任务最相似的替代品。它只能在已有的先验知识流形（Manifold）上插值，无法外推到一个全新的物理空间。

### （三）计算深度不足

我们证明了：1层Transformer = 1步梯度下降。一个100层的模型，最多也只能执行几十步、上百步的有效梯度下降。而真正的OOD任务（比如从头训练一个视觉模型）往往需要几万、几十万步的梯度迭代才能收敛。大模型那极其有限的层数，根本无法提供足够的计算深度来完成OOD的参数拟合。

当然，随着RLVR带来的模型推理能力的提升，模型在完成一个任务（即上下文不清空）的情况下的KV Cache更新次数和有效计算深度大大增加。“组合型OOD”往往能被解决：如给一道全新的数学竞赛题，整体来看它是OOD的，但它可以把这个问题拆解，原子步骤（加减乘除、代数变换）都在它的先验特征空间内，RL训练出的思维链能通过千万步的内部梯度下降，把这些已知特征重新组合，最终攻克难题。但对于“绝对型OOD”，即输入的底层特征在模型的MLP中根本不存在映射的情况，该方法仍然无效。

## 七、优化问题的等价性

> [图片 32：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在神经心理学中， 记忆是由输入引起的神经更新， 而学习是获取有效和有用记忆的过程。 我们遵循 Behrouz 等人的定义： 定义 1 （联想记忆）： 给定一组键 （Keys) ]Rdk 和值 (values) c， 联想记忆是一个算子 M： ℃ -+ 用于映射这两组集合。 为了从数据中学习这种映射， 我们定义一个目标函数 £@ ·） 来衡量映射的质量， M 可以定义为： M* = arg min £ （M）） （1） （原理阐释与推导分析 〕 核心思想： 这里将 “ 记忆 " 看作是一个函数 MO 传统的观点认为训练模型是调整参数， 而这里的观点是： 训练过程本身就是在一个函数空间中寻找最优解， 使得这个函数能最好地把 Key 映射到 valueo 物理意义： 方程 （1） 本质上是在说， 最佳的记忆状态丿 * 是那个能最小化预测误差的状态。 在深度学习中， ℃ 通常是输入数据， 是标签或目标， £ 是损失函数。 NL 的独特之处在于， 它认为模型内部的每一个组件 （甚至是优化器） 都在解这样一个方程。

事实上，无论是Attention注意力模块的前向计算，还是传统意义上的“优化”，都可以视为这样一个“寻找最优函数”的过程。下面我们推导梯度下降法与其的等价性：

> [图片 33：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们要用梯度下降法训练一个单层 MLP （参数为 W)。 优化目标是 · W* = arg minC(); 0 “，，） 梯度下降的更新规则导致权重更新等价于： =， 一 + 1 VWtC(Wt; + 1）

这里x_t+1是在t+1时刻输入的一个批量训练数据。由于y_t+1=W_t*x_t+1，对W_t的梯度即为对y_t+1的梯度和x_t+1的积：

> [图片 34：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

VVt 一 + 1 （+ 1） + 1 （4） 其中 + 1 = W + 1 是模型输出。 我们可以令 + 1 = Vyt 1C(Wt; + 1）， 并将反向传播过程重新表述为寻找最优联想记忆的优化问题的解。 我们旨在找到映射输入 + 1 到其对应的 + 1 的最佳彬。 这就变成了求解以下优化问题 IIW 一腭 （使用点积相似度作为目标）： 1 Wt + 1 = arg min 〈 H厂购+1， + 1 〉 + 2 + 1 （5）

设W_t为此时模型的权重，x_t+1为当前的输入，y_t+1=W_t*x_t+1为模型在当前权重的线性输出。我们把损失函数L(W_t;x_t+1)称为“惊讶程度”。这既是因为模型预测越正确，惊讶程度越低，也是因为交叉熵损失函数的信息论背景本来就是“惊讶度”。

我们定义“惊奇信号”为：

> [图片 35：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

+ 1 其中： 彬当前时刻的模型权重。 · + 1： 当前的输入数据。 一 Vy = + 1： 模型在当前权重的线性输出 （假设是单层 MLP)0 + 1 £： 损失函数。

它可以理解为“Loss对神经网络输出层的指令”，即怎样调整网络的输出y，以使损失最小。

> [图片 36：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

预测误差的直接体现： + 1 （即 VyC) 直接衡量了模型当前的输出 + 1 和真实标签 （或目标） 之间的差距。

对于t+1时刻新输入的数据x_t+1，如果我们只需要根据用已有数据训练出来的W_t，就能将输入转化为正确标签y_t+1，那么权重W_t已经是最优了，完全没有必要更新，u_t+1=0。反正，如果预测和y_t+1相差很大，惊讶程度就会很高，则意味着W_t需要较大的调整。

举一个例子：

> [图片 37：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 输入： "The sky is.. 模型预测旷真实标签： ...green" （纟录色） ...blue" （蓝色） 在这个过程中： £ （惊讶程度）： 模型发现真实是 Blue 而不是 Green, 非常震惊！ · Loss 值很大 （比如 5 · 0）。 这就是 “ 惊讶程度 "。 + 1 （惊奇信号）： 这个信号是一个向量， 它告诉模型具体的误差信息 · "Green 的概率太高了， 要降低！ “ "Blue 的概率太低了， 要升高！ ' 这个向量 + 1 就是具体的 “ 修正指令 "。 Nested Learning 的记忆过程 · 模型会把这次经历存进 M LP： “ 当你下次看到 'The s ky is...• 佃） 时， 记得要把那个叫的修正指令调出来。 '

这可以解释为什么神经网络（如MLP）可以作为记忆模块。训练前的神经网络里没有信息，给定一个输入x，它的输出y是一个噪声（先验为噪声分布）。而“训练”的过程，就可以视为告诉神经网络：“下次再遇到x，你的输出应该往u方向移动。”或者说，就是让它“记忆”输入与“碰到这个输入时，怎么（从先验的随机状态开始）改变自己的输出”之间的关系。

或者说，MLP的梯度下降更新本质是一个“压缩”过程，将当前输入数据及其对应“怎么修改输出”的信息“压缩”进权重中。模型通过持续进行在线梯度下降（Online Gradient Descent），实时地修改自己的神经网络权重，存储读过的数据片段的信息。

更进一步地说，惊奇信号的含义实际上是一种“反思”，即“根据当前神经网络输出与目标（标签）的差距来看，我的权重应该做怎样的改进”，并将反思写入权重，让模型实现在推理阶段的持续改进。

下面我们再以优化视角看这个问题：

> [图片 38：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

VVt 一 + 1 （+ 1） + 1 （4） 其中 + 1 = W + 1 是模型输出。 我们可以令 + 1 = Vyt 1C(Wt; + 1）， 并将反向传播过程重新表述为寻找最优联想记忆的优化问题的解。 我们旨在找到映射输入 + 1 到其对应的 + 1 的最佳彬。 这就变成了求解以下优化问题 IIW 一腭 （使用点积相似度作为目标）： 1 Wt + 1 = arg min 〈 H厂购+1， + 1 〉 + 2 + 1 （5）

> [图片 39：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 刂正一禺腭 （6） 2 叻 + 1

这条表达式的第一项是W*x_t+1（输出）和u_t+1（Loss对输出的梯度）的点积相似度，W的更新会让输出尽量沿着梯度的反方向；第二项是正则化项，避免权重更新过大。这一步将梯度下降公式逆向推导回了一个优化问题。

> [图片 40：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

定义目标函数： 1 IIW 一腭 2 仂 + 1 步骤 1： 对第一项求导利用迹的性质 〈 B 〉 = Tr(ATB)， 我们有： = 0 0 铲 + 1， + 1 〉 （： 这里砌 + 1 + 1 正是外积形步骤 2： 对第二项求导 D 1 IIW 一腭 DW 2 + 1 步骤 3： 求解极值点令总导数为零： VwJ(W) = （t + 1 厂 £ t + 1） = t + 1 + 1 1 2 + 1 7 九 + 1 1 7 九 + 1

> [图片 41：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

VV = Wt 一 + 1@t + 10 + 1

带动量的梯度下降也可以类似看待：

> [图片 42：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

接下来， 我们引入动量项。 更新规则如下： 在方程 8 中， 如果我们令 + 1 = V £ （+ 1）， 那么方程 8 可以被重写为： m, VWtC(Wt; + 1） 〉 + + 1 一 mt 目 2 （10）

> [图片 43：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

分层结构： 第 1 层 （外层）： 更新权重 WO 它使用的是 m 的值。 第 2 层 （内层）： 更新动量 mo 方程 （10） 表明， 更新动量 m 的过程， 实际上是在求解一个优化问题， 目的是让 m 尽可能与当前的梯度方向一致 （最大化点积 / 相似度 ), 同时保持与上一步的 mt 不会偏离太远 （正则化项）。 结论： 带有动量的梯度下降是一个双层嵌套优化过程。 内层记忆负责 " 压缩 " 过去的梯度历史， 外层利用这个压缩后的信息来更新模型。

事实上，前面的方程也可以被重写为这样的优化问题：

> [图片 44：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

mt+l = arg min 一 〈 m, V £ 0 帱； + 1） 〉 + T/t+111m 一 mtl 2 （10） 项 A: 趋势对齐这个优化问题描述了动量 m 是如何在每一步自我更新的。 项 A: 趋势对齐 (Alignment Term) 项 B: 历史惯性数学含义： 这是两个向量的内积的负数。 要最小化这个负值， 就等于要最大化它们的正内积。 物理意义： 这要求新的动量 m 的方向， 必须尽可能地与当前的梯度方向 (Vwt £） 保持一致。 这代表了对 " 当前新信息 " 的吸收。

> [图片 45：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

项 B： 历史惯性 (lnertia Term) + 1 一 m 2 数学含义： 这是新动量 m 与旧动量 mt 之间的欧几里得距离的平方。 物理意义： 这是一种正则化约束。 它要求新的动量不能背离上一步的动量太远。 这代表了对 “ 过去历史 " 的保留。

> [图片 46：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为什么说求解这个公式就是动量更新？ 让我们通过求导来证明。 设目标函数为 J(m)： 77 2 其中 gt+l = VwtC(Wt; + 1） 是当前梯度。 步骤 1： 对 m 求导 DJ(m) Dm 步骤 2： 令导数为 0 （寻找极值点） = 0

> [图片 47：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 m 一 mt + · gt+l （令： 2 + 1 结论： 这正是我们熟悉的动量累积公式的形式 (New—Momentum = 0 一 om 曰忉 m + Gradient)。 这证明了： 动量的更新过程， 本质上是在求解一个让 “ 保持惯性 " 和 “ 跟随新梯度 " 达到最佳平衡的优化问题。

我们也可以类似看待Adam：

> [图片 48：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

作者首先证明了标准的 * * 带动量的梯度下降 (Momentum SGD) * * 本身就是一个联想记忆系统。 在公式 （18） 中， 作者展示了动量项 m 可以被视为以下优化问题的解： 这表明动量 m 是一个元记忆模块 (Meta Memory Module), 它学习如何将梯度的历史 “ 压缩 " 进参数 m 中。 然而， 这个基础版本的动量是 ' 无值 (value-less)" 的， 因为它试图将所有梯度方向映射到单位矩阵的方向， 忽略了参数空间在不同维度上的曲率差异 （即梯度的方差或尺度）。

> [图片 49：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了让这个记忆模块更强大 （即接近 Adam), 作者引入了 * * " 更有表现力的关联 (More Expressive Association) 作者修改了联想记忆的目标函数， 不再映射到而是映射到一个预处理矩阵 (value)o 新的优化目标变为： min 〈 mVC()t； (i)T,Pt 〉 由此推导出的更新规则为： m 许 1 = + 一 PiV £ 0 铲；） 这个公式在数学形式上等价于预处理动量梯度下降 (preconditioned Momentum GD)

> [图片 50：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Adam 的本质： Adam 优化器通过二阶矩估计 （） 来调整每个参数的学习率。 在嵌套学习的视角下， 这等价于将 Pt 设定为梯度的逆标准差 （即 1 /）。 因此， Adam 不仅仅是在做梯度下降， 它实际上是一个联想记忆系统， 正在学习如何将 * * " 当前的梯度 (Keys) " 映射到 “ 经过曲率校正的更新值 (values) 0 最优性证明 (Optimality)： 论文指出， 经过 " 微小的修改 “ （指将预处理矩阵 Pt 纳入记忆目标 ), Adam 变成了模型梯度的最佳联想记忆 (Optimal Associative Memory)。 这意味着， Adam 的更新规则 （自适应矩估计） 在数学上是在求解一个优化问题： 寻找一个最优的记忆状态 m, 使得它能最好地压缩梯度流中的 * * 一阶矩 （方向） 和二阶矩 （不确定性 / 曲率） * * 佇审口判凵、 0

线性Attention的前向计算也可以看成两层结构：预训练时，内层网络（注意力模块）可以看成一个线性神经网络，权重V*K^T是一个与序列长度无关的矩阵，其权重高频优化（读入一个token就更新一次），实现对输入token的短期记忆；外层网络（如把输入映射到Q、K、V的层，以及将注意力层输出映射到最终输出的层）的权重以极低的频率优化（仅在预训练时优化一次），记忆通用知识。

> [图片 51：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对于线性 Attention， 其核心更新规则为： 这实际上等价于通过梯度下降优化以下目标： （16） M 衅 1 = 〈 M隔+1， + 1 〉 + IIM 一 （15） 注意， 这里我们定义损失函数为负的点积相似度 £： （原理阐释与推导分析 〕 一 〈 “ 左， 叻， 其梯度正是一 T。 推导： 对目标函数 〈 · M 叻关于 M 求导， 得到省左 T。 如果在梯度下降中减去这个梯度 （假设为了最小化负相似度 ), 或者加上这个梯度 （为了最大化相似度 ), 我们就 = M01d + ．。 这正是线性 Attention 中 KV 缓存的更新方式厶得到 M netV (Hebbian update)o 结论： Transformer 中的线性 Attention 模块， 本质上是一个正在通过梯度下降进行在线训练的优化器， 它的目标是压缩 Key 和 value 之间的映射关系。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 引入一个由权重矩阵 W e 参数化的参考线性模型扒 = VVc, 以及一个包含输入样本和标签坊的训练数据集刀 = { （，） } 1。 学习的目标是最小化平方误差损失：

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
