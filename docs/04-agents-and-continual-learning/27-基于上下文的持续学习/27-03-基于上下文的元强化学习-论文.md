---
title: "27.3 基于上下文的元强化学习（论文）"
source_docx: "第4部分 大模型智能体与持续学习/27.基于上下文的持续学习/27.3 基于上下文的元强化学习（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 27.3 基于上下文的元强化学习（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、元强化学习的基本思想

元强化学习（Meta-RL）的目标不是让智能体掌握某一个具体任务，而是让智能体掌握一种学习策略。通过在大量的不同任务分布p(T)上进行元训练，从而在面对未见过的全新任务时，能够利用极少量的交互数据（Few-shot）实现快速适应。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

一个强化学习任务丆可以被定义为一个马尔可夫决策过程 (MDP)： 其中： 5， 灵： 状态空间和动作空间 （通常假设跨任务共享）。 + 1， (t): 状态转移概率 （不同任务可能不同， 如机器人的摩擦系数变化）。 · （(t): 奖励函数 （不同任务可能不同， 如目标方向变化）。 H： 时间视界 (Horizon)0

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Meta-RL 假设存在一个任务分布丆）。 我们的目标是找到一组元参数 (Meta-parameters) 六使得智能体在从分布中采样出的新任务丆 ～ 丆） 上， 经过 K 步更新 （或 K 条轨迹的适应） 后， 累积奖励最大化。 数学上， 最大化目标函数 J(O)： max J(0)： 乥丆、 丆） 其中， 九是由 O 参数化的适应过程 (Adaptation Procedure)， 丆是轨迹。

## 二、基于上下文的元强化学习概述

我们已经知道，Transformer本质上等价于在上下文上作梯度下降，它本身就是一个持续学习的优化器。因此，对基于Transformer的大模型进行强化学习训练，本质上就是一个元学习过程，只不过没有显式地求二阶Hessian矩阵。因此，可以基于上下文设计元强化学习算法。

另一种思路是适应新任务本质上是一个部分可观察马尔可夫决策过程（POMDP）中的推断问题。在这种范式下，策略网络的权重在面对新任务时是固定不变的（不需要梯度下降）。相反，智能体会将过去的交互经验（状态、动作、奖励的序列）编码为一个隐变量（Latent Context/Representation）。策略网络只要通过将此上下文作为额外的输入条件，就可以隐式地改变其在不同任务中的行为。

下面介绍基于上下文的元强化学习算法。这些算法虽然在现在鲜有被直接提及，但却隐式地体现在现在的大模型中。

## 三、RL2

### （一）基本原理和架构

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在传统的单任务 RL 中， 智能体面对环境只接收当前状态 sto 但在 RL2 中， 为了让网络知道自己刚刚的 " 试错 " 是否有效， 它必须拥有工作记忆 (working Memory)0

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在传统的单任务 RL 中， 智能体只接收当前状态 sto 但在 RL2 中， 为了让网络知道自己刚才 “ 试错 " 的结果， 网络在时间步 t 的输入不仅包含当前状态， 还包含上一步的动作一 1、 上一步的奖励 0 一 1 以及当前是否为回合结束的标志一 1。 一 1， 一 1， 一 1）。 RL 使用循环神经网络 （如 LSTM 或 GRU) 来处理设输入向量为这个序列： 策略直接由隐藏状态生成： 圃@在） 在这里， 发生了两个时间尺度的学习： 慢速 RL （外环 Meta-training)： 也是权重更新的过程。 在成千上万个不同的任务上， 使用策略梯度算法 （如 PPO 或 TRPO) 来更新 RNN 的网络权重快速 RL （内环 Adaptation)： 也是隐藏状态演化的过程。 在面对一个全新任务时， 权重 O 保持固定不动。 智能体通过与环境交互， 不断将 @， a, r) 喂给 RNN, RNN 的隐藏状态随之更新。 这个持续演化的实际上充当了该任务的工作记忆 (working Memory) 或信念状态 (Belief State), 从而瞬间改变了策略输出。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

慢速 RL （外环 Meta-training)： 也是权重更新的过程。 在成千上万个不同的任务上， 使用策略梯度算法 （如 PPO 或 TRPO) 来更新 RNN 的网络权重快速 RL （内环 Adaptation)： 也是隐藏状态演化的过程。 在面对一个全新任务时， 权重 O 保持固定不动。 智能体通过与环境交互， 不断将 @， a, r) 喂给 RNN, RNN 的隐藏状态随之更新。 这个持续演化的实际上充当了该任务的工作记忆 (working Memory) 或信念状 (Belief State)

### （二）工作流

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

采样任务： 从任务分布丆） 中采样一批任务。 1 ． 构建序列 (Trials)： 对于每一个任务， 执行 N 个回合 (Episodes), 这 N 个回合拼接成一 2 ． 个超长的序列 （称为一个 Trial)。 关点： 在同一个 Trial （同一个任务丿内的不 F]Episode 之间， RNN 的状态九不重置， 而是保留下来跨回台传递。 只有在切到另一个全新任务隐 0 次态九才会被潸零。 计算回报： 累计整个 Trial 中所有回合的总奖励。 3 ． 4 ． 权重更新： 将这些长序列数据输入到 TRPO 或 PPO 算法中， 最大化期望总奖励， 计算梯度并更新 RNN 权重

### （三）实现探索与利用平衡的机制

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

RL2 实现探索与利用的机制是完全隐式的 (lmplicit), 由 RNN 在元训练中自己 " 悟 ' ' 出来。 隐式的结构化探索： 假设任务是在迷宫中寻找一个随机放置的目标。 在 Trial 的第 1 个回合 （探索期 ), 由于是空的， RNN 会输出一套 " 扫街 “ 策略 （比如系统性地遍历每个房间）。 在这个过程中， 它虽然没得到奖励， 但它记录了哪些地方去过 （通过中的 a 和 5 更新到五向利用的自动切换： 一旦在某个时间步碰到了目标， 收到了正奖励一 1 > 0， 这个信号就会输入给 RNN 并大幅改变隐藏状态。 在随后的第 2 到第 N 个回合中 （利用期）， 包含了 " 目标位置记忆 " 的隐藏状太五厶直接驱使策略以最短路径走向目标。 /、 t 厶元目标驱动： 因为外环 PPO 优化的目标是最大化整个 Trial （N 个回合） 的总收益， 这就迫使 RNN 必须在最初的几个回合中采取高效率的试错行动 （探索）， 以便在后续的回合中能够一直拿到最高分 （利用）。

### （四）Kimi Delta Attention中隐式的RL2

Kimi Delta Attention维护的线性注意力层本质上就可以视为RNN，从而本质上来说，在RL训练时这就是一个RL2算法：隐状态在每一个token生成（每一个epoch）后不会被清空，直到任务完成才会被重置。它的效果优于传统的RL2，这是因为：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在原始的 RL2 论文中， 作者使用的是 LSTM 或 GRU, 但这带来了严重的瓶颈， 而 KDA 能有效解决这些痛点： Meta-training 阶段的并行化加速： LSTM 在训练 （外环） 时必须使用 B PTT （沿时间反向传播 ), 只能一步步串行计算。 而 KDA 作为线性注意力， 在训练时可以通过 * * 关联形式 (Associative Form) * * 或 Chunkwise 并行计算机制， 像 Transformer 一样并行处理整个长序列。 这极大地缩短了 Meta-RL 中极其漫长的外环训练时间。 细粒度长程记忆控制： 传统 LSTM 的门控是粗粒度的。 K DA 引入了通道级 (Channel-wise) 门控， 让每个隐状态的维度独立决定记忆衰减。 这意味着网络可以学习到： 用某些通道去锁死 " 目标全局坐标 " 这种绝对不能遗忘的信息 （门控值逼近 1), 用另一些通道去记录 " 刚才走过哪个路口 " 的短期信息 （快速衰减）， 从而在漫长的 Trial 中保持记忆的纯净度。

本质上来说，现在的Agentic RL就可以视为RL2的一种演进：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

隐式到显式： RL2 将过去回合的试错经验压缩在 RNN 的隐状态或 KDA 的 St 矩阵中； 而 Agentic RLVR 将带有环境反馈的试错经验 （" 我刚才试了这段代码， 服务器报了 404， 所以我现在换个 API 试试 "） 以显式自然语言 Token 的形式写在了上下文窗口 ()V Cache) 中。 学会探索 (Learning to ExpIore)： 在 Agentic RLVR 的 Meta-training （外环权重更新） 过程中， 大模型逐渐学会了一种通用的、 面对真实世界未知环境的探索策略。 比如， 当接到一个陌生任务时， 它学会的第一步不再是目目写代码， 而是输出 <action> 1 s -la </action> 或 <action> cat README ． md </action> 来探氵则环境状态。 跨回合 (Cross-episode) 泛化： 只要环境的反馈被记录在上下文中， 模型在测试阶段 （权重 O 冻结） 就能通过阅读前几步的真实报错 (ot), 利用注意力机制改变随后的动作 （+ 1 ), 实现与真实世界的动态博弈和自适应修正。

## 四、PEARL

### （一）核心架构

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 概率上下文编码器 (Probabilistic Context Encoder) 在面对一个任务时， 智能体会收集到一系列交互经验 (Transitions), 这些经验被称为上下文 (Context), 记为 CI:N 51， 01， TI, 51， · ·， SN,ON,TN,SN。 为了不依赖 RNN 那样严格的序列顺序， PEARL 设计了一个置换不变 (permutation-invariant) 的编码器 q+(zlc)o 它假设上下文中的每一次状态转移都是独立提供任务信息的。 因此， 编码器将每个转移元组 @， a, 50 通过多层感知机 (MLP) 映射为一个独立的高斯分布， 然后将所有分布连乘起来， 得到最终的后验隐变量分布： 乥囗曰 ． ^ · 其中， z 就是代表当前任务特征的连续隐变量， 严和 0 分力刂辅出均值和方差的网络。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · 条件 Actor-Critic 网络 (Context-conditioned Actor-Critic) PEARL 的底层强化学习算法通常基于 SAC (Soft Actor-Critic)。 在推断出隐变量 z 后， z 会作为额外的输入， 与当前状态 5 一起喂给策略网络和价值网络： · Actor 网络 （策略）： （s z Critic 网络 (Q 值）： Qo(s,a z 这意味着， 网络权重 O 在不同任务之间是完全共享且固定的， 策略的改变仅仅依赖于前向传播时输入的 z 的变化。

### （二）损失函数

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 编码器的 KL 散度正则化 PEARL 强制后验分布 w(zlc) 逼近一个先验为了防止编码器对某一个具体的上下文序列过拟合， 分布 p(z) （通常设为标准正态分布 （0， 1))0 这通过最小化 KL 散度来实现： 其中是调节信息瓶颈强度的超参数。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · Critic 与 Actor 的损失函数 Critic 的更新目标是最小化引入了 z 的贝尔曼残差 (Bellman Error)： s， a， 7、 湖）、 B ZA-'Clé 这里的目标价值函数包含了 SAC 特有的熵最大化项 (a 为温度系数）： Actor 的更新目标则是最大化期望 Q 值并同时最大化策略熵： cactor@） = [a log T9(aIs, z) 一 ‰@ a, z) SA'B z ～， a ～ 丌 9 2 在元训练期间， 来自 Critic 损失的梯度会反向传播流经 z, 从而更新编码器参数迫使编码器提取那些 “ 对预测奖励和价值最有用 " 的上下文佇串口 / 《 凵、 0

### （三）训练工作流

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

阶段一： Meta-training （元训练） 该阶段的目标是学习到一个通用的编码器 @ 和策略网络。 1 ． 初始化： 初始化网络参数 O, 为每个训练任务丆初始化空的重放缓冲区昼。 2 ． 数据收集循环： 从任务分布丆） 中采样一个任务不初始化当前任务的上下文 ct = 0。 后验采样探索： 在每个 Episode 开始前， 使用编码器计算分布并采样隐变量 z ～ q+(zlct)o 在这个 Episode 内， z 保持固定不变。 智能体使用策略 （国 5， z) 与环境交互， 收集数据 @， a, r, 50 并将其存入缓冲区中。 更新上下文 c ct IJ {(s,a,r, 的 }。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 ． 网络优化循环： 随机采样一个批次 (Batch) 的任务。 对于每个任务， 从其对应的昼中随机抽取两组不相交的批次数据： batch · 上下文批次 c · 送入纟扁码器， 推断出 z 的分布。 batch ． RL 训练批次 b ． 用于计算 SAC 损失。 从分布中采样 z， 计算 Ccritic， £ “ “ 和 £ 人加使用梯度下降法联合更新参数和

### （四）Agentic RL本质上是PEARL等Context-based Meta-RL算法的演进

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

无论是 PEARL 还是 Agentic RL, 它们解决问题的核心数学公式是完全等价的： 在测试阶段冻结网络权重 O， 依靠不断增长的历史交互上下文 c， 来推断当前任务并输出最优动作 ao · Context-based Meta-RL （如 PEARL): 策略网络的形式为 （， C1:t-1)0 其中， 上下文 c = { （· · } 是一堆交互 51， 01， TI, 52， · 经验。 PEARL 使用一个纟扁码器将 c 压缩成一个连续的隐变量 z， 然后 Actor 根据 z 输出动 Agentic RL （基于 LLM): 策略网络 (LLM) 的形式为 (atlPromptt)0 这里的 Promptt 就是上下文。 它忠实地记录了历史轨迹： 用户指令 （50）、 模型调用的工具代码 （a “ 一 1）、 环境返回的终端报错或网页内容 （5 “）、 以及客观校验反馈 (rl•t-l)。 LLM 通过自注意力枧制 (Self-Attention) 处理这段 Prompt， 直接输出下一步的动作。 两者在数学上是同构的， 只是状态载体发生了变化： 从隐式、 不可读的高维连续向量 (z 或 ht), 变成了显式、 人类可读的离散 Token 序列 ()V Cache 中的文本）。

具体来说：

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

上下文记忆 (Context c)： 1 ． PEARL: 记忆是一个无序的经验回放集合 c = 5 為 Oi, 5 + 1 = 1。 LLM Agent: 记忆是 * * 上下文窗口 ℃ ontext Window) * * 中拼接的 Token 序列 （包含了 Syste m Prompt、 之前的动作命令、 环境返回的真实终端报错以及步骤得分）。 2 ． 任务推断与隐变量 (Encoder & Latent z)： PEARL: 使用基于 MLP 的概率编码器， 输出一个连续的高斯分布隐变量 z ～， 2）。 z 代表了网络对当前陌生任务的 " 信念 "。 LLM Agent: LLM 的多头自注意力机制 (Self-Attention) 充当了天然的编码器， 而深层的 KV Cache 构成了高维隐状态。 更进一步， 在带有显式推理的 Agent 中， 模型通过生成 “ 反思文本 (RefIection / Chain-of-Thought)", 将对环境的推断显式地输出为了人类可读的 ‰ 策略网络 (Actor）： 3 ． PEARL: 策略是 （国 5， z)， 通过连续动作空间的强化学习算法更新。 LLM Agent: 策略是自回归语言建模头 (alPrompt, Reflection)， 它在词表分布上进行温度采样， 生成具体的 Python 代码或 Bash 命令。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

虽然本质相同， 但 Agentic RL 代表了 Meta-RL 演进的终极形态， 它解决了传统 Meta-RL 的几个致命痛占 · 1 ． 极度泛化 （从 “ 白板 " 到 “ 世界模型先验 "）： 传统的 Meta-RL 是从零开始 (Tabula Rasa) 在特定环境中 “ 元训练 " 的， 它只能泛化到非常相似的同分布任务。 而 Agentic LLM 已经通过海量 2 ． 3 ． 互联网数据获得了极强的先验知识 （世界模型）。 它不需要再从头通过试错去学习 " 什么是 404 Error" 或 " 什么是 Python 语法 “， 它可以直接调用预训练概念， 实现真正意义上的 Zero- shot 或 Few-shot 跨域泛化。 无损的长程信度分配： 无论是 RL 的 RNN 还是 PEARL 的高斯分布， 在压缩几千步的 Trial 上下文时都会发生严重的信息丢失。 而 Agentic RL 依托现代大模型的 KV Cache （或是你之前关注的 KDA 线性注意力机制 ), 能够以极高的保真度保留极其久远的关键试错细节。 显式的思维链 (Chain-of-Thought) 干预： Agentic RL 可以在观测和动作 a 之间， 插入显式的推理想法。 这种内部语言空间使得 " 探索逻辑 " 变得可解释， 并且可以被外部规则 （如 RLVR) 进行精准的组内优势 (Advantage) 排序。

## 五、LLM Agent with Meta-RL（LAMER）

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 跨回合训练框架 (Cross-episode Training Framework) LAMER 吸收了 RL2 的宏观精髓， 将环境交互的视距提升到了 Trial （试验） 级别。 一个完整的 TriaI 包含多个独立的 Episode （回合）。 奖励重塑： 算法优化的目标不再是单个回合的收益， 而是跨回合折扣回报 (Cross-episode discounted return)o 乥 7 一丑丆。 叩 （丆）， 丆、 丌 9 其中 K 是 Trial 内的总回合数， 蛰是第个回合的总奖励。 探索的涌现： 这种严苛的奖励设计迫使大模型学会 “ 放长线钓大鱼 "。 在早期的 Episode 中， 模型会主动放弃眼前的安全低分， 采取一些大概率会导致当前回合失败 （但能探明环境机制或扌非除错误代码） 的探索性动作。

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · 结合自我反思的上下文策略适应 (ln-context POlicy Adaptation via SeIf-RefIection) 这就是 PEARL 中生成推断隐变量 z 的最完美文本化体现。 LAMER 在测试 （部署） 阶段不进行任何网络参数的梯度更新。 相反， 在每一个 Episode 结束后 （比如代码跑崩了 ), LAMER 会强制 LLM Agent 根据刚才积累的交互日志， 生成一段纯文本的自我反思 (Reflection)0 这段反思文本会深入分析错误归因 （例如： " 我尝试向数组里 Append 字典， 但引发了类型错误， 因为接口需要 JSON 字符串 " ), 并提出下一个回合的改进策略。 这个生成的 Reflection， 会被作为强有力的先验上下文， 直接 Prefix （前置拼接） 到下一个 Episode + 1 的 Prompt 中。 依靠这种纯文本的流转， Agent 的策略在下一个回合中发生了一次无需算力开销的 RL2 飞

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
