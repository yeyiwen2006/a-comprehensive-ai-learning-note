---
title: "26.3 基于注意力匹配的KV压缩（论文）"
source_docx: "第4部分 大模型智能体与持续学习/26.上下文与记忆/26.3 基于注意力匹配的KV压缩（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 26.3 基于注意力匹配的KV压缩（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、提出背景

在长上下文场景中，将长上下文总结为短文本虽然常用，但会丢失大量信息；在潜在空间训练紧凑KV缓存的方法（如Cartridges方法）能保持高性能，但需要昂贵的端到端梯度优化，耗时极长，通常需要数小时GPU算力才能压缩一个上下文。于是，我们希望不依赖于端到端的输出概率优化，而是直接优化紧凑的键和值，使其在每一个KV头上，对于每一个query，尽可能复现原始的“注意力输出”和“注意力质量（Attention Mass）”。

## 二、核心思想

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

该方法的核心思想是： 不依赖于端到端的输出概率优化， 而是直接优化紧凑的键和值， 使其在每一个 KV 头上， 尽可能复现原始的 “ 注意力输出 " 和 “ 注意力质量 (Attention Mass)" 假设原始上下文有 T 个 token, 其键和值为 K, V e ]RTxd, 我们要将其压缩为长度为 t （其中 t < T) 的紧凑缓存 0， 0 e。 为了补偿压缩后注意力总量的流失， 作者引入了一个每个 to ke n 标量偏差 (Scalar Bias) e IRt。 给定一组参考查询 (Reference Queries) q e 我们需要同时满足以下两个数学目标： 1 ． 2 ． 匹配局部注意力输出 (Local Attention Output) exp(qKT)V E 厂 1 exp(qKT) 匹配注意力质量 (Attention Mass) exp(qCkT + 司 0 E 厂 1 exp(q(Ck)T + 乥 exp(qKJ） 乥 （q （0 +

（1）

这是为了对于query，经过上下文聚合后输出的结果不变。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

该方法的核心思想是： 不依赖于端到端的输出概率优化， 而是直接优化紧凑的键和值， 使其在每一个 KV 头上， 尽可能复现原始的 “ 注意力输出 " 和 “ 注意力质量 (Attention Mass)" 假设原始上下文有 T 个 token, 其键和值为 K, V e ]RTxd, 我们要将其压缩为长度为 t （其中 t < T) 的紧凑缓存 0， 0 e。 为了补偿压缩后注意力总量的流失， 作者引入了一个每个 to ke n 标量偏差 (Scalar Bias) e IRt。 给定一组参考查询 (Reference Queries) q e 我们需要同时满足以下两个数学目标： 1 ． 2 ． 匹配局部注意力输出 (Local Attention Output) exp(qKT)V E 厂 1 exp(qKT) 匹配注意力质量 (Attention Mass) exp(qCkT + 司 0 E 厂 1 exp(q(Ck)T + 乥 exp(qKJ） 乥 （q （0 +

（2）

这是为了确保被压缩后的旧上下文，在未来遇到新输入的token时，依然能保持与原始完整上下文同等的“话语权”或“权重占比”。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

传统的上下文压缩方法 （如 Token 丢弃、 KVzip 等） 通常只是从 T 个 token 中挑出个重要的 to ke n 留下来 < 丆）。 因为指数函数 exp@) 永远大于 0， 所以保留个 to ke n 的 Mass 必然会系统性地小于原始 T 个 to ke n 的 ass： 乥 e （q （0 加 < 乥 exp(qKJ）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当你用这种粗暴的压缩方法后， 一旦系统追加了新的 to ke n (Kfixed)， 由于压缩块的 Mass 变小了， 它在上述加权混合公式中的占比就会骤降。 模型会不成比例地把注意力过度集中在 “ 新 token" 上， 从而在数学层面上被动地 “ 遗忘 " 或 “ 忽视 " 了被压缩的历史上下文。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

《 一个通俗的类比： 股东大会代表制原始情况： 公司有 ℃ 00 个小股东 （原始丆个 token), 每人 1 票， 总投票权 (Attention Mass) 为 ℃ 00。 传统压缩 （丢弃法）： 选出 50 个核心股东代表去开会， 其他人不去了。 如果按人头算， 旧股东阵营的总投票权骤降到了 50。 当有新投资人 （新 token) 带着 ℃ 0 票入场时， 旧股东阵营的话语权就被彻底稀释了。 匹配注意力质量 （论文做法）： 同样选出 50 个代表 （挑选 0）， 但是赋予他们 “ 代理投票权 " （标量偏差。 比如代表身上背了 20 个人的票， 代表 B 身上背了 30 个人的票。 这样虽然人少了， 但旧阵营的总票数依然是 ℃ 00 (Mass 匹配）。 当新投资人进来时， 旧阵营的决策影响力 （局部注意力输出） 和话语权占比 （混合权重） 依然完美保持不变。

## 三、算法流程

共分3个步骤：采样参考查询（Qref）；选择保留的键（Ck）并拟合偏差（β）；得到值（Cv）。

### （一）Step1：采样参考查询（Qref）

对于不同的查询Q，模型在用这些Q查询原文时，为了保证语法的连贯性、指代的准确性以及语义的连贯，会同时把注意力分散到历史中的多个关键token上。那些在不同Q下多次获得较高注意力分数的关键词，就是高价值的、需要保留的信息。我们无法预知未来的用户提问，也就不知道未来的Q分布，故我们希望诱导模型生成高质量的Q。有以下两种方法：

1.Repeat-prefill（重复预填充）

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

它的具体做法是构造这样一个特殊的输入序列送给模型： " { 原始长文本 C} 接下来， 请一字不差地重复前面的文本： { 再次输入原始长文本 C}"

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

内部重构时的 “ 疯狂回看 "： 当模型在预填充 (Prefill) 阶段处理到第二个 {C} 时， 为了准确预测下一个词， 当前 toke n 的 Q 向量必须极度精准地去匹配第一个 {C} 中对应位置的 K 向量。 这就好比你在抄写一段长文， 你的眼睛 (Query) 会不断地、 强烈地去回看原稿 （Key)。 提取参考查询： 这个 " 回看 " 过程中产生的 Q 向量， 完美代表了 * * " 当模型真正需要从长上下文中精确提取细节时， 它会发出怎样的查询请求 " * *。 提取出来： 在代码层面， 我们只是在模型前向传播 （Fo rwa rd Pa (s) 经过这些特定 token 时， 把某些层注意力头里计算出的 Q 矩阵直接拦截并保存下来。 这些真实且高质量的 Q 就构成了我们的 " 参考查询集 (Qref)", 作为后续压缩算法的 " 靶子 "。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当大模型在输出哪怕是一个完全 " 照抄 " 的词时， 它为了保证语法的连贯性、 指代的准确性以及语义的连贯， 其内部的 Query 向量会同时把注意力分散到历史中的多个关键节占上举个例子： 原文本是 " 苹栗公司的 CEO 库克在今天的发伤会上丿掏出了一部全新的手胡。 " 当模型在 " 重复 " 生成 * * " 手机 " 这个词时， 它的 Q 向量不仅会去看原文本里的 “ 手机 "， 还会分配大量的注意力去看 ' 苹果公司 "、 " 库克 " 和 “ 发布会 " * *。 因为模型需要这些上下文来确定当前语境。 结果就是： 像 " 苹果公司 "、 " 库克 “ 这样的核心实体词 （Key), 会在模型重复整段话的过程中， 被无数个后续生成的词的 Q 反复 “ 回看 " 和依赖。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

内部重构时的 “ 疯狂回看 "： 当模型在预填充 (Prefill) 阶段处理到第二个 {C} 时， 为了准确预测下一个词， 当前 toke n 的 Q 向量必须极度精准地去匹配第一个 {C} 中对应位置的 K 向量。 这就好比你在抄写一段长文， 你的眼睛 (Query) 会不断地、 强烈地去回看原稿 （Key)。 提取参考查询： 这个 " 回看 " 过程中产生的 Q 向量， 完美代表了 * * " 当模型真正需要从长上下文中精确提取细节时， 它会发出怎样的查询请求 " * *。 提取出来： 在代码层面， 我们只是在模型前向传播 （Fo rwa rd Pa (s) 经过这些特定 token 时， 把某些层注意力头里计算出的 Q 矩阵直接拦截并保存下来。 这些真实且高质量的 Q 就构成了我们的 " 参考查询集 (Qref)", 作为后续压缩算法的 " 靶子 "。

2.Self-study（自学/自我提问）

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这是一种基于合成数据的方法。 我们将原始上下文 {C} 喂给模型， 并给它下达一些指令 （例如： “ 总结关键事实 " 或 " 根据上文提出 3 个问题 " ), 让模型自己生成对话或问答。 在模型 * * 生成响应 (Decoding) * * 的过程中， 提取其注意力头产生的查询向量。 这模拟了真实的下游推理任务中查询向量的分布。

### （二）Step2：选择保留的键（Ck）并利用（2）式拟合偏差（β）

只有一定数量的token会被保留，它们的键构成Ck。这一步选择保留哪些token（即保留哪些键）。有以下两种方法：

1.保留最高注意力键并拟合偏差

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

计算局部注意力分数： 针对每一个参考查询佑， 计算其在所有原始键 K 上的 Softmax 注意力分布： 佑 = softmax(qtKT) e 跨查询聚合打分： 对于第 j 个键， 它在 “ 个不同的查询下会得到 “ 个分数 ·， 鰨孬）。 通过计算这些分数的均方根 (Root Mean Square, RMS), 得到该键的全局重要性得分力。 排序与截断： 根据力对所有原始键进行降序排列， 直接提取 Top-t 个键构成 0。

在选择好一定数量键后，为保证注意力质量不变，根据（2）式，需要加上偏差。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

我们的根本目标是让压缩后的缓存， 在任意查询佑下， 其 " 注意力质量 ' 等于原始缓存的质量

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了用少量的乯个键 （0） 凑出原本庞大的质量 mi, 作者为每个选中的键引入了一个待学习的标量偏差 (Scalar Bias)。 近似等式变为：

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

根据指数函数的性质 exp(A + B) = exp(A) exp(B)， 我们可以将上式拆解： 乥 exp(ßj) · exp(qt(Ck)Tj)

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

此时， 我们定义一个新变量 = exp()J）。 由于任何实数的指数都必定大于 0， 所以这里的隐含了一个严格的物理约束： 它必须是非负的 （屿 > 0 或放宽至 > 0）。 等式变形为：

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们将所有参考查询上的方程联立， 写成矩阵形式。 令矩阵丿，，， 目标向量为 mo 上面的方程组就可以简洁地表示为： 之 4， = exp(qt(Ck)Tj)， 权重向量为了找到最能让等式成立的 w, 我们最小化等式两边的欧几里得距离 ()2 范数 ), 并带入前面推导出的非负约束条件。 这就构成了经典的 NNLS 目标函数： min 目@诧 a ] w 一 w>0 7 2

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

· 固定选好的 0 后， 目标是求解权重向量 w （其中 = exp(ßj)) 以匹配目标注意力 ． 质更 7。 这被转化为一个非负最小二乘 (NNLS) 优化问题： minw20 目 c e “ 引 Aw 一 m 《 2。 求解后取对数得到 = log()j）。 直观上， 代表保留下来的紧凑键 " 代理 " 了多少个原始键的注意力质量。

但这种“选择得分最高的键”的策略的盲区在于忽视了键与键之间的相关性和重叠度。假设组建一支5人的篮球队，如果按“个体最高得分”来选，你可能会选出联盟里得分最高的5个中锋。他们每个人的绝对实力都极强（注意力分数极高），但把他们放在一个队里，由于位置和技能高度重合（信息冗余），球队根本无法运转。

在LLM的注意力机制中，如果原文中有两个表达相似概念的词，或者同一句话中的两个强关联 token，它们可能会在参考查询下同时获得极高的注意力分数。如果采用最高分数策略，算法会把这两个高度冗余的键都选进来，浪费了原本就极其稀缺的压缩容量。

2.正交匹配追踪（Orthogonal Matching Pursuit, OMP）

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设你想调配出一种极其复杂的特定颜色 （目标注意力质量 m), 但你手头有上万种基础颜料 （原始的所有键）， 而由于成本限制， 你最终只能挑出其中的种来混合 （预算个紧凑键）。 OMP 采用的是一种贪心且不断自我修正的策略： 1 ． 2 ． 贪心寻找： 每次我都去盯着当前调出来的颜色和目标颜色之间的 " 色差 （残差） "。 在剩下的颜料里， 挑出一种与这个 “ 色差 " 最接近、 最能弥补当前差距的颜料。 正交投影 （重拟合）： 每次挑入新颜料后， 不能只管新加的， 而是要把目前挑出来的所有颜料重新按比例混合一次 （重新计算权重 w), 以确保当前的混合方案在数学上是 " 最优解 " （即残差与当前所选特征空间正交， 误差最小）。

这样，我们每次都取在语义上互补的键，就能尽可能覆盖全面的信息，不出现重复。

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 4 ． 5 ． 初始化阶段： 计算原始上下文的 " 目标注意力质量向量 m。 设定初始残差 r = m （一开始差距最大 ), 并将 " 已选键集合 " S 设为空集 0。 计算相关性 (Compute Correlation)： 遍历所有目前还没有被选中的原始键 j SO 计算当前的残差 r 与该键对应的特征列万之间的内积： = 丿。 内积越大， 说明这个键越能解释剩下的残差。 贪心选择 (Greedy Selection)： 在所有未选的键中， 找出相关性得分最高 （也就是最对口） 的那个键 j*， 把它正式拉入已选集合 S 中： 重拟合权重 (Refit Weights via NNLS): 这一步是 OMP 的灵魂。 现在 S 里面有了新成员， 我们需要对 S 中所有的键重新分配 " 代理权重 （标量偏差） "。 我们通过求解一个非负最小二乘 (NNLS) 问题， 找到一组非负权重 w, 使得它们组合起来最接近目标 m： 一 arg min ．， 一 7 2 w>0 更新残差 (Update Residual)： 用目标向量减去当前已拟合出的结果， 计算出最新的残差： T 一 m 一 su,'

这也可以视为从m向已有键构成的超平面作投影，找到残差。

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 4 ． 5 ． 初始化阶段： 计算原始上下文的 " 目标注意力质量向量 m。 设定初始残差 r = m （一开始差距最大 ), 并将 " 已选键集合 " S 设为空集 0。 计算相关性 (Compute Correlation)： 遍历所有目前还没有被选中的原始键 j SO 计算当前的残差 r 与该键对应的特征列万之间的内积： = 丿。 内积越大， 说明这个键越能解释剩下的残差。 贪心选择 (Greedy Selection)： 在所有未选的键中， 找出相关性得分最高 （也就是最对口） 的那个键 j*， 把它正式拉入已选集合 S 中： 重拟合权重 (Refit Weights via NNLS): 这一步是 OMP 的灵魂。 现在 S 里面有了新成员， 我们需要对 S 中所有的键重新分配 " 代理权重 （标量偏差） "。 我们通过求解一个非负最小二乘 (NNLS) 问题， 找到一组非负权重 w, 使得它们组合起来最接近目标 m： 一 arg min ．， 一 7 2 w>0 更新残差 (Update Residual)： 用目标向量减去当前已拟合出的结果， 计算出最新的残差： T 一 m 一 su,'

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

6 ． 循环迭代： 重复步骤 2 到步骤 5， 每轮挑一个键， 直到集合 S 中塞满了我们预设的个键为止。

### （三）Step3：得到值（Cv）

现在β和Ck都确定了。由于加上了偏差β，为保证注意力输出不变，根据（1）式，被选中的token的Cv不会等于其原来的V值。对于第i个测试查询，注意力输出yi满足：

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

exp(qtKT)V 乥 exp(qtKj) 在我们的压缩模型中， 既然 0 和已经固定， 那么对于查询， 它在压缩键上产生的归一化注意力权重也就彻底固定了， 我们把它记为 e 对 exp(qiCkT + 司 E 厂 1 exp(qt(Ck)T + 我们要寻找一个新的紧凑值矩阵 0 e ]Rtxd, 使得它在权重的加权下， 输出的结果尽可能等于原始目标：

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

exp(qtKT)V 乥 exp(qtKj) 在我们的压缩模型中， 既然 0 和已经固定， 那么对于查询， 它在压缩键上产生的归一化注意力权重也就彻底固定了， 我们把它记为 e 对 exp(qiCkT + 司 E 厂 1 exp(qt(Ck)T + 我们要寻找一个新的紧凑值矩阵 0 e ]Rtxd, 使得它在权重的加权下， 输出的结果尽可能等于原始目标：

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

那么， 整个优化问题就变成了一个极其标准的普通线性回归 (ordinary Least Squares, OLS)： 我们要最小化它的 L2 误差： 0 丫 0 = arg min 目 [ c 忉 r 引 0 熟悉线性代数的话， 可以直接写出它的解析解 （正规方程）： 0 = （xT 尸 T 丫

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

那么， 整个优化问题就变成了一个极其标准的普通线性回归 (ordinary Least Squares, OLS)： 我们要最小化它的 L2 误差： 0 丫 0 = arg min 目 [ c 忉 r 引 0 熟悉线性代数的话， 可以直接写出它的解析解 （正规方程）： 0 = （xT 尸 T 丫

解析解：

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

那么， 整个优化问题就变成了一个极其标准的普通线性回归 (ordinary Least Squares, OLS)： 我们要最小化它的 L2 误差： 0 丫 0 = arg min 目 [ c 忉 r 引 0 熟悉线性代数的话， 可以直接写出它的解析解 （正规方程）： 0 = （xT 尸 T 丫

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 该方法的核心思想是： 不依赖于端到端的输出概率优化， 而是直接优化紧凑的键和值， 使其在每一个 KV 头上， 尽可能复现原始的 “ 注意力输出 " 和 “ 注意力质量 (Attention Mass)" 假设原始上下文有 T 个 token, 其键和值为 K, V e ]RTxd, 我们要将其压缩为长度为 t （其中 t < T) 的紧凑缓存 0， 0 e。 为了补偿压缩后注意力总量的流失， 作者引入了一个每个 to ke n 标量偏差 (Scalar Bias) e IRt...
- 共分3个步骤：采样参考查询（Qref）；选择保留的键（Ck）并拟合偏差（β）；得到值（Cv）。
- （一）Step1：采样参考查询（Qref）
- 内部重构时的 “ 疯狂回看 "： 当模型在预填充 (Prefill) 阶段处理到第二个 {C} 时， 为了准确预测下一个词， 当前 toke n 的 Q 向量必须极度精准地去匹配第一个 {C} 中对应位置的 K 向量。 这就好比你在抄写一段长文， 你的眼睛 (Query) 会不断地、 强烈地去回看原稿 （Key)。 提取参考查询： 这个 " 回看 " 过程中产生的 Q 向量， 完美代表了 * * " 当模型真正需要从长上下文中精确提取细节时， 它会发出怎样的查询请求 " * ...
- 计算局部注意力分数： 针对每一个参考查询佑， 计算其在所有原始键 K 上的 Softmax 注意力分布： 佑 = softmax(qtKT) e 跨查询聚合打分： 对于第 j 个键， 它在 “ 个不同的查询下会得到 “ 个分数 ·， 鰨孬）。 通过计算这些分数的均方根 (Root Mean Square, RMS), 得到该键的全局重要性得分力。 排序与截断： 根据力对所有原始键进行降序排列， 直接提取 Top-t 个键构成 0。
- 我们将所有参考查询上的方程联立， 写成矩阵形式。 令矩阵丿，，， 目标向量为 mo 上面的方程组就可以简洁地表示为： 之 4， = exp(qt(Ck)Tj)， 权重向量为了找到最能让等式成立的 w, 我们最小化等式两边的欧几里得距离 ()2 范数 ), 并带入前面推导出的非负约束条件。 这就构成了经典的 NNLS 目标函数： min 目@诧 a ] w 一 w>0 7 2
- 在LLM的注意力机制中，如果原文中有两个表达相似概念的词，或者同一句话中的两个强关联 token，它们可能会在参考查询下同时获得极高的注意力分数。如果采用最高分数策略，算法会把这两个高度冗余的键都选进来，浪费了原本就极其稀缺的压缩容量。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
