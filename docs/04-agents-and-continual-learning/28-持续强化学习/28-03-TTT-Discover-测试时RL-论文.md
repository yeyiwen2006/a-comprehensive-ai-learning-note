---
title: "28.3 TTT-Discover：测试时RL（论文）"
source_docx: "第4部分 大模型智能体与持续学习/28.持续强化学习/28.3 TTT-Discover：测试时RL（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 28.3 TTT-Discover：测试时RL（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、提出背景

传统的Test-Time Scaling（测试时扩展）方法通常是在冻结的LLM上进行搜索（例如AlphaEvolve使用进化策略提示模型）。冻结的模型无法内化在解决当前难题时的经验和教训，就像一个学生只做题但不消化知识。TTT-Discover在测试阶段对LLM进行RL训练。模型在尝试解决特定问题的过程中，利用产生的数据实时更新权重，从而不仅优化当前的搜索路径，还提升了模型本身解决该问题的能力。

## 二、问题定义和核心思想

论文将一些科学发现定义为一个特殊的强化学习环境，具有以下特征：

1.目标单一：只需要找到一个极值解，而不是优化平均性能。

2.无需泛化：模型只需要解决当前这一个特定的测试问题，不需要泛化到其他问题。（该方法主要适用于本地部署的模型）

3.客观验证：奖励函数（Reward）是连续且可验证的（例如代码运行时间、数学公式的界限）。

TTT-Discover结合了搜索（Search）和学习（Learning）。其核心循环是：从缓冲区重用状态 -> 采样新动作 -> 评估奖励 -> 更新模型权重。

需要注意的是，这不属于元强化学习，因为模型在预训练和后训练中都没有这样的学习。经过此方法在测试时学习的模型，通用性能会下降，也就意味着这种方法并不适用于通用大模型的训练。

## 三、学习方法：熵奖励函数

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Jß(6) = E） [logE S•oretLSe

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 指数加权与 Softmax 算子： 核心在于 e 这一项。 在数学上， log E[eßX] 被称为累积生成函数 (Cumulant Generating Function) 的一种形式， 也被视为 Softmax （软最大值） 算子。 原理： 当 + 0 时， 根据泰勒展开， 该目标退化为标准的期望奖励 E[R] （风险中性）。 当 > 0 时， 指数函数是凸函数， 会对高奖励样本赋予极大的权重， 对低奖励样本极不敏感。 当 + 时， 该目标函数在数学上等价于 maxr 咒 （丆）。 物理意义： 这就好比我们在寻找 " 彩票中奖号码 "。 标准 RL 试图让你买的所有彩票平均回报最高 （但这可能导致你只敢买稳赚不赔的小额理财）； 而熵目标函数迫使模型去通过优化参数， 极大化 " 中大奖 " （极值解） 的概率， 哪怕这意味着其他尝试会彻底失败。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 ． 梯度的重加权机制 (Reweighting)： 该目标的梯度形式揭示了它是如何更新模型的： VeJß(6) = E 丆、 丌。 [Ve log 丌 （小） · 屿 （小） 其中权重 （到 5） 为： 屿 （小 E 丌 [eßR(T)] + 1 原理： 这是一个自归一化的重要性采样 (SeIf-NormaIized lmportance Sampling) 权重。 解释： 模型参数的更新方向不仅仅取决于奖励月的大小， 而是取决于月在指数尺度下的相对占比。 如果某条轨迹的奖励略高于其他轨迹， 在指数放大下， 它的权重会变得非常大， 主导梯度的方向。 这使得模型能迅速锁定那些 " 鹤立鸡群 " 的优秀解。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

自适应 5） 的 KL 约束原理 · 3 ． 论文没有使用固定的而是通过以下约束求解 5）： = 7 这里是由指数奖励加权后的理想分布。 原理 (Tru st Region / REPS)： 这借鉴了相对熵策略搜索 (Relative Entropy Policy Search, REPS) 的思想。 如果太大， 权重分布会坍缩到一个样本上， 导致梯度方差爆炸， 训练不稳定。 如果太小， 权重趋于均匀， 模型无法有效学习到好坏样本的差异。 作用： 通过固定 KL 散度 （即固定信息几何上的 " 步长 " ), 算法动态决定对于探索尚不充分的状态， 保持较小的以防过拟合偶然的噪声； 对于已经发现明显优劣路径的状态， 增大以激进地利用好路径。

## 四、搜索方法：PUCT重用

仅仅依靠上述的训练目标是不够的，还需要一个高效的“搜索”机制来决定每次训练从哪个状态（State）开始。重用缓冲区H中的旧状态（即之前的中间解或完整解）相当于为当前的尝试增加了额外的“时间步”，从而扩展了探索的有效视界。这使得模型能够在已有成果的基础上继续优化，涌现出更复杂的解决方案。选择状态的方法改编自蒙特卡洛树搜索（MCTS）中的UCT（Upper Confidence Bound for Trees）算法。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 + 丆 1 + 规@

### （一）核心思想：从UCB到PUCT

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

总评分 = 价值估计 (Q) + 探索加成 (U) 利用： 选当前最好的探索： 选尝试得少的 UCB 是这一思想的鼻祖， 最初用于多臂老虎机 (Multi-Armed Bandit) 问题。 PUCT 是这一思想在蒙特卡洛树搜索 (MCTS) 中的进化版， 被 AlphaGo 和 TTT-Discover 等现代 AI 系统采用。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在最基础的 UCBI 算法中， 我们假设对所有选项一无所知， 完全依靠统计数据： SCOre ln N 一丿 + c 塢： 平均奖励 （利用）。 ln N · · 随着总次数 N 增加而增加， 随着该选项被选次数屿增加而减少。 局限性： 它假设所有选项初始概率是均等的。 但在复杂的科学发现或围棋中， 我们通过直觉 （或模型） 知道某些方向明显比其他方向更有希望， UCB 无法利用这种 " 直觉 "。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

PUCT (Polynomial UCT) 引入了一个关键项： 先验概率 P(s)0 它允许搜索算法在缺乏数据时， 先 " 相信 " 模型的直觉， 随着数据增多再逐渐转向依赖真实奖励。

### （二）公式解释和改进

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

score(s) = Q(s) + · P(s) · 先验 PUCT 相比 UCB 的两个重大改进： 1 + 丆衰减系数 1 ． 2 ． 引入尸 （5） (Prior)： UCB: 对所有未探索节点一视同仁。 PUCT: 利用尸 （5） 给不同节点不同的 " 起跑线 "。 在 TTT-Discover 中， P(s) 基于缓冲区中的扌非名 (Rank), 即 " 好状态更有可能产生好子代 "。 这意味着算法会优先探索那些模型认为有潜力的路径， 而不是盲目乱撞。 1 多项式级别的探索衰减 · PUCT 的名字来源之一是其探索项的衰减速率。 随着访问次数 5） 的增加， 探索加成项立才会比 UCB 的 ln 衰减得更平滑或更激进 （取决于具体调参）， 使其更适合在深层树结构中快速收敛到高概率路径。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

利用项 (ExpIoitation): Q(s) 1 ． · 定义： Q(s) 一 _ maXs'eChild(s) 咒 （的 （如果是新节点则为月 （5））。 原理： · 在传统中， Q(s) 是子节点的平生率。 但在这里， 为了 “ 发现 "， 我们采用乐观原则 (Optimism)0 如果一个起始代码片段曾经生成过一个极其高效的内核 （哪怕只有一次 ), 那么它就被认为是一个极具潜力的 " 种子 "， 无论它生成过多少垃圾代码。 使用 max 而非 mean 使得搜索能够容忍高方差， 只要上限够高， 就会被优先选择。 2 ． 先验项 (Prior): 尸@） 形式： P(s) (IHI 一 rank(s)), 即基于扌非名的线性分布。 原理： 这是一个启发式引导。 我们假设 " 好种出好苗 "， 即当前奖励高的状态更有可能通过变异产生更好的子状态。 使用扌非名 (Rank-based) 而非绝对奖励数值 (value-based) 是为了鲁棒性。 奖励数值的分布可能极其不均匀 （例如长尾分布 ), 直接归一化会导致概率集中在最好的一个上。 扌非名分布则保证了即使是 Top 一 ℃ 0 的解也有被选中的非零概率， 维持了种群的多样性。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

score(s) = Q(s) + · P(s) · 先验 PUCT 相比 UCB 的两个重大改进： 1 + 丆衰减系数 1 ． 2 ． 引入尸 （5） (Prior)： UCB: 对所有未探索节点一视同仁。 PUCT: 利用尸 （5） 给不同节点不同的 " 起跑线 "。 在 TTT-Discover 中， P(s) 基于缓冲区中的扌非名 (Rank), 即 " 好状态更有可能产生好子代 "。 这意味着算法会优先探索那些模型认为有潜力的路径， 而不是盲目乱撞。 1 多项式级别的探索衰减 · PUCT 的名字来源之一是其探索项的衰减速率。 随着访问次数 5） 的增加， 探索加成项立才会比 UCB 的 ln 衰减得更平滑或更激进 （取决于具体调参）， 使其更适合在深层树结构中快速收敛到高概率路径。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 ． 4 ． 1 + 探索项 (ExpIoration): 1 + 7 小） 符号 · 丆是父节点 （这里指整个缓冲区） 的总访问次数， 5） 是状态及其后代的被访问次数。 原理 (UCB - Upper Confidence Bound)： 这是一项基于计数的不确定性奖励。 随着总训练步数 T 增加， 所有状态的探索加成都会变大 （分子增大）。 但是， 对于那些已经被频繁访问过的状态 5， 分母 1 + 5） 会迅速增大， 压制其得分。 动态平衡： 这迫使算法去关注那些 " 看起来还不错 (Q 和 P 较高） 但还没被怎么尝试过较小） “ 的潜力股。 一旦某个潜力股被尝试了很多次但没有产出新高 Q, 它的得分就会下降， 算法会自动转向其他分支。 探索系数 c 与 Scale · c 控制探索的激进程度， 5 “ = Rma 同一量级。 一 Rmtn 用于将探索项和利用项 （奖励） 拉伸到原理： 确保 Q （奖励值） 和 U （探索加成） 可以直接相加， 避免因为奖励数值过大或过小导致探索项失效或主导搜索。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

TTT-Discover 选择 PUCT 的原因是： 搜索空间太大： 科学发现的解空间几乎是无限的。 标准的 UCB 需要对所有可能得解进行大量的随机尝试才能收敛， 效率太低。 利用长尾分布： 论文发现高奖励的解往往很少 （长尾）。 PUCT 通过 （5） （基于扌非名的先验） 能够迅速锁定那些 " 虽然探索次数少， 但排名很高 " 的潜力股， 从而在有限的计算预算内 (Budget) 找到极值解。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- score(s) = Q(s) + · P(s) · 先验 PUCT 相比 UCB 的两个重大改进： 1 + 丆衰减系数 1 ． 2 ． 引入尸 （5） (Prior)： UCB: 对所有未探索节点一视同仁。 PUCT: 利用尸 （5） 给不同节点不同的 " 起跑线 "。 在 TTT-Discover 中， P(s) 基于缓冲区中的扌非名 (Rank), 即 " 好状态更有可能产生好子代 "。 这意味着算法会优先探索那些模型认为有潜力的路径， 而不是盲目乱撞。 1 多项式级别...

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
