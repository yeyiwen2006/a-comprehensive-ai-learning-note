---
title: "28.4 EvoTune：进化搜索与RL结合（论文）"
source_docx: "第4部分 大模型智能体与持续学习/28.持续强化学习/28.4 EvoTune：进化搜索与RL结合（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 28.4 EvoTune：进化搜索与RL结合（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、核心思想

EvoTune的核心思想是将进化搜索（作为探索策略）与强化学习（作为优化策略）紧密结合。

进化搜索（Search）：负责在程序空间中探索，生成新的训练数据。

强化学习（Learning）：利用搜索发现的高质量程序作为信号，更新LLM的权重，使其生成的分布π向高分区域通过。

这种设计符合Sutton的“Bitter Lesson”观点：搜索生成数据，学习从数据中提炼模式以指导未来的搜索。

需要注意的是，这不属于元强化学习，因为模型在预训练和后训练中都没有这样的学习。经过此方法在测试时学习的模型，通用性能会下降，也就意味着这种方法并不适用于通用大模型的训练。

## 二、算法架构

1.进化搜索

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

该阶段的目标是最大化奖励函数 r@,）， 其中是提示词， 是生成的程序。 = arg [E 程序数据库 (Program Database): 维护一个 " 岛屿模型 " (lsland-based) 的数据库气存储生成的有效程序及其评分。 提示构建： 从数据库中采样高分程序， 利用少样本提示 (Few-shot prompting) 构造， 引导 LLM 生成新的候选程序泸评估与存储： 评估生成的程序， 更新数据库 Dt。 lJ{@t, （） }。

2.RL训练

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

每进行方刀次搜索迭代后， 利用积累的数据对 LLM 进行微调。 RL 目标： 优化策略以最大化期望奖励， 同时使用 KL 散度约束防止模型偏离参考模型 (Reference Model, 即初始模型： [r@, 一 D 歹园可@唰园@翊 max E、 影、 丌 9 算法选择： 作者没有使用 PPO （计算昂贵 ), 而是采用了 DPO (Direct Preference Optimization), 将问题转化为偏好优化问题。 偏好数据集构建： 对于同一个提示根据奖励值 r(y) 将生成的程序分为 " 高分 " 组和 " 低分 " 组， 构建三元组 （， 一）。 + 1

（1）基于DPO算法的损失函数

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

EvoTune 使用的损失函数定义为： 其中： 一 log 丌雨 + 丌， 可 + 丌雨一丌， 可一 · 是当前正在训练的 LLM 策略。 · 可是参考策略 （即初始的 Base LLM)O · +， 一） 是偏好数据三元组， 其中是评分较高的程序， 是评分较低的程序。 · 伊是 Sigmoid 函数。 0 是控制正则化强度的超参数。 尸是 f-divergence 中凸函数的导数， 决定了 KL 散度的方向。

（2）Forward RL

这里尤其值得注意的是，它采用了Forward RL而非Reverse RL：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

标准的 DPO 通常基于 Reverse KL （D五丞@目丌 “ 刁）， 这倾向于通过集中概率质量到单一模式来 " 求模 "(Mode-seeking)， 容易导致输出多样性下降 (Mode Collapse) 在进化搜索中， 多样性 (Diversity) 是至关重要的。 为了保持多样性， EvoTu n e 采用了基于 Forward KL 正则化的 DPO 目标形式。 其损失函数为： 丌雨 + 一 log。@尸 （丌， 可包一其中对于 Fo rwa rd KL, 函数尸包） = 一 1 /； 而 Reverse KL 则是尸） = log + 1。 + 1 实验证明： 使用 Forwa rd KL 的变体不仅发现了最高分的程序， 而且生成的唯一解 (Unique Scores) 数量显著多于 Reve rse KL 和基线， 证明了其在维持搜索多样性方面的有效性。

这背后的原理是：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

多样性保护 (Diversity Maintenance): 进化搜索非常依赖种群的多样性。 标准 D PO （Reve rse (L) 有很强的 Mode-seeking 倾向， 会使模型收敛到单一解， 导致 Mode Collapse + 1 0 Mass-covering 行为： Fo rwa rd KL 具有 Mass-covering' 的特性， 它鼓励策略分布覆盖参考分布丌 7 可中的高概率区域， 而不是仅仅集中在某一个峰值上。 这使得模型能生成更多样化的解 (Unique Solutions)， 从而更有利于后续的进化迭代。 + 1

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 每进行方刀次搜索迭代后， 利用积累的数据对 LLM 进行微调。 RL 目标： 优化策略以最大化期望奖励， 同时使用 KL 散度约束防止模型偏离参考模型 (Reference Model, 即初始模型： [r@, 一 D 歹园可@唰园@翊 max E、 影、 丌 9 算法选择： 作者没有使用 PPO （计算昂贵 ), 而是采用了 DPO (Direct Preference Optimization), 将问题转化为偏好优化问题。 偏好数据集构建： 对于同一个提示根据奖励值 r...
- EvoTune 使用的损失函数定义为： 其中： 一 log 丌雨 + 丌， 可 + 丌雨一丌， 可一 · 是当前正在训练的 LLM 策略。 · 可是参考策略 （即初始的 Base LLM)O · +， 一） 是偏好数据三元组， 其中是评分较高的程序， 是评分较低的程序。 · 伊是 Sigmoid 函数。 0 是控制正则化强度的超参数。 尸是 f-divergence 中凸函数的导数， 决定了 KL 散度的方向。
- 多样性保护 (Diversity Maintenance): 进化搜索非常依赖种群的多样性。 标准 D PO （Reve rse (L) 有很强的 Mode-seeking 倾向， 会使模型收敛到单一解， 导致 Mode Collapse + 1 0 Mass-covering 行为： Fo rwa rd KL 具有 Mass-covering' 的特性， 它鼓励策略分布覆盖参考分布丌 7 可中的高概率区域， 而不是仅仅集中在某一个峰值上。 这使得模型能生成更多样化的解 (U...

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
