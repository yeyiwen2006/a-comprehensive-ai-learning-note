---
title: "17.6 PPO算法在强化微调中的应用"
source_docx: "第3部分 大语言模型/17.强化微调/17.6 PPO算法在强化微调中的应用.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 17.6 PPO算法在强化微调中的应用

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、PPO算法的目标函数（和前面在强化学习部分中介绍的相同）

策略的目标函数：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

T9 at St 丌先 atl St CLIP

其中：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

T9 at St 丌先 atl St CLIP

价值A_t的估计方法：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果我们只看一步未来， 那么动作的 Q 值可以被估计为 + 7铲@“ 1)。 此时， 的估计值直接等于 TD Error: æQ(st,at) PPO 默认使用 GAE 来计算优势函数。 这是一个非常精妙的设计， 它将 TD Error 进行了指数加权移动平均。 其中 /\ (Lambda) 是一个范围在 [ 0， 1 ] 之间的超参数。 通过 /\ 调节 TD Error 的作用： 当 = 0 时 （高偏差， 低方差）： = = 0 + 7V(St+1) 此时完全等于当前的单步 TD Erroro 优点： 方差小 （只依赖一步随机奖励 0）。 一 V(st) 缺点： 偏差大 （严重依赖 Critic 网络 （+ 1） 的准确性， 如果 Critic 没训练好， 估计就是错的）。 当 /\ = 1 时 （无偏差， 高方差）： 此时公式展开后， 中间的 V 项会相互抵消， 最终变成了 Monte CarIo Retu rns （真实回报） 减去 Baselineo 优点： 偏差小 （真实回报是最准确的 " 事实 "）。 缺点： 方差极大 （因为累加了每一步环境的随枳性）。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果我们只看一步未来， 那么动作的 Q 值可以被估计为 + 7铲@“ 1)。 此时， 的估计值直接等于 TD Error: æQ(st,at) PPO 默认使用 GAE 来计算优势函数。 这是一个非常精妙的设计， 它将 TD Error 进行了指数加权移动平均。 其中 /\ (Lambda) 是一个范围在 [ 0， 1 ] 之间的超参数。 通过 /\ 调节 TD Error 的作用： 当 = 0 时 （高偏差， 低方差）： = = 0 + 7V(St+1) 此时完全等于当前的单步 TD Erroro 优点： 方差小 （只依赖一步随机奖励 0）。 一 V(st) 缺点： 偏差大 （严重依赖 Critic 网络 （+ 1） 的准确性， 如果 Critic 没训练好， 估计就是错的）。 当 /\ = 1 时 （无偏差， 高方差）： 此时公式展开后， 中间的 V 项会相互抵消， 最终变成了 Monte CarIo Retu rns （真实回报） 减去 Baselineo 优点： 偏差小 （真实回报是最准确的 " 事实 "）。 缺点： 方差极大 （因为累加了每一步环境的随枳性）。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果我们只看一步未来， 那么动作的 Q 值可以被估计为 + 7铲@“ 1)。 此时， 的估计值直接等于 TD Error: æQ(st,at) PPO 默认使用 GAE 来计算优势函数。 这是一个非常精妙的设计， 它将 TD Error 进行了指数加权移动平均。 其中 /\ (Lambda) 是一个范围在 [ 0， 1 ] 之间的超参数。 通过 /\ 调节 TD Error 的作用： 当 = 0 时 （高偏差， 低方差）： = = 0 + 7V(St+1) 此时完全等于当前的单步 TD Erroro 优点： 方差小 （只依赖一步随机奖励 0）。 一 V(st) 缺点： 偏差大 （严重依赖 Critic 网络 （+ 1） 的准确性， 如果 Critic 没训练好， 估计就是错的）。 当 /\ = 1 时 （无偏差， 高方差）： 此时公式展开后， 中间的 V 项会相互抵消， 最终变成了 Monte CarIo Retu rns （真实回报） 减去 Baselineo 优点： 偏差小 （真实回报是最准确的 " 事实 "）。 缺点： 方差极大 （因为累加了每一步环境的随枳性）。

## 二、PPO算法运用于强化微调的工作流（和前面介绍的略有不同）

1.数据采集阶段

在机器人控制等传统RL的环境中，智能体连续与环境持续交互（如2048次），形成一条长轨迹序列，后续再将序列随机打乱成小批量计算梯度。但LLM微调中的PPO使用的是向量化环境，我们可以并行处理一系列Prompt，各自并行生成轨迹并存入Replay Buffer：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

“ 2048 步 “ 实际上是所有并行环境生成的 Token 总数。 假设你的显卡并行能力允许你同时处理 N 个 Prompt （并行环境数， 比如 N = 16）。 时刻 = 1： 模型同时看到了 16 个不同的 Prompt, 并发出了 16 个 Token （每个 Prompt 一个）。 这在计数器上算作 16 个 Stepso 时刻 = 2： 模型基于之前的 Token， 又并发生成了 16 个 Tokeno 计算公式是： Tot al Steps （2048） = 并行 Prompt 数量 × 每个回复的平均长度举个例子： 如果你的图片里写 “ 运行 2048 步 "， 它实际上可能发生了以下情况： 1 ． 2 ． 3 ． 4 ． 系统从数据集里抽出了 16 个不同的 Prompt0 模型对这 16 个 Prompt 并行生成回答。 每个回答平均生成了 128 个 Token 就结束了。 总收集的数据量 (Steps) = 16 × 128 = 204 & 所以， 这 2048 步里包含了 16 个不同 Prompt 的完整故事， 而不是 1 个 Prompt 的超长故事。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

“ 2048 步 “ 实际上是所有并行环境生成的 Token 总数。 假设你的显卡并行能力允许你同时处理 N 个 Prompt （并行环境数， 比如 N = 16）。 时刻 = 1： 模型同时看到了 16 个不同的 Prompt, 并发出了 16 个 Token （每个 Prompt 一个）。 这在计数器上算作 16 个 Stepso 时刻 = 2： 模型基于之前的 Token， 又并发生成了 16 个 Tokeno 计算公式是： Tot al Steps （2048） = 并行 Prompt 数量 × 每个回复的平均长度举个例子： 如果你的图片里写 “ 运行 2048 步 "， 它实际上可能发生了以下情况： 1 ． 2 ． 3 ． 4 ． 系统从数据集里抽出了 16 个不同的 Prompt0 模型对这 16 个 Prompt 并行生成回答。 每个回答平均生成了 128 个 Token 就结束了。 总收集的数据量 (Steps) = 16 × 128 = 204 & 所以， 这 2048 步里包含了 16 个不同 Prompt 的完整故事， 而不是 1 个 Prompt 的超长故事。

2.优势计算阶段（和传统RL中一致）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

初始化： 初始化 Actor 网络和 Critic 网络。 大循环 (For each iteration)： 1 ． 2 ． 数据收集阶段 (lnteraction)： 使用当前策略在环境中运行丆步 （例如 2048 步）。 收集轨迹数据： {，，， + 1， log 丌。 1 鼠 lst)。 利用 Critic 网络计算状态价值 V(st)0 优势计算阶段 (Advantage)： · 计算 TD Error: = + 7 （st + 1） 一 V(st)0 · 计算 GAE (Generalized Advantage Estimation) At。 这是方差之间做平衡。 一个递归公式， 用来在偏差和

3.优化更新、同步策略阶段

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

收集阶段： 显卡像收割机一样， 通过并行处理很多 Prompt, 迅速填满了一个容量为 2048 的经验池 (RepIay Buffer)0 这个池子里可能混杂了 16 个不同问题的问答记录。 打乱 (ShuffIe): 为了训练稳定 （打破数据之间的相关性 ), 我们把这 2048 个 Token （数据点） 彻底打乱。 切分 (Mini-batch): 把打乱后的 2048 个数据， 切分成小块 （比如 Batch Size = 64）。 这意味着： 2048 / 64 = 32 个 Mini-batcheso 模型会用这 32 个小批次来更新参数。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 ． 4 ． 优化更新阶段 (Optimization)： 重要： 此时丌固定不动 （作为分母）， 我们优化的是新的 （作为分子）。 · 将收集到的个数据打乱， 分成多个 Mini-batch （例如每个 batch 64 个数据）。 Epoch 循环 （例如重复 10 次）： 对每个 Mini-batch: 1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 同步策略： T9(aIs) 计算新概率比率 0@） = 丌司 d 计算 Clip 损失 LCLIP 计算 Critic 的价值损失 = （（(t) 一 V） 2 target。 计算熵正则项 S （鼓励探索）。 总 LOSS： = 一力 C 刀 7 尸 + CI 丆一 C2S （注意梯度上升 / 下降符号）。 反向传播更新 O 和夯本轮更新结束， 令 @。 · 清空数据缓冲区， 进入下一次大循环。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 ． 4 ． 优化更新阶段 (Optimization)： 重要： 此时丌固定不动 （作为分母）， 我们优化的是新的 （作为分子）。 · 将收集到的个数据打乱， 分成多个 Mini-batch （例如每个 batch 64 个数据）。 Epoch 循环 （例如重复 10 次）： 对每个 Mini-batch: 1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 同步策略： T9(aIs) 计算新概率比率 0@） = 丌司 d 计算 Clip 损失 LCLIP 计算 Critic 的价值损失 = （（(t) 一 V） 2 target。 计算熵正则项 S （鼓励探索）。 总 LOSS： = 一力 C 刀 7 尸 + CI 丆一 C2S （注意梯度上升 / 下降符号）。 反向传播更新 O 和夯本轮更新结束， 令 @。 · 清空数据缓冲区， 进入下一次大循环。

## 三、LLM为何选择PPO而非SAC？为何选择On-Policy而非Off-Policy？

### （一）LLM输出离散而非连续动作

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

SAC 的原生设计： SAC 是为连续动作空间 ℃ ontinuous Action Space) 设计的最大熵强化学习算法。 在机器人控制中， 动作是关节的角度或力矩 （实数值）。 虽然存在 ' Discrete SAC" 的变体， 但在处理高维离散空间时， 计算成本极高。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

LLM 的场景： LLM 的生成过程是在一个巨大的词表 (vocabulary) 中选择下一个 Tokeno 动作空间大小 （）： 通常在 32， 000 到 128， 000 甚至更大 （例如 GPT-4 或 Qwen 的词维度灾难： 在 Actor-Critic 架构中， Critic 网络需要评估动作价值 Q(), a)o 对于 Discrete SAC， 为了计算目标值 (Target Q) 和策略的熵， 通常需要对所有可能的动作进行边缘化 (Marginalization) 或 Softmax 计算。 · 公式涉及计算丌@到 · [Q(s', 的一 alogn(a'ls')] 的期望。 这意味着每一步更新， 你都要对 50， 000 + 个 Token 进行全概率计算， 这在计算上是极其昂贵的。

那或许你会说，既然词表大小高达 10 万，那么它看起来确实很像一个连续空间。如果我们把Policy的输出改成一个连续嵌入向量，而不是一个10万维的概率分布，显存和计算量岂不是大大降低？但在实践中，这条路目前被证明是走不通的。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · “ 梯度阻断 "： 如何在离散的悬崖上求导？ 这是最核心的数学死结。 连续动作的假象： 假设你的 Actor 网络输出一个连续向量 v e （比如 d = 4096）。 为了把这个向量输入给环境 （或者用来预测下一个 token), 你必须把它映射回真实的词表中。 最近邻搜索 (Nearest Neighbor Search)： 你必须在 Embedding Table 中找到与 v 距离最近的那个 Token Embedding eko 左 = argmlnll V 一 ej112 jeV 梯度的噩梦： 这个 argmin 操作是不可导的 （或者说梯度几乎处处为 0）。 想象一下， 你的输出向量 v 在空间中移动了一占占如果它还没跨过边界， 最近邻的 Token 依然是此时梯度为 0 （因为输出没变， Loss 没如果它突然跨过了边界， 最近邻变成了 Token mo 这是一个阶跃跳变 (Step Function), 导数无穷大或未定义。 没有梯度， 反向传播就断了， Acto r 网络就学不到任何东西。 虽然有 Gumbel-Softmax 这样的松弛技巧 (Relaxation), 但在 ℃ 万维空间中， 它的方差极大， 训练极其不稳定。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · “ 梯度阻断 "： 如何在离散的悬崖上求导？ 这是最核心的数学死结。 连续动作的假象： 假设你的 Actor 网络输出一个连续向量 v e （比如 d = 4096）。 为了把这个向量输入给环境 （或者用来预测下一个 token), 你必须把它映射回真实的词表中。 最近邻搜索 (Nearest Neighbor Search)： 你必须在 Embedding Table 中找到与 v 距离最近的那个 Token Embedding eko 左 = argmlnll V 一 ej112 jeV 梯度的噩梦： 这个 argmin 操作是不可导的 （或者说梯度几乎处处为 0）。 想象一下， 你的输出向量 v 在空间中移动了一占占如果它还没跨过边界， 最近邻的 Token 依然是此时梯度为 0 （因为输出没变， Loss 没如果它突然跨过了边界， 最近邻变成了 Token mo 这是一个阶跃跳变 (Step Function), 导数无穷大或未定义。 没有梯度， 反向传播就断了， Acto r 网络就学不到任何东西。 虽然有 Gumbel-Softmax 这样的松弛技巧 (Relaxation), 但在 ℃ 万维空间中， 它的方差极大， 训练极其不稳定。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · “ 无人区 " 问题： Embedding 空间的稀疏性你可能会想： " 那我不要 argmax， 我直接把输出的连续向量 v 喂给下一层网络不就行了？ “ 这里涉及到了流形假设 (Manifold Hypothesis)0 词向量不是均匀分布的： 在 4096 维的高维空间中， 有效的 Token Embedding 只占据了极小极小的一部分点 (Point Cloud)0 绝大多数空间都是无效的空白区域 (void Space), 也就是所谓的 “ 无人区 "。 连续控制的灾难： RL 的探索 (Exploration) 通常是在动作上加高斯噪声。 如果在连续向量 v 上加噪声， 你会大概率落入 “ 无人区 "。 结果： 这个向量既不像 “ 苹果 "， 也不像 " 香蕉 "， 它是一个没有任何语义信息的杂乱向量。 语言模型崩塌： 当你把这个无意义的向量传给下一层， 整个 Transformer 的注意力机制就会处理混乱的信息， 导致后续生成的全是乱码。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 · 计算效率的悖论： KNN 比 Matrix Multiply 慢你提出用连续方法是为了提升效率， 但实际上可能更慢。 离散方法 （现状）： 输出层是一个线性变换 (Linear Layer)： W ． 九。 虽然 W 很大 （4096 × 100000L 但这只是一个大规模矩阵乘法， GPU 对此优化到了极致 (Tensor Cores)。 连续方法 （你的设想）： 如果 Actor 输出一个向量， 要把它变成具体的词 （为了给人类看， 或者计算 Rewa rd ), 你需要做全库最近邻搜索。 每一轮生成， 都要计算该向量与 ℃ 万个 Embedding 的距离。 这本质上还是 1 × 4096 和 4096 × 100000 的矩阵运算， 加上排序 /Argmin 操作。 这并不比直接输出 Logits 快， 反而因为多了索引操作而变慢。

### （二）LLM更适合On-Policy而非Off-Policy

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在传统强化学习 （如机器人控制、 游戏 (I) 的语境下， 大家绞尽脑汁想要实现 Off-Policy, 因为 " 数据利用率 (Sample Efficiency) " 被视为圣杯。 但在大语言模型 (LLM) 的语境下， On-Policy 不仅是妥协， 甚至变成了一种核心优势。 这完全颠覆了传统 RL 的经验法则。 我们可以从数学本质、 计算架构、 以及对齐目标三个维度来彻底拆解这个现象。

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 数学维度的崩塌： 重要性采样的诅 Off-Policy 算法的核心在于能够利用 “ 旧策略 " 产生的数据来更新 " 新策略 “。 为了纠正分布差异， 通常需要用到 * * 重要性采样 (lmportance Sampling, (S) * * 权重： new at St Told at St 在机器人控制中， 动作序列通常很短， 或者动作空间很简单， 这个是可控的。 但在 LLM 中， 情况完全失控： 序列长度的连乘效应： LLM 的一个 " 轨迹 （Trajectory） " 通常包含数百甚至上千个 Tokeno 整个 at St 轨迹的概率比是每一步概率比的连乘 · Ptrajectory neW 丌 old

在Off-Policy算法中，我们会利用旧策略产生的数据来更新新策略。根据估计价值的方式，我们分别讨论两种不同情况：

（1）无Q网络的情形

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

J(Tnew 叫 0 = 丆， ～ ． 丌 ew 因为我们只有丌的数据， 必须使用重要性采样 (lmportance Sampling): J(Tnew 尸 （丆丌丆 ～ ． 丌翟 P@丌。司展开轨迹概率尸 （丆）， 环境动力学消掉后， 剩下的就是动作概率的比率： 尸@丌壶这就是 “ 连乘 " 的来源： t=0 new t=o Told at St new at St 丌 0 at St at St Off- Policy Gradient E 在 LLM 中， 丆 1024， 只要稍微偏离 1， 连乘结果就会在 0 和 + 之间剧烈震荡 （方差爆 0

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在机器人控制中， 动作序列通常很短， 或者动作空间很简单， 这个是可控的。 但在 LLM 中， 情况完全失控： 序列长度的连乘效应： LLM 的一个 " 轨迹 （Trajectory） " 通常包含数百甚至上千个 Tokeno 整个轨迹的概率比是每一步概率比的连乘 · new at St Ptrajectory 7Told at St 假设 T = 1024 （常见的上下文长度 ), 即使新旧策略在每一步的差异微乎其微 （例如比值为 1024 1024 I.OI), 最终的权重也会变成 L01 26， 739。 或者反过来， 0 ． 99 0 · 00003。 方差爆炸： 这意味着 IS 权重的方差是无穷大的。 使用 Off-Policy 数据会导致梯度估计极其不稳定。 On-Policy 的优势： PPO 强制 T01d （通过 Clip 和 2048 步即弃 ), 使得 1， 从而规避了长序列带来的数值爆炸问题。

这种连乘的爆炸效应会让重要性采样方法变得极为不可控。

（2）有Q网络的情况：避免了方差爆炸，却可能导致偏差爆炸

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 LLM 中， 生成一个回复通常需要 T = 1000 步。 在 70B 参数的模型和稀疏奖励下是必然的）。 假设我们的 Q 网络有一个微小的估计误差 e （这 Qtarget （5 1 _ 一 1 + EndValue + 6 Qtarget ()T 2 = 一 2 + 7Qtarget@丆一 1） + （50） 一乥 ' 乥 7 0 + 7 6

由于Q值高估问题，epsilon往往为正值，累加起来会让Critic的判断失灵。

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 计算架构的倒置： 重计算比生成更贵在传统 RL 中： 环境交互慢 （物理时间 ), 神经网络计算快。 所以我们要把存下来的数据反复在 GPU 上跑。 在 LLM RLHF 中： 环境交互 （生成文本） 就是神经网络计算， 而且极其昂贵。 如果你想用 Off-Policy （如 SAC 或 IMPALA) 来利用旧数据， 你需要知道旧数据在当前新参数下的概率分布 （为了计算 log 或 KL 散度）。 这意味着你需要把旧数据 (Prompt + Response) 重新输入到当前的 70B 模型中做一次 FO rwa rd Pa sso

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 · 对齐目标的特殊性： 我们要 “ 微调 " 而非 “ 寻找全局最优 " 这是最本质的区别。 传统 RL （如下围棋 AlphaGo)： 目标是全局最优。 我们希望 AI 发现人类从未见过的神之一手。 Off-Policy 允许 Agent 在 Replay Buffer 中保留那些偶然发现的 " 高分怪异动作 "， 并不断强化它。 LLM 对齐 (RLHF): 目标是受限微调。 我们不希望 LLM 另辟蹊径去发明一种新的语言， 也不希望它通过输出乱码来 Hack 奖励模型。 我们希望 LLM 保持在 SFT （监督微调） 模型的邻域内， 只是在风格和安全性上做微小的调整。 Off-PoIicy 的危险： 它倾向于探索远离当前策略的区域， 极易导致模型崩塌 (Model CoIlapse), 即模型为了刷高 Reward 而开始胡言乱语。

Off-Policy之所以容易偏离策略，和它自身的特性有关。

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

机制： Off-Policy 算法将历史产生的所有数据 @， a, r, 5 /） 都存入 Replay Buffer, 并在训练时随机采样。 后果： 你的 Buffer 里可能包含 ℃ 0 个 Epoch 之前的策略 （司产生的数据。 那时候的策略可能非常随机 （探索初期 ), 偶然尝试了一个极其离谱的动作 （例如在 LLM 中输出了一个生僻的乱码单词 ), 但恰好 Rewa rd Model 误判给了高分 （Rewa rd Hacking)0 虽然当前的策略 Tnew 已经很成熟， 绝对不会主动输出那个乱码， 但当 Off-Policy 算法从 Buffer 中捞出这条数据时， 它会发现： “ 嘿！ 这里有个动作得分为 ℃ 0！ “ 于是， 算法会更新当前的策略， 强行把它拉向那个历史上的 " 离谱高分区域 "。 对比： On-Policy (PPO) 生成完数据就扔了。 如果刚才偶然出现了一个离谱的高分， 但之后没再出现， PPO 很快就会遗忘它， 不会反复去 " 回味 “ 那个错误。

On-Policy是在现有策略下求解策略梯度，据此“走一步”调整概率分布，往往不会过度偏离现有解。在“大模型研究中的现象解释与理论分析”部分也证明了，On-Policy方法本质上是反复在Feasible Policies和Optimal Policies中做投影，最终收敛的策略理论上是所有最优策略中，距离初始策略KL散度最小的那一个。

另外，对“未见区域”的高估问题也是容易让它偏离策略的一个原因。

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Q 值的泛化误差： 神经网络具有泛化能力。 对于当前策略丌从未访问过的状态一动作对） Q 网络也会给出一个预测值。， tLnSeen， 高估倾向： 由于缺乏对这些 @ a） 的真实数据约束， Q 网络的预测往往方差很大。 而在 = + 7 max Q 的更新公式中， 那个 max 操作会特意挑选出那些被错误高估的值。 结果： Q 网络会产生一种幻觉： " 虽然我没去过那里， 但我 ' 觉得 ' 那里满地黄金。 ' 行动： 于是 Actor 网络就会被这种幻觉吸引， 去探索那些远离当前分布、 且实际上可能并没有高回报的区域。

### （三）LLM微调需要控制更新幅度

LLM的微调必须控制更新好幅度。如果模型参数更新幅度过大，LLM 会迅速发生“语言崩塌”，为了奖励最大化而破坏在预训练中获得的语言能力，开始输出乱码或重复无意义的字符（也是一种Reward Hacking）。

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

PPO 的核心在于通过 Clip 操作限制新旧策略之间的差异。 其目标函数如下： 其中。@） = 是概率比率。 丌 9 司 d ()t St 作用： 这个 6 （通常为 0 ． 2） 强制策略砌不能偏离 “ 太远。 结果： 这保证了 LLM 在学习人类偏好的同时， 保留了预训练阶段获得的语言能力 （语法、 逻

除了Clip操作限制一步更新中策略变化的幅度之外的，在现代大模型中往往还会加和参考分布（预训练模型输出分布）之间的KL项，避免多次微小的策略更新积累起来之后偏得太远。

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

SAC 的目标是最大化累积奖励的同时最大化熵 （E nt ro (y)： J(T) 一乥 [r(st, + a 丌@的 〗 风险： 虽然熵能鼓励探索， 但在 RLHF 中， 我们通常希望模型输出确定性高、 逻辑严密的回答， 而不是为了探索而尝试高熵 （随机） 的 Tokeno 过强的熵正则化可能导致 LLM 生成内容变得发散且不连贯。

### （四）稀疏奖励下Q网络训练的稳定性难题

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

SAC (Actor-Critic 强耦合）： SAC 严重依赖 Critic (Q 网络） 的准确性。 在 LLM 中， 训练一个能够准确评估 ' 这句话好不好 " 的 Critic 网络 (Reward Model 的代理） 本身就非常困难。 如果 Critic 发散或估计不准， Actor 会迅速学坏。 · 在大规模稀疏奖励场景下， Q-learning 类方法的 bootstrapping 容易导致价值估计偏差 (Overestimation bias)o PPO （类似蒙特卡洛的回归）： P PO 使用 GAE (Generalized Advantage Estimation) 来计算优势函数 At。 虽然它也用 Critic (Value Network), 但它更多地利用了整个轨迹的实际回报 (Returns)0 相比于 SAC 这种高度依赖 bootstrapping 的 TD-learning, PPO 在长序列生成任务 (Episode length 很大） 中通常更鲁棒。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- J(Tnew 叫 0 = 丆， ～ ． 丌 ew 因为我们只有丌的数据， 必须使用重要性采样 (lmportance Sampling): J(Tnew 尸 （丆丌丆 ～ ． 丌翟 P@丌。司展开轨迹概率尸 （丆）， 环境动力学消掉后， 剩下的就是动作概率的比率： 尸@丌壶这就是 “ 连乘 " 的来源： t=0 new t=o Told at St new at St 丌 0 at St at St Off- Policy Gradient E 在 LLM 中， 丆 1024...
- 除了Clip操作限制一步更新中策略变化的幅度之外的，在现代大模型中往往还会加和参考分布（预训练模型输出分布）之间的KL项，避免多次微小的策略更新积累起来之后偏得太远。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
