---
title: "17.3 专家迭代与拒绝采样"
source_docx: "第3部分 大语言模型/17.强化微调/17.3 专家迭代与拒绝采样.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 17.3 专家迭代与拒绝采样

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、专家迭代（Expert Iteration）的基本思想

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

专家迭代由两个角色组成， 它们在一个循环中互相促进： 1 ． 2 ． 学徒 (Apprentice)： 即我们要训练的神经网络 (Policy）。 它的特点是推理速度快， 但决策质量受限于当前权重。 专家 (Expert)： 这通常不是一个外部的人类或更强的模型， 而是一个基于当前学徒能力之上的搜索算子 (Search Operator)。 例如： 当前策略 + 蒙特卡洛树搜索 (MCTS)， 或者当前策略 + 拒绝采样 + 验证器。 专家的特点是计算成本高、 速度慢， 但通过多步搜索或验证， 能产出比学徒质量更高的决策。 算法本质： 学徒通过模仿学习 (SFT)， 学习 “ 如何像专家一样做决策 "， 从而不需要搜索也能快速得到高质量结果。 而在下一轮迭代中， 能力变强的学徒又能支撑专家进行更深层的搜索。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

阶段仁专家生成数据 (Expert lmprovement) 在这个阶段， 我们在固定参数 O 的基础上， 利用更消耗计算资源的算法 (Expert) 来生成优于当前模型直接输出的数据。 假设当前状态为 (Prompt)， 当前模型策略为砌。 我们定义专家算子， 它基于进行增在 LLM 推理场景中， 的具体实现通常是： 搜索 (Search)： 对同一个采样 N 次 (MajorityVoting)， 或者进行集束搜索 (Beam Search)0 · 验证 (verification)： 利用 Unit Test （代码） 或答案比对 （数学） 过滤出正确路径。 树搜索 (MCTS)： 如 AlphaZero, 通过构建搜索树来评估哪个动作的价值 Q(), a) 最高。 产出： 一批高质量的轨迹数据 Dnew = {， “ “） }， 其中佐 “ “ 是经过搜索验证后的正确推理路径。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

阶段 ll: 模仿学习 (lmitation Learning / Distillation) 在这个阶段， 我们将阶段丨生成的数据 Dnew 视为 " 标准答案 ' ' （Ground Truth), 使用监督学习 (SFT) 来更新模型的参数。 目标是最小化 KL 散度 （即 Cross-Entropy Loss)， 让模型的 " 直觉 “ 去逼近专家搜索后的分布 = arg min E、 刀 [ 一 log@(yexpert： 在 AlphaZero 不仅要姒台动作 (Policy Head), 还要拟台胜率 (value Head 丿。 但在 LLM 推理中， 目前主是只拟台生成的 Token 序列 cy 丿。 阶段 lll： 迭代 (lterate) 使用更新后的参数 “ 替换旧模型。 因为基础模型变强了， 下一轮的搜索 (Expert) 可以基于更高的起点探索到更优的解 （例如解开之前解不开的难题 ), 生成质量更高的数据。

更新参数的式子看起来是监督学习，实际上却完成了“策略提升”的过程，是给定环境和奖励下的强化学习。当然，前面讲蒙特卡洛树搜索时也讲到过专家迭代思想下不用监督微调的更新方式的方法。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这里有一个关键的数学原理： 多步搜索通常优于单步预测。 令 （为状态的真实价值 （能否解题）。 · 学徒策略是对最优策略的参数化近似。 专家策略丌是基于进行 Lookahead （向前看多步） 后的改进策略。 根据贝尔曼方程 (Bellman Equation) 的性质， 只要搜索算子是关于价值函数的 " 改善算子 " (lmprovement Operator), 那么： Performance@(To)) P erformance （砌 Exlt 的训练过程就是： （丌，） （策略更新： 让学徒追上专家） 1 ． + 1 = （+ 1） （专家基于新学徒再次提升） 巳 p 曰呓这种螺旋上升的过程， 理论上可以让模型最终收敛到该任务的最优解。

## 二、拒绝采样微调（Rejection Sampling Fine-Tuning，RFT）

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当 SFT 模型具备一定能力后， 利用它自己生成数据来进一步提升性能。 这被称为 " 专家迭代 " (Expert lteration)。 原理利用 SFT 后的模型对同一个问题生成个不同的推理路径 1，， 一 ·， }。 利用外部验证器 (Rule-based Checker， 如 Python 解释器或答案匹配） 过滤出结果正确的路径， 将这些 “ 模型自己生成的正确路径 " 加入训练集进行再次微调。 算法工作流 1 ． 2 ． 3 ． 4 ． 5 ． 生成 (Rollout)： 对于数据集中的问题使用当前策略采样个解。 设置较高的 temperature 以增加多样性。 验证 (Evaluation): 对于数学题： 提取最终答案， 与 Ground Truth 比对 (Exact Match)0 对于代码题： 运行 Unit Test0 过滤与构造： 保留通过验证的样本 （-7 ．。 再训练： 将新样本混合入原始 SFT 数据， 进行新一轮 S FTO 迭代： 重复上述过程。

## 三、STaR（Self-Taught Reasoner）：从错误后修正的推理中学习

模型生成推理过程和答案。如果答案错误，向模型注入正确答案（Hint），要求模型生成能够推导出正确答案的推理过程（Rationalization）。将这些“后视修正”的推理数据加入训练集，进行监督微调。

## 四、专家迭代思想的应用

1.LLM

一般如果后续还要用PPO进行针对推理的优化，通常会先跑几轮专家迭代。PPO对初始策略（Initial Policy）很敏感，我们需要一种方式把模型的分布拉到一个较高的水平（SFT后的模型往往还是不够强），再上PPO去优化那些极其困难的Case或进行对齐。专家迭代就适合作为这种方式。而对于GRPO，它是基于组内相对优势更新的，这几个对的样本会产生极大的正向梯度，每一轮更新，模型都在对自己生成的“好样本”进行强化，对“坏样本”进行抑制，实质上起到了专家迭代的作用，免去了“评估搜索结果”这个消耗额外算力的环节。

2.AlphaZero

通过原始蒙特卡洛树搜索（而不是针对LLM的改进版本），探索可能的路径。通过交叉熵损失，让策略网络的输出接近蒙特卡洛树搜索方法采样的结果；价值网络拟合的目标价值则直接是最终的输赢结果（只有最终奖励1或-1），即希望输出的预测值v能准确预测最终结果z。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们希望神经网络输入某个历史状态时， 输出的预测值能无限接近最终结果。 AlphaZero 的总损失函数包含三部分 （价值、 策略、 正则项 ), 其中关于价值更新的部分是均方误差 (Mean Squared Error, MSE): 完整的损失函数为： 一产 log + 到因 2 策略损失正则化价值损失 z： 这一盘棋最终是谁赢了 （真实结果）。 鬱： 神经网络当时在状态 5 预测的赢面。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

你可能会问： MCTS 搜索得出的 Q 值不是比网络笪接预测的， 更准吗？ 为什么不用 Q 作为标签来更新？ 这是一个很好的直觉， 但在 AlphaZero 中： · Policy Network 的目标是拟合 M CTS 的搜索概率丌 （即 MCTS 觉得哪一步好）。 · Value Network 的目标是拟合真实结果 zo 原因： MCTS 的 Q 值虽然比， 准， 但它依然是基于网络预测的聚合出来的， 存在偏差 (Bias)0 如果让网络去拟合 Q, 相当于 " 左脚踩右脚 "， 可能会导致亻扁差累积。 而使用真实结局 z 是无偏的 (Unbiased)， 虽然方差大 （一盘棋赢了不代表这步棋一定好）， 但只要数据量够大， 网络就能学到最准确的局势判断。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
