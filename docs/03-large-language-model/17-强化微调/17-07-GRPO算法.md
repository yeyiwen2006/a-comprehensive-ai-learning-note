---
title: "17.7 GRPO算法"
source_docx: "第3部分 大语言模型/17.强化微调/17.7 GRPO算法.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 17.7 GRPO算法

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

GRPO是一种去除Critic网络的PPO变体。通过利用LLM强大的并发生成能力，我们可以运用蒙特卡洛方法进行价值的无偏估计，来代替神经网络可能有偏的价值估计，从而大幅降低显存消耗（少一个网络），并保持训练的数学稳定性。

## 一、运用蒙特卡洛方法估计价值

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在强化学习中， 优势函数 a) 的定义是： 我们来看 GRPO 是如何计算优势的： Q(s,a) 的估计： 对于第个采样 0 ” 其实际获得的奖励 0 就是动作价值 Q(s,a) 的单次蒙特卡洛采样 （无偏估计）。 （5） 的估计： GRPO 计算这一组采样的平均值 = 上 E G j=l 丿。 根据大数定律 (Law of Large Numbers)， 当 G 时： + E[RIs] = 以 5） 所以， GRPO 的优势计算公式 = 一， 本质上就是： Advant age S ampled Q(), a) 一 Monte Carlo Estimated （5）

运用蒙特卡洛方法的好处在于其无偏性。Q网络的更新中，一个状态动作对Q值的偏差会在其他Q值更新时产生影响，很容易导致误差累积，蒙特卡洛方法则无此问题。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

如果直接用 MC 估计这么好 （无偏）， 为什么 PPO 还要费力去训练一个 Critic 神经网络来预测铲@） 核心原因在于： 方差 (variance)。 传统 RL 的困境 (SingIe Sample) 在传统的强化学习 （如机器人控制、 打 Atari 游戏） 中， 我们通常只能基于当前的 5 采样一条轨迹。 如果你只采样一次得到， 直接用 r 当做 Q， 方差极大。 如果你没有 Critic, 你需要用历史平均奖励作为 Baselineo 这导致训练非常不稳定， 收敛极慢。 PPO (Actor-Critic) 的解法 PPO 引入 Critic 网络 （5）： 优点： 降低方差。 Critic 见过很多类似的状态， 它的预测比较平滑稳定。 缺点： 引入偏差 (Bias)0 如果 Critic 没训练好 （拟合误差）， 它给出的 Baseline 就是错的， 带偏 Actor 的更新方向。 而且显存占用翻倍。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

GRPO 之所以能成功 " 复古 " 回蒙特卡洛方法， 是因为它利用了 LLM 推理的一个独特工程特性： 同一个 Prompt 可以极低成本地并发生成多条舛果、 - 口 0 在机器人领域， 你很难让机器人 “ 完全一致地 " 把同一个动作重复做 64 次来求平均 （环境不可逆）。 但在 LLM 中， 你可以轻松设定 n u m return_sequences=64。 GRPO 的方差控制术虽然 MC 估计通常方差大， 但 GRPO 通过以下方式控制住了方差， 从而不再需要 Critic： 1 ． 2 ． Group 采样 (G 足够大）： 它不是用 1 个样本去估计铲@）， 而是用 G 个样本 （比如 DeepSeek 用 G = 64）。 根据中心极限定理， 样本均值的方差是单样本方差的。 通过增大 G， 直接把 MC 的方差压下来了。 组内标准化 (Group Normalization)： 公式二不仅仅是减去均值， 还除以了标准差。 这相当于对奖励信号做了动态缩放 (Dynamic Scaling), 进一步稳定了梯度的幅度， 起到了类似 Batch Normalization 的亻

## 二、模型架构与表达式

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了后续推导的严谨性， 我们统一使用以下符号 · q: 输入的问题 (Prompt), 采样自分布 P(Q)O G: 组的大小 (Group Size), 即对每个问题采样的输出数量。 针对同一个 q 采样的第个输出序列， 其中 e { 1， · 一， G}O： 第个输出序列 ot 的长度 (Token 数量）。 0 “： 序列中的第个 Tokeno 徇： 当前待优化的策略模型 (Policy Model)0 “： 采样时使用的旧策略模型。 可： 参考模型 (Reference Model, 通常是 S FT 模型 ), 用于 KL 约束。 0： 第个输出序列获得的完整奖励 （由 Rewa rd Model 或环境给出）。： 第个输出序列的优势函数 (Advantage)。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

PPO Policy Model GRPO P01icy Model 0 Reference Model Reward Model Value Model Reference Model Reward Model GAE Computauon Trained Models Models Figure 4 《 Demonstration of PPO and our GRPO GRPO foregoes the value model, instead estimating the baseline from group scores, significantly reducing training resources.

1.群体采样（代替期望计算）

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

对于每一个输入 q, 我们使用旧策略采样一组共 G 个输出： 0 02， ． 随后， 我们对每个输出计算奖励 frl，， 一， 0G}、 01q）

2.优势估计

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这是 GRPO 取代 Critic 网络的关键。 传统的 A2C/PPO 使用神经网络 （5） 预测基准值， 而在 GRPO 中， 我们利用当前这组样本的平均奖励作为基准。 计算第个样本的优势翡： Ti 嘰一 gr、 ot + 6 gr0UP

除以标准差是因为考虑到不同类型的问题给分模式不同，可能互相不可比，故将其标准化。但也可能带来问题，比如对于过于难或简单的问题，几乎所有回答得分都一样时，该问题权重会被人为拉大。针对此问题后续已有工程上的优化。

3.目标函数与长度归一化

目标函数J(θ)如下：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

q、 P(Q),{oz}f.1 ～ 丌穆况 d 乥 (min （佐以， 1 长度归一化一 e, 1 + 6） 翡） 一 0 刀 “ D

这里Clip和KL同时存在，二者作用略有不同。Clip限制单次梯度更新的幅度，防止策略在一次迭代中变化过大导致训练崩塌；KL项由于保证最终策略距离最初策略不太远，关注累积效应，保证模型不会为了取得更高Reward而让语言能力退化。

D_KL,t=pi(old)/pi(ref)，即当前策略下该动作的概率/参考（预训练模型）策略下该动作的概率，这对pi_old求期望后是反向KL散度（在让pi_old拟合pi_ref的情况下，反向KL散度为KL(pi_old||pi_ref)，大模型一般均用反向KL散度；这里不需要遍历词表中所有词，因为遍历是为了求期望，这里用组采样蒙特卡洛就能得到期望）。

之所以用反向KL散度而非前向，是因为反向KL散度KL(pi_old||pi_ref)会对新策略高概率、原策略低概率的区域给出较大惩罚，进而呈现“模式搜索”行为，让新的概率空间收敛到预训练后概率较大的空间里的特定模式（如果跳出空间往往表现为输出乱码、不符合正常语义的内容等，即“模式崩塌”）；前向KL散度则会倾向于覆盖预训练后的高概率空间，呈现“均值搜索”行为。RL获得的空间往往是预训练的子集（这也可能是RL难以让模型获得预训练完全没见过的技能的原因之一）。

方括号内，对于同一个输入，从第1到第G个输出取平均，然后是一个长度归一化项。这个长度归一化项是GRPO的另一个关键。

标准的策略梯度下，我们优化的目标是总奖励的期望：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

标准的策略梯度 (Standard PG) 原本我们想优化的目标是总奖励期望： 其梯度是 （忽略 Baseline)： VJ EV10gT(atIst)

目标函数J的梯度是对每一个动作的策略计算并累加的，假设J对每个动作的梯度大小为1，那么长为100的序列得到的梯度就约为长为10的序列的十倍，这会导致模型被长序列主导更新。但对于同一个问题而言，每个答案序列贡献的梯度应该是“平等”的，故我们需要除以序列长度T：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

V JGRPO

但这也会产生一个问题，那就是当我们把J的梯度除以T时，意味着把目标函数J本身也除以了T，目标函数变成了“单位token能获得的奖励的期望”。这会导致模型在回答错误时生成大量无用的token。原因在于：

当优势函数为正值时，意味着生成的结果是正确的，目标函数为正。由于目标函数是“单位token能获得的奖励的期望”（有分母T），为了让目标函数变大，策略在优化过程中会倾向于生成简短的正确答案，即这样的答案会产生比其他答案更大的梯度更新幅度。

当优势函数为负值时，意味着生成的结果是错误的，目标函数为负。为了让目标函数变大（惩罚的绝对值变小），策略会倾向于生成较长的答案（因为目标函数中除以较大T），即这样的答案的梯度幅度更大。

## 三、优势函数估计的有偏性问题及解决方案

GRPO的优势函数估计看似是无偏的，实则往往对难题低估、对简单题高估。例如，一次取8条路径，对于一道答对概率只有1％的难题，答对奖励为1，答错奖励为0，则8条路径都答错时没有梯度，有一条答对时，答对的那条路径优势函数1-1/8=7/8，但实际上优势函数为1-1/100=99/100.对于简单题则相反，如答对概率为99％，一条答错，答对者优势函数即为1/8，实际上只有1/100.

对此，可将难题优势函数乘以更大系数，简单题优势函数乘以更小系数，以尽可能实现无偏估计。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 为了后续推导的严谨性， 我们统一使用以下符号 · q: 输入的问题 (Prompt), 采样自分布 P(Q)O G: 组的大小 (Group Size), 即对每个问题采样的输出数量。 针对同一个 q 采样的第个输出序列， 其中 e { 1， · 一， G}O： 第个输出序列 ot 的长度 (Token 数量）。 0 “： 序列中的第个 Tokeno 徇： 当前待优化的策略模型 (Policy Model)0 “： 采样时使用的旧策略模型。 可： 参考模型 (Referenc...
- D_KL,t=pi(old)/pi(ref)，即当前策略下该动作的概率/参考（预训练模型）策略下该动作的概率，这对pi_old求期望后是反向KL散度（在让pi_old拟合pi_ref的情况下，反向KL散度为KL(pi_old||pi_ref)，大模型一般均用反向KL散度；这里不需要遍历词表中所有词，因为遍历是为了求期望，这里用组采样蒙特卡洛就能得到期望）。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
