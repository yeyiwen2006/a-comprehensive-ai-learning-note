---
title: "17.1 基于人类反馈的强化学习"
source_docx: "第3部分 大语言模型/17.强化微调/17.1 基于人类反馈的强化学习.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 17.1 基于人类反馈的强化学习

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、产生背景

经过了监督微调以及针对推理的强化微调之后，模型极其聪明，但存在两个问题：

1.难用：可能会中英文夹杂、说话重复、或者语气很冲，因为它只管把题做对，不管人类看着舒不舒服。

2.安全性问题：价值观可能未和人类对齐。

## 二、核心原理

1.奖励模型

我们不可能让人类给所有训练回答打分。因此，我们用一个问题，让模型生成两个（或多个）回答，让人类对这些回答的优劣给出排名，通过回答与排名的对应数据来训练奖励模型。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设我们的奖励模型参数为对于同一个提示词有两个回答： 绗 (Winner): 人类标注员认为更好的回答。 (Loser)： 人类标注员认为较差的回答。 · （： 模型给出的标量分数。 损失函数 （定义为： —E 其中： · 是 Sigmoid 函数： a(z)， 它将数值压缩到 （0， 1） 区间， 表示概率。 l+e¯ 一，） 是两者分数的差值。

这实际上是对“模型正确判出更好回答的概率”用了交叉熵损失。上式鼓励了奖励模型给更优的回答给出高分，反之亦然。

这个损失函数本质是对优样本给分大于劣样本的极大似然估计：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

根据 Bradley-Terry 模型， 这个概率被建模为： exp(r(yw)) exp(r(yw)) + exp(r(yD) 我们将分子分母同时除以 exp(r(Yl))， 得到： exp(r(yw) 一 r(yD) 1 + exp(r(y

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

根据 Bradley-Terry 模型， 这个概率被建模为： exp(r(yw)) exp(r(yw)) + exp(r(yD) 我们将分子分母同时除以 exp(r(Yl))， 得到： exp(r(yw) 一 r(yD) 1 + exp(r(y

2.强化学习

运用强化学习算法如PPO、GRPO，利用第二步的奖励模型为LLM的回答给出奖励，进行强化学习。以PPO为例：

在RLHF的PPO目标函数中，除了Clip这一项，通常还会减去一项KL散度（KL散度实际上是log外面加期望，目标函数即R_total整体的期望）：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 4 OCR 未识别出有效文字：未识别出有效文字。]

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一道锁： KL Penalty （针对 “ 忘本 " 与 “ 作弊 "） 对比对象： 当前策略 vs 初始 SFT 模型丌 7 可。 · 可是谁 · 它是你的 “ 老师 " 或者 " 出厂设置 " （SFT 模型）。 它在这个训练过程中是永远不变的 (Frozen)。 作用： 这是一个长期约束。 不管你训练了多少轮 (Epoch), 不管你更新了多少次， 你都不能偏离 " 出厂设置 " 太远。 如果不加这一项， 模型为了刷分 （Rewa rd Hacking), 可能会输出乱码或利用 Rewa rd Model 的漏洞。 KL 惩罚强迫它 " 说人话 "。 第二道锁： PPO Clip （针对 “ 数学崩坏 "） 对比对象： 当前策略 vs 上一轮策略丌产 · 丌是谁： 它是几分钟前的你自己。 它是动态变化的。 作用： 这是一个短期约束 （步长控制）。 · 它保证的是数学上梯度更新的稳定性。 它不管你有没有偏离 SFT 模型， 它只管 " 这一次参数更新别太猛， 别把神经网络搞崩了 "。

拓展：PPO-Penalty原算法 vs RLHF实际运用时的Penalty

"PPO with Adaptive KL Penalty" 的变体，它把 KL 直接加在 Loss 函数里。

但在 RLHF 中，我们通常选择把KL“通过每一步的Reward 注入”，而不是直接加在最终的Loss里。这是为了让价值函数V(s)感知到难度，如果把 KL 扣分算在Reward里，那么V(s)在预测“这个状态（生成这个token）好不好”时，就会自动把“偏离SFT模型的代价”也考虑进去。智能体会通过V(s)提前预判到“如果我这就开始乱说话，虽然可能骗到高分，但会被 KL 罚得很惨所以这并不是一个好的状态”。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
