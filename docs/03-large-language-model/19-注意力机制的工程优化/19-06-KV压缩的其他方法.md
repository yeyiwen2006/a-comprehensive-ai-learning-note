---
title: "19.6 KV压缩的其他方法"
source_docx: "第3部分 大语言模型/19.注意力机制的工程优化/19.6 KV压缩的其他方法.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 19.6 KV压缩的其他方法

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、KV Cache量化方法：精度的适当降低

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

原理： KV Cache 默认通常是 FP16 (16-bit)0 通过量化技术将其压缩为襯 T8 甚至 INT40 数学 · = round(Kfp16/scale 8 效果： 显存占用直接减半或变为 1 / 4。 虽然会有极微小的精度损失， 但在长文本推理中几乎无感。

## 二、存在信息损失的K、V序列压缩

还有一些方法不处理每一个独立的 token，而是将序列总结或聚类成少量原型的“概念”或“块”，然后在这些原型上执行注意力，最后再将结果广播回原始 token。

典型代表：

Pooling/Clustering：对 K 和 V 进行池化或聚类，减少其数量。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

H20 (Heavy Hitter Oracle)： 只保留那些注意力分数比较高的 Token (Heavy Hitters)， 把分数低的 Token 从 Cache 里踢出去 (Evict)。 · Token Merging (ToMe)： 发现两个 Token 向量很像 （余弦相似度高）， 就把它们合成一应用程度： 中等偏低 (Niche / Emerging) 备。 痛点 · 1 ． 动态性灾难： KV Cache 变稀疏后， 内存不再连续， 很难利用 GPU 的并行计算能力， 反而可能变慢。 2 ． “ 大海捞针 " 失效： 如果删掉了看似不重要但实际包含关键信息的 Token （比如一个人名 ), 模型回答长文本问题时就会 " 幻觉 "。 代表技术： StreamingLLM （保留开头和最近的 Token)、 H200 原理： 基于一个假设： “ 不是所有 Token 都重要 "。 像 “ 的 "、 后可能就不需要查它了。 现状： 主要用于 * * 超长文本 (Long Context) * * 的极端场景， 或者资源极其受限的端侧设 IS "the" 这种词， 生成完

Learned Compressors：使用一个可学习的网络（如低维投影）来压缩 K 和 V。但有损且不可逆：一旦压缩成摘要，原始的逐字逐句信息就丢了，很难做精确引用。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- H20 (Heavy Hitter Oracle)： 只保留那些注意力分数比较高的 Token (Heavy Hitters)， 把分数低的 Token 从 Cache 里踢出去 (Evict)。 · Token Merging (ToMe)： 发现两个 Token 向量很像 （余弦相似度高）， 就把它们合成一应用程度： 中等偏低 (Niche / Emerging) 备。 痛点 · 1 ． 动态性灾难： KV Cache 变稀疏后， 内存不再连续， 很难利用 GPU 的并行...
- Learned Compressors：使用一个可学习的网络（如低维投影）来压缩 K 和 V。但有损且不可逆：一旦压缩成摘要，原始的逐字逐句信息就丢了，很难做精确引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
