---
title: "19.8 线性注意力（Linear Attention）"
source_docx: "第3部分 大语言模型/19.注意力机制的工程优化/19.8 线性注意力（Linear Attention）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 19.8 线性注意力（Linear Attention）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、基本原理

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

线性化注意力的核心思想是： 通过改变计算顺序， 避免显式地计算和存储这个 “ × “ 的注意力矩阵它的目标是将计算复杂度从 0 （硭） 降低到 0）。 如何实现？ 答案是利用矩阵乘法的结合律。 · 标准计算顺序： (QKAT) v 先算 “ × “ 矩阵， 再与 V × d) 相乘。 · 线性化计算顺序： Q (KAT V) 先算 K （得到一个 d × d 的矩阵） 再与 Q × d) 相乘。

实现线性化并非无条件的，必须去掉Softmax，这是因为：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

标准注意力 (Standard Attention): Output = Softmax(QKT)V 请注意， 这里不仅有乘法， 还有一个非线性的 Softmax 包裹在中间。 Softmax(QKT) Q · Softmax(KT) 你不能把 Softmax 拆开， 也不能把括号移到后面去先算 K。 因为 Q 和 K 的每一项都必须先交互、 求和、 归一化之后， 才能和相乘。 这意味着你必须构建那个巨大的 N × N 矩阵。

我们需要一个核函数来近似Softmax函数。具体数学推导：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

首先， 我们把标准注意力写成一个按行归一化的形式。 对于第个查询向量佑的输出仂一 1 exp 我们可以将其抽象为一个更通用的相似度函数 sim(•, E 亠 1 sim(qi, 与 sim(qi,）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

其中： + 丿是一个将原始向量映射到另一个特征空间的函数。 它被称为核函数。 现在， 我们将这个核函数代入输出的计算中： 由于 +(qt) 对于求和索引 j 是常数， 我们可以将其提到求和号外面： 这里 ． 表示夕卜积。 但更常见的写法是将其视为矩阵乘法。

注：内积表示行向量*列向量=标量；外积表示列向量（m*1）*行向量（1*n）=矩阵（m*n）。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

让我们定义两个关键的累积状态矩阵 ． 1 S： 键和值的聚合状态。 （注意： 这里） 是行向量， 其转置是列向量， 与屿 （行向量） 做夕卜积， 得到一个矩阵。 我们对所有 j 的这样的矩阵求和。） 2 Z： 归一化因子的聚合状态。 IR " 可现在， 对于整个序列的输出 0 中的第行， 可以计算为： +(qt)S +(qDZ 这里除氵去是逐元素除法， 因为 4(qt)Z 是一个标量 （归一化因子）。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

计算 S 和 Z: 我们需要遍历序列中的每个位置来计算严屿和严， 然后累加。 这一步的复杂度是 0 · 计算所有输出仂： 对于每个位置 i, 我们计算 +(qt)S 和 4(qt)Z0 这一步的复杂度是 0 · 总复杂度是 0 伊咗，．。@。）。 如果我们固定和 （它们是与序列长度 “ 无关的常数）， 那么复杂度就是 0 （“）， 即线性复杂度。

*注：K和V本来分别为n*d_k和n*d_v维，K转置后（函数fai不影响元素个数）和V矩阵相乘，容易知道矩阵相乘计算复杂度为n*d_k*d_v，也就是O(n)。

## 二、如何寻找核函数fai(x)？

对于 sim(q, k) = exp(q · k^T)=fai(q) · fai(k^T)

在当我们在深度学习模型（如Transformer的原始注意力机制）中直接使用Softmax时，我们是在进行数值计算，对于单个数值x，exp(x)是一个成熟的数学函数，计算机可以高效处理。但当q和k是两个向量时：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

(q， 到 2 4！

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对于 m=l 这可以看作是向量 q 和 k 在原始 d 维空间中的内积。 对于 m=2 注意！ 这可以重写为两个新向量的内积 2 让 2 （q） 是一个 d2 维的向量， 包含所有 q-i q-j 的组合 (i,j 从 1 到 d) · 让 2 （k） 是一个 d2 维的向量， 包含所有 k-i k j 的组合那么：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

现在， 我们可以为每个幂次定义一个特征映射： · 对于 m=e · 对于 m=l · · 对于 m=2 · 对于 m=3 · · 依此类推 ． ． = 1 （1 维） 1 （q） = q (d 维） 甲 2 （q） 包含所有 q-i q-j （d2 维） 甲 3 （q） 包含所有 q-i q-j q-k （d3 维） 现在， 让我们构造一个无限维的超级特征映射 @(q) 这里每个分量都除以相应的 V(m!) 来匹配泰勒展开中的系数。 佑 ‰ ql

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

： 1 + 乥 qtk + 乥 q， ‰ 岳 + 一乥 q， ‰ q j + · 但注意 ． 巧 q q j k j 丿 q j 因此： kJ qk k （q · k） 2 kJ (q · k） 3

计算 exp(q·k) 这个简单的操作，在数学上等价于：将向量 q 和 k 映射到一个无限维的特征空间，在这个无限维空间中，计算它们的内积，这个无限维空间包含了原始向量的所有可能的多项式组合（一次项、二次项、三次项...）。

线性注意力的出现本身就是为了解决长序列下O(n^2)的问题，但如果用这个函数，它自身将需要构造一个趋于无限维的特征空间，在一个本身就趋于无限长的序列上，这是计算机无法处理的。

因此我们必须对这个函数进行近似。常见近似方法：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． elu 函数： 对于， 输出 x； 对于 )(<=e， 输出 a(exp(x)-l) （通常 a= 1） elu(x)+l: 对于 〉 9， 约为 )(+ 1 （线性增长）； 对于 x 〈 =， 变为 exp(x) （指数增长）。 设计动机： 这个函数在 )(<=o 时具有类似指数的行为， 而在 )(>0 时保持线性， 整体上试图模拟指数函数的形状， 但它是有限维的 （输出维度与输入 x 相同）。 +(x)=ReLU(x)2: 2 ·。 这是一个简单的多项式函数。 ReLlJ(x) 将负值置零， 正值保留。 然后取平方。 它对应一个二阶多项式核， 计算简单， 但近似能力相对较弱。 3 基于随机特征的方法：。 这是一种数学上更严谨的近似方氵去。 通过随机投影 （如随机傅里叶特征） 来构造一个显式的、 有限维的特征映射 (P(x)， 使得 (P(q)•Q(k) 近似于某个目标核函数 （如高斯核， 它与指数核有关联）。 学习得到的核函数： 4 ·。 不手动设计， 而是让模型自己学习。 可以用一个小型神经网络 （如一层 MLP) 作为， 在训练过程中通过梯度下降来优化它， 使其能最好地配合整个模型完成亻壬务。

但这些核函数都不能足够好地拟合Softmax，这是线性注意力性能不如Full Attention的核心原因。

## 三、在自回归中的优势

线性化注意力还有一个独特优势：它可以被轻松地改写为递归形式，非常适合自回归推理。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

St = 乥） ％ 丿 = St-1 + 碴@严在生成第个 token 时： 1 ． 2 · 3 · 我们已经有之前所有 token 的累积状态 St-1 和一 1。 我们计算当前 token 的， 并更新状态： St = St-1 + 严， = 一 1 + 严 +(qt)St 然后使用当前查询计算输出： 一 +(qt)Zt

这就像一个RNN，在生成第 t 个token时，我们已经有了前 t-1 个token的完整总结 S_{t-1} 和 Z_{t-1}。当新的第 t 个token产生时，我们不需要回溯整个历史，只需要将当前token的贡献 ϕ(k_t)^T v_t 加到 S_{t-1} 上，将 ϕ(k_t)^T 加到 Z_{t-1} 上，就得到了新的状态 S_t 和 Z_t，然后用当前查询 q_t 与最新的状态交互：o_t = (ϕ(q_t) S_t) / (ϕ(q_t) Z_t)，得到当前时刻的输出。

因此，我们不需要存储整个键值的历史，只需要维护一个固定大小的状态 S 和 Z。每一步的计算成本是常数 O(1)（相对于序列长度），极大地提高了长文本生成的效率。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
