---
title: "19.2 Flash Attention"
source_docx: "第3部分 大语言模型/19.注意力机制的工程优化/19.2 Flash Attention.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 19.2 Flash Attention

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、Flash Attention解决的核心问题

如果说KV Cache解决了推理阶段的时间复杂度问题，那么Flash Attention的核心目的是解决训练阶段的空间复杂度问题，即IO瓶颈（Memory Bound）。

在GPU中，计算单元（SRAM）速度极快但容量小，而显存（HBM）容量大但读写速度慢。标准 Attention 的计算过程中，由于中间矩阵（n*n）太大，无法放入SRAM，导致必须频繁地与HBM进行读写交换。

Flash Attention 利用了两个核心数学技巧来实现加速和显存节省：分块计算（Tiling）和重计算（Recomputation）。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

输入矩阵为 Q, K, V e IRN × do 准 Atte ntion 输出 O 计算公式为： s： QK (S e P 一 softmax(S) (P e IRNXN) O = PV (O e IRNxd)

## 二、分块与Online Softmax

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

给定序列长度 N 和注意力头维度 d, 以及 GPU 片上 SRAM 的容量 M （以浮点数元素个数计）。 为了确保计算时 Q、 K、 V 的子块都能容纳在 SRAM 中， 论文中将分块大小计算如下： 列分块大小 （对应， 的序列维度）： Bc = 「 · 行分块大小 （对应 Q 的序列维度）： Br = min 4d ' 根据这两个块大小， 输入矩阵被切割： Q e 被切分为 = 「 雳 ] ^ 块： QI， · 一， QT， 每块大小为 Br × do K, e ]RNxd 被分别切分为一 「 个块： 1， ·， KT 与， 一 ·，， 每块大小为 Bc × do 输出矩阵 0 e ]RN><d 及统计量向量 I e、 m e 也按行被切分为块。

注意：这里不会把一个token的q（k和v同理）向量切分在不同的块中。也就是说，Q和K的相乘是[ Q_1 Q_2 ... Q_Tr]^T*[K_1 K_2 ... K_Tc]，这里对任意i,j，Q_i和K_j逐个相乘，得到Tr*Tc个块。输出是这Tr*Tc个块的Softmax(Q*K)*V结果矩阵加和（而不是拼接），因此可以分块计算后相加。在SRAM中维护一个“总和块”，每次计算一个这样的块并与“总和块”相加即可。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了避免一次性生成巨大的 N × N 矩阵， Flash Attention 将输入 Q, K, V 切分成小的引 ock， 在 SRAM 中分块计算。 难点： Softmax 是全局操作， 依赖于整行的最大值和分母之和。 m1 一 7 softmax(c 如果只看一部分数据， 无法确定全局的 m 和分母。 其中 m 一解决方案： 使用 Online Softmax 算法。 我们可以维护局部统计量， 并在处理新的 Block 时动态更新结果。 假设我们处理两个分块， 第一块的局部最大值为 ml， 局部指数和为未归一化的输出为 01。 第二块对应 m2， 12， 02。 合并后的全局最大值 mnew 和全局指数和 lnew 更新规则如下： neW 7 7 2 7 2 _ mnew

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

解决方案： 使用 Online Softmax 算法。 我们可以维护局部统计量， 并在处理新的 Block 时动态更新结果。 假设我们处理两个分块， 第一块的局部最大值为 ml， 局部指数和为未归一化的输出为 01。 第二块对应 m2， 12， 02。 合并后的全局最大值 mnew 和全局指数和 lnew 更新规则如下： m1 一 7 最终的输出 Onew 更新规则为： neW 尸 (diag(h)e 7 7 2 7 2 _ mnew 01 + diag(12)em2 7 1 一 77 通过这种方式， Flash Attention 只需要遍历一次 K, V， 并在 SRAM 中不断更新 O 的值， 最终写回 HBM0 中间产生的 N × N 的 Attention 矩阵 S 和 P 从未完整地存在于 HBM 中。

算法工作流：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

外层循环： 遍历 Query 分块对于 = 1 到： = 0， 1 ． 2 ． 3 ． 从 HBM 中将 0 加载到 SRAM 中。 在 SRAM 中为当前块初始化局部累加器： 0 = 0， 内层循环： 遍历 Key /Value 分块对于 j = 1 到： 77 1 ． 2 ． 3 ． 4 ． 从 HBM 中将塢和场加载到 SRAM0 计算局部注意力分数： 在 SRAM 中计算矩阵乘法： 计算局部最大值： 更新全局最大值： = rowmax()j） 比较旧的行最大值与刚计算出的局部最大值： neW max mi, 7 ．

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

计算局部的未归一化指数权重： 更新全局指数和 （带有缩放补偿）： 4 ． 5 ． 6 ． 7 ． 8 ． 一 lt + row 更新输出矩阵块 （带有缩放补偿与新值的累加）： 0 + PtjVj 状态更替： 将当前状态覆盖为新的全局状态， 准备处理下一个块： mt， ／ 些骘 0 最终归一化： 当内层循环结束 （即当前与所有的 K, V 块都计算完毕 ), 对累加好的 0 进行真实的 Softmax 归一化： 0 = diag(li)-lOt

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

5 ． 写回 H B M： 将计算完毕的 0、 以及统计量 mt （用于可能的反向传播重计算） 从 SRAM 一次性写回到 HBM0

## 三、重计算

在反向传播时不存储庞大的注意力权重矩阵P，而是将其重新算一遍，以避免n*n的空间复杂度让显存爆炸。

1.为什么反向传播需要P？

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了看清反向传播， 必须先列出前向传播的三个关键步骤： 1 ． 计算 Score: S = Q KT 2 ． 计算 Probability (Softmax): P = softmax(S) 3 ． 计算 Output: O = PV 曰 DC DC DC 我们假设最终的损失函数是乙我们的目标疋求 DQ ' DK ' DV。

假设我们已经得到了dL/dO.

计算dO/dV：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

OL pT， dO

计算dO/dQ和dO/dK：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

dP = dO · VT 这一步用到了 V, 暂时不需要 PO

接下来我们需要面对dP/dS，注意到Softmax函数不是线性函数，我们无法像用V^T表示dP/dO一样直接用常参数表示出dP/dS，相反，我们必然会用到P（或S）的值。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在微积分中， Softmax 函数） 有一个非常特殊的导数性质： 也就是说， Softmax 的导数计算， 必须用到 Softmax 的输出值本身。 在矩阵形式下， dS 的计算公式如下 （@ 代表逐元素乘法）： （： 这里 1 代表全 7 向量丿用于表示行求和仔细看这个公式， 里面全是 P！ 直觉解释： Softmax 函数在两端 （概率接近 0 或 1) 非常平缓， 梯度很小； 在中间梯度大。 要计算梯度 dS, 你必须知道当前的概率值 P 到底是在平缓区还是陡山肖区。 结论： 不知道 P， 就无法穿过 Softmax 层把梯度传给 Q 和 KO

2.重计算采用的方法

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

正因为上述两个原因， 反向传播计算梯度时， P 是绝对不可或缺的。 标准 Attention 算法的选择是： 存下来。 · 在前向传播时算好了 P, 即使它有 N × N 那么大， 也硬着头皮存进 HBM 显存里， 留给反向传播用。 这就是显存爆炸的根 / 丿、 0 Flash Attention 算法的选择是： 现用现算。 既然 P 必须用到， 但我又不想存它。 那我在反向传播需要用到 P 的那一瞬间， 利用已经存好的 Q, K 和 SRAM 里的统计量， 重新执行一遍前向计算 softmax( 昼 《） 生成出 P 的一个小块， 用完即丢。

3.为什么Q、K不用担心这一点？

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

简单来说， 不讨论 Q,K, V 是因为它们太 “ 小 " 了， 而 P （以及 S) 太 “ 大 " 了。 这里的 “ 大 " 和 " 小 “， 是指随着序列长度 N 增长时， 显存占用的增长速度不同： Q, K, V 的大小是线性增长 (O(N))0 P 的大小是平方增长 (O(N2))0 当 N 变得非常大 （长文本处理） 时， 平方项才是那个 " 撑爆显存的罪魁祸首 "。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

对 Q,K,V: 因为它们很小 (O(Nd))， 而且重计算它们的成本也不低 （要从更底层的输入再算一遍线性层 ), 所以 Flash Attention 选择把 Q, K, V 保存 (Cache) 在 HBM 显存里对 P： 因为它太大了 (O(N2)), 存它会导致 OOM (Out Of Memory), 而且它的 ℃ 开销巨大。 所以 Flash Attention 选择不存它， 而是用 Q, K, V 重新算一遍。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
