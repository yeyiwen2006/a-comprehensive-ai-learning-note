---
title: "19.5 Multi-Head Latent Attention"
source_docx: "第3部分 大语言模型/19.注意力机制的工程优化/19.5 Multi-Head Latent Attention.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 19.5 Multi-Head Latent Attention

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、基本思想

DeepSeek提出的MLA（Multi-Head Latent Attention）旨在解决现代GPU显存带宽不足的情况下，KV Cache显存占用过大的问题。它通过低秩矩阵分解和矩阵吸收技术，在推理时极大地压缩了KV Cache的体积，又不会出现MQA、GQA的损失问题。

在线性代数中，矩阵的“秩”代表了矩阵中真正独立的、不相关的信息量。假设我们有一个100*100的矩阵，如果第2行是第1行的2倍，第3行是第1行的3倍……那么虽然它100 行，但实际上只有1行是有用的信息。它的秩就是1。

在标准的 Multi-Head Attention 中，我们有H个头（比如32个）。每个头分开反向传播，模型会分别为每个头学习不同的投影矩阵W_K、W_V。事实上，这32个头学到的 Key 和 Value不是完全独立的，它们之间存在极强的相关性。比如处理单词“苹果”：头1关注它的“颜色（红）”，头2关注它的“形状（圆）”，头3关注它的“类别（水果），这些特征虽然不同，但它们都源自同一个语义核心“苹果”。W_K和W_V矩阵用于特征重组，它们事实上关注的是：头关注的特征向量可以看作原特征向量各个维度怎样的加权，既然特征之间存在强相关性，这些权重向量也就存在强相关性。因而DeepSeek MLA 的核心假设是：多头注意力（MHA）中的 Key-Value 矩阵虽然维度很高，但在数学上是“低秩”（Low-Rank）的。即：大部分参数都是冗余的。

数学上，如果一个大矩阵W_Full是低秩的，它就可以被无损（或极低损耗）地分解为两个低秩矩阵W_down*W_up的乘积，在MLA中这两个矩阵分别被称为降维矩阵和升维矩阵。从而我们可以进行如下MLA操作。

## 二、第一步：生成C_KV

在运用KV Cache的标准多头注意力中，我们每次读入一个token（x_t），都会生成d_model维（维度较高，如1024维）的向量k_t和v_t（其中包含了各个头，如第1-32维为第1个头，以此类推），存入显存：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

标准 MHA (MuIti-Head Attention) DeepSeek M LA = W CKVt = 铲刀五 v = RoPE （彬五月）

MLA认为，由于不同头的向量相关性较高，这里很多维事实上是“浪费”的，我们只需要用一个共用的降维矩阵W_down_KV将真正有用的信息存入一个极低维度（如64维）、所有头共用的向量C_KV_t，后续每个头再乘以升维矩阵的不同列（保留不同头的多样性）即可。得到C_KV_t的表达式：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

标准 MHA (MuIti-Head Attention) DeepSeek M LA = W CKVt = 铲刀五 v = RoPE （彬五月）

位置信息单独储存：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

标准 MHA (MuIti-Head Attention) DeepSeek M LA = W CKVt = 铲刀五 v = RoPE （彬五月）

## 三、第二步：运用结合律对注意力计算进行优化

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

数学推导： 原本的注意力计算应该是 Query 乘以 " 解压后的 Key"0 Score ‰ · (CKV · 彬乙） 利用矩阵乘法结合律 (AB)C = A(BC), 我们将 Key 的升维矩阵 WI 乙移动到 Query 端： S core （‰ · V% 乙） · CKV

我们先让Query去乘固定的参数矩阵W_UK^T，得到一个新的、被变换过的Query，记为 Q_absorbed，则对于第i个注意力头，当前（第t个）Token和第j个Token间的注意力权重为：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

SCOre

由于Q_absorbed是对于当前Token而言的，因此这一步只涉及1个Token的计算，开销极小。接下来乘以C_KV^T，我们只需要存储t*64维的压缩向量C_KV，而永远不需要在显存里复原巨大的t*1024维的K矩阵。在完成“矩阵吸收”后，这步就变成了Q(i)_absorbed对C_KV^T进行MQA操作，但却有了MHA的性能。

我们还需要加上解耦位置编码，单独计算RoPE部分的分数。从而对于第i个注意力头，当前（第t个）Token和第j个Token间的注意力权重为：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

内容分数 (Content) 位置分数 (RoPE) · CKVj + qt Scoret

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

（独有）： 第个头携带位置信息的 Query 向量。 （通常共享）： 携带位置信息的 Key 向量。 在 DeepSeek 的部分配置中， 为了进一步压缩， 个位置 Key 也可以在不同头之间共享。 作用： 这就相当于标准 Attention 中的位置匹配， 确保模型知道 Token 之间的相对距离。 这

## 四、运用分配律对信息聚合（注意力权重*V）进行优化

设C_KV_j向量的维度为d_c，升维后每个头的特征维度为d_h。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们已经算出了第个头在第 t 步对历史所有 j 的注意力概率分布： 注意力权重 (Probabilities)： 尸 · = Softmax(Score 压缩潜在向量 (Compressed Latent Vector)： c， 訇 （来自 KV Cache, 维度 dc) · value 升维矩阵 Up-Projection): 彬 '） （模型参数， 维度 dc ×） UV 我们的目标是计算该头的输出向量。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果我们按照标准逻辑， 第个头的输出 '） 需要先计算出专属的 value, 再求和： 原理解析： 解压操作。 UV 在这个公式中， 对于每一个头的每一个历史时刻 j， 都需要执行一次 c 0 · 冫的这是一个三重循环 （时间 × 头数 H × 历史 j), 计算量极大。

在只看第i个头的特征维度（有d_h个）的情况下：对于每一个j，都需要执行一次C_KV_j*W_UV，以计算出它的V，再乘标量P_t,j，单个token的V值计算的时间复杂度O(1*d_c*d_h+d_h)也就是；第1-t个token的都要这样计算然后求和，单个头的计算复杂度O(t*d_c*d_h).

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

DeepSeek 利用了矩阵乘法的分配律。 对于特定的头而言， 升维矩阵荪， 是一个常数矩阵 （它不随求和变量 j 变化）。 因此， 我们可以把它提取到求和符号 E 的外面。 优化后的推导过程： UV 这一变换将计算重构为两个阶段， 针对第个头的操作如下： 阶段一： 压缩空间内的聚合 (Context Aggregation) StLm · C 五孬

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

StLm 操作： 计算第个头的上下文聚合向量。 · C 五铲孬原理： 虽然 c， 厂是共孚的， 但因为每个头的权重号也是各不相同的。 维度： 结果是一个维度为 dc 的低维向量。 不同， 所以每个头聚合出来的结果

这一步对于第j个token执行一次权重P_t,j（标量）*C_KV_j（d_c维向量），再对每一个历史token（共t个）得到的值相加，计算复杂度O(t*d_c).

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

阶段二： 延迟升维 (DeIayed Up-Projection) St 7 UV · 操作： 将聚合后的向量映射回第个头的特征空间。 原理： 这一步昂贵的矩阵乘法， 对于每个头只需执行 1 次， 而不是次。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

计算出所有 H 个头的 ' 后， 我们将它们拼接起来， 并通过输出投影矩阵 0 得到最终的隐状态输 （2） Concat，，

这步C_sum为1*d_c维，W_UV为d_c*d_h维，单个头计算的时间复杂度O(1*d_c*d_h).

总体而言，计算复杂度降低了d_h倍。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
