---
title: "16.6 多模态大语言模型"
source_docx: "第3部分 大语言模型/16.大语言模型的基本原理/16.6 多模态大语言模型.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 16.6 多模态大语言模型

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、以文本为介质的多模态感知

1.基本思想

这种方式先由感知模型统一将图片、音频等转为语言（视频隔一定时间采样帧，转为图片+音频）。例如，独立训练（而非在LLM整体中端到端训练）一个已经预训练好的视觉Encoder，由于视觉Encoder嵌入空间和语言模型嵌入空间不一致，输出需要经过一个适配器后，再以具有语言意义、可以直接输入纯语言模型的token流的形式交给LLM处理。

这样的问题在于，一维语言难以描述高维细节，即适配器转化时存在信息损失。此外，模型整体也无法端到端优化。

2.视觉Encoder：以CLIP为例

输入图像被划分为固定大小的方格网格（通常为32×32或16×16像素），每个patch被展平为向量，转化为视觉token，经过多头自注意力层，得到图像的编码向量序列。

视觉Encoder通过“对比”来学习。模型被训练来“拉近”匹配的（正）样本对，同时“推远”不匹配的（负）样本对。如模型获取（图像，文本）对，图像和它对应的文本匹配时构成一个“正样本对”，不匹配时构成“负样本对”。给定一个 batch 的图像-文本对 (I1, T1), (I2, T2), ..., (In, Tn), 模型学习的目标是最大化匹配对 (Ii, Ti)的相似度得分，最小化 s(Ii, Tj)的相似度得分（j != i），要区分哪些图文是配对的，哪些不是（监督信号直接来源于数据本身，(Ii, Ti)是匹配的，其他随机组合的图文对 (Ii, Tj)被认为是大概率不匹配的）。

损失函数为InfoNCE。假设我们有：1 个“锚点”（Anchor）样本，记为 q（Query，查询）；1 个“正样本”（Positive），记为 k+（Key，与 q 匹配的键）；N 个“负样本”（Negatives），记为 k1-, ...,kN-（与q不匹配的键），我们希望q与k+的相似度远大于q与所有ki-的相似度。

InfoNCE 损失函数（针对单个q）的表达式如下：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

exp(sim(q,） 0） 一 log exp(sim(q,） 0） + 乥， 一 1 exp(sim(q,） 0）

sim(a, b)：相似度函数。通常使用余弦相似度或点积。

τ：温度系数。这是一个超参数，后续会解释。

exp(sim(q, k+) / τ)/[(exp(sim(q, k+) / τ)+sigma(exp(sim(q, ki-) / τ)))]：运用Softmax回归，将q与正样本匹配的得分除以q与“所有候选样本”（1 个正，N 个负）匹配的总得分，得到在给定q和N+1个候选样本（1 正 N 负）的情况下，模型认为k+是“正确匹配”（即正样本）的预测概率。我们的目标是让这个概率趋近于 1。

-log：交叉熵损失 (Cross-Entropy Loss)，假设我们的“真实标签”是一个one-hot编码 [1, 0, 0, ..., 0]（即第一个样本k+是正样本，其他都是负样本），Softmax 的输出就是模型预测的概率分布（例如 [0.9, 0.05, 0.03, ...]），对这个(N+1)类的分类问题取交叉熵损失，就得到了上述 InfoNCE 公式。

InfoNCE 损失就是将“对比学习”问题（拉近/推远）转化为了一个(N+1)选 1 的“分类问题”，并使用标准的交叉熵损失函数来优化模型，让模型总能从一堆负样本中准确地“识别”出那一个正样本，以实现“最大化相应序列和文本之间的表示相似性，并最小化负对之间的相似性”。训练后，一张“狗”的图像的向量表示会非常接近“一只狗”的文本向量表示，而远离“一只猫”的文本向量表示。

温度τ控制着 Softmax 函数的锐利程度。低温时相似度的值被放大，使得 Softmax 分布变得非常“尖锐”。exp(10)远大于exp(5)。模型被迫更“用力”地推开负样本，即使是那些与锚点有一定相似度的“困难负样本”（Hard Negatives）。这有助于模型学习到更精细的特征表示。高温时Softmax 分布变得非常“平滑”。exp(1)和exp(0.5)的差异不大。模型对负样本的惩罚相对宽松，最终呈现的嵌入结果是不会过分关注困难的负样本。

## 二、原生多模态感知

1.基本思想

将图片/音频/视频等多模态输入编码为token，和文本token并存，视觉信息和文本信息被编码为token向量的过程都参与LLM的端到端训练，得到的嵌入空间也是同一个嵌入空间（如“苹果”一词和苹果的图片位置相近）。模型在混合了多模态的数据集上进行预训练，能够直接理解不同模态间的深层关联（例如：直接从音频中感知情绪并转化为对应的画面描述），而无须经过文本中转。

2.可学习的视觉embedding层

和文本的给定输入图像（H*W*C），首先将其划分为N个固定大小的Patch（例如14*14像素）。Patch序列X_v = {x_1, x_2, ..., x_N}输入到可学习的视觉embedding层中，转换为token（嵌入向量）序列。随后，视觉token与文本token一起参加混合预训练。

3.混合预训练

模型接收的不再是单纯的文本流，而是模态混合流。假设输入是“[图片] 是一只猫”，模型实际接收的Embedding序列S为：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

S = Concat(Z(l) · Z， 到， Etext， 到）

这个拼接后的长序列S将直接输入到Transformer的第一层。

在 Transformer 的第l层，输入为H^{(l)}。此时，Query (Q)、Key (K)和Value (V)矩阵是由混合序列生成的。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设当前正在生成文本 Token （作为 Query), 它需要 “ 回顾 " 上下文中的图像信息。 设 Qtext 为当前文本的查询向量， ICtmg 和四为图像 Patc h 对应的键值对。 注意力计算不再区分模态， 而是统一计算： Attention(), K,） = Softmax 这里 [； ] 表示在序列维度上的拼接。 Q · Kte 了 ICtm img

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

相关性计算 (QKT): 当模型处理单词 " 猫 " 时， Qcat 会与图像中所有 Patch 的计算点积。 如果某个 Patc h 包含 " 胡须 “ 或 " 尖朵 " 特征， 点积值会非常大。 信息注入 （）： 通过 Softmax 加权， “ （图像的视觉信息） 被直接加权求和并注入到当前文本 Token 的隐状态 H(I) 中 cat 这意味着： 在第 1 层， 文本向量就 “ 吸收 " 了视觉向量； 在第 2 层， 吸收了混合信息的文本向量再次与原始视觉向量交互， 进行多步推理。

训练使用Next token prediction，目前仍然只生成文本token（即多模态限于感知阶段）。梯度贯穿模型的所有架构，包括视觉embedding层。

4.多维位置编码

对于图像，可采用2D位置编码：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了解决图像特有的空间关系问题， Gemini 对 RoPE 进行了二维扩展。 对于图像 Patc h 的位置 （和特征向量我们将向量分为两半 = 2 ]。 旋转操作定义为： RoPE2D， = Concat(RoPEID （， 1， £）， RoPEID@2,） 这样， 任意两个 Patc h 之间的注意力分数 Scoret•j 将不仅取决于它们的内容， 还取决于它们的相对空间距离 Ac 和 Ayo 这让模型能够理解 " 左边 "、 “ 上面 " 等几何概念。

对于视频，则可采取3D位置编码（x,y,t）。

5.微调

监督微调阶段，由于高质量指令-回答对往往为文本形式，可以不采用多模态混合训练。RL阶段的输入往往也是多模态混合信息流，这也符合Agent在真实世界中运行时环境反馈的特点。

## 三、图像处理的优化

### （一）语义对齐完备编码器（SAE）（LongCat-Next）

1.模型架构

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 SAE 的网络架构中， 输入图像并不是在同一个深度通道中 “ 一条路走到黑 "， 而是采用了多层级 / 双分支的提取策略。 · 显式语义对齐特征的提取 （深层网络）： 语义特征主要回答图像 " 是什么 " （What）。 SA E 利用深层的 Vision Transformer (ViT) 块， 通过多层的全局自注意力机制 (Self-Attention)， 将局部像素块的特征高度抽象化。 = ViTdeep@） e 隐式底层细节特征 “ 的提取 （浅层 / 旁路网络）： 底层特征主要回答图像细节 " 在哪里 " 以及 " 长怎样 “ （Where & H (w)。 SAE 会从 ViT 的浅层或中间层引出旁路分支 (Skip-connections), 或者使用一个轻量级的卷积辅助网络。 因为浅层网络尚未经历过度的全局池化， 保留了完整的高频空间结构、 边缘梯度和原始纹理。 ViTshallow

最后动态门控融合：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设一个多层感知机 (MLP) 网络用于预测门控权重矩阵 G e [ 0， 1 ] N、 刀其中为 Sigmoid 激活函数， [ ·， · ] 表示通道拼接。 最终的融合公式为哈达玛乘积 （逐元素相乘）： 丆 0 = G @ Fsem + （1 一 G) @ “ 物理意义： 对于图像中的 “ 大片纯色天空 "， 门控机制会放大 Fsem （强调这里是 " 天空 " ), 抑制多余的丆 ． 而对于图像中的 " 密集书本文字 “ 或 " 毛发 "， 门控机制会大幅放大丆以确保高频纹理 reS， reS， 被送入量化器， 保证后续渲染时不会模糊。

2.损失函数

（1）独立训练对齐阶段

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对 Fse 的监督： 细粒度密集对齐 (Dense Semantic Alignment) SA E 摒弃了传统的全局 C 凵 P 对比损失 （容易丢失局部物体信息 ), 转而采用细粒度的 " 区域一文本 " 密集对齐损失。 假设 T(i) 是与图像第个区域对应的文本特征向量， 模型通过交叉熵或对比学习最大化它们的相似度： S€TI exp(sim(Fs:m, T@))/T) log E 厂 1 exp(sim(Fs(!)n 丆叫 0） 这种监督使得 Fsem 具备了极高的语言亲和力， 大语言模型 (LLM) 能够轻松读懂它。 对 Fre 的监督： 掩码像素级重建 (Masked Pixel-level Reconstruction) 为了确保揖 “ 真正 " 隐式 " 地记住了物理世界的细节 （用于未来的图像生成 ), SAE 对其施加了类似 MAE (Masked Autoencoder) 或轻量级扩散模型的重建损失。 模型必须仅凭借 Fres 就能还原出原始的高频像素 /、、 get 2 target 2 在这里， 数学上的均方误差 (MSE) 强迫 “ 保留了所有光影、 材质等不能用语言轻易描述的隐式特征。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对 Fse 的监督： 细粒度密集对齐 (Dense Semantic Alignment) SA E 摒弃了传统的全局 C 凵 P 对比损失 （容易丢失局部物体信息 ), 转而采用细粒度的 " 区域一文本 " 密集对齐损失。 假设 T(i) 是与图像第个区域对应的文本特征向量， 模型通过交叉熵或对比学习最大化它们的相似度： S€TI exp(sim(Fs:m, T@))/T) log E 厂 1 exp(sim(Fs(!)n 丆叫 0） 这种监督使得 Fsem 具备了极高的语言亲和力， 大语言模型 (LLM) 能够轻松读懂它。 对 Fre 的监督： 掩码像素级重建 (Masked Pixel-level Reconstruction) 为了确保揖 “ 真正 " 隐式 " 地记住了物理世界的细节 （用于未来的图像生成 ), SAE 对其施加了类似 MAE (Masked Autoencoder) 或轻量级扩散模型的重建损失。 模型必须仅凭借 Fres 就能还原出原始的高频像素 /、、 get 2 target 2 在这里， 数学上的均方误差 (MSE) 强迫 “ 保留了所有光影、 材质等不能用语言轻易描述的隐式特征。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

仅仅分开监督还不够。 如果揖 “ 偷偷包含了一部分语义信息， 或者 Fsem 冗余地记录了位置信息， 就会导致后续量化时 “ 信息通道堵塞 " 0 为了让这两种特征在数学空间上绝对纯粹， SAE 引入了正交惩罚项 (orthogonal penalty)o 对于任意一个图像块六我们希望它的语义向量与细节向量在特征空间中尽量垂直 （即余弦相似度趋近于 0）： 个互不重叠的子空间中。 1 Cortho Sem， 2 Sem 2 reS reS 2 在总损失函数 £ 忉 = Csem + + £ “ / 旧的优化下， Fsem 和 Fres 被物理隔绝在了两

（2）原生多模态融合阶段

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当 SAE 这双 " 眼睛 " 彻底发育成熟后， 它才会被正式接入到基于 MoE 架构的 Transformer 基座中， 开启真正的多模态统一训练。 全局优化目标： 阶段一的局部损失宀 I 被完全抛弃。 整个庞大系统的全局唯一优化目标， 统一收敛为最纯粹的下一个 Token 预测损失 （£ 丷司。 SA E 的网络状态： 为了保护阶段一好不容易构建出的正交特征空间不被语言模型的强大梯度 “ 冲毁 "， SAE 的绝大部分核心权重在这一阶段会被冻结 (Frozen)0 联合微调： 通常只开放 SAE 与 LLM 之间的投影层 (Projector), 或者赋予 SAE 极小的学习率 （也可以引入 LoRA 旁路） 进行极微度的联合更新。 此时， 算力和梯度的重点全部转移到了 LLM 基座上， 让基座专心学习如何 " 阅读 " 和 “ 扌非列 " SA E 吐出的视觉 Tokeno

### （二）动态分辨率处理

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设输入的原生高分辨率图像为 lorig e RH><W><3， 其中 H 和分别为高度和宽度。 设 Vision Transformer (ViT) 基础编码器所能接受的标准输入尺寸 （即单个基础块的尺寸） 为 S × S （例如 336 × 336）。 为了控制计算复杂度和内存消耗， 系统会设定一个最大允许的子块数量上限 Nmaxo 第一步： 宽高比感知与最优网格计算 (Grid Optimization)， 然后计算出一个最贴近该比例的二维网格排布模型首先感知图像的原始宽高比月 = (Nh,Nw), 其中 Nh 是行数， Nw 是列数。 模型会在所有满足总块数 Nh × Nw Nmax 的整数对候选集 g 中， 寻找与原始比例差异最小的组合。 其数学优化目标可以表示为最小化对数比例差： (Nh,Nw) = arg min log (h,w)eg 一 10g(R) 解释： 通过这一步， 如果输入是一张宽幅全景图 （例如宽高比为 1： 4）， 算法可能会计算出 1 × 4 的网格； 如果是竖版手机截图， 可能会算出 3 × 1 或 4 × 2 的网格。 这确保了后续切分时的物理网格扌非布与原图形状高度一致。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设输入的原生高分辨率图像为 lorig e RH><W><3， 其中 H 和分别为高度和宽度。 设 Vision Transformer (ViT) 基础编码器所能接受的标准输入尺寸 （即单个基础块的尺寸） 为 S × S （例如 336 × 336）。 为了控制计算复杂度和内存消耗， 系统会设定一个最大允许的子块数量上限 Nmaxo 第一步： 宽高比感知与最优网格计算 (Grid Optimization)， 然后计算出一个最贴近该比例的二维网格排布模型首先感知图像的原始宽高比月 = (Nh,Nw), 其中 Nh 是行数， Nw 是列数。 模型会在所有满足总块数 Nh × Nw Nmax 的整数对候选集 g 中， 寻找与原始比例差异最小的组合。 其数学优化目标可以表示为最小化对数比例差： (Nh,Nw) = arg min log (h,w)eg 一 10g(R) 解释： 通过这一步， 如果输入是一张宽幅全景图 （例如宽高比为 1： 4）， 算法可能会计算出 1 × 4 的网格； 如果是竖版手机截图， 可能会算出 3 × 1 或 4 × 2 的网格。 这确保了后续切分时的物理网格扌非布与原图形状高度一致。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第二步： 无损比例缩放与自适应填充 (Proportional Resizing & Padding) 确定了网格维度 （，， N！。） 后， 目标网格的总物理分辨率被确定为： 一凿 × S target 一 Nw × S target 为了将原图放入这个网格中且不产生任何拉伸形变， 算法计算等比例缩放系数 (Scale)： Scale = mm 然后按此系数将原图等比缩放为新尺寸 (H', (')： target 丑 target W H' = round （丑 × Scale) W' = round(W × Scale） 缩放后的图像 “ 2 会被置于目标网格的中心或左上角。 对于未能完全填满的边缘空白区域 （由于 H' < 且 / <） 算法会使用背景色 （如均值像素或纯黑） 进行零填充 target target ' 77 老 arg × Wtarget × 3 (Zero-Padding), 得到最终准备切分的标准图像 a e ]R 0

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 视觉Encoder通过“对比”来学习。模型被训练来“拉近”匹配的（正）样本对，同时“推远”不匹配的（负）样本对。如模型获取（图像，文本）对，图像和它对应的文本匹配时构成一个“正样本对”，不匹配时构成“负样本对”。给定一个 batch 的图像-文本对 (I1, T1), (I2, T2), ..., (In, Tn), 模型学习的目标是最大化匹配对 (Ii, Ti)的相似度得分，最小化 s(Ii, Tj)的相似度得分（j != i），要区分哪些图文是配对的，哪些不是（监督信号直...

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
