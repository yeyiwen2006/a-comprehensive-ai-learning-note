---
title: "18.4 高维向量的量化（论文）"
source_docx: "第3部分 大语言模型/18.模型压缩/18.4 高维向量的量化（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 18.4 高维向量的量化（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、问题背景

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在人工智能和深度学习领域， 高效处理高维向量至关重要。 例如： 大语言模型 (LLM) 的 KV Cache: 自回归模型在生成长文本时， 需要存储大量历史 Token 的键值对 (Key-Value Cache)0 随着上下文变长， 内存消耗和访存延迟会显著增加。 通过量化压缩这些向量， 可以大幅降低推理成本。 + 1 · 向量数据库与近似最近邻搜索 (ANNS)： 在检索增强生成 （RAG） 和信息检索中， 需要快速计算查询向量与数据库向量的内积或余弦相似度。 现有方法的局限性： 目前的量化算法往往面临两难选择。 要么计算速度慢、 缺乏硬件加速器 （如 GPU) 的兼容性， 无法满足在线 (Online) 实时量化的需求； 要么在给定位宽 (Bit-width) 下无法达到最优的失真下界。 此外， 许多现有方法依赖于离线数据 (Data-dependent), 需要耗时的预处理和微调。 + 3

下面给出了一些学界就高维向量量化算法的研究。

## 二、针对均方误差优化的算法

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 针对均方误差优化的 Turbo Quantmse 该算法旨在最小化量化前后的均方误差 (MSE)0 其核心理论依据是： 将输入向量进行随机旋转后， 其各个坐标在数学上会服从 Beta 分布 （在高维下渐近于高斯分布 ), 且不同坐标之间几乎相互独立。 因此， 可以对每个坐标独立应用最优的标量量化 (Lloyd-Max 算法） + 1 TurboQuantms 的工作流： 1 ． 2 ． 3 ． 初始化 （预计算）： 生成一个全局的随机旋转矩阵 II e。 针对 Beta 分布求解一维连续 K-Means 问题， 预先计算并存储最优的聚类中心 ℃ entroids) 代码本 C1,C2, ·， C2b。 + 2 量化过程 (QUANT)： 接收输入向量 e · 应用随机旋转矩阵将其转换为一 II ·。 对于旋转后向量的每一个坐标屿， 在代码本中找到距离最近的聚类中心， 并记录其索输出这些由 b 个比特表示的索引向量。 反量化过程 (DEQUANT)： 接收索引向量， 从代码本中查找对应的聚类中心， 重建向量。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 针对均方误差优化的 Turbo Quantmse 该算法旨在最小化量化前后的均方误差 (MSE)0 其核心理论依据是： 将输入向量进行随机旋转后， 其各个坐标在数学上会服从 Beta 分布 （在高维下渐近于高斯分布 ), 且不同坐标之间几乎相互独立。 因此， 可以对每个坐标独立应用最优的标量量化 (Lloyd-Max 算法） + 1 TurboQuantms 的工作流： 1 ． 2 ． 3 ． 初始化 （预计算）： 生成一个全局的随机旋转矩阵 II e。 针对 Beta 分布求解一维连续 K-Means 问题， 预先计算并存储最优的聚类中心 ℃ entroids) 代码本 C1,C2, ·， C2b。 + 2 量化过程 (QUANT)： 接收输入向量 e · 应用随机旋转矩阵将其转换为一 II ·。 对于旋转后向量的每一个坐标屿， 在代码本中找到距离最近的聚类中心， 并记录其索输出这些由 b 个比特表示的索引向量。 反量化过程 (DEQUANT)： 接收索引向量， 从代码本中查找对应的聚类中心， 重建向量。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

· 应用逆旋转矩阵进行恢复： 一 IIT ·。

## 三、针对内积无偏估计的算法

MSE最优的量化器在估计内积时会引入偏差，这在低位宽时尤其明显。故需要针对内积无偏估计的算法。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 初始化： 实例化一个位宽为 b 一 1 的 QUANT 布的随机投影矩阵 S e Rdxd 量化过程 (QUANT)： 量化器， 并生成一个元素服从标准正态分使用 b 一 1 位的 QUAN T 灬对输入向量进行量化， 得到索引记通过反量化计算残差向量： = 一 DEQUANTmse(idc)0 对残差向量应用 l-bit QJL 变换： qjl = sign （S · r)o 输出元组： （记 q， 目 r112） （包含 MSE 索引、 残差的 l-bit 符号向量和残差的 L2 范反量化过程 (DEQUANT)： 首先通过 DEQUANTmse@南） 饭复出基础向量灬 e。 丌 / 2 · 通过 QJ L 的逆映射复残差向量： = · 将两者相加得到最终重建向量 · 灬 e + 讠@。 目 r112 · sT · qjlo

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
