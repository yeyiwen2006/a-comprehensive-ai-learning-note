---
title: "18.2 模型蒸馏"
source_docx: "第3部分 大语言模型/18.模型压缩/18.2 模型蒸馏.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 18.2 模型蒸馏

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、核心思想

在深度学习中，我们通常训练一个庞大、复杂的模型来获得高准确率。但在实际部署（如手机端、IoT设备）时，由于算力和内存限制，无法直接使用这个大模型。我们让一个小模型（Student）通过模仿一个大模型（Teacher）的行为，来学习大模型中蕴含的“暗知识”，从而在保持较小参数规模的同时，尽可能达到接近大模型的性能。下面引入两个概念：

Hard Target（硬标签）：老师告诉学生：答案是“A”。这对应传统的交叉熵损失函数，即训练时专注增加输出A的概率，不关心输出B、C的概率。

Soft Target（软标签）：老师告诉学生：“答案选A，但B也有点道理，C是大错特错的。”这种包含概率分布的信息（如A=0.7, B=0.2, C=0.1）蕴含了类别之间的相似度关系，即大模型内的“暗知识”。

模型蒸馏会同时学习这两种标签。（不可只学习软标签，那样会导致输出正确答案的概率下降）

## 二、数学表达

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 带温度的 Softmax 在标准的分类任务中， 神经网络输出 logits， 经过 Softmax 得到概率。 在蒸馏中， 我们引入超参数丆： exp(zt/T) Ej exp(zj/T) 当 T = 1 时， 这就是标准的 Softmaxo 当 T > 1 时， 概率分布变得更 " 平滑 “ (Softer)0 这使得原本接近 0 的错误类别的概率被放大， 从而暴露出模型学到的类别间的隐含关系 （例如： 在识别 " 狗 " 时， 模型认为像 " 猫 “ 的概率比像氵彐车的概率大）。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 损失函数 (Loss Function) 学生模型的总损失函数通常由两部分组成： = a 力五刀 + （1 一 a)LCE 其中： 力五刀 （蒸馏损知软损失）： 衡量学生模型产生的软概率分布与教师模型产生的软概率分布之间的差异。 通常使用 KL 散度 (Kullback-Leibler Divergence)0 注意， 计算此时使用的是高氵皿力 “ （学生损失 / 硬损失）： 衡量学生模型的输出与真实标签 (Ground Truth) 之间的差异。 通常使用标准的交叉熵损失 (Cross-Entropy Loss)0 注意， 计算此时使用的是 T = 1。 a： 平衡两个损失权重的超参数。

注意这里的KL散度是forward KL，因为：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

KL 散度的标准数学定义为： DKL(PIIQ) = E ～ 尸 log 请注意公式中的期望符号 E 冖宀这意味着我们要从分布 P 中采样数据来计算这个积分或求和。 在机器学习的语境下， 谁负责在物理世界 （或计算图中） 生成采样数据， 谁就必须排在 KL 散度的前面。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 · 数学推导： 为什么高温有效？ 假设 logits 相比于 T 很小 （即 zJT 《 I), 我们可以对 Softmax 函数进行泰勒展开。 Softmax 输出大约为： 假设 logits 均值为 0 （即 1 + zJT N + E ． zj/T = O), 则简化为： 1 + zJT

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

（s 忉园的梯度通常假设我们使用交叉熵损失函数 （或 KL 散度） 进行蒸馏， 其针对学生模型 Logit 形式为： DL 1 （s 忉） (Teacher) Dz(Student) （： 这里的 1 / T 来自于饉法则中对 z， / T 求导产生的系数丿现在， 我们将上面的高温概率近似代入 qt： DL 1 Dz s 忉 T

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

（s 忉园的梯度通常假设我们使用交叉熵损失函数 （或 KL 散度） 进行蒸馏， 其针对学生模型 Logit 形式为： DL 1 （s 忉） (Teacher) Dz(Student) （： 这里的 1 / T 来自于饉法则中对 z， / T 求导产生的系数丿现在， 我们将上面的高温概率近似代入 qt： DL 1 Dz s 忉 T

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 （S 忉 “ 0 (Teacher）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

忽略常数系数一一一我们发现梯度正比于： N 2 ' DL （s 忉） (Teacher) Dzt 回顾均方误差 (MSE) 的梯度： 如果我们直接定义损失函数为 Logits 之间的 MSE: LMSE = 1 （Student) (Teacher）） 2 其梯度正是 (z(Student) 2 这就证明了： 在高温极限下， 最小化 KL 散度 （或软交叉熵） 等价于最小化 Logits 之间的均方误差。

当然，实际上我们一般取T为2~20，平衡Hard Target的“关注最大值”和MSE的“平衡关注各方向分布”。从实际意义上来说：

在正常温度下，Softmax会把最大的Logit变成接近1的概率，而把其他Logits压缩成接近 0。

Teacher Logits: [10, 5, 2] -> Softmax: [0.99, 0.009, 0.001]

Teacher Logits: [10, 8, 8] -> Softmax: [0.8, 0.1, 0.1]

如果只看概率，很多细微的差异（比如5和2的区别）被“压扁”了，学生模型很难学到。

而Logits直接反映了模型对各个类别的原始置信度。

Logits [10, 5, 2]明确告诉学生：虽然类别1最强，但类别2比类别3强得多（强3个单位），这种信息，就是暗知识 (Dark Knowledge)。

上面模型蒸馏的数学表达，事实上正是“匹配Logits”思想的体现。它的结果是：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

方向对齐： 蒸馏过程要求学生生成的 Logits 向量 zs 和教师的 Logits 向量指向同一个方向。 这意味着学生不仅要学会 " 谁是第一名 "， 还要学会 " 第二名、 第三名 “ ． 第 N 名 “ 的相对排序。 相对大小对齐： 一代表了类别和类别的相对差距 (Log-odds)0 Logits Matching 强迫学生模型复刻这种相对差距。 如果老师认为 " 猫 " 比 " 汽车 “ 更像 " 狗 " ℃ 倍， 学生也必须认为 “ 猫 " 比 " 汽车 " 更像 “ 狗 " 10 倍。

## 三、模型蒸馏的多种方法

1.基于响应：利用教师模型最后一层的输出，这是最经典的方法（如上文所述）。

2.基于特征：不仅学习输出，还强制学生模型的中间层特征图拟合教师模型的中间层。通常需要引入额外的卷积层来匹配特征图的维度。

损失函数：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 Feature

3.基于样本关系：

对于同一批N个样本，分别获取它们在教师模型的特征空间中的特征向量（一般为其中一层的向量），以及在学生模型的特征空间中的特征向量。由于教师模型和学生模型特征向量维度不同，无法直接计算损失，故我们考虑计算向量两两之间的“相似度”（标量）。

我们对教师模型的这些特征向量两两计算余弦相似度或欧氏距离，得到N*N的“相似度矩阵”，对学生模型也得到这样的矩阵。如果用的是欧氏距离，为了消除维度差异，我们还会加归一化因子。我们的目标是让这两个矩阵长得像，即结构一致性：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

N N 月 a 历 0 其中为可以是 Huber Loss 或力 2 Losso

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Huber Loss 是一种鲁棒回归损失函数， 它结合了均方误差 (MSE/L2) 和平均绝对误差 (MAE/LI) 的优点。 简单来说， 它的设计哲学是： “ 小错误用平方惩罚 （为了平滑收敛 ), 大错误用线性惩罚 （为了防止梯度爆炸）。 "

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Huber Loss 力，） 对于预测误差 a = 一刁的定义如下： for @ > 力如） = 其中： · a 是残差 （真实值与预测值的差）。 2 (delta) 是一个超参数， 作为 “ 分界占 "

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

当误差很小 （@| 时： 它是一个抛物线 （像 MSE)0 优点： 在 0 点附近是可导的， 梯度会随着误差变小而变小， 有利于模型在最后阶段精细收敛， 不会像 MAE 那样在 0 点附近震荡。 当误差很大 （国 > 时： 它变成了直线 （像 MAE)0 优点： 它的梯度是常数 （或者一）。 这意味着即使遇到极端的离群点 (Outliers), 产生的梯度也不会无穷大， 避免了 MSE 那种 “ 因为一个离群点导致整个模型参数被带偏 " 的情况。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对噪声鲁棒： 教师模型并不是完美的， 它产生的关系矩阵可能包含噪声。 如果某两个样本， 教师认为距离是 ℃ 0 （可能是异常值 ), 而学生认为是 1， 由于 MSE 是平方增长， Loss 会变得巨大 （992 9800）， 这会导致学生模型的梯度剧烈波动。 使用 Huber Loss, 这部分的惩罚是线性的， 相对温和。

## 四、On-policy蒸馏

传统的模型蒸馏是Off-policy的，教师模型自回归生成，学生模型则每次基于问题和教师模型的第1~i-1个token生成第i个token，让它的输出分布接近教师模型的输出分布。这就类似于描红，学生拿着半透明的描红纸盖在上面。教师写了前三笔，学生就顺着前三笔的笔势去“猜”第四笔该落在哪。学生全程都被教师的笔迹限制在一条正确的路径上。

既然学生模型在训练时从来没有自己自回归生成，那么当它被部署到真实环境（Test Time）中时，一旦没有了大师的字帖打底，学生必须自己从头写到尾。如果它在第3步不小心写歪了一点点，到了第4步时，它面对的上下文就是一个包含错误的、自己从未在训练中见过的陌生前缀。由于它在训练时只见过完美的路径，完全不知道怎么在错误路径上纠偏，接下来的生成就会彻底崩溃。

On-policy蒸馏则较好地解决了这个问题：让学生模型自己在环境中自由探索，自回归生成完整的轨迹y。即便这条轨迹包含错误、绕路或表述不佳，也都保留下来。把这条“充满学生个人风格（甚至错误）”的轨迹喂给教师大模型。教师大模型就像批改作业一样，针对这条轨迹的每一个时间步，给出全词表上的标准概率分布（Logits）。学生模型再根据这个分布来更新自己的参数。学生模型完全在自己的能力边界和状态空间内进行探索。它学到的是“如果在我的水平下走到了这一步，大模型会建议我下一步怎么救场或继续”。由于通过学生的轨迹采样，故运用反向KL散度学习：

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

． D 五刀 （丌，， der Tteacher 黟 ． ～ 丌司

这样可以使模型分布稳定在教师“圈定”的合理范围内，保证输出质量。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
