---
title: "21.2 VAPO算法（论文）"
source_docx: "第3部分 大语言模型/21.强化微调的前沿探索/21.2 VAPO算法（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 21.2 VAPO算法（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

VAPO（Value-model-based Augmented Proximal Policy Optimization）回归了Actor-Critic，是PPO的一种改进算法，也融合了PPO以及GRPO/DAPO的一些设计思想。

## 一、主要改进

1.价值预训练（Value Pre-training）

在正式强化学习（RL）之前，先在一个高质量数据集上专门训练Critic（价值模型）。在 RLHF 初期，如果Critic是随机初始化的，它对长序列的价值估计是一团糟。Actor会被错误的Critic 带偏，浪费算力。预训练让Critic在“上岗”前就已经学会了判断什么是有希望的推理路径，从而在RL开始的第一步就能提供准确的长期回报（Long-term Return）估计，避免让Actor白白浪费大量算力。

2.解耦GAE（Decoupled GAE）

强化学习中，“偏差-方差权衡”是一个重要的话题。蒙特卡洛方法（舍弃Critic网络）无偏，但是方差大，训练不稳定；时序差分方法（运用Critic网络）每一步Critic估计的偏差会不断累积，导致在几百步的推理中，最关键的最终答案的奖励很难传导回第一步。

VAPO算法提出了解耦GAE方法，将这二者结合起来。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

` 104 ． 丿 GRPO 成分 （锚定真实回报） ppo 成分 （依赖时序差分） 或者在计算 GAE 的递归过程中， 强制注入真实回报： 解耦修正项

这里R就是蒙特卡洛估计中的G_t：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Gt = + 70 + 1 + 7 0 + 2 + 一 · + 7 _ 咒 “ I · 在数学推理中， 通常只有最后有奖励， 且 7 一 1。 所以 0 = 咒疖，，。 I （最终奖励是多少， 这里就是多少）。

解耦GAE方法最重要的意义是在早期Critic有偏差时引入蒙特卡洛方法准确估计，在后期蒙特卡洛方法不稳定时用Critic促进收敛。因此，参数β一般会随时间递增而衰减，如从1.0到0.1.（事实上，即便不调整β，R_final-V(s_t)的绝对值一般也会随着V(s_t)估计趋稳而减小，但衰减效果更好）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

早期 (Critic 弱时）： 我们需要更强的 MC 信号 (GRPO 成分） 来告诉模型 “ 什么是对的 "。 解耦项保证了即使 Critic 预测 V(st+1) 是垃圾， 最终的咒也能直接越过中间环节， 给修正方向。 后期 (Critic 强时）： Critic 已经学会了每一步的精细价值。 此时 AGAE (PPO 成分） 变得非常准， 能够区分出 " 最后答案虽然对了， 但中间第 5 步其实有点哕嗦 " 这种细微差别。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

VAPO Advant age WI · Reality (GRPO/MC) + W2 · Prediction (PPO/Critic) · 它利用 GRPO 的 “ 真 " （真实回报） 来纠正 Critic 的偏差。 · 它利用 PPO 的 “ 稳 " ℃ ritic 提供的密集信号） 来降低长序列蒙特卡洛采样的方差。 这种设计使得 VAPO 既能像 GRPO 那样在大模型推理初期快速找到正确方向， 又能像 PPO 那样在后期精细打磨每一步的逻辑质量。

3.长度自适应GAE（Length-Adaptive GAE）

长度自适应GAE和解耦GAE一样，也是平衡偏差与方差的重要方法。但区别在于，解耦GAE（Decoupled GAE）解决的是“信号源准不准”的问题（对抗 Critic 的早期无能），长度自适应GAE（Length-Adaptive GAE）解决的是“信号传得远不远”的问题（对抗数学上的指数衰减），以适应不同长度的推理问题。

定义有效视野：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

艹灬乥一 1 一入

这本质上是用几何分布的数学期望定义的。可以理解为：持续获取回报，但每次衰减λ，期望上等价于：假设一个人从t处出发，专门负责收取后面的回报，用于更新A_t。此人每次有λ的概率能继续往前走，获取下一轮回报，有1-λ的概率被拦住（也就是以后的回报全部被直接“截留”，不能被A_t获取），则在数学期望上能一直走到第1/(1-λ)轮，即A_t可以获取前1/(1-λ)轮的回报，这就是“有效视野”。

一个固定的λ（如0.95，Horizon=20）不能适应不同长度要求。对于一个10步的回答，看20步太远了（远处样本权重偏大，而远处样本是蒙特卡洛采样获得，有随机性，方差大）；对于一个1000 步的推理，看20步简直是“鼠目寸光”（偏差大，看不见结尾的奖励）。因此，我们希望有效视野H和序列长度L成正比（H=α*L），得到λ=1-1/(α*L)。下面以α＝1为例：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

让我们代入数值 （设 a = 1) 看看发生了什么： 样本类型短对话中等推超长 CoT 长度力 20 ℃ 00 标准 GAE （/\ = 0 ． 95） /\ = 0 ． 95 = 0 ． 95 /\ = 0 ． 95 = 0 ． 99 /\ = 0 ． 95 自适应 GAE （_ 1 / 力） = 0 ． 999 效果分析两者一致， 视野合适。 标准 GAE 开始看不清结尾； 自适应 GAE 强行提升 ,\， 减少衰减。 标准 GAE 信号完全消失； 自适应 GAE 将推向极限， 确保第 ℃ 00 步的奖励能传回第 1 步。

对于长样本（Long CoT），它通过增大λ（接近1），将算法行为推向蒙特卡洛，容忍高方差，但确保信号不丢失；对于短样本（Short QA），它保持适中的λ，利用时序差分的低方差特性，让训练更稳定。

4.Token级损失

同DAPO，对所有token的损失进行平均（而非对样本平均），所有token对最终损失的贡献均匀分配，避免关键错误被稀释。

5.组采样

标准PPO假设环境是平稳的。但在数学推理中，如果模型很弱，采样1次大概率是错的（Reward=0）。如果一直采样一直错，Critic就学不到东西。VAPO的Group Sampling则是对每个Prompt，一次采4个或8个，只要其中有一个是对的，Critic 就能通过对比（解耦GAE中利用真实回报）学到什么是对的。

## 二、工作流与PPO对比

1.初始化

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

PPO: 通常直接初始化 Actor （来自 S FT 模型） 和 Critic （通常基于 Rewa rd Model 初始化）。 不同点： 直接开始训练， 没有专门针对 Critic 的预热环节。 VAPO： 新增步骤： value Pretraining （价值预训练） 不同点： 在进入 RL 循环前， 先使用 S FT 数据或推理数据监督训练 value Model （玲）。 目的： 消除奖励模型带来的偏差， 让 Critic 在 “ 上岗 " 第一天就能比较准确地预测长序列价值， 防止 PPO 早期崩盘。

2.数据采样

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

维度采样策具体操数据形目的奖励计算 Value 计算标准 PPO (Standard PPO) 单次采样 (Single/Random Sampling) 对于一个 Prompt q， 通常采样 1 个回答 a （或者 Batch 内不同 Prompt 各采一个）。 轨迹 {， 0 } 是独立的。 获取环境反馈， 计算梯度。 通常只看结果奖励 (Outcome Rewa rd） 或稀疏奖励。 在采样时顺便计算 V(st)0 VAPO (Augmented PPO) 组采样 (Group Sampling) 对于一个 Prompt q, 强制策略生成一组 G 个不同的回答 {， · 数据以 “ 组 （Group） " 为单位存在， 组内包含成功和失败的样本。 增强对比信号。 通过生成多个回答， 提高采样到 “ 正确答案 “ 的概率， 确保存活下来的样本中有正向信号可子。 密集奖励。 通常结合结果奖励和过程奖励 (Process Rewa rd）。 同样需要计算 V(st), 但用于后续更复杂的解耦计算。

注：同时处理若干个Prompt，都存入经验回放池。如同时处理16个Prompt，标准PPO在每个Prompt下各生成64个token的回答，就会往经验回放池存入1024个数据；VAPO在每个Prompt下各生成8组各64个token的回答，就会存入8192个数据。

3.优势估计

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

PPO: 算法： 标准 GAE (Generalized Advantage Estimation)0 公式： = E(7Ä)löt+10 特征： 是固定的常数 （例如 0 ． 95）。 局限： 对长短不一的序列使用相同的视野， 导致长序列信号丢失。 VAPO: 算法： 解耦 GAE + 长度自适应 GAEO 步骤 1 (Length Analysis): 先计算每个回答的长度。 1 步骤 2 (Dynamic Ä)： 为每个样本单独计算， 满足 = alio 序列越长， 越接近步骤 3 (DecoupIed CaIcuIation): 结合 Critic 预测 （5） 和真实回报月， 使用解耦公式计算， 确保信号穿透整个长序列。

4.优化更新

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

PPO: · Loss 聚合： 通常计算 Sample-level Meano 即将一个 Batch 内所有样本的 Loss 求平均。 问题： 长文本 （1000 token) 和短文本 （℃ token) 权重相同， 导致长文本的每个 toke n 梯度被稀释 ℃ 0 倍。 更新： 同时更新 O 和夯 VAPO: · LOSS 聚合： Token-level Losso · 操作： 将所有样本的所有 Token 损失求和 ()E 力 055D， 然后除以总 Token 数。 目的： 实现 "Token 平权 "， 保证长链推理中的每一个步骤都对梯度有足够的贡献， 避免关键错误被稀释。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
