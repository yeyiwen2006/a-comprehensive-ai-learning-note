---
title: "21.1 DAPO算法（论文）"
source_docx: "第3部分 大语言模型/21.强化微调的前沿探索/21.1 DAPO算法（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 21.1 DAPO算法（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

DeepSeek的成功证明了GRPO范式的有效性和优越性。但是，GRPO仍然存在稳定性问题，容易导致训练崩溃。DeepSeek拥有足够多的数据，可以使其趋于稳定。但对于中小规模的强化微调，如果数据批量较小，就必须对算法本身进行改进。

DAPO（Decoupled Clip and Dynamic Sampling Policy Optimization）就是其中一种改进，它基本上沿用了GRPO的算法原理，但做出了一些工程策略上的改进。

## 一、改进点

1.Clip-Higher 机制（解耦 Clip）

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这是 DAPO 最显著的数学改进。 原理： 传统的 PPO/GRPO 使用对称的裁剪范围 （1 一 6， 1 + 6）。 DAPO 将上界和下界解耦， 设定为 （1 一 qow, 1 + 0， 匆 /，）。 目的： 研究者发现， 在训练早期， 模型需要探索那些原本概率较低 (low-probability) 但可能是正确的 tokeno · 操作： 通过增加的值 （例如设置得比 qow 更大）， 允许模型在 " 变好 " 的方向上 （即概率比昀@） > 1） 迈出更大的步子， 而不会被过早截断。 这能显著提升模型在训练早期的熵 (Entropy), 即探索能力。

2.动态采样

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这是一个数据效率层面的改进。 原理： 在数学推理等任务中， 如果对于一个问题 q, 模型采样的 G 个答案 { 01， · ·， OG } 全对 (Reward 全为 1） 或者全错 (Reward 全为 O), 那么这组数据提供的梯度信息是非常有限甚至无效的 （因为没有对比）。 操作： DAPO 引入了一个过滤条件： 0 < 和， 》 is-equivalent(), 0， < G 只有当一组样本中既有正样本又有负样本时， 才进行训练。 这意味着它会自动过滤掉那些 " 太简单 " 或 " 太难 " 的 prompt, 只保留有 " 有效梯度 " 的样本。

3.Token级策略梯度损失（为什么这样更公平的原因见后）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这是对损失函数聚合方式的改进。 原理： 标准的实现通常是计算每个样本的 Loss, 然后对 Batch Size 取平均。 但这忽略了样本长度的差异。 长序列包含更多 token， 如果简单对样本取平均， 长序列中每个 to ke n 的贡献会被稀释。 操作： DAPO 对 Batch 内的所有 Token 一起求和并取平均 （即除以 @小见公式 8）。 这保证了无论回复长短， 每个 toke n 对梯度的贡献是公平的。

4.超长奖励调整

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

原理： 为了防止模型为了 " 刷分 " 而生成无限长的废话 （Rewa rd Hacking 的一种）。 · 操作： 定义一个最大长度阈值， 超过该阈值后施加 " Soft 惩罚 "。 越长罚得越重。

### （二）工作流对比

GAPO：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

GRPO 标准工作流 1 ． 2 ． 3 ． 4 ． 5 ． 采样 (Rollout)： 对于每个问题 q, 从旧策略丌岿采样一组 G 个输出 { 01 四 2， 奖励计算 (Evaluation): 计算每个输出的奖励 frl，， 优势估计 (Advantage)： · 计算组内奖励的均值和标准差。 ri—mean · 标准化优势： 翡一 std(r)+e 损失计算 (Loss)： · 计算每个样本的 PPO 损失。 聚合方式： 对 G 个样本的损失取平均值上力 05。 反向传播： 更新模型参数。 ·， OG}O

DAPO：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 过采样 (Over-Sampling)： 对于每个问题 q, 进行采样 （可能采样多于 G 个， 或者采样 G 个后进行筛选）。 奖励计算 (Evaluation): 计算奖励， 判断每个答案是否正确 （Rewa rd： 1 或 0）。 2 ． 动态采样筛选 (Dynamic Sampling - 关键改进）： 3 ． 检查这一组样本的奖励分布。 判断条件： 如果这一组答案全部正确 (Reward 全为 1） 或全部错误 (Reward 全为 O), 则直接丢弃整组数据， 不进行训练。 原理： 全对或全错意味着没有 " 相对优势 " 差异， 产生的梯度无效或噪声大。 DAPO 只训练吲些模型处于 " 懂与不懂之间 " 的边界样本。 优势估计 (Advantage)： 4 ． 对保留下来的有效组， 计算组内相对优势解耦损失计算 (Decoupled Loss - 关键改进）： 5 ． · 计算 Token 级别的概率比 Tit(O)0 Clip-Higher: 使用非对称截断范围 [ 1 一 qow, 1 + €high] 计算损失。 聚合方式： 不对样本取平均， 而是对所有 Token 求和， 最后除以总 Token 数 1

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

6 ． 反向传播： 更新模型参数。

### （三）为什么目标函数按token平均？

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

我们需要区分两个概念： “ 样本权重 " 和 “ 决策权重 (Token 权重） " 在大模型中， 每一个 Token 的生成都是一次独立的决策 (Action)。 短样本 （10 Tokens)： 模型做了 ℃ 次决策。 长样本 （1000 Tokens)： 模型做了 ℃ 00 次决策。 如果使用 GRPO/PPO 的标准平均 (Sample-level Averaging)： 1 力 055 055 sample BatchSize 这相当于说： “ 做 10 次决策的考试 " 和 “ 做 1000 次决策的考试 "， 权重是一样的。 短样本中， 每一个 Token （每一次决策） 的梯度权重是。 长样本中， 每一个 Token （每一次决策） 的梯度权重是 1000。 结论： GRPO 的这种做法， 实际上是在极度偏袒短样本。 它认为短样本中的每一个字， 比长样本中的每一个字重要 IOO 倍。 这会导致模型倾向于走捷径， 因为长推理链条中间的错误修正被稀释了。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

如果使用 DAPO 的求和平均 (Token-level Averaging)： 1 乥力 055 力 055 忉 e “ 丆 To e “ 5 这相当于说： “ 每一个决策 (Token) 都是平等的。 ' 1 无论你是在短句子里， 还是在长篇大论里， 每一个 Token 贡献的梯度权重都是 To 左 “

总结与应用：GRPO更能保证模型对每一个样本训练充分，DAPO则避免二值奖励中长链推理样本的关键逻辑步骤梯度过小，更适合提高复杂推理的收敛效率和逻辑密度。可以根据不同需要，选择合适的训练方法。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
