---
title: "21.3 GSPO算法（论文）"
source_docx: "第3部分 大语言模型/21.强化微调的前沿探索/21.3 GSPO算法（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 21.3 GSPO算法（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、序列级优化（Sequence-Level Optimization）

GRPO 在“评价”和“优化”上存在着不匹配：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

整体奖励 (Global Reward): 在大多数任务 （如数学推理、 代码生成） 中， 环境只会在模型生成完整个回答后， 给出一个分数 （Rewa rd）。 比如： “ 这就话是对的 " 或者 " 这个答案是错的 “。 这个分数是对整个序列 (Sequence) 的评价， 而不是对某个字 (Token) 的评价。 局部优化 (Local Optimization): 虽然奖励是针对整体的， 但在 GRPO 的数学公式中， 重要性采样 (lmportance SampIing) 是在 Token 级别进行的。 回顾 GRPO 的核心项： （忉左 e 砌） 丌。 （忉 e 砌） 这意味着， 模型在更新参数时， 会逐个审视每一个 tokeno 即便整个句子得分很高， 如果某个具体的 toke n 在新策略下的概率变低了忉 < 1), 模型在更新该 toke n 的参数时可能会受到抑制。 后果： 这种机制假设每个 to ke n 对最终结果的贡献是独立的。 但在长逻辑链中， 很多 token 只有组合在一起才有意义。 对单个 toke n 进行独立的微调 （局部优化）， 往往忽略了上下文的整体连贯性， 容易引入高方差噪声。

GSPO则将二者进行了统一：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

整体优化 (GIobal Optimization): GSPO 摒弃了逐 Token 计算比率的做法， 转而计算整个序列的联合概率比率。 乃 （整个句子） 鼠整个句子） 这意味着， 模型不再纠结于某个 to ke n 的概率变化， 而是看生成这整句话的概率是变大了还是变小了。 如果整句话的奖励很高， GSPO 会统一增强这句话中所有 token 的权重； 反之则统一抑制。 裁剪 (Clipping) 也发生在序列级： 一旦这整句话的概率变化超标， 就停止更新。 这保证了模型不会因为某一句话学得太猛而崩塌。

### （二）长度归一化

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果不做归一化， 序列概率计算公式为所有 toke n 概率的连乘： P(sequence) = 1 × P2 × · 这会带来两个致命问题： 1 ． 2 ． 数值下溢 (Numerical Underflow): 每个 token 的概率都是小于 1 的数 （比如 0 ． 9）。 当序列 1000 1.7 × 10 一 46）。 计长度达到几百甚至几千时， 连乘的结果会迅速趋近于 0 （例如 0 · 9 算机无法精确处理这么小的数， 计算新旧概率比值 0 / 0 会导致数值极不稳定。 长度偏见 (Length Bias): 长序列的联合概率天然比短序列低得多。 短回答 （5 个词）： 联合概率可能是 10 一 5。 长回答 （100 个词）： 联合概率可能是 10 100 如果不归一化， 长回答的概率比率 (lmportance Ratio) 波动会极其剧烈， 甚至爆炸。 模型会倾向于只生成短句子， 因为短句子的概率更容易优化。

GSPO使用几何平均：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

I/L

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 丌走） log

这样，每个序列L个概率比值（大小是L次方级别）的1/L次方就和概率的一次方处于同一大小级别了。这样做实际上是在衡量“平均每个Token的生成质量提升了多少”（取对数之后，就是概率比值对数的算术平均），使得长回答（详细推理）和短回答（直接给出答案）站在了同一起跑线上进行比较，消除了长度带来的不公平影响。

因此，GSPO的梯度更新也就和GRPO不同。GSPO的目标函数在未被截断时为：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Ucspo(0) = E@@） ·

从而策略梯度：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

· Vo log 圃@） 系数对所有 t 恒定

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

（注： 我将常数项移入了求和内部以便对比） 现象： 这里的缩放系数是） ·。 注意，） 是基于整个序列算出来的， 对于序列中的第 1 个词还是第 ℃ 0 个词， 这个） 的数值是完全一样的。 结果 （统一增强 / 抑制）： 如果整句话奖励高 （翡 > 0） 且序列概率比正常 ()t 1)： 整个序列中每一个 toke n 的 log 概率梯度， 都会乘以同一个正系数。 模型会 " 一视同仁 " 地增加这句话中所有词的生成概如果发生 CIipping: GSPO 检查的是） 是否越界。 一旦越界， 剪切操作会让整个序列的梯度变为 0 （或被限制）。 这意味着： 要么全改， 要么全不改 （或者全被限制 ), 不会出现 " 前半句猛改， 后半句不改 " 的分裂情况。

对比GRPO的策略梯度：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

GRPO 的梯度 （局部视角）： VUGRPO 乥 · · log 丌） 系数随 t 变化现象： 这里的缩放系数包含 p 到 （即单个 to ke n 的概率比）。 结果： 即使整句话的优势是正的 （好回答 ), 如果句子中间第 5 个词的佐 5 很小 （新模型不太想生成它 ), 那么第 5 个词的更新幅度就会变小， 甚至方向可能被 Clip 截断。 每个 Token 获得的推力是不一样的。

因此，如果整句话的奖励很高，GSPO会统一增强这句话中所有token的权重；反之则统一抑制。这增强了奖励和惩罚的整体性，能更准确地对不同推理链条做出全局性的判断，避免被某些局部细节过度干扰。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
