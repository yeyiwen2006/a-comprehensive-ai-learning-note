---
title: "21.7 基于经验的强化学习（论文）"
source_docx: "第3部分 大语言模型/21.强化微调的前沿探索/21.7 基于经验的强化学习（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 21.7 基于经验的强化学习（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

RLVR天然存在经验浪费的问题，模型生成的推理轨迹只会在被用于更新一轮梯度后就被丢弃，而从来不会像人类那样过一段时间后还会回头复盘。让模型学会“温故而知新”，让模型根据“错题本”，把每一次宝贵的成功经验都内化为自己的能力对训练效率和能力提升都至关重要。下面介绍ExGRPO框架。

## 一、经验的选取

选取经验时，应当选取难度合适（模型正确率25-75％）的题目。这也像人类学习，反复巩固已经掌握的内容或者一开始就学太难的内容，效果都不好。

对于同一道题的推理路径，研究发现推理轨迹的Token平均熵是一个优秀的指标，在所有做对的题目中，选择熵值最低者。这是因为那些推理过程逻辑更正确的解法，其对应的熵值显著更低。高熵的正确轨迹很多时候只是幸运的瞎猜，反复学习这些轨迹不仅没有帮助，反而可能污染模型的逻辑能力。

ExGRPO会建立一个“经验回放池”，像一个巨大的“错题本”，专门收集模型在训练过程中所有成功的推理案例，利用高斯分布概率模型，有偏地优先从中等难度的分组中抽取问题。同时，随着模型能力的不断提升，模型已经完全掌握（例如连续多次全部成功解答）的问题会被移出学习队列，让模型始终聚焦于更具挑战性的任务。

## 二、目标函数

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

ExGRPO 是在 GRPO 的基础上构建的。 对于输入的查询 q, 当前参考策略会生成 K 个轨迹构成的集合 ‰ = { } 0 每个轨迹的优势值通过组内标准化计算 （论文跟随 Dr.GRPO 的设置， 去除了长度和标准差标准化， 仅保留均值中心化） + 3

ExGRPO采用了一种混合策略的优化目标，除了对重要性采样进行修正外，在每一次训练迭代中，Mini-Batch中一部分计算资源用于让模型探索全新的问题（On-policy），另一部分则用于学习从经验池中精心筛选出的经验（Off-policy），平衡了探索新知和复习旧知。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在每次参数更新时， ExGRPO 会构建一个混合的 Mini-Batch 其中包含来自当前数据集刀的同策略样本 0 “ 以及来自经验回放缓冲区 E 的历史经验样本这两部分数据通过超参数 0 p e [ 0， 1） 进行比例混合。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

UExGRpo@ 1 乥 CLIP(wt(0),Ä(ot, ‰）） Expansion of ()n-policy objective JGRPO(q， gq； 9) 1 Experiential Off-Policy objective

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一项： 同策略目标 (On-policy Objective) 权重： 占总目标的 1 一 p 比例。 工作流： 对于从召 “ 采样的当前问题 q, 模型实时生成 K 个轨迹并计算 GRPO 损失。 丌 9 （tlq,Oi,<t 重要性采样： wt(O) =， 用于限制新旧策略更新幅度。 第二项： 经验异策略目标 (Experiential Off-Policy Objective) 权重： 占总目标的 p 比例。 混合组构建： 对于从缓冲区采样的历史问题 q*， ExGRPO 会构建一个混合优势估计组其中 0 * 是过去策略生成的一条高质量历史轨迹， 而剩 pa 余的 K 一 1 条轨迹是由当前参考策略丌实时生成的。 异策略重要性校正： 为了消除直接使用历史数据带来的分布偏移 (Distribution Shift) 并保证梯度无偏， 对于重播的轨迹 0 *， 其重要性权重被专门重新加权为当前策略与生成该丌穆 （lq 0 轨迹的过去策略之间的比值： '@） =

## 三、策略塑形

为解决直接优化低熵历史轨迹可能损害强化学习探索能力的问题，上述目标函数的第二项被改为：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

e-XP

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在公式的后半部分 （即异策略经验目标中 ), 原有的 CLIP(w*(O), 烈@'， gq*)） 被替换为了这里的是一个引入的小常数 0 （在实践中通常设为 0 · 1） 的非线性转换函数：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

之所以用細） 替代标准的 PPO/GRPO CLIP 机制， 是基于该函数的以下几个关键数学性质及其带来的强化学习收益： 抑制高概率信号 （防止过度拟合）： 当重要性权重 w 很大时 （即当前策略已经非常倾向于生成这个历史动作 ), 細） 的极限趋近于 1。 这意味着它起到了类似截断 (Clipping) 的作用， 抑制了极端的权重， 控制了异策略学习带来的方差， 防止模型在这个已经很确定的轨迹上过度更新。 + 2 放大低概率信号 （鼓励探索新意）： 当重要性权重 w 很小且接近 0 时 （即当前策略不太可能生成这个动作， 但在历史经验中它是个好动作 ), 由于 （如 0 · 1） 的存在， 細）， 这实 0 际上在局部放大了这些微弱的信号。 动态权衡： 这种设计使得模型能够从经验轨迹中那些对当前策略来说仍然 " 新颖 (novel) " 的部分中学习， 而不是盲目地强化那些已经具有高概率的动作， 从而在利用经验的同时保持了策略的熵 （探索性） + 2

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- ExGRPO 是在 GRPO 的基础上构建的。 对于输入的查询 q, 当前参考策略会生成 K 个轨迹构成的集合 ‰ = { } 0 每个轨迹的优势值通过组内标准化计算 （论文跟随 Dr.GRPO 的设置， 去除了长度和标准差标准化， 仅保留均值中心化） + 3
- 第一项： 同策略目标 (On-policy Objective) 权重： 占总目标的 1 一 p 比例。 工作流： 对于从召 “ 采样的当前问题 q, 模型实时生成 K 个轨迹并计算 GRPO 损失。 丌 9 （tlq,Oi,<t 重要性采样： wt(O) =， 用于限制新旧策略更新幅度。 第二项： 经验异策略目标 (Experiential Off-Policy Objective) 权重： 占总目标的 p 比例。 混合组构建： 对于从缓冲区采样的历史问题 q*， ExGR...

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
