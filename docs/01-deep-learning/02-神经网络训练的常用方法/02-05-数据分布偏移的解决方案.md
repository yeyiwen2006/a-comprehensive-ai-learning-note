---
title: "2.5 数据分布偏移的解决方案"
source_docx: "第1部分 深度学习/2.神经网络训练的常用方法/2.5 数据分布偏移的解决方案.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 2.5 数据分布偏移的解决方案

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、数据分布偏移带来的问题

在机器学习中，我们希望的优化目标是Loss=(1/N)*sigma(L(y,f(x)))=E_p(x,y)[L(y,f(x))]，这代表所有真实数据的平均损失，也可以视为真实数据按其概率（密度）加权的损失。而我们实际上能优化的是Loss_train=(1/n)*sigma(L(y,f(x)))=E_p_train(x,y)[L(y,f(x))]，这代表样本数据的平均损失，也可以视为样本数据按其概率（密度）加权的损失。我们希望Loss_train尽量接近Loss，这样我们就能通过优化Loss_train来实现优化Loss，这也就意味着我们希望真实数据概率分布和样本概率分布一致，这样按概率（密度）加权的损失才会一致。

当输入维度很高、特征量很大时（如图片），我们说的“概率分布p”默认为概率密度函数。这个概率密度一般无法直接求解，后续会介绍拟合方法。

如果这两者不一致，如样本中暗色的图片特别多，亮色的图片特别少，那么在优化Loss_train时，暗色的图片在期望中的权重大，梯度下降方向就会被“让暗色的图片的Loss_train下降”主导，亮色的图片就很难得到充分训练，使得模型识别图片对光照的鲁棒性下降。

这种偏移可能带来灾难性的后果。 假设我们训练了一个贷款申请人违约风险模型，用来预测谁将偿还贷款或违约。这个模型发现申请人的鞋子与违约风险相关（穿牛津鞋申请人会偿还，穿运动鞋申请人会违约）。此后，这个模型可能倾向于向所有穿着牛津鞋的申请人发放贷款，并拒绝所有穿着运动鞋的申请人。一旦模型开始根据鞋类做出决定，顾客就会理解并改变他们的行为。不久，所有的申请者都会穿牛津鞋，而信用度却没有相应的提高。通过将基于模型的决策引入环境，我们可能会破坏模型。当模型的输出被直接（且不加批判地）用作下一轮的输入时，任何微小的偏见都会被无限放大，最终导致系统“失控”。

## 二、分布偏移的分类

1.协变量偏移

特征x的分布p(x)变了，但规则p(y|x)不变。

这基于“x导致y”的假设（如因素 x决定公司股价y）。

例如：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

训练 (Ptrain(X))： 真实的猫狗照片胤飞、 0 测试 (Ptest (x))： 猫狗的卡通画。 x 变了： " 照片 " 的像素分布和 " 卡通 " 的像素分布完全不同。 P(y 》 x) 不变： “ 卡通猫 " 的概念仍然是 " 猫 “ = 1), “ 卡通狗 " 的概念仍然是 " 狗 "）。 规则是稳定的。 为什么会失败： 模型在训练时可能学会了依赖 “ 照片 " 特有的特征 （如 " 毛发纹理 "、 “ 真实光影 "), 而这些特征在 " 卡通 " 测试集中完全不存在。 = 0

2.标签偏移

标签y在样本标签中出现的概率p(y)变了，但同样的标签对应的特征p(x|y)不变。

这基于“y导致x”的假设（如疾病y导致症状x）。

例如：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

训练 （夏季）： 尸 （流感） = 1 ％， 普通感冒） = 10 ％。 测试佟季）： 尸 （t 流感） = 20 ％， （普通感冒） = 40 ％。 P(y) 变了： “ 流感 " 这个标签本身变得更常见了。 P(x 《 不变： “ 流感 " 在夏天和冬天引起的 " 症状 “ （如发烧、 咳嗽） 是一样的。 为什么有利： 标签通常是低维的 （例如 ℃ 个类别 ), 而特征 x 是高维的 （例如 ℃ 0 万个像素）。 纠正尸） （10 个数字） 的偏移， 远比纠正 P(x) （℃ 0 万个像素） 的亻扁移要容易得多。

3.概念偏移

x和y的对应关系，即规则本身P(y|x)变了。

例如：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

输入 x： " 一种甜的碳酸队料。 规则尸 《 x) 取决于地理位置 · · 在纽约， 规则是尸 = ” soda" 《 x) 1。 · 在芝加哥， 规则是尸一 POP" 《 x) 1。 · 在亚特兰大， 规则是尸 = ” coke ” 》 x) 1。 这是最难的问题。 如果规则瞬息万变， 模型就必须从头开始学。 但幸运的是， 在实践中， 概念偏移通常是缓慢渐进的。

## 三、实例分析

1.医学诊断

问题： 癌症检测器。

数据： 病人 = 老年男性；健康人 = 大学生（健康对照组）。

偏移类型： 极端的协变量偏移。

失败原因： 模型根本没有学习“癌症 vs 健康”。它学习的是“老年人 vs 年轻人”。它会捕捉任何相关的虚假相关性（激素水平、体力活动、饮食），导致它在真正的老年健康人（它从未见过的群体）身上 100% 失败。

2.自动驾驶汽车 & 坦克：

“路沿检测器”： 用“游戏引擎”数据来扩充训练集。

问题： 游戏引擎偷懒了，把所有的“路沿”都渲染成了同一种简单的纹理。

偏移类型： 协变量偏移。

失败原因： 模型没有学会识别“路沿的几何形状”，它学会了识别“那种特定的游戏纹理”。但这在真实世界中毫无用处。

“坦克探测器”：

无坦克 = 早上拍的森林（有阴影）。

有坦克 = 中午拍的森林（无阴影）。

失败原因： 模型学会了识别“阴影”，而不是“坦克”。

3.非平稳分布（缓慢发生的概念偏移）

广告模型： 2009 年的模型不知道“iPad”是什么。P(点击 | "iPad") 这个规则在 2010 年被创造了出来。

垃圾邮件过滤器： 垃圾邮件发送者会不断进化他们的词汇和手段。P(垃圾邮件 | 邮件内容) 这个规则一直在被动地变化。

推荐系统： 圣诞节过后，P(感兴趣 | "圣诞帽") 这个规则从 1 变成了 0。

4.总结

人脸特写： 协变量偏移。（训练集里没有“脸占满屏幕”的x）。

美国/英国： 概念偏移。（"football"对应的y 从“橄榄球”变成了“足球”）。

数据类型不均衡： 标签偏移。（训练时 P(y) 是均衡的，但现实中 P(y) 是不均衡的）。

## 四、解决方案

1.对于协变量偏移：重要性采样和加权Loss

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 我们想优化的 " 真实风险 " 是 Rtest =。 多我们只有 “ 训练数据 " 一 [ · · 0 一些凶一我们可以把对 P 的期望， 改写为对 P · 的加权通过引入一个比率 ß(x) = test， traln 期望： =、 in[ß （x） · Ptest (x) 重要性权重 （公式 4 · 9 · 4）： ß(x) = Ptrain (x) 含义： 如果一个训练样本 x 在 " 测试集 " 中出现的概率是在 " 训练集 “ 中的 ℃ 倍， 那么它就非常重要， 我们应该给它 ℃ 倍的权重。

我们无法显式求解每一个样本在两个集合中概率密度函数的值，但是我们可以训练一个鉴别器，直接拟合二者的比值：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

事实证明， 我们不需要知道 Ptest 和 Ptrain 这两个值！ 我们只需要知道它们的比率 ß(x)o 训练一个鉴别器 f(x) （它是一个输出概率的逻辑回归或神经网络 ), 让它去区分 " 来自 Ptest （标签 1） " 还是 “ 来自 Ptrain （标签 0） "。 这个鉴别器 f(x) 为了最小化自己的分类损失， 它必须隐式地学习到这两个分布的相对密这正是原文中公式 4 · 9 · 7 所展示的数学魔法： 鉴别器的输出 f(x) = P(z = 1 | x)， 通过贝叶斯公式可以直接转换成我们想要的那个比率 ß(x)o 尸区 2 一 1） 尸 （2 一叫 x） 尹区 2 一 0） 尸 （2 一 0 区） 1 一烈 x）

这里鉴别器训练时用训练集和测试集的等量数据，保证p(z=1)=p(z=0).

例如，用卡通图片为主的数据集训练主模型，测试集以现实环境中的图片为主，为了增强泛化性，运用鉴别器，对于训练集中偶然出现的真实环境图片，会认为它高概率来自测试集，从而给它赋予高权重，使得测试集训练时增强对现实的泛化能力，避免严重偏移为一个卡通图像识别模型。

2.对于标签偏移：计算“混淆矩阵”C

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

问题： 医院的 AI 诊断模型。 类别 (y)： 3 个类别： { 0： 健康， 1： 普通感冒， 2： 流感 } 特征 (x)： 症状 （如发烧、 咳嗽、 喉咙痛等）。 标签偏移假设： P(x 1 不变。 含义： “ 流感 " 的症状 = 2 对应的 x) 在夏天和冬天是一样的。 训练集 (Train)： 在夏天收集的数据。 流行度 Ptrain）： { 0： 健康 （80 ％ ), 1： 感冒 （19 ％）， 2： 流感 （1 ％） } 模型会学到： " 感 ' ' 极罕见， 它会非常不愿预测 ' ' 感 "。 测试集 (Test)： 部署在冬天。 未知的真实流行度 Ptest）： {？？，？？，？？ } 我们猾 ' ' 感 ' ' 的比例会大大增加。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

模型的 “ 缺陷 "： 由于它是在 Ptrain 上训练的， 它天生具有一种偏见 (Bias), 即它认为 “ 流感 " 的先验概率只有 1 ％。 在冬天使用时， 它会系统性地低估 " 流感 " 的发生率， 并把很多 “ 流感 " 病人错判为 " 普通感冒 " 0

为解决这个问题，我们将数据集（不包括测试集）分为训练集和验证集。验证集也来自夏天（P_train），所以我们拥有真实标签。我们让模型预测这个验证集。然后我们统计它的“犯错模式”，并将结果 normalized（归一化），得到混淆矩阵C：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

0 丿的精确含义： Ctj = Ptrain = 犭是行 （预测值 ), 是列 （真实值）。 0 2 的含义是： 当一个病人真的得了 " 流感 " = 2） 时， 我们的模型有多大概率会错误地预测他是 " 普通感冒 " = 1）？ 我们的例子 （假设）： 假设我们计算出的 C 如下例是真实， 行是预测）： 真实： 健丨预测健： [ 9 四预测感： [ 9 · 1 预测流： [ 9 · 9 解读这个 “ 指纹 "： 感 | 丨 9 丨 9 流 8 | e ． 3 ] 7 ] (C-ej) (C-1j) (C-2j) 0 2 = 0 ． 3： 模型有 30 ％ 的概率把 " 流感 " 错判为 " 感冒 “。 0 2 = 0 ． 7： 模型有 70 ％ 的概率能正确识别 “ 流感 "。

接下来我们把模型已经部署在冬天（测试集）。运行模型，比如看了 1000 个新病人，得到被“污染”的数据：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们没有什么： 我们没有这 ℃ 00 个病人的真实标签）。 我们有什么： 我们可以看到模型的预测）。 计算平均值： 我们简单地统计模型预测的百分比。 假设模型在 ℃ 00 个病人中， 预测了： " 健康 " ． = 0）： 200 次 " 感冒 " ． = 1）： 600 次 " 流感 " ． = 2）： 200 次结果 （平均模型输出）： 我们得到了一个向量 ßtest@）： .utest@） = [ 0 · 2， 0 ． 6， 0 · 2 ] 重要认知： 我们不认为这是真实的流行度。 我们知道这个结果被 C " 污染 " 了 （例如， 那个 0 ． 6 的 " 感冒 " 里， 肯疋混入了很多被错判的 " 流感 “）。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们没有什么： 我们没有这 ℃ 00 个病人的真实标签）。 我们有什么： 我们可以看到模型的预测）。 计算平均值： 我们简单地统计模型预测的百分比。 假设模型在 ℃ 00 个病人中， 预测了： " 健康 " ． = 0）： 200 次 " 感冒 " ． = 1）： 600 次 " 流感 " ． = 2）： 200 次结果 （平均模型输出）： 我们得到了一个向量 ßtest@）： .utest@） = [ 0 · 2， 0 ． 6， 0 · 2 ] 重要认知： 我们不认为这是真实的流行度。 我们知道这个结果被 C " 污染 " 了 （例如， 那个 0 ． 6 的 " 感冒 " 里， 肯疋混入了很多被错判的 " 流感 “）。

接下来我们运用混淆矩阵和全概率公式，从被“污染”的概率数据中复原真实概率数据：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

公式： ßtest@） = 我们来推导它 （以 “ 感冒 " 为例）： 我们在冬天观测到的 “ 平均感冒预测率 " (ßtest@ = 1）， 即 0 ． 6） 是如何产生的？ 根据全概率公式， 它由三部分组成： 1 ． （真实是 " 健康 " 0 = 0） 且被错判为 " 感冒 = 1）） 的概率 2 ． （真实是 " 感冒 " 0 = 1） 且被正确判为 " 感冒 = 1）） 的概率 3 ． （真实是 “ 流感 " 0 = 2） 且被错判为 " 感冒 = 1）） 的概率 1） + 尸一 1： 2） P 一 2） 代入我们的符号： = 1） = 0， 0 · Ptest = 0） + 01 · = 1） + 0 2 · Ptest(y = 2） 矩阵形式： 这正是矩阵 C 的第二行 = 1） 与 " 真实标签分布 " 向量 Ptest） 的点积！ 结论： 对所有都成立， 因此 ßtest@） = C · Ptest） 成立。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

劬 = 01 ·

3.对于缓慢的概念偏移：在线学习（Online Learning）

如非平稳分布时，不要从头开始训练，而是使用新数据持续更新（微调）现有的模型权重，让模型“跟上”变化。另外，老虎机 (Bandits)是“在线学习”的特例，指的是你的“行动”（预测）是有限的几种选择（例如“拉 1 号杆”或“拉 2 号杆”）。

4.强化学习

明确引入了“奖励” （Reward）。RL 的目标是学习一个策略（如何行动），以最大化长期奖励。

RL 是“鞋子-贷款”问题的真正解： 银行（Agent）做出“发放贷款”（Action）的决策，环境（客户）做出“穿牛津鞋”（State change）的反应，银行最终得到“盈利情况”（Reward）。RL 框架天生就是为了解决这种“反馈循环”而设计的。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 输入 x： " 一种甜的碳酸队料。 规则尸 《 x) 取决于地理位置 · · 在纽约， 规则是尸 = ” soda" 《 x) 1。 · 在芝加哥， 规则是尸一 POP" 《 x) 1。 · 在亚特兰大， 规则是尸 = ” coke ” 》 x) 1。 这是最难的问题。 如果规则瞬息万变， 模型就必须从头开始学。 但幸运的是， 在实践中， 概念偏移通常是缓慢渐进的。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
