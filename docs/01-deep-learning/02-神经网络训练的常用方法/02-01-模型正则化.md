---
title: "2.1 模型正则化"
source_docx: "第1部分 深度学习/2.神经网络训练的常用方法/2.1 模型正则化.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 2.1 模型正则化

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、模型正则化的数学基础

1.偏差与方差

偏差大：预测数据均值偏离实际均值，欠拟合。

方差大：预测数据学到了太多噪声，过拟合。

2.贝叶斯公式

先验概率p(A)是对一个事件概率如疾病发病率的初始估计；

p(B|A)是A发生的情况下，能观测到证据B如阳性的概率；

p(B)是A发生与否两种情况下观测到证据B的概率和；

p(A|B)是观测到证据B的情况下，对概率估计的进一步更新。

3.贝叶斯公式在正则化中的应用

（1）基本量

p(参数)：先验项，也就是对“参数值为X”这一事件发生概率的初始估计，若无正则化项则可忽略本项。

p(输出|参数)：这里的“输出”指的是，在已知“参数值为X”的条件下，出现输出Y的概率；损失函数最小时，p(输出|参数)最大（最大似然估计得到）。

p(输出)：尝试过所有参数后，得到该输出Y的平均概率，一般认为是常数（从可输出的词的列表中完全随机抽取）。

（2）基本解释

p(参数|输出)=p(输出|参数)*p(参数)/p(输出)，或者说认为p(参数|输出)正比于p(输出|参数)*p(参数)，我们的目的是在给定输出的情况下，求出最可能产生这样输出的参数，也就是p(参数|输出)最大，在设计损失函数时，会把这种情形定义为损失函数最小（如loss=-logp(参数|输出)），一轮轮训练中通过梯度下降一步步接近该参数组合，每次训练的过程就是把参数初始概率分布变成考虑输出后的概率分布，并通过梯度下降让参数接近概率最大似然估计的参数值。

（3）正常情况

没有加入正则化项时，我们对p(参数)没有任何事先估计【初始权重值从高斯分布取样是为了能进行梯度操作而不至于全0，仅为优化起点，不代表先验估计】，这时p(参数|输出)正比于p(输出|参数)，也就是我们认为，当某个给定参数的模型最可能产生某个输出时，想要让另一个模型实现该输出的最好方法就是拟合该模型的参数。

（4）过拟合

训练时有时会落入一个p(输出|参数)很大的参数组合，但其实出现了过拟合，在测试集中p(输出|参数)反而小【注意计算贝叶斯后验不能用测试集，只能用训练集，原因前面讲过】，为了我们抑制过拟合，必须手动调整p(参数)的分布，让p(参数|输出)最大的情形同时受训练时得出正确输出概率p(输出|参数)和先验分布p(参数)的影响。

（5）加入正则化后

在loss中加入正则化项，也就等价于通过控制初始概率分布函数p(参数)，使其不为定值，从而在估计中加入我们的额外偏好，p(输出|参数)大不一定代表loss小（如一些正则化项会给予复杂模型惩罚），让包含实现对参数最终分布的调整。

## 二、L2正则化（权重衰减）

即给损失函数加上“lamda*1/2*参数平方和”项。lamda为正则化强度；1/2是为了在求正则项关于参数向量W的梯度时求导等于lamda*W而没有其他系数。

概率本质：由前面损失函数是取log得到的可知，p(参数)其实就是该参数下的高斯函数的值，等价于高斯先验，默认各参数接近0的概率大。

收敛方式：由于在0附近导数约为0，接近0时收敛极慢（收敛速度趋于0），一般不会收敛到0；但不会出现很大的参数（惩罚比L1大）。

对减小模型复杂度的作用：

（1）所有参数向零收缩，显著削弱高阶项，类似于限制多项式中高阶项系数。

（2）避免特征共线性带来的权重抵消问题（这种情形的惩罚值会很大）。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

权重抵消效应的产生机制 1 ． 参数解的不确定性当特征高度相关时 （如、 心朊 V2）， 模型无法区分每个特征的独立贡献。 例如 ． · 真实关系： Y= 2 № 十 3 № ． 若与入 2 强相关 （№ 0 ． 5 №） 则以下解均可能成立 ． Y= 4 十 0 ． X2 或 Y= 0 ． 十 6 入 2 或丫 = 5000A 后 _ 4998 入 2 其中最后一种情况即为权重抵消和 № 的系数符号相反且绝对值极大， 它们的贡献相互抵消， 最终组合结果仍接近真实值， 但单个系数的可解释性丧失河圣 2 ． 梯度更新的不稳定性在梯度下降优化过程中， 高度相关特征的梯度方向可能冲突： ． 若特征和入 2 相关且量级差异大 （如 № = 100）， 则权重 “ 的微小变化会导致剧烈震荡， 最终可能收敛到为负 2 为正的抵消状态

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

了权重抵消效应的影响 1 ． 模型可解释性降低 · 例如： 在房价预测中， 若 “ 房间数量 ” 和 “ 房间数量 × 2 " 同时作为特征， 模型可能賦予前者正权重 （+ 5000）、 后者负权重 （一 4998）， 导致 “ 房间数量增加反而降低房价 " 的反常识结论 0。 2， 参数估计方差增大 · 共线性使权重方差膨胀 ()I F 值增大）， 样本的微小扰动会导致系数剧烈波动， 模型稳定性下降 3 ． 特征重要性失真 · 在逻辑回归等线性模型中， 权重符号和大小用于判断特征与目标的关系 （如正 / 负相关）。 权重抵消可能使实际正相关的特征在模型中表现为负相关 0

其他好处：避免梯度爆炸，避免矩阵病态。

## 三、L1正则化

给损失函数加上“lamda*参数绝对值和”项，等价于对参数的初始估计为拉普拉斯先验，由于一次函数处处斜率相等，部分参数会被归零（L2在参数接近0的时候梯度也接近0，参数再向0的更新几乎停滞），剩余参数接近0的程度没有L2那么大（L2对偏离较大的值惩罚更强烈）。

## 四、矩阵病态问题的优化

用拟合方式求解正规方程：输入矩阵（已知）A×参数矩阵（待求解）X＝输出矩阵（已知）b，由之前的分析知这也就是最小化目标函数：||AX-b||^2。

用线性变换思想：参数矩阵由多个原向量（每列都是一个即将被变换的向量）构成，参数矩阵代表线性变换，输出矩阵代表各个原向量分别变换后的向量。

若输入矩阵X列相关程度大，相当于这个矩阵代表的线性变换把空间“压缩到一起”，这时参数矩阵即矩阵方程的解即原向量就会因变换后向量的微小波动而变化巨大，正则化效果在数学上等价于通过改变线性变换矩阵来解决病态问题：（岭回归就是L2正则化，下面是由目标函数推导正规方程的过程；在维数匹配的前提下，矩阵乘法对加法满足分配律）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

岭回归的目标函数 J(x) = llAx— 坷 2 +， \ 2 刂对 2 其中 · 」 是 m × “ 的设计矩阵 （m 个样本， “ 个特征） · 是 “ × 1 的待求解参数向量， · b 是 m × 1 的观测值向量， · > 0 是正则化参数。 推导正规方程的步骤 1 ． 展开目标函数将目标函数展开为矩阵形式到展开后得 = x ． 「、 」 x 一 2b Ax+b b + /\2x J(x) （注 ． 因为 b Ax 是标量， 其转置等于自身因此 b Ax = (Ax) b = x 」

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 ． 对目标函数求梯度对 J(x) 关于向量 x 求梯度 （导数） 并利用以下矩阵求导法则： Vx(x 13x) = 213x （当召对称时） · vx(cTx) = 计算每项的梯度 ． 1 ． Vx(x 」 (x) 亠 2 x （因为工是对称矩阵） —2ATb 2 ． Vx(—2b、 4x 〕 = 3 ． Vx(b b) = 0 4 ． Vx(A2x x) = 2， \ 合并梯度 ． VxJ(x) = 2 」 x 一 2 」 ． 「 b + 2， \ 2x 3 ． 令梯度为零以最小化目标函数在最小值点处， 梯度必须为零向量： 24 Ax— 2 」 b 十 2z\ x = 0

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

4 ． 整理为正规方程形式提取公因子 x， 并引入单位矩阵 /： （、 4T、 4 + ,\21)x = ATb 这就是岭回归的正规方程。 关誕说明 1 · 正则化项的作用额外项 A27 加到丿 04 上， 确保矩阵 （」 T 」 +， \ 27） 严格正定 （即使、 4 奇异）。 这使得方程一定有唯一解： x = （、 4T、 4 十 A27） 一 1 」 b 2 ． 与普通最小二乘对比普通最小二乘的正规方程为 ,4TAx = ATb0 岭回归通过添加， \ 2 / 解决了、 4T、 4 不可逆或病态的问题。

## 五、暂退法（Dropout）

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 基本思想 Dropout 在训练阶段以概率 p 随机将神经元的激活值置零 （称为 “ 丢弃 '，）， 迫使网络不依赖特定神经元或特征组合， 从而提升鲁棒性。 其数学表达为 W 其中缩放操作 0 概率为方否则确保激活值的期望不变 (E[h'] =， 避免训练与测试时数据尺度不一致 0 @ 2 ． 作用机制 · 打破共适应性神经元无法依赖固定组合， 需独立学习有效特征， 减少对噪声的敏感度 0。 · 集成学习效应每次迭代训练不同的子网络， 最终模型相当于多个子网络的集成， 提升泛化性才

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

暂退法 (Dropout) 通过随机丢弃神经元来打破神经网络中的 “ 共适应性 " (Co-adaptation)， 这是防止过拟合的核心机制。 共适应性指神经元之间形成过度依赖的固定组合， 导致模型对训练数据的噪声敏感而泛化能力下降。 以下从原理、 机制到实际效果分层次解析， 睾 1 ． 共适应性的本质： 神经网络的脆弱依赖 ． 定义共适应性指某些神经元通过复杂权重组合学习特定特征时， 过度依赖其他神经元的输出。 例如， 神经元 A 仅在神经元 B 激活时才有效， 两者形成 “ 共生关系 " ． 风险过拟合： 模型过度依赖训练数据中的噪声或偶然特征组合。。 脆弱性 ． 若测试数据中部分特征觖失， 依赖链断裂导致预测失效。 冗余性降低 ． 模型无法学习多组独立特征表示， 削弱鲁棒性。 案例 ． 在图像分类中， 若一组神经元仅通过组合 “ 条纹纹理 + 黄色 " 识别老虎， 当测试图片光照变化导致黄色缺失时， 模型可能完全失效。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

大 2 ． 暂退法如何打破共适应性？ 暂退法通过动态随机丢弃神经元， 强制网络避免固定依赖路径： · 随机子网络训练每次迭代随机屏蔽部分神经元 （概率为 l-p）， 剩余神经元组成新子网络。 这迫使每个神经元必须： 1 ． 独立有效、 不依赖特定伙伴神经元也能提取有用特征， 2 ． 冗余学习： 相同功能需由多组神经元分散承担 》 《 · 抑制复杂噪声拟合共适应性常体现为对训练数据噪声的复杂拟合。 暂退法通过随机丢弃， 使模型无法维持复杂依赖链， 从而更关注普适特征 0 · 期望一致性补偿训练时对保留神经元的输出放大 1 / 0 叩） 倍， 测试时直接输出原值。 这确保整体激活强度稳 v 免因丢弃导致信号衰减。

注：可以认为比如每个特征由一个神经元负责，一个识别动物的神经网络中，老虎这个输出和黄色、条纹等特征虽然需要相关，但不能过度依赖这些表层特征（也就是对这些特征过拟合，形成比较固定的神经元之间的组合），也不能一个特征只有一个神经元负责（风险大且数字只能上下调，灵活性差），这会使模型在训练数据略有变化时（如例子里说的黄色缺失）泛化性、鲁棒性差。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
