---
title: "2.4 参数初始化"
source_docx: "第1部分 深度学习/2.神经网络训练的常用方法/2.4 参数初始化.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 2.4 参数初始化

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、Xavier（Glorot）初始化

为了避免梯度消失或爆炸，我们希望每一层输出的方差与输入的方差大致相等。除了在每次计算结束后用批量规范化“拉回来”之外，我们还希望计算过程本身就能保证这一点。在线性网络中，如y_j=sigma(x_i*w_ij)+b_j，输入与输出方差的关系取决于参数的取值，因此参数如何初始化至关重要。

假设权重和输入独立同分布，且它们的均值都为 0。权重的方差为sigma^2，这是我们要解的。输入的方差为Var(x_i)=Var(X)。

考虑一个简单的线性层，输出y_j=sigma(x_i*w_ij)（忽略偏置），对任意j，Var(y_j)=sigma(Var(x_i*w_ij))=n_in*Var(x_i*w_ij)（n_in为输入数量）

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

根据方差公式 var(Z) = EV2] 一 Z])2: E[WtXi] 一 ] E = 0 · 0 = 0 · 因为明 = 0， 所以 Var(W) = 一 (E[W])2 一 Etw2]0 · 同理 Var(X) = 将这些代入， 我们得到：

前向传播的目标是保持方差不变，即 Var(Y) = Var(X)，故Var(W)=1/n_in

同理，对于反向传播，为了保持梯度不变性，我们希望：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

DL var( Dzl¯l DL） = var （

这里z_l是第l层的输出（尚未经过激活函数）。接下来进行推导：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了推导出 l/nout， 我们必须使用 xavier (Glorot) 初始化的假设， 这意味着我们的激活函数是 tanh 或 sigmoid。 1 ． 2 ． 3 ． 4 ． 激活函数： 我们使用 tanh。 我们假设输入 z 大部分位于其线性区域 (z 0）。 线性假设： 在这个区域， f(z) z， 更重要的是， 导数 f'(z) 均值假设： 权重彬、 输入 a = 0 = 0 独立性假设： 彬、 DL、 以及梯度的均值都为 0。 1 相互独立。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们来分析梯度是如何从 I 层反向传播到 I 一 1 层的。 · zl = ‰ I 一 1 + bl 0 俨的形状是 （砌， 砌一 1）） · 砌一 1 是一 1 层的神经元数量 (fan-in) · 砌是 I 层的神经元数量 (fan-out) 根据链式法则， 我们要求解 DL Dzl¯l DL Dz1 Dzl 一 1 Da 一 Dzl 一 1

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们来看 DL Dzl¯l 中的任意一个神经元亦 DL Dz 一 DL (WI)T Dz1 DL 孬 Dzl 注意！ 这个求和乥老 1 是对 I 层的所有个神经元进行的。 · 砌正是 I 一 1 层的输出数量 (fan-out)o 所以我们令 nout = 砌。

反向传播中，对于前一层（第l-1层）的一个神经元j，L对它的梯度是第l层的全部（第1至n_out个）神经元传播而来的梯度的总和。故需要Var(W)=Var(w_k,j)=1/n_out.

实际操作中，取二者的调和平均数2/(n_in+n_out)作为方差。

## 二、Kaiming初始化

若激活函数使用ReLU，f'(z)在z>0时为 1，在z<0时为 0，其方差：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Var(AB) = Var(A)Var(B) + Var(A)E[B]2 + Var(B)E 刈 2 令 = WI 且 B = a 一应用我们的假设 E[WI] = 0： 怖 （酽产 1） = r （酽） 怖r@一 1） + 怖 （酽）@@I 一 1D2 + 怖r@一 1） （0） 2 根据方差定义 E [ 天 2 ] = 怖 r （天） + (E[X])2, 我们可以将上式简化为： 最后， 代回到 Var(Zl) 的公式中， 我们得到了前向传播的主公式 ·

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

需要矧@） 2 ] 和怖老） 之间的关系。 根据定义， E[(al)2] = E[(max(0, zl))2]O 我们假设 zl 是一个均值为 0 (E zl] = 0） 且对称的分布 （例如高斯分布， 这是由 E[W] = 0 带来的合理假设）。 E[(al)2] 的计算可以被分解为两部分： (max(0, z))2p(z)dz 02p(z)dz + z p(z)dz 因为 p(z) 是对称的， 所以、 z2 z） 恰好是 z2p(z)dz 的一半。 而老） 2 ] = z2p(z)dzo 因此， 我们得到： E[(al)2] = 江 [ 老） 2 ]。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

又因为 E[ZI] = 0 （由 E[W E [ 老） 2 ]。 = 0 保证 ), 所以老） =） 2 ] 一 @ zl])2

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

（式 2） E[(d)2] = -Var(Zl)

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

· 来自 （式 2）： E[(al)2] = 怖老） 来自 （式 1）： var(zl) 将 （式 1） 代入 （式 2）： 1 2 这就是信号二阶矩 E [ 天 2 ] 的逐层递推关系。 我们的目标是让信号稳定， 即： 现在， 我们将这个目标代入递推公式中： 1 2

得到W的方差应为2/n_in.

反向传播：

设sum是L对第l层输入a的梯度（包含了第l层n_out个神经元传来的梯度之和），f’(z)是激活函数的梯度，则由独立随机变量乘积方差公式：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

DL 凵） = var(sum) · 怖 （尸） + var(sum) · @的） 2 + 怖 （尸） · (E[sum])2 var( 假设 E sum] = 0： DL Dz 一 DL） = var(sum) · 矧 （尸） 2 ] Dz 一我们已经推导出 Var(sum var （= 0 · 5。 代入： nout r （彬） · var （

则W的方差应为2/n_out.

一般来说，取前向传播对应的方差2/n_in即可达到足够好的效果（一般用2/n_in和2/n_out初始化差别也不大）。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
