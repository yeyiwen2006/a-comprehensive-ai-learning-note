---
title: "8.4 表征因果的自编码"
source_docx: "第1部分 深度学习/8.生成模型/8.4 表征因果的自编码.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 8.4 表征因果的自编码

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、因果表征学习的基本概念

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

CRL 的核心任务是解决一个逆问题 (lnverse problem)： 我们观测到了复杂的宏观数据 x （如图像、 基因表达 ), 我们假设它们是由一组不可见的、 符合因果逻辑的潜变量 z （如物体位置、 转录因子活性） 通过某种未知的混合函数 g 生成的。 我们的目标是反求出 g-l 和 zo

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设存在一组潜在的因果变量 z e ]Rno 这些变量之间不是独立的， 而是由 * * 结构因果模型 (SCM) * * 控制的： 其中： Zi = fi(PA(zD, 6 PA(Zi) 是的父节点 （直接原因）。 · 0 是外生噪声 (Exogenous Noise)。 是确定性的物理 ／ 生物枧制。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们无法直接看到 ‰ 我们看到的是通过一个非线性函数 g 变换后的高维观测值 x e x = g(z) （通常在深度学习中， 这个 g 可以看作是一个生成器 (Decoder), 例如物理世界的光线渲染过程， 或者生物体内的转录翻译过程。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

CRL 的目标是训练一个编码器 (Encoder) 九： ]Rd + lRn, 使得学到的表征 = h(x) 与真实的因果变量 z 之间满足一一对应关系 ()p tO permutation and element-wise transformation)： 如果能做到这一点， 我们就说模型实现了可辨识性 (ldentifiability)o

g是一个将因果变量变换为可观测表征的函数，g^-1则是我们需要希望得到的编码器，即编码时在高维空间中得到的表征就是因果变量。

## 二、VAE为代表的普通算法在表征因果时的问题

标准VAE对数据的先验分布p(z)是各向同性的，我们对各个不同的坐标轴是同等看待的。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

请注意 p(z) 一 （0 是一个球体 （各向同性）。 对于一个球体来说， 任何方向都是一样的。） 如果我们把坐标轴旋转一个角度 R, 得到 z' = 假设真实世界的因子是 z “ age， Zg “ 曰、。 RZtrue。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对于 KL 散度项： KL 散度衡量的是后验分布聚合起来后的整体形状与 p(z) （球体） 的差异。 无论你的数据云团是 “ 正着放 " 的椭球， 还是 " 斜着放 " 的椭球， 只要通过线性变换能变成球体， KL 散度并没有偏好哪一个方向。 数学上， 欧几里得距离和高斯分布在旋转变换下是不变的。 对于重构项： 解码器 Dec(z) 是一个神经网络。 神经网络极强， 它完全可以学会 " 先反向旋转， 再解码 "。 这意味着， 模型可以随意定义坐标轴的方向， 只要解码器配合着改一下权重， 重构结果完全一

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

结论： 既然 “ 正的坐标系 " 和 " 歪的坐标系 “ 在 Loss 上得分一模一样， 随初始化的就会随机选择一个坐标系。 这就导致了 ZI 往往是 0 ． 6 × Age + 0 ． 8 × G ender 这种混合物。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设你训练了一个 VAE 来分析细胞状态， 你希望得到两个变量： ·： 细胞衰老程度 ·： 炎症水平你输入一个老年的、 高炎症的细胞， VAE 把它映射到潜空间的 （5， 5）。 你输入一个年轻的、 无炎症的细胞， VAE 把它映射到 （一 2， 一 2）。 看起来分得很好， 对吧？ 现在， 你想设计一种药物， 只逆转衰老， 但不影响炎症 （即只减小， 保持不变）。 但如果 VAE 的坐标轴是纠缠的 （旋转过的）： 模型学到的实际上是 " 衰老 + 炎症 "。 当你试图降低 ZI 时， 你不仅让细胞变年轻了， 还意外地关闭了它的免疫反应 （降低了炎症）， 导致病人死于感染。 这就是为什么我们说： 仅凭观测数据训练的 VAE， 无氵去进行安全的因果干预。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设你训练了一个 VAE 来分析细胞状态， 你希望得到两个变量： ·： 细胞衰老程度 ·： 炎症水平你输入一个老年的、 高炎症的细胞， VAE 把它映射到潜空间的 （5， 5）。 你输入一个年轻的、 无炎症的细胞， VAE 把它映射到 （一 2， 一 2）。 看起来分得很好， 对吧？ 现在， 你想设计一种药物， 只逆转衰老， 但不影响炎症 （即只减小， 保持不变）。 但如果 VAE 的坐标轴是纠缠的 （旋转过的）： 模型学到的实际上是 " 衰老 + 炎症 "。 当你试图降低 ZI 时， 你不仅让细胞变年轻了， 还意外地关闭了它的免疫反应 （降低了炎症）， 导致病人死于感染。 这就是为什么我们说： 仅凭观测数据训练的 VAE， 无氵去进行安全的因果干预。

事实上，根据机器学习中的定理，在没有任何归纳偏置（Inductive Bias）的情况下，无监督地解耦因果因子在数学上是不可能的。如果你不给模型施加“条件独立”或“稀疏性”这样的先验，模型就永远有无限种解释数据的方式。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
