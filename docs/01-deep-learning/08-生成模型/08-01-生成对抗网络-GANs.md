---
title: "8.1 生成对抗网络（GANs）"
source_docx: "第1部分 深度学习/8.生成模型/8.1 生成对抗网络（GANs）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 8.1 生成对抗网络（GANs）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、核心思想

生成对抗网络是一个非常巧妙且强大的无监督学习框架，由 Ian Goodfellow 在 2014 年提出。GANs的核心思想是设置一个“二人游戏”。想象一下，我们有两个相互竞争的神经网络：

生成器 (Generator, G)：它的工作是“凭空”创造出看起来很真实的数据（比如，假的毕Z加索画作）。它试图模仿真实数据的分布。

判别器 (Discriminator, D)：它的工作是当一个“鉴定专家”，判断一个输入数据是来自真实数据集（真实的毕加索画作）还是生成器伪造的（假的画作）。

它们通过一种“对抗”的方式共同进化：生成器G努力地学习，试图生成越来越逼真的数据来“欺骗”判别器D；判别器D努力地学习，试图提高自己的鉴别能力，准确“识破”所有来自G的伪造品。这个过程就像一场军备竞赛，最终的理想状态是：生成器G变得非常出色，它造出的“假货”足以以假乱真，后续会证明，这将导致判别器D无法分辨，只能靠猜（即判断为真的概率为 50%）。

## 二、目标函数

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

min max （刀， G) = E ^ 艹） [log 刀 ] + E 乛） [log （1 一 D(G(z)) 〗 G 刀

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 符号与参与者 (The PIayers) G （生成器）： 我们的 “ 伪造者 "。 它是一个神经网络。 刀 （判别器）： 我们的 " 鉴定专家 "。 它也是一个神经网络。 ·： 代表一个真实数据 （比如一张真实的猫的照片）。。 忉）： 真实数据的概率分布 （理论上所有真实猫的照片的集合）。 ～ 佐。 忉 （刁意味着是从这个真实数据集中随机抽取的一个样本。 · z: 一个随机噪声向量 （通常是从一个简单分布， 如高斯分布或均匀分布中采样）。 z 是 G 创造 " 咸 " 的来 pz(z): 噪声向量 z 的分布。 z ～ pz(z) 意味着 z 是从这个噪声分布中随机抽取的一个样本。

这里需要说明的是，对于数据x，D(x)的真实值服从0-1分布，但x的概率分布并不是0-1分布。这里运用交叉熵损失时，函数里对应的概率分布应该是D(x)的分布，而不是p_data(x)这个x的分布。

p_data(x)则是x本身的概率密度分布，它往往不一定是离散的p(x=...)=...形式，而更可能是一个是一个理论上的、隐式的、未知的概率密度分布，是我们试图用模型去学习和模拟的“目标分布”。比如，想象宇宙中存在一个理想的、完美的函数p_data，它是所有图片的概率密度函数。如果你给这个函数输入一张非常逼真的、自然的人脸图片，它会输出一个高概率密度0.0001；输入一张很怪异的图片，它会输出一个较低的概率值10^-20。这个p_data(x)理论上包含了研究范围内所有“有效”数据（例如所有人脸）的生成规律。

我们虽然没有p_data概率密度函数本身，但我们有从这个函数中采样 (Sample) 得到的结果。这个“采样结果”就是你的“训练数据集”，例如，ImageNet 数据集就是p_data(x)（其中x是“自然图像”）的一组样本。在 GAN 的目标函数中，当看到E_x~ p_data(x)这一项时，它的实际操作含义是：“从你的训练数据集中随机抽取一个 batch 的数据x。”我们使用这个经验分布近似p_data，而随着GAN的训练，所谓“逼真”其实就代表G生成的数据（图像）越来越的分布接近给定数据集中的分布。事实上，我们也就认为这是在用端到端的方法近似非常复杂的p_data函数。之所以需要 GAN 这样的生成模型，正是因为 p_data(x) 过于复杂，我们无法用传统方法（如最大似然估计）直接对其进行建模，因此只能通过这种“博弈”的方式来间接学习它。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · 网络的输入与输出输入： 噪 zo 输出： 一个 " 伪造 “ 的数据。 G 试图让 G(z) 看起来和一样真实。 输入： 一个数据 （可能是真实的也可能是伪造的 G(z))0 输出： 一个介于 0 和 1 之间的概率值。 这个值表示刀认为这个输入数据是真实的概率。 刀） + 1 表示刀认为非常可能是真实的。 刀） + 0 表示刀认为非常可能是伪造的。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 · 目标函数的逐项解析现在我们来看这个函数的两大部分： 第一项： 真实数据的得分第二项： 伪造数据的得分 E (Expectation): 表示 “ 期望值 "， 在实际训练中， 我们无法遍历所有数据， 所以会用一批 (mini-batch) 样本的平均值来近似这个期望。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

含义： 这一项衡量的是判别器刀正确识别真实数据的能力。 刀的目标 (maxD): 刀想要最大化这一项。 当输入一个真实数据时， 希望自己的输出刀） 尽可能接近 1 （即 " 我确定这是真当刀 （+ 1 时， log 刀） 会接近 log(l) = 0。 这是 log 函数在其定义域 （0 到 1） 内的最大值。 G 的目标： G 对这一项没有控制权， 因为它只和真实数据有关。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

． 第二项： E 含义： 这一项衡量的是判别器刀正确识别伪造数据的能力。 D(G(z)): 这是刀对 G 生成的假数据 G(z) 的判断 （它认为 G(z) 是真实的概率）。 1 一 D(G(z)): 这就表示刀认为 G(z) 是伪造的概率。 刀的目标 (maxD): 刀想要最大化这一项。 当输入一个伪造数据 G(z) 时， 刀希望自己的输出 D(G(z)) 尽可能接近 0 （即 " 我确定这是假的 "）。 当 D(G(z)) + 0 时， 1 一 D(G(z)) 接近 1， 因此 log （1 一 D(G(z))) 接近 log(l) = 0 所以， 通过让 log 刀） 和 log （1 一刀 （G(z））） 都趋近于 0， 刀就在最大化 V(), G)O G 的目标 (minc;）： G 想要最小化这一项 （因为 G 无法影响第一项）。 G 的目标是 " 欺骗 “ 刀。 它希望刀在看到 G(z) 时， 误以为它是真的， 即 D(G(z)) 尽可能接近 1。 当 D(G(z)) 1 时， 1 一接近 0。 · log(l 一 D(G(z))) 会趋近于 log(o) = 这样， G 就成功地最小化了 （刀， G)O

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

4 · 总结： minc maxD V （刀， G) · maxD （刀， G） （判别器的目标）： 刀试图最大化 （， G)O 它通过训练， 力求做到： 1 ． 看到真实数据输出刀） 2 ． 看到伪造数据 G(z)， 输出 D(G(z)) = 0。 如果是一个完美的判别器， （刀， G） 的最大值是 0 + 0 = 0。 · minc （刀， G） （生成器的目标）： G 试图最小化 （， G)O 它通过训练， 力求做到： 1 ． 生成 G(z)， 让刀误判， 使得 D(G(z)) = 1。 当 G 变得更强时， 第二项 log(l 一 D(G(z))) 会被推向一

5.目标函数相关拓展

（1）判别器D的目标函数

注意到，这个目标函数V(D, G)的形式与二元交叉熵损失 (Binary Cross-Entropy Loss) 非常相似。把D看作一个二元分类器，真实数据 (x) 的标签为 y=1；伪造数据 (G(z)) 的标签为 y=0。D的目标是最小化分类的交叉熵损失，这等价于最大化我们上面的V(D, G)。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 标准的二元交叉熵 (BCE) 损失首先， 我们回忆一下标准的二元交叉熵损失函数。 它通常用于二元分类问题 （比如判断 " 是 " 或假设： 是真实标签 = 1 表示 " 真 "， = 0 表示 “ 假 "）。 是你的模型预测 = 1 的概率。 那么， 单个样本的 BCE 损失是： 力 = 一匆 · log(p) + （1 一 · log(l 一这个公式的含义是： 一 [ 1 · log(p) + 0 ] = 一 log(p)o 一 [ 0 + 1 · log （1 一 = 如果一 1 （真实标签为 “ 真 "）： 损失变为 = 为了最小化损失你需要最大化 log(p)， 也就是让 1。 如果 = 0 （真实标签为 “ 假 "）： 损失变为 = 为了最小化损失你需要最大化 log(l 一， 也就是让 p 0。 这完全符合我们对分类器的期望。 一 log(l

注意：这里我们的损失项要有两部分：一部分鼓励“把真的分入‘真’类别”，真实概率分布为：p_data满足为1时代表符合该类别（“为真”），为0时不符合。一部分鼓励“把假的分入‘假’类别”，真实概率分布为：p_z满足为1时代表符合该类别（“为假”），为0时不符合。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 判别器刀的 “ 分类任务 " 现在， 我们把判别器刀视为一个二元分类器。 它的任务就是给输入数据贴上 " 真 “ 或 " 假 " 的标签。 输入 1： 真实数据的目标： 将其分类为 “ 真 "。 真实标签 = 1。 刀的预测： p = 刀 （刁。 一 10g(D@))0 根据 BCE 公式， 这部分对应的损失是： 力输入 2： 伪造数据 G(z) 刀的目标： 将其分类为 “ 假 "。 真实标签 = 0。 的预测： P = D(G(z))o 根据 BCE 公式， 这部分对应的损失是： 力和一 log （1

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 · 连接 （刀， G) 和 BCE 损失刀的总损失就是它在真实数据上的损失和在伪造数据上的损失的总和 （或期望）： = log 刀 ] +E[- log(l 一 D(G(z)) 〗 力刀 = Lrea1 + 力和。 （为了简洁， 我们暂时省略了期望符号 E 外的 ～ 佐和 z、） 现在， 我们再回头看 GANs 的目标函数 V(), G)： 刀， G) = [log 刀 ] + E[10g(1 一 D(G(z)) 〗 对比一下力刀和 （刀， G)， 我们能清楚地看到： 一力刀

（2）生成器G的目标函数

按照原公式，生成器G的目标是最小化E[log(1 - D(G(z)))].这个目标函数在理论上是完美的，但在实践中，尤其是在训练初期，会引发一个严重的问题：梯度消失 (Vanishing Gradients)。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们来分析一下为什么： 1 ． 2 ． 3 ． 4 ． 训练初期， G 很差： 一开始， G 只是一个随机的神经网络， 它生成的 G(z) 都是无意义的噪声 （比如一堆雪花点）。 刀很容易 “ 碾压 " G: 判别器刀的任务很简单： 区分 “ 真实的猫 " 和 " 一堆雪花点 "。 刀会很快学， 并且非常自信地将 G 生成的 G(z) 判定为假。 刀的输出会发生什么？ 当看到 G(z) （雪花点） 时， 它会非常确定这是假的。 · 因此， D(G(z)) 的输出会非常接近 0。 G 的损失函数出了什么问题？ G 的损失是 = log(l 一 D(G(z)))0 如果 D(G(z)) 接近 0 （比如 0 · 001L LG 就会是 log(l 一 0 ． 001） log(l) 这看起来 G 的损失很 " 低 " （接近 0）， 但这只是一个假象。 真正的问题在于梯度。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

让我们看一下 G 的损失函数 LG = log （1 一的图像， 其中 )( 轴： 刀的输出 P （从 0 到 1） Y 轴： G 的损失 LG 您会注意到： = 是的输出： 当 p 接近 0 时 （也就是非常自信地判定 G(z) 为假时 ), 这条曲线非常平坦。 " 平坦 " 在微积分中意味着斜率 （梯度） 接近于 0。 这就是灾难发生的地方： G 生成了很差的图像 + 刀自信地将其判为假 ． 0） -+ G 得到的损失函数梯度 0 G 无法更新参数， 它完全学不到东西。 这个问题被称为 “ 饱和 " (Saturating)： G 在它最需要学习的时候 （即它表现最差的时候） 反而学得最慢。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

原始论文的作者提出了一个简单而实际的修正方法。 他们建议， 不要让 G 最小化 log(l 一 D(G(z))), 而是让 G 最大化 10g(D(G(z)))0

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

旧损失 （蓝色）： log(l 一。 当 p + 0 时， 梯度是平坦的 （消失）。 新损失 （绿色）： 一 log(p)o 当 p + 0 时， 梯度极其陡峭 （趋向一）。 这个新的损失函数具有我们正想要的特性： 它恰好在 G 表现最差时 （即 D(G(z)) 接近 0 时） 提供了巨大而强烈的梯度信号。 这使得 G 在训练一开始就能够快速学习。

但此时其实仍然没有完全解决梯度消失问题。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

V9 LG 通过链式法则， 我们把它分解成三部分： d(- log Vo LG 一 Part 1： 来自 G 的损失 V9gG(z) part 2： 来自 D 内部 part 3： 来自 G 内部 （其中 p 一刀 （G(z）） 是的最终输出， a = G(z) 是的输入）

Part 1：

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 您关注的部分 (Part 1） 当 G 很差时， = D(G(z)) + 0。 这一项过趋向无穷大。 您是对的， G 的损失函数本身正在 " 拼命地 " 产生一个巨大的信号。 2 · “ 真正消失 " 的部分 (Part 2） VaD(a) （即 VG(z)D(G(z))) 这是刀的输出概率相对于刀的输入 a 的梯度。 刀是如何输出一个 0 到 1 的概率的？ 它的最后一层是一个 Sigmoid 函数。 的目标是把真假拉开。 当它变得很强时 （不重叠 ), 它会给 G(z) 一个极低的分数。 为了输出 p+ 0， S 匆 mo 记函数的驸氕 （我们称之为 logits) 必须是一个很大的负数 （比如

Part 2：

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 您关注的部分 (Part 1） 当 G 很差时， = D(G(z)) + 0。 这一项过趋向无穷大。 您是对的， G 的损失函数本身正在 " 拼命地 " 产生一个巨大的信号。 2 · “ 真正消失 " 的部分 (Part 2） VaD(a) （即 VG(z)D(G(z))) 这是刀的输出概率相对于刀的输入 a 的梯度。 刀是如何输出一个 0 到 1 的概率的？ 它的最后一层是一个 Sigmoid 函数。 的目标是把真假拉开。 当它变得很强时 （不重叠 ), 它会给 G(z) 一个极低的分数。 为了输出 p+ 0， S 匆 mo 记函数的驸氕 （我们称之为 logits) 必须是一个很大的负数 （比如

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Logistic Function

在x很小或很大时，用于把输出转换为概率的Sigmoid函数的导数（梯度）趋近于0.

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

G 的总梯度是： Vo LG = （趋向无穷大） · （趋向 0） · （来自 G 的梯度） 来自 log 来自 S 匆 mo 滬

在这个不定型中，Sigmoid函数往往占主导，导致梯度消失。类比就是打开了“消防栓”，但“水管”（D网络）内部被“水垢”（Sigmoid 函数）给堵住了。当D变得很强时，它内部的 Sigmoid 函数会饱和（变得平坦）。这个“平坦”的 Sigmoid 层就像一个被水垢堵住的、极窄的瓶颈（梯度趋于0）。WGAN通过修改D函数，解决了这个问题（见后）。

### （四）训练终点

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们有一个固定的生成器 G, 我们想找到能最大化 V(), G) 的最优判别器刀 *。 我们的目标函数是： 以刀， G） 一佐。 10g(D@))dc + 一 D(G(z)))dz 为了简化， 我们知道 G(z) 会形成一个伪造数据的分布 （。 所以我们可以把第二项也写成关于的积分：。 忉 10g(D@)) + log(l 一刀国） ] 要找到使这个积分最大的刀 （， 我们只需要看积分号里的函数 （被积函数）， 并对任一个求它的最大值。

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们有一个固定的生成器 G, 我们想找到能最大化 （刀， G) 的最优判别器刀 *。 我们的目标函数是： “ 10g(D@))dc + pz(z) log(l 一 D(G(z)))dz 为了简化， 我们知道 G(z) 会形成一个伪造数据的分布 （。 所以我们可以把第二项也写成关于的积分：。 忉 10g(D@)) + log(l 一刀国） ] 要找到使这个积分最大的刀 （， 我们只需要看积分号里的函数 （被积函数）， 并对任一个求它的最大值。 令 a = （和 b = （， 我们要找来最大化刀） = a log(D) + blog(l 一切。 这是一个标准的求极值问题， 我们对刀求导并令其为 0： dD 刀 1 一刀

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

a + b 把 a 和 b 换回来， 我们就得到了最优判别器刀气刁：

以上推导通过对D求导，找到了使得V(D,G)最大的D，也就是最优判别器。这个公式告诉我们，一个完美的判别器，其输出概率D(x)应该等于“这个x来自真实数据的概率”除以“它来自真实数据或伪造数据的总概率”。

当然，判别器的训练只是手段，最终训练终点还要看生成器的输出。容易想到，如果G变得完美，伪造数据完全学习了真实数据的分布，p_(x) = p_data(x)，则D*(x)=1/2.下面我们来严格证明，当判别器D取D*时，maxDV(D,G)=V(D*,G)关于G最小等价于取到这种情况。

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Pdata log AOPdata 南忉 + （注意： 我们把佐。 （简写为砌，） 简写为） 经过一点数学变换， 这个表达式可以被重写为： log C(G) = 2 · 刀 “ 刀 （佐。 忉目一 2 log 2 DJSD 代表 Jensen-Shannon Divergence (JSD， 杰森 ． 香农散度）。 散度 (Divergence) 是信息论中用来衡量两个概率分布之间差异性的数学工具。 G 的最终目标 G 的目标是 minc C(G), 也就是： min [ 2 · s 刀 （“ 忉目一 2 log 2 ]

训练生成器G的最终数学目标，就是最小化真实数据分布p_data(x)和伪造数据分布p_g(x)之间的 JSD 散度，当且仅当两个分布完全相同时，即p_g(x)=p_data(x)时，JSD 散度才为 0。总之，目标函数中minG和maxD博弈的巧妙设计，使得G的任务变成了学习一个完美的p_g分布，使其与p_data一致。

## 三、GANs的应用

1. 图像生成与艺术创作

这是GANs最著名的应用。目标是让模型学习一个庞大数据集（比如10万张人脸照片）的“精髓”，然后能从一个随机噪声向量z出发，生成一张全新的、不属于数据集中任何一张，但又看起来极其逼真的人脸。可以用StyleGAN, BigGAN）等模型生成不存在的“模特”照片、创造数字艺术、游戏角色设计。与变分自编码器 (Variational Autoencoders, VAEs)的对比：

对比维度

生成对抗网络 (GANs)

变分自编码器 (VAEs)

核心思想

对抗 (Adversarial)：一个“生成器”和一个“判别器”相互博弈。

编码-解码 (Encoding-Decoding)：一个“编码器”将图像压缩成一个概率分布（潜空间 z），一个“解码器”再从 $z$ 中采样来重建图像。

训练目标

欺骗判别器：G的目标是让D无法区分真假。

最小化重建损失：目标是让“解码后的图像”与“原始图像”尽可能接近。

生成质量

高保真 (Sharp)：由于D的存在，G被迫生成非常清晰、锐利的图像边缘和纹理，否则“一眼假”。

较模糊 (Blurry)：VAEs最小化的是像素级的平均误差（如L2损失），这会鼓励模型生成“安全”的、模糊的平均图像，以在所有样本上都表现“还行”。

潜空间 $z$

纠缠 (Entangled)：原始GANs的z空间通常是无序的。改变z的一个维度，可能同时改变人脸的多个特征（如头发、年龄、朝向）。

平滑 (Smooth)：VAEs的z空间被特意正则化，是连续且平滑的。这使得它非常适合“插值”——比如在两张人脸的z向量之间平滑过渡。

VAEs 擅长学习一个平滑且有意义的潜空间（适合插值和理解数据结构），但生成结果偏模糊。GANs 擅长生成高度逼真的图像（适合追求视觉保真度），但训练更不稳定，潜空间也不如VAEs规整。

2. 图像到图像的转换

这类应用的目标是：给定一张输入图像A，将其转换为另一张具有特定风格或特征的输出图像B。这里，输入和输出都是图像，而且往往在结构上有一定的对应关系。典型模型如Pix2Pix（需要成对的训练数据，比如一张草图对应一张照片），CycleGAN（不需要成对数据，通过循环一致性损失实现非配对转换）。应用如将航拍地图转换为谷歌地图样式，将黑白照片上色，将手绘草图转换为逼真照片，将白天场景转换为夜晚场，将物体从语义分割图还原为真实图像等。在这个领域，传统的神经网络架构主要依赖于编码器-解码器 (Encoder-Decoder) 结构，通常是卷积神经网络 (CNNs)，并且常常会加入跳跃连接（如 U-Net 结构）来保留细节。对比：

对比维度

基于 GANs 的方法 (如 Pix2Pix, CycleGAN)

基于 CNNs 的编码器-解码器 (如 U-Net)

核心思想

学习映射G和判别真假D：生成器G学习从 A 域到 B 域的映射，判别器D判断G生成的 B 像不像真实的 B 域图像。

学习像素级映射：编码器逐步提取特征，解码器逐步重建图像，中间通过跳跃连接传递细节。

损失函数

对抗损失+像素级损失：除了让G骗过D的对抗损失外，还会结合L1或L2损失，确保生成的图像与目标在像素层面相似。

像素级损失：通常是 L1 或 L2 损失，衡量生成图像和目标图像之间的像素差异。

生成质量

细节更丰富、更真实：判别器D的存在迫使生成器G生成高频细节，使图像看起来更逼真、更锐利，尤其是在复杂纹理和结构上。

可能更平滑、更模糊：纯粹的像素级损失（如L2）会倾向于生成像素平均值，以避免大的惩罚，导致图像缺乏锐利的细节和真实感。

对数据对的需求

Pix2Pix 需要成对数据；CycleGAN 不需要成对数据，通过引入“循环一致性损失”来学习非配对转换，这是一个巨大的优势。

通常需要成对数据：为了训练模型从输入图像到输出图像的像素级映射，需要大量的(输入A, 输出B) 对。

灵活性

高度灵活：可以学习非常规的映射，如风格迁移、物体转化等。CycleGAN甚至能在没有直接对应的情况下进行域转换。

相对受限：主要用于重建、去噪、超分辨率等有明确像素级对应关系的转换。

基于 CNNs 的编码器-解码器 方法（如U-Net）在有大量成对数据且任务偏向于结构性恢复（如图像去噪、超分辨率）时表现良好，但可能缺乏真实感。GANs 通过引入判别器，能够生成更具真实感和高频细节的图像，即使在任务更具创意性或缺乏成对数据（如CycleGAN）时也能出色完成。

3.数据增强与模拟

有时，我们的真实数据非常少或非常昂贵。如在医学影像（如CT或MRI）中，要诊断一种罕见的癌症。我们可能只有几百张（甚至几十张）带标签的“阳性”图像。如果只用这点数据去训练一个AI模型，它很难学会，而且极易“过拟合”。这时我们往往采取以下方案：

（1）对已有数据进行变换：对于图像而言，就是旋转、裁剪、缩放、翻转、平移、添加噪声、改变对比度或亮度等。

（2）迁移学习：利用预训练模型进行微调。

（3）利用VAE（变分自编码器）：在编码-解码的过程中，一个 VAE 的编码器输出一个概率分布，解码器不是从一个“点”来重建，而是从这个“模糊区域”中随机采样一个点来进行重建。

（4）利用GAN生成：我们可以训练一个基于生成器-判别器架构的GANs模型（比如一个变种的DCGAN或WGAN），判别器以这几百张真实癌症图像的分布为参考基准。一旦训练完成，这个GANs的生成器就可以从随机噪声开始，生成成千上万张看起来逼真、但又与原始图像不完全相同的“假”癌症图像。

这种方法在其他领域也有很多应用。金融中，也可以用其模拟生成逼真的（但非真实的）欺诈交易数据，用于训练反欺诈系统。自动驾驶中，可以生成更多在现实中难以采集的“边缘案例”驾驶场景（如暴风雪中的行人、罕见的交通事故）。

对比维度

基于 GANs 的数据增强

传统数据增强

核心思想

生成新样本 (Generation)：学习真实数据的潜在分布 $p_{data}$，然后从这个分布中采样，创造出全新的数据点。

变换旧样本 (Transformation)：对已有的数据进行简单的几何或颜色变换。

具体方法

训练一个完整的生成器 $G$ 来“伪造”数据。

旋转、裁剪、缩放、翻转、平移、添加噪声、改变对比度或亮度。

数据多样性

高（理论上）：能生成在原始数据集中不存在的、但在同一分布下的全新组合。例如，一个“介于A和B之间”的新病灶。

低：生成的数据与原始数据高度相关。一张“旋转过的猫”在本质上并没有提供关于“猫”的全新信息。

解决的问题

数据稀缺和不平衡：当某一类别的数据（如罕见病）极度稀缺时，GANs是唯一能“无中生有”补充该类别的方法。

过拟合：通过增加训练数据的“表面”数量，使模型（如CNN）看到更多变化，防止它记住特定的训练样本。

挑战

训练不稳定：GANs训练困难，容易“模式崩溃”（Mode Collapse），即 G 只学会了生成少数几种“安全”的假样本。

简单有效：易于实现，计算成本低，是训练几乎所有视觉模型的标配步骤。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- （4）利用GAN生成：我们可以训练一个基于生成器-判别器架构的GANs模型（比如一个变种的DCGAN或WGAN），判别器以这几百张真实癌症图像的分布为参考基准。一旦训练完成，这个GANs的生成器就可以从随机噪声开始，生成成千上万张看起来逼真、但又与原始图像不完全相同的“假”癌症图像。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
