---
title: "4.7 双向循环神经网络"
source_docx: "第1部分 深度学习/4.循环神经网络/4.7 双向循环神经网络.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 4.7 双向循环神经网络

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、隐马尔可夫模型中的动态规划

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果我们想用概率图模型来解决这个问题， 可以设计一个隐变量模型： 在任意时间步假设存在某个隐变量， 通过概率尸 （昀丨） 控制我们观测到的昀。 此夕卜， 任何 + + 1 转移都是由一些状态转移概率 P （+ 1） 给出。 这个概率图模型就是一个隐马尔可关模型 (hidden Markov model, HMM), 如图 9 ． 4 ． 1 所示。 图 9 ． 4 7 隐马尔可夫模型因此， 对于有 T 个观测值的序列， 我们在观测状态和隐状态上具有以下联合概率分布：） = ll P()t 《 一 1） P@ 》 同， where P()1 》 九 0） = 尸伊 1） · P01， · 现在， 假设我们观测到所有的， 除了与， 并且我们的目标是计算 P （与 《 刁）， 其中 （9 ． 4 ． 1） 由于尸 （与 | 一 j） 中没有隐变量， 因此我们考虑对九 1， · T 选择构成的所有可能的组合进行求和。 如果任何可以接受左个不同的值 （有限的状态数 ), 这意味着我们需要对 T 个项求和， 这个任务显然难于登天。 幸运的是， 有个巧妙的解决方案： 动态 (dynamic programming)o

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果我们想用概率图模型来解决这个问题， 可以设计一个隐变量模型： 在任意时间步假设存在某个隐变量， 通过概率尸 （昀丨） 控制我们观测到的昀。 此夕卜， 任何 + + 1 转移都是由一些状态转移概率 P （+ 1） 给出。 这个概率图模型就是一个隐马尔可关模型 (hidden Markov model, HMM), 如图 9 ． 4 ． 1 所示。 图 9 ． 4 7 隐马尔可夫模型因此， 对于有 T 个观测值的序列， 我们在观测状态和隐状态上具有以下联合概率分布：） = ll P()t 《 一 1） P@ 》 同， where P()1 》 九 0） = 尸伊 1） · P01， · 现在， 假设我们观测到所有的， 除了与， 并且我们的目标是计算 P （与 《 刁）， 其中 （9 ． 4 ． 1） 由于尸 （与 | 一 j） 中没有隐变量， 因此我们考虑对九 1， · T 选择构成的所有可能的组合进行求和。 如果任何可以接受左个不同的值 （有限的状态数 ), 这意味着我们需要对 T 个项求和， 这个任务显然难于登天。 幸运的是， 有个巧妙的解决方案： 动态 (dynamic programming)o

1.前向递归（“对上次递推”）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

要了解动态规划的工作方式， 我们考虑对隐变量后尸，， XT, 五 1， 丌 2 （h2）： 九 7， 的依次求和。 根据 （9： 4： 0 将得出： （9 互 2） 71 乥乥丌 2 （同尸 （1 九 2） 尸@ 《 尸@ | 》 五凵） P@t 《 同丌 3 （h3）：

上式中，pi_t(h_t)表示的是所有“得到(h_1,...,h_t-1,h_t,x_1,..,x_t-1)的组合的概率”求和（这里h_1,...,h_t-1有多种组合要求和，x_1,..,x_t-1是给定的），也就是输出x_1,..,x_t-1并得到t时刻的隐状态h_t的总概率，是“实际发生概率”.从而等号右边表示：在各种h_T选择下，得到(x1,...,xT)的概率，对h_T求和（pi_t(h_t)内部已经隐含了对h_1,...,h_t-1的各种组合的求和，也隐含了得到x_1,..,x_T-1的概率，故这里只需要对h_T求和）。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

通常， 我们将前向莲归 (forward recursion) 写为： 丌“@司乥 Tt(ht)P@t 1 尸@ 递归被初始化为丌 1 （五 1) = P 伊 1）。 符号简化， 也可以写成 + 1 = 煮 0， 0）， 在循环神经网络中讨论的隐变量模型中的更新方程。 其中歹是一些可学习的函数。 （9 ． 4 3） 这看起来就像我们

2.后向递归（“对下次递推”）

这里我们设的ρ_t(h_t)是“在已经得到h_t，x_t条件下，最终得到(x_t+1,...,x_T)的概率”，是“给定状态下的预测概率”，包含了对经过的各种可能的h_t+1,...,h_T路径概率的求和.

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

尸 01， ． · ,CT, 九 1， 九 1 因此， 我们可以将后向递归 (backward recursion) 写为： 一 1@一 1） = 乥丨一 1） 尸@ 丨九加 D， （9 ． 4 ． 4） （9 ． 4 ． 5） 初始化 PT 伊刁 = 1。 前向和后向递归都允许我们对 T 个隐变量在 0） （线性而不是指数） 时间内对伊 1， · ·， 九刁的所有值求和。 这是使用图模型进行概率推理的巨大好处之一。 它也是通用消息传递算法 （） 的一个非常特殊的例子。 Aji and McEliece, 2000

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

尸 01， ． · ,CT, 九 1， 九 1 因此， 我们可以将后向递归 (backward recursion) 写为： 一 1@一 1） = 乥丨一 1） 尸@ 丨九加 D， （9 ． 4 ． 4） （9 ． 4 ． 5） 初始化 PT 伊刁 = 1。 前向和后向递归都允许我们对 T 个隐变量在 0） （线性而不是指数） 时间内对伊 1， · ·， 九刁的所有值求和。 这是使用图模型进行概率推理的巨大好处之一。 它也是通用消息传递算法 （） 的一个非常特殊的例子。 Aji and McEliece, 2000

注：这里ρ1(h1)内部已经包含了对经过各种h2,...,hT路径的求和.

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果我们想用概率图模型来解决这个问题， 可以设计一个隐变量模型： 在任意时间步假设存在某个隐变量， 通过概率尸 （昀丨） 控制我们观测到的昀。 此夕卜， 任何 + + 1 转移都是由一些状态转移概率 P （+ 1） 给出。 这个概率图模型就是一个隐马尔可关模型 (hidden Markov model, HMM), 如图 9 ． 4 ． 1 所示。 图 9 ． 4 7 隐马尔可夫模型因此， 对于有 T 个观测值的序列， 我们在观测状态和隐状态上具有以下联合概率分布：） = ll P()t 《 一 1） P@ 》 同， where P()1 》 九 0） = 尸伊 1） · P01， · 现在， 假设我们观测到所有的， 除了与， 并且我们的目标是计算 P （与 《 刁）， 其中 （9 ． 4 ． 1） 由于尸 （与 | 一 j） 中没有隐变量， 因此我们考虑对九 1， · T 选择构成的所有可能的组合进行求和。 如果任何可以接受左个不同的值 （有限的状态数 ), 这意味着我们需要对 T 个项求和， 这个任务显然难于登天。 幸运的是， 有个巧妙的解决方案： 动态 (dynamic programming)o

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

尸 01， ． · ,CT, 九 1， 九 1 因此， 我们可以将后向递归 (backward recursion) 写为： 一 1@一 1） = 乥丨一 1） 尸@ 丨九加 D， （9 ． 4 ． 4） （9 ． 4 ． 5） 初始化 PT 伊刁 = 1。 前向和后向递归都允许我们对 T 个隐变量在 0） （线性而不是指数） 时间内对伊 1， · ·， 九刁的所有值求和。 这是使用图模型进行概率推理的巨大好处之一。 它也是通用消息传递算法 （） 的一个非常特殊的例子。 Aji and McEliece, 2000

为保证t=T时公式形式的一致性，初始化ρT(hT)=1.

3.二者的结合

如果我们只知道时间序列输出x_1,...,x_T中除了x_j之外其他所有元素，但不知道隐状态h_1,...,h_T，这时如果逐个枚举每个h_j的全部可取的值（假设每个h_j的取值空间有k个值），其计算复杂度为O(k^T)，显然行不通.

这里我们采用前向递归与后向递归结合的方法：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

（9 ． 4 ． 6） 因为符号简化的需要， 后向递归也可以写为一 1 = g(pt, 昀）， 其中 g 是一个可以学习的函数。 同样， 这看起来非常像一个更新方程， 只是不像我们在循环神经网络中看到的那样前向运算， 而是后向计算。 事实上， 知道未来数据何时可用对隐马尔可夫模型是有益的。 信号处理学家将是否知道未来观测这两种情况区分为内插和外推， 有关更多详细信息， 请参阅 （Doucet a/., 2001

（这里分母是得到除了x_j之外其他所有元素的概率，是和隐状态无关的常数）

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

（9 ． 4 ． 6） 因为符号简化的需要， 后向递归也可以写为一 1 = g(pt, 昀）， 其中 g 是一个可以学习的函数。 同样， 这看起来非常像一个更新方程， 只是不像我们在循环神经网络中看到的那样前向运算， 而是后向计算。 事实上， 知道未来数据何时可用对隐马尔可夫模型是有益的。 信号处理学家将是否知道未来观测这两种情况区分为内插和外推， 有关更多详细信息， 请参阅 （Doucet a/., 2001

从计算复杂度的角度，无论是前向递归还是后向递归，对T个隐变量的情况对(h_1,...,h_T)的所有取值情况求和，递归计算过程的计算复杂度均为O(kT).上述公式相当于x_j以前取前向递归，以后取后向递归，计算复杂度仍然为O(kT).（注：这里的计算复杂度是执行计算时的，并不包括训练阶段训练概率值之类的）

## 二、双向循环神经网络

在序列学习中，我们以往假设的目标是：在给定观测的情况下（例如，在时间序列的上下文中或在语言模型的上下文中）， 对下一个输出进行建模。但实际上，对于文章挖空填空这样的任务，每个短语的“下文”传达了重要信息（如果有的话），而这些信息关乎到选择哪个词来填空， 所以无法利用这一点的序列模型将在相关任务上表现不佳。例如，如果要做好命名实体识别 （例如，识别“Green”指的是“格林先生”还是绿色）， 不同长度的上下文范围重要性是相同的。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对于任意时间步给定一个小批量的输入数据 Xt e （样本数每个示例中的输入数 d), 并且令隐藏层激活函数为夯在双向架构中， 我们设该时间步的前向和反向隐状态分别为 Ht e “ × 九和 Ht e 其中九是隐藏单元的数目。 前向和反向隐状态的更新如下： 其中， 权重每九 e 厂 e Ht = +(XtW Ht = +(XtW (b) （9 ． 4 ． 7） (f) 厂 e 和偏置 bh e， bh e × 九都是模型参数。 “ × 2 方接下来， 将前向隐状态 Ht 和反向隐状态 Ht 连接起来， 获得需要送入输出层的隐状态 Ht e。 在具有多个隐藏层的深度双向循环神经网络中， 该信息作为输入传递到下一个双向层。 最后， 输出层计算得到的输出为 Ot e （q 是输出单元的数目）： Ot = HtWhq + bq （9 ． 4 ． 8） 这里， 权重矩阵 q e 和偏置 bq e 是输出层的模型参数。 事实上， 这两个方向可以拥有不同数量的隐藏单元。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对于任意时间步给定一个小批量的输入数据 Xt e （样本数每个示例中的输入数 d), 并且令隐藏层激活函数为夯在双向架构中， 我们设该时间步的前向和反向隐状态分别为 Ht e “ × 九和 Ht e 其中九是隐藏单元的数目。 前向和反向隐状态的更新如下： 其中， 权重每九 e 厂 e Ht = +(XtW Ht = +(XtW (b) （9 ． 4 ． 7） (f) 厂 e 和偏置 bh e， bh e × 九都是模型参数。 “ × 2 方接下来， 将前向隐状态 Ht 和反向隐状态 Ht 连接起来， 获得需要送入输出层的隐状态 Ht e。 在具有多个隐藏层的深度双向循环神经网络中， 该信息作为输入传递到下一个双向层。 最后， 输出层计算得到的输出为 Ot e （q 是输出单元的数目）： Ot = HtWhq + bq （9 ． 4 ． 8） 这里， 权重矩阵 q e 和偏置 bq e 是输出层的模型参数。 事实上， 这两个方向可以拥有不同数量的隐藏单元。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

双向循环神经网络的一个关键特性是： 使用来自序列两端的信息来估计输出。 也就是说， 我们使用来自过去和未来的观氵则信息来预测当前的观测。 但是在对下一个词元进行预测的情况中， 这样的模型并不是我们所需的。 因为在预测下一个词元时， 我们终究无法知道下一个词元的下文是什么， 所以将不会得到很好的精度。 具体地说， 在训练期间， 我们能够利用过去和未来的数据来估计现在空缺的词； 而在测试期间， 我们只有过去的数据， 因此精度将会很差。 下面的实验将说明这一占另一个严重问题是， 双向循环神经网络的计算速度非常慢。 其主要原因是网络的前向传播需要在双向层中进行前向和后向递归， 并且网络的反向传播还依赖于前向传播的结果。 因此， 梯度求解将有一个非常长的链。 双向层的使用在实践中非常少， 并且仅仅应用于部分场合。 例如， 填充缺失的单词、 词元注释 （例如， 用于命名实体识别） 以及作为序列处理流水线中的一个步骤对序列进行编码 （例如， 用于机器翻译）。 在 14 ． 8 节和 1 5 ． 2 节中， 我们将介绍如何使用双向循环神经网络编码文本序列。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 如果我们想用概率图模型来解决这个问题， 可以设计一个隐变量模型： 在任意时间步假设存在某个隐变量， 通过概率尸 （昀丨） 控制我们观测到的昀。 此夕卜， 任何 + + 1 转移都是由一些状态转移概率 P （+ 1） 给出。 这个概率图模型就是一个隐马尔可关模型 (hidden Markov model, HMM), 如图 9 ． 4 ． 1 所示。 图 9 ． 4 7 隐马尔可夫模型因此， 对于有 T 个观测值的序列， 我们在观测状态和隐状态上具有以下联合概率分布：） = l...
- 要了解动态规划的工作方式， 我们考虑对隐变量后尸，， XT, 五 1， 丌 2 （h2）： 九 7， 的依次求和。 根据 （9： 4： 0 将得出： （9 互 2） 71 乥乥丌 2 （同尸 （1 九 2） 尸@ 《 尸@ | 》 五凵） P@t 《 同丌 3 （h3）：

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
