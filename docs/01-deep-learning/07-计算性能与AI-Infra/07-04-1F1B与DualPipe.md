---
title: "7.4 1F1B与DualPipe"
source_docx: "第1部分 深度学习/7.计算性能与AI Infra/7.4 1F1B与DualPipe.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 7.4 1F1B与DualPipe

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、分布式训练框架中的基本训练步骤

1. F（Forward）：前向计算

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

动作： 输入数据经过当前层的算子 （如矩阵乘法彬）， 输出激活值公式： 关键任务： 计算预测结果， 并 * * 缓存 ℃ heckpointing) * * 中间层的激活值， 留给后续反向传播使用。 在流水线并行中： F 的输出需要发送给下一个 (Downstream) GPU 节点。

2.B（Backward for Input / Activation Gradient）：激活梯度计算

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

动作： 根据上一层传回的梯度， 计算对当前层输入的梯度。 公式： DL DL Dy Dy 关键任务： 它是反向传播的 " 接力棒 "。 计算出的必须立刻传回给上一个 (Upstream) GPU 节点， 以便那一层继续计算。 地位： 在流水线并行中， B 位于关键路径 (Critical path) 上。 如果 B 算得慢或传输延迟， 整个流水线都会卡住 （产生 Bubble)0

3.W（Backward for Weights / Parameter Gradient）：权重梯度计算

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

DL 动作： 根据梯度一， 计算对当前层参数 O 的梯度。 公式： DL VO = DO DL Dy Dy DO 关键任务： 计算出的 VO 会被存放在梯度缓冲区中， 最终由优化器 (Optimizer) 用于更新模型权重。 地位： w 不在关键路径上。 因为 VO 只需要在这一层本地更新， 不需要传给其他 Rank 来解开计算依赖。

## 二、1F1B

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们有 4 个 GPU (Rank 3), 流水线深度 PP = 4。 对于 RankO 来说： 它处理微批次 ml 的前向计算 (FI) 非常快。 但是： 丆 1 算完后， 必须传给 Rank 1 -+ Rank2 -+ Rank3, 在 Rank3W 出 Loss 后， 梯度再从 Rank3 ． Rank2 ． 分 Rank1, 最后才传回 Rank 0。 结论： 当 RankO 拿到 rnl 的梯度准备算反向 (BI) 时， 中间已经过去了一段很长的时间。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了不让 GPU 闲着， RankO 在等待 BI 的梯度传回来的这段时间里， 会继续往后算， 丆 3， 跽 · · 蚂： 代表当前正在处理的第个微批次的前向。 Bt-k： 代表之前某个已经跑完一圈回来的微批次 （第一左个） 的反向。 这里的实际上取决于 GPU 所在的 Rank 位置。 · 在流水线末端 （如 Rank3)， 丆和 B 靠得很近， 很小。 · 在流水线顶端 （如 RankO), 丆和 B 隔得很远， 很大。

“气泡”大小：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设每个阶段 （或 B) 耗时为 1 个单位时间： 1 ． 2 ． 3 ． 4 ． 5 ． T = 0： RankO 执行丆 1。 T = PP— 1： 经过 PP— 1 跳， 丆 1 到达最后一个 Rank 并完成计算， 立即算出 Losso T = PP: 最后一个 Rank 开始执行反向计算 BI。 T = PP+ ()P 一 1）： 梯度经过 PP 一 1 跳， 传回 Rank00 T = 2PP 一 1： Rank 0 终于可以开始执行 BIO 结论： 在看来， 从丆 1 结束到 BI 开始， 中间隔了 2 尸 P 一 2 个时间片。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这里有一个概念上的区分： 单任务延迟 vs 流水线总气泡。 总气泡 (BubbIe)： 指的是在整个训练任务中， 所有 GPU 处于 " 空闲 （ldle） " 状态的总时间。 · 在 IFIB 中， RankO 在等待 BI 回来的这段时间内， 并没有闲着， 而是在疯狂地跑 2， 3， · 真正无法掩盖的空闲时间只有： 最开始启动时的 " 热身 " 和最后结束时的 " 收尾 “。 将所有 GPU 的空闲时间相加再平均， 每个 GPU 的平均气泡大小确实约为 ()P 一 1） × （丆 + B)O

## 三、DualPipe

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们将以 4 个 GPU (Rank 0 · 3), 流水线并行深度 PP = 4 为例。 正向流 (DI)： 数据从月 0 + 咒 1 -+ 月 2 + 月 3 （逻辑层 LI + L8)0 反向流 (D2)： 数据从月 3 + 月 2 + 咒 1 + 月 0 （逻辑层 LI + L8， 注意是镜像的）。 · 计算拆分： 每个阶段分为丆 （前向）、 B （算输入梯度， 传给邻居）、 W （算权重梯度， 本地

### （一）双向热身阶段

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

时间步 （Rank 0 （LI 一 2， L7 一 8） 1 （ml） 1 （m2） 1 (mD 1 （m4） + 2 （砌） Rank 1 （L3 一 4， L5 一 6） 1 （ml） 1 + 2 （砌） 1 ()D + 2）

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Rank 2 （L5 一 6， L3 一 4） 2 （砌） 2 （砌） + 1 （ml） Rank 3 （L7 一 8， LI 一 2） 2 （砌） 2 （砌） 2 (m4) + 1 （ml） 备注双向同时启动数据向中间汇聚碰头： 中间节点处理双向流此时 ml 到达咒 3， 砌到达月 0

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

分析： 在 T4 结束时， rnl 在月 3 算完了全部前向 （逻辑 L1-L8)， 砌在 0 算完了全部前向。 从 5 开始， 系统将产生第一批梯度并进入复杂的对冲阶段。

注意：m1和n1是完全不同的数据批次。

### （二）稳态阶段

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在稳态下， 一个 GPU 并不是按顺序执行， 而是通过两个主要的 * * 并行块 ℃ andidate Blocks) 来工作。 在一个完整的稳态步内， 它包含以下两个核心对冲： 对冲 A： 丆刀 1 与 B 刀 2 的完全重叠 · 计算内容 · GPU 同时启动两个算子： 1 ． 正向流新批次的前向计算刀 1 （m，）。 2 ． 反向流老批次的激活梯度计算 B 刀 2 （）。 通信掩盖 · 在计算丆刀 1 时， 后台接收 B 刀 2 需要的下游梯度。 · 在计算 B 刀 2 时， 后台发送丆刀 1 产生的激活值。 结论： 只要计算时间 T(F) T(B), 这两个算子就能把彼此的通信时间完全吞掉。 对冲 B： 丆刀 2 与 B 刀 1 的完全重叠与上述逻辑镜像， 处理反向流的前向和正向流的激活梯度。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

步骤 C： 填补 W (weight Gradient) · 计算： 上述 B 计算完后， 立刻在后台异步算彬 （权重梯度）。 原理： 彬不影响任何人， 它是 “ 计算废料 "， 哪里有缝隙就塞在哪里。

在一个对冲周期（如F_D1与B_D2对冲）内共有4个通信步骤，都被掩盖在计算时间中：

在F_D1计算时：

Send-Backward-D2: 把上一批次算好的n_j-1梯度发给右边。

Recv-Backward-D2: 从左边接收本批次n_j的梯度。

在B_D2计算时：

Send-Forward-D1: 把算好的m_i激活值发给右边。

Recv-Forward-D1: 从左边接收下一批次m_i+1的激活值。

### （三）收尾阶段

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当所有微批次的前向都跑完了， GPU 进入纯反向模式。 1 ． 处理剩余的 B： 把最后几个微批次的输入梯度算完并传走。 此时已经没有丆可以对冲了， 但因为 PP 被分成了两个方向， 气泡的绝对时长减半。 2 ． 全力算 W: 这是最后的体力活， 所有 GPU 并行计算自己负责的层权重更新。

### （四）气泡减少的核心原因

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 IFIB 中， RankO 算出丆 1 后， 要等 2()P 一 1） 个时间片才能拿到梯度算 BIO Timewait = ()P 一 1） × （丆 + B)

DualPipe采取了以下方式：

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 双向对冲 (Bidirectional)： · 在 IFIB 等待梯度的时间里， DualPipe 让 GPU 去跑 “ 另一个方向 " 的前向和反向。 数学逻辑： 将一个 PP 链拆成两个半深度的 PP 链对冲。 气泡从 ()P 一 1） 变成了 B 与拆分 (z e ro Bubble 思想）： · 在传统做法中， 反向传播是 B + VVO 必须等这两个都算完， 梯度才能发给上一个 GPUO DualPipe 算完 B （只需矩阵乘氵去的一半工作量） 立刻就把梯度发走。 这样邻居 GPU 就能提前开始干活。 结果： 关键路径上的等待时间进一步压缩。 完全重叠 (Full Overlap)： 利用现代 GPU 的双工带宽 (NVLink 可以同时全速 Send 和 Recv)。 DualPipe 调度算法保证了： 当 GPU 在做计算时， 带宽始终是满的； 当带宽在传输时， SM 核心始终在计算。

### （五）折叠流水线技术

DualPipe的“代价”是一个GPU需要存两倍的权重。原因：

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

正向流 Direction 1： 从 0 -+ 月 at 3。 Rank 必须有第 1 层。 反向流 Direction 2： 从 3 -+ 月 at 0。 但请注意， 反向流也是一个完整的前向一反向过程。 如果它从 Rank3 开始起步， Rank3 就必须有第 1 层。 这样一来， 第 1 层在 RankO 和 Rank3 都有， 参数量自然翻倍了。

为了解决这个问题，可以采用“折叠”的方法：

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在折叠架构下， 我们不再让 RankO 和 Rank 3 都存第 1 层， 而是这样分配 Stage: 物理 Rank Rank 0 Rank 1 Rank 2 Rank 3 负责的逻辑 Stage ()x 参数， 不冗余） Stage 0 （入口） 和 Stage 7 （出口） Stage 1 和 Stage 6 Stage 2 和 Stage 5 Stage 3 和 Stage 4 物理位置属性流水线两头流水线折返

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

虽然每个 Stage 只存了一份 ()x 参数）， 但因为每个 GPU 现在都手握一个靠前的 Stage （如 SO) 和一个靠后的 Stage （如 (7)， 对冲依然可以发生： 1 ． 任务组合： 当 RankO 正在处理微批次 m10 的 Stage 0 （前向计算） 时， 它手里可能正好拿着微批次 ml 绕了一圈回来后的 Stage 7 （前向或反向计算）。 算子对冲 · 2 ． · 计算 A： 0 （新批次的前向）。 · 计算 B · BS7 （老批次的反向）。 通信掩盖 · 3 ． Rank 0 在发送 0 给 Rank 1 的同时， 正好在接收 Rank 1 传回来的 BS7 梯度。 这就是 “ 折叠 " 的精髓： 它利用了流水线的深度 （一个微批次从 SO 到 S7 走一圈很慢） 创造了时间差， 让同一个 GPU 在同一时间处理不同生命周期的任务。

但实际上很多大模型并不一定会用“折叠”的方法，原因有两方面。

1.MoE模型的特点

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在一个超大规模 MoE 模型中， 模型参数被分为两部分： 共享参数 (Shared Parameters)： 包括注意力机制 (Attention)、 层归一化 (LayerNorm) 和一些共享的稠密层。 专家参数 (Expert Parameters)： MoE 层中成百上千个独立的专家 (MLP 块）。

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这是最核心的工程实现细节： 专家参数是不参与流水线镜像冗余的。 在分布式训练中， 我们通常同时使用 PP (Pipeline ParalleIism) 和 EP (Expert Parallelism)： 1 ． pp 决定了路径： 它决定了微批次在哪几个 GPU 之间流转。 2 ． EP 决定了存放： 它决定了某一个具体的专家 E100 物理上存在哪块显存里。 在 DualPipe 的双向流中： 当一个微批次运行到某一层需要调用专家时， 它会发起一个 All-to-All 通信， 去寻找那个专家所在的物理 GPUO 无论这个微批次是属于 " 正向流 " 还是 " 反向流 “， 它去寻找的都是同一个物理地址上的专家。 结论： 专家参数在整个集群里依然只存了一份。 DualPipe 只需要在每个 Rank 上多存一份轻量级的共享层参数和路由逻辑 (Router) 即可。

2.激活值对显存占用更大

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

对于超长上下文 ℃ ontext Length) 训练， 显存瓶颈往往不是参数 (weights)， 而是激活值。 · 在训练时， 为了反向传播， 每一层的前向计算结果都要存下来。 DualPipe 的显存压力： 真正让显存紧张的是它同时在跑两个方向的流， 这意味着显存里要同时存放 m 批次和 “ 批次的激活值。 但 DeepSeek 巧妙地化解了这一点： · 通过 M LA (Multi-head Latent Attention) 大幅压缩了 KV Cache 和激活值的体积。 配合 Recomputation （激活值重计算） 技术， 可以在需要时再计算部分激活值， 用计算时间换取显存空间。 相比之下， 多存那一套共享参数所占用的几 GB 显存， 简直是 “ 洒洒水 "。

3.训练调度方面

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

带宽压榨： lx 参数的折叠流水线在调度上非常死板 （必须走 v 字型）。 而 2x 参数的 DualPipe 拥有两条完全独立且对称的路径， 这使得它能百分之百跑满双向 NVLink 带宽， 调度灵活性极高。 气泡率的极致追求： 在折叠模型中， 由于 B 和彬的依赖关系更复杂， 很难做到像 DualPipe 那样几乎 " 零气泡 "。 DeepSeek 的目标是把 MFU （模型算力利用率） 推向 70 ％ 以上， 2 × 冗余是一笔划算的交易。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
