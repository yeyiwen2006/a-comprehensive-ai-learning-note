---
title: "10.3 状态分布与占用度量"
source_docx: "第2部分 强化学习/10.强化学习的基本知识/10.3 状态分布与占用度量.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 10.3 状态分布与占用度量

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、状态分布和占用度量的定义

状态分布定义为智能体处于状态s的折扣后概率：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

定义了 "'@）， 即在策略丌下的折扣状态访问分布 · ' （5）： 表示在时刻智能体处于状态 5 的概率。 7： 折扣因子 (Discount Factor), 0 7 < 1。 1 一 7： 归一化因子 (Normalization Factor)。

进而可以定义占用度量，即智能体在s发出动作a的折扣后概率：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

pT(s,a

此时有关系：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

（5） 丌@5）

## 二、原理推导

1.为什么需要折扣因子？

我们通常希望最大化累积折扣回报：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 4 OCR 未识别出有效文字：未识别出有效文字。]

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

利用期望的线性性质， 我们可以把求和号拿到期望外面： J(T) _ 乥 7tET[R(st, ] J(T) _ 乥 7 ' 乥 （5） 丌@5） 咒@ a) 现在， 我们将对于 5 和 a 的求和提到最前面： J(T) _ 乥 a) 乥 7 ' 瞟@丌@5） 这正是未归一化的占用度量为了让这一项变成概率分布 （和为 1), 我们乘以并除以 （1 一 7）： 乥月@a） （1 一 7） 乥 7 ' 瞟@） 丌@5） pT(s,a

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1

在强化学习中，我们的目标往往是找到一个好的策略，策略用概率分布表示，优化目标正是在概率分布下回报的数学期望。本情境下，目标函数正比于能使得回报R在ρ_Π的概率分布下的数学期望，把问题转化为找这样的概率分布的问题，状态-动作对在分布中的概率，等于该状态-动作对对总价值的贡献权重，符合我们对马尔可夫决策过程的认知。

2.为什么需要归一化因子？

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这是一个非常关键的数学细节。 我们需要证明 （5） 是一个合法的概率分布， 即其对所有状态求和必须为 1。 推导如下： 我们对所有状态求和： 交换求和顺序 （假设级数收敛）： 乥 （5） 时刻 t 在任意状态的概率之和为 1 因为无论乯是多少， 智能体一定会在某个状态， 所以 E、 号 ' （5） = 1。 公式变为：

## 三、占用度量的数学含义：按几何分布在时间上采样

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

通常我们理解的 “ 访问概率 "， 往往指的是 * * " 长期来看， 在所有时间步中， 我有百分之多少的时间待在这个状态 " * * （即简单的频率统计）。 但加上折扣因子 7 后， 现在的声@ a) 显然不再是简单的 “ 时间频率 " 了 （因为它重前期、 轻后之所以依然称之为 " 概率 "， 是因为我们改变了 “ 采样时间严的概率分布。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

传统视角 （无折扣， 7 = 1) 你假设每一个时刻被抽到的概率是相等的 （这在无限时间内其实无法实现数学上的均匀分布， 通常取极限）。 这就导致了我们常说的 " 平稳分布 " （Stationary Distribution)， 即单纯的时间平均占比。 折扣视角 （有折扣， 7 < 1) 这里的 pT(), a) 实际上定义了一个新的随机实验。 在这个实验中， 我们不是随机均匀地抽取一个时刻， 而是按照几何分布 (Geometric Distribution) 来抽取时刻采样规则： 抽到 = 0 的概率是 （1 抽到 = 1 的概率是 （1 抽到 = 2 的概率是 （1 一 7） 7 一 7） 72 抽到时刻的概率是采样时刻 = = （1 一 7 厅。 现在的物理含义是： " 如果不确定在哪个时间点观察， 而是按照上述 ' 越近越重要 ' 的规则随机抽取一个瞬间， 那么在这个瞬间， 智能体位于 @， a) 的概率是多少？ “

也可以这样理解：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了更直观地理解， 我们可以引入一个 * * " 随机终止 “ * * 的设定 （这也是 7 最经典的物理意义）： 假设这个游戏不是无限进行的， 而是在每一步结束后， 上帝会掷一个骰子： 有 7 的概率让游戏继续。 有 1 一 7 的概率让游戏突然束苤、 - 口 0 那么， pT(), a) 衡量的就是： 当游戏 “ 突然结束 " 的那一瞬间 (Game Over Snapshot)， 智能体正处于状态 5 且正在做动作 a 的概率。 这个解释完美契合了公式： 游戏恰好在时刻乯结束的概率是 （1 一 7） 酽。 · 在时刻智能体位于 @， a) 的概率是号'@， a)o 把所有可能结束的时刻加起来， 就是终止时看到 @， a) 的总概率。 因为游戏一定会结束 （在某一个时刻 ), 所以所有 @， a) 的这种概率加起来一定等于 1。 因此， 它疋一个完美的概率分布。

## 四、状态分布的递归表示

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

当我们计算时） 时， 我们统计的是 * * " 在从 = 0 到 = 的所有时间步中， 智能体出现在 5 / 的加权总和对于任何一次出现， 它发生的时间只有两种可能： 1 ． 它发生在乯 = 0： 这就是 " 初始状态 "。 2 ． 它发生在 > 0： 这就是 " 从上一时刻转移过来的 "。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 5 ' 的总概率 = 作为起点的权重 + 从别处转移来的权重

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第一项： 初始贡献尸 （的 s， a） 丌 （5） " ' (s)dsda 第二项： 转移贡献

其中v_0(s’)指的是初始状态下在s’的概率p(s_0=s’)，和v_Π不是一个函数。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一项： （1 一 7 炻 （的含义： 智能体在 = 0 时刻就位于 5 ／ 的加权概率。 （的： 初始状态分布， 即 P()o = （1 一 7）： 这是一个时间权重的归一化系数。 · 回忆的定义： " ' （5） 当乯 = 0 时， 70 = 1。 所以乯 = 0 这一时刻对总分布的贡献就是系数 （1 一 7） 乘以概率 （的。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第二项： 7 f P （的 5， a） 丌司 " ' （5） ds 含义： 智能体从上一时刻 （任意状态 5） 经过一步转移到达 s' 的加权概率。 · ""@）： 上一时刻在状态 s 的概率密度。 · 丌 （国 5）： 在 5 选择动作 a 的概率。 P （的 5， a)： 执行 a 后从 5 跳到 5 ／ 的概率。 f · · ·： 对所有可能的 " 前任 “ 状态和动作进行积分 （如果是离散空间就是求和乥） 流向 5 / 的可能性汇总。 关键的折扣因子。 7： · 因为从 5 到 s' 经过了 1 个时间步。， 把所有如果在上一时刻 （比如 t) 的权重是， 那么到达 5 / 时已经是时刻 + 1， 权重变成了 7 相比于上一时刻， 权重衰减了 7 倍。 所以必须乘上 7。

## 五、占用度量相关定理

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

进一步得出如下两个定理。 定理 1： 智能体分别以策略丌 1 和丌 2 和同一个 MDP 交互得到的占用度量 p 丌 1 和 p 丌 2 满足 pT ． # 二丌 1 = 丌 2 P 定理 2： 给定一合法占用度量 p， 可生成该占用度量的唯一策略是

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
