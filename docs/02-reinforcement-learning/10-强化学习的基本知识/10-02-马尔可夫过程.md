---
title: "10.2 马尔可夫过程"
source_docx: "第2部分 强化学习/10.强化学习的基本知识/10.2 马尔可夫过程.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 10.2 马尔可夫过程

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

首先说明：强化学习是一种训练方法（和监督学习等相对应），而不是一种模型架构或优化算法。

## 一、马尔可夫过程

多臂老虎机中，智能体的行动并不对环境产生影响，但真实世界并非如此。我们引入马尔可夫过程的概念。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

马尔可夫过程 （Ma rkov process） 指具有马尔可夫性质的随机过程， 也被称为马尔可夫涟 （Markov chain)。 我们通常用元组西 P) 描述一个马尔可夫过程， 其中 5 是有限数量的状态集合， 尹是状态转移矩阵 （state tra nsition matrix）。 假设一共有 “ 个状态， 此时 = { SI， 52， ．， sn}o 状态转移矩阵定义了所有状态对之间的转移概率， 即尸 （s 小 1） P(SIISn P@小 1 P(snIsn 矩阵尹中第 i 行第 j 列元素 P （与） = P(St+ = St = (i) 表示从状态 s， 转移到状态力的概率， 我们称 P （的司为状态转移函数。 从某个状态出发， 到达其他状态的概率和须为 1， 即状态转移矩阵的每一行的和为 1。 图 3 ． 1 是一个具有 6 个状态的马尔可夫过程的简单例子。 其中每个绿色圆圈表示一个状态， 每个状态都有一定概率 （包括概率为 0） 转移到其他状态， 其中 56 通常被称为终止状态 (terminal state）， 因为它不会再转移到其他状态， 可以理解为它永远以概率 1 转移到自己。 状态之间的虚线箭头表示状态的转移， 箭头旁的数字表示该状态转移发生的概率。 从每个状态出发转移到其他状态的概率总和为 1。 例如， 有 90 ％ 概率保持不变， 有 10 ％ 概率转移到 ”， 而在 ” 又有 50 ％ 概率回到 ”， 有 50 ％ 概率转移到。 图 3 ． 1 马尔可夫过程的一个简单例子

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

给定一个马尔可夫过程， 我们就可以从某个状态出发， 根据它的状态转移矩阵生成一个状态序列 (episode)， 这个步骤也被叫做采样 (sampling)。 例如， 从 51 出发， 可以生成序列 s 1 + 2 + + 56 或序列 + 1 + + 53 + + 55 + + 56 等。 生成这些序列的概率和状态转移矩阵有关。

## 二、马尔可夫奖励过程

1.回报

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在马尔可夫过程的基础上加入奖励函数 r 和折扣因子 7， 就可以得到马尔可夫奖励过程 (Markov reward process)。 一个马尔可夫奖励过程由 〈 5， r,py 〉 构成， 各个组成元素的含义如下所示。 · 5 是有限状态的集合。 · 是状态转移矩阵。 · r 是奖励函数， 某个状态 5 的奖励 r@） 指转移到该状态时可以获得奖励的期望 · 7 是折扣因子 (discount factor)， 7 的取值范围为 [ 0， 1）。 引入折扣因子的理由为远期利益具有一定不确定性， 有时我们更希望能够尽快获得一些奖励， 所以我们需要对远期利益打一些折扣。 接近 1 的 7 更关注长期的累计奖励， 接近 0 的 7 更考虑短期奖励。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在一个马尔可夫奖励过程中， 从第时刻状态 St 开始， 直到终止状态时， 所有奖励的衰减之和称为回报 0 （Retu (n)， 公式如下 ． Gt =， + 1 “ 1 + 7， + 2 + 一， 乥 1 kRt4k

以LLM为例：每次生成就就是一次“状态转移”（已有全文即为状态），行动就是从词表（几万个词）中按概率选一个词（token），转移到下一个状态（新的全文）；奖励函数可以对某个状态（结果）或者某个过程进行奖励。

2.价值函数

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在马尔可夫奖励过程中， 一个状态的期望回报 （即从这个状态出发的未来累积奖励的期望） 被称为这个状态的价值 (value)。 所有状态的， 价值函数的输入为某个状态， 输出为这个状态的价值。 我们将价值函数写成 (value function) 价值就组成了价值函数 V(s) = = 的展开为 V(s) = E[Gt = 司 = E [ 咒 + 7 （咒 t + 1 + 7 咒 2 + · · = E [ 咒 + 衅 1 ] St = = E[Rt + 7 st + 1） = s] = R 5 在上式的最后一个等号中， 一方面， 即时奖励的期望正是奖励函数的输出， 即 E [ = s] = r(s); 力一方面， 等式中剩余部分 E [ 7 （St + 1） = 司可以根据从状态 5 出发的转移概率得到， 即可以得到上式就是马尔可夫奖励过程中非常有名的贝尔曼方程 (Bellman equation)， 对每一个状态都成立。 若一个马尔可夫奖励过程一共有 “ 个状 ·， }， 我们将所有状态的价值表示成一个列向量 V = 平@1），）， …， V(sn)]T, 同理， 将奖励函数写成一个列态， 即 5 = {S1,S2, 向量 R = [r@1）， r@2） 尸 ．， r(sn)]T0 于是我们可以将贝尔曼方程写成矩阵的形式： V = R + 7 V 瑕） 瑕 52） 我们可以直接根据矩阵运算求解， 得到以下解析解： P@小 1） ISI) 尹@1 卜 2） P@小 2） 尹@1） 尹@2 瓦） V 一 R + 7 户 V v = 一尸 R 尸@小 1） 尹 （卜 2 P 瓦） 以 52） 以上解析解的计算复杂度是 0 3）， 其中 “ 是状态个数， 因此这种方法只适用很小的马尔可夫奖励过程。 求解较大规模的马尔可夫奖励过程中的价值函数时， 可以使用动态规划 (dynamic programming) 算法、 蒙特卡洛方法 (Monte-Carlo method) 和时序差分 (temporal difference)， 这些方法将在之后的章节介绍。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在马尔可夫奖励过程中， 一个状态的期望回报 （即从这个状态出发的未来累积奖励的期望） 被称为这个状态的价值 (value)。 所有状态的， 价值函数的输入为某个状态， 输出为这个状态的价值。 我们将价值函数写成 (value function) 价值就组成了价值函数 V(s) = = 的展开为 V(s) = E[Gt = 司 = E [ 咒 + 7 （咒 t + 1 + 7 咒 2 + · · = E [ 咒 + 衅 1 ] St = = E[Rt + 7 st + 1） = s] = R 5 在上式的最后一个等号中， 一方面， 即时奖励的期望正是奖励函数的输出， 即 E [ = s] = r(s); 力一方面， 等式中剩余部分 E [ 7 （St + 1） = 司可以根据从状态 5 出发的转移概率得到， 即可以得到上式就是马尔可夫奖励过程中非常有名的贝尔曼方程 (Bellman equation)， 对每一个状态都成立。 若一个马尔可夫奖励过程一共有 “ 个状 ·， }， 我们将所有状态的价值表示成一个列向量 V = 平@1），）， …， V(sn)]T, 同理， 将奖励函数写成一个列态， 即 5 = {S1,S2, 向量 R = [r@1）， r@2） 尸 ．， r(sn)]T0 于是我们可以将贝尔曼方程写成矩阵的形式： V = R + 7 V 瑕） 瑕 52） 我们可以直接根据矩阵运算求解， 得到以下解析解： P@小 1） ISI) 尹@1 卜 2） P@小 2） 尹@1） 尹@2 瓦） V 一 R + 7 户 V v = 一尸 R 尸@小 1） 尹 （卜 2 P 瓦） 以 52） 以上解析解的计算复杂度是 0 3）， 其中 “ 是状态个数， 因此这种方法只适用很小的马尔可夫奖励过程。 求解较大规模的马尔可夫奖励过程中的价值函数时， 可以使用动态规划 (dynamic programming) 算法、 蒙特卡洛方法 (Monte-Carlo method) 和时序差分 (temporal difference)， 这些方法将在之后的章节介绍。

## 三、马尔可夫决策过程

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

智能体的策略 (Policy) 通常用字母丌表示。 策略丌@司 = 尸 （山 = aISt = 司是一个函数， 表示在输入状态 5 情况下采取动作 a 的概率。 当一 ^ 策略是确定性策略 (deterministic policy) 时， 它在每个状态时只输出一个确定性的动作， 即只有该动作的概率为 1， 其他动作的概率为 0； 当一个策略是随机性策略 (stochastic policy) 时， 它在每个状态时输出的是关于动作的概率分布， 然后根据该分布进行采样就可以得到一个动作。 在 MDP 中， 由于马尔可夫性质的存在， 策略只需要与当前状态有关， 不需要考虑历史状态。 回顾一下在 MRP 中的价值函数， 在 MDP 中也同样可以定义类似的价值函数。 但此时的价值函数与策略有关， 这意为着对于两个不同的策略来说， 它们在同一个状态下的价值也很可能是不同的。 这很好理解， 因为不同的策略会采取不同的动作， 从而之后会遇到不同的状态， 以及获得不同的奖励， 所以它们的累积奖励的期望也就不同， 即状态价值不同。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们用 V 丌@） 表示在 MDP 中基于策略丌的状态价值函数 （state-va lue function) 定义为从状态 s 出发循策略丌能获得的期望回报， 数学表达为 ． 0 （5） = E 丌 00 = 5 ]

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

不同于 MRP, 在 MDP 中， 由于动作的存在， 我们额外定义一个动作价值函数 (action-value function)。 我们用 Q 丌@a） 表示在 MDP 遵循策略丌时， 对当前状态 5 执行动作 a 得到的期望回报： QT(), a) = E 丌 00 = s, = 司状态价值函数和动亻乍价值函数之间的关系： 在使用策略丌中， 状态 5 的价值等于在该状态下基于策略丌采取所有动作的概率与相应的价值相乘再求和的结果： aeA 使用策略丌时， 状态 5 下采取动作 a 的价值等于即时奖励加上经过衰减后的所有可能的下一个状态的状态转移概率与相应的价值的乘积：

## 四、贝尔曼期望方程

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在贝尔曼方程中加上 " 期望 " 二字是为了与接下来的贝尔曼最优方程进行区分。 我们通过简单推导就可以分别得到两个价值函数的贝尔曼期望方程 (Bellman Expectation Equation) 0@ = ET@ + 气 st + 1） = 5 ] 乥丌 （小） r(s,a) + 7 的 5， a)V7(s') a€A s'€S = 小， a) + 的 a) 乥丌@时） Q 不 '， 的 s'eS

## 五、贝尔曼最优方程

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

强化学习的目标通常是找到一个策略， 使得智能体从初始状态出发能获得最多的期望回报。 我们首先定义策略之间的偏序关系： 当且仅当对于任意的状态 5 都有 V 丌@） 之 ''@， 记丌 > 丌 '。 于是在有限状态和动作集合的 MDP 中， 至少存在一个策略比其他所有策略都好或者至少存在一个策略不差于其他所有策略， 这个策略就是最优策略 (optimal policy)。 最优策略可能有很多个， 我们都将其表示为丌 *@）。 最优策略都有相同的状态价值函数， 我们称之为最优状态价值函数， 表示为： 同理， 我们定义最优动作价值函数 ． Vs e 5 Q*(), a) 一 maxQT(s,a), Ys e 5， a e 为了使 Q 丌@a） 最大， 我们需要在当前的状态动亻乍对@a） 之后都执行最优策略。 于是我们得到了最优状态价值函数和最优动作价值函数之间的关系： 这与在普通策略下的状态价值函数和动亻乍价值函数之间的关系是一样的。 另一方面， 最优状态亻介值是选择此时使最优动亻乍价值最大的那一个动作时的状态价值： aeA

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

根据铲 * （5） 和 Q 气 5， 的关系， 我们可以得到贝尔曼最优方程 (Bellman optimality eq uation） 5 笔 5 对 05

我们发现，和贝尔曼期望方程与策略pi挂钩不同的是，贝尔曼最优方程与策略pi无关，只与环境的状态转移特征有关。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
