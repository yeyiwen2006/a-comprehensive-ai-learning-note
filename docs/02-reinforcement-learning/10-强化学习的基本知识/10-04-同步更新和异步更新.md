---
title: "10.4 同步更新和异步更新"
source_docx: "第2部分 强化学习/10.强化学习的基本知识/10.4 同步更新和异步更新.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 10.4 同步更新和异步更新

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、同步更新

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在训练 ChatGPT、 凵 ama 等大模型时， 使用的是 PPO (Proximal POlicy Optimization) 或其变体。 在这个场景下， 几乎不存在异步更新。 流程 · 多个 GPU (workers) 并行地让 LLM 生成文本。 1 ． 2 ． 3 ． 4 ． 5 ． 数据收集 (Rollout)： 同步等待 (Barrier)： 样本）。 系统必须等待所有 GPU 都生成完指定数量的数据 （例如每张卡生成 64 个计算梯度 (Backward): 基于这批汇总的数据， 计算梯度。 梯度平均 (All-Reduce): 使用分布式训练技术 （如 DDP 一 Distributed Data Parallel)， 将所有显卡上的梯度取平均。 更新 (Update): 所有显卡同时更新模型参数。

## 二、异步更新

但有的场景下，几十个线程（Workers）在同时进行，它们的速度差异巨大。线程A计算完毕，如果等待线程B，将造成巨大的资源浪费。此时，往往直接将其送进中央服务器，收集到一定量的数据就直接更新模型，不等待线程B。如下面这些场景：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · AutoML 与神经架构搜索 (NAS) _ _ 最典型的场景这是该问题最原本的出处。 场景描述： AI 负责设计神经网络结构或调整超参数。 动作差异： 动作 A （快）： 动作 B （慢）： 训练一个只有 3 层的浅层网络 （耗时 30 秒）。 训练一个 ℃ 0 层的深层 ResNet （耗时 2 小时）。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · AI for Systems （数据库 / 编译器 / 系统优化） 这是目前工业界应用 RL 的热点领域， 动作时长的方差极大。 场景 A: 数据库参数调优 (Knob Tuning) 动作： 修改数据库配置并运行基准测试 (Benchmark)0 差异： 修改 work mem 可能只需要重启 （快）； 修改 index 策略可能需要重建索引 （极慢， 几十分钟）。 场景 B： 编译器优化 (Compiler Optimization) 动作： 差异： 决定对代码块应用哪种优化 Passo “ 死代码消除 " （瞬时）； " 多面体优化 " 或 “ 循环展开 " （可能导致编译时间激增）。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · AI for Systems （数据库 / 编译器 / 系统优化） 这是目前工业界应用 RL 的热点领域， 动作时长的方差极大。 场景 A: 数据库参数调优 (Knob Tuning) 动作： 修改数据库配置并运行基准测试 (Benchmark)0 差异： 修改 work mem 可能只需要重启 （快）； 修改 index 策略可能需要重建索引 （极慢， 几十分钟）。 场景 B： 编译器优化 (Compiler Optimization) 动作： 差异： 决定对代码块应用哪种优化 Passo “ 死代码消除 " （瞬时）； " 多面体优化 " 或 “ 循环展开 " （可能导致编译时间激增）。

A3C算法也是异步更新，但后来研究人员发现不如同步更新的A2C算法。

## 三、异步更新的时间加权

1.异步更新遇到的问题

异步更新主要运用于不同动作耗时相差巨大的场景。在标准的分布式强化学习框架中，多个“执行者”（actors）会同时生成不同的代码方案并运行它们，然后将结果（代码、执行结果、奖励）发送给一个“学习者”（learner）来更新模型。在实际场景中，这会产生问题：如在 10 分钟（600秒）内，Actor可以执行120次动作A，只能执行2次动作 B。“学习者（Learner）”收到了120个关于A的梯度更新信号，却只收到了2个关于 B 的信号。模型参数被A的梯度“淹没”了，导致智能体误以为A是最佳策略，从而陷入局部最优。

以上文提到的两个场景为例。在用AI确定神经网络架构时，如果不进行额外调整，AI会疯狂地生成浅层网络，因为能在短时间内刷出大量的梯度更新。虽然深层网络效果好，但因为反馈太慢，被AI“遗忘”了。类似地，在进行System优化时，也会更倾向于用时较短的动作。

从数学上可以这样解释：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

梯度更新公式通常为： VJ(O) = log 砌@5） · a 〗 其中砌是策略分布， A(s,a) 是优势函数。 注意： 这里的期望是假设我们按照策略砌的概率分布进行采样的。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在异步分布式系统中， 数据的生成速率与执行时间 At 成反比。 实际采样概率 Psample (a) 不再仅仅等于策略概率 T(a)， 而是被时间扭曲了： s “ 7 孕 OC At(a) 这意味着， 耗时短的动作被采样的概率被人为放大了。 如果我们直接用收集到的数据进行更新， 我们实际上是在优化一个被时间扭曲的目标函数， 而不是原本的目标函数。

事实上，使用经验回放池的Off-Policy算法（DQN, SAC, DDPG）理论上也是不同步的，Worker 只管往池子里扔数据（可以是异步的），Learner从池子里随机抽取数据来训练。虽然Worker 是异步的（快动作扔得快），导致池子里“快动作”的样本变多，但由于 Learner 是随机均匀采样，如果池子足够大且混合得足够好，偏差会被稀释，但不完全消除。当动作速度差异不明显时，我们一般会忽略这种误差。

2.解决方案：时间加权

既然由于时间短导致采样次数多，那就在每次更新时，根据耗时长度赋予权重。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

作者引入了时间项 Atk （动作的执行时长） 作为权重乘数。 修正后的梯度估计为： Atk · Vo log （举 k) · （，）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

_ 乥 psample@ · (At(a) · v log 丌@ · · At(a) · log 丌@ · A(a) At(a) _ 乥丌@ · v log 丌@ · A(a) 结论： 通过乘以 At(a)， 分子中的时间权重正好抵消了分母中因为采样频率带来的 1/At(a) 因子。 这样， 梯度的期望值重新回归到了无偏的策略分布 T(a) 上， 从而实现了对长耗时动作的 " 公平考虑 "。

举两个实际例子：

（1）量化交易

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在金融领域， RL 被用来生成交易策略。 场景描述： Ag e nt 决定是开仓、 平仓还是持有。 动作差异： 高频策略 (Scalping): 持仓几秒钟就平仓， 赚取微小价差。 交易极其频繁， 数据点极其密集。 趋势策略 (Tre n d Following): 持仓几天甚至几周， 等待大行情。 后果： 在异步框架下， 高频交易产生的样本量是趋势交易的成千上万倍。 Learner 会被高频噪音淹没， 导致模型认为 “ 只有频繁交易才能赚钱 "， 从而错过长线的大鱼。 修正： 必须给长持仓时间的交易赋予极大的权重 (At)， 告诉模型 " 这一单虽然等了一周， 但赚了 ℃ ％， 值得你好好学 “。

（2）药物发现与分子模拟

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

场景描述： Ag e nt 生成分子结构， 并调用物理模拟器来验证其性质。 动作差异： 低精度估算： 使用经验公式或粗糙的代理模型 （几秒）。 高精度模拟： 使用 D FT （密度泛函理论） 或全原子动力学模拟 （几小时甚至几天）。 后果： AI 会倾向于生成那些 “ 容易被低精度模拟器计算 " 的分子， 而避开那些结构复杂、 需要高精度算力验证的潜在神药。

注：为了数值稳定性，通常会将△t进行截断或归一化，防止极长任务导致梯度爆炸。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
