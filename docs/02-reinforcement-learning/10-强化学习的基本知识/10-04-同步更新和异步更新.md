---
title: "10.4 同步更新和异步更新"
source_docx: "第2部分 强化学习/10.强化学习的基本知识/10.4 同步更新和异步更新.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 10.4 同步更新和异步更新


## 一、同步更新

在训练 ChatGPT、Llama 等大模型时，使用的是 PPO（Proximal Policy Optimization）或其变体。在这个场景下，几乎不存在异步更新。

流程：

1. 数据收集（Rollout）：多个 GPU（Workers）并行地让 LLM 生成文本。
2. 同步等待（Barrier）：系统必须等待所有 GPU 都生成完指定数量的数据（例如每张卡生成 64 个样本）。
3. 计算梯度（Backward）：基于这批汇总的数据，计算梯度。
4. 梯度平均（All-Reduce）：使用分布式训练技术（如 DDP - Distributed Data Parallel），将所有显卡上的梯度取平均。
5. 更新（Update）：所有显卡同时更新模型参数。

## 二、异步更新

但有的场景下，几十个线程（Workers）在同时进行，它们的速度差异巨大。线程A计算完毕，如果等待线程B，将造成巨大的资源浪费。此时，往往直接将其送进中央服务器，收集到一定量的数据就直接更新模型，不等待线程B。如下面这些场景：

1. AutoML 与神经架构搜索（NAS）——最典型的场景

这是该问题最原本的出处。

- 场景描述：AI 负责设计神经网络结构或调整超参数。
- 动作 A（快）：训练一个只有 3 层的浅层网络（耗时 30 秒）。
- 动作 B（慢）：训练一个 100 层的深层 ResNet（耗时 2 小时）。

2. AI for Systems（数据库/编译器/系统优化）

这是目前工业界应用 RL 的热点领域，动作时长的方差极大。

- 场景 A：数据库参数调优（Knob Tuning）
- 动作：修改数据库配置并运行基准测试（Benchmark）。
- 差异：修改 `work_mem` 可能只需要重启（快）；修改 `index` 策略可能需要重建索引（极慢，几十分钟）。
- 场景 B：编译器优化（Compiler Optimization）
- 动作：决定对代码块应用哪种优化 Pass。
- 差异：“死代码消除”（瞬时）；“多面体优化”或“循环展开”（可能导致编译时间激增）。

A3C算法也是异步更新，但后来研究人员发现不如同步更新的A2C算法。

## 三、异步更新的时间加权

1.异步更新遇到的问题

异步更新主要运用于不同动作耗时相差巨大的场景。在标准的分布式强化学习框架中，多个“执行者”（actors）会同时生成不同的代码方案并运行它们，然后将结果（代码、执行结果、奖励）发送给一个“学习者”（learner）来更新模型。在实际场景中，这会产生问题：如在 10 分钟（600秒）内，Actor可以执行120次动作A，只能执行2次动作 B。“学习者（Learner）”收到了120个关于A的梯度更新信号，却只收到了2个关于 B 的信号。模型参数被A的梯度“淹没”了，导致智能体误以为A是最佳策略，从而陷入局部最优。

以上文提到的两个场景为例。在用AI确定神经网络架构时，如果不进行额外调整，AI会疯狂地生成浅层网络，因为能在短时间内刷出大量的梯度更新。虽然深层网络效果好，但因为反馈太慢，被AI“遗忘”了。类似地，在进行System优化时，也会更倾向于用时较短的动作。

从数学上可以这样解释：

> [图片内容待重建：img-18994d4e4101-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-18994d4e4101-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
事实上，使用经验回放池的Off-Policy算法（DQN, SAC, DDPG）理论上也是不同步的，Worker 只管往池子里扔数据（可以是异步的），Learner从池子里随机抽取数据来训练。虽然Worker 是异步的（快动作扔得快），导致池子里“快动作”的样本变多，但由于 Learner 是随机均匀采样，如果池子足够大且混合得足够好，偏差会被稀释，但不完全消除。当动作速度差异不明显时，我们一般会忽略这种误差。

2.解决方案：时间加权

既然由于时间短导致采样次数多，那就在每次更新时，根据耗时长度赋予权重。

> [图片内容待重建：img-18994d4e4101-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-18994d4e4101-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
举两个实际例子：

（1）量化交易

> [图片内容待重建：img-18994d4e4101-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
（2）药物发现与分子模拟

> [图片内容待重建：img-18994d4e4101-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
注：为了数值稳定性，通常会将△t进行截断或归一化，防止极长任务导致梯度爆炸。

## 参考文献

暂无已核验参考文献。
