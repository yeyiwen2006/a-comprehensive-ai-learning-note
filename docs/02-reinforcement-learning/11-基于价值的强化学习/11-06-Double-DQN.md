---
title: "11.6 Double DQN"
source_docx: "第2部分 强化学习/11.基于价值的强化学习/11.6 Double DQN.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 11.6 Double DQN

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、DQN对Q值的过高估计

DQN普遍存在过高估计Q值的问题。这是因为其更新目标为：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

=r+-ymaxQ(s',a';w

神经网络的输出Q(s',a')总是包含噪声（估计误差）。由于取max，往往最后留下的是被高估的结果，这种高估会随着贝尔曼方程的迭代不断向回传播，导致整个系统的Q值普遍虚高。假设误差epsilon服从均匀分布，我们可以推导出估计值的最大值与真实值最大值之间的期望偏差：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了让数学推导可行， 文中做了一些简化的假设， 但这并不影响结论的普适性：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

真实价值相等： 假设在状态 5 下， 所有动作 a 的真实期望回报 Q*(s,a) 都是一样的， 记为为什么要这样假设？ 如果真实价值本身有高有低， 那么 max 操作选到最大值是理所应当的。 我们现在的目的是证明： 即使所有动作真实价值都一样， 仅仅因为有 “ 噪声 "， max 操作也会导致评估结果偏高。 噪声分布： 神经网络的估算误差 = Qw-@， a) 一 V 气 5） 服从区间 [ 一 1， 1 ] 上的均匀分布 (Uniform Distribution)0 这意味着误差是随机的， 且均值为 0 （无偏）。 即 E[ea] = 0。 独立性： 不同动作的估算误差是相互独立的。 动作数量 · 动作空间的大小为 mo

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们的目标是计算： 在这种情况下， 神经网络估算出的最大值 maxa Qw- （5， a) 比真实值气 5） 高出多少？ 即计算期望： E [maxa 6。 ]。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

0 1 < 一 1 > 1

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

现在的关键是： 我们需要求 m 个误差中的最大值 (maxa） 的分布。 逻辑： 如果 m 个数的最大值都要小于那么这 m 个数每一个都必须小于计算： 由于它们是独立的， 联合概率等于各自概率的乘积。 P(max _ ll P()a = 2

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

最后一步是求这个 " 最大误差 " 的数学期望 (Expectation)0 其中是概率密度函数 (PDF), 即 CDF 的导数。 文中给出的积分公式是： 期望的定义是 E[X] 一 · 一一尸 （max ea 孓刁 d 这是 PDF

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

mc 一 1 m + 1

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

E [maxQ 一 （一 1

如果只有一个动作可以选择，没有max操作，那么随着每一次采样：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设你只有一个动作 alo 真实情况： 它的价值是 0。 · 噪声： 服从 [ 一 1， 1 ] 的均匀分布。 这意味着你有时候会估成 0 · 8 （高估）， 有时候会估成一 0 · 9 （低估）。 虽然单次看有误差， 但如果你试了很多次， 求平均： 平均误差 = 结论： 因为噪声是对称的 (Symmetric)， 正向误差和负向误差会相互抵消。 所以平均来看， 你没有过高估计， 也没有过低估计。 这叫做无偏估计 (Unbiased Estimation)0

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

现在你有两个动作 al 和 a20 真实情况： 两者的真实价值都是 0。 规则： Q-Learning 的 max 操作会强迫你挑选估值最高的那个动作。 让我们模拟几次 " 挑选 " 的过程： 第 1 次尝试： · al 噪声为一 0 · 5 （低估）。 · a2 噪声为 + 0 · 8 （高估）。 Max 操作： 系统选了 + 0 · 8。 结果： 过高估计 + 0 · 8。 第 2 次尝试： · al 噪声为 + 0 · 3 （高估）。 · a2 噪声为一 0 · 9 （低估）。 Max 操作： 系统选了 + 0 · 3。 结果： 过高估计 + 0 · 3。

动作越多，过高估计的偏差就越接近噪声的上界（这里是1）。因为动作越多，越容易每次都“运气好”碰上一个正向误差特别大的噪声，而max操作一定会选中它。

## 二、Double DQN

前面我们提过DQN高估Q值的问题。归根到底，这种误差来源于我们在计算目标值时，使用同一个网络（目标网络）来做两件事：选择哪个动作是最好的；评估这个动作的价值是多少。这样，每次动作都会选择高估的样本（因为高估所以选择），而每一次更新都直接运用对选择动作的价值估计（因为选择所以用于更新），导致每一次更新都写入了被高估的值。

Double DQN打破了这个链条：使用训练网络w来决定哪个动作最好，因为训练网络更新最及时，能反映最新的策略；使用目标网络w-来计算选定动作的Q值。Double DQN的更新目标变为：

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

刀刀 QN 由训练网络选择动作

如果训练网络w因为噪声错误地认为某个动作最好（高估了），通过将其放入目标网络w-进行评估，由于两个网络的参数不同，w-极有可能不会同时也高估这个动作。这样就抵消了大部分的过高估计偏差。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 前面我们提过DQN高估Q值的问题。归根到底，这种误差来源于我们在计算目标值时，使用同一个网络（目标网络）来做两件事：选择哪个动作是最好的；评估这个动作的价值是多少。这样，每次动作都会选择高估的样本（因为高估所以选择），而每一次更新都直接运用对选择动作的价值估计（因为选择所以用于更新），导致每一次更新都写入了被高估的值。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
