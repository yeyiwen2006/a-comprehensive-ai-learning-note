---
title: "11.2 动态规划算法"
source_docx: "第2部分 强化学习/11.基于价值的强化学习/11.2 动态规划算法.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 11.2 动态规划算法

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

动态规划（dynamic programming）是程序设计算法中非常重要的内容，能够高效解决一些经典问题，例如背包问题和最短路径规划。动态规划的基本思想是将待求解问题分解成若干个子问题，先求解子问题，然后从这些子问题的解得到目标问题的解。动态规划会保存已解决的子问题的答案，在求解目标问题的过程中，需要这些子问题答案时就可以直接利用，避免重复计算。本章介绍如何用动态规划的思想来求解在马尔可夫决策过程中的最优策略。

基于动态规划的强化学习算法可以在已知环境状态转移和奖励对的情况下求解策略，主要有两种：一是策略迭代（policy iteration），二是价值迭代（value iteration）。其中，策略迭代由两部分组成：策略评估（policy evaluation）和策略提升（policy improvement）。具体来说，策略迭代中的策略评估使用贝尔曼期望方程来得到一个策略的状态价值函数，这是一个动态规划的过程；而价值迭代直接使用贝尔曼最优方程来进行动态规划，得到最终的最优状态价值。

不同于前面介绍的蒙特卡洛方法和后面介绍的时序差分算法，基于动态规划的这两种强化学习算法要求事先知道环境的状态转移函数和奖励函数，也就是需要知道整个马尔可夫决策过程。在这样一个白盒环境中，不需要通过智能体和环境的大量交互来学习，可以直接用动态规划求解状态价值函数。但是，现实中的白盒环境很少，这也是动态规划算法的局限之处，我们无法将其运用到很多实际场景中。另外，策略迭代和价值迭代通常只适用于有限马尔可夫决策过程，即状态空间和动作空间是离散且有限的。

1.策略迭代（Sarsa的基础）

（1）策略评估

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

策略评估这一过程用来计算一个策略的状态价值函数。 回顾一下之前学习的贝尔曼期望方程： a 巳 4 s'€-S 其中， 丌@司是策略丌在状态 5 下采取动作 a 的概率。 可以看到， 当矩道奖励函数和状态转移函数时， 我们可以根据下一个状态的价值来计算当前状态的价值。 因此， 根据动态规划的思想， 可以扌巴计算下一个可能状态的价值当成一个子问题， 把计算当前状态的价值看作当前问题。 在得知子问题的解后， 就可以求解当前问题。 更一般的， 考虑所有的状态， 就变成了用上一轮的状态价值函数来计算当前这一轮的状态价值函数， 即 aeA s'€-S 我们可以选定任意初始值 0。 根据贝尔曼期望方程， 可以得知為 = V 丌是以上更新公式的一个不动点 (fixed point)。 事实上， 可以证明当时， 序列 { 身会收敛到 '， 所以可以据此来计算得到一个策略的状态价值函数。 可以看到， 由于需要不断做贝尔曼期望方程迭代， 策略评估其实会耗费很大的计算代价。 在实际的实现过程中， 如果某一轮 m “ 5 “ 1 （5） 一气的值非常小， 可以提前结束策略评估。 这样做可以提升效率， 并且得到的价值也非常接近真实的价值。

（2）策略提升

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

使用策略评估计算得到当前策略的状态价值函数之后， 我们可以据此来改进该策略。 假设此时对于策略丌， 我们已经矩道其价值丌， 也就是知道了在策略丌下从每一个状态 5 出发最终得到的期望回报。 我们要如何改变策略来获得在状态 5 下更高的期望回报呢？ 假设智能体在状态 5 下采取动作 a， 之后的动亻乍依旧遵循策略丌， 此时得到的期望回报其实就是动作价值 Q 丌@a)o 如果我们有 Q 丌@a) > VT(s), 则说明在状态 5 下采取动作 a 会比原来的策略丌@司得到更高的期望回报。 以上假设只是针对一个状态， 现在假设存在一个确定性策略丌 ／， 在任意一个状态 5 下， 都满足于是在任意状态 5 下， 我们有这便是策略提升定理 (policy improvement theorem)。 于是我们可以直接贪心地在每一个状态选择动作价值最大的动作， 也就是 = arg max{r(), a) + 7 乥 P （的 5， 俨时） } 我们发现构造的贪心策略丌 ／ 满足策略提升定理的条件， 所以策略丌 ' 能够比策略丌更好或者至少与其一样好。 这个根据贪心氵去选取动亻乍从而得到新的策略的过程称为策略提升。 当策略提升之后得到的策略丌 ' 和之前的策略丌一样时， 说明策略迭代达到了收敛， 此时丌和丌 ／ 就是最优策略。 策略提升定理的证明通过以下推导过程可以证明， 使用上述提升公式得到的新策略丌 ' 在每个状态的价值不低于原策略丌在该状态的价值。 = E 丌，@亡 + 7 气 St 十 1） lSt = s] E 丌，@ + IQ 丌 （十 1， 丌气 st 十 1 st = s] = [Rt + 7 月衅 1 + 72 气 St + 2 月 St = 司 [Rt + 7 月衅 1 + 72 咒 “ 2 + 73 ' （St + 3 月 St E 丌，@亡 + 7 1 + 7 咒衅 2 + 7 咒 t + 3 + 一 · lSt 可以看到， 推导过程中的每一个时间步都用到局部动作价值优势丌 （St + 1） QT(St+1, 丌气 St 十 1)）， 累积到无穷步或者终止状态时， 我们就， ` 亻曰

（3）策略迭代

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

总体来说， 策略迭代算法的过程如下： 对当前的策略进行策略评估， 得到其状态价值函数， 然后根据该状态价值函数进行策略提升以得到一个更好的新策略， 接着继续评估新策略、 提升策略 ． “ ． ． 直至最后收敛到最优策略 （收敛性证明参见 4 ． 7 节） 页略泮竹 0 策升策 1 策略提升 2 略竹策幣升丌 0 ． 一分丌， 一分丌一一分丌一一分丌 ． 一分结合策略评估和策略提升， 我们得到以下策略迭代算法： · 随机初始化策略丌@） 和价值函数犷@ · while A > 0 do （策略评估循环） A 0 对于每一个状态 s e S: · end while · 对于每一个状态 s e S: · 若丌司 d = 丌， 则停止算法并返回和丌， 否则转到策略评估循环

和Sarsa的关系：

策略评估时，假设按当前策略，在s状态下采取动作a的价值为：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

既这里用的是 Q ' 的丌 （的） 者 E 丌 （的的 Q 时@）， 而不是 maxo 它老老实实计算当前策略的表现。

这里Π(s’)表示假设下一步仍然按同策略（更新前的策略）走时的Q.

然后策略改进时再贪婪更新。

而在Sarsa中，我们不知道P(s’|s,a)的分布，因此无法求期望，只能用“采样+移动平均”：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Sarsa 是 State-Action-Reward-State-Action 的缩写。 它的更新公式如下： 更新公式： Q(s,a) 艹 Q(s,a) +a + IQ @， 的一 Q(s,a) TD Target 关系揭秘： Sarsa 的目标月 + 7Q （的 (') 使用的是智能体实际采取的下一个动作 a'o 这意味着 Sarsa 在估算的是当前策略丌的价值 QT， 而不是最优价值 Q*O 这对应了 PI 中的 “ 策略评估 " 步骤。 随着训练进行， 我们通常会逐渐减小 6 （让策略变得越来越贪婪）。 这个过程让策略不断变好。 这对应了 PI 中的 “ 策略改进 " 步骤。

4.价值迭代（Q-Learning的基础）

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

确切来说， 价值迭代可以看成一种动态规划过程， 它利用的是贝尔曼最优方程： aeÄ 5 ℃ S 将其写成迭亻弋更新的方式为 aeÄ 5 ℃ S 亻介值迭代便是按照以上更新方式进行的。 等到 V ' 1 和 V 相同时， 它就是贝尔曼最优方程的不动点， 此时对应着最优状态价值函数 V 能然后我们利用丌 （5） = argmaxa{r(s， a) + 1 乥 8， 的 5， a） “ 1 （5 '） }, 从中恢复出最优策略即可。 价值迭代算法流程如下： · 随机初始化以 s） · while > do 对于每一个状态 s G S: V(s) “ 小， a) + 7 乥酽 P(s'Is， a） 的 · end while · 返回一个确定性策略丌@） = argmaxa{r(), a) + 7 乥， P 时卜， a） 的 }

和Q-Learning的关系：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在已知环境模型 （转移概率和奖励的情况下， VI 通过迭代直接计算最优动作价值函数 Q*(s,a)0 更新公式 （利用期望）：

而Q-Learning并不知道这个概率分布，只能“采样+求移动平均”：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Q(s,a) “ Q(s,a) + a R+1maxQ(s',a') 一 Q(s,a) TD Target

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
