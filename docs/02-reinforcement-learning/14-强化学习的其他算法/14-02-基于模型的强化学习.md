---
title: "14.2 基于模型的强化学习"
source_docx: "第2部分 强化学习/14.强化学习的其他算法/14.2 基于模型的强化学习.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 14.2 基于模型的强化学习

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、模型预测控制（Model Predictive Control, MPC）

以下棋为例。在无模型（Model-Free）的算法中，我们看到当前棋局，直接凭经验落子（策略网络直接输出动作）。而MPC则是：看到当前棋局，我在脑海里（环境模型）推演：如果我下A处，对手可能下B，那我再下C……推演H步之后，发现局势大好，于是我决定现在的第一步就走A。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

MPC 的核心是求解一个优化问题。 在时刻给定当前状态， 我们要寻找一个长度为丑的动作序一 1 }， 使得未来的累积奖励最大化： 列 at:t+H + 一 at+H t+H—1 at:t+H = max at•.t+H 其中， + 1 是由环境模型 5。 a，） 预测出来的。 关键点 (Receding Horizon ControI)： 虽然我们计算出了一串最优序列 {@， + 1， · · ． }， 但在实际执行时， 我们只执行第一个动作可。 等到下一时刻 + 1, 我们观察到了真实的 + 1， 再重新进行一轮推演。 这样可以不断修正模型预测与现实的误差。

由于动作空间可能是连续且高维的，直接用数学公式解出上面的argmax很难。为此，我们可以采取如下操作：

### （一）打靶法

简单粗暴，随机生成1000条动作轨迹，扔进模型里跑一下，看哪条轨迹得分最高，就选哪条。在高维空间中，随机乱猜很难猜中最优解，效率极低。

### （二）交叉熵方法（Cross-Entropy Method, CEM）

1.核心步骤

（1）初始化：假设动作服从某个分布（比如正态分布）。

（2）采样：从这个分布里采样一批动作序列。

（3）评估：用模型评估这些序列的得分。

（4）筛选精英：挑出得分最高的那些序列（比如前10%）。

（5）进化：用这些精英序列的统计特征（均值和方差）来更新分布的参数。让分布的中心向高分区域移动，并缩小方差（更确信）。

（6）迭代：重复上述过程，直到分布收敛。

2.和交叉熵的关系

CEM是一种进化算法，它从当前分布中生成一堆样本，挑出表现最好的精英样本，它认为：“这前10%的精英样本就是真理，就是正确答案。”于是它倾向于更新分布参数（比如高斯分布的u和sigma），使得这个分布生成这些“精英样本”的概率最大，即我们让当前的分布去拟合精英样本的分布。衡量这两个分布差异的指标就是交叉熵，推导如下：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设我们想求一个最优的策略分布参数 O, 使得在该分布下采样得到的累积奖励 (@) 最大。 但在 CEM 的每一步迭代中， 我们的目标变得更具体： 我们要找到一个新的参数 O 使得它生成的分布 netV， （“） 尽可能接近理想的精英分布。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Ptdeal）： 这是我们心中理想的分布 （即只在精英样本上概率为 1， 其他为 0 的经验分布）。 Q@;： 我们要优化的参数化分布 （比如高斯分布）。 衡量尸和 Q 差异的标准方法是 KL 散度： 刀 “ (Pideal 目 Q) 一乥 “） log

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

交叉熵的负数熵 H(P)

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

因此， 最小化 KL 散度， 等价于最小化交叉熵 H(), Q): arg min DKL(Pdea1 目 Q) `、 arg min

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这也等价于最大化对数似然 (Maximum Likelihood Estimation, MLE)： 乥 log Q@; 0) arg max eElites

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

既然我们要最大化精英样本在当前分布下的对数似然： log Q@;。 假设我们的分布 Q 是一个高斯分布， 2）。 高斯分布的极大似然估计 (MLE) 有闭式解 (Analytical Solution)： · 最优的均值就是样本的算术平均值。 · 最优的方差 “ 就是样本的方差。

## 二、PETS算法（Probabilistic Ensembles with Trajectory Sampling）（UC Berkeley，NeurIPS 2018）

模型是不完美的。如果环境模型预测错了，MPC就会根据错误的信息规划出错误的策略。PETS引入了概率（Probabilistic）和集成（Ensembles）来量化这种风险。

### （一）两种不确定性

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

偶然不确定性 (Aleatoric Uncertainty): * 含又： 世界本身就是随机的。 比如扔硬币， 你模型再好也无法确定是正面还是反面， 只能预测 50 ％ 的概率。 PE 丆 S 的解法： 神经网络不输出单一的值， 而是输出一个高斯分布 （均值和方差）。 损失函数： 最大化似然估计 (Max Likelihood)， 即最小化负对数似然损失： 对于高斯分布， 这等价于： 2 St + 1。 一 ß0(St, at + log ao(st, IOSS 2 ()t， （直观解释： 预测误差要小， 同时当误差大时， 模型应该预测出更大的方差。 来承认自己不确

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

偶然不确定性 (Aleatoric Uncertainty): * 含又： 世界本身就是随机的。 比如扔硬币， 你模型再好也无法确定是正面还是反面， 只能预测 50 ％ 的概率。 PE 丆 S 的解法： 神经网络不输出单一的值， 而是输出一个高斯分布 （均值和方差）。 损失函数： 最大化似然估计 (Max Likelihood)， 即最小化负对数似然损失： 对于高斯分布， 这等价于： 2 St + 1。 一 ß0(St, at + log ao(st, IOSS 2 ()t， （直观解释： 预测误差要小， 同时当误差大时， 模型应该预测出更大的方差。 来承认自己不确

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

认知不确定性 (Epistemic Uncertainty): 含又： 智能体 “ 无知 " 导致的。 比如某个区域智能体从来没去过， 它对此一无所知。 PE 丆 S 的解法： 集成学习 (Ensemble)0 训练一组神经网络 （比如 5 个 ), 每个网络的参数初始化不同， 训练数据采样也略有不同。 应用： 对于一个从未见过的状态， 这 5 个网络预测的结果会天差地别 （方差大）； 对于熟悉的状态， 大家的预测会很一致。

### （二）PETS的策略

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一阶段： 建立 “ 世界观 " （模型训练） 收集数据： 智能体在真实环境里胡乱玩 （或者基于之前的策略玩 ), 收集一堆真实数据 D = 1 ． 训练集成模型： 初始化 5 个神经网络 1， 2 ． 每个网络都输出高斯分布伊， 列。 使用训练这 5 个网络， 每个网络看到的训练数据略有不同 (Bootstrap 采样 ), 导致它们参数不同。

后续第二、三阶段为利用模型的输出进行前向计算。若干次前向计算后，我们会用收集到的数据训练环境模型，损失函数见后面（三）部分所示。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第二阶段： 基于模型的规划 (MPC/CEM) 现在， 智能体处在状态 5。 它要决定下一步怎么做。 1 ． 2 ． CEM 初始化： 初始化一个动作分布 （比如均值砌 “ = 0， 方差很大的正态分布）。 CEM 迭代 （开始打靶）： 步骤 2 · 1 采样动作： 从动作分布中采样 N 个候选动作序列 （比如 1， 2， 步骤 2 · 2 评估 (Propagation) _ _ 最关键的一步： 对于每一个候选序列翡 （例如 " 向左 5 步 "）： 生成个粒子 （例如 20 个）， 初始状态都是当前的 sto

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设我们有 5 个环境模型 （MI 亻 2 3， M4， M5）。 现在的任务是： 评估 * * " 向左走 " * * 这个动作序列好不好。 P ETS 不会只派 1 个侦察兵去探路， 而是派 20 个侦察兵 （粒子） 同时从起点 50 出发， 都执行 " 向左走 " 的指令。 分配规则 (Trajectory Sampling, (S)： 侦察兵 1， 2， 3， 4 号听从模型 MI 的指挥 （由 MI 预测下一步）。 侦察兵 5， 6， 乙 8 号听从模型 2 的指挥。 侦察兵 1 乙 18， 19， 20 号 + 听从模型厶的指挥。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 ． · 让这 20 个粒子分别依据分配到的模型和动作序列进行 H 步推演。 · 计算这 20 个粒子推演得到的奖励总和的均值， 作为动作序列翡的得分。 步骤 2 · 3 筛选精英： 对 N 个动作序列的得分排序， 选出前 ℃ ％ （精英）。 步骤 2 · 4 更新分布： 用精英的均值和方差更新动作分布。 重复上述步骤 27 一 24 若干次 （比如 5 次丿。 决策： CEM 迭代结束后， 取最终分布的均值作为最优动作 a*。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第三阶段： 执行与循环执行： 智能体在真实环境执行动作 a* 的第一步。 1 ． 观察： 得到真实的下一个状态 + 1。 2 ． 循环： 重新开始规划 （回到第二阶段 ), 或者每隔一段时间把新数据加入 D 重新训练模型 （回 3 ． 到第一阶段）。

第二、三阶段是前向计算，也就是“走一步”。走一定步数之后，模型会用这些步数采集到的数据训练环境模型。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

结论： 正是通过这种 * * " 把鸡蛋放在 5 个篮子里， 看最后剩多少鸡蛋 " * * 的机制， PETS 隐式地惩罚了那些会导致模型产生分歧的动作。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当你问 " 向左走怎么样？ " 时， PETS 实际上是在做一次蒙特卡洛模拟 (Monte Carlo Simulation)： 它模拟了 20 个平行宇宙， 有的宇宙由模型 1 主宰， 有的由模型 2 主宰。 只有当动作在所有平行宇宙里都能拿到不错的平均分时， PETS 才会采纳这个动作。

也就是说，对那些总体得分较高且各环境模型之间没有较大分歧的动作，模型才会给它们较高的A_i值，使其被选进前10％“精英”中，被采用进真实动作分布。这种策略对一些容错率低的环境尤其有用。

### （三）环境模型（M1-M5）的损失函数

本质上，这是在强制用高斯分布拟合的前提下，取预测正确概率的最大似然。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 数学推导假设神经网络预测的下一个状态服从高斯分布： p 雨 =， 其中是真实标签 （下一个状态）， 是输入 （当前状态和动作）。 我们要最大化似然概率 p）， 等价于最小化负对数似然 (Negative Log-LikeIihood, NLL)O 对于高斯分布， 其概率密度函数 (PDF) 为： p 包 = 1 exp 2 丌 02 一 2 2 伊 2 对其取负对数 （一 log)， 忽略常数项 log(2T), 得到的损失函数 £ 为： 包一脚 （翊 2 + log） 2

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一项： 加权预测误差 (weighted MSE) 包一 （翊 2 2 国含义： 这是归一化的均方误差。 作用： 如果模型预测的方差 2 很大 （说明模型认为这里噪声很大）， 那么即使预测均值亻扁离了真实值 （分子很大）， 这一项的整体损失也会被分母的。 2 缩小。 潜在风险： 如果只有这一项， 模型会倾向于通过输出无穷大的方差 （伊 2 +） 来让损失函数趋近于 0。 这就像学生考试时说 " 这道题我不确定， 答案可能是从负无穷到正无穷 "， 虽然技术上覆盖了正确答案， 但亳无意义。

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第二项： 不确定性惩罚 (Uncertainty penalty) log） 含义： 对方差本身的惩罚 （正则化项）。 作用： 这一项阻止了模型 “ 作弊 "。 它迫使模型尽可能输出小的方差。 如果模型为了降低第一项而盲目增大偏第二项就会急剧增大， 导致总 Loss 变大。 结果： 模型必须在 “ 预测准确 （减小分子） " 和 " 保持自信 （减小 log 伊） " 之间找到平衡。

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在数据本身就很嘈杂的区域 （高偶然不确定性）： 无论模型怎么调整预测误差一 2 始终很大。 为了由第一项造成的损失最小化， 模型不得不增大伊 2 （以增大分母）。 虽然这会增加第二项 log 的惩罚， 但这是值得的。 结论： 模型学会了由衷地承认： “ 这里真的很乱， 我预测不准， 方差很大。 " 在数据非常确定、 干净的区域 （低偶然不确定性）： 模型可以把预测得很准， 分子一 2 接近 0。 此时， 模型会倾向于减小伊 （因为第一项已经很小了， 减小可以降低第二项 log 的损失）。 结论： 模型学会了说： “ 这里我很确定， 方差很小。 "

## 三、MBPO算法（Model-Based Policy Optimization）

MBPO结合了Model-Based的高数据利用效率和Model-Free（如SAC）的高性能。

### （一）基于模型方法的复合误差问题

纯粹的 Model-Based 方法（如PETS）完全依赖模型推演。如果模型第一步预测偏了 1%，第二步就是基于偏了的数据再预测，误差会指数级累积。推演越长，结果越不可信。

### （二）MBPO的创新：分支推演

传统思路是从s_0开始一直用模型推演到底，但MBPO并不如此。它的步骤如下：

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 5 ． 智能体在真实环境中玩， 把真实数据 @， a, 0 50 存入真实经验池。 从真实经验池中随机取一个状态 57、。 以此为起点， 用模型推演步很小， 比如 1 到 5 步 ), 生成数据 (sreal, a, r, 51， 把这些生成的 " 短数据 " 存入模型经验池。 用模型经验池的数据去训练一个 Model-Free 的智能体 （比如 SAC)。

每次推演都从“真实状态”出发，好比每走几步就看一眼地图（真实数据），消除了累积误差。虽然只推演几步，但可以生成成千上万条这样的短数据。对于SAC这样的算法来说，数据量越大，训练越稳。MBPO证明了，只要模型推演的步数k控制得当（平衡了模型误差和数据增益），基于模型的方法可以比无模型方法快很多倍（采样效率高），且最终效果一样好。

### （三）MBPO的工作流

初始化与预热

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第一阶段： 初始化与预热初始化策略网络 (Actor) 和价值网络 ℃ ritic)o 1 ． 初始化集成环境模型 (Ensemble Model)0 2 ． 初始化两个空的经验池： 和 D 3 ． model。 预热 (warm-up)： 在训练开始前， 先用随机策略在真实环境中跑几千步， 把数据存入 D 4 ． 这是为了让模型一开始有数据可学。

大循环：

总的来说，MBPO继承了PETS的集成环境模型（M1-M5）以及其更新过程（即第二阶段）。但MBTO没有用PETS直接用环境模型决定策略的方式，而是像Dyna-Q那样，取一个过去出现过的状态，根据当前策略预测起到为主函数提供数据的辅助作用（即第三阶段），主函数利用真实数据和环境数据进行更新（即第四阶段）。

第一阶段：真实交互

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第五阶段： 真实交互 (Real lnteraction) 1 ． 2 ． 3 ． 智能体在真实环境中执行一步动作。 将产生的数据 （sreal， a, r, sreal) 存入真实经验池 D 省 0 回到第二阶段循环。

第二阶段：环境模型更新（把最新现实输入给环境模型，类似PETS，对集成环境模型M1-M5进行更新，损失函数也和PETS完全相同）

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第二阶段： 模型训练 (Model Learning) 目的： 让 “ 大脑 " 跟上最新的现实。 1 ． 采样： 从真实经验池 “ 中随机抽取一批数据。 2 ． 训练： 使用最大似然估计 (MLE) 训练集成模型。 输入： @ a 目标： 输出高斯分布， 以最大化真实 5 / 的概率。： 这一步通常每隔一定步数 250 步丿才进行一次， 不是每一步都练。

第三阶段：分支推演（让环境模型输出推演数据，喂给SAC）

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第三阶段： 分支推演 (Branched Rollout) 一 _ MBPO 的灵魂目的： 利用模型生成大量短期的高质量数据。 1 ． 确定起点： 从真实经验池 “ 中随机采样 B 个真实状态 57、。 2 ． 短期推演： 以这些 57、 为起点， 用当前的策略丌和环境模型进行步推演通常很小， 如 57、 e For = 0 to 一 1： 动作佑 ～ 丌@， 下一个状态 5 许 1， 0 ～ Model(st, 佑） （利用集成模型预测） · 将虚拟样本 @，，， + 1） 存入模型经验池： 这里生成的虚拟数据量非常大， 通常是真实数据的刀十

这一阶段与Dyna-Q不同的是，Dyna-Q直接取了(s_t,a_t)，仅仅预测s_t+1,r_t。而这里只取一个s_real，后面都根据当前策略推演。

第四阶段：策略优化（利用真实数据和环境模型数据，训练SAC）

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第四阶段： 策略优化 (PoIicy Optimization) 目的： 训练 SAC 智能体。 1 ． 2 ． 混合采样： 从两个池子中按照一定比例采样构建训练 Batch0 从 “ 采一小部分 （保证真实性）。 从 D 灬采绝大部分 （利用模型的高效性）。 例如： Ba tch Size = 256， 其中真实戈尹居 ℃ ％， 模型数据 90 ％。 SAC 更新： 利用这个混合 Batch 更新 Actor 和 Critic 的参数 （梯度下降）。： MBPO 的特点是 " 环境动一步， 网络练多步 “。 通常真实环境每走 7 步， SAC 会进行 G 次 （例如 20 次丿梯更新。 这极大地提高了样本利用率。

这里的第四阶段类似于把Dyna-Q的“利用上次真实交互更新”和“利用环境更新”合为一体，更好地并行计算。

伪代码：

初始化;

先随机玩 5000 步存入真实池;

for epoch in range(total_epochs):

--- 步骤 A: 真实交互 ---

根据SAC的策略执行一步交互;

存入模型经验池;

--- 步骤 B: 训练/更新模型 ---

if epoch % 250 == 0: # 每 250 步真实交互，重新训练一次模型

使用真实数据训练模型，以最小化Negative Log-Likelihood;

--- 步骤 C: 分支推演 (数据生成) ---

从真实池中选取起点;

for i in range(k_steps): # k步推演

随机选一个模型网络，由当前策略预测下一个状态和奖励;

存入模型经验池;

状态更新，准备下一步推演;

--- 步骤 D: 策略更新 (SAC) ---

for _ in range(G_updates): # 环境每走1步，SAC训练G次(如 20 次)

混合采样(模型数据占比通常很高);

更新SAC参数;

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 二、PETS算法（Probabilistic Ensembles with Trajectory Sampling）（UC Berkeley，NeurIPS 2018）

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
