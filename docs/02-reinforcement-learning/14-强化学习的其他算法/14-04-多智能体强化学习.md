---
title: "14.4 多智能体强化学习"
source_docx: "第2部分 强化学习/14.强化学习的其他算法/14.4 多智能体强化学习.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 14.4 多智能体强化学习

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、目标和基本方法

### （一）目标：为每个智能体学习一个策略pi_i，以最大化其自身的期望累积奖励。

### （二）基本方法

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

氵巳式原理优占缺占完全中心化 (FuIIy Centralized) 将所有智能体视为一个 " 超级智能体 “。 输入是联合状态， 输出是联合动作。 环境变为稳态， 理论收敛性好。 维度爆炸： 状态 / 动作空间随智能体数量指数增长， 难以训练。 完全去中心化 (Fully Decentralized) 假设每个智能体独立， 将其他智能体视为环境的一部分。 扩展性强， 不受智能体数量限制， 避免维度灾难。 非稳态问题： 由于忽略其他智能体的策略变化， 收敛性无法保证。

## 二、IPPO 算法（Independent PPO）

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

IPPO 属于完全去中心化方法。 它直接对每个智能体使用单智能体 PPO (Proximal Policy Optimization) 算法进行训练。 尽管理论上不能保证收敛， 但在实践中效果往往不错且具备良好的扩展性。 IPPO 算法工作流： 1 ． 2 ． 3 ． 初始化： 为 N 个智能体分别初始化各自的策略网络砌和价值网络圪。 循环训练 (For 训练轮数 do): 数据采集： 优势估计： 策略更新： 价值更新： 结束循环。 所有智能体在环境中同时运行， 各自收集一条轨迹数据 (Trajectory)0 对每个智能体利用其价值网络圪和广义优势估计 (GAE) 计算优势函数对每个智能体最大化 PPO-Clip 目标函数来更新策略 @。 对每个智能体通过最小化均方误差损失函数来更新价值网络。

## 三、MADDPG算法（OpenAI，2017）

### （一）CTDE范式

为了平衡环境非稳态和可扩展性的矛盾，学术界提出了CTDE（Centralized Training with Decentralized Execution，中心化训练去中心化执行）范式。

1.训练阶段：允许使用全局信息（所有智能体的状态、动作、奖励）。就像足球队的教练，拥有上帝视角，指导球员如何配合。

2.执行阶段：智能体仅根据自己的局部观测进行决策。就像球员上场后，只能根据自己看到的局部情况踢球，不再依赖教练的实时指令。

### （二）模型架构

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

MADDPG (Multi-Agent DDPG) 是 CTDE 范式下的代表算法， 基于 Actor-Critic 架构。 · Actor （策略网络）： 仅输入局部观测， 输出动作。 用于执行阶段。 Critic （价值网络）： 输入全局信息 x （包含所有观测 01， · ·，） 和所有动作 al， · 用于训练阶段。 数学推导与梯度公式： 我们定义全局状态信息为 x, 所有智能体的动作集合为 a = {a ．， ONO 亻又 1 ． Critic 更新 （中心化部分）： 每个智能体的 Critic 网络 Q (), al，，） 旨在评估在全局状态下采取联合动作的价值。 损失函数定义为均方误差： X X 其中， 目标值使用目标网络计算 ()D Target)：： 这里 Q# 是目 Critic 网络， 是目标 cto 厂网络。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 ． Actor 更新 （去中心化执行的基础）： 对于确定性策略 (Deterministic Policy)， 我们通过最大化 Q 值来更新策略。 由于 Q 是中心化的， 它能告诉 Actor： " 考虑到其他队友的动作， 你这样做好不好 "。 确定性策略梯度公式为： VozJ(ßi) = Ex,a、 佐@，） · Qß(), al， 解释： V 研佐@，）： Actor 怎么变动参数能让输出的动作变化。 Va Q#(. 一）： Critic 告诉 Actor, 动作往哪个方向变动能让 Q 值 （全局收益） 变大。

### （三）训练工作流

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 初始化： 为每个智能体初始化 Actor 网络和 Critic 网络 Qt0 初始化对应的目标网络和 QO 初始化经验回放池刀。 主循环 (For 序列 = 1 to M do): 初始化随机过程 （用于探索噪声）， 获取初始全局观测 xo 步数循环 (For t=l tO max_steps dO）： 决策： 每个智能体根据当前策略和局部观测 ot 选择动作： 佑 = 佐@，） + Mt0 ·， (x), 环境返回奖励 r 和新观测 x。 交互： 执行联合动作 a · 存储： 将转换元组 (), a, r, (') 存入经验回放池。 训练 （当池子足够大时）： 从 D 中随机采样一批数据 (Batch)0 更新 Critic: 计算目标值最小化 Loss 更新每个智能体的 0。 注意此时 Critic 输入的是所有智能体的动作。 更新 Actor: 使用梯度上升法更新每个智能体的。 Acto r 根据局部观测输出动作， 但梯度方向由中心化的 Critic 提供指引。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

· 软更新： 使用丆参数软更新目标网络： 伊 TO + （1 一丆） 伊。 结束步数循环。 3 ． 结束主循环。

和其他Actor-Critic算法一样，训练完毕后，Critic即舍弃，留下Actor根据训练后“冻结”的策略进行前向推理。因此，训练完毕后，推理就完全去中心化。

## 四、多智能体+编排器

以Kimi K2.5 Agent Swarm为例，推理过程中，当输入一个任务时，一个编排器负责确定输入任务需要子智能体的数量及角色（以上下文形式提供给智能体）、分配任务、调度智能体；每个子智能体都是同一个大模型的“分身”，权重完全相同。

训练方法：先训练子智能体（大模型）；在子智能体训练完毕后，冻结子智能体的参数，用端到端的强化学习方法训练编排器（刚开始先用小模型充当子智能体，后续再换为大模型）。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
