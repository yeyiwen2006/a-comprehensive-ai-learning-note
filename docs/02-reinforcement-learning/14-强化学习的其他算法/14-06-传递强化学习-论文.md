---
title: "14.6 传递强化学习（论文）"
source_docx: "第2部分 强化学习/14.强化学习的其他算法/14.6 传递强化学习（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 14.6 传递强化学习（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、时序差分算法和蒙特卡洛方法的价值回传步数（递归次数）

传统RL中，价值函数的更新主要依赖蒙特卡洛算法和时序差分算法，前者方差大，后者偏差大。在二者间取折中的TD-n算法，则需要人工调整n以平衡偏差和方差。从价值函数回传的时间复杂度角度看：

时序差分算法（TD）：V(s_t)=r+gamma*V(s_t+1)，每次更新，价值信息回传一个时间步，故在长度为T的序列上回传需要T个时间步，每一步都会引入额外的偏差，导致最终误差大。

TD-n算法：利用n步实际采样的经验，公式为：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

V(st) 0 + 70 + 1 +

每进行一次更新，价值信息就在轨迹上向前跨越了n个时间步。要覆盖总长度为T的路径，信息只需要跨越T/n次（称其为递归次数）。虽然n越大递归越少（偏差累积越小），但由于n步采样引入了更多未观测到的随机性，方差会随之剧增。

蒙特卡洛算法：只需要一个时间步（根据每一步实际获得的奖励计算），但方差大。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Method 1 Divide & Conquer Horizon Decomposition 772 # Recursions O(T) 0 （77 司 772

## 二、传递强化学习的核心原理

### （一）价值递推公式

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在目标条件 RL 的确定性环境下， 我们定义矿@， g) 为从 5 到 g 的最短步数。 根据三角不等式， 对于任意中间点 w：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 RL 中， 价值函数通常定义为折扣回报。 对于到达目标的任务， 若每步奖励为 0， 到达目标奖励为 1， 则价值函数定义为： = 7 其中 0 < 7 < 1。

这里V是当前状态s和目标g的函数。从中可以看出，传递强化学习是目标导向的强化学习的一种变体.

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

我们将三角不等式的两边同时作为 7 的指数。 由于 0 < 7 < 1, 指数函数 = 的是单调递减的。 因此， 不等号方向会反转： 7

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

0 “） 代回上式， 得到： 将气 5， g） = 7

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

由此推导出传递贝尔曼更新规则 (Transitive Bellman Update): weS

### （二）递归次数：logT

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

TRL 采用的是分治法 (Divide-and-Conquer)0 它不是从后往前 “ 挪 "， 而是从中间 " 切 "。 工作机制： TRL 的核心更新式是 V(s,g) 铲@， W)V(W,g)0 1 ． 为了求长度为 T 的路径价值， 它寻找中点 w， 将其拆分为两个长度为 T/2 的子问题。 2 ． 这两个子问题再各自寻找中点， 拆分为长度为 T/4 的子问题。 3 ． 以此类推， 直到子问题的长度缩短到 1 （即直接从数据集中观测到的相邻状态）。 数学推导： 这是一个典型的二分查找或合并排序的结构。 其递归深度满足 2h =， 因此五 = log2 TO 优势： 相比于 TD 的线性增长， O(log T) 极大地减少了函数逼近器 （神经网络） 在递归过程中产生的误差累积。 例如， 当 T = 1024 时， TD 需要 ℃ 24 次迭代， 而 TRL 理论上只需 ℃

## 三、价值高估问题的解决方案

### （一）计算max时只使用已有样本的状态和动作

1.限制子目标w的范围在已有状态中：不在整个状态空间搜索中间状态w，而是仅从数据集中已有的状态里提取子目标（称为行为子目标）。

2.利用已有动作求解损失，拟合最优动作：使用非对称的Expectile损失函数来近似max操作，从而在不显式迭代所有状态的情况下，只利用样本内的动作来实现最大化：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

对于一个目标值丆和我们的估计值 Q, Expectile LOSS 定义如下： 其中是一个在 （0 ． 5， 1） 之间的超参数 （论文实验中通常取 0 · 7 或 0 · 9）： 当丆 > Q 时 （即目标值比当前估计高， 代表这是一个 " 更好 " 的路径组合 ), 权重为当丆 < Q 时 （即目标值比当前估计低）， 权重为 1 一

如果k接近1，损失函数会极度惩罚“低估”优秀路径的行为，从而迫使神经网络Q去拟合数据集中表现最好的那部分组合，而不被平庸的数据拉低平均值。这就实现了隐式的最大化。

### （二）基于距离的重权化

算法会为损失函数分配权重，优先保证短路径（小问题）的价值估计准确，因为长路径的准确性依赖于短路径的组合。这类似于动态规划中“由小到大”的求解逻辑。

## 四、损失函数

对于初始状态s_i，采取动作a_i，经过s_k，最终到达状态s_j：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 10 OCR 未识别出有效文字：未识别出有效文字。]

1.目标值的计算

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

（1） 目标值 （乘法递归） 根据我们之前推导的公式， 目标值不再是 r + 7， 而是两个子问题的乘积： 这里是从数据集里采样出来的、 位于 st 和力之间的一个行为子目标。

L_k(...)这步操作相当于对不同s_k取最大值，即这个损失函数设计的核心目的是让价值函数Q(s_i,a_i,s_j)拟合对于不同s_k，T_TRL的最大值。

2.距离重权化

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

其中 d(st, 5 = log 1 （1 + d(st, 力佑， 力） 代表估计的时间步数。

距离越短的任务，权重越大，即这样的任务的Q估计值的准确性更加重要。因为长任务的价值是由短任务“拼”出来的。如果短任务（比如只跨越几步的子路径）的Q估计值都没学准，根据分治法，长任务的误差比然呈指数级爆炸。所以必须优先保证局部准确。

3.算法工作流

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 4 ． 收集碎片： 局部对齐： 择优录取： 从离线轨迹中随机切出三点 （， 力）。 检查当前的神经网络是否满足 Q 佞力 Q(), · Q(k,j)0 通过权重， 如果发现某一种左的切分方式能产生更大的价值 （代表更短的路径 ), 就让 Q 佞力向这个更大的值靠拢。 由小及大： 优先训练距离近的点对， 逐渐建立起对整个大地图 （长时序） 的价值感知。

## 五、问题与挑战

虽然TRL解决了O(T)的递归问题，但它也引入了一个新开销：它需要对状态空间中的点对 （Pairs）进行采样。相比于TD学习只需采样相邻点，TRL对数据集的覆盖质量和采样多样性有更高的要求。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
