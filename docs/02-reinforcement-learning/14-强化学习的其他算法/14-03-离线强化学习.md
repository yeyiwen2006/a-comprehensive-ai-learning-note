---
title: "14.3 离线强化学习"
source_docx: "第2部分 强化学习/14.强化学习的其他算法/14.3 离线强化学习.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 14.3 离线强化学习

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、离线强化学习的定义和主要方法

离线强化学习的核心在于：智能体不与环境交互，仅从已收集的静态数据集（Experience Replay Buffer）中学习策略。这解决了现实场景（如自动驾驶、医疗、推荐系统）中在线试错成本过高或反馈滞后的问题。

在在线策略（On-policy）或离线策略（Off-policy）算法中，智能体可以通过交互修正误差。但在纯离线设定下，直接使用传统Off-policy算法（如DDPG）会失败，因为数据集中状态-动作对(s, a)的分布与当前策略pi产生的分布不匹配，价值函数Q(s, a)会对数据集中未出现的动作（Out-of-Distribution, OOD）给出错误的高估值。由于智能体无法去环境验证，这个错误会不断累积，导致策略失效。

为了解决这个问题，主要有两种思路：限制策略选择（如BCQ）和 限制价值函数（如CQL）。

## 二、批量限制Q-learning（BCQ）

BCQ的核心思想是：为了解决“外推误差” (Extrapolation Error)，我们将策略限制在离线数据集（Batch）的分布范围内，并在该范围内寻找最优动作。

### （一）模型组成

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Q 网络 (Critic): Q 鼠 5， a)o 通常使用两个 Q 网络 (Double Q-learning) Q91, Q92 来减少过估 VAE （生成模型）： 用于模拟数据集中的行为策略分布珏 （a 司。 由编码器 Ew(), a) 和解码器切@ z) 组成。 扰动网络 (Perturbation Network): 0 （5， a)o 这是一个确定性策略网络， 用于在 VAE 生成的动作基础上进行微调。

### （二）工作流

1.训练VAE

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第二步： 训练 VAE (Training VAE) 这是 BCQ 的基石。 目的： 让模型学会 " 在当前状态 5 下， 离线数据集里通常会采取什么样的动作矿。 输入： 从 Buffer 中采样的真实 @， a) 对。 输出： 重构的动作 åo

具体训练方式见后文。

2.计算目标Q值

在Q-learning中，目标Q值为max(奖励+下一状态s’的Q值)。这里的难题是怎么估计下一状态s’的Q值。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

生成候选动作： 我们不敢随意取 maxa Q(s', a)， 因为 Q 网络在未见过的动作上估值是不准的。 所以， 我们利用目标 VAE 随机采样 “ 个潜在变量 z ～ （0，， 输入解码器， 生成 “ 个属于数据分布的候选动作 {， 扰动调整： 这些动作虽然 " 像 " 数据， 但不一定是最优的。 我们使用目标扰动网络对这 “ 个动作进行微调： 艹可 + 0， （的可）。 选择最优： 将这 “ 个微调后的动作输入目标 Q 网络， 选取其中 Q 值最大的那个动作来计算 Target0 公式： （： 始论文中通常亘接取两个 O 网络的最值来计算丿 Clipped Double o-learning)

3.更新Q网络

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

标准的 TD Error 最小化：

4.更新扰动网络（见后文）

### （三）VAE的训练

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 前向传播 (Forward): 编码器 (Encoder): 输入 （5， a)， 输出潜在变量 z 的分布参数： 均值和标准差重参数化 (Reparameterization Trick): 为了让梯度可导， 我们采样 6 ～ （0，， 然后令解码器 (Decoder): 输入 @， z), 输出重构动作 å = 刀@， z)o 损失函数 (Loss Function): VAE 的目标是最大化 ELBO (Evidence Lower Bound)0 损失函数包含两部分： 重构误差 (Reconstruction Loss): 希望生成的 å 尽可能接近真实的 ao 通常使用均方误差 (MSE)0 KL 散度 ()L Divergence): 希望潜在变量 z 的分布接近标准正态分布 （0，， 防止过拟数学公式如下： 力 VAE 重构误差 KL 散度正则化

### （四）为什么要用VAE，而不能用回归、交叉熵等方式？

1.回归的问题

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

如果仅仅是用一个普通的回归网络 （比如 a = 丌 （5）） 去拟合数据， 当一个 5 对应多个优良动作 a 时， 网络会发生严重的错误 （比如取平均值）。 VAE 正是专门用来解决这种 “ 一对多 " 映射 （即多模态分布 Multimodal Distribution) 问题的。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设你正在训练一个机器人在十字路口导航 （状态 5）。 · 在历史数据中， 有时候机器人会左转 （动作 al _ 有时候机器人会右转 （动作 a2 = +1)0 这两个动作都是合理的。 如果你训练一个普通的确定性神经网络 a = N （5） 来最小化均方误差 (MSE)： 网络为了让两边的误差都最小， 最终会输出两个动作的平均值： a = 0 （直行）。 结果： 机器人撞墙了。 这就是 “ 多峰分布的均值往往是低概率区域 " 的问题。

2.交叉熵的问题

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

交叉熵是分类任务的标配 （比如识别猫和狗）。 “ log(ypred) 如果动作是离散的 （比如： 上、 下、 左、 右 ), 用交叉熵完全没问题。 但在 BCQ 应用的场景 （如机器人控制、 自动驾驶） 中， 动作通常是连续的数值。 假如我们要用交叉熵拟合连续动作， 必须先进行 “ 离散化 "(Discretization)： 例子： 假设控制一个机械臂的一个关节， 角度范围是 [ 一 180。， 180。 ]。 做法： 把角度切分成 ℃ 0 个格子 (Bins)0 训练： 这就变成了 ℃ 0 分类问题， 可以用交叉熵。 问题来了 （维度灾难）： 如果机械臂有 7 个关节 （7 维动作空间 ), 每个关节切分 ℃ 0 个格子。 总的类别数量： 1007 = 100， 000， 000， 000， 000 （100 万亿个类别）。 结论： 输出层需要 ℃ 0 万亿个神经元， 这是无法计算的 (Softmax 算不过来）。

3.VAE的好处

把VAE的编码器和解码器合起来，实际上就是一个“模仿器”，其能有效在给定a的情况下，“模仿”出一批相似的a，无论a的分布如何。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

VAE 的核心在于引入了一个隐变量 (Latent variable) zo 你可以把 z 理解为一个 " 随机的模式开关 " 或者 " 意图 "。 在 BCQ 的 CVAE 中， 映射关系不是 a， 而是 s + z ao 训练阶段： 编码器看到的是 “ 具体答案 " 编码器 E(), a) 的输入同时包含了状态 5 和具体的动作 ao 当你给它输入 @， 左转） 时， 纟扁码器会学习输出一个特定的 Zleft （比如 z 的分布偏向一 1）。 当你给它输入 @， 右转） 时， 纟扁码器会学习输出另一个特定的 zr、 匆 （比如 z 的分布亻扁向 + 1）。 关键点： 纟扁码器并不困惑， 因为你把 " 答案 " a 告诉它了。 它只是负责把这个具体的 @， a) 对应到一个潜在空间的坐标 z 上。 推理 / 生成阶段： 解码器负责 “ 一对多 " 的展开在实际使用 （生成动作） 时， 我们没有真实的 a, 流程如下： 1 ． 我们给定状态 5。 2 ． 我们从标准正态分布中随机采样多个噪声 z ～ （0，。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

VAE 的核心在于引入了一个隐变量 (Latent variable) zo 你可以把 z 理解为一个 " 随机的模式开关 " 或者 " 意图 "。 在 BCQ 的 CVAE 中， 映射关系不是 a， 而是 s + z ao 训练阶段： 编码器看到的是 “ 具体答案 " 编码器 E(), a) 的输入同时包含了状态 5 和具体的动作 ao 当你给它输入 @， 左转） 时， 纟扁码器会学习输出一个特定的 Zleft （比如 z 的分布偏向一 1）。 当你给它输入 @， 右转） 时， 纟扁码器会学习输出另一个特定的 zr、 匆 （比如 z 的分布亻扁向 + 1）。 关键点： 纟扁码器并不困惑， 因为你把 " 答案 " a 告诉它了。 它只是负责把这个具体的 @， a) 对应到一个潜在空间的坐标 z 上。 推理 / 生成阶段： 解码器负责 “ 一对多 " 的展开在实际使用 （生成动作） 时， 我们没有真实的 a, 流程如下： 1 ． 我们给定状态 5。 2 ． 我们从标准正态分布中随机采样多个噪声 z ～ （0，。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 ． 解码器刀 （5， z) 将这些不同的 z 映射回动作空间： 假如采样到了 ZI （对应左转模式）， 解码器输出 a 假如采样到了 （对应右转模式）， 解码器输出 a + 1。 结论： VAE 并没有把 5 强行映射到一个固定的 a， 而是学会了条件概率分布 P 司。 通过改变输入的 z， 同一个 5 就可以生成出在这个状态下所有可能见过的动作。

### （四）扰动网络的更新

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Q: 扰动网络的实际意义是什么？ A: 它是 “ 局部最优 " 的搜寻者。 VAE 的局限性： VAE 只是单纯地 " 模仿 " 数据集。 如果数据集中有很多次优的动作， VAE 也会生成次优动作。 仅仅从 VAE 采样， 我们只能得到 " 大概率出现在数据集里的动作 “， 但不一定是 " 好动扰动的作用： 扰动网络 0@， a) 接收一个 VAE 生成的动作并输出一个微小的调整量它的目标是在不偏离数据分布太远的前提下， 让 Q(s,a + 0 的值变得更大。 Batch-Constrained 的体现： 为了防止这一步调整得太离谱 （导致 OOD 问题 ), 我们通常会限制扰动网络输出的范围， 例如限制在 [ 一之间通常是个很小的数， 如 0 ． 05）。 这意味着我们只允许在数据流形的 " 管状邻域 " 内进行策略提升。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Q: 什么时候更新扰动网络？ A: 在更新 Q 网络之后， 或者与 Q 网络交替更新。 它的更新逻辑遵循确定性策略梯度 (DPG) 的思路。 1 ． 2 ． 3 ． 冻结 Q 网络： 我们只优化 （扰动网络的参数 ), 不改变 Q 的参数。 采样输入： 从 VAE 采样出的 a 是这一步的输入 （注意： 这里不用 Buffer 里的真实因为我们要优化的是 VAE 生成出来的那些动作）。 梯度上升： 我们希望最大化 Q 值。 一 E 一、 刀 [ + 0@at.ae 这里 “

## 三、保守Q-learning（CQL）

CQL算法基于SAC算法，对Q函数的更新方式进行了修改。既然在无法与环境互动的情况下，未知动作容易被高估，那就直接在Q函数的更新目标中加入惩罚项，人为压低没见过的动作的Q值。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

CQL 的 Critic 损失函数 LCQL(O) 由两部分组成： 标准 BelIman 误差和保守正则项 (Conservative Regularization)0 CQL 保守正则项标准的 Bellman 误差 （同 SAC) 我们重点解释这个保守正则项 R(O)O 数学形式在连续动作空间的 CQL (CQL(H)) 中， 保守项通常设计为： wD log

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 一 E。、 ‰@嘲 （拉高数据动作） 这一项前面有个负号， 所以最小化损失函数等于最大化这一项。 含义： 它要求 Q 网络给数据集中真实存在的动作打高分。 作用： 保证已知数据的价值被正确估计， 防止 Q 值整体塌陷到负无穷。 2 ． log Ea exp(Q9(s, a)) （压低所有动作， 特别是 00D） 这是 LogSumExp 函数， 它是 Soft-Maximum 算子。 · 它近似于 Q(s,a)0 含义： 最小化这一项， 等于压低当前状态 5 下所有动作的 Q 值。 矛盾与平衡： 第一项想让数据集里的动作 Q 值变大。 第二项想让所有动作 Q 值变小。 最终结果： 只有那些真实存在于数据集中的动作， 因为有第一项的支撑， Q 值能保持住； 而那些不在数据集中 (OOD) 的动作， 只受到第二项的打压， Q 值会被迅速压

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在连续动作空间中， log exp(Q(s,a)) 的求和 （积分） 是无法直接计算的。 CQL 通过重要性采样 (lmportance Sampling) 来近似这一项。 实际代码实现中， 损失函数通常写成这样： 力@） = a、 刀 E。、 [ 一。、 + B ellman Error 压低采样动作拉高真实动作这里的萨@5） 是一个用来生成 “ 可能被高估的动作 " 的分布。 在实际运行步骤中， 我们通常采样以下几种动作来计算 “ 压低项 "： 1 ． 当前策略生成的动作： a、 （5） （这是最容易过拟合的地方）。 2 ． 均匀分布的随机动作： a ～ Uniform(—1, 1）。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
