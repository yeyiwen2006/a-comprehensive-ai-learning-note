---
title: "14.5 目标导向的强化学习"
source_docx: "第2部分 强化学习/14.强化学习的其他算法/14.5 目标导向的强化学习.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 14.5 目标导向的强化学习

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、目标导向的强化学习——把目标变成变量

### （一）目标导向

在强化学习中，有时我们希望模型不仅能完成单一任务，还能迁移到通用任务。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 传统 RL 的定义 （单一任务） 在传统的强化学习 （比如训练一个超级马里奥 (I) 中， 任务是固死的。 输入： 只有状态 5 （比如屏幕画面）。 目标： 只有一种赢法 （救出公主）。 价值函数： Q(s,a)0 意思是 " 在状态 5 做动作 a 能得多少分 "。 局限性： 这种 AI 只能做这一件事。 如果你把终点挪个位置， 它就傻了， 必须重新训练。 2 · 目标导向 RL (GoRL) 的定义 （通用任务） HER 解决的是 Goal-Oriented （目标导向） 问题。 这里的 “ 问题定义 " 引入了目标 g 作为变量。 输入： 状态 5 + 目标 g。 神经网络的输入层变成了 concatenate(state, goal)。 目标： 目标是动态的。 比如机械臂抓取， 目标 g 可以是桌子上的任意坐标 （z)o 价值函数： Q(s,a,g)0 意思是 " 在状态 s 做动作为了去目标 g， 能得多少分 "。 通用性： 这种 AI 学会的是 " 如何从位置 A 到达位置 B " 的通用逻辑， 而不是死记硬背某一条路亻工。 一句话总结 “ 问题定义 "： 我们不再训练智能体 " 去终点 “， 而是训练它 “ 去你指定的任何地方 "。 数学上， 这意味着 MDP （马尔可夫决策过程） 多了一个维度 g， 奖励函数咒也不再只看 5， 而是看 5 和 g 的距离。

数学上，这意味着MDP（马尔可夫决策过程）多了一个维度g，奖励函数R也不再只看s，而是看s和g的距离。

### （二）稀疏奖励困境

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

场景： 机械臂抓球假设你在训练一个机械臂去抓取桌上的球。 1 ． 2 ． 3 ． 4 ． 随机探索： 刚开始， 机械臂是乱动的。 极低概率： 机械臂乱挥一万次， 可能只有一次碰巧抓到了球。 传统 RL 的困境： 第 1 到 9999 次尝试： 没抓到球 + 奖励 = 一 1 （或者 0）。 第 ℃ 000 次尝试： 抓到了奖励 = + 1。 结果： 只有那一次成功的经验是有正反馈的。 但对于神经网络来说， 正样本太少了！ 网络看了 9999 个负样本， 它会学到一个结论： " 无论我做什么都是错的 / 没分的 "。 梯度消失， 训练失败。

### （三）HER（事后经验回放）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 正常采集 （失败的尝试） 任务失败。 指令： · 行动： 现实： 你告诉智能体： “ 你的目标是去 A 点 (g = “。 智能体尝试了一通， 但是因为太笨， 最后跑到了 B 占、 0 原始数据 （存入 Buffer)： 一 1， 5 /， Goal 这条数据告诉智能体： f±B 跑是 0 不到的分的。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 事后回放 (HER 的核心操作： 篡改记忆） HER 算法在把数据存入回放池之前， 会进行一次 “ 篡改 "： 思考 (Hindsight)： “ 虽然你没去成 A, 但是如果我当初给你的指令就是去 B 点呢？ 那你岂不是冫两分完成了任务？ “ 篡改： 将目标 g 从 A 强行修改为 B （智能体实际结束的位置）。 重算奖励： 因为现在目标是 B， 而你确实到了 B， 所以奖励 r 变成了 + 1 （成功）。 伪造数据 （存入 Buffer)： { 5， a,r = + 1， 5 ／， Goal = B} 这条数据告诉智能体： 如栗你的目标是那你刚才那套动作就是完美的 /

智能体虽然还没学会怎么去A，但它先学会了怎么去B。随着它学会去的地方越来越多（右边的B,右前方的C,左边的D,正前方的E...），掌握了怎么去不同点的运动规律，最终它就能学会怎么去A。再举一个射箭的例子：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

传统 RL 教练： " 你脱靶了！ 0 分！ 这个动作是错的， 下次别这么干。 " 后栗： 射手只知道自己錯兀但不知么射才能中树干， 也不知道怎么射才能中靶 / 心， 挫败感极强。 HER 教练： 1 ． 2 ． 3 ． " 首先， 对于靶心来说， 你确实脱靶了， 0 分。 " （存入原始经验） （画个圈） “ 但是！ 我现在在树干上画个圈， 假设我们的目标本来就是射树干。 那你刚才这一箭简直是神射！ ℃ 0 分！ " （存入 HER 经验） " 快记住刚才射中树干的感觉 （动作力道、 角度）， 虽然它对射靶心没用， 但它教会了你如何控制弓箭射向右上角。 " 为什么要这样？ 因为控制弓箭的物理规律是通用的。 如果你学会了怎么射树干 （右上）、 怎么射草地 （下方）、 怎么射天空 （上方 ), 你最终就能掌握微调的技巧， 从而射中靶心。

这样，智能体就能从失败中学习了。

工作流如下：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． Rollout （采样）： 智能体以目标 g 运行， 产生轨迹丆 Storage （存储 - 关键步骤）： 将 (st,，， + 1， g) 存入 Buffero 此时大概率是一 1。 正常存储： HER 采样 (ReIabeIing)： 从轨迹中选一个后续状态 （比如） 作为新的 " 假目标 " g'o · 计算新奖励 r' = 咒 （， (') （此时通常为或 + 1）。 · 将伪造的元组 （，， r， + 1， (') 也存入 Buffero 3 ． Training （训练）： 从 Buffer 中随机采样一批数据 （混合了真数据和假数据）。 用 DDPG、 SAC 或 DQN 算法更新神经网络。 神经网络看到输入 @， (') 居然得到了高分， 它就学会了控制物体移动的物理逻辑。

### （四）价值函数

前面我们得到的数据混杂了多种不同目标下的奖励，无法直接套用DDPG、SAC或DQN算法。因此我们将价值函数改造为通用价值函数近似器（Universal Value Function Approximators, UVFA）。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

传统的 RL 假设任务是固定的， 所以目标隐含在环境逻辑里， 不需要告诉神经网络。 但在 HER （目标导向 (L) 中， 我们将目标 g 显式地作为神经网络的一个输入。 这被称为 UVFA (UniversaI VaIue Function Approximators)0 传统 Critic: Q(s,a) + 输入状态和动作， 输出价值。 目标导向 Critic: Q(s,a,g) + 输入状态、 动作和目标， 输出 " 在该目标下， 这样做有多好 “。 同理， 策略网络 (Actor) 也必须知道目标： 传统 Actor: a = 丌 （5 目标导向 Actor: a = T(s,g) + 输入状态和目标， 输出 “ 为了去那里， 我现在该做什么 "。

这样g就变成了函数的输入之一，往往以向量表示（如在三维空间中的位置）。由神经网络的泛化性，即便神经网络未到过a，也可以用附近其他目标的数据作出泛化估计。能这样估计的原因有以下三点：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 共享物理规律 (Shared Dynamics) 这是最本质的原因。 无论目标是去 A 还是去 B, 环境的物理规律 （动力学） 是不变的。 例子： 假设你控制一个机械臂。 任务 A： 把手伸到左边的桌角。 任务 B： 把手伸到左边的椅子上。 共同技能： 为了完成这两个任务， 你都需要学会 “ 如何让手肘关节向左旋转 " 以及 " 如何控制力度 "。 神经网络的作用： 神经网络的隐藏层 (Hidden Laye (s) 学到的不是 " 去 A 的路径 "， 而是 " 如何控制身体 " 的物理表征。 当网络通过 HER 数据 （去 B 的经验） 更新参数时， 它学会了： " 当我输出这个动作时， 手臂会往左移动 ℃ 厘米。 " 下次遇到去 A 的任务 (A 也在左边 ), 网络就会调用这部分已经训练好的参数 （知识）： ' ' 我想往左移， 我知道输出那个动作可以做到。 "

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 目标空间的连续性 (Geometric Continuity) 在 HER 中， 目标 g 通常是一个向量 (vector) （例如坐标印， 而不是一个独热编码 (One- h ot (D)O 神经网络对于连续输入的处理具有天然的泛化能力。 数学视角： 神经网络拟合的是一个函数 5， g）。 对于连续函数， 如果 gl 和 g2 在空间上很接近 （比如坐标很近 ), 那么网络对它们的输出也会很接近。 迁移过程： 1 ． 2 ． 3 ． 4 ． HER 让你在目标 B = [I.O, 1.0] 上获得了成功经验， 网络参数被更新， 记住了 “ 在这个位置怎么做能到 [1.0, 1.0]"0 现在让你去目标 = 1.0,1.1]0 因为输入向量 B 和非常相似， 神经网络会激活相似的神经元路径， 输出相似的 Q 值和动作。 虽然你没练过去 A, 但因为你练过去 A 附近的 B, 泛化能力让你瞬间学会了去 AO

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 · 学习的是 “ 相对距离 " 而非 “ 绝对坐标 " 这一点稍微抽象一点， 但对于理解 HER 至关重要。 在很多实现中， 神经网络其实并不是死记硬背 “ 当目标是 g 时做什么 "， 而是隐式地学会了处理状态 5 和目标 g 之间的差值 (Difference vector)。 lnput （5， g) + Network learns + A = (g 一 5） 状态 5 = 0， 0 ]， 目标 B = [ 1， 1 ]。 差值是 [ 1， 1 ]。 状态 5 / = [ 5， 5 ]， 目标 = [ 6， 6 ]。 差值也是 [ 1， 1 ]。 迁移： 如果你在 5 = [ 0， 0 ] 时学会了如何移动到 B = [ 1， 1 ] （即学会了如何消除 [ 1， 1 ] 的差值 ), 那么当你处于 5 / = [ 5， 5 ] 并试图去 = [ 6， 6 ] 时， 虽然地点完全不同， 目标完全不同， 但 “ 消除差值 " 的相对任务是一样的。 通过 HER 提供的去 B 的经验， 智能体学会了 " 如何消除 [ 1， 1 ] 这个差值向量 '。 这个技能可以被平移应用到空间中的任何位置。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

4 · 价值函数的传播 (Bellman Updates) 即使目标 A 和 B 离得很远， HER 依然有效， 因为 Q-Learning 具有拼接路径的能力。 假设： 起点 + 中间点 B —+ 终点 AO 1 ． 2 ． 3 ． HER 训练： 智能体误打误撞到了 BO HER 修改目标为 B， 让智能体学会了 " 如何从起点到 B"O 正常训练： 智能体在 B 附近随机探索， 偶然到了 AO 它学会了 " 如何从 B 到 A"O 价值传播： Q （起点， 动作， r+7Q(), 动作， 当智能体想去 A 时， 它发现： “ 虽然我不知道怎么直接一步到 A, 但我知道只要到了 B， 我就能有很高的价值 （因为我从 B 能到 A)。 而我也知道怎么去 B （归功于 HER)0"

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

目标导向贝尔曼方程 (Goal-Conditioned Bellman Equation) 在这里， 奖励函数 r 变成了 r(s,a,g) （即： 只有当接近 g 时， r 才高）。 因此， 对于经验回放池中的一条样本 @， a, s',g), 更新公式变为： r(s,a,g) +7maxQ( a， g） 其中一 T(s',g) 当前目标下的奖励 Critic 也要看目标这就解释了为什么能混用数据： 虽然数据池里有各种各样的目标 .gl， .g2， g3 · · ·， 但因为我们将 g 作为输入喂给了网络， 网络学会的是一个条件概率分布。 它不会混淆， 而是学会了： " 如果输入是目标 A, 我就输出高分； 如果输入是目标 B, 我就输出低分 "。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

目标导向贝尔曼方程 (Goal-Conditioned Bellman Equation) 在这里， 奖励函数 r 变成了 r(s,a,g) （即： 只有当接近 g 时， r 才高）。 因此， 对于经验回放池中的一条样本 @， a, s',g), 更新公式变为： r(s,a,g) +7maxQ( a， g） 其中一 T(s',g) 当前目标下的奖励 Critic 也要看目标这就解释了为什么能混用数据： 虽然数据池里有各种各样的目标 .gl， .g2， g3 · · ·， 但因为我们将 g 作为输入喂给了网络， 网络学会的是一个条件概率分布。 它不会混淆， 而是学会了： " 如果输入是目标 A, 我就输出高分； 如果输入是目标 B, 我就输出低分 "。

以DDPG + HER为例，具体操作如下：

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

A. 网络输入的拼接 (Concatenation) 在代码实现时， 我们通常把状态向量 5 和目标向量 g 拼接在一起作为一个整体输入。 假设： 状态 5 维度为 ℃ （机械臂关节角度、 速度等）。 目标 g 维度为 3 （目标坐标 ×， y， z）。 那么： · Acto r 网络输入： Critic 网络输入： 维度 10 + 3 = 13。 输入向量为 concat([s, (])。 维度 10 + 3 + action dimo 输入向量为 concat([s, a， (])。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

B. 改进后的 DDPG 更新流程假设我们从经验池采样了一批数据 B = {@，，， 可， (i)}ff=l （注意： 这里的可能是原始目标， 也可能是 HER 篡改后的目标， 奖励 0 是根据算出来的）。 1 ． 2 ． 3 ． 计算目标值 (Target Q): 使用目标网络 (Target Actor 丌 ／ 和 Target Critic）： = 丌气， gD = r(Si,，） + 7Q 气 5，， ai,gi)： 这里计算 Q / 须把这条数据对应的目标仂传进去。 更新 Critic 网络： 最小化预测值与目标值的均方误差 (MSE)： 1 E(Q(Si, 一） 2 更新 Actor 网络： 目的是让 Critic 给出的评分最高： 1

这样，网络就变成了一个“通用导航仪”：只要你输入起点s和终点g，它就能利用物理规律告诉你怎么走，而不受限于训练时那个固定的终点。

### （五）目前面临的问题

目标导向的强化学习虽然能从失败中学习，但当动作空间巨大、不同任务相关性弱、长时间找不到“主线相关的奖励”时，也无异于盲人摸象。

## 二、自监督的目标导向强化学习（NeurlPS 2025）

与SAC/PPO这种基于动态规划（贝尔曼方程）的方法不同，这篇论文的方法本质上是一个自监督序列建模问题。

### （一）模型构成

1.输入和输出

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

网络圃接收一对状态作为输入： “ 我现在在哪 " 和 “ 我要去哪 " 原始输入： 当前状态 (Current State): st 5 目标状态 (Goal State): g e 5 注意： 在训练阶段， 这个 g 通常是通过 Hindsight （事后诸葛亮） 机制从同一条轨迹的未来时间步 5 衅 k 中采样得到的。 预处理 (Embedding): 通常会将和 g 拼接 (Concat) 或分别通过 Embedding 层映射后相加， 形成网络的初始隐状态 zo： zo = Embed(st, g)

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这是一个多头网络 (Multi-head Network), 通常有两个输出头： 策略头 (PoIicy Head)： 输出动作的概率分布丌 （国 g)o 对于离散动作空间： 输出 Logits （维度等于动作数）。 对于连续动作空间： 通常输出高斯分布的均值和方差伊 （或通过离散化处理）。 含义： 为了到达 g， 我在这一步应该做什么？ 价值 / 距离头 (value/Distance Head) （辅助任务）： 输出当前状态到目标 g 的预估距离 （时间步数。 含义： V(st, g) 一妩这个输出主要用于辅助训练， 帮助网络理解 " 远近 “ 关系， 有时也用于测试时的路径规划剪枝。

总结：

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

输入： st + （也就是 g) 网络： ℃ 00 层深度推理目标： （真实的动作， 这是一个固定的 Ground Truth)

2.损失函数和更新方式

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这是最关键的部分。 不同于 RL 的 Reward Maximization， 这里完全是监督学习 (Supervised Learning) 的损失函数。 假设我们从经验池中采样到一条轨迹片段： （， st + 1， ．， 5 衅。 我们将 + 设为目标 g， 则真实标签为动作和距离

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

最大化给定目标下生成实际动作的似然概率 （即最小化负对数似然 NLL)：）、 D [log （》 st,g = st+k St ,at 'St+k 如果动作是离散的： 这就是标准的 Cross-Entropy Loss （交叉熵）。 如果动作是连续的： 这就是 MSE Loss （假设方差固定） 或 Gaussian NLLO

如果有距离预测，则还有距离预测损失：

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

让网络预测和 g 之间的时间差 Cdist@） = E [ 目场@， 5-） 2

3.网络架构

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

普通的 ResNet 在 RL 中很难训练极深。 论文提出了一种改进的残差块结构， 结合了 Spectral NormaIization （谱归一化） 和 Gating （门控） 机制。 标准 ResNet 块： 论文采用的 Gated Pre-Act Block: 这种结构主要为了保证信号传播的稳定性 (Lipschitz 连续性 ), 防止梯度爆炸。 CI+I = 昀 + · a(BN(W2+(BN(WICl))))

### （二）核心思想

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在传统观点中， 层数增加 = 特征抽象程度增加。 在这篇论文中， 作者认为： 对于目标导向 (Goal-Conditioned) 的任务， 网络的层数对应着 “ 思考的时间 "。 如果任务是 “ 从 A 走到 B"， 网络越深， 它在内部进行的非线性变换次数越多。 作者发现， ℃ 00 层的网络在没有任何显搜萦算法 (ØMCTS 的筲况下， 学会了在网络内部 ' ' 模拟 ' ' 这种搜索。

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

· Layer 10 还在想： " 前面有个墙 "。 · Layer 500 还在推演： " 绕过这个墙后面好像是死路 "。 · Layer 900 终于得出结论： " 必须先退回三步， 然后走右边 "。

### （三）为什么加深网络不适合传统RL算法？

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 核心机制错位： 深度即规划 (Depth is Planning) 这是最本质的原因。 这篇论文的核心论点是： 深层网络的层级计算过程， 实际上是在模拟从 st 到 g 的路径搜索。 在 CRL/Goal-Conditioned 场景下： 输入是 （g)o 网络的第一层看到起点和终点， 中间的层可以被视为在潜空间 (Latent Space) 中一步步 " 推演 " 如何从 st 变换到 go 每一层都在缩短当前状态表征与目标状态表征之间的距离。 这种结构天然契合 " 规划 " 的定义。 在 SAC/TD3 场景下 · 输入仅为 （目标隐含在 Rewa rd 函数中， 网络看不见）。 网络无法在内部构建 " 路径 "， 因为它不知道要往哪里走。 在这种情况下， 增加深度并不能带来 " 规划 " 收益， 只能增加特征提取的抽象程度。 但在简单的 MuJoCo 或控制任务中， 提取特征并不需要 ℃ 00 层， 3 一 5 层足矣。 多余的 995 层成了优化的累敖 / 丶 ． 口 ． 0

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 监督信号的稀疏性与崩塌 (Signal Complexity vs. Capacity) 这涉及到信息论中的容量匹配问题。 CRL 的监督信号 ． CRL 通常基于对比学习或未来状态预测。 例如： " 预测和 st+k 的相对距离 “ 或 " 判断 5 ／ 是否是 5 的后继 “。 这种亻苎号是高维、 稠密且结构化的。 它可以有效支撑起 ℃ 00 层网络的巨大参数量， 防止过拟合。 SAC/TD3 的监督信号 ． 这类算法的核心是学习一个价值函数 Q(), a)o 输出是一个标量 (ScaIar)0 用一个 ℃ 00 层、 数百万参数的巨型网络去拟合一个单一的标量数值， 极易发生秩崩塌 (Rank Collapse)0 即： 深层网络的特征空间坍缩成一维， 导致网络失去了区分不同状态的能力。 此外， Q-Learning 的 Target 本身带有高方差 (Bootstrapping 带来的误差累积）。 让一个 ℃ 00 层的网络去拟合一个充满噪声的标量目标， 只会放大噪声， 导致梯度爆炸或发散。

## 三、为什么目标导向方法目前仍主要限于机器人等领域？

尽管 GORL 在机器人控制领域非常成功，但在大语言模型中大规模应用面临以下核心挑战：

1.目标的空间无限性与语义模糊性

在机器人领域，目标通常是坐标(x, y, z)；在LLM中，目标是自然语言指令，如要求完成某道题目。语义空间是离散且高维的，很难有一个自动化、不会被hacking的方式将失败的回答重塑为对另一个目标的成功回答（找出这个目标和验证都不容易）。而且，LLM的状态s中已经包含了上下文，事实上隐含了目标，如果强行把目标和过程拆开来反而是自找麻烦。

2.状态表示（State Representation）的难题

LLM的状态s是已生成的Token序列。在文本生成中，定义“距离目标的距离”变得非常抽象。目前的RLVR只看终点（Outcome-based），而GORL强依赖于状态转移过程的建模。

3.算力成本与数据效率

GORL 需要模型在训练时不断改变目标进行探索。LLM 的推理成本极高。在海量目标空间下进行充分探索，其计算量呈指数级增长。目前的 RLVR（如GRPO算法）通过组内相对比较已经非常耗能，GORL的多目标采样会让训练成本更难以承受。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
