---
title: "13.5 DDPG算法"
source_docx: "第2部分 强化学习/13.综合价值与策略的算法/13.5 DDPG算法.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 13.5 DDPG算法

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、核心思想

DDPG算法可以视为DQN的一种改进算法。我们知道，DQN无法用于输出连续动作，是因为它有一个取max的操作。当输出动作有无限多种时，我们无法直接进行“取极大似然”这一操作，或者说无法直接求出这个优化问题的解析解。由此，我们想到训练一个神经网络来学习、输出这个让价值函数取极大的动作。输入是所在状态，输出是动作，这显然是一个策略网络。

由此，它成为了一个结合价值与策略的Actor-Critic架构，但同时又以DQN为基础，后面我们会看到它引入了DQN的经验回放、目标网络等。和REINFORCE、PPO中“输出动作由在概率分布中随机采样”不同，DDPG的输出动作是对概率分布中极大值点的逼近。

## 二、网络架构

DDPG算法引入了“目标网络”思想：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Acto r 网络 （）： 当前策略网络。 输入 5， 输出 ao Critic 网络 (Q+)： 当前价值网络。 输入 5 和 a， 输出 Q(s,a)0 Target Actor 伊伊）： 目标策略网络。 结构同 Actor， 参数滞后更新。 Target Critic (Q+')： 目标价值网络。 结构同 Critic, 参数滞后更新。

两个目标网络的用途后续会介绍。

## 三、数学原理与更新公式

1.Critic的更新（拟合Bellman最优方程）

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Critic 的任务是估算 Q 值。 由于是 Off-policy, 我们使用 TD Erroro · 计算目标值 (Target)： 这里用到了 Target Actor 和 Target Critico 注意： 这里没有 max 操作 （因为动作是连续的， 没法穷举）。 DDPG 认为 Target Actor 输出的动作 / （的近似就是那个最优动作。 · 损失函数： 通过最小化 MSE 来更新 Q+O 1 2

Critic的任务是拟合Q网络，获取环境的价值特征，与自身采取的策略无关，因而损失函数类似于DQN，而没有出现累积奖励J。

2.Actor的更新（确定性策略梯度定理）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这是 DDPG 的精髓。 我们希望 Actor 输出的动作 a = （5） 能够让 Critic 打分 Q(), a) 最高。 目标函数： J(0) = “ 小）） ] 利用链式法则求梯度： V9J 直观解释： E VaQ+(s,a)I v 犁小） Actor 的调整能力 Critic 指出的方向 Critic 告诉 Actor： " 在状态 5， 如果你把动作 a 增大一点， Q 值会增加 0 ． 5。 " （这是 VaQ) 1 ． 2 ． Actor 收到信号， 计算自己的参数 O 该怎么变才能让输出的 a 增大。 （这是 VokD 两者相乘， 直接通过反向传播更新 3 ．

### （四）提高训练稳定性的技术

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

A. 经验回放 (Replay Buffer) 同 DQN0 DDPG 是 Off-poIicy 的， 训练时把数据 （5， a, 50 存入 Buffer， 更新时随机抽取 Batch0 这消除了数据的时间相关性， 提高了样本效率。 B. 软更新 (Soft Update) DQN 的目标网络是每隔 C 步把参数直接复制过去 (Hard Update)。 用软更新 · 伊 TO + （1 一伊 DDPG 觉得这样太剧烈了， 改其中丆是一个很小的数 （如 0 ． 001）。 这意味着目标网络的变化非常缓慢， 像蜗牛一样跟着主网络爬。 这极大地增强了 Critic 训练的稳定性。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

C. 探索噪声 (Exploration Noise) 因为策略是确定性的 （输入 5 必得 a), Actor 自己不会去尝试新动作。 时的动作上手动加噪声 atrain 为了探索， 我们必须在训练原论文使用的是 OU 噪声 (Ornstein-UhIenbeck process), 因为它具有时间相关性 （模拟物理惯性 ), 适合机器人控制。 现在很多实现发现简单的高斯噪声 (Gaussian Noise) 效果也不错。

### （五）算法完整流程

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 初始化： Actor 伟 Critic Q 及其对应的 Target 网络， Q' 循环 (Episode)： 初始化噪声进程。 循环 (Step)： 1 ． 2 ． 3 ． 4 ． 6 ． 8 ． 根据当前状态 5。 选择动作 = (t) + Noiseo 执行动作， 得到， st + 1。 存入 Buffer 月。 采样： 从月中随机采一个 Batch0 5 ． 算目标： 一 + 7Q 气的的）。 更新 Critic: 最小化一 Q(), a))20 7 ． 更新 Actor: 最大化 Q@虽））。 软更新 Target： 伊 TO + （1 一丆） 伊。

### （六）DDPG 的优缺点

1.优点

Off-policy：样本效率高，比PPO快（指需要的交互次数少）。

解决连续控制：填补了DQN在连续动作空间的空白。

2.缺点

对超参数敏感：非常难调（特别是学习率和噪声）。

Q值估计过高：Critic的更新目标实质上和DQN的r+gamma*maxQ(s,a)，由于取max操作，被高估的数值更可能被取到，容易导致结果Q值虚高，误导策略。后来TD3（Twin Delayed DDPG）算法通过用两个Critic取最小值，解决了这个问题。

稳定性不如 PPO：容易收敛到局部最优或震荡。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- C. 探索噪声 (Exploration Noise) 因为策略是确定性的 （输入 5 必得 a), Actor 自己不会去尝试新动作。 时的动作上手动加噪声 atrain 为了探索， 我们必须在训练原论文使用的是 OU 噪声 (Ornstein-UhIenbeck process), 因为它具有时间相关性 （模拟物理惯性 ), 适合机器人控制。 现在很多实现发现简单的高斯噪声 (Gaussian Noise) 效果也不错。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
