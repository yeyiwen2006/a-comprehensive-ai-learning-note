---
title: "13.2 TRPO算法与重要性采样"
source_docx: "第2部分 强化学习/13.综合价值与策略的算法/13.2 TRPO算法与重要性采样.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 13.2 TRPO算法与重要性采样

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、提出背景

在强化学习中，尤其是在策略梯度方法中，我们通过不断更新策略网络（一个概率分布）来让智能体表现得更好。但这里有一个巨大的风险：如果一次更新步子迈得太大，新策略可能会彻底“跑偏”，选择一个很差的动作，进而采样到差的数据，算出不准的梯度，恶性循环，导致训练崩溃、性能急剧下降，并且很难恢复。

## 二、数学表达

在机器学习中，KL散度D_KL(P||Q) = H(P,Q) - H(P)可以表示预测分布Q和标签分布P之间的差异，这里H(P, Q) 是交叉熵，即用 Q 的编码系统去编码 P 的数据所需的平均长度。准确地说，这是前向KL散度，衡量了使用了错误的编码系统（Q）而导致与正确编码系统（P）相比额外多出的信息量，也就是Q偏离P的程度，在旧策略P概率高而新策略Q概率低的时候，惩罚会很大，这保证了新策略覆盖原策略，不会过度收缩（Collapse），避免忽略了真实分布的某些模式。还有一种KL散度是反向KL散度，即D_KL(Q||P)，它在旧策略P概率低而新策略Q概率高的时候，惩罚很大。

将KL散度运用到强化学习中，则是像一个 “稳定器”或“刹车” 。大模型一般均用反向KL散度），因为反向KL散度会对新策略高概率、原策略低概率的区域给出较大惩罚，进而让新的概率空间不跳出预训练后概率较大的空间（如果跳出空间往往表现为输出乱码、不符合正常语义的内容等，即“模式崩塌”）。RL获得的空间往往是预训练的子集（这也可能是RL难以让模型获得预训练完全没见过的技能的原因之一）。

我们知道，KL散度=H(P,Q)-H(P)，H(P,Q)为交叉熵。在TRPO或PPO这类算法中，我们为什么用KL散度而不用交叉熵呢？不是因为梯度不同（它们梯度其实一样），而是因为“标尺”的零点不同。KL散度提供了一个绝对的零点（即KL=0代表没变化），这让我们能设定一个通用的安全阈值（Trust Region）；而交叉熵的“零点”是浮动的（等于旧策略的熵H(P)），导致无法设定一个固定的阈值来限制更新幅度。

TPRO算法可以表达为这样一个优化问题：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

目标函数 (Surrogate Objective)： 策略的数据来估算新策略的表现： maX11111Z e 9 这里的 ． 一是概率比率。 是优势函数。 利用重要性采样 (lmportance Sampling)， 我们可以用旧 S¯uP901d， ～ 丌 9 old 丌 9 5 丌先 a 5 约束条件 (Constraint)： 使用 KL 散度 ()L Divergence) 来衡量两个概率分布的距离： subj ect to 刀丌） 孓是我们要设定的 " 信赖域 " 半径 （也就是我们允许策略变动的最大幅度）。

## 三、目标函数（重要性采样）的解释

TRPO是一个Online Policy，随着策略的更新，从旧策略中采样的数据就无法使用，造成了极大的数据浪费。为了能有效利用旧数据，我们采取了统计学中重要性采样的方法。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设你的任务是统计全国人民的平均收入。 目标分布 P (Target)： 全国人民的分布。 采样分布 Q (Proposal)： 你 （作为采样员） 只能在上海陆家嘴 （金融中心） 街头随枧拉人问收入。 直接算平均值的问题： 如果你直接算陆家嘴路人的平均收入， 结果肯定远高于全国平均水平。 因为你采样的地点 （分布 Q) 偏向富人。 重要性采样的解决方案： 你可以继续在陆家嘴采样， 但算账的时候要加权重： 1 ． 富人 （在陆家嘴很多， 但在全国比例没那么高）： 给他们的收入乘一个小于 1 的权重 （降权）。 2 ． 普通收入者 （在陆家嘴很少， 但在全国比例很高）： 如果你在陆家嘴好不容易逮到一个普通人， 这个样本就太珍贵了， 它代表了全国那一大群人。 给他的收入乘一个大于 1 的权重 （加权）。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设你的任务是统计全国人民的平均收入。 目标分布 P (Target)： 全国人民的分布。 采样分布 Q (Proposal)： 你 （作为采样员） 只能在上海陆家嘴 （金融中心） 街头随枧拉人问收入。 直接算平均值的问题： 如果你直接算陆家嘴路人的平均收入， 结果肯定远高于全国平均水平。 因为你采样的地点 （分布 Q) 偏向富人。 重要性采样的解决方案： 你可以继续在陆家嘴采样， 但算账的时候要加权重： 1 ． 富人 （在陆家嘴很多， 但在全国比例没那么高）： 给他们的收入乘一个小于 1 的权重 （降权）。 2 ． 普通收入者 （在陆家嘴很少， 但在全国比例很高）： 如果你在陆家嘴好不容易逮到一个普通人， 这个样本就太珍贵了， 它代表了全国那一大群人。 给他的收入乘一个大于 1 的权重 （加权）。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设你的任务是统计全国人民的平均收入。 目标分布 P (Target)： 全国人民的分布。 采样分布 Q (Proposal)： 你 （作为采样员） 只能在上海陆家嘴 （金融中心） 街头随枧拉人问收入。 直接算平均值的问题： 如果你直接算陆家嘴路人的平均收入， 结果肯定远高于全国平均水平。 因为你采样的地点 （分布 Q) 偏向富人。 重要性采样的解决方案： 你可以继续在陆家嘴采样， 但算账的时候要加权重： 1 ． 富人 （在陆家嘴很多， 但在全国比例没那么高）： 给他们的收入乘一个小于 1 的权重 （降权）。 2 ． 普通收入者 （在陆家嘴很少， 但在全国比例很高）： 如果你在陆家嘴好不容易逮到一个普通人， 这个样本就太珍贵了， 它代表了全国那一大群人。 给他的收入乘一个大于 1 的权重 （加权）。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们需要计算某个函数在目标分布 (@) 下的期望 （比如计算策略丌下的期望回报）： 尸 f 困难： 我们没法从） 里采样 （比如 P 是我们还没训练出来的新策略）， 我们手里只有从另一个分布 (@) （比如旧策略） 里采出来的样本。 变换魔法： 我们在积分里强行乘以 1 （即 Q@))• 把积分符号变回期望符号， 注意现在的分布变成了 Q: E，、 p 了国 ] =、 Q · 这就是重要性权重 (lmportance weight)o

这样，我们就能对旧策略Q采样出来的数据进行“再利用”了。

应用在TRPO中，即：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

凹到 TRPO/PPO 的公式： 丌 bld 丌训@5）

当然，这个公式是在s给定的条件下的期望，还没有考虑s的占用度量会随着策略变化而变化。考虑后，公式如下：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

利用重要性采样变换后： J(0) 困难点： 计算 = E SAJP901d， ～ 丌 9 old 5 old 丌先 a 5 状态分布比率 （被忽略项） 动作分布比率是极其困难的。 因为 5） 这种 " 状态稳态分布 " 是由环境动力学 P （的 5， a) 和 old 策略丌经过无穷多步交互后纠缠在一起决定的。 除非你知道环境的完美模型并解复杂的流方程， 否则根本算不出来。 TRPO 的做法： TRPO 直接强行 々 old 遇到的状态分布还没来得及发生剧烈变化。 " 也就是说， 它假设 · “ 在我更新策略的一瞬间， 我

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

利用重要性采样变换后： J(0) 困难点： 计算 = E SAJP901d， ～ 丌 9 old 5 old 丌先 a 5 状态分布比率 （被忽略项） 动作分布比率是极其困难的。 因为 5） 这种 " 状态稳态分布 " 是由环境动力学 P （的 5， a) 和 old 策略丌经过无穷多步交互后纠缠在一起决定的。 除非你知道环境的完美模型并解复杂的流方程， 否则根本算不出来。 TRPO 的做法： TRPO 直接强行 々 old 遇到的状态分布还没来得及发生剧烈变化。 " 也就是说， 它假设 · “ 在我更新策略的一瞬间， 我

我们可以看到，重要性采样必须以限制Q相对于P的偏差为前提。否则，就好比你在陆家嘴（Q）好不容易抓到一个极度罕见的贫困户，根据公式，这个人的样本权重会变成几万倍，这一个样本就会主导整个梯度的计算，导致神经网络的更新方向剧烈波动，模型直接训练崩溃。

## 四、优化问题的求解

TPRO可以被写成下面的优化问题：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

maximizeo 力@） = E 丌 0 subject to 刀“@“）

求解如下：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一步： 泰勒展开 （近似化） 我们假设参数更新量为 = O 一 001d0 由于我们需要限制很小 （信赖域 ), 我们可以对目标函数和约束进行泰勒展开。 1 · 对目标函数） 做一阶展开 （线性化）： 注意： 力@。司是常数， 不影响优化。 Vo 力。 司其实就是标准的策略梯度 (Policy Gradient), 我们记为向量 go 近似后目标： maximize g £

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 对约束条件刀五做二阶展开 （二次化）： 刀人 d 忙） 刀五先 ld。 司 + V 刀 + 一 V2 刀人第一项： 刀刀@壶忙。 司 = 0。 第二项： KL 散度在两个分布相等时取极小值， 导数为 0， 所以 V 刀人 = 0。 第三项： V2 刀人是 KL 散度的二阶导数矩阵 (Hessian Matrix), 也就是费雪信息矩阵 (Fisher lnformation Matrix, 同 M）， 我们记为 H0 近似后约束： <

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第二步： 求解析解 （拉格朗日乘子法） 现在问题变得非常干净了： maxlmlzex subject tO 1 一 cTHc < 2 这是一个经典的二次约束线性规划问题。 我们可以构建拉格朗日函数： 对求导并令其为 0： g 一 = 0 亠， Hc 一于是我们得到了更新方向 （还没有确定步长）： 1

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这就是著名的 “ 自然梯度 (Natural Gradient)" 方向。 它不是沿着普通的梯度 g 走， 而是用曲率矩阵 H 的逆去修正了方向。 接下来确定步长 （缩放系数）。 将 = ßH- g 代入约束条件 1 cTI--Ic =

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

25 最终的参数更新公式： 25

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第三步： 数值求解 （共轭梯度法 CG) 虽然公式推出来了， 但工程上根本没法直接算。 痛点： H 是 Hessian 矩阵， 维度是 N × N (N 是参数量）。 如果参数有 ℃ 0 万， H 就有 1 万亿个元素， 内存直接爆炸， 更别提求逆矩阵 H-I 了。 TRPO 的核心魔法： 共轭梯度法 (Conjugate Gradient, CG)O 我们需要计算的是向量 s = H-lg, 这等价于求解线性方程组： Hs = g CG 算法的特点是： 求解 Hs = g 不需要显式地写出矩阵 H, 只需要能够计算 “ 矩阵 ． 向量乘积 " (Matrix-Vector Product, MVP) 即可。 也就是说， 只要你能给我一个函数， 输入向量输出 Hv， 我就能迭代解出 so

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

H 是 KL 散度的二阶导数。 计算 Hv 不需要真的算二阶导， 有一个极其高效的技巧 (Pearlmutter Trick)： Hv = V9(VoDKL · 1 ． 2 ． 3 ． 先算 KL 散度的梯度 Vo 刀加算这个梯度和向量省的点积 （得到一个标量）。 再对这个标量求一次梯度。 在 PyTo rc h /TensorFlow 这种自动微分框架下， 这只需要做两次反向传播 (Backward), 速度非常快， 且不需要存储矩阵。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第四步： 线性搜索 (Line Search) _ 一最后的保险我们前面做了一阶和二阶近似。 但是， 当步子迈出去之后， 真实的 KL 散度可能会超过或者真实的目标函数反而下降了。 为了保证万无一失， TRPO 最后会做一个回溯线性搜索 (Backtracking Line Search)： 1 ． 2 ． 3 ． 4 ． 5 ． 算出理想更新量。 设实际更新量 AO = aJc* （初始 a = 1）。 检查两个条件： 条件 A： 刀人无@“ + (O) 孓 （真的满足约束吗？） 条件 B： + (O)。 司 （真的提升目标了吗？） 如果不行， 就让 j + 1 （比如 a 变成 0 ． 5， 0 ． 25 ． “ ), 缩短步长再试。 直到满足条件， 才真正更新参数。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

总结 TRPO 的求解流程 1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 准备数据： 用旧策略收集一批轨迹。 算梯度： 计算策略梯度 go 算方向 (CG)： 利用 " Hessian 一向量积 " 技巧， 跑 ℃ 步共轭梯度算法， 解出 s 算系数： 代入公式算出最大步长系数线性搜索 · 沿着 ßs 方向试探， 找到满足约束的最大步长。 更新： “ = 先 + final—stepo 这就是为什么 TRPO 被称为 " 精确但昂贵 “： 它为了算准这一步， 动用了二阶近似、 逆矩阵估计 (CG) 和线性搜索三把牛刀。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
