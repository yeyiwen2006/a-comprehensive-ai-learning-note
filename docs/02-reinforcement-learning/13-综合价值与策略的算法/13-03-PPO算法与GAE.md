---
title: "13.3 PPO算法与GAE"
source_docx: "第2部分 强化学习/13.综合价值与策略的算法/13.3 PPO算法与GAE.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 13.3 PPO算法与GAE

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

PPO由TRPO衍生而来。TRPO提出了运用重要性采样、限制更新范围等，但它作为一个带约束的优化问题，求解十分复杂，是一个“精确但昂贵”的算法，在大模型中并不实用。我们想到，可以将约束条件直接写入目标函数。具体而言，有两种方法，也因此产生了PPO的两种变体。

## 一、PPO-Penalty

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

目标函数如下： T9 at St St 》 TO · St T901d at St 第一项： 就是 TRPO 中的代理目标 (Ratio × Advantage)， 负责让策略变好。 第二项： KL 散度惩罚项。 刀人衡量新旧策略分布的差异。 0 (Beta)： 惩罚系数。 0 越大， 越不允许策略剧烈变化。

这里第一项同TRPO，即找到一个新策略，让获得奖励在新策略下的期望最大；第二项的前向KL散度主要意在确保旧策略π_old概率高的区域，如预训练中学习到的常见的人类语言模式和世界基础知识，新策略π_new概率也高，避免模型为了获取更高的Reward而丢弃预训练中学习到的知识而发生“模式崩塌”；而对于一些旧策略概率较低的高难度问题解法，新策略在提高概率的同时不会受到过度的惩罚。这样可以保证每次更新都在一个可信的“信任区域”内进行。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

PPO-Penalty 的精髓在于它不是用固定的 0， 而是根据每一轮更新后的实际 KL 散度值 d = 刀五刀 （丌。 丌，， “） 动态调整： 设定一个目标 KL 值 d （比如 0 ． 01）： targ 1 ． 如果 d < dtarg/1.5 （策略变动太小了）： 说明我们太保守了， 于是减小 （比如 / 2 ), 让下一轮步子迈大占 2 ． 如果 d > dtarg × 1.5 （策略变动太大了）： 说明我们太激进了， 于是增大 （比如 × 2), 让下一轮受到的惩罚更重。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

PPO-Penalty 的精髓在于它不是用固定的 0， 而是根据每一轮更新后的实际 KL 散度值 d = 刀五刀 （丌。 丌，， “） 动态调整： 设定一个目标 KL 值 d （比如 0 ． 01）： targ 1 ． 如果 d < dtarg/1.5 （策略变动太小了）： 说明我们太保守了， 于是减小 （比如 / 2 ), 让下一轮步子迈大占 2 ． 如果 d > dtarg × 1.5 （策略变动太大了）： 说明我们太激进了， 于是增大 （比如 × 2), 让下一轮受到的惩罚更重。

## 二、PPO-Clip（主流）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

T9 at St 丌先 atl St CLIP

上式中A_t表示动作的相对价值（下面会讲），我们希望新策略能让价值在该策略下的期望最大，经重要性采样可得上式，J对θ的导数即为此时参数网络更新的梯度。A_t大于0时，我们会希望pi_θ更大，这等价于让r_t更大。E_t是按旧策略采样获得的数据。

在工程中，我们先按照pi_old走若干步，然后开始若干次更新。在每一轮策略更新中固定pi_old仍为采集数据时用的策略（在代码实现里就是对过去的样本直接算术平均），重复若干次下述操作：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当 > 0 （动作是好的）： 原本希望 0 越大越好 （增加概率）。 Clip 限制： 一旦 > 1 +e, 公式这就变成了 （1 + e)At, 这是一个常数， 导数为 0。 结果： 策略更新停止。 这防止了因为某个动作侥幸得高分， 就把它的概率提得无限高。 当 < 0 （动作是坏的）： 原本希望 0 越小越好 （减小概率）。 一 e， 公式这就变成了 （1 一 e)At, 导数为 0。 Clip 限制： 1 结果： 策略更新停止。 这防止了因为某个动作导致负分， 就过度毁坏策略结构。

一旦超出范围，目标函数J直接被“抹平”，以最大化目标函数为目的的本轮策略更新“失去动力”，自然就停止了。

## 三、为什么PPO-Clip效果更优？

1.稳定性：PPO-Penalty的β调整虽然也是动态的，但有时候调整滞后，或者震荡，导致训练不稳定。而PPO-Clip的截断是硬性的、逐个样本生效的，极其稳健。

2.计算量：PPO-Penalty需要计算pi_new和pi_old间的 KL 散度（虽然比TRPO的二阶导简单，但也需要算 log 概率的差值），而且两个概率都在变化，故计算开销大。PPO-Clip只需要做简单的乘法和比大小（min, clamp），计算开销低。

3.超参数：PPO-Clip的epsilon通常固定 0.2 就能适配大多数环境（Atari, MuJoCo,机器人）。PPO-Penalty 的d_targ和beta初始值则比较敏感。

## 四、损失函数

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

公式 · 力忉忉 I CLIP Actor loss + C1 力 Critic loss 一 C2 S Entropy

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

注意符号的微妙之处 （假设优化器是做梯度下降 Minimize)： ． 我们希望最大化奖励。 为了用梯度下降， 我们加负号变成一 LCLIP。 ／ 丆： 我们希望最小化预测误差 (MSE)0 所以直接是 +。 S （熵）： 我们希望最大化熵 （鼓励探索）。 为了用梯度下降， 加负号变成一 SO 把它们加在一起， PyTorch/TensorFlow 的优化器就可以一键优化所有目标。

反向传播时，Loss的不同项就会将各个梯度分别传给不同模块的对应变量：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

输入图像一一 > [ 共享 CNN 层 [ Actor 头 ] 输出动作概率一 > 提取出的特征向量 [ Critic 头 ] 输出价值 V

## 五、优势函数A_t：GAE（Generalized Advantage Estimation）

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Advantage （）： 衡量动作比在该状态下 " 平均表现 “ 好多少。 理论定义为 Q(st, （(t)。 但在实际计算中， 我们无法直接得知 Q, 只能通过采样来估计。 情况一： 单步估计 (l-step estimation) 如果我们只看一步未来， 那么动作的 Q 值可以被估计为昀 +7V(st+1)0 此时， 的估计值直接等于 TD Error: zQ(st,at)

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

Advantage （）： 衡量动作比在该状态下 " 平均表现 “ 好多少。 理论定义为 Q(st, （(t)。 但在实际计算中， 我们无法直接得知 Q, 只能通过采样来估计。 情况一： 单步估计 (l-step estimation) 如果我们只看一步未来， 那么动作的 Q 值可以被估计为昀 +7V(st+1)0 此时， 的估计值直接等于 TD Error: zQ(st,at)

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

PPO 默认使用 GAE 来计算优势函数。 这是一个非常精妙的设计， 它将 TD Error 进行了指数加权移动平均。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

其中 /\ (Lambda) 是一个范围在 [ 0， 1 ] 之间的超参数。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

通过调节 TD Error 的作用： 当 = 0 时 （高偏差， 低方差）： t = 0 + 7V(St+1) 此时完全等于当前的单步 TD Erroro 优点： 方差小 （只依赖一步随机奖励）。 缺点： 亻扁差大 （严重依赖 Critic 网络 V(st+1) 的准确性， 如果 Critic 没训练好， 估计就是错的）。 当 ,\ = 1 时 （无偏差， 高方差）： 此时公式展开后， 中间的项会相互抵消， 最终变成了 Monte Carlo Returns （真实回报） 减去 Baselineo 优点： 亻扁差小 （真实回报是最准确的 “ 事实 "）。 缺点： 方差极大 （因为累加了每一步环境的随机性）。

那这里A_t是用什么动作计算得到的呢？后面会提到，PPO实际运行时先用老策略走一定步数，再统一计算途中各个t时刻的优势函数，再更新。因此A_t就是用这些步数的实际动作算出来的。

## 六、在工程中的运行流程（如OpenAI Baselines或Stable Baselines3）

在工程中，走一步更新一次意味着串行计算，GPU利用率极低。基于我们已经通过重要性采样保证了数据的可复用性，我们往往以一个“大循环”为单位：每一次都一次性走若干步（如2048步），收集一批数据，运用这批数据训练更新策略，再扔掉这批数据。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

初始化： 初始化 Actor 网络和 Critic 网络。 大循环 (For each iteration)： 1 ． 2 ． 数据收集阶段 (lnteraction)： 使用当前策略在环境中运行丆步 （例如 2048 步）。 收集轨迹数据： {，，， + 1， log 丌。 1 鼠 lst)。 利用 Critic 网络计算状态价值 V(st)0 优势计算阶段 (Advantage)： · 计算 TD Error: = + 7 （st + 1） 一 V(st)0 · 计算 GAE (Generalized Advantage Estimation) At。 这是方差之间做平衡。 一个递归公式， 用来在偏差和

注：在收集数据的过程中，一般我们会顺便把V(s_t)算出来，这是因为在 PPO 的工程实现中，Actor和Critic通常共享部分底层网络（CNN/MLP）。当让Actor选择动作a_t时，必须把s_t喂进网络。既然数据已经流过网络了，顺便让Critic输出一下V(s_t)，几乎不增加额外的计算成本。如果不存下来，等到训练阶段，还得把s_t重新喂一遍网络来得到V(s_t)用于计算优势，重复计算，浪费GPU。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

初始化： 初始化 Actor 网络和 Critic 网络。 大循环 (For each iteration)： 1 ． 2 ． 数据收集阶段 (lnteraction)： 使用当前策略在环境中运行丆步 （例如 2048 步）。 收集轨迹数据： {，，， + 1， log 丌。 1 鼠 lst)。 利用 Critic 网络计算状态价值 V(st)0 优势计算阶段 (Advantage)： · 计算 TD Error: = + 7 （st + 1） 一 V(st)0 · 计算 GAE (Generalized Advantage Estimation) At。 这是方差之间做平衡。 一个递归公式， 用来在偏差和

注：优势是在走完动作步后、更新策略梯度前统一计算的，GAE是TD Error加权后，再一直从t累加到2048得到的。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 ． 4 ． 优化更新阶段 (Optimization)： 重要： 此时丌固定不动 （作为分母）， 我们优化的是新的 （作为分子）。 · 将收集到的个数据打乱， 分成多个 Mini-batch （例如每个 batch 64 个数据）。 Epoch 循环 （例如重复 10 次）： 对每个 Mini-batch: 1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 同步策略： T9(aIs) 计算新概率比率 0@） = 丌司 d 计算 Clip 损失 LCLIP 计算 Critic 的价值损失 = （（(t) 一 V） 2 target。 计算熵正则项 S （鼓励探索）。 总 LOSS： = 一力 C 刀 7 尸 + CI 丆一 C2S （注意梯度上升 / 下降符号）。 反向传播更新 O 和夯本轮更新结束， 令 @。 · 清空数据缓冲区， 进入下一次大循环。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 ． 4 ． 优化更新阶段 (Optimization)： 重要： 此时丌固定不动 （作为分母）， 我们优化的是新的 （作为分子）。 · 将收集到的个数据打乱， 分成多个 Mini-batch （例如每个 batch 64 个数据）。 Epoch 循环 （例如重复 10 次）： 对每个 Mini-batch: 1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 同步策略： T9(aIs) 计算新概率比率 0@） = 丌司 d 计算 Clip 损失 LCLIP 计算 Critic 的价值损失 = （（(t) 一 V） 2 target。 计算熵正则项 S （鼓励探索）。 总 LOSS： = 一力 C 刀 7 尸 + CI 丆一 C2S （注意梯度上升 / 下降符号）。 反向传播更新 O 和夯本轮更新结束， 令 @。 · 清空数据缓冲区， 进入下一次大循环。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设参数设置如下： n-steps （缓冲区大小） = 2048 batch_size （小批次大小） = 64 n-epochs （复用次数） = ℃ 在一个 “ 大循环 " 里， 反向传播发生的次数是： 数据分批： 2048 个数据被分成 2048 / 64 = 32 个 Mini-batch0 1 ． 一轮遍历： 这 32 个 Mini-batch 会被依次喂给 GPU, 产生 32 次反向传播。 2 ． 多轮复用： 我们要重复跑 ℃ 个 Epoch0 3 ． 总次数： 32 × 10 = 320 次反向传播。 4 ．

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
