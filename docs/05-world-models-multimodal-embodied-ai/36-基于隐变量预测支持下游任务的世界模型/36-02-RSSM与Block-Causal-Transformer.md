---
title: "36.2 RSSM与Block-Causal Transformer"
source_docx: "第5部分 世界模型、多模态生成与具身智能/36.基于隐变量预测支持下游任务的世界模型/36.2 RSSM与Block-Causal Transformer.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 36.2 RSSM与Block-Causal Transformer

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、RSSM

### （一）模型架构

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在构建环境动力学模型时， 通常有两种极端： 1 ． 2 ． 纟屯确定性模型 （如标准 RNN): 状态转移是完全确定的。 这种模型容易出现的一个致命缺陷是， 它无法捕捉环境动力学中固有的不确定性和多种可能的未来 (Multiple futures)。 如果用于规划， 规划器 (Planner) 会很容易利用这种确定性模型的不准确性 （即过拟合到单一的错误未来） 纟屯随机性模型 （如传统 SSM): 每一次状态转移都包含从概率分布中采样。 虽然这能建模不确定性， 但纯粹的随机转换使得模型极难在跨越多个时间步的情况下可靠地记忆佇串口一 0

而且，如果环境存在随机性，确定性网络往往会输出所有可能未来的“模糊平均值”。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在构建环境动力学模型时， 通常有两种极端： 1 ． 2 ． 纟屯确定性模型 （如标准 RNN): 状态转移是完全确定的。 这种模型容易出现的一个致命缺陷是， 它无法捕捉环境动力学中固有的不确定性和多种可能的未来 (Multiple futures)。 如果用于规划， 规划器 (Planner) 会很容易利用这种确定性模型的不准确性 （即过拟合到单一的错误未来） 纟屯随机性模型 （如传统 SSM): 每一次状态转移都包含从概率分布中采样。 虽然这能建模不确定性， 但纯粹的随机转换使得模型极难在跨越多个时间步的情况下可靠地记忆佇串口一 0

RSSM采用了类似LSTM的方法，将隐状态（历史记忆）和反映当前状态的潜变量分开，从而结合了二者的优点。在POMDP（部分可观测马尔可夫决策过程）中：

历史记忆h_t作为隐状态，其转移用确定性规则：由对t-2时刻及以前的记忆h_t-1、t-1时刻的状态潜变量s_t和t-1时刻的动作a_t-1经过函数得到。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 确定性状态模型 (Deterministic state model) 物理意义： 用于汇总历史信息并维持跨越多个时间步的确定性记忆。 拟合方式： 通常由一个循环神经网络 (RNN, 如 GRU) 拟合。

利用包含了t-1时刻及以前状态和动作的历史记忆h_t预测世界状态潜变量s_t则随机采样：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 ． 随机状态模型 / 先验分布 (Stochastic state model / Prior) ～ st ht) 物理意义： 在不观察当前真实图像的情况下， 仅凭过去的记忆预测当前可能的状态分布。 它为模型引入了不确定性， 以刻画多种可能的未来。 拟合方式： 由一个多层感知机 (MLP) 拟合， 输出对角高斯分布的均值和方差。

而实际上世界状态的潜变量s_t（也就是获得当前真实观测o_t后的“预测正确答案”）：（注：理论上可用贝叶斯计算，但涉及不可求的积分，故直接用神经网络拟合）

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 ． 近似后验模型 / 变分编码器 (Approximate posterior / Variational Encoder) 物理意义： 结合过去的确定性记忆和当前的真实高维像素观测， 推断出当前最准确的潜在状态分布。 拟合方式： 由卷积神经网络 ℃ (N) 提取图像特征后， 接多层感知机 (MLP) 输出对角高斯分布的均值和方差。

当我们预测出s_t后，可基于h_t和s_t生成像素图像o_t：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

4 ． 观测重构模型 (Observation model / Decoder) 物理意义： 验证潜在状态是否包含了足够的环境信息。 通过潜在状态还原出高维的像素图像。 拟合方式 · 由反卷积神经网络 (Deconvolutional Neural Network) 拟合。

以及预测奖励：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

5 ． 奖励预测模型 (Reward model) 0 ～， st) 物理意义： 预测在当前潜在状态下能获得的标量奖励， 这是规划器 (Planner) 在潜在空间中评估动作好坏的唯一依据。 拟合方式： 由多层感知机 (MLP) 拟合。

### （二）工作流

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 1： 数据采样 L+k B 从经验回放池 D 中均匀随机抽取批量大小为 B、 长度为的序列片段 {@，， t=k 1 步骤 2： 状态展开与分布计算 (Forward Pass) 对于序列中的每一个时间步 = 1 · ． ·： 1 ． 2 ． 3 ． 4 ． 执行确定性状态转换： 将上一步的一 1、 一 1 （通过重参数化采样得到） 和动作一 1 输入 RNN 不得到当前的确定性状太五计算先验分布 p: 将输入 MLP, 输出当前先验高斯的均值和方差两 “， 丐 7 7、。 计算后验分布 q: 将当前的图像通过 CNN 提取特征， 结合输入到另一个 MLP, 输出后验高斯的均值和方差 “， 丐 “。 重参数化采样： 从后验分布 （“， %ost) 中采样出潜在向量。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 3： 计算重构与预测将和采样得到的输入反卷积解码器预测重构图像， 并输入奖励模型预测标量奖励步骤 4： 损失计算 (Loss Computation) 1 ． 计算重构图像与真实图像的 MSE （重构损失）。 2 ． 计算预测奖励与真实奖励的 MSEO 3 ． 计算先验分布 p 与后验分布 q 之间的 KL 散度 （如果使用 Latent Overshooting, 还需要并行计算并累加跨越多步的先验与当前后验的 KL 散度）。 步骤 5： 参数更新 (Backpropagation) 将所有时间步的损失累加求平均得到总损失 £）， 使用优化器 （如 Adam) 计算梯度， 更新构成这 5 个函数的所有神经网络参数 0 艹 O 一 aVoC(O)。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 3： 计算重构与预测将和采样得到的输入反卷积解码器预测重构图像， 并输入奖励模型预测标量奖励步骤 4： 损失计算 (Loss Computation) 1 ． 计算重构图像与真实图像的 MSE （重构损失）。 2 ． 计算预测奖励与真实奖励的 MSEO 3 ． 计算先验分布 p 与后验分布 q 之间的 KL 散度 （如果使用 Latent Overshooting, 还需要并行计算并累加跨越多步的先验与当前后验的 KL 散度）。 步骤 5： 参数更新 (Backpropagation) 将所有时间步的损失累加求平均得到总损失 £）， 使用优化器 （如 Adam) 计算梯度， 更新构成这 5 个函数的所有神经网络参数 0 艹 O 一 aVoC(O)。

### （三）损失函数

损失分为三部分：仅有过去记忆时对现在潜变量的预测p和包含过去记忆与现在观测时的真实现在潜变量q之间的KL散度，体现的是预测下一时刻的潜变量和真实下一时刻的潜变量的差距，反映预测下一时刻状态的能力；图像重构损失反映由潜变量重构像素的能力；奖励损失反映由潜变量预测奖励的能力。表达式如下：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

） [lnp(otlst) + lnp(rtlst)] + [ “ 〗 5 小， -1 q(StlO<t,a<t

这里o_t和r_t均满足高斯分布，故这两项等价于MSE Loss。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

由于标准目标只通过一步 KL 散度训练随枧路径， 模型在多步预测时容易发散。 Overshooting 将约束扩展到了距离为 d 的所有未来时间步： Ot， 曰、 s 九 00 g Latent 物理意义： 它不仅要求 " 由 t 一 1 步预测的步先验 “ 要与后验对齐， 还要求 " 由一 2 步、 t 一 d 步连续多步推演得出的步先验 " 也要与当前的后验对齐。 这种在潜在空间中的一致性正则化， 极大增强了模型在不依赖真实观测时的长期演化能力。

## 二、基于RSSM的应用

### （一）PlaNet：基于世界模型的搜索规划

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 外层循环： 模型训练与数据收集 (Algorithm 1） 初始化： 使用随机动作收集少量的初始种子回合 (Seed episodes) 存入经验回放池。 模型拟合： 从中随机抽取数据块， 利用上述的变分下界公式 （如 Equation 3） 计算损失， 并通过梯度上升更新 RSSM 的参数 0。 环境交互 · 使用更新后的模型， 通过规划算法 （见下文 CEM) 选择动作， 在动作上加入高斯探索噪声， 与环境交互并将新经验存入。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 内层循环： 基于 CEM 的动作规划 (Algorithm 2） 在每一个时间步， 智能体不使用策略网络 (Policy Network) 输出动作， 而是通过交叉熵方法 (Cross Entropy Method, CEM) 和模型预测控制 (MPC) 实时搜索最优动作序列：） ～ Normal(), 伊， 用于表初始化信念： 构建一个随时间变化的正态分布信念 q(at•t+H 示在规划视界 (Horizon) H 内的最佳动作序列。 迭代优化 · 1 ． 从当前信念中采样 J 个候选动作序列。 2 ． 将这些序列输入到学习好的 RSSM 模型中， 仅在潜在空间中推演未来状态， 并计算预期奖励总和月仂。 3 ． 挑选出奖励最高的 K 个动作序列。 4 ． 使用这个优秀序列的均值和方差重新拟合动作信念分布。 执行与重规划： 经过次迭代后， 提取最终信念均值的第一个动作去执行。 由于采用了 MPC 枧制， 接收到新观测后， 动作序列的信念会重置为零均值和单位方差， 以避免陷入局部最优

### （二）Dreamer 3：利用世界模型进行RL训练

1.世界模型

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Ste p 1： 世界模型学习 (World Model Learning) 世界模型通过自动编码技术， 将复杂的感知输入 （如图像） 压缩成紧凑的潜在表示。 · 它被实现为一个循环状态空间模型 (RSSM)0 首先， 编码器将感知输入映射为随机表示： ～ （，）。 然后， 动力学预测器根据之前的状态预测未来的表示： ～）。 通过这种方式， 世界模型学会了理解环境的内在结构， 并能在隐空间中 " 想象 " 未来的状态演变。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在数学上， RSSM 是一种结合了确定性循环神经网络 (RNN) 和随机状态变量的生成式模型。 在时间步， 定义动作、 观测输入、 奖励以及回合继续标志 0 e { 0， 1 }。 + 4

2.Critic

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Ste p 2： 评论家学习 (Critic Learning) · 评论家和演员完全在世界模型 " 想象 " 出的抽象轨迹中进行学习， 而无需直接与真实环境交互。 · 评论家接收模型状态些 {， }， 并学习评估该状态的价值。 为了考虑长期收益， 评论家预测的是回报分布的期望值， 并使用自举 (bootstrapped) 的 ,\ 一回报来整合预测的奖励和价值。 Ste p 3： 演员学习 (Actor Learning) 演员的目标是选择能够最大化回报的动作， 同时通过熵正则化 (entropy regularizer) 来鼓励探索。 它通过策略梯度方法 (Reinforce estimator) 在想象的轨迹上进行优化。

3.Actor

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Ste p 2： 评论家学习 (Critic Learning) · 评论家和演员完全在世界模型 " 想象 " 出的抽象轨迹中进行学习， 而无需直接与真实环境交互。 · 评论家接收模型状态些 {， }， 并学习评估该状态的价值。 为了考虑长期收益， 评论家预测的是回报分布的期望值， 并使用自举 (bootstrapped) 的 ,\ 一回报来整合预测的奖励和价值。 Ste p 3： 演员学习 (Actor Learning) 演员的目标是选择能够最大化回报的动作， 同时通过熵正则化 (entropy regularizer) 来鼓励探索。 它通过策略梯度方法 (Reinforce estimator) 在想象的轨迹上进行优化。

4.损失函数

（1）预测损失

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 预测损失 Cpred cpred(+) 些一 ln 鼠， (t) 一 lnp+ （0， (t) 一 ln 鼠， ht) 这部分通过负对数似然， 迫使模型准确地重构观测输入、 奖励和回合标志。 + 2 2 · 动力学损失 £， ()L 平衡与空闲位） 0 “ 同些 max(), KL[sg(w(ztIht, 司） 目 lht)]) 其核心目的是让动力学预测器逼近编码器给出的后验分布。 这里的精妙之处在于使用了 （即 stop-gradient, 停止梯度） 操作。 这表示在拉近两个分布的距离时， 只更新动力学预 sg 0 测器的参数， 而不影响编码器提取特征的方式。 同时， 公式外围包裹的 max(l, 一） 引入了 * * 空闲位 (Free bits) * * 技术： 当 KL 散度下降到 1 nat L44 bits 以下时， 模型就会停止在这项损失上的惩罚， 避免学到过于退化的平凡动力学， 从而将学习重点转移回预测损失上。 + 4

（2）动力学损失

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 预测损失 Cpred cpred(+) 些一 ln 鼠， (t) 一 lnp+ （0， (t) 一 ln 鼠， ht) 这部分通过负对数似然， 迫使模型准确地重构观测输入、 奖励和回合标志。 + 2 2 · 动力学损失 £， ()L 平衡与空闲位） 0 “ 同些 max(), KL[sg(w(ztIht, 司） 目 lht)]) 其核心目的是让动力学预测器逼近编码器给出的后验分布。 这里的精妙之处在于使用了 （即 stop-gradient, 停止梯度） 操作。 这表示在拉近两个分布的距离时， 只更新动力学预 sg 0 测器的参数， 而不影响编码器提取特征的方式。 同时， 公式外围包裹的 max(l, 一） 引入了 * * 空闲位 (Free bits) * * 技术： 当 KL 散度下降到 1 nat L44 bits 以下时， 模型就会停止在这项损失上的惩罚， 避免学到过于退化的平凡动力学， 从而将学习重点转移回预测损失上。 + 4

（3）表示损失

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 · 表示损失 £， “ crep(+) 些 max(), KL[w()t 在， 目 sg(p+(±tlht))]) 与上一个损失相反， 表示损失的梯度方向被限制在了编码器上。 它的目的是反向规范编码器的行为， 促使它提取出来的隐状态变得更加 " 有规律 "、 更容易被动力学模型预测。 通过巧妙运用停止梯度操作和不同的损失缩放 （在论文中设定为 ßpred = 1， “ 一 1 0 0 ． 1), RSSM 成功解决了以往 “ 强正则化在简单图像有效， 但在复杂 3D 环境会抹杀关键细节 " 的进退两难问题， 实现了跨领域的鲁棒性。 + 2

## 三、结合并行扫描的状态空间模型

### （一）核心算法

RSSM的问题在于，当前时刻的隐状态依赖于上一时刻的输出，且时间步传递中存在非线性激活函数，必须先算t=1，再算t=2，以此类推，时间复杂度为O(L)，成为长轨迹计算的瓶颈。

而S5WM采取如下工作流：

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

步骤 1： 收集与采样数据 (Data SampIing) 从重放缓冲区 (Replay Buffer) 中均匀采样一批历史序列数据 （包含观测、 动作、 奖励 0 和连续性标志 ct)o 步骤 2： 训练状态空间世界模型 (World Model Training) 编码： 编码器 (Encoder) 将观测图像或状态映射为后验随枧表示荔 ～ w(•lot)o 特征融合： 将与上一动作通过多层感知机 (MLP) 融合为一个输入向量。 并行序列建模： 将整个序列的 “ 输入到 S5 块中， 利用上述的并行扫描技术， （并行） * * 计算出整个序列的隐藏状态 “ 和确定性表示 “。 解码与损失计算： 模型使用预测头来预测先验随机状态、 观测在、 奖励和连续性 0。 最后通过最小化包含预测损失、 动态损失 ()L 散度） 和表示损失的联合损失函数 £ （司来更新世界模型权重。 步骤 3： 训 | 练 Actor-Critic (Actor-Critic Training in lmagination) 潜在空间想象： 从真实数据中采样初始状态， 然后在没有任何环境交互的情况下， 利用训练好的世界模型在潜在空间中 " 想象 " 出未来视野长度为 H （如 ℃ 步） 的轨迹。 策略更新： Actor 根据确定性状态和随机状态输出动作； Critic 预测引导的一回报 (Bootstrapped A-returns)0 利用世界模型的完全可微性， 将一阶梯度反向传播给策略网络， 从而优化 Actor 和 Critic 的参数。

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

= 一 1 + But = Cct + 刀

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在传统的 RSSM 中， 后验随机变量的推断不仅依赖于观测仂， 还依赖于具有时序依赖的确定性状态， 即荔 ～ (•， 仂）。 这在架构上锁死了并行性。 S5WM 的设计中， 将后验推断彻底解耦： 完全且仅从当前时刻的观测仂推断得出， 即 ～ （·）。 这样， 所有的 ZI:L 都可以用编码器 ℃ (N) 一口气并行计算完毕， 随后再送入 S5 序列层进行并行扫描。

这里能实现并行的核心在于：

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们将初始元素定义为一个二元组 ek a， b 通过数学推导， 序列状态可以展开为一系列二元操作符 ． 的连续运算。 这个操作符定义为： 这里 @ 代表矩阵乘法， 代表矩阵向量乘法。 此时， 奇迹出现了。 对于一个长序列的计算， 我们不需要像 RNN 那样串行计算： 因为满足结合律， 我们可以将其构建成一棵二叉树来并行计算： 在有足够并行处理器 （如 GPU) 的情况下， 原本 （的串行时间复杂度， 被直接对数化， 降维打击至 （log。

### （二）并行状态重置

RL中，数据往往由多个回合拼接而成。在不断往回递归的过程中，如果已经到了序列最开始，就需要立即截断，防止上一回合状态污染本回合。

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在强化学习中， 数据往往是由多个不同的回合 (Episode) 拼接而成的。 当遇到回合边界时， 模型必须 * * 重置 (Reset) * * 隐藏状态， 以防止上一回合的垃圾信息污染下一回合。 如果在并行计算中遇到截断信号， 普通的 SSM 算法会崩溃， 因为它不知道如何在并行的树状结构中 " 氵月 ． 苓 ．。 作者巧妙地扩展了前面的二元操作符， 引入了连续性预测变量 e { 0， 1 }。 他们将初始元素扩展为三元组： €k a， €k,b' Ck c 并设计了带有条件分支的新并行操作符： aj,a' aj,b' aj,c 这个分段函数的设计绝妙无比。 当 （历史数据） 的佑信息， 只保留当前的丐亻苎息。 if a = 1 时 （代表回合结束 ), 操作符会直接丢弃来自左侧子树这就使得模型在执行 0 （log 的大规模并行计算时， 能够自动且并行地处理序列中任意位置的 “ 重置 " 信号！

## 四、Block-Causal Transformer

RSSM作为RNN，在长程规划上存在劣势。Block-Causal Transformer是Transformer在世界模型中的一种应用范式，在Dreamer 4中就用了这种架构。

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

经典因果 Transformer (Standard Causal Transformer) 它采用的是严格的 Token 级下三角掩码。 对于任意两个 Token 索引 i,j e { 1， 2， .,N}, 掩码定义为： 0， causal if j if < 丿这意味着， 即使 Token 和 Token j 属于同一帧视频， 在序列中排在后面的 Token 也只能单向观测扌非在前面的 Tokeno 这在自然语言中是合理的 （词有先后顺序 ), 但在图像中会迫使模型采用从左上角到右下角的逐块阅读顺序， 严重破坏了同一时刻空间的整体连贯性。 Block-Causal Transformer 它采用的是块级因果掩码 (Block-lower-triangular Mask)0 我们定义映射函数才@） 为 Token 所在的时间步 （帧序号）。 其掩码定义为： 0， block ift(i)） ift(i) <） 在这个矩阵中， 主对角线上是大小为 S × S 的 " 全零矩阵块 " （允许双向注意力 ), 而主对角线右上 block 方则是一。 当 t(i) = 0） 时 （即两个 Token 在同一时间帧内 ), 为

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当用于诸如 Dreamer 4 的交互式动力学建模或物理 PDE 预测时， BCT 的工作流如下： 1 ． 2 ． 3 ． 4 ． 数据分块与编码 (Tokenization & Patching)： 将输入的连续视频帧序列离散化。 每一帧被切割为 S 个 Patch, 并映射为特征向量序列。 复合位置编码注入 (Spatiotemporal Positional Encoding)： 为每个 Token 注入两个维度的一个是它在当前帧内的二维空间坐标，， 另一个是它所在的时间步位置信息一块因果注意力计算 (BIock-Causal Attention CaIcuIation)： 同帧并行计算： 在第乯个 Block 内部， 执行 S 个 Token 的全连接自注意力计算， 提取当前帧的高分辨率空间语义和物体关系。 历史特征提取： 同时， 这 S 个 Token 共同作为 Query, 去 Attend 过去所有帧 （1 到 t 一 1） 生成的 Key 和 value, 捕获时序上的物理动力学 (Dynamics)0 下一帧块级自回归预测 (Block-wise Autoregressive Generation)： 不同于经典 GPT 必须逐个 Token 吐出， BCT 在完成前帧的上下文建模后， 可以直接将其作为上下文先验 ℃ ontextual Priors), 结合诸如 Shortcut Forcing 等机制， 并行且高效地生成第 + 1 帧的元整空间表征。

也就是说，在生成第t+1帧时，它刚开始是噪声向量的组合，前t帧的信息在Attention模块中作为上下文不断与之融合。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
