---
title: "36.5 任意步动力学预测（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/36.基于隐变量预测支持下游任务的世界模型/36.5 任意步动力学预测（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 36.5 任意步动力学预测（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、问题背景与核心思想

传统动力学模型大多采用单步预测：输入当前状态s_t和动作a_t，预测下一状态s_t+1。但在长程自回归推演中，误差会持续累积并不断放大。

Any-step Dynamics Model（ADM）提出，未来状态不一定非要依赖上一步预测结果层层递推得到，也可以基于历史状态直接进行任意步数预测。

## 二、ADM的核心思想与模型架构

### （一）核心思想：“回溯”

每次预测下一个状态s_t+1时，不是仅仅基于当前状态s_t和动作a_t，而是从不同远近的历史状态出发，根据(s_t,a_t),(s_t-1,a_t-1,a_t),...等都可以预测s_t+1。在这t个预测中随机选择一个，这样相当于对历史远近进行蒙特卡洛采样，在长距离多次生成过程中，就既能有效回顾较远的历史，避免偏差累积，又能关注较近的状态，实现精准生成。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

at+l GRU at+k—l GRU GRU Any-step MLP N （蜘 Eå+k) St+k 苤以 Dynamics ModeI (ADM) rt+k § + 1 Sm+1 random S + 1 choice

### （二）模型架构

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

由于输入计划的步长是可变的， 作者采用了包含门控循环单元 (GRU) 的循环神经网络 (RNN) 来实现 ADM。 为了处理输入维度， 模型会将初始的单步状态进行复制， 使其与多步动作序列的长度相匹配， 然后依次送入 RNN。 RNN 输出的隐藏状态会被送入多层感知机 (MLP), 最终输出目标状态 “ 和奖励 0 + 的高斯分布参数， 即均值和标准差 (vst+k， 与 (Vt+k, k)。

### （三）训练目标

最大化k步预测（k为1至m间的任意值）的对数似然：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

）、 刀 [logT9 （5 + k， + St' at:t+k—l (st,at t+k—l， S +

### （四）Off-policy时的不确定性量化

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

不确定性量化原理： 当面临轨迹分布发生变化或进入风险区域时， 使用不同回溯长度预测出的状态会表现出明显的分歧。 ADMPO-OFF 创造性地利用这一特性， 通过计算不同回溯长度预测结果的方差 （或标准差） 来自然地量化模型的不确定性且刀 M， 彻底摆脱了对集成模型的依赖。 + 4 不确定性公式 ． ' 4 刀 M@t，） = E 1 1 悲观价值迭代机制： 在离线训练好的 ADM 中进行环境展开时， 算法会对每一步生成的奖励进行惩罚： = r 一刀 M （(t), 其中是惩罚系数。 通过惩罚高不确定性区域的奖励， 算法构造了一个悲观的贝尔曼算子， 强制策略保持保守， 避免采取未知区域的危险动作 + 4

## 三、ADM-v2的改进

### （一）状态初始化与动作演化分离

第一代ADM通过直接建模任意步长的状态转移，减少了自举带来的误差累积。但由于其结构设计问题，ADM需要将初始状态多次复制，反复引入起始状态以对齐动作序列的长度，这导致了RNN隐藏表示与初始状态的强耦合，且无法通过并行计算加速任意步长的预测。

ADM-v2对这一结构做了更自然的重构：把状态编码和动力学预测分开，先将起始状态编码为隐变量，将这一隐表示作为循环单元的初始隐藏状态，后续递推只输入动作序列，不再重复输入起始状态。这样能让预测效果更优，也更符合基于隐变量的世界模型的范式。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

+ 1 GRU GRIJ t+1 《 (a)ADM GRU t+1 GRIJ GRU t+l at+m—l GRU Transiticm t+m t+m ADM2 unit t+1， (b) ADM-v2 t+1 ADM2 Unit t+m t+m

模型分为三个组件：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

状态编码器 (State Encoder)： 将回溯起点状态编码为隐向量， 即 = 曰 (st)o 这个直接作为 GRU 的初始隐藏状态。 ' GRU 单元： 因为起点状态的信息已被压缩在中， 后续 GRU 的每次循环前向传播不再重复输入起点状态， 而仅接收动作序列中的当前动作： 衅 1 = （， at) 五

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

转移解码器 (Transition Decoder)： 接收更新后的隐藏状态 + 1， 解码并输出下一状态 + 1 和奖励 0 + 1 的独立高斯分布参数： （脚 + 1， 1， + 1， 1） = de 吻 （+ 1）

### （二）并行任意步滚动推演（Parallel Any-step Roll-out，PARoll）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在 2m State Roll-out lnit ADM2 Unit State ADM2 Unit ADM2 Umt ADM2 Unit ADM2 Unit m 一 ADM2 Unit ADM2 Unit ADM2 Unit ADM-v2 ADM2 ADN•I-v2 Unit m+t ADM2 02m + 1 ADM-v2 ADM-v2 2 + 1 ADM-v2 山 n + 2 & “ 十 2 3m 十 1 ADM-v2 Unit

如图，维护m个并行预测分支，每两个分支之间有1步的时间差，每个分支在中间都每隔m步（设定的最大预测步长）进行一次预测。

第一步：轨迹初始化

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在进行模型 rollout 之前， 算法首先需要构建 m 条并行的预测时间线 (Timeline), 其中 m 是模型训练时设定的最大预测跨度。 + 1 1 ． 2 ． 3 ． 数据采样： 从真实环境离线数据集 “ 中采样一段长度为 m 一 1 的真实状态一动作序列： 50，， 5 OI， · ．， Sm 一 2， am—2， Sm 一 1。 并行编码与对齐： 这 m 个真实状态分别作为 m 条时间线的起占第 1 条时间线 （图中第一行）： 以 50 为起点。 首先通过状态纟扁码器 (State Encoder) 将 50 编码为初始隐状态 1）。 接着， 将动作序列 (ao, al, ·， 鰨， 一 2） 依次输入到 ADM2 Unit 中进行递归前向传播， 使其时间步对齐到 m 一 1, 最终得到隐状态五 （1） m—l。 第 2 条时间线 （图中第二行）： 以 51 为起点。 状态编码器将 51 编码为 2）。 然后依次输 ·，， 一 2） 更新隐状态， 对齐到时间步 m 一 1， 得到隐状态原 2） 入动作 (al, · m—l。 依此类推， 第 m 条时间线 （图中最后一行）： 直接以 sm 一 1 为起点。 通过状态编码器编码一 1 的隐状态：） 1， 无需输入任何历史动作。 后， 直接得到时间步 m 初始化完成： 经过上述过程， 我们在时间步 m 一 1 获得了 m 个多样化的、 包含了不同历史回溯信息的隐状态集合 （九 （1） 九 （2） m 一 1， m— ·

第二步：并行生成Roll-out与多样化预测

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

从时间步 = m 一 1 开始， 模型正式进入未知未来的 rollout 阶段。 1 ． 2 ． 动作采样与隐状态并行更新： 策略网络丌根据当前状态给出动作 am-l。 此时， m 条时间线上的 ADM2 Unit 并行接收这个动作， 并同步更新各自的隐状态： m—l'Om 1 其中 e { 1， 2， 一 ·， m} 代表不同的时间线。 状态与奖励的并行解码： 接着， 转移解码器并行对这 m 个隐状态进行解码， 输出预测状态和奖励的高斯分布参数， 并从中采样： = deco(h%))

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 ． 4 ． 预测多样性的来源： 在同一时间步 m 预测出的 m 个状态 @（1） ^ ·，） 具有天然的多样性， 因为它们是由不同的回溯起点经过不同长度的跨度 (Stride) 直接预测而来的。 例如， 是从 50 经过 m 步动作演化而来， 而是从 sm 一 1 经过 1 步动作演化而来。 + 2 持续演化： 对于后续的时间步 > m, 算法重复执行并行的隐状态更新和解码生成操作。 在此过程中， 从 m 个多样化预测中均匀随机选择一个 （例如） 作为下一步策略采样的依据，， 4 刀虱亻 2 并同时利用这些预测的方差来并行估计当前步的模型不确定性 + 1 0

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 ． 4 ． 状态与奖励并行预测： 通过解码器并行预测 m 个高斯分布， 并采样得到多样化的状态集合 + 1） 和奖励集合。 这些预测来源于具有不同步长的直接推理序列， 天然保证了预测的多样性。 + 1 模型不确定性估计： 在每一步算法从上述 m 个多样化预氵则中均匀选取一个作为下一步的真实采样， 并利用不同时间线的分布方差来量化模型在该处的不确定性 (Model 且刀 / 2 Uncertainty，）： 且刀 l'l / 2 1

第三步：跨度重置

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

由于 ADM-v2 在训练时设定了最大预测跨度为 m， 多步直接预测 （即隐状态的连续传播） 不能无限期地进行下去， 否则会导致严重的误差累积。 1 ． 2 ． 3 ． 重置触发条件： 当某条时间线的直接预测步数达到了最大跨度 m 时， 必须截断隐状态的传播， 并进行重置 (Reset) 重置方法： 调用状态编码器 (State Encoder), 将该时间线在当前时间节点刚刚预测出的状态重新编码， 作为新的起始隐状态进入下一个长度为 m 的预测窗口。 例如， 对于第条时间一戋， 它的重置时间节点是 (m + 一 1， 2m + 一 1， 3m + 一 1， · ·）。 此时隐状态更新方式变为： m + 一 1 enco 5 “ m + 犭一 1 交错重置的高效性： 在图中可以清晰地看到， 不同时间线的重置操作 （即图中右侧断开的箭头并重新引入 ADM-v2 块的起点） 在时间轴上是交错排列的 （呈对角线状）。 在实际执行时， 每个时间步仅有一条时间线 （具体为 @ mod m) + 2） 会达到最大跨度并触发重置。 这意味着每一步只需要执行一次额外的状态编码器前向传播， 极大地保证了算法的运行效率。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

由于 ADM-v2 在训练时设定了最大预测跨度为 m， 多步直接预测 （即隐状态的连续传播） 不能无限期地进行下去， 否则会导致严重的误差累积。 1 ． 2 ． 3 ． 重置触发条件： 当某条时间线的直接预测步数达到了最大跨度 m 时， 必须截断隐状态的传播， 并进行重置 (Reset) 重置方法： 调用状态编码器 (State Encoder), 将该时间线在当前时间节点刚刚预测出的状态重新编码， 作为新的起始隐状态进入下一个长度为 m 的预测窗口。 例如， 对于第条时间一戋， 它的重置时间节点是 (m + 一 1， 2m + 一 1， 3m + 一 1， · ·）。 此时隐状态更新方式变为： m + 一 1 enco 5 “ m + 犭一 1 交错重置的高效性： 在图中可以清晰地看到， 不同时间线的重置操作 （即图中右侧断开的箭头并重新引入 ADM-v2 块的起点） 在时间轴上是交错排列的 （呈对角线状）。 在实际执行时， 每个时间步仅有一条时间线 （具体为 @ mod m) + 2） 会达到最大跨度并触发重置。 这意味着每一步只需要执行一次额外的状态编码器前向传播， 极大地保证了算法的运行效率。

这意味着每隔m步就要把隐状态输出为预测状态，以此重新编码新的隐状态，完成重置。这样相比于随机回溯步数的好处在于，无论随机选中哪一条分支，都能获得来自历史较远状态和较近状态的信息（每隔m步一个状态），更好地满足平衡防止误差累积和提高精度的需要。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

由于 ADM-v2 在训练时设定了最大预测跨度为 m， 多步直接预测 （即隐状态的连续传播） 不能无限期地进行下去， 否则会导致严重的误差累积。 1 ． 2 ． 3 ． 重置触发条件： 当某条时间线的直接预测步数达到了最大跨度 m 时， 必须截断隐状态的传播， 并进行重置 (Reset) 重置方法： 调用状态编码器 (State Encoder), 将该时间线在当前时间节点刚刚预测出的状态重新编码， 作为新的起始隐状态进入下一个长度为 m 的预测窗口。 例如， 对于第条时间一戋， 它的重置时间节点是 (m + 一 1， 2m + 一 1， 3m + 一 1， · ·）。 此时隐状态更新方式变为： m + 一 1 enco 5 “ m + 犭一 1 交错重置的高效性： 在图中可以清晰地看到， 不同时间线的重置操作 （即图中右侧断开的箭头并重新引入 ADM-v2 块的起点） 在时间轴上是交错排列的 （呈对角线状）。 在实际执行时， 每个时间步仅有一条时间线 （具体为 @ mod m) + 2） 会达到最大跨度并触发重置。 这意味着每一步只需要执行一次额外的状态编码器前向传播， 极大地保证了算法的运行效率。

### （四）全视野rollout的理论边界

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 离线策略优化： ADM2PO-fh 在离线学习中， 策略极易陷入动力学模型预测不准确 （即不确定性高） 的 " 未探索区域 (Out-of- “ 4 刀 2 distribution)"。 ADM-v2 利用 PARoII 估计的不确定性作为悲观惩罚项 (Penalty term) 0 + 1 且刀 M2 是一个合法的 e 一不确定性量化器定理 3 · 1 （不确定性量化器有效性）： 论文证明了 ·。 它严格界定了真实 Bellman 算子与 ADM-v2 代理算子之间的误差上界： “ 4 刀 2 TQ(st,at) 一丆 ' Q@“ 〗@0 孓 （St, at + 1 0 应用： 基于此， ADM2PO-fh 算法在 Q 值估计中引入修正项：， 4 刀 M2Q （）： 刀 72 （）， 从而在面临高风险区域时收紧 Q 值， 防止过估计。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · 离线策略评估 (OPE): ADM20PE ADM-v2 可以直接作为一个 surrogate environment （代理环境）， 通过让待评估的策略在模型中进行 N 次完整的全视野轨迹采样， 取平均回报作为策略的评估值。 + 1 定理 3 · 2 （性能边界 Bounds)： 评估回报与真实回报的差距受限于长期预测误差和策略散度。 论文证明了返回的差距受到 C(ö e） 的约束： 77， 丌 27r 77 27r 勿 （丌） 一丌） “ 引孓 + 1 0 核心结论： 当最大 m 步直接预测误差满足特定条件 （巋 “ < 的 OPE 误差上界比传统的单步动力学模型更加紧致 (Tighter), 理论上提供了更可靠的评估 0 7 丌 2）） 时， ADM-v2 生成

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 3 ． 4 ． 预测多样性的来源： 在同一时间步 m 预测出的 m 个状态 @（1） ^ ·，） 具有天然的多样性， 因为它们是由不同的回溯起点经过不同长度的跨度 (Stride) 直接预测而来的。 例如， 是从 50 经过 m 步动作演化而来， 而是从 sm 一 1 经过 1 步动作演化而来。 + 2 持续演化： 对于后续的时间步 > m, 算法重复执行并行的隐状态更新和解码生成操作。 在此过程中， 从 m 个多样化预测中均匀随机选择一个 （例如） 作为下一步策略采样的依据...
- 3 ． 4 ． 状态与奖励并行预测： 通过解码器并行预测 m 个高斯分布， 并采样得到多样化的状态集合 + 1） 和奖励集合。 这些预测来源于具有不同步长的直接推理序列， 天然保证了预测的多样性。 + 1 模型不确定性估计： 在每一步算法从上述 m 个多样化预氵则中均匀选取一个作为下一步的真实采样， 并利用不同时间线的分布方差来量化模型在该处的不确定性 (Model 且刀 / 2 Uncertainty，）： 且刀 l'l / 2 1

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
