---
title: "38.4 世界-策略统一架构的具身智能（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/38.结合世界模型的具身智能与物理推理/38.4 世界-策略统一架构的具身智能（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 38.4 世界-策略统一架构的具身智能（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、基于世界模型的微调

基于具有对物理世界深刻认知的世界模型进行微调，可以获得具身智能需要的“大脑”和训练环境。下面以宇树科技UnifoLM-WMA-0为例介绍。

### （一）工作模式

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

UnifoLM-WMA-O 的核心在于让模型理解机器人与环境相互作用时的物理规律。 为了实现这一目标， 该架构被设计为支持两种独立但互补的核心工作模式： 1 ． 2 ． 决策模式 (Decision-Making Mode / POlicy Enhancement)： 在执行任务时， 世界模型会提前预测机器人在给定动作下与环境物理交互的未来关键信息。 这些具有物理常识的 “ 未来特征 " （如物体的形变、 位移预判） 会被输入到动作策略头中， 辅助机器人生成极其精准的下一步动作。 仿真模式 (Simulation Mode / Simulation Engine)： 模型作为一个交互式物理仿真器运行。 输入机器人给定的动作指令后， 模型能够自回归地生成高度还原真实场景的物理环境反馈 （以视频帧的形式）。 这相当于在神经网络内部为机器人构建了一个逼真的 " 梦境 " 交互环境， 可用于生成海量合成数据以强化学习。

### （二）核心架构和工作流

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

UnifoLM-WMA-O 的训练与部署工作流分为严谨的三个阶段， 逐步从单纯的 “ 视频生成 " 过渡到 " 物理决策 "： Ste p 1： 视频生成模型基础微调 (world Model pre-training) 输入： 海量跨实体机器人数据集 （如 Open-X Embodiment Dataset) 中的图像观测 (Observation) 和文本指令 (Text lnstruction)o 处理： 对预训练的视频生成扩散模型进行针对性微调， 使其生成能力从 “ 生成通用视频 " 收敛到 “ 生成枧器人真实作业场景视频 "。 输出： 具备基础物理直觉的视频大模型。 Ste p 2： 决策模式后训练 (Decision-making post-training) 输入： 下游具体任务数据集 （如具体型号机器人的抓取、 叠积木动作数据）。 处理： 冻结部分世界模型参数， 接入动作头 (Action Head)0 将世界模型提取的中间特征表征 （包含对物理交互的理解） 与环境观测共同喂入动作头。 输出： 优化动作头网络， 使其能够输出精确的关节控制指令序列。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Ste p 3： 仿真模式后训练 (Simulation post-training) 输入： 当前环境观测、 文本指令， 以及给定的动作序列 (Action Condition)0 处理： 强迫世界模型不仅要根据文本预测未来， 还必须严格遵循输入的动作序列来生成对应的未来物理反馈视频 （如机械臂向前推 ℃ cm， 画面中的物体必须表现出受力位移）。 输出： 一个高保真的交互式神经网络仿真器。

1.基于机器人数据、图像和文本指令的基础微调

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

子步骤 1： 独立编码与降维压缩 (Encoding & Compression) 高分辨率的像素图像和离散的自然语言文本无法直接在同一个数学空间中计算。 因此， 第一步必须将它们映射到各自的高维连续特征空间中。 1 ． 2 ． 图像观测处理 (lmage Observation Processing)： 为了大幅降低扩散模型的计算复杂度， 系统通常不会在像素空间直接操作， 而是使用预训练的变分自纟扁码器 (VQ-VAE 或连续 VAE) 的编码器模块设输入的初始图像观测为高分辨率张量 00 e “ × 彬 × 3 经过纟扁码器后， 图像被压缩为隐空间 (Latent Space) 特征图 zo： zo = E(oo), zo e ]R 其中九， 是压缩后的空间分辨率， c 是隐通道数。 此时的 zo 包含了当前机器人视角的极其浓缩的几何与物理状太佇串文本指令处理 (Text lnstruction Processing)： 输入的自然语言指令 （例如： " 将红色的苹果放入篮子中 "） 由大语言模型 （如 T5 或 C 凵 P Text Encoder) 丆进行分词和特征提取。 将其转化为包含语义信息的上下文嵌入序列 “ ． Ete 刀 × 刀

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

子步骤 1： 独立编码与降维压缩 (Encoding & Compression) 高分辨率的像素图像和离散的自然语言文本无法直接在同一个数学空间中计算。 因此， 第一步必须将它们映射到各自的高维连续特征空间中。 1 ． 2 ． 图像观测处理 (lmage Observation Processing)： 为了大幅降低扩散模型的计算复杂度， 系统通常不会在像素空间直接操作， 而是使用预训练的变分自纟扁码器 (VQ-VAE 或连续 VAE) 的编码器模块设输入的初始图像观测为高分辨率张量 00 e “ × 彬 × 3 经过纟扁码器后， 图像被压缩为隐空间 (Latent Space) 特征图 zo： zo = E(oo), zo e ]R 其中九， 是压缩后的空间分辨率， c 是隐通道数。 此时的 zo 包含了当前机器人视角的极其浓缩的几何与物理状太佇串文本指令处理 (Text lnstruction Processing)： 输入的自然语言指令 （例如： " 将红色的苹果放入篮子中 "） 由大语言模型 （如 T5 或 C 凵 P Text Encoder) 丆进行分词和特征提取。 将其转化为包含语义信息的上下文嵌入序列 “ ． Ete 刀 × 刀

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

子步骤 2： 跨模态时空注意力融合 (Cross-Modal Attention Fusion) 这一步是整个处理的核心。 扩散模型的主干网络 （通常是 3D U-Net 或 Diffusion Transformer, DiT) 需要把 “ 文本的意思 " 和 " 图像的现状 " 融合进对未来画面的生成过程中。 在网络内部， 主要通过 * * 交叉注意力机制 ℃ ross-Attention) * * 来实现文本指令对视觉生成的 " 指设网络当前层处理的未来视频帧隐变量特征为 z ViStLal 0 Query （查询） 来自视觉特征本身： Q = WQZvisual · Key （键） 和 value （值） 来自文本特征： K = VV “， = VV “

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

物理解释： 这个数学操作使得网络在生成 " 枧械臂抓取苹果 " 的画面时， 画面中 " 机械臂 " 和 " 苹果 " 所在区域的像素特征 (Query)， 会强烈地与文本指令中 " 抓取 " 和 “ 苹果 " 的语义向量 (Key/Value) 产生共鸣 （高权重 ), 从而确保生成的视频严格遵循文本指令的约束。 子步骤 3： 基于条件的隐空间去噪 (Conditional Denoising) 最后， 融合了图像观测 zo 和文本指令 “ 的特征将被用作条件 (Condition), 指导模型预测未来视频序列的噪声。 假设我们在预测未来的第帧的隐状态， 网络接收加噪的未来隐状态 zt(k) 为扩散步数 ), 并以拼接 ℃ oncatenation) 或自注意力 (Self-Attention) 的方式注入初始观测 zo。 网络的去噪目标函数为： 2 2 通过不断优化这个损失函数， 模型学会了： " 在看到初始画面 zo， 且听到指令 “ 时， 接下来的物理世界应该如何演变 "。

2.决策模式

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在决策模式下， 目标是输出动作 7。 系统会提取世界模型在逆向去噪过程中的中间层特征表示 (Feature Representations) M （， c)， 这组特征隐含了模型对场景几何和动力学规律的高维理解。 动作策略头接收此特征， 并输出对应的动作。 对于连续型物理控制任务， 采用均方误差 (MSE) 计算与专家真实动作 aGT 的行为克隆损失： 2 action 2 最终在端到端联合微调时， 系统的总损失是两者的加权和， 以此保证表征的物理一致性和动作的执行准确性： total AICWM + £ a 砌

3.仿真模式：采用条件扩散模型的损失。

## 二、LingBot-VA：基于MoT的策略与世界模型统一

在具身智能中，世界模型和VLM/VLA模型可以合二为一，采用MoT架构。下面以Lingbot-VA为例分析。

### （一）顺序双流架构

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

视觉观察首先通过因果视频 VAE 被压缩为紧凑的隐变量 e × 4。 动作向量也被投影为对应维度的 Token。 在每个自回归步骤中， 模型首先基于观察和动作历史， 预测未来 K 帧视频画面的隐状态： + 1 汤 + 五 ' ～ 《 9 （到 Z < O<t 随后， 逆动力学模型利用预测的未来视觉状态、 历史观察和历史动作解码出具体动作：

需要注意的是，上面的世界模型用的条件是a_<t而不是a_≤t。这是因为第一阶段预测未来画面，第二阶段（逆动力学）才根据预测的画面去推导当前应该执行什么动作a_t。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

视频流： 非常庞大， 基于 Wan2.2-5B 预训练模型初始化， 隐藏层维度高达 = 3072。 因为它需要理解极度复杂的视觉场景和物理动态。 + 3 动作流： 非常轻量， 深度虽与视频流相同， 但隐藏层维度仅为 = 768 （缩小了 4 倍） 是因为动作分布 （如机械臂的 7 自由度关节角和 1 维夹爪操作） 在本质上比像素级视觉画面简单得多， 不需要庞大的参数量来拟合。 + 4

在世界模型中，这两个流是融合在统一Transformer中进行推理的（视频会进行降采样，保证Transformer维度对齐）。在注意力层，二者统一进行交互：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

视频流： 基于预训练的 Wan2.2-5B 模型初始化， 拥有较大的维度大小。 动作流： 具有相同深度但宽度大幅减小 （参数量极低 ), 专门处理动作分布。 通过交替扌非列下采样视频 （降采样率丆 = 4） 与动作 Token, 构建统一序列 · ·，。 + 1， · · · ]。 在 MoT 结构中， 这两种模态通过交叉注意力机制融合特征， Zt' at 1， at,2' · 同时保持各自特征空间的独立性。

在前馈神经网络或MoE层，不同模态则分开处理。二者的输出头也是分开的，但对于每次输出token，总是只选择一个。

输出时，模型可以在输出序列中交替输出视频token和动作token。

### （二）工作流与异步推理方法

整体上“块内扩散，块间自回归”，每个块包含K个token：

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 基于 KV-Cache 的同步推理 (AIgorithm 1） 这是标准流程， 利用大模型的 KV 缓存机制避免历史数据重复计算： 步骤 1： 获取初始图像 00， 通过 VAE 编码为 zo， 将其存入缓存 CO 步骤 2： 采样随枧噪声， 结合缓存 C, 通过积分流时间至 5 = 0 · 5 （受惠于噪声历史增强技术， 下文详述） 生成未来的视频块预测 + 1 “。 步骤 3： 基于预测出的视频块和缓存， 积分流时间至 5 = 1.0 生成未来的动作块 “ 一 1。 步骤 4： 机器人执行动作， 收集真实环境的新观察， 并将新的 z 与 a 追加到缓存 C 中， 循环执行。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · FDM-grounded 异步推理 (Algorithm 2） 由于自回归生成视频非常耗时， 模型设计了异步流水线， 将 “ 计算 " 与 " 物理执行 " 重叠， 隐藏推理延迟： 步骤 1： 当枧器人正在执行当前的动作块 “ 一 1 时， 并行开启预测线程。 步骤 2 （前向动力学模型 / FDM 接地）： 不同于朴素异步模型使用过期的 " 幻觉预测 "， LingBot- VA 强制提取最新的真实环境观测， 通过 FDM 预测执行动作后的视觉状态 + “ + 五， 以修正预测轨迹 (Re-align)0 步骤 3： 缓存这个基于反馈接地的状态， 进而预测未来的序列 + 五 + 1 “ 2 五和后续动作 at + 五： t + 2 五一 1。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设当前系统处于时间步仁机器人侧： 正在花费物理时间执行动作块。 模型侧： 必须立刻开始计算下一时间步的动作 + 1， 以保证执行完后无缝衔接。 困境： 此时现实世界中， 执行后的最终真实视觉状态尚未发生， 相枧无法捕捉。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

如果采用朴素的异步策略 （如图 B-I 所示 ), 模型会直接去 KV-Cache 里拿它在上一轮骤一 1 时刻） 凭空预测出来的幻觉状态， 并基于这个过期的幻觉去预测未来的 + 1。 因为视频生成模型天生偏好时间上的平滑性， 它会顺着幻觉轨迹一路生成下去， 完全无视现实世界中已经发生的微小物理偏差， 最终导致彻底的 " 开环退化 " （轨迹漂移） 和失去对环境的反应能力。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了打断这种 " 用幻觉预测幻觉 " 的恶性循环， 系统会提取目前所能拿到的最新鲜的真实环境反馈： 即执行之前的那个真实观测荔一 1。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

此时， 模型不使用旧的预测， 而是执行一次 FDM 前向传播。 它将最新的真实观测荔一 1 以及当前电机确切正在执行的动作一起喂给模型， 让模型 " 想象 “ 在这样的真实前置条件下， 施加该动作后， 当前的视觉状态应该变成什么样 （得到新的）。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这一步是纠正的核心： 系统将这个基于真实数据反馈刚刚 " 推演 " 出来的荔， 强制更新并塞入 KV- Cache 中， 替换掉原本那个陈旧的、 未接地的预测。 随后， 模型基于这个刚刚与环境反馈重新对齐 (Re-align) 的坚实基础， 再去预测未来的视频块岛 + 1 并解码出后续的动作序列 + 1。

### （三）为什么视觉流只需要去噪到s=0.5，动作流就必须到s=1？

简单来说，前者是为了加速，后者是为了精确。

1.视觉流

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在自回归生成中， 视频画面的维度极高， 生成视频 Token 并对其进行多步去噪是整个推理过程最大的延迟罪魁祸首。 然而， 作者敏锐地发现： 逆动力学模型 （决定 “ 下一步该做什么 " 的网络） 并不需要 * * 像素级完美 (pixel-perfect) * * 的未来画面。 只要视频画面能呈现出大致的语义结构和运动趋势， 动作解码器就能提取出足够的信息来规划动作。 + 3

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了让模型具备 “ 看半成品做决策 " 的能力， 作者在训练阶段刻意给模型增加了难度。 他们以 = 0 ． 5 的概率， 将历史视觉特征 z« 替换为部分加噪的特征乏。 · + 1

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

)e ． + Sat Z saug e [ 0 ． 5， 1 ] Z< t 这里的 saug 模拟了未完全去噪的状态。 通过这种增强， 动作解码器被强行训练出了 “ 从半模糊的视频状态中准确预测动作 " 的鲁棒性。

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

正因为动作解码器在训练时 “ 见惯了 " 带有噪声的画面， 在实际推理时， 视频流就不必费力气完全去噪到 5 = 1 了。 只需将常微分方程 (ODE) 积分到 5 = 0 · 5 （注： 论文在后续的实际真枧部署细节中提到使用了欧拉求解器走 3 步， 具体积分到了 5 = 0 · 6), 就能直接把这个半噪声的 + 1 “ 五丢给后续使用。 这使得视频生成的去噪步数直接减半， 大幅降低了计算开销。 + 3

2.动作流

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

半模糊的视频可以看懂， 但 " 半噪声 " 的动作是绝对无法在物理世界中执行的。 机器人的动作空间 （双臂共 30 维， 包含末端位姿的 XYZ、 旋转四元数、 关节角度和夹爪开合） 需要极其精确的连续数值。 任何残余的噪声都会导致灾难性的物理碰撞或抓取失败。 因此， 动作流必须彻彻底底积分 = I.O, 以获得完全干净、 确定的动作指令。 + 4

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

得益于非对称的混合 Transformer (MoT) 架构， 动作流的隐藏层维度极低 （仅为视频流宽度的 1 / 4）， 参数量很小。 对如此低维的数据进行完整的 ℃ 步 ODE 积分 （到 s = I.O), 其带来的计算开销微乎其微， 完全不会像视频生成那样成为系统的算力瓶颈。 + 3

## 三、物理token的统一自回归预测

PAR、PhysGen等论文及NVIDIA的DreamDojo等提出了状态与策略统一编码于“物理token”中，统一进行自回归生成，再在下游解码为状态与策略输出的范式。

### （一）核心思想

将过去的视频和动作均编码为统一的物理token。设t时刻的视频帧token集合为o_t（PhysGen中每个o_t包含360个token），动作token集合为a_t（PhysGen中每个a_t包含8个token），则上文序列为[prompt,o_1,a_1,o_2,a_2,...,o_t,a_t]，联合预测下一步的[o_t+1,a_t+1]。预测出的[o_t+1,a_t+1]再通过扩散解码，分别得出连续的未来状态值（视频）和要采取的动作值。

当然具体来说，o_t和a_t也不一样：o_t内部的token可以并行生成；a_t内部则按时间顺序严格串行，但利用Multi-token prediction的方法，以在动作块的内部拉长微观的规划视野，即便预判2到3步极其微小的机械臂位姿，也能让自回归生成的动作轨迹更加连贯，有效避免了控制上的“短视”和抖动。

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

envlronment evolution 0 一艹艹 0 ． 0 autoregressl(jn External World execute update decode encode

### （二）自回归Transformer

主干Transformer网络采取自回归生成范式。因果掩码方式如下：

1.帧内部：块级全注意力

对于物理token中的视频帧部分，掩码允许同一个画面内的所有图像块相互“看到”彼此。单张图像内的空间信息是同时存在的，没有时间上的先后顺序。全注意力机制能保证模型完整地提取这一帧的全局空间特征。

2.动作内部：时间因果注意力

对于物理标记中的动作部分，模型执行严格的时间因果掩码。在一个时间步的动作块内，各个token仍有先后顺序，时间上靠前的动作标记无法关注到排在它后面的动作标记。这是因为动作是随时间线性发生的连续序列，必须遵守“过去决定未来，未来不能干涉过去”的物理法则。

3.动作单向关注视觉

在同一个时间步t中，动作token被允许单向地关注视频token。这也就是让模型学会了“给定目标状态，反推所需的动作”的逆运动学推导。

4.跨时间步：全局时间因果

在不同的时间步之间，严格遵守时间因果掩码，用t时刻以前的信息预测t+1时刻，防止未来帧的信息泄露到过去的规划中。

### （三）扩散解码器

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

与传统的离散分类不同， PhysGen 引入了基于 DiT (Diffusion Transformer) 的去噪过程， 利用扩散损失直接在连续空间中估计概率分布。 + 1 模型被训练来预测注入的噪声， 从而最小化均方误差损失：

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 27 OCR 未识别出有效文字：未识别出有效文字。]

推理阶段去噪的公式为：

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 一 t 一 1 去噪后的物理标记最终被分离， 并解码为预测的视频帧和要在物理世界中执行的机器人动作

### （四）BOA token

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在真实的物理世界中， 因果关系是非常严谨的。 假设你是一个机器人， 你要完成一个任务： 初始时刻 = 0）： 你睁开眼睛， 看到了桌子上的杯子。 此时， 你获得了一个初始的视觉观察结果 (Frame 0）。 但在这个极其短暂的瞬间， 你还没有执行任何动作。 第一步 = 1): 你的大脑处理了 Frame 0 的信息， 决定伸出机械臂。 你输出了第一个动作 (Action 1)0 因为这个动作， 物理世界发生了变化， 你看到了新的画面 (Frame 1）。

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

PhysGen 框架的特色是把帧标记 (Frame token) 和动作标记 (Action token) 拼在一起， 组成一个个统一的 " 物理标记 (Physical Token)" Pn 来进行自回归预测：

> [图片 31：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

按照我们刚才的物理直觉， 输入给模型的历史序列是这样的： 视觉观察序列： { 00， 0 0 一， ON 一 1 } （从 0 开始， 多一个初始帧） 2， · 对应动作序列： { 1， 2， 一， N 一 1 } （从 1 开始， 少一个初始动作） 如果你想强行把它们两两打包成召， 就会遇到一个尴尬的时间偏移 (temporal offset) 问题： 00； 什么都没有？ ] 这里对不齐了！ PI = 01； 山 ] P2 = 02 2 ]

> [图片 32：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了在数学结构上完美对齐这两个长度不一的序列， 让它们能够顺利放进 Transformer 里跑， 作者在动作序列的最前面， 人为地塞入了一个可学习的占位符， 叫做 BOA (Begin of Action) token

### （五）训练工作流

1.预训练

在预训练阶段，模型的主干网络仅仅在海量的视频数据上进行了训练。这个阶段模型没有见过任何机器人的动作指令，它学到的是“世界是如何运转的”（例如：杯子掉在地上会碎、手碰到物体物体会移动等纯物理和时间动态）。

这个阶段只训练了语言Tokenizer、视觉Tokenizer（3D-VAE）、自回归Transformer主干网络，以及帧生成器（Frame Diffusion），动作模块并不参与。

2.动作微调

为了让模型能够控制机器人，作者会在具体的下游任务上进行微调（Fine-tuning）。在这个阶段，模型会使用带有动作标签的演示数据。但是，因为模型在第一阶段已经懂得了物理世界的运作规律，它所需的动作数据量极小。

此时，模型会增加一个用于编码动作的Action Tokenizer（一个MLP），以及一个轻量级的动作解码器（Action-DiT）。微调时，模型被训练为一旦看到BOA token，后面就按“360个视频帧token+8个动作token”交替生成，系统也会按token数量交替分发给不同解码器。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
