---
title: "38.5 仅在训练时融入预测能力的具身智能（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/38.结合世界模型的具身智能与物理推理/38.5 仅在训练时融入预测能力的具身智能（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 38.5 仅在训练时融入预测能力的具身智能（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、Fast-WAM：仅在训练时预测状态的推理

### （一）问题背景

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在具身智能领域， 视觉 · 语言 ． 动作模型 (v LA, Vision-Language-Action) （例如 OpenVLA、 丌 0 等） 在过去取得了巨大进展。 然而， 传统的 VLA 主要基于静态的图文数据进行预训练， 缺乏对物理世界动态演变过程的建模。 为了解决这一问题， AI 领域发展出了一条新的技术路线： 世界动作模型 (WAMs, world Action Models)0 WAMs 将 “ 预测未来视觉画面 " 与 " 预测机器人动作 " 结合在统一的框架中， 从而让模型具备了物理先验知识。 然而， 现有的 WAMs 大多遵循 * * " 先想象， 后执行 " (imagine-then-execute)** 的范式： 在推理时， 模型必须先生成未来的视频帧， 然后再基于这些预测的未来画面来输出动作。 这一范式存在一个致命弱点一一高昂的推理延迟。 因为在测试时需要通过扩散模型迭代去噪生成视频， 这使得模型很难在机器人上实现低延迟的实时控制。 因此， 本文提出了一个触及灵魂的核心问题： WAMs 真的需要在测试时显式地 “ 想象 " 未来画面吗？ 还是说， 它的性能提升仅仅是因为在训练阶段引入了视频预测任务 (Video Co-training), 从而学到了更好的物理世界表征？

### （二）基座模型

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

基座模型： Fast-WAM 采用了最新的开源视频扩散 Transformer——Wan2.2-5B 作为其世界建模的骨干网络 (Backbone)0 它复用了 Wan2.2 的视频 DiT、 文本编码器 (T5) 以及视频 VAEO 动作专家 (Action Expert)： 在基座之上， 作者引入了一个参数量约为 1 B 的动作专家 DiT, 使总模型参数达到 6B0 MoT 架构： 整体采用带有共孚注意力的混合 Transformer (MoT, Mixture-of- Transformer) 架构， 分为视频分支和动作分支。

### （三）模型架构与训练工作流

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第一步： Token 的精准分组在送入这个 M oT 架构之前， 所有的输入信息被严格划分为三个 Token 组： 1 ． 视觉锚点 （九）： 当前第一帧观测图像的 " 干净 “ 潜在特征 （不加噪）。 2 ． 未来视频 01 一 ·）： 未来视频帧的加噪潜在特征 （仅在训练时存在）。 3 ． 动作序列 1 一 · 鰨）： 要生成的动作块的加噪特征。 第二步： 全局文本 Cross-Attention 注入在这三个 Token 组进入 DiT 的每一层时， 所有的 Token 组都会通过 Cross-Attention 机制去关注 (Attend (o) 语言特征向量数学逻辑： 这意味着九、 fl:h 和 al•h 都会作为 Query (Q)， 而文本特征作为 Key （K) 和 value 物理意义： 这保证了无论是提取当前环境的视觉特征、 推演未来的物理变化， 还是规划机器人的具体动作， 都始终受到人类语言指令的绝对引导。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这里的 " 第一帧 " 是相对于原本要生成的 " 未来视频块 “ 而言的锚点 (Anchor)0 模型摒弃了生成未来画面的过程， 而是将这唯一的一帧当前图像输入到视频骨干网络中进行一次单向的前向传播， 提取出世界表征， 然后直接交给动作专家网络去生成后续的一系列动作。 这种设计赋予了 Fast-WAM 类似标准 VLA （视觉一语言一动作模型） 的极速实时响应能力。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第一步： Token 的精准分组在送入这个 M oT 架构之前， 所有的输入信息被严格划分为三个 Token 组： 1 ． 视觉锚点 （九）： 当前第一帧观测图像的 " 干净 “ 潜在特征 （不加噪）。 2 ． 未来视频 01 一 ·）： 未来视频帧的加噪潜在特征 （仅在训练时存在）。 3 ． 动作序列 1 一 · 鰨）： 要生成的动作块的加噪特征。 第二步： 全局文本 Cross-Attention 注入在这三个 Token 组进入 DiT 的每一层时， 所有的 Token 组都会通过 Cross-Attention 机制去关注 (Attend (o) 语言特征向量数学逻辑： 这意味着九、 fl:h 和 al•h 都会作为 Query (Q)， 而文本特征作为 Key （K) 和 value 物理意义： 这保证了无论是提取当前环境的视觉特征、 推演未来的物理变化， 还是规划机器人的具体动作， 都始终受到人类语言指令的绝对引导。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第三步： 结构化 Self-Attention 掩码 （组合的核心） 在注入了文本信息后， Token 之间需要在 DiT 内部进行自注意力 (Self-Attention) 交互。 这就是 Video DiT 和 Action DiT 真正 “ 组合 " 与 " 隔离 ' ' 的地方。 作者设计了严格的注意力掩码来控制信息流 Video DiT 的内部交互： 未来的加噪视频 Token （荪 ． · ·） 可以在视频分支内双向互相关注， 并且被允许去关注第一帧干净特征 （）。 这使得模型能够基于初始状态推演未来。 Action DiT 的内部交互： 动作 Token 1 · ． · 鰨） 可以在动作分支内双向互相关注， 同样也被允许去关注第一帧干净特征 （九）。 这使得模型能够基于当前视觉状态来规划连贯的动作序不可跨越的红线 （解耦的关键）： 动作 Token 绝对不允许关注到未来的视频 Token。 同时， 第一帧干净 Token （九） 不关注任何其他 Token （避免被噪声污染）。 + 1

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

严禁越界的根本原因： 如果在训练时， 动作特征看到了未来的视频特征， 动作专家就会产生 " 依赖性 " 一一它会根据 “ 未来已经发生的结果 " 来反推 " 现在该做什么动作 “。 然而， 在真实环境部署 （推理） 时， 为了追求 190 亳秒的极低延迟， Fast-WAM 会把整个未来视频生成分支直接砍掉。 如果模型在训练时养成了看未来视频的习惯， 到了推理时一旦看不见未来视频， 动作分支就会因为输入信息的缺失而彻底崩溃。 因此， 结构化掩码强迫动作预测只能 “ 立足当下 " （基于第一帧和文本 ), 从根本上解耦了训练时的视频辅助和推理时的动作生成。

### （四）损失函数

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

对于目标变量 （既可以是动作序列 al 丑， 也可以是未来视频特征， 算法采样高斯噪声 6 ～ （0， 和时间步 t e （0， 1）， 构建插值样本： 模型通过标准的流匹配损失函数来预测速度场： 总的优化目标是动作损失和视频协同训练损失的加权和：

### （五）推理工作流

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

推理阶段彻底抛弃了显式的视频生成， 实现了极速响应。 1 ． 2 ． 3 ． 单次前向传递： 仅保留第一帧的干净潜在特征， 将其输入视频骨干网络进行一次单向的前向传播， 提取出具有物理意义的潜在世界表征 z(), 0。 直接动作去噪： 动作专家 DiT 直接基于这个表征来参数化动作分布： 输出执行： 由于不需要对未来视频帧实例化和去噪， 动作生成仅需 ℃ 步无分类器引导 ℃ FG： 1 ． 0） 的去噪即可输出动作序列。

### （六）实验结果

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 极速的推理延迟： 在真实机器人任务中， Fast-WAM 的延迟仅为 190 毫秒， 而采用 " 先想象后执行 " 的 Fast-WAM-lDM 延迟高达 8 ℃ 亳秒。 Fast-WAM 实现了超 4 倍的提速。 比肩前沿的性能： 在没有进行大规模具身预训练 (Embodied Pretraining) 的情况下， Fast- 2 ． WAM 在 RoboTwin 上取得了 9t8 ％ 的成功率， 在凵 BERO 上取得了 97 ． 6 ％ 的平均成功率， 不仅碾压了不带视频预测的 VLA 基线， 甚至与最强的预训练 WAMs 模型不相上下。 回答核心假设 （最关键的发现）： 3 ． · Fast-WAM 的表现与显式生成未来的变体 (Joint 和 IDM) 极其接近。 · 但是， 如果去掉了训练时的视频协同训练任务 (). 0 · video co-train), 模型在 RoboTwin 的成功率骤降至 83 ． 8 ‰ 在真机折叠毛巾任务上的成功率更是断崖式跌至 ℃ ％。 最终结论： WAMs 强大的根本原因在于训练阶段的视频预测任务重塑了模型对物理世界的表征能力， 而并不在于推理阶段显式地把未来画面画出来。 Fast-WAM 证明了我们可以 " 鱼与熊掌兼得 “： 既能在训练时享受世界模型带来的物理先验， 又能在部署时拥有 VLA 一样的极速实时响应。

## 二、Being-H0.7：训练时让“动作预测”与“基于未来状态的动作预测”对齐

### （一）问题背景

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

具身大模型当前主要有两条技术路线， 但它们均存在明显的局限性： 1 ． v LA （视觉 · 语言 ． 动作模型）： 这类模型 （如 RT- 2， OpenVLA 等） 将当前的视觉和语言观察直接映射为底层控制动作。 · 痛点： 由于机器人动作数据本身极为稀疏且缺乏多样性， VLA 模型容易陷入 " 行为崩溃 '。 模型倾向于依赖表面的统计相关性， 而未能学到基于物理现实 (behavior collapse) 规律的、 具有组合泛化能力的动作表征。 2 ． WAM （世界 ． 动作模型）： 基于视频生成模型构建， 通过预测未来的像素帧演变来推断世界动态。 · 痛点： 首先， 在推理阶段实时生成 (rollout) 未来视频帧会带来极高的计算延迟， 完全无法满足机器人的高频控制需求。 其次， 预测像素中哪怕极其微小的瑕疵或幻觉， 也会随着轨迹传递到动作解码中， 导致长周期亻壬务失败。 最后， 其训练成本极其曰

### （二）核心思想

Being-H0.7不需要在像素层面预测未来，而是直接在训练时让动作预测与基于未来状态的动作预测对齐。模型引入了一组可学习的Latent Queries，在Transformer层层前向传播的过程中，这组查询向量不断从多模态上下文中汲取与任务相关的信息，将其压缩重组、生成动作；另外在训练时，将观测到的未来状态也压缩到Latent空间，也从多模态上下文中汲取与任务相关的信息，将其压缩重组、生成动作；让前者（先验分支）向后者（后验分支）对齐。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了强迫这个推理空间真正具备 " 预测未来 （世界建模） " 的能力， 作者设计了创新的 * * 训练期双分支联合对齐 (Dual-Branch JOint Alignment) * * 架构 ． Prior Branch （先验分支）： 这是最终用于实机部署的干线分支。 它仅依赖当前的指令和观察， 使用 Latent Queries 生成动作。 · Posterior Branch （后验分支）： 这是仅存在于训练阶段的辅助分支。 它将干线中的 Latent Queries 替换为通过视觉编码器提取的未来观察嵌入 (Future Embeddings) · 对齐机制： 在隐藏层特征级另刂， 强行计算先验分支和后验分支的对齐损失， 迫使先验分支在仅看到当下画面的情况下， 推断出与 " 看到未来 " 相同的特征表示。

### （三）训练工作流

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

· 步骤 1： 上下文编码与序列构建给定亻壬务指令厶时间长度为 H 的历史观测 0 一伊 0 以及机器人本体状态 5。 在将这些特征映射并拼接后， 算法在动作块 ao 的预测序列前， 插入数量为 K、 维度为 d 的潜在查询矩阵 Q e RKxd, 构建先验分支的完整 Transformer 序列： · 步骤 2： 后验分支的未来特征提取同步地， 算法获取未来的真实观测视频帧 50： T。 使用冻结的预训练 ViT 和 Perceiver 重采样器 E 将其压缩成大小与 Q 严格一致的未来嵌入 “： 五 × d

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

· 步骤 2： 后验分支的未来特征提取同步地， 算法获取未来的真实观测视频帧 50： T。 使用冻结的预训练 ViT 和 Perceiver 重采样器 E 将其压缩成大小与 Q 严格一致的未来嵌入 “： 五 × d “ “ = E@0： 刁 e IR 步 3： 混合 Transformer (MoT) 双分支前向传播为了提高计算效率， 算法没有串行执行两次巨大的前向传播， 而是利用 Mixture-of- Transformers 架构将这两个分支 " 打包 " 成一个长序列。 在这个过程中， 应用了双分支注意力掩码 (Dual-Branch Attention Mask)： 保证两个分支都能 " 看到 " 共享的上下文 c = 一： 0； 的但是先验分支和后验分支的 tokens 彼此相互不可见 （孤立）。 · 步骤 4： 损失函数计算整个框架通过三个损失函数进行联合优化： 1 ． 动作流匹配损失旧 ow Matching Loss)： 基于扩散生成范式， 为两个分支分另刂计算对加噪动作的去噪速度场损失。 若真实动作为 a， 加噪时间为 t, 目标速度为 = a 一， 则：

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

、 £ ' ost P7、 tor po po = £ 岩、 + CFM po

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 ． 隐状态联合对齐损失 (Joint AIignment Loss)： 在第 ／ 个包含对齐的 Transformer 层， 提取先验分支的隐状太灬与后验分支的隐状太 “ 计算均方误差， 促使先验空间涌现对未来的世界建模能力： 1 align 九 PO 《 2 3 ． 防崩溃正则化损失 (ReguIarization Loss)： 为防止对齐操作导致隐特征趋于全 0 或方向单一化， 施加了针对范数 (Norm) 和秩 (Rank) 的惩罚项。 范数正则化 （防止幅值收缩） 騰一 = [ 月吐以丆一 2 2 秩正则化 （防止方向坍缩， 由 Gram 矩阵 G 的归一化特征值的信息熵表示） · 騰。 巛 H） = 乥 log

### （四）推理工作流

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

由于模型解耦了特征对齐和前向生成， 推理阶段极为轻量级： 1 ． 观察采集： 机器人摄像头实时采集当前画面， 处理并拼接为上下文向量。 2 ． 纯先验分支执行： 完全丢弃掉庞大的后验分支和所有的未来视频输入需求。 只保留 Transformer 骨干、 Latent Queries Q 以及用于扩散的 FIow Matching 生成头。 3 ． 动作块输出与 UAC 异步控制： 模型一次性快速生成连续的未来动作块 ao:To 在真实的系统部署中， 结合了通用异步分块机制 (UAC)： 服务器端异步运行模型推理预测未来的动作序列， 而底层客户端 （机器人驱动器） 从一个线程安全的缓冲池中持续取出历史前缀锁定动作去执行， 两者解耦， 以极低的延迟 （约 3 一 4 ms/step) 实现超平滑的动态控制。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
