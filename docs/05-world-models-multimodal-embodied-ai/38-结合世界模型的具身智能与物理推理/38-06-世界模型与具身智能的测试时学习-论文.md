---
title: "38.6 世界模型与具身智能的测试时学习（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/38.结合世界模型的具身智能与物理推理/38.6 世界模型与具身智能的测试时学习（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 38.6 世界模型与具身智能的测试时学习（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、WorldAgen：对世界模型头和共用基座的LoRA微调

现有的世界模型往往是静态的。在具身智能等应用场景中，如果它们被部署到从未见过的环境，无法主动更新对环境的理解，容易导致性能下降。一种世界模型的Test time training方法是对世界模型头和共用基座进行LoRA微调。

### （一）核心架构

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了实现联合预测与动态适应， worldAgen 将问题建模为部分可观察马尔可夫决策过程 (POMDP)， 并在单个 Transformer 主干网络上设计了两个相互协作的头部： 智能体模型 (Agent Model Head): 这是一个基于任务条件预测动作的网络头。 它根据当前的任务指令、 观察块、 机器人状态以及智能体历史信息， 来预测下一步的动作块。 世界模型 (world Model Head)： 这是一个与具体任务无关 (task-agnostic) 的状态预测网络头。 它根据历史的状态一动作轨迹， 预测未来的环境观察块。 混合单向注意力掩码 (Mixed Unidirectional Attention Mask)： 为了让这两个模型共享表示且不发生信息泄露， 作者设计了独特的掩码机制。 局部掩码 (Local Mask)： 确保当前时间步内的观察、 状态、 指令和动作占位符无法提前 " 看到 " 真实的动作块。 全局掩码 (Global Mask)： 强制世界模型对任务指令保持不可见， 防止跨模态的信息泄露， 从而保证世界模型学到的是纯粹的环境物理规律， 而非指令捷径。

### （二）核心算法工作流

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 轨迹分割与动作分块工作流 (Traj e ctory Splitting and Action Chunking) 为了平衡真实机器人的执行效率与观察序列的计算开销， worldAgen 对连续轨迹进行了灵活的分块处理： 初始化： 给定一条包含 T 个时间步的完整轨迹序列。 初始单元构建： 聚合前 “ 个时间步作为初始轨迹单元 ％， 并关联其对应长度的初始动作块。 后续单元采样： 对于后续的轨迹单元， 算法均匀采样 “ 个时间步来构建当前的观察块 0 和状态块 St0 动作块关联： 将 m 个连续的动作组合成当前对应的动作块。 输出： 通过固定观察块的长度为系统有效减少了冗余的视觉信息并显著提升了运行效率。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · 测试时训练 (Test-Time Training, TTT) 工作流 TTT 是本文使模型主动适应全新环境的核心突破点， 整个过程在极短的时间 （每个场景约 2 到 8 分钟） 内完成： 阶段 1： 随机探索与数据收集。 部署在目标环境时， 智能体首先执行自由的探索性随枧动作 （无需人工演示）。 阶段 1： 轨迹记录与重标记。 记录下这些探索轨迹的状态与动作， 并将原本的任务指令全部替换为通用的 “ no 一丨 ang " 标记。 这确保了后续的适应过程只关注环境动力学， 而不受特定任务指令的干扰。 阶段 2： 世界模型计算损失。 利用预训练时相同的联合优化目标 （结合了动作损失 Ca 与观察损失 (O), 并在 TTT 阶段仅基于预测观察块的重构损失进行计算。 完整的预训练损失函数为： 阶段 2： 轻量级微调更新。 保持智能体策略头 (Agent Policy) 冻结， 仅使用 Lo RA （低秩自适应） 对共享的主干网络 （如 Qwen3) 进行参数高效微调。 更新公式为： 一 V £ 0

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 · 两步推理 (Two-Step lnference) 工作流在实际部署执行任务时， 模型按以下交替步骤生成动作并预测环境： 智能体模型步骤： 系统使用一个全零初始化的动作占位符构建输入序列。 智能体模型预测出当前的实际动作块， 并用其覆盖原有的占位符。 世界模型步骤： 接着， 以任务指令、 当前观察块以及更新后的预测动作块为条件， 世界模型预测出下一个时间步的观察块 0。 循环往复： 模型交替执行这两步， 自回归式地完成整条轨迹的操作与状态更新。

### （三）方法优势

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

卓越的基准表现： 在 CALVIN 这一旨在评估长视野、 自然语言约束下机器人操作的基准测试中， WorldAgen 展现了超越 3D Diffusor Acton CLOVER 等当前最先进 VLA 模型的表现。 强大的跨任务迁移与环境适应性： 在注重终身学习和知识迁移的凵 BERO 实验中， TTT 显著提升了模型在陌生环境中的表现， 平均成功率从 75 ． 5 ％ 跃升至 79 ． 0 ％。 消融实验证明世界模型的价值： 实验证实， 剥离世界模型 （即移除图像预测能力） 会导致性能断崖式下跌 （在 CALVIN 上成功长度下降超 30 ％， LIBERO 成功率下降超 67 ％ ), 这充分说明了环境动态建模对动作策略学习的巨大反哺作用。

## 二、世界模型的动态LoRA与主模型组合

这种思路将世界模型作为LoRA微调得到的“专家”，根据实际环境选择并与主模型组合使用。

### （一）Test-time Mixture of World Models：LoRA组合中运用可学习路由

1.问题背景

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

目前， 基于 LLM 的具身智能体越来越多地被部署在家庭、 工厂等真实世界中。 传统的具身智能体大多采用单体架构， 其能力在训练后就被固定了， 这使得它们在面对不断变化的任务和环境时显得力不从心。 为了解决这个问题， AI 领域引入了混合专家模型 (Mixture-of-Experts, MoE)0 MoE 的核心思想是通过路由机制 (Router) 将输入分配给不同的 " 专家 " 模块， 从而实现高效的领域特化。 现有 MoE 的局限性： 尽管 MoE 能够提供结构上的模块化， 但传统的 MoE 在部署后， 其路由函数是固定的。 这意味着如果智能体遇到了训练分布之外的 " 未见领域 (Unseen domains)"， MoE 无法灵活调整， 只能进行昂贵的重新训练。 本文的解决方案： TMoW 框架打破了这种僵局。 它允许在 * * 测试时 (Test-time) * * 动态更新路由函数， 使智能体能够重新组合现有的世界模型， 甚至在少量样本的帮助下高效地整合新的世界模型。

2.核心架构与推理时工作流

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

TMoW 的架构设计包含三个关键的创新机制。 在详细解释这三个机制之前， 我们需要了解 TMoW 将每个 " 世界模型 " 视为一个特定领域的专家， 并通过轻量级适配器 （如 LoRA) 集成到基础模型 M 中， 形成世界模型混合 M {m, }J A. 多粒度基于原型的路由 (Multi-granular Prototype-based Routing) 由于不同的环境可能在全局上不同， 但在局部 （如物体层面的交互） 上存在共性， TMoW 使用了一种分层图表示方法来提取 " 原型 (Prototype) "

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在具身智能 (Embodied AD 中， 环境状态非常适合被抽象为图 (Graph) 结构。 例如， 节点 (Nodes) 代表环境中的实体 （如枧器人、 杯子、 桌子、 房间）， 边 (Edges) 代表它们之间的空间或功能关系 （如杯子一在一 ． 上一桌子， 冰箱一在一 ． 里一厨房） 利用图处理器进行路由的核心原理， 就是将当前环境的图结构特征提取出来， 并与预先存好的各个 “ 专家 " 特征 （原型） 进行对比， 从而决定在当前状态下该听哪个专家的。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

TMoW 的图处理器使用的是消息传递神经网络 (MPNN), 具体实现上结合了图卷积网络 (GCN) 和多层感知机 (MLP)。 为了防止图神经网络常见的过平滑 (oversmoothing) 问题， 作者只在特定的几层 （第 0， 4， 8， 12 层） 使用 GCN 来捕获图结构， 其余层使用标准的 MLP0 + 3 在 M PN N 中， 特征是通过层层递进的方式提取的： 浅层 (Early layers)： 节点只和直接相连的邻居交换信息， 因此提取的是 * * 局部物体级别 (Local object-level) * * 的特征 （例如， 单纯认识一个 " 苹果 “ 或 " 桌子 "） + 3 深层 (Deeper layers)： 经过多次消息传递， 节点的感受野扩大， 提取的是 * * 全局场景级别 (Global scene-level) * * 的结构模式 （例如， 理解这是一个 " 厨房的典型布局 "）

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

TMoW 的图处理器使用的是消息传递神经网络 (MPNN), 具体实现上结合了图卷积网络 (GCN) 和多层感知机 (MLP)。 为了防止图神经网络常见的过平滑 (oversmoothing) 问题， 作者只在特定的几层 （第 0， 4， 8， 12 层） 使用 GCN 来捕获图结构， 其余层使用标准的 MLP0 + 3 在 M PN N 中， 特征是通过层层递进的方式提取的： 浅层 (Early layers)： 节点只和直接相连的邻居交换信息， 因此提取的是 * * 局部物体级别 (Local object-level) * * 的特征 （例如， 单纯认识一个 " 苹果 “ 或 " 桌子 "） + 3 深层 (Deeper layers)： 经过多次消息传递， 节点的感受野扩大， 提取的是 * * 全局场景级别 (Global scene-level) * * 的结构模式 （例如， 理解这是一个 " 厨房的典型布局 "）

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在 M PN N 的第 I 层， 隐藏状态 H(I) 的更新过程为： 其中 sG 是消息生成函数， AGG 是聚合函数， U 尸刀是更新函数， 是邻接矩阵， 月是关系特征。 + 1

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

仅仅有环境状态图是不够的， 智能体还需要知道当前的任务指令 （例如 " 把杯子放到桌子上 “）。 图处理器通过交叉注意力机制 ℃ ross-Attention) 将指令信息融合到图的边权重中， 这被称为 " 上下文感知边矩阵 ℃ ontext-aware edge matrix, E) " + 3 调节函数九计算了图节点隐藏状态 H(I-I) 与指令嵌入重@） 之间的交互： 其中 QH,KH 是图状态的 Query 和 Key， Q, ICt 是指令的 Query 和 Key， d 是嵌入维度。 + 2 随后， 这个调节系数会以哈达玛乘积 （@） 的形式作用于图的邻接关系上， 动态调整信息传递的路亻工： 原理直白解释： 如果当前任务是 “ 洗苹果 "， 那么图处理器在聚合信息时， 会赋予 " 水槽 “、 “ 苹果 " 之间连接更高的权重， 而削弱与 “ 沙发 " 相连的边。 这就是上下文感知的本质。

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

经过上述两步， 图处理器已经将 " 当前观察 0 + 当前指令严映射成为了每一层 I 的一个领域嵌入向量 (Domain Embedding) E(I) + 1 接下来就是最关键的路由决策 (Routing)： 1 ． 2 ． 算相似度： 计算当前领域嵌入 0 与预先存在字典里的 N 个世界模型对应的原型余弦相似度 ℃ osine Similarity)， 得到路由得分 + 1 之间的选拔与归一化： 为了保证计算的高效和专注， 路由器会进行稀疏化， 只保留得分最高的前个专家 (Top-K)， 并使用一个带有缩放超参数丆的 Softmax 函数将它们转化为概率分布

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

经过上述两步， 图处理器已经将 " 当前观察 0 + 当前指令严映射成为了每一层 I 的一个领域嵌入向量 (Domain Embedding) E(I) + 1 接下来就是最关键的路由决策 (Routing)： 1 ． 2 ． 算相似度： 计算当前领域嵌入 0 与预先存在字典里的 N 个世界模型对应的原型余弦相似度 ℃ osine Similarity)， 得到路由得分 + 1 之间的选拔与归一化： 为了保证计算的高效和专注， 路由器会进行稀疏化， 只保留得分最高的前个专家 (Top-K)， 并使用一个带有缩放超参数丆的 Softmax 函数将它们转化为概率分布

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

经过上述两步， 图处理器已经将 " 当前观察 0 + 当前指令严映射成为了每一层 I 的一个领域嵌入向量 (Domain Embedding) E(I) + 1 接下来就是最关键的路由决策 (Routing)： 1 ． 2 ． 算相似度： 计算当前领域嵌入 0 与预先存在字典里的 N 个世界模型对应的原型余弦相似度 ℃ osine Similarity)， 得到路由得分 + 1 之间的选拔与归一化： 为了保证计算的高效和专注， 路由器会进行稀疏化， 只保留得分最高的前个专家 (Top-K)， 并使用一个带有缩放超参数丆的 Softmax 函数将它们转化为概率分布

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

B. 测试时原型优化 (Test-time Prototype Refinement) 当智能体遇到完全陌生的环境时， 固定的原型可能无法完美匹配当前观察到的特征。 TMoW 允许在不更新神经网络权重 （无需反向传播） 的情况下， 仅仅通过与环境的交互来动态微调原型。 原理与数学推导： 基于当前环境嵌入 0， TMoW 通过加权插值更新原有的原型： 这里的 Ap = （1 一 a sim(E(l)）） 筠 + a m 西 （0 p(l) )Ap 是一个优化项， 它是基于各个原型之间的相似度计算出的加权和： 这种微调使得原型空间能够向当前高频遇到的特征靠拢， 增强了对未知域的适应性。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

C. 基于蒸馏混合的模型增强 (DistiIled Mixture-based Model Augmentation) 如果新的环境与所有见过的领域都截然不同 （例如场景发生巨大变化 ), 仅仅调整路由权重是不够的。 此时， TMoW 会利用少量演示数据 (Few-shot demonstrations), 通过知识蒸馏构建一个全新的世界模型。 原理与数学推导： 新模型 m' 会先根据现有的路由得分进行初始化 （相当于集百家之长）： 新模型的原型 p 也会被计算出来并直接无缝整合到现有的路由系统中。 随后， 新模型只需在少量数据上进行微调：

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 18 OCR 未识别出有效文字：OCR did not return a result.]

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

工作流 (Algorithm 3 简化）： 初始聚合： 利用新域的少量样本通过图处理器获得路由得分， 按权重将现有适配器线性组合成 1 ． 一个新适配器 m'o 2 ． 快速微调： 使用这极少量的演示数据对 m' 进行微调。 系统集成： 将新微调好的 m' 及其计算出的原型加入到现有的世界模型集合中 （容量从 N 扩 3 ． 展为 N + 1)0

3.训练方法

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 1： 准备领域专属数据每个环境领域 j 都有其对应的专家演示数据集。 数据中的核心是 " 指令一轨迹 " 对， 其中轨 @凸， + 1 )}t 包含了智能体在环境中的序列变化， 即 " 当前观察状态、 执行的动作、 以及下一步的观察状态 + 1 "。 步骤 2： 独立训练各个领域的适配器 (LoRA) 对于每一个领域 j， 模型会在其专属数据集上抽取小批量数据单独训练对应的 LoRA 适配器 mj。 训练使用的核心损失函数是教师强制损失 (Teacher-forcing loss, CTF)' 它的作用是监督模型根据轨迹准确预测 " 下一个动作 " 和 " 下一个观察结果 “。

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 3： 提取多粒度原型 (Prototype Extraction) 在各自的适配器训练完之后， 框架会利用基于消息传递神经网络 (MPNN) 的图处理器， 遍历每一层 I， 从各领域的数据中提取出代表该领域特征的 " 多粒度原型 “ p。 这些原型将在后续的测试阶段用作路由分配的 " 指南针 "。 步骤 4： 混合世界模型的联合微调 (Joint Mixture Training) 为了让这些各自为战的适配器能够更好地协同工作， 框架会将所有的适配器 {mj}ff=l 与基础模型 M 组装在一起， 形成最终的混合架构 M {m, } 《 1。 接着， 将所有领域的数据集混合起来 （即 U 1 马）， 在这个联合数据集上进行再一次的微调训练。 此时的权重更新会作用于所有的适配器， 以优化整个混合体的协同预测能力： + 2 mj mj 一 V 屿 [ E （．， 刁 £ “@亻 {m } j j=l, 丆 〗 为 e { 1，

### （二）World Model Implanting：LoRA世界模型与主推理模型通过动态注意力结合

1.模型架构

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在 WorMl 框架中， 这二者是一种 * * " 大脑与外挂知识库 " * * 的解耦与动态协作关系。 主模型 （推理模型丌： 扮演 “ 大脑 " 和 " 通用协调者 “ 的角色。 它通常是一个具有强大通用逻辑的大型语言模型 （例如本研究中使用的 Llama-3.2-3B 或 11B 参数模型）， 负责提供通用的逻辑推理、 指令理解和高级任务规划能力。 在测试时， 主模型的参数是完全冻结的。 世界模型 (MI...MN): 扮演 " 特定领域的领域专家 " 或 “ 即插即用的记忆模块 "。 它们通常是参数量较小的模型 （例如 IB 参数模型）， 各自在特定的环境数据集 （如厨房、 卧室或特定游戏环境） 上独立训练。 它们不负责全局规划， 而是专门预测在自己熟悉的领域中 " 事物将如何转移 "、 " 哪些动作是可行的 （环境可供性） "。 这种关系带来了极强的扩展性： 当智能体前往一个新环境时， 主模型本身不需要重新训练， 只需 " 拔掉 " 旧环境的世界模型， " 植入 “ 新环境的世界模型， 就能迅速维持跨领域的适应力。

关于测试时更新：

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

场景： 零样本适应 (zero-shot Adaptation) _ 完全不更新权重在遇到完全没见过的新领域且没有任何演示数据时， 系统依赖仅在源域数据上训练过的模型。 此时， 整个模型不进行任何梯度反向传播或参数更新。 它完全依靠在元学习阶段培养出的那套 " 复合注意力网络 " 的固有参数， 实时、 动态地计算出当前的注意力权重分配 （即在 Test-time 仅做前向传播式的构成）。： 少样本适应 (Few-shot Adaptation) _ 只更新 “ 复合注意力模块 " 的权重如果系统在测试时得到了目标领域的极少量演示 （比如 l-shot 或 5-shot 数据）。 此时系统会进行高效的参数高效更新 (parameter-efficient updates)。 更新的对象仅仅是复合注意力模块 （0）。 具体来说， 只更新上述公式里的彬， WK, W6, 彬以及用于维度匹配的线性投影层 Lo 的权重矩阵。 因为只需要微调这个轻量级的 " 调度中心 "， 而不动大模型本身， 所以算力成本极低， 能够实现极快的数据高效适应。

2.算法与工作流

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 1： 基于原型的世界模型检索 (Prototype-based World Model Retrieval) 当智能体面对一个新状态时， 它不需要激活所有已知的世界模型， 而是仅检索最相关的 K 个模型。 状态表示与对象提取： 利用对象检测模型刀将环境状态转化为对象级状态， 并通过嵌入模型提取嵌入集合原型聚类： 为了避免计算两组海量嵌入之间的距离， 算法对历史数据集的嵌入集合使用 k-center 聚类方法提取 " 原型 ' ' (Prototypes) 尸。 聚类的数学优化目标为： = argmin max mlll 》 一 C C 匚 ] C 《 一 ceC 这确保了以极少的数据 （约 0 ． 1 ％ 的总嵌入数据） 代表整个特定领域的关键特征。 距离度量与检索： 给定当前状态的嵌入原型通过计算它与各个世界模型原型之间的 Wasserstein 距离，， 筛选出最匹配的 K 个世界模型集合 7 ． 0 步骤 2： 世界级复合注意力对齐 (World-wise Compound Attention)

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

线性投影 (Linear Projection)： 将每个世界模型的中间层表示映射到推理模型的嵌入空间维度中， 即 = L9(lM,)0

交叉注意力分为世界级和推理级两个步骤，前者是给不同世界模型分配权重，后者是给每个token分配权重。先对每一个token，在不同世界模型之间运行世界级交叉注意力：

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

世界级交叉注意力的主要职责是 * * " 知识整合 “ * *， 即根据智能体当前面临的状况， 决定各个世界模型的重要性并提取相关特征。 Query （查询）： 来自推理模型 （主模型） 当前层的隐藏状态 ／ 丌， 它代表了主模型当前的田考逻辑 " 或需求。 Key （键） 与 value （值）： 来自被检索出的 K 个世界模型的中间层输出 lMk。

经过世界级交叉注意力，对于每一个token，我们已经按照推理模型的需要，将不同世界模型的输出进行了加权求和。

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

推理级交叉注意力的职责是 * * " 知识对齐 “ * *， 即将上一步打包好的领域知识， 准确地 " 翻译 “ 并注入回主模型的推理空间中， 以指导下一步动作。 Query （查询）： 依然来自推理模型的隐藏状木 ／ / 凵、 丌召 0 · Key （键）： 所有相关世界模型输出的直接求和 (IMI + … + IMK)O · value （值）： 世界级交叉注意力刚刚输出的整合表示忉是气 Q，， 刁。

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一步： 矩阵乘法 （计算相关性得分） 主模型用它的 Query 矩阵去乘 Key 矩阵的转置： Scores = QRKR 这里 ]RNqxd × IR d><Nk 会得到一个形状为 Nq × Nk 的二维得分矩阵。 这个矩阵里的每一个元素 S,J， 代表着主模型序列中的第个 token, 对环境特征序列中的第 j 个对象 / 图块的 “ 需求程度 "。 第二步： 沿序列维度的 Softmax （真正的动态门控） Softmax 并不是对全局或单个标量做的， 而是沿着 Nk 所在的维度 （每一行） 进行的： Attention Weight s = Softmax 此 Softmax 会将每一行的分数转化为一个概率分伤。 这就意味着： 对于主模型当前思考的每一个 specific token, 它都会在 Nk 个环境特征中动态分配一个加和为 1 的注意力权重。

、用Softmax归一化后，再乘以各token的值（即世界模型输出聚合值）构成的矩阵。

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 3： 动作生成与环境交互由植入了领域知识的策略砌 = 0@荪山） 针对当前状态预测并输出环境动作。 智能体执行， 环境更新至下一状态 st + 1， 循环该过程直至任务完成。

## 三、结合线性注意力实现上下文学习

### （一）理论基础

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了从理论上厘清 ℃ L 在世界模型中的工作原理， 作者将 ℃ L 拆解为两种底层的涌现机制： 环境识别 (Environment Recognition, (R)： 模型将训练集中见过的所有环境动态编码进网络参数 (Parametric Memory) 中。 在推理阶段， 模型利用上下文 CT 仅仅是为了 " 识别 " 自己当前所处的是训练集中的哪一个环境 e， 识别完成， 它就调用对应环境的固化预测规则。 其概率可以表示为： 佐月@+ 1） _ Ep(eIqt, 屉@+ 1 励） eeE 环境学习 (Environment Learning, EL): 当面对开放世界或环境多样性极大时， 环境数量激增会导致 ER 机制失效。 此时模型不再依赖参数中死记硬背的特定环境映射， 而是将自身化作一个 “ 上下文记忆器 (ln-context memorizer)", 直接通过在当前上下文中实时收集和累积状态转移的证据来推测未来。 其公式为： p(qtICT)

> [图片 31：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

ER 的误差上界： T 仞刀月， 佐 0） 孓 min max TV(p€1， 佐 2） + min 丆 （，。， 佐 0） ,€2 eeE 上界中的最后一项 （最佳匹配误差 Best Matching Error) 是一个常数残差项。 这意味着即使给 ER 提供无限长的上下文 T， 它在未见环境中的误差也降不下去， 其泛化能力被结构性锁死。 EL 的误差上界 · 2p0 目 log （40 阄 TV(PEL, 佐 0） 孓与 ER 不同， EL 的误差上界会随着上下文长度的增加以 T 一 1 / 2 的速度不断衰减， 这意味着它具备真正的零样本泛化潜力。 理论结论： 要迫使模型放弃 ER 并涌现出更强大的 EL 机制， 必须同时满足三个条件： 极高的环境多样性、 较低的任务复杂度、 以及极其长久的上下文窗口。

### （二）算法工作流

> [图片 32：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

算法工作流如下： 观测编码 (Observation Encoding): 接收当前时间步的观氵则输入。 对于图像， 使用 1 ． ResNet 构成的变分自编码器 (VAE) 方将其压缩为低维隐状态的分布特征孬对于直接的低维物理特征， 则使用多层感知机 (MLP) 提取。 动作编码 (Action Encoding)： 使用感知机对模型采取的动作序列 c ] 进行降维 2 ． 映射， 得到隐表示。 3 ． 极长时序建模 (Temporal Modeling)： 由于不直接建模完整的状态估计概率， 模型将极长的历史隐状态和动作序列 @1，， 一 ·， 5） 作为上下文送入时间预测网络。 在训练阶段， 利用具有分块并行计算 ℃ hunk-wise parallelization) 能力的门控槽位注意力网络 (Gated Slot Attention) 进行编码以提升效率； 在推理阶段则保留循环形式， 持续更新记忆状态并输出当前步的隐藏时序特征。 状态与观测解码 (State & Observation Decoding)： 先通过状态解码器 gs 将映射为对 4 ． 下一步隐空间高斯分布的预测 &s,to 最后， 利用图像解码器将预测出的隐状态还原重建成下一帧预测的观测图像在。 整个过程依赖重建损失 £ 0 和状态转移的 KL 散度损失 Cs 进行优化。

### （三）实验验证

> [图片 33：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

作者在连续控制 （随机倒立摆） 和室内导航 （迷宫） 两个环境中验证了上述理论。 证明多样性驱动 E L (Cart-Pole 实验）： 只在 4 种倒立摆变体环境中训练的模型 (4-Envs), 在未见环境中的预测误差极高， 表现为典型的 ER 模式。 但当训练环境扩大到 8000 种 (8K-Envs) 时， 模型在遇到新环境时虽然前几步预测一般， 但随着 T （提供给模型的上下文步数） 增加， 其预测误差持续下降并突破 BME 误差红线， 成功展现出极强的 EL 泛化能力。 证明长上下文的重要性 （导航实验）： 在具有极大随机性和复杂纹理的迷宫数据集中， 作者验证了包含长轨迹 (32K-L 数据集） 训练出的 L2World, 在未见环境中的多步自回归预测 (PSNR 指标） 上显著超越了较短上下文的变体 (32K-S)0 由于架构瓶颈， 基于 LSTM 的 Dreamer-v3 和基于 Diffusion 的 NWM 根本无法有效利用这些长上下文数据， 难以涌现 ELO L2World 甚至在真实物理仿真环境 ProcTHOR 中也展现出了跨域零样本迁移的能力。

## 四、MPC中状态预测的动态更新

### （一）问题背景

MPC（模型预测控制）的经典范式中，没有显式的策略网络，而是通过在世界模型上用梯度下降优化动作参数（不更新世界模型的参数）：

> [图片 34：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 初始化： 从标准正态分布中随机采样， 初始化一个长度为 H 的连续动作序列 {åt}{l。 + 1 前向展开 (Rollout): 将初始的潜在状态 ZI 和当前的动作序列输入到冻结的世界模型九中， 通过递归调用九， 推断出最终的潜在状态切 + 1。 + 1 计算损失 (Loss)： 计算预测的最终状态 H + 1 与目标状态 zgoal 之间的距离作为损失函数： + 1 ZH+I 一 ZgoaI 2 goal 反向传播与更新： 因为世界模型是由神经网络 （如 ViT) 构成的， 它是端到端可微的。 算法会让梯度直接穿透整个世界模型， 计算出损失 Cgoal 相对于每一个输入动作的偏导数 V {。， } Cgoal。 然后通过梯度下降 （或 Adam 等优化器） 更新动作序列本身： + 4 { } { } 一 · V {åt } Cgoal

> [图片 35：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

问题所在： 世界模型通常是在离线的 “ 专家轨迹 " （Expert Trajectories) 数据集上通过 " 预测下一个状态 “ 的目标进行训练的。 然而， 在使用梯度下降法进行规划时， 模型却需要优化一个动作序列。 这种训练和测试时的不匹配导致了以下两个致命问题： 1 ． 2 ． 分布外状态 (Out-of-Distribution, 00D） 导致误差累积： 梯度下降在探索中间动作序列时， 常常会产生偏离专家行为的动作， 将世界模型推向训练时从未见过的潜在状态区域。 在这些区域， 模型的预测误差会急剧放大并随时间累积， 导致长期规划彻底失效。 优化地形崎山区 (Rugged Optimization Landscape)： 由世界模型诱导的动作层面的优化损失曲面往往非常不平滑， 包含大量极小值或平坦区域， 使得梯度下降极难找到全局最优解。

### （二）利用物理模拟器的在线世界建模

> [图片 36：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了解决分布外状态导致的误差累积问题， 在线世界建模通过使用真实的物理模拟器 (Simulator) 来 " 纠正 " 基于梯度的规划 (GBP) 所生成的轨迹， 并将这些纠正后的轨迹重新加入训练集中。 这迫使世界模型学习到规划算法在测试时会涉足的 " 非常规 " 状态区域， 从而扩大其可靠预测的范围。

> [图片 37：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在线世界建模工作流 (Algorithm 2） 该算法的工作流如下： 1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 初始化： 加载预训练的世界模型、 环境模拟器九、 编码器嗎， 以及专家轨迹数据集丆， 并初始化一个新的空数据集尸用于存放纠正后的轨迹。 采样与规划： 从数据集丆中采样一条轨迹， 提取初始状态 ZI 和目标状态 + 1。 使用梯度下降法 (GBP) 在当前世界模型上执行规划， 得到预测的最优动作序列 { }t 一 1 模拟器纠正： 将预测的动作序列 { } 放入真实的物理模拟器九中执行， 获得真实的后续状态序列 {。 特征编码： 使用编码器嗎， 将真实的后续状态序列转换为潜在空间表示 { 丬 }。 轨迹聚合： 将初始状态、 预测动作以及纠正后的真实潜在状态拼接成一条新轨迹） 并将其加入到新数据集尸中。 模型微调： 合并数据集， 使用下一个状态预测目标 (Next-State Prediction Objective) 对世界模型进行重新训练。 重复此过程。

### （三）对抗世界建模

> [图片 38：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在线世界建模虽然有效， 但在很多场景下调用真实物理模拟器的成本极高。 对抗世界建模提供了一种无需模拟器的方法。 它利用对抗训练的思想， 故意在状态和动作上添加最大化模型预测误差的微小扰动 (Perturbations), 从而平滑动作优化的损失曲面， 使梯度规划更加稳定。 对抗训练的目标函数为： min9E(ot,at,ot+I)、 丆 [ max e ze 狁为了保证计算效率， 该方法采用了快速梯度符号法 （Fa st Gradient Sign Method, FGSM) 来单步生成对抗样本， 而非耗时的多步投影梯度下降 (PGD) 对抗世界建模工作流 (Algorithm 3） 该算法的工作流如下： 1 ． 初始化： 加载预训练的世界模型九， 设置动作和状态的扰动缩放系数， 2 0 2 ． 采样与计算扰动边界 · 从数据集中采样一个批次 (Minibatch) 的轨迹。 · 计算该批次数据中动作和潜在状态的标准差， 乘以缩放系数， 得到最大允许的扰动半径和 0， 并设定上升步长 aa 和 az （通常为半径的 t25 倍）。

> [图片 39：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

初始化随机噪声： 从均匀分布 Uniform(—e, 6） 中为动作和状态采样初始随机噪声和 3 ． 生成对抗样本 (FGSM 攻击）： 对于序列中的每一个时间步仁 4 ． · 计算预测误差相对于噪声的梯度： 目 +， +） 一 Zt+1 2。 沿着梯度上升方向更新噪声 （即让模型预测变得更差）： + a · 5 匆 V。 使用截断操作 (Clip) 将噪声限制在最大扰动半径 [ 一 6 闰内。 · 将生成的对抗噪声加到原始动作和状态上， 得到对抗样本和丬。 5 ． 模型微调： 使用这些包含对抗扰动的轨迹替换原始轨迹， 基于下一个状态预测目标对世界模型九进行训练更新。 重复此过程。

### （四）实验结果

> [图片 40：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

论文在多个机器人操作任务 （如 PushT、 Rope、 Granular) 和导航任务 （如 PointMaze、 wall) 上评估了这两种方法。 性能大幅提升： 应用对抗世界建模后， 基于梯度的规划 (GBP) 在开环和模型预测控制 (MPC) 设置下均大幅超越了基线模型 (DINO-WM) 计算效率极高： 传统上， 为了避开梯度规划的陷阱， 研究者多采用免梯度的交叉熵方法 ℃ ross-Entropy Method, CEM), 但其需要海量采样， 计算极慢。 而对抗世界建模賦能的梯度规划， 不仅在成功率上追平甚至超越了 CEM, 还将计算时间压缩到了 CEM 的 ℃ ％ 左右。 鸿沟成功闭合： 实验数据证明， 经过这两种方法微调的模型， 在面对规划算法生成的轨迹时， 其预测误差与面对专家轨迹时的误差差异显著缩小甚至反转， 成功闭合了 Train-Test Gap。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
