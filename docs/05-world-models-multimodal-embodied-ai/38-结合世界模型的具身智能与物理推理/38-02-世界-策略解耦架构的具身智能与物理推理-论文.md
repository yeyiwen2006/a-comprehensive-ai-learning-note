---
title: "38.2 世界-策略解耦架构的具身智能与物理推理（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/38.结合世界模型的具身智能与物理推理/38.2 世界-策略解耦架构的具身智能与物理推理（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 38.2 世界-策略解耦架构的具身智能与物理推理（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、世界模型作为具身策略模型的训练与评测环境

### （一）问题背景

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

随着通用机器人策略 （如基于大模型的 Vision-Language-Action 策略） 的发展， 它们能够通过自然语言指令在各种环境中执行任务。 然而， 评估这些策略面临巨大挑战： 硬件评估成本高昂且危险： 在真实硬件上涵盖所有标称 (Nominal) 和边缘 (Edge-case) 场 1 ． 景是不切实际的。 特别是评估 " 语义安全性 " （Semantic Safety， 如不把塑料放在炉子上） 时， 真实测试可能会危及机器人、 环境或人类。 2 ． 传统物理模拟器的局限： 传统模拟器需要耗费大量精力手动创建逼真的资产， 难以精确模拟非刚体和接触动力学， 且存在巨大的 " Sim 一 to 一 Rea | “ （仿真到现实） 视觉差异。

可以利用世界模型作为测试具身策略的环境，利用其在海量网络视频上训练获得的生成能力，来替代物理引擎进行仿真。在具身智能的训练方法从以模仿学习为主转向与RL结合的背景下，这就显得尤其重要。

### （二）核心方法

以Google的研究为例，为了让视频生成模型理解机器人动作，可在覆盖多种操作技能和场景的大规模机器人数据集上对Veo进行微调，随后开始利用该世界模型作为评估机器人的环境。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

该视频模拟器评估策略的完整工作流如下： 1 ． 2 ． 3 ． 4 ． 5 ． 场景初始化与编辑： 获取真实世界的一帧初始 RGB 图像。 如果需要进行分布外 (OOD) 评估或安全红蓝对抗 (Red Teaming)， 系统会使用图像生成模型 Gemini 2 · 5 Flash lmage （代号 NanoBanana) 根据语言描述对初始帧进行生成式编辑 （例如： 添加新物体、 改变背景）。 多视角合成 (Multi-view Synthesis)： 由于 ALOHA 2 枧器人包含 4 个扌聂像头 （顶视、 侧视、 左右手腕 ), 系统使用微调后的 veo 2 模型， 仅凭单一的俯视纟扁辑图像， 合成出其他三个视角的初始观测图像。 策略推出 (PoIicy Rollout / Action Conditioning): 将合成的多视角初始帧和任务指令输入给待评估的机器人策略 （如基于 Gemini Robotics On-Device, 简称 GROD 的策略）。 策略输出未来的一系列机器人位姿 (Poses)。 视频生成： 微调后的 veo (Robotics) 模型以 " 当前图像观测 " 和 " 未来的机器人位姿序列 “ 为条件， 自回归地生成连续的未来视频帧。 结果判定： 人类评估员对生成的 8 秒长视频进行二元成功 / 失败 (Success/Failure) 打分， 以此预测策略在真实世界的表现。

### （三）块级预测

世界状态预测（视频生成）需要耗费的算力显著大于动作预测需要的算力，因此我们考虑不必总是逐帧预测，而是采用块级预测：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

状态采样： 首先， 系统从包含真实交互记录的离线经验回放缓冲区刀中， 采样出一个历史观测状态作为起始占 + 4 动作预测： VLA 策略模型接收当前状态和任务语言指令 I, 一次性预氵则出一个包含个连续动作的 “ 动作切块 "： ·， at+k—l

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在这个阶段， UMM-World 展现出了它与传统视频预测模型最大的不同： 直接跳跃预测： UMM-World 不会去逐帧渲染乯 + 1 + 2， · · + 一 1 时刻的冗余物理中间过程， 而是直接建模条件概率 st+k ～）。 它一步到位地输出执行完整个动作切块后的第 + 帧图像。 + 2

### （四）交错视角解码

机器人往往有多个摄像头，这些摄像头所获得的图像信息必须满足三维一致性，即共同构成一个一致的三维世界。为了解决多个视角的一致性问题，UMM-World等采取的方法是不孤立地生成每个视角的图像，而是将头部视角、腕部视角等放在同一个序列中交替解码，每次先生成其中一个视角，再把该视角的图像放入上下文，以此作为条件生成下一个视角，以此类推。

### （五）块级奖励评估

目前，生成式世界模型仍存在误差累积的问题。而很多机器人任务都是稀疏奖励的，很小的视觉偏差，就可能把一个状态从成功翻成失败，或者反过来，对强化学习影响巨大。短期内为避免该问题，可采取块级奖励评估，每完成一个小块的动作，就给予奖励：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

生成 “ 的图像后， UMM-World 会立即切换身份， 充当一个 " 视觉一语言任务裁判 “ （Reward Model) + 1 0 /、 统会向 UMM-World 输入一个结构化提示词 （例如： " 仔细检查物体状态， 判断任务 [ 折叠毛巾 ] 是否成功完成， 必须回答 Yes 或 №。 "） + 1 0 模型根据新生成的图像回答 Ye s 或 No， 从而输出二元奖励信号。 这个块级别的奖励等效于这个步骤中每个单步奖励的贴现总和： + 1 = 乥 7 ' 一 1 T(St+i,

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

连续推演： 生成的未来状态 st+k 会作为新的起点反馈给 VLA 模型， VLA 继续预测下一块动作 + UMM-World 再预测 + 2 左。 如此循环 “ 次， 形成一条由世界模型 “ 想象 " 出来的合成交互轨迹。 ' + 4 强化学习优化： 最终， 算法使用 Flow-Noise （一种 PPO 的变体） 在这条极大地缩短了视界的合成轨迹上计算优势 (Advantage), 并更新 LA 模型的权重参数， 完成一次策略迭代。

### （六）反映用世界模型评估机器人准确性的指标

以Google的研究为例，使用了以下指标：

1.MMRV

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

MMRV 用于衡量 “ 视频模型预测的策略扌非名 " 与 " 真实世界测得的策略排名 " 之间的一致性。 较低的 MMRV 值表示较高的排名一致性， 其值域范围为 [ 0， 1 ]。 给定 “ 个不同的枧器人策略丌 1， · · ” 丌 “ 它们在真实世界中的成功率记为月尸 I， 在 veo pred Rpred。 M M RV 的定义如下： 模拟器中预测的成功率记为月 1 乥 max RankViolation@， 力 MMRV： 1 孓 j “ 其中， **RankViolation （秩违反度） * * 的计算方式为： pred RankViolation 佞力： = 一月！。 · （月 pred

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

卧 ] 是指示函数 (lndicator Function)0 如果视频模型对策略和的强弱排序预测， 与真实世界中的实际强弱扌非序不一致， 则该函数值为 1； 若一致则为 0。 一 1 表示两个策略在真实世界中成功率的实际差距。 因此， MMRV 惩罚的是那些 “ 实际性能差距很大， 却被模拟器扌非错序 " 的情况。 如果两个策略实际表现本就很接近而被扌非错， 系统受到的惩罚会相对较小。

2.预测成功率与真实成功率之间的线性相关程度：可用皮尔逊相关系数等量化。

## 二、策略模型在世界模型中展开MCTS

### （一）核心思想

传统的基于模型的强化学习（Model-based RL）试图通过学习来重建整个环境状态或视觉像素流来解决这个问题。潜变量动力学路线将其简化为生成环境对应的潜变量，但由于存在视觉重建损失，事实上仍然试图压缩学习环境的所有要素，仍可能将模型容量浪费在与规划决策无关的视觉细节上。

将世界模型直接用于搜索规划的路线放弃了“重建真实环境”的执念。在智能体系统中，策略网络先运用蒙特卡洛树搜索等规划算法进行推演规划，再在现实世界中执行行动。这里世界模型用于MTCS阶段，世界模型完全服务于规划。

MeZero即这样的范式的初始形态。世界模型内部构建的“隐藏状态”没有任何特定的环境语义约束，只要这个隐式状态能准确预测对规划最直接相关的三个核心量即可：奖励（Reward）、策略（Policy）和价值（Value）。

### （二）模型架构

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 表征函数 (Representation function) 九： · 将过去的真实历史观测值 （如连续的棋盘画面或雅达利游戏画面） 01， · · ·， 映射为初始的隐藏状态 50。 · 它是内部规划的起点： 50 = 厉@1， · 动态函数 (Dynamics function) g： · 负责在脑海中模拟环境的动态转移。 给定上一步的隐藏状态計一 1 和当前假设采取的动作 a， 它能生成下一个隐藏状态計并预测即时奖励。 状态与奖励的转移方程： 产， 5 预测函数 (prediction function) · 负责对当前局势进行评估。 根据当前的隐藏状态計， 预测该状态下的动作选择策略 p （该下哪一步） 以及价值函数 （当前的胜率或预期收益估计）。 策略与价值预测方程：，

这里只对环境观测进行了嵌入处理，事实上可以将动作也编码为隐空间中的特征向量，以让物理上结果相似的动作在嵌入空间中被拉近，赋予了智能体极强的泛化能力，它在测试某个动作时学到的规律，能够自动泛化到特征空间相邻的其他动作上，提升了学习效率。

在连续控制任务中，策略网络的输出也可以采用高斯分布来表示连续动作空间中的策略。

输入处理网络：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

二维图像输入 (Vision)： 对于类似 Atari 或基于摄像头的视觉控制任务， Ez-\/2 基本沿用了 EfficientZero 验证过的高效残差卷积架构。 一维状态输入 (Proprio Control)： 为了处理连续控制中的低维向量输入 （如机器人关节角度、 速度等 ), E z -V2 使用了一种变体架构， 将所有的卷积层替换为全连接层。 它还引入了运行均值模块 (Running mean block) 来归一化输入特征， 并采用了 Pre-LN Transformer 风格的残差模块以防止梯度爆炸和提升表达能力。 + 2

### （三）为什么动态函数是完全确定性的，而不是像SSM那样存在随机采样？

RSSM的核心任务是重建和预测未来的实际观测（如像素画面）。如果环境存在随机性，确定性网络往往会输出所有可能未来的“模糊平均值”，因此RSSM必须引入隐式的随机变量来学习真实环境的概率分布。

而MuZero走的是“价值等价”路线。它的内部隐藏状态没有任何具体环境状态的语义约束。这个隐藏状态存在的唯一目的，就是为了准确预测未来相关的三个量：策略、价值和奖励。 既然价值在强化学习中本身就是对未来累计收益的数学期望，确定性的动态模型只需要学会转移到一个能正确输出该期望价值的抽象状态即可，完全不需要像RSSM那样去精确采样还原出每一条具体的随机物理分支。

同时，MuZero高度依赖蒙特卡洛树搜索（MCTS）进行规划。现在的MTCS算法（包括LLM中运用的MTCS）中状态转移是完全确定性的，它的随机性只存在于“动作选择”阶段，不涉及环境反馈的随机性。一旦对于给定的动作，环境反馈具有随机性，就会变为随机树搜索，计算量大大增加。

### （四）算法工作流

MuZero在世界模型中进行蒙特卡洛树搜索，而在真实环境中进行实际行动，每次行动后会根据实际数据对世界模型进行更新。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 规划 (Planning): 在隐式模型中推演在真实环境的每一个时间步， MuZero 会使用蒙特卡洛树搜索 (MCTS) 在学到的隐藏状态空间中进行未来轨迹的推演。 · 它从表征网络提取的当前局势隐状态 50 出发， 沿着决策树向下搜索。 · 在树的内部推演时， 每次假想执行一个动作， 就调用动态函数 g 获取假想的奖励和新状态 5。 随后立刻调用预测函数获取对新状态的策略和价值评估， 用于引导后续的搜索方向。 · 最终， MCTS 会综合成百上千次推演的结果， 输出一个比单次网络预测更优的推荐策略 (Search policy) 和搜索价值评估。 2 · 行动 (Acting)： 与真实环境交互代理 (Agent) 根据 MCTS 规划阶段输出的策略进行动作采样， 在真实环境中执行动作 at+lo 真实环境接收动作后， 推进时间， 返回新的观测画面仂 + 1 和真实的奖励信号 + 1。 这一步产生的数据轨迹 （包含历史观测、 实际动作、 MCTS 策略、 真实奖励等） 会被存入经验回放缓冲区 (Replay Buffer) 中， 作为日后训练的宝贵教材。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

3 · 训练 (Training)： 端到端的模型对齐在训练阶段， 算法会从回放缓冲区中抽取一段真实发生的历史轨迹。 表征函数九提取出初始状态后， 模型会在脑海中 " 展开 (Unroll) “ 连续预测未来 K 个步骤 （实验中 = 5）。 所有的网络参数 0 通过基于时间的反向传播 （B PTT) 进行端到端的联合训练。 其总损失函数设计如下： lt(0) _ 乥宀） + 1V(zt+k,） + 萨@艹 + 0@目 2 这三个核心损失项强制模型校准自己的预测能力： 策略损失地迫使模型自己预测的快速策略逼近经过深思熟虑的 MCTS 搜索策略 + 价值损失： 迫使模型预测的局势价值逼近最终的真实结果或多步自举 (Bootstrapping) 的目标价值 + 奖励损失 lr： 迫使动态网络预测的虚构奖励逼近真实世界反馈的观测奖励 +

最后一项为参数L2正则化。

后来的EfficientZero还加上了自监督一致性损失，这是类似于潜变量动力学路线的损失项，即根据下一状态输入对应的真实环境状态和预测的下一环境状态的差距计算损失：

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

m、 St + 1， 5 + 1 真实的目标隐状态。 它是把真实的下一帧图像 + 1 送入表示网络 (Representation Network） 得到的编码向量。 在自监督学习中， 它代表了 “ 正确答案 "。 岛 + 1： 预测的隐状态。 它是模型利用当前状态和动作， 通过动态网络 (Dynamics Network 0) “ 想象 " 推演出来的下一帧结果。 PI: 投影器 (projector)。 这是一个 3 层 MLP 网络， 用于将特征映射到一个更适合计算对比损失的高维空间中。 它同时作用于真实分支和预测分支。 + 3 P2： 预测器 (predictor)o 这是一个额外的 2 层 MLP 网络， 仅作用于预测分支 + 1 的分支）。 它的作用是对齐两个分支的非对称性， 这也是 SimSiam 架构的核心设计。 + 4 sg(•): 停止梯度 (Stop-gradient) 操作。 这是整个公式最关键的技巧！ 它意味着在计算损失并反向传播时， 绝对不允许梯度流向真实观测的分支。 也就是说， 真实图像编码出的特征 + 1 就像一个固定住的 " 锚点 (Anchor)", 强制要求预测出来的特征向它靠拢， 而不是让两者一起坍缩成零 （防止模式坍缩） + 2 £ 2： 负余弦相似度 (Negative Cosine SimiIarity)。 衡量两个高维向量在方向上的对齐程度。

### （五）经验回放的异策略修正（重分析）

我们知道，世界模型用于MCTS的工作流如下：

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 环境交互与数据收集 (SeIf-Play) · 将当前观察到的图像序列传入表示网络获取隐状态 sto · 在基础上， 利用动态网络 g、 价值网络 V 和策略网络在大脑中 " 模拟展开 "， 执行 MCTS （蒙特卡洛树搜索）。 MCTS 结束后输出优化的动作访问概率分布。 智能体根据该分布在真实环境中执行动作， 获得真实奖励。 · 将产生的数据元组存入优先经验回放缓冲区 (Prioritized Replay Buffer)0 数据重分析与目标制备 (Reanalyze & Target Preparation) 后台进程不断从 Replay Buffer 中采样历史轨迹。 根据数据的 " 陈旧程度 " 动态计算截断步长 ／： I = C'urrent 使用 GPU 和当前的最新模型参数， 在历史状态上重新运行 MCTS, 获得修正后的价值目标和策略目标 （应用 3 ． 3 节的异策略修正机制）。 模型参数优化 (Network Training) 汇总上述的所有损失项， 计算总步级损失）：

在这个过程中：

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在传统的强化学习中， 更新当前的价值网络需要构造一个目标值 (Target value， 即 (t)o 在普通的 Muzero 中， 这个目标值是通过查看经验回放缓冲区 (Replay Buffer) 里未来左步的真实奖励， 加上第步的预测价值算出来的。 这里存在一个严重的异策略 (Off-policy) 偏差 · 缓冲区里的数据是由一个 " 很老、 很笨 “ 的旧策略收集的。 如果在计算目标值时， 完全采用旧策略在环境中乱走的轨迹， 那么这个低质量的目标值就会把当前更聪明的模型 " 带偏 " EfficientZero 的修正逻辑如下： 1 ． 2 ． 3 ． 动态截断旧动作， 保留部分真实反馈： 公式 I = 」） · 忉 （1， 的意义在于评估数据有多老。 数据越老， I 越小。 算法会沿着旧轨迹真实执行步的旧动作， 拿到环境中真实的奖励反馈。 这确保了目标值的计算不会完全脱离真实的物理环境。 用最新模型 “ 接管 " 未来预测： 在沿着旧轨迹走到第 I 步的状态 st+l 时， 算法不再继续顺着旧轨迹走， 而是调用当前的最新模型， 在 5 衅 I 这个状态上重新进行一次蒙特卡洛树搜索 (MCTS)0 重构高质量目标值： 通过最新参数运行的 MCTS， 算法得到了一个更能反映当前策略实力的根虱亻 C S 节占价值。 用它来替代原本的旧价值， 修正后的目标值公式变为： t+l I 虱亻 C S 7 + 7 vt+l

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在传统的强化学习中， 更新当前的价值网络需要构造一个目标值 (Target value， 即 (t)o 在普通的 Muzero 中， 这个目标值是通过查看经验回放缓冲区 (Replay Buffer) 里未来左步的真实奖励， 加上第步的预测价值算出来的。 这里存在一个严重的异策略 (Off-policy) 偏差 · 缓冲区里的数据是由一个 " 很老、 很笨 “ 的旧策略收集的。 如果在计算目标值时， 完全采用旧策略在环境中乱走的轨迹， 那么这个低质量的目标值就会把当前更聪明的模型 " 带偏 " EfficientZero 的修正逻辑如下： 1 ． 2 ． 3 ． 动态截断旧动作， 保留部分真实反馈： 公式 I = 」） · 忉 （1， 的意义在于评估数据有多老。 数据越老， I 越小。 算法会沿着旧轨迹真实执行步的旧动作， 拿到环境中真实的奖励反馈。 这确保了目标值的计算不会完全脱离真实的物理环境。 用最新模型 “ 接管 " 未来预测： 在沿着旧轨迹走到第 I 步的状态 st+l 时， 算法不再继续顺着旧轨迹走， 而是调用当前的最新模型， 在 5 衅 I 这个状态上重新进行一次蒙特卡洛树搜索 (MCTS)0 重构高质量目标值： 通过最新参数运行的 MCTS， 算法得到了一个更能反映当前策略实力的根虱亻 C S 节占价值。 用它来替代原本的旧价值， 修正后的目标值公式变为： t+l I 虱亻 C S 7 + 7 vt+l

### （六）多步价值估计网络的训练（价值前缀）

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

原理： 作者提出了一个非常符合直觉的改进： 人类在玩游戏时， 很难精准预测出 " 我会在第 15 帧因为不移动而丢球 "， 但很容易预测出 " 如果在一段视野内我都不动， 我肯定会丢球 "。 因此， EfficientZero 不再强制模型逐帧预测瞬时奖励， 而是利用 LSTM 直接预测一段未来轨迹中的累积折扣奖励之和 （即价值前缀 value prefix)。 数学表达： 价值前缀定义为： 二 0 7 0 + EfficientZero 设计了一个网络架构 （实验中使用 LSTM), 以上下文展开的隐状态序列为输入， 端到端地输出这个标量前缀： value-prefix 一 ·， St + 左一 1 由于 LSTM 具有记忆能力， 它可以自动平滑和整合中间因 " 状态混淆 (State Aliasing)" 带来的不确定性， 从而更稳健地指导 MCTS 的探索。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

原理： 作者提出了一个非常符合直觉的改进： 人类在玩游戏时， 很难精准预测出 " 我会在第 15 帧因为不移动而丢球 "， 但很容易预测出 " 如果在一段视野内我都不动， 我肯定会丢球 "。 因此， EfficientZero 不再强制模型逐帧预测瞬时奖励， 而是利用 LSTM 直接预测一段未来轨迹中的累积折扣奖励之和 （即价值前缀 value prefix)。 数学表达： 价值前缀定义为： 二 0 7 0 + EfficientZero 设计了一个网络架构 （实验中使用 LSTM), 以上下文展开的隐状态序列为输入， 端到端地输出这个标量前缀： value-prefix 一 ·， St + 左一 1 由于 LSTM 具有记忆能力， 它可以自动平滑和整合中间因 " 状态混淆 (State Aliasing)" 带来的不确定性， 从而更稳健地指导 MCTS 的探索。

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 原始 MuZero 的 MCTS 步骤 （单步标量累加） · 节点扩展： 当 MCTS 搜索树向下展开一个新节点时， 动态网络会预测出一个隐状态 + № 奖励获取： 奖励网络会针对这个孤立的隐状态预测一个单步标量奖励 0 + № Q 值计算： 为了计算某个动作的总价值用于 UCB 公式， MuZero 需要顺着树的路径， 把沿途每个节点预测出的单步奖励进行折扣累加： Q(st, a) = E 一 0 0 + 7 +。 + 1

这里v_t+k表示叶子节点的价值估计。

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · EfficientZero 的 MCTS 步骤 (LSTM 序列端到端输出） 节点扩展： 当向下展开节点时， 动态网络预测出隐状态韧。 此时， 奖励网络被替换为了一个带有记忆的 LSTM 网络。 前缀获取： 模型不再针对孤立状态预测单步奖励。 相反， LSTM 会接收当前的隐状态韧以及上一层传下来的隐藏状态上下文 + 仁 1， 直接输出一个累积标量 0 （在代码架构中， 代表的正是公式中的一 0 7 + 即价值前缀）。 Q 值计算： M CTS 在计算 U CT 中的 Q 值时， 不再需要手动顺着树向上回溯累加奖励。 对于到达的第层节点， LSTM 直接吐出的那个标量， 加上第层的预测价值 + 左的折扣， 就是最终的 Q(st, a)。 + 2

这里相当于用LSTM聚合了k次遇到的真实奖励，再加上叶子节点的价值估计。但LSTM存在衰减问题，偏重后期奖励，而RL中我们实际需要偏重早期奖励。为了解决这个问题，实际上LSTM一般走若干步（如5步）就重置一次，而不是一次性走到底；另外，奖励预测的LSTM用了每步监督的损失函数：

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第 1 步输入岛 + 1： LSTM 吐出一个标量。 它的监督标签是。 第 2 步输入岛 + 2： LSTM 结合记忆吐出下一个标量。 它的监督标签是砌 + 7 + 1。 第 3 步输入 + 3： 监督标签是 + 7 + 1 + 7 砌 + 2。 算法通过计算这展开的几步中每一步的损失并求平均， 作为最终的网络更新梯度 （即论文公式 6 中 unroll 1 1

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在 EfficientZero 中， LSTM 每步预测的 " 价值前缀 (Value Prefi 幻 “ 的监督信号 （即真实的标签 Ground Truth), 直接来源于智能体过去在真实环境中收集并存放在经验回放缓冲区 (RepIay Buffer) 中的真实奖励序列。 由于模型的训练是后台进程从经验回放缓冲区中采样历史轨迹来进行的， 对于采样到的任意一条轨迹片段， 算法已经 " 预知 " 了该片段在当时每一步所获得的真实环境奖励 （在论文中记为， 以区别于模型预测的奖励）。 锐 t + 锐 + 2， · 因此， 当动态模型在时间轴上向前展开 (Unroll) 并由 LSTM 逐步接收隐状态时， 算法可以利用这些已知的真实环境反馈， 为 LSTM 的每一步精确计算出对应的折扣累加目标作为监督信号： 第 1 步展开： LSTM 输出第一个标量预测。 此时的监督信号是单步的真实奖励。 第 2 步展开： LSTM 结合记忆输出第二个标量预测。 此时的监督信号是两步的真实折扣奖励之和 + 7 + 1。

需要注意的是，前面说的经验回放时用MCTS重分析和训练LSTM是异步进行的。训练LSTM时，只抽取真实走过的历史轨迹，而不关心这个轨迹的每一步动作是怎么被搜索出来的。

## 三、策略模型在世界模型中用Gumbel搜索估计价值

### （一）基于采样的Gumbel搜索

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

EfficientZero： 使用传统的蒙特卡洛树搜索 (MCTS) 作为策略改进算子， 这在面对庞大甚至连续的动作空间时， 搜索模拟的计算成本会呈指数级增长。 EZ-V2: 引入了基于采样的 Gumbel 搜索 (SampIing-based Gumbel Search)。 这种搜索方法即使在有限的模拟预算下也能保证策略的改进。 为了适应连续控制， 它在搜索的根节点通过混合当前策略先验分布来进行动作采样， 使得搜索不仅极大降低了计算成本， 还在高维动作空间中实现了极佳的探索与利用平衡。 + 2

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了在连续和高维动作空间中进行高效的树搜索， E z -V2 引入了 " 基于采样的 Gumbel 搜索 "。 它能够在有限的模拟次数下， 实现对探索与利用的最佳平衡， 并能在数学上严格保证策略改进 (PoIicy lmprovement)o 数学推导与保证： 对于给定状态 5， 算法使用 Gumbel-Top-k 技巧采样 K 个动作。 为了保证探索性， 动作集 As 被分为两部分： SI 采样自当前策略， 而采样自一个更平缓的先验分布。 多臂老虎机 (Bandit) 算法会从 As 中选出具有最高 Q 值的推荐动作。 这一过程的策略改进性质可通过以下不等式证明： q(s,a) a) q(), a*s) max 由于是整个集合中的最大值， 其必然大于等于子集的平均值。 进一步地， 随着采样数量 s 到 +， 根据大数定律， 我们有： 因此， 最终推荐动作的价值严格大于或等于当前策略的期望价值， 在数学上实现了策略的单调提升。

### （二）经验回放的运用：基于搜索的价值估计

1.如何估计经验回放中动作的价值

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

EfficientZero： 在重新分析 (Reanalyze) 历史旧数据时， 使用基于自适应步长的时序差分 (TD) 方法来构建目标价值。 但旧策略产生的数据 (Stale transitions) 依然会导致离策略 (Off-policy) 误差。 EZ-V2: 提出了基于搜索的价值估计 (Search-based Value Estimation, SVE)。 它不再依赖旧的 TD 目标， 而是利用当前最新的环境模型和策略， 在大脑中重新展开搜索树， 并将搜索根节点的经验均值作为目标价值。 这不仅能更有效地利用训练早期的历史数据， 还从根本上缓解了严重偏离策略的问题。 对于该估计器的误差， 论文还给出了严格的数学理论上界证明。

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在强化学习理论中， 绝对的 " 真实价值 " 是指从当前走到游戏结束的实际总得分。 但因为游戏过程往往很长， 算法无法总是等待回合结束， 因此需要构建一个高质量的近似值， 即目标价值 (Target Value)0 E z -V2 提出了 * * 混合价值目标 (Mixed value Target) * * 枧制， 这意味着目标价值荔的来源是动态切换的， 它融合了真实环境和树搜索的结果： 场景 A： 处理刚收集的 “ 新数据 " （预计算优先级时） 当数据工作进程刚刚在真实环境中执行完动作， 并准备将其放入经验回放缓冲区时， 这条数据非常新鲜。 此时的目标价值主要依赖于真实环境互动的结果。 算法会使用多步时序差分 (Multi-step TD Target)： 把在真实环境里刚拿到的未来几步真实奖励累加起来， 再加上未来某个状态的价值预估。 此时， 真实物理世界的反馈占据了主导地位。 场景 B： 处理缓冲区里的 “ 旧数据 " （重分析 Reanalyze 时） 当数据已经在缓冲区里存放了一段时间， 收集数据时的旧策略已经落后于当前的最新模型 （即离策略问题）。 此时如果再盲目相信当时的真实环境奖励， 反而会误导网络。 因此， 目标价值会切换为基于搜索的价值估计 (SVE)0 此时的目标价值纯粹是蒙特卡洛树搜索 （基于采样的 Gumbel 搜索） 的结果。 算法利用当前最聪明的大脑 （最新模型参数）， 在旧状态上重新展开一棵搜索树， 将大量模拟路径的平均回报作为新的目标价值。

用这个混合目标作为“真实价值”来指导价值网络的更新。

2.基于搜索的价值估计（SVE）的具体原理

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在极低数据量下， 算法必须反复重用旧策略收集的陈旧数据， 这会导致严重的离策略 (Off-policy) 偏差。 E z -V2 摒弃了传统的 N 步自举方法， 提出了 SV E 机制： 利用最新的环境模型在大脑中进行 " 想象搜索 "， 并将搜索树根节点的经验均值作为修正后的目标价值。 价值目标定义： E 《 0 （50） 其中 N 是搜索的模拟次数， （50） 是第 “ 次节点展开时的自举估计： H(n) （50） = 乥其中 H(n) 为搜索深度， 为模型预测的瞬时奖励。

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

SV E 误差上界的严谨推导： 模型预测不可避免存在误差。 假设状态转移、 奖励和价值预测的误差上限分别为、 0 和 0， 且奖励函数具有 Lr-Lipschitz 连续性， 价值函数具有厂 Lipschitz 连续性。 SVE 估计的均方误差 (MSE) 可分解并由柯西不等式放缩： 一） ] 2 《 e2s + 24 E[(V(St) 一诿@）） ] 孓 2 + 2 H(n) 最终推导出 SVE 的理论误差上界为： N MSEv(Vs) 平乥乥 72 气 + + 7 “ "） (Löes2 +） 这个定理表明： SV E 误差的级数由于衰减因子 7 的存在是收敛的； 且当环境模型趋于完美 (e -+ 0） 时， SV E 的估计误差将严格趋近于 0。

3.优先级预计算

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

EfficientZero： 按照常规做法， 当新收集的轨迹刚存入经验回放缓冲区 (Replay Buffer) 时， 将其优先级设置为当前缓冲区内的最大优先级值。 EZ-V2: 提出了一种预热 (warm-up) 机制。 在将新数据存入前， 算法会使用当前最新的模型对其进行一次预计算， 直接求出它的贝尔曼误差 (Bellman error)。 这使得那些真正蕴含丰富学习信号的新数据能获得极高的回放概率， 从而进一步榨取有限数据的价值， 提升了样本效率 + 1

贝尔曼误差指的是价值网络的预测（“直觉”）和上述混合价值目标（实际价值）之间的差距，以此调整价值网络。

### （三）基本工作流

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

步骤 1： 数据收集工作流 (Data Workers / Self-play) · 数据工作进程 (Data workers) 定期从主节点获取最新更新的神经网络权重参数。 · 在真实环境中， 使用当前的观测输入表征网络得到状态 st。 调用基于采样的 Gumbel 搜索在脑海中进行规划， 输出推荐的动作并与真实环境交互。 · 将生成的交互轨迹 （包含观测、 动作、 真实奖励） 通过优先级预计算 （计算 Bellman 误差以赋予初始优先级） 后， 存入经验回放缓冲区 (Replay Buffer)0 步骤 2： 数据重分析工作流 (Batch Workers / Reanalyze) 批处理工作进程不断从经验回放缓冲区中采样历史轨迹。 使用当前最新的模型重新审视这些旧数据 (Reanalyze)0 算法不会沿用当时旧策略生成的目标， 而是针对旧状态， 重新运行一次 Gumbel 搜索。 通过最新的搜索， 为这些旧状态生成更新、 更准确的 SVE 价值估计 （或混合价值目标） 以及最新的目标策略。 步骤 3： 模型优化工作流 (Learner) · 学习器 (Learner) 接收上述经过重分析洗礼的 Batch 数据。

> [图片 31：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

结合前面的机制， 网络最终沿时间轴展开 I 灬 7、 步， 进行端到端的综合梯度下降优化： I u “ 7 ℃ 一 1 unroll

对于经验缓冲区中每个选到的数据：

> [图片 32：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

结合前面的机制， 网络最终沿时间轴展开 I 灬 7、 步， 进行端到端的综合梯度下降优化： I u “ 7 ℃ 一 1 unroll

## 四、策略模型在世界模型中并行搜索评估：扩散模型预测控制（D-MPC）

扩散模型预测控制（Diffusion Model Predictive Control，D-MPC）曾提出了在RL环境中运用世界模型展开并行搜索的方法。

### （一）模型架构与工作流

> [图片 33：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 离线模型学习阶段 (Offline Model Learning) 在这个阶段， 算法利用预先收集的离线数据集 （包含状态、 动作和奖励的轨迹数据） 来训练三个关键组件： 多步动力学扩散模型佐： 该模型不再是一步步预测， 而是给定当前状态、 历史信息和未来一整段动作序列 “ 丆一 1， 直接联合生成未来丆步的完整状态轨迹 + “ + 丆。 这极大地缓解了复合误差问达。 多步动作提议扩散模型 p: 通过行为克隆 (Behavior Cloning) 的方式， 学习离线数据中的专家或次优策略， 给定当前状态和历史， 直接生成未来丆步的候选动作序列 “ F 一 1。 这为规划器提供了一个极佳的搜索起点， 避免了盲目搜索。 启发式价值函数 J： 使用 Transformer 架构训练一个回归网络， 用于评估某一段状态一动作轨迹的未来折现奖励总和。 其数学目标如下： J （5 十 + 丆， at:t+F—1

这里的“复合误差”指的是自回归中的误差累加。

> [图片 34：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 · 在线规划与控制阶段 (Online Planning) 在实际与环境交互时， D-MPC 采用了一种简单而高效的 " 基于采样的规划器 " （Samp 丨 ing 一 based Planner)： 步骤 1 （采样候选动作）： 利用训练好的动作提议扩散模型生成 N 个合理的未来步动作序列集。 步骤 2 （预测未来状态）： 将这 N 个动作序列输入到动力学扩散模型中， 预测出对应的 N 条未来状态轨迹。 步骤 3 （评估与排序）： 使用价值函数 J 对这 N 条 " 状态一动作 “ 轨迹进行打分。 步骤 4 （执行与重规划）： 选出得分最高的那条轨迹， 仅执行该轨迹的第一个动作。 随后进入下一个时间步， 观测新状态， 并重复上述整个规划过程 （即滚动时域控制 / Receding Horizon Control)。

### （二）方法优势

> [图片 35：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 零样本适应全新奖励 (Zero-shot Reward Adaptation)： 如果测试时面临全新的任务 （例如让原本只会走路的机器人跳跃 ), 只需要在打分阶段 （步骤 3） 将原本的价值函数 J 替换为新的奖励函数进行排序即可。 论文展示了 D-MPC 能够利用简单的基于高度的新奖励， 让基于次优数据训练的机器人在测试时表现出弓步、 平衡和连续跳跃等全新行为。 快速适应新的物理动态 (Adaptation to N ove | Dynamics)： 因为 D-MPC 将 “ 动力学佐 " 和 “ 动作提议 p " 解耦 (Factorized)， 当机器人发生硬件故障 （如电机扭矩受限导致动态改变） 时， 只需收集极少量的探索数据， 单独微调动力学扩散模型即可迅速恢复性能。 相比之下， 像 Diffuser 这种将状态和动作联合建模的方案在微调后性能反而会大幅下降。

## 五、Mindjourney：策略模型在世界模型中动态束搜索-剪枝

### （一）世界模型部分

这里运用了通过视角变换构建世界模型的方法：

> [图片 36：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

世界模型 )'V 充当了一个自我中心模拟器。 给定一张参考图像和一段相枧位姿序列， 它可以合成一段遵循该运动轨迹的第一人称视频。 动作空间 (Action Space)： 模型定义了一组基础的自我中心动作集合灵 = {move-forward d, turn-left， t urn- right } 其中 d 是移动距离， 和是旋转角度。 一段长度为 m 的轨迹可以表示为一个有序元组， 鰨，）， 其中 e 儿视频生成 (Action-Driven Video Generation)： 每个基础动作 a e 灵都会被映射到一个相对相机位姿变换 p(a) = c e SE(3)0 因此， 轨迹丆可以转化为一个确定性的位姿序列 C = (CI, 一 · ,cm)o 给定初始的参考图像 0 e “ × 彬 × 3， 由位姿条件驱动的视频扩散模型 W 会将其映射为一段合成视频： } 00， C) = 01，

### （二）结合剪枝的空间束搜索算法

> [图片 37：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

因为离散的动作轨迹空间会随步数呈指数级膨胀， 论文采用了一种交替进行 " 路径扩展 " 与 " VLM 启发式剪枝 " 的束搜索算法。 具体工作流如下： 1 ． 轨迹扩展 (Trajectory Expansion)： 在搜索树的深度 m 处， 当前束 (Beam) 节点存储了一条轨迹丆。 对于每个基础动作 a' e 灵， 系统允许其连续重复执行最多次。 由此生成的候选轨迹集为： C(T) 将这些候选轨迹输入世界模型 W, 渲染出对应的未来帧， 形成局部观察集 ．） =

> [图片 38：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 ． VLM 启发式评估 (VLM-Based Heuristics)： 为观察集中的每一对 （，，） 生成一段自然语言描述 desc(T') （例如： " 向前移动 0 ． 2 米， 然后右转 30 度 "）。 · 将空间推理问题 q、 轨迹描述 sc （丆 0 以及合成图像， 输入给承担搜索任务的 VLM (Search VLM)0 该 VLM 会通过两个不同标准输出两个标量分数： 探索分数 (Exploration Score) 5。 “ （丆 ／）： 评估继续沿着该轨迹探索的价值有多大。 帮助分数 (Helpfulness Score) 5 /， 砌 （丆 /）： 评估当前视角对立即回答问题有多大帮助。 剪枝与缓存： 剔除分数低于预设阈值 “ 和砌的节点。 保留 sexp 最高的个轨迹作为下一步搜索的 Beam 节点； 同时， 将 Sh 砌最高的 H 个视角缓存到 " 证据缓冲区 (Evidence Buffer)" 中。

> [图片 39：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 ． 答案生成 (Answer Generation)： 当达到最大搜索步数 “ 时， 系统会汇总缓存的所有 " 有帮助 " 的轨迹、 合成图像及其自然语言描述。 问答 VLM （QA VLM) 会一次性接收原始问题和这些收集到的多视角证据， 并推理出最终答案。

### （三）局限性

> [图片 40：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

世界模型的长期一致性衰减： 消融实验表明， 当搜索步数过长 （想象的轨迹偏离初始帧太远） 时， 当前视频扩散模型 （如论文自研的 SW M 或开源的 SVC) 会出现幻觉、 纹理扭曲或意外的视角倾斜。 这种退化的图像会为 VLM 注入噪声， 反而拉低了最终准确率。 单视角的进入限制： 目前的框架仅能从单张图片启动探索。 如果问题给定了多张图片， 它还无法将这些图片视为进入同一场景的不同 “ 入口 " 进行多源融合。

## 六、搜索时世界模型的自适应开启

### （一）问题背景

> [图片 41：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

尽管多模态大语言模型发展迅速， 但它们在视觉空间推理上仍然不可靠， 尤其是当正确答案依赖于未见过的视角或潜在的物理变换时。 为了解决这个问题， 近期的一些研究引入了 * * 世界模型 (world Models) * * 来进行 “ 视觉想象 (Visual lmagination)", 即让模型在脑海中模拟出采取某个动作后的新视角。 + 2 然而， 现有的方法通常采用 * * " 始终开启 （A | ways 一 on 尸 * * 的无差别想象策略。 论文通过对这种策略的统计分析发现： ' 不必要 (Unnecessary)： 在高达 54 ％ 的情况下， 原始观测图像已经提供了足够的信息， 视觉想象是多余的。 误导性 (Misleading)： 生成过多的想象视图不仅会增加巨大的计算成本， 还可能引入噪声， 干扰下游的推理， 导致性能下降。 + 1 成本高昂 (lnefficient)： “ 始终开启 " 策略带来了近百倍的 Token 消耗和 30 倍的推理时间增长， 但带来的准确率提升却非常有限。 基于这些痛点， 论文提出了 AV ℃ (Adaptive Visual lmagination Control， 自适应视觉想象控制） 框架， 旨在将视觉想象转化为一种可控的资源， 搞清楚 “ 何时需要想象 " 以及 " 需要多少想象 " 0

### （二）核心算法与工作流

> [图片 42：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 门控与动作规划 (Gating & PIanning) 给定当前的自我中心观测图像厶空间问题 q 以及候选答案集合 = 用一个策略模型 (Policy Model) 来明确评估当前视觉证据的充分性。 策略模型会输出一个决策变量 d e {skip, call—wm} + 2 如果 d = skip， 则跳过想象， 直接回答。 如果 d = call—wm, 模型会额外生成一个离散的动作计划丌如何移动或旋转以获取有效视角。 · ·， a }。 系一充首先使 · ·， 刁， 指导世界模型

> [图片 43：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了提高决策鲁棒性， 系统会对策略模型进行多次独立采样 （共 M 次 ), 并通过多数投票来决定最终是否调用世界模型。 2 · 动作执行与轨迹生成 (Action Plan Execution) 当决定调用世界模型时 (d = call—wm)， 系统会将策略模型生成的动作计划输入给视觉世界模型彬。 世界模型会根据初始观测和动作序列， 渲染出一系列连贯的想象视图厶 = { 厶， 相比于穷举所有动作， 这种由策略引导的生成方式大幅减少了调用次数。

> [图片 44：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

世界模型并不是只生成一个画面， 而是会根据不同的策略生成多条不同的视觉轨迹 （视频流 / 图像序列）。 动作计划的多样性： 系统会对策略模型进行 M 次独立解码采样 （论文默认设定为 5 次 ), 以此来实现策略级的测试时计算缩放 (Test-Time Scaling)。 这 M 次采样可能会生成 M 个不同的动作计划序列 T(m) （例如有的计划是 " 前进 0 ． 25 米 "， 有的是 " 左转 9 次 "） 世界模型的执行： 当决策为调用世界模型时， 这 M 个不同的动作计划会被输入给视觉世界模型。 世界模型会严格遵循这些被提议的动作序列， 为每一个动作计划渲染出一组连贯的、 对应自我中心视角的 " 想象轨迹 "

> [图片 45：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 · 轨迹级验证与筛选 (Trajectory Selection) 由于不同的采样可能产生质量参差不齐的轨迹 （有些包含新信息， 有些可能因生成噪声而退化）， AV ℃ 引入了一个轨迹级的验证器 (Verifier) VO 验证器将整个生成的轨迹作为一个整体进行评分， 以确保空间上的时序一致性， 并选出得分最高的唯一轨迹用于后续推理。

> [图片 46：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

4 · 最终答案预测 (Final Answer prediction) 最后， 视觉语言模型 ()A Model) 结合原始观测和筛选出的最优想象视图厶， 进行空间推理， 并输出最终答案。 如果门控决定跳过想象， 则厶， = 0。

## 七、Interactive Physical Reasoner

Interactive Physical Reasoner是一个在游戏中训练，可以在游戏、物理引擎中运行，并具备一定的真机泛化能力的物理AI系统，有解耦的动作空间、世界模型和策略模型。

### （一）问题背景

> [图片 47：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在构建能在新环境中泛化的智能体时， 现有主流路线均存在明显的短板： 视觉与领域的巨大差异 (Visual Domain Gaps)： 不同游戏的画面和控制方式千差万别， 传统的智能体往往会过拟合于特定游戏的视觉细节， 而无法提取通用的物理规律。 视觉语言模型 (VLMs) 缺乏前瞻性： 虽然近年来各大 AI 公司推出了强大的多模态大模型 （例如 OpenAl 于 2025 年 8 月最新发布的具有强推理能力的 GPT-5, 以及阿里的 Qwen3-VL)， 它们在静态语义推理上表现卓越， 但在交互式环境中却缺乏预测物理后果的能力 （即 " 前瞻性 " Look-ahead)， 难以在动态世界中闭环规划。 世界模型 (world ModeIs) 过于注重视觉模仿： 这类模型 （如 Genie、 Dreamer) 擅长想象未来的画面， 但它们往往只是在模仿表面的视觉相关性， 缺乏对物理本质和因果关系的深度分析， 导致在目标驱动的任务中容易崩溃。 强化学习 (RL) 的样本与奖励困境： 强化学习严重依赖密集的奖励信号， 且容易过拟合到特定任务的捷径上， 难以跨域迁移。 为了解决上述问题， 研究团队构建了 G21J (Game-to-Unseen) 基准测试， 包含 ℃ 00 多款具有不同物理机制和因果规律的游戏 （涵盖复古游戏、 HTML 网页游戏以及如 Minecraft、 Portal 2 等商业游戏）， 要求智能体在未见过的游戏中实现 " 零样本迁移 " (zero-shot Transfer)0

### （二）基于物理特征的动作代码（PhysCode）的训练

现有的控制接口（如键盘的上下左右）在不同游戏乃至现实世界中含义完全不同，直接学习会导致控制上的混淆。PhysCode旨在学习一个跨域通用的离散潜在动作空间,这是一个“物理中心”的离散动作编码，不直接用原始键盘动作，也不直接用自然语言动作，而是把动作映射成一组更贴近动力学后果的latent code。直觉上，它想表达的不是“按了W键”，也不是“向前跳”，而是“产生某种具有特定动力学效果的干预”。这样可以让模型更好地泛化到现实世界。

> [图片 48：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

阶段 1： 学习物理中心的动作代码 (PhysCode) 不同游戏的操作按键可能完全不同 （例如， 按键 “ A " 在游戏甲中是跳跃， 在游戏乙中是攻击）， 这会导致严重的 " 控制冲突 "。 为了解决这个问题， IPR 学习了一个跨域共享的离散化动作空间 PhysCode, 它通过聚类物理动态 （而非按键） 来表示动作。 工作流与数学推导： 首先， 对于时间步提取三种多模态特征： DlNOv3 提取的视觉外观特征。 光流 (Optical Flow) 提取的运动特征。 T5 提取的轻量级语言语义特征 0。 使用门控网络将它们投影并融合六烁尸 I 細）， weet 计算归一化的门控权重 (Softmax)： exp(am) E m'e{f,u,e} exp(am')

> [图片 49：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

阶段 1： 学习物理中心的动作代码 (PhysCode) 不同游戏的操作按键可能完全不同 （例如， 按键 “ A " 在游戏甲中是跳跃， 在游戏乙中是攻击）， 这会导致严重的 " 控制冲突 "。 为了解决这个问题， IPR 学习了一个跨域共享的离散化动作空间 PhysCode, 它通过聚类物理动态 （而非按键） 来表示动作。 工作流与数学推导： 首先， 对于时间步提取三种多模态特征： DlNOv3 提取的视觉外观特征。 光流 (Optical Flow) 提取的运动特征。 T5 提取的轻量级语言语义特征 0。 使用门控网络将它们投影并融合六烁尸 I 細）， weet 计算归一化的门控权重 (Softmax)： exp(am) E m'e{f,u,e} exp(am')

> [图片 50：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

得到融合特征： = 丆 + wu + we 0 随后， 通过一个时空 Transformer 编码器将映射为连续变量。 接着， 使用 VQ-VAE 的码本 ℃ odebook， 包含个向量） 对其进行离散化量化： 2 arg IIIIII { 1。。 五 } 量化后的向量记作三 ‰。 该阶段的优化目标是特征预测与 VQ 损失： £ 凵 = IIft+A 一不腭 + sg 囫一腭 + 7 一 5g0 腭

> [图片 51：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

损失函数： 其目标是准确预测下一帧的视觉特征 ft+A， 迫使编码捕捉真实的物理动态变化： = IIft+A 一 ft+A 腭 + sg 一腭 + 到一 sg 腭

> [图片 52：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在拆解公式前， 我们需要明确公式中各个变量是如何计算出来的： 1 ． 2 ． 3 ． 编码阶段： 时空纟扁码器 EV 接收融合了视觉和语义的多模态特征序列， 输出一个连续的潜在丨向更 Zt。 量化阶段 （离散化）： 模型在一个包含个向量的码本 ℃ odebook) 中， 找到与欧氏距呙最近的向量， 记为。 + 2 解码与预测阶段： 解码器刀接收当前时刻的视觉特征和离散化的动作特征， 尝试预测未来时刻的视觉特征 ft+A。

第一项预测未来不是训练世界模型，而是为了让离散化的动作代码更精准地表示动作，帮助世界模型预测未来，只有Codebook中的PhysCode准确表征了动作带来的物理因果（例如“跳跃”导致“高度增加”），解码器才能据此准确预测出未来的画面特征；第二项把编码器的输出固定住当作目标，以优化码本里的向量表示；第三项约束编码器，让它“承诺”于它所选择的那个码本向量，否则编码器可能会随意输出极大的值，导致其输出在空间中剧烈游走，难以稳定映射到有限的码本向量上。

关于码本中向量如何得到：刚开始随机初始化，后来随着反向传播训练的过程不断更新，类似于统一参与训练的Visual Embedding层。

> [图片 53：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

纯靠梯度下降更新码本有时会非常不稳定 （容易出现某些码本向量永远不被选中， 即 " 死码 " 问题）。 因此， 论文采用了更稳定的 EMA 方法。 EMA 的本质是平滑地计算落入该聚类中心的荔的平均值。 + 1 其严谨的数学推导和工作流如下： 对于码本中的第个向量， 模型在训练时会维护两个额外的变量： 1 ． 命中计数 N 层记录这个向量被匹配到的次数。 2 ． 特征累加 mk: 记录所有匹配到这个向量的连续特征的总和。 在时间步如果有若干个数据点被分配给了第个向量， 模型会按照以下公式进行指数滑动平均更新 （假设 a 为接近 1 的衰减率， 如 0 ． 99）：

> [图片 54：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

纯靠梯度下降更新码本有时会非常不稳定 （容易出现某些码本向量永远不被选中， 即 " 死码 " 问题）。 因此， 论文采用了更稳定的 EMA 方法。 EMA 的本质是平滑地计算落入该聚类中心的荔的平均值。 + 1 其严谨的数学推导和工作流如下： 对于码本中的第个向量， 模型在训练时会维护两个额外的变量： 1 ． 命中计数 N 层记录这个向量被匹配到的次数。 2 ． 特征累加 mk: 记录所有匹配到这个向量的连续特征的总和。 在时间步如果有若干个数据点被分配给了第个向量， 模型会按照以下公式进行指数滑动平均更新 （假设 a 为接近 1 的衰减率， 如 0 ． 99）：

### （三）潜空间世界模型的训练

> [图片 55：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

阶段 2： 训练基于潜在特征的世界模型 (Latent world Model) 有了统一的 PhysCode， 世界模型不再需要预测繁杂的像素， 而是直接在特征空间中进行预测和评工作流与数学推导： 给定当前特征和 PhysCode 的嵌入， 预测器会输出未来时刻的特征预测 ft+A 以及该动作的价值评估： 训练分两步进行： 1 ． 2 ． 特征预测损失 （学习物理动态）： 价值评估损失 （学习任务收益， 类似强化学习中的 TD 学习）： t，

> [图片 56：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

阶段 2： 训练基于潜在特征的世界模型 (Latent world Model) 有了统一的 PhysCode， 世界模型不再需要预测繁杂的像素， 而是直接在特征空间中进行预测和评工作流与数学推导： 给定当前特征和 PhysCode 的嵌入， 预测器会输出未来时刻的特征预测 ft+A 以及该动作的价值评估： 训练分两步进行： 1 ． 2 ． 特征预测损失 （学习物理动态）： 价值评估损失 （学习任务收益， 类似强化学习中的 TD 学习）： t，

### （四）VLM的训练

训练VLM时，冻结参数的世界模型被用作训练环境。

> [图片 57：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

阶段 3： 预测强化的互动推理 (Prediction-reinforced Reasoning) 这一阶段将 VLM （论文使用了 Qwen3-VL-8B) 与世界模型结合， 形成闭环。 工作流与数学推导： 1 ． VLM 观察当前环境和任务提示， 生成 B 个候选的 PhysCode 动作序列。 世界模型在脑海中 " 想象 " 这 B 个序列在未来几步的结果， 并打分 （计算回报 R(b))0 2 ． 通过归一化计算每个候选序列的优势函数 A(b)。 3 ． 使用 GRPO （群组相对策略优化） 算法更新 VLM 的参数， 让 VLM 更倾向于输出那些在物理 4 ． 上可行且能带来高收益的动作： 1

### （五）推理工作流

推理时，世界模型被用作动作的并行搜索空间和打分器。

> [图片 58：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 候选生成： 给定当前帧和任务提示 g, VLM 会像训练时一样， 提出 B 个候选的潜在动作序列 ℃ andidate PhysCode sequences) 世界模型打分： 保留在闭环中的世界模型通过短视距的 " 想象 " （即特征空间中的前向推演） 对这些候选动作序列进行打分和剪枝。 择优执行： 系统选择得分最高的候选动作序列， 然后通过一个轻量级的路由器 Tenv 将选定的 PhysCode 映射为具体的环境控制指令并执行。

### （六）评估标准

> [图片 59：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了全面衡量智能体的物理能力， 论文受到马斯洛需求层次理论的启发， 设计了递进的三级评估指 1 ． 2 ． 3 ． 生存 (Survival)： 类似于物理直觉， 评估智能体躲避危险、 存活的步数。 好奇心 (Curiosity)： 像婴丿一样探索未知， 评估智能体在状态空间中覆盖的广度。 效用 (UtiIity)： 高阶的目标驱动推理， 评估智能体完成得分、 通关等复杂目标的效率， 使用人类归一化得分 (HNS) 来衡量 · score agent HNS = SCOrehuman scorer、 a7司 SCOrerand

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 在 EfficientZero 中， LSTM 每步预测的 " 价值前缀 (Value Prefi 幻 “ 的监督信号 （即真实的标签 Ground Truth), 直接来源于智能体过去在真实环境中收集并存放在经验回放缓冲区 (RepIay Buffer) 中的真实奖励序列。 由于模型的训练是后台进程从经验回放缓冲区中采样历史轨迹来进行的， 对于采样到的任意一条轨迹片段， 算法已经 " 预知 " 了该片段在当时每一步所获得的真实环境奖励 （在论文中记为， 以区别于模型预测的奖...
- 在强化学习理论中， 绝对的 " 真实价值 " 是指从当前走到游戏结束的实际总得分。 但因为游戏过程往往很长， 算法无法总是等待回合结束， 因此需要构建一个高质量的近似值， 即目标价值 (Target Value)0 E z -V2 提出了 * * 混合价值目标 (Mixed value Target) * * 枧制， 这意味着目标价值荔的来源是动态切换的， 它融合了真实环境和树搜索的结果： 场景 A： 处理刚收集的 “ 新数据 " （预计算优先级时） 当数据工作进程刚刚在真实...
- 世界模型 )'V 充当了一个自我中心模拟器。 给定一张参考图像和一段相枧位姿序列， 它可以合成一段遵循该运动轨迹的第一人称视频。 动作空间 (Action Space)： 模型定义了一组基础的自我中心动作集合灵 = {move-forward d, turn-left， t urn- right } 其中 d 是移动距离， 和是旋转角度。 一段长度为 m 的轨迹可以表示为一个有序元组， 鰨，）， 其中 e 儿视频生成 (Action-Driven Video Generati...

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
