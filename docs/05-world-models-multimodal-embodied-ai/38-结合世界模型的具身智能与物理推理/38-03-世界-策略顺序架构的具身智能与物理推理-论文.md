---
title: "38.3 世界-策略顺序架构的具身智能与物理推理（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/38.结合世界模型的具身智能与物理推理/38.3 世界-策略顺序架构的具身智能与物理推理（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 38.3 世界-策略顺序架构的具身智能与物理推理（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、WorldLM：将世界模型融入LLM的编码器

### （一）核心思想

传统VLM主要依赖静态编码器，工作范式是描述性推理，即详细描述图像中现有的静态内容；WorldLM引入生成式编码器，模型不仅能编码图像的当前状态，还能预测合理的未来发展情况（例如预测静止的火星车将会开走）。这种基于预测的表征学习赋予了模型对物理世界动态的内在理解。

### （二）模型架构

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

语义视觉编码器 (Semantic Vision Encoder)： 使用预训练的 Sig 凵 P 提取输入图像的静态语义特征。 通过投影层映射为文本空间的视觉 Token。 = IZP(S) = MLPs (S)

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

生成式编码器 (Generative Encoder)： 采用稳定视频扩散模型 (SVD) 作为纟扁码器。 输入图像首先通过 VAE 编码为潜在变量， 并被复制 T 次形成初始视频潜在状态 Zo。 模型仅执行单步 Euler 降噪， 以捕捉动态先验该步骤不渲染完整视频， 而是从 U-Net 中间层提取隐藏状态 H, 并投影为动态 Token。 = Psvd(H) = MLPd(H)

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

大语言模型主干 (LLM Backbone)： 将两组特征拼接为完整的视觉表征。 结合文本提示后， 由 LLM （如 LLaMA-2 或 Qwen-2.5) 自回归生成最终回答。

### （三）损失函数

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在训练中直接使用了大语言模型中标准的文本损失 (text-loss) 具体来说， 结合论文中的训练方案 (Training recipe)， 该损失函数的应用逻辑如下： 常规的自回归文本生成目标： 作者采用了与 Prismatic-VLMs 相同的单阶段指令微调 (single- stage instruction tuning) 策略。 模型在融合了静态视觉 Token、 动态生成 Token 以及文本提示后， 由 LLM 主干网络自回归地生成文本回答。 这里的损失函数就是计算模型生成的文本与真实指令文本之间的差异， 通常也就是大语言模型训练中标准的交叉熵损失 (Cross- Entropy LOSS)。 仅作用于部分参数： 在计算出文本损失后， 梯度回传时仅更新轻量级的投影层 (projectors) 和 LLM 主干网络。 作为特征提取器的语义视觉编码器 (Sig 凵 P) 和生成式编码器 (SVD) 均保持完全冻结 (frozen) 状态。 关于 “ 文本损失 " 的一个重要设计探索： 论文在第 5 ． 2 节还专门进行了一项有趣的消融实验： 他们尝试仅使用这个 " 文本损失信号 (text- loss signal) " 去解冻并联合微调生成式编码器 SV D 的核心组件 (U-Net 和 VAE)。 然而， 实验发现这种策略很大程度上是无效的， 甚至会导致模型在各项基准测试中的性能全面下降。 作者得出的结论是， 高层的文本语义监督信号， 并不适合用来调整底层基于物理动态的生成式先验模型。

## 二、利用动作模型物理先验训练世界模型

### （一）问题背景

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当前的视频世界模型缺乏明确的可执行性约束。 它们生成的视频在像素层面上可能非常逼真连贯， 但往往会违反刚体物理和运动学约束 （例如发生机械臂形变、 自交叉或时间上的突变）。 在实际部署中， 通常会使用一个逆动力学模型 (lnverse Dynamics Model, IDM) 将这些生成的视频帧提取并解码为可执行的机器人动作。 然而， 如果视频中包含上述违背物理规律的视觉伪影， IDM 就会将其翻译成极端不稳定、 高频抖动或超出物理限制的控制指令， 从而导致任务失败。 这种视觉生成与物理可执行控制之间的错位， 被作者定义为 “ 可执行性鸿沟 "。

### （二）核心算法与工作流

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

第一步： 训练逆动力学模型 (IDM) IDM 的作用是从视觉观察序列中推断出枧器人的底层控制指令。 模型输入一个以时间为中心的短时视频帧窗口一 “ + 为时间上下文半径）， 并预测出对应的真实执行动作。 网络结构上， IDM 通过卷积主干提取空间特征， 随后经过空间 Softmax 层将特征图转化为 2D 坐标， 最后由多层感知机 (MLP) 映射到动作空间。

这一步在真实的机器人“视频-动作轨迹”数据对上，通过监督回归进行训练，使得IDMA能准确地将输入的视频解码为机器人地动作指令。其损失函数定义为：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

乥目一 “ 司一目 2 第二步： 构建基于 IDM 的可执行性奖励信号 (lDM-based Executability Rewa rd） 由于视频生成器本身不受机器人运动学的约束， EVA 将冻结权重的 IDM 作为奖励模型， 通过评估生成视频所隐含的动作序列的 “ 平滑度 " 与 " 合规性 “ 来提供密集的奖励。 即使视频出现如图 2 所示的严重视觉伪影， IDM 也会将其转化为抖动或越界的动作， 从而产生低奖励分数， 确保证了信号的鲁棒性。 · 给定生成的视频， IDM 预测出完整的关节动作序列 = { }t 一 1。 · 通过有限差分计算出关节空间的速度、 加速度和加加速度 (jerk)。 为了惩罚不平滑的运动， 算法对加速度和加加速度应用了鲁棒的 Huber 惩罚函数： Huber@; = 2 1

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

从而得到运动平滑度惩罚项： · 同时， 算法会惩罚超出机器人本体物理极限 （如最大速度 “ 和最大加速度） 的动作违规行为： 0） 腭， = Etll max(lvtl 一 v · 将上述各项进行加权组合得到总惩罚 （）： Pacc = Etll max(latl 一 a 2 2 · 最后， 将惩罚值映射为用于网络更新的有界奖励月 （） （为惩罚基准尺度， 7 为衰减

这里事实上是利用对机器人动作的物理先验来给予RL奖励，通过梯度反传让视频生成模型生成符合物理规律的动作。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

利用上述构建的奖励信号， EVA 框架采用群组相对策略优化 (GRPO) 算法对预训练的视频生成主干网络进行微调。 · 在优化过程中， 模型被引导生成不仅符合文本指令， 更能诱导出平滑、 物理可行控制命令的视频轨迹。 通过持续的强化学习， 模型学会了避开如图 5 所示的导致执行失败的典型特征， 如运动学不合理 （形态变形、 关节模糊、 时间不连续） 以及错误的物体接触 （如穿模或丢失接触）。

## 三、ABot-World：利用世界模型实现具身闭环

### （一）核心组件

高德的ABot-World是一个统一的具身智能体系，其中包括3D生成引擎ABot-3DGS、世界模型ABot-PhysWorld和VLA模型。

ABot-3DGS生成模拟的3D空间，这里的“空间”不是只有几何外观，而是带物理属性的。每个物体都会被赋予质量、摩擦系数等参数，从一开始就构成一个可计算、可干预的物理环境。

多模态世界模型ABot-PhysWorld预测该空间随着时间和机器人干预的状态演变，输出的每一帧不仅是像素，更是包含质量、接触力场、惯性张量的可微分物理状态快照，支持“动作条件化推演”与“零样本泛化”。

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Dual-Engine System Abot-WorId Data Generation Engine Dream 0 d VLA Model № 卜 MO / NO ／ “ 丿 End Signal Failed RL Training Engine

### （二）世界模型与策略模型的闭环

ABot-World不是一个静态模型，而是一个具备自我修正能力的认知基座，能接入真实世界的执行反馈，让自己越用越准。VLA和世界模型构成完整的闭环（预测—执行—反馈—自我修正）。例如，机器人根据ABot-World的推演去抓杯子，结果实际执行中夹爪滑脱了。这个误差信号会立刻回传给ABot-PhysWorld，模型自动调整参数，下次预测就会更精准。闭环工作流如下：

1.预测：基于DiT架构，以当前观测和动作为输入，在潜空间中直接生成符合时空动力学的未来状态序列。

2.执行：将推演出的最优动作指令下发给机器人执行机构。

3.反馈：采集真实世界的执行结果（例如预测能抓起水杯，但实际机械夹爪滑脱了）。

4.自我修正：利用真实世界的误差信号反哺认知基座，实时调整模型权重或状态记忆，使得模型“越用越准”。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
