---
title: "34.4 生成式世界模型中的多种扩散范式（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/34.多模态生成与生成式世界模型/34.4 生成式世界模型中的多种扩散范式（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 34.4 生成式世界模型中的多种扩散范式（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、流匹配模型的步长拼接

### （一）问题背景

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在视频生成或动力学建模中， 传统的扩散模型 (Diffusion Models) 或流匹配 (Flow Matching) 为了生成高质量的下一帧状态 + 1， 通常需要数十步 （例如 K = 64） 的去噪迭代。 这在需要极快推理速度的强化学习交互中是不可接受的。

为了减少需要的步数，Dreamer 4采用了Shortcut Forcing，以提高大步长下的性能。

### （二）核心思想

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

模型在训练时， 算法会像掷骰子一样随机抽取一个步长 d （例如 d e { 1 / 64， 2 / 64， 4 / 64，）。 所谓 " 根据采样的步长 d 选择不同的损失函数 “， 其实是构建了一个 * * " 从小步试探到大步跨越 “ 的自我蒸馏阶梯 * *

1.选择最小步长时

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们设定的最小可能步长是 1 / 64。 当采样的 d = 1 / 64 时， 模型只需要进行一次微小的去选择的损失函数： Cflow （流匹配损失）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

传统扩散模型大多采用 v-prediction （速度预测 ), 即网络预测噪声梯度 = 昀一 （其中昀是干净数据， 是高斯噪声）。 但在频域 (Fourier Transform) 视角下， 目标包含一个白噪声项。 自然图像的频谱通常遵循 1 / 规律 （低频能量高 ), 而白噪声频谱是平坦的。 强迫网络学习含有大量高频成分的极易在长视频序列生成中导致误差累积 (Error Accumulation) 和高频伪影。 Shortcut Forcing 采用了 x-prediction, 即直接让网络预测干净的潜在表征。 无论当前的噪声时间步丆 e [ 0， 1 ] 是多少， 网络总是试图直接 " 看透 " 噪声， 输出无噪的最终状态。 为了让跨步预测保持一致， 它引入了 Bootstrap Losso 将网络的输出转换回 v-space 后， 通过 （1 一丆） 2 的乘子将 Bootstrap 损失缩放回 x-space 尺度进行优化。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

当模型进行最小步长 dmin 的去噪预测时， 它采用的是流匹配损失。 传统模型倾向于预测速度向量 t' = 昀一 （其中是干净数据， 是纯噪声 ), 但这会导致模型在频域上过度关注高频噪声， 进而引发长序列生成的误差累积。 Dreamer 4 强制模型在任何噪声水平丆 e [ 0， 1 ] 下， 直接输出对最终干净表征的预测值 （崭， 不 c)o 其基础的流匹配损失在空间内直接计算均方误差 (MSE)： 2

2.选择较大步长时

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设算法抽中了一个大步长 d = 1 / 8。 我们希望模型能够 “ 一步顶过去好几步 "。 但如果直接让它跨这么大步去拟合真实数据， 步子太大容易扯到蛋 （即产生模糊或不符合物理规律的伪影）。 选择的损失函数： Cb00tstrap （自举一致性损失） 工作流： 1 ． 2 ． 3 ． 4 ． 算法先让模型用当前的能力， 走两步较小的 " 半步 " （即连续走两次 d/ 2 = 1 / 16）。 将这两次 " 半步 " 累加起来的结果作为 " 临时标准答案 “ (Teacher Target)0 算法再让模型直接跨越一个完整的 “ 大步 " （即 d = 1 / 8）。 计算 Cb00tstrap， 强迫 " 走一大步 " 的输出， 必须等于 " 走两小步 " 的输出。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

由于直接在空间计算跨步损失尺度不一致， Dreamer 4 将预测出的转换回空间： 计算出两者的 MSE 后， 再乘以一个 （1 一丆） 2 的缩放因子， 将损失重新拉平到空间的尺度上： bootstrap Vtarget 2

（3）动态权重

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了让模型将有限的参数容量集中在 " 信息量最大 " 的去噪阶段 （即靠近干净数据的阶段， 此时丆 1), Dreamer 4 引入了随信号水平线性递增的权重函数：） = 0 · 9 丆 + 0 · 1 最终的整体损失就是根据采样的步长 d 选择 Cflow 或 Cb00tstrap， 并乘以权重 （丆） 进行优化。

## 二、自适应目标的扩散模型

### （一）问题背景

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在标准的 DDPM 中， 神经网络被训练用来预测添加到干净图像上的噪声在生成过程的起点 （即时间步 T， 噪声极大时 ), 输入给网络的几乎是纯高斯噪声。 此时， 信号 （真实图像数据） 被完全淹没 （即灬 e 》 砌） 如果在这个极端条件下， 我们强迫网络去预测输入中的 " 噪声成分 "， 网络会发现一个能轻易让损失函数降到最低的 " 捷径 "： 直接输出输入本身数学上表现为， 网络退化成了恒等映射 (ldentity Function)： 这会导致什么致命后果？ 扩散模型的本质是利用网络输出来估计数据分布的分数函数 (Score Function, logpT(c))。 如果网络在采样的第一步 （高噪声阶段） 只输出了输入本身， 这意味着它提供了极其糟糕的分数估计。 这就好比一艘飞船在起飞的最初阶段导航系统就严重失效， 导致生成的图像一开始就亻扁离了真实的数据流形。 + 4 为了弥补这种糟糕的单步估计， DDPM 不得不依赖成百上千次的微小去噪步数 (Denoising Steps) 来缓慢修正航向。 但在世界模型中， 为了保证智能体训练的计算效率， 我们最多只能负担几步 （例如 “ < 10 甚至 “ = 1） 的去噪计算。 在步数如此之少的情况下， DDPM 的第一步误差无法被修正， 直接导致了严重的生成质量下降。 + 2

### （二）核心算法

对于一个基于扩散模型的生成式世界模型，假设对于t+1时刻，初始噪声为x_t+1，此前上下文为y_t，则：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

刀鼠 1， 統） CskipCt+1 ． + CoutF9 (CinCt+1，） 在这个公式下， 神经网络的实际训练目标变为了： 1 out 这个看似复杂的公式， 实现了一个极其优雅的渐变目标： 1 ． 当噪声极大时 》）： 2 CskipCt+l 系数 0 0。 此时， 公式中的残差连接被切断， 的训练目标完全被干净的真实亻苎号 + 1 所主导。 + 1 原理优势： 网络无法再通过 " 输出输入 " 来偷懒。 它被迫在纯噪声中努力寻找并直接预测出底层的真实图像结构 （+ + 1）。 这在缺乏信号的初始阶段， 提供了精确得多的分数估计 (Score Estimate) + 2

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

2 ． 当噪声极小时 + 0）： 系数 cskip 1。 此时， 目标变成了 + 1 一 + 1， 即要求网络预测那一点点微小的残差噪原理优势： 如果此时依然让网络预测干净图像 0， 由于输入已经和 0 几乎一样， 网络的输出与输入差异极小， 会导致梯度消失， 训练目标变得平庸 (Trivial)。 预测残差噪声则放大了这部分微小差异， 迫使模型在最后阶段精雕细琢， 完善高频细节。

### （三）扩散MoE

可采用双专家或多专家设计，以更好地实现自适应。高噪声专家负责早期去噪和全局结构，低噪声专家负责后期细节处理。

## 三、离散扩散模型

### （一）状态空间

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

一般扩散模型 （连续扩散）： 运行在连续的高维空间中 （无论是直接的像素空间， 还是 VAE 映射后的连续潜空间）。 数据变量可以取任意实数值， 因此微小的扰动 （如细粒度的高斯噪声） 是可以被量化和累加的。 Muddit （离散扩散）： 运行在 * * 有限的离散类别空间 ℃ ategorical Space) * * 中。 文本被转化为词表中的 to ke n 索引， 图像被 VQ-VAE 转化为预定义 codebook 中的离散索引。 在离散空间中， 变量的状态是互斥的整数， 不存在 " 两个 to ke n 中间的值 "， 因此无法直接施加连续的数值噪声。

### （二）前向扩散过程

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

离散扩散无法加减噪声， 因此 Muddit 将前向破坏建模为状态的类别转移。 具体来说， 采用了一个连续时间马尔可夫链 ℃ TMC), 由转移率矩阵 0 控制： = Qtpt Muddit 采用的是吸收态 (Absorbing State/Masked) 扩散： 随着时间推移， 任何一个真实的离散 to ke n （无论是文字还是图像块） 都有概率突变为一个特殊的掩码 token (Mask Token, m)。 一旦变成掩码， 就不再改变。 因此， 它的 " 纯噪声 " 状态并不是高斯白噪声， 而是一个完全由掩码 token 组成的序列。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

其中， 是时间时的状态分布， Qt 是转移矩阵。 模型采用吸收态 (Masked) 扩散： 任何真实 to ke n 都有概率变成一个特殊的掩码 to ke n m = （0， ” 0， 1）， 并且一旦变成就不会再复。 模型在时间观察到原 token 未被掩码的生存概率为。 Muddit 采用了余弦掩码调度策略 ℃ osine Masking Schedule)， 其掩码率 = 1 一的密度函数为： 2 2 这保证了 70 + 0 （全干净数据） 和 71 -+ 1 （全掩码数据）。

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 在乯较小 （刚开始掩码） 时， 曲线较为平缓， 这意味着掩码率增加得比较慢， 保留了较多干净的上下文佇串口 ' 匚 0 2 ． 在趋近于 1 （掩码过程尾声） 时， 曲线变得陡峭， 掩码率迅速增加至完全掩码状态。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

（： 根据上下文丿公左得的实阮上表示的是采样时间步乯的概率密度函数， 而非码率本身。 丿这个密度函数在 + 0 时会趋于无穷大， 意味着模型在训练时会更频繁地采样那些掩码率较低 （包含大量干净 token) 的时间步， 从而让模型在拥有丰富上下文的情况下更好地学习预测剩下的掩码。

### （三）去噪损失函数

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在离散空间中， 梯度 log） 在数学上是未定义的 （不存在合适的梯度）。 因此， Muddit 的网络本质上是一个掩码 token 预测器 (Masked-token predictor)o 模型通过观察当前未被掩码的 toke n （上下文 ), 直接预测被掩码位置的原始干净 to ke n 属于各个类别的概率分布。 其优化目标是连续时间负证据下界 (Negative ELBO)， 使用的是交叉熵损失 ℃ ross- Entropy) 而非 MSE:

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 0 log(G@t,， c) · 刁

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

期望项 E 这里的 q （） 是前向后验概率， 表示在给定真实干净序列的情况下， 时间时的掩码序列的分布。 该项的含义是： 模型需要针对所有可能的加噪 （掩码） 状态求期望损失。 + 2 积分项： 由于 Muddit 将离散扩散建模为连续时间马尔可夫链 ℃ TMC), 因此损失是计算在整个时间轨迹 e [ 0， 1 ] 上的积分。 + 2 重加权系数 l—at 这是一个时间相关的动态权重系数。 其中可 dat 曰疋生存概率对时间的导数， 而分母 1 一正是掩码率。 该系数在模型训练架构中充当了交叉熵重加权 ℃ ross-Entropy Re 一 weighted) 的作用， 它会根据当前时间步的掩码比例， 动态调整预测损失的惩罚力度。 + 2 对数似然 log(G@t,， c) ·： 这是标准交叉熵损失的核心部分。 g （昀，， c） 是模型 (MM-DiT) 预测出的分类概率向量。 是真实干净 to ke n 的 One-hot 向量。 c 是跨模态条件。 当执行 “ 文生图 " 时， c 是文本嵌入； 执行 " 图生文 " 时， c 是图像嵌入。

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这个目标函数的伟大之处在于它的高度对称性： 切换生成任务 （从文本到图像变为图像到文本） 仅仅意味着改变了条件信号 c, 而公式 £ “ 订的主体结构完全不变。 这种对称性使得优化过程在不同任务之间保持绝对一致， 允许模型只需使用同一套参数就能联合训练双向的生成能力。

### （四）模型架构

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

Muddit 的架构设计旨在将 " 文本 “ 和 “ 图像 " 置于完全对称、 统一的生成范式下。 它的整体架构由以下几个核心组件构成： 文本编码器 （詞： 采用预训练且冻结的 C 凵 P 模型， 用于提取文本 toke n 的嵌入特征。 + 1 图像编码器 (Eimg)： 采用预训练且冻结的 VQ-VAE, 将原始像素图像量化映射为紧凑的离散 codebook 索引 （即图像 token) + 1 0 统一生成器 (G)： 这是模型的核心， 采用了一个多模态扩散 Transformer (MM-DiT) 主干网络。 最关键的是， 该生成器使用 Meissonic （一个强大的高分辨率文本到图像模型） 的预训练权重进行初始化。 这种初始化为模型注入了丰富的图像先验知识和跨模态语义关联。 + 2 采样器 (S): 负责在逆向扩散过程中， 根据生成器预测的概率分布， 动态地更新和选择 to ke n + 2 0 统一解码器： 包含一个图像解码器 Dtmg （直接使用 VQ Decoder 将 token 还原为像素） 和一个文本解码器刀 “。 为了保持高效， 文本解码器仅是一个附加在共享特征空间上的轻量级线性头 (Linear Head) + 4 0

## 四、为什么扩散过程实质上实现了推理？

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

此前， 业界普遍假设视频模型的推理遵循 " 帧思维链 " ℃ hain 一 of 一 Frames， CoF), 即推理是随着视频帧的播放按时间顺序展开的。 然而， 本研究提出并验证了 * * " 步思维链 “ ℃ hain 一 of 一 Steps， CoS) * 机制。 由于扩散模型中的双向注意力机制可以覆盖整个视频序列， 因此模型实际上是在每一个去噪步 (Denoising Step) 中， 同时对所有帧进行全局的推理和不断修正。 工作原理推导在扩散模型 （基于流匹配机制） 的数学框架下， 潜变量在噪声与清晰数据之间的连续演变路径可以表示为： （1 一 5） 0 + 5 昀其中为干净的数据潜变量， 昀为符合正态分布的噪声。 模型在训练中学习一个速度场 (velocity Field) （，， c)， 从而在任意去噪步 5 估计出当前的清晰结果： 0 = eTs 通过解码不同扩散步的， 作者观察到了两种极其典型的 " 步思维链 " 探索工作流：

1.多路径探索

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

工作流： 在早期的去噪步骤中， 模型表现得如同经典的广度优先搜索 (BFS) 算法。 它会同时在隐空间中实例化并探索多个候选的推理轨迹 （例如让视频里的机器人在迷宫中同时走向两条岔路）。 随着去噪步骤的推进， 次优的路径被逐渐 " 修剪 " 和抑制， 最终在晚期步骤中收敛到唯一正确的路线。

2.叠加式探索

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

工作流： 在处理空间对齐或物体扌非序等任务时， 模型在早期并不会早早敲定一个状态， 而是将多个互斥的逻辑假设 （例如同一个物体不同角度的残影） 直接叠加渲染在一起。 随着噪声的降低， 这些重叠的阴影会逐渐塌缩为最终符合逻辑的实体。

为了进一步证明CoS才是主导机制，作者设计了干扰实验，其结果表现出了显著的反差：

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了进一步证明 CoS 才是主导机制， 作者设计了干扰实验， 其结果表现出了显著的反差： 干扰注入模式 Noise at Step Noise at Frame 操作机制在特定的某一个去噪步 (Step)， 对所有帧注入强噪声。 在特定的某一帧 (Frame), 对所有的去噪步注入强噪声。 对基准测试性能的影响严重阻断推理轨迹， 导致模型在 VBVR-Bench 的得分从 0 ． 685 断崖式下跌至 0 ． 3 以下。 性能下降极其微小。 由于模型能通过其他帧的上下文在后续步骤中自我修复， 推理并未受阻。

## 二、推理能力及其在架构中的分化

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

随着模型规模和数据量的扩大 （例如 wan2.2 等大规模模型 ), 作者观察到视频生成展现出了类似大语言模型 (LLM) 的涌现推理特性： 工作记忆 (working Memory)： 在遮挡或物体移出画面的任务中， 早期的扩散步能够建立持久的 “ 记忆锚点 "， 确保被遮挡的物体在后续帧中仍然能保持物理状态的连贯性， 从而解决 " 客体永久性 " 问题。 自我纠正与增强 (Self-correction and Enhancement)： 模型在扩散过程中会出现 " 顿悟时刻 " (Aha moments)o 如果它在早期生成了错误的形状或不完整的弹道轨迹， 模型有能力在经历几个扩散步后推翻之前的假设， 并将其修正为全面、 结构正确的最终答案。 先感知后行动 (perception before Action)： 模型自动发展出了一套处理视频推理的通用协议。 在早期的去噪步骤中， 它优先做语义定位 （找到 Prompt 描述的关键物体）； 而在后期的步骤中， 才开始为其添加动作规划、 运动学变化和复杂的物理交互。

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了从底层架构寻找证据， 研究提取了 DiT (Diffusion Transformer) 在单次前向传播中的层级 Token 激活状态， 发现网络在内部发生了高度的功能分化： 浅层 (Layers 0 四）： 主要负责感知， 注意力分散在全局结构和环境背景上。 中层 （约 Layer 9 至 30）： 激活特征开始高度集中到语义相关的前景物体上， 并爆发出大量与运动和交互相关的推理特征。 作者进行了一项极其关键的因果性隐空间特征替换实验 (Latent Swapping)： 当把第 20 层的隐变量替换为另一个物体的表征时， 最终的逻辑推理结果发生了彻底反转。 这确凿地证明了中间层是逻辑推理的核心区。 深层 （晚期层）： 将推理完成的隐式表示加以巩固和融合， 生成最终所需的像素级状态。

## 三、推理集成算法

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

基于 " 模型在早期扩散步会探索多条推理路径 " 以及 " 推理信息主要集中在中间网络层 “ 这两个深刻的洞察， 作者提出了一套极其轻量、 无需重新训练的算法工作流， 以此来利用隐空间中偏向正确答案的概率偏差： 1 ． 2 ． 3 ． 4 ． 5 ． 多重初始化： 给同一个模型分配 3 个不同的随枧噪声种子 (Seeds)， 独立启动前向推理进程。 锁定关键步骤： 在决定推理轨迹走向的最关键的第一个扩散步 （5 = 0） 拦截模型。 提取活跃窗口： 分别提取 3 个进程中 DiT 骨干网络里的 " 推理活跃层 " （即第 20 层到第 29 层的中间层区段） 的隐式表示 U(I)O 时空集成投票： 对这三个版本的隐变量进行空间和时间维度的平均 (Averaging)， 这相当于在隐空间进行了一次 “ 专家投票 "， 有效过滤掉了随机种子带来的特定噪音干扰。 引导生成： 利用平均后的稳定潜变量继续后续的生成。 实验证明， 这一简单的集成策略使得模型在 VBVR-Bench 推理基准测试上的绝对得分直接提升了 2 ％。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
