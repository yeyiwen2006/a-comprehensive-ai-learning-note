---
title: "34.3 扩散模型的蒸馏与Self Forcing"
source_docx: "第5部分 世界模型、多模态生成与具身智能/34.多模态生成与生成式世界模型/34.3 扩散模型的蒸馏与Self Forcing.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 34.3 扩散模型的蒸馏与Self Forcing

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

视频扩散模型虽然视觉保真度极高，但由于需要迭代去噪全部帧，生成一段短视频通常需要1到2分钟，这在实时人机交互和机器人领域并不现实。通过改进的蒸馏技术，可将庞大缓慢的模型转化为极速的实时模型。同时，视频生成模型仅以Ground Truth为上文进行Next State Prediction极易导致训练崩溃，需要自主适应长轨迹生成，具备在过程中纠错的能力。

## 一、一致性蒸馏

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

一致性模型 ℃ onsistency Models, (M) 的原始思想是： 无论在概率流 ODE 轨迹上的哪一个时间点我们都训练一个学生网络 （，， 让它直接一步映射回轨迹的终点 （即干净数据）。 其数学约束为：

这种方法在训练中一般用于模型轨迹初始化，让学生模型直接利用教师模型生成的轨迹训练，快速学会去噪。由于教师模型往往每生成一帧都需要走较多的步数（如N=48步），但为了降低延迟，学生模型并不能走这么多步，故只采样关键时间步。由于教师模型和学生模型走的步数不同，这里改为预测真实数据，而不是教师预测的噪声。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了让模型具备快速生成和自回归 （逐块生成） 的能力， 第一步需要为学生模型打下坚实的基础， 让它学会用极少的步数完成原本需要很多步的去噪过程。 工作流： 1 ． 系统首先运行庞大的教师模型， 生成完整的 N = 48 步去噪轨迹数据 {， 月 2 ． 算法从这条极长的轨迹中， 极其精简地子采样出 = 4 个关键时间步。 3 ． 因果学生生成器接收带噪潜变量和多模态条件 c （文本、 音频、 参考图像 ), 并尝试在这 4 步内直接预测出没有任何噪声的纯净视频块。 数学公式： 在此阶段， 学生模型通过最小化预测值与真实无噪数据之间的均方误差进行学习。 0 2

## 二、轨迹分段一致性蒸馏（TSCD）

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

直接让网络跨越极长的时间步去预测， 对于视频这种高维且高度非线性的数据来说， 学习难度极大 （容易陷入局部最优， 导致画面模糊）。 引入自 HyperSD 的 TSCD 技术， 其核心思想是 * * " 分而治之 " * * 1 ． 2 ． 3 ． 时间域分段： 将完整的时间轨迹 [ 0， T] 划分为个连续的子区间： 乯 0， ]， 标， 乯 2 ]， ·， 一 1，， 其中分段一致性约束： 在每一个子区间一 1 ] 内， 学生网络不再被强迫去预测遥远的， 而是只被要求保持该区间内的一致性。 设 （·， 昀为强大的教师模型 (Teacher Model) 在区间 @， 引上极其精确的数值求解器 （如 DDIM 多步求解 ), TSCD 的优化目标 （损失函数） 可以写为： （其中犷是学生网络参数的指数移动平均 EMA 副本， 用于稳定训练）。 2 优势： 通过将一条极长且曲折的积分曲线切分成多段相对平滑的短曲线， 学生模型拟合扩散过程的难度大幅降低。 在推理时， 模型只需在每个预设的宏观节点跳跃， 就能极其精确地逼近完整的扩散轨迹， 从而在较少步数下提供速度与保真度的强劲平衡 （实现了 4 倍加速）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在生成 （去噪） 时， 如果模型当前处于时间点它不再好高骛远地去预测遥远的， 而是只预测当前分段的下界端点 （即更接近真实数据的状态 ctl_l)。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

直接让网络跨越极长的时间步去预测， 对于视频这种高维且高度非线性的数据来说， 学习难度极大 （容易陷入局部最优， 导致画面模糊）。 引入自 HyperSD 的 TSCD 技术， 其核心思想是 * * " 分而治之 " * * 1 ． 2 ． 3 ． 时间域分段： 将完整的时间轨迹 [ 0， T] 划分为个连续的子区间： 乯 0， ]， 标， 乯 2 ]， ·， 一 1，， 其中分段一致性约束： 在每一个子区间一 1 ] 内， 学生网络不再被强迫去预测遥远的， 而是只被要求保持该区间内的一致性。 设 （·， 昀为强大的教师模型 (Teacher Model) 在区间 @， 引上极其精确的数值求解器 （如 DDIM 多步求解 ), TSCD 的优化目标 （损失函数） 可以写为： （其中犷是学生网络参数的指数移动平均 EMA 副本， 用于稳定训练）。 2 优势： 通过将一条极长且曲折的积分曲线切分成多段相对平滑的短曲线， 学生模型拟合扩散过程的难度大幅降低。 在推理时， 模型只需在每个预设的宏观节点跳跃， 就能极其精确地逼近完整的扩散轨迹， 从而在较少步数下提供速度与保真度的强劲平衡 （实现了 4 倍加速）

## 三、Self forcing与分布匹配蒸馏（DMD）

### （一）核心思想

基于Teacher forcing的一致性蒸馏是一个Off-Policy的过程。由于学生模型的生成在块间是自回归的，一旦学生模型的生成轨迹发生偏离，就会进入OOD区域，这就意味着模型极不稳定。我们希望进行On-policy蒸馏，即Self forcing：在学生模型已有生成的基础上，让学生生成的最终数据分布p_Φ(x)，接近真实的物理世界数据分布p_data(x)（可以由教师模型近似代替）。

研究表明，在Self forcing中如果直接对学生生成的数据和真实数据用MSE Loss，极易导致训练崩溃，但用分布匹配蒸馏方法能解决这个问题。后续会解释原因。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在概率统计中， 衡量两个分布差异最经典的工具是 Kullback-Leibler (KL) 散度。 我们希望最小化重点来了： 在 Score-based 扩散模型的数学框架下， 要通过梯度下降来缩小这两个分布的差距， 其损失函数对生成数据的梯度， 恰好等于这两个分布的 “ 分数 (Score)" 之差 · vxDKL@目 “ 忉） = logp+(c) 一 logpdata@)

第二项是真实去噪数据分布对数的梯度，根据扩散模型的基本原理，教师模型学习真实数据分布，故这一项约等于教师模型输出的噪声预测；第一项是学生模型预测的去噪数据分布对数的梯度，我们用一个“评论家网络”学习学生模型输出数据分布，在其上进行加噪，噪声预测即为本项，从而计算梯度，更新学生模型的参数。

### （二）工作流

1.评论家网络学习学生模型的去噪方式

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

经过步骤一， 学生模型虽然能快速自回归生成， 但会面临致命的 " 暴露偏差 " （Exposure Bias) 即前一帧生成的微小误差， 会在自回归过程中像滚雪球一样累积， 最终导致画面崩坏 （如闪烁、 黑屏）。 为了解决这个问题， 引入了分布匹配蒸馏 (DMD) 算法， 并在该阶段引入一个可训练的 “ 评论家网络 " ℃ ritic Network)，。 + 2 工作流： 1 ． 2 ． 3 ． 学生生成器根据高斯噪声 z 和多模态条件 c 生成预测视频帧 （即 = 系统给这个生成的重新人为添加丆时刻的噪声， 得到崭。 评论家网络的任务是充当 " 裁判 "， 它需要紧紧追踪学生生成器不断变化的生成分布， 尝试对崭进行去噪， 以还原出。 数学公式： 评论家网络通过最小化标准扩散去噪目标来更新自己的参数： 0 2

2.根据教师模型与评论家网络的差距，梯度更新学生模型

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

现在我们有了不断生成的 “ 学生 " 和一个能追踪学生水平的 " 裁判 “， 最后一步就是引入一个冻结的、 拥有绝对正确知识的 " 金牌教师分数网络 "， 来强行纠正学生的错误。 工作流： 1 ． 2 ． 3 ． 4 ． 数学公式： 学生生成器的梯度更新公式如下 （该公式指导了生成器的纠偏方向）： 系统将同一个加噪数据薅和多模态条件 c 同时喂给 " 金牌教师 “ 和 " 裁判 " + 2 计算两者给出的分数 （梯度） 差异。 教师给出的是绝对正确的去噪方向， 而裁判给出的是基于学生当前水平的去噪方向。 + 2 这两者的差值构成了精准的惩罚信号。 该信号通过链式法则反向传播给学生生成器， 强迫生成器调整参数， 使其输出的分布无限逼近真实教师的分布。 + 1 整个 DMD 训练过程就是 * * 步骤二 （更新裁判） 与步骤三 （更新学生生成器） * * 交替进行的过程。 S9 丆， 丆， C

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

（注： 多模态条件 c 的加入使这里的计算极易崩溃， 因此论文才强调必须搭配高质量的训练数据和激进的学习率调度。） + 3

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

整个梯度更新交替进行的工作流如下： 1 ． 2 ． 3 ． 4 ． 5 ． 单步生成： 学生生成器根据噪声和条件直接生成预测数据。 随机加噪： 系统给这个生成的重新人为添加时刻的噪声， 得到加噪数据薅 （文档文字部分也记作）。 计算分数差异： 将同一个加噪数据薅和条件 c 同时喂给 " 金牌教师 “ 网络和 " 裁判 " 评论家网络。 + 1 立即反向传播： 计算两者给出的分数 （梯度） 差异， 这个差值构成了精准的惩罚信号， 并通过链式法则立刻反向传播给学生生成器， 更新其参数。 数学公式表示为： + 1 + 1 S9 丆， 丆， C 交替更新： 整个 DMD 训练过程就是不断在 " 更新评论家网络 " 和 " 更新学生生成器 “ 之间交替进 + 1 仃。

这个过程是On-policy的，因为是用学生模型的生成进行加噪后，再作为数据给教师模型和评论家网络。

### （三）为什么DMD效果良好，MSE Loss却会崩溃？

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

MSE 的致命缺陷 （均值回归问题）： 如果要求 Student 模型只用 1 步就生成图像， 它生成的轨迹必然会和多步的 Teacher 模型不同。 比如， 给定相同的文本， Teacher 画了一只黄色的猫， Student 画了一只黑色的猫。 两者都是合理的， 但如果在像素级强行计算 MSE, 系统会认为 Student 犯了弥天大错， 并强迫 Student 去拟合黄色和黑色的平均值一一最终模型会生成一团模糊的灰色。 DMD 的解决方案 （分布级对齐）： DMD 放弃了像素级的强对齐， 转而使用 KL 散度来衡量 " 生成分布 " 和 “ 真实分布 " 的整体差异。 其核心梯度近似为： VOCDMD 这里： E 0 （巛。@囝 DO · éreal： 由冻结的强大 Teacher 模型 （代表真实数据分布） 计算出的分数。 · €fake： 由一个专门拟合 Student 虚假数据的模型计算出的分数。 DMD 的逻辑是： 只要 Student 生成的内容 （比如黑猫） 符合真实世界的概率分布 (Teacher 认为黑猫也很合理， éreal 和 éfake 差异很小 ), 就不产生惩罚梯度。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

MSE 的致命缺陷 （均值回归问题）： 如果要求 Student 模型只用 1 步就生成图像， 它生成的轨迹必然会和多步的 Teacher 模型不同。 比如， 给定相同的文本， Teacher 画了一只黄色的猫， Student 画了一只黑色的猫。 两者都是合理的， 但如果在像素级强行计算 MSE, 系统会认为 Student 犯了弥天大错， 并强迫 Student 去拟合黄色和黑色的平均值一一最终模型会生成一团模糊的灰色。 DMD 的解决方案 （分布级对齐）： DMD 放弃了像素级的强对齐， 转而使用 KL 散度来衡量 " 生成分布 " 和 “ 真实分布 " 的整体差异。 其核心梯度近似为： VOCDMD 这里： E 0 （巛。@囝 DO · éreal： 由冻结的强大 Teacher 模型 （代表真实数据分布） 计算出的分数。 · €fake： 由一个专门拟合 Student 虚假数据的模型计算出的分数。 DMD 的逻辑是： 只要 Student 生成的内容 （比如黑猫） 符合真实世界的概率分布 (Teacher 认为黑猫也很合理， éreal 和 éfake 差异很小 ), 就不产生惩罚梯度。

当然，DMD也存在缺点。除算力开销大、需要强大的教师模型外，还需考虑不稳定问题：

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

MSE 的最大优点是 * * " 老实且稳定 “ * *。 对于任意一个像素点， 预测值和真实值的欧氏距离是一个极其平滑的凸函数近似 （在局部 ), 这使得损失在极其庞大的集群上进行分布式训练时， 能非常稳定地下降 （这也是 Scaling Law 的基石）。 而分布匹配 （无论是基于对抗网络的 GAN Loss， 还是基于分数匹配的 DMD) 是一个动态博弈的过程。 不稳定性： 在训练过程中， Student 模型和评估分布差异的 Fake 模型 （或判别器） 相互拉扯。 如果其中一方进化过快， 梯度就会瞬间爆炸或消失， 导致整个耗资巨大的训练集群停摆。 模式崩溃 (Mode Collapse)： 为了使得全局分布差异最小， 模型常常会 " 投枧取巧 "。 它发现只要反复生成某几种它极其擅长的高质量画面 （比如永远只生成慢动作的静止风景 ), 就能在分布评估中获得高分。 这会导致模型丧失生成多样性 (Diversity)。

### （四）梯度截断

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在标准的自回归生成中， 如果我们生成一个长度为 N 的视频序列， 且每一帧需要经过步扩散去噪 (Diffusion Denoising Steps), 那么完整的计算图深度将是 N × TO 如果使用标准的随时间反向传播 (BPTT, Backpropagation Through Time), 我们需要将这 N × 丆步的所有前向激活值 (Activations) 都保存在显存中， 以便反向传播时计算梯度。 这种操作会导致显存占用随序列长度和扩散步数呈指数级爆炸， 即便是拥有 80GB 显存的顶级算力卡 （如 H ℃ 0） 也无法承受几秒钟视频的完整 B PTTO 因此， 随机梯度截断通过在计算图上进行 “ 剪枝 "， 在保证模型能够学习到容错能力的前提下， 将显存占用限制在一个恒定的、 可接受的范围内。

具体截断方法包括以下两种：

1.沿时间步数截断

这指的是第i步生成中的梯度最多回传到第i-m步，防止计算图深度过大。

2.沿去噪步数截断

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

维度二： 扩散步维度的随机截断 (Stochastic Step Truncation) 即使切断了帧与帧之间的梯度， 单帧内部的 T 步去噪 （哪怕在使用 Few-step 模型时 T = 4） 依然会占用较多显存。 随机截断策略会在训练的每次迭代中， 为序列中的每一帧随枧采样一个去噪步数 st e { 1， 2， · ． ·， }。 在反向传播时， 只计算从第 T 步到第 st 步的梯度， 直接丢弃更早去噪步骤的计算图。 这不仅节省了显存， 还引入了随机性， 起到了类似 Dropout 的正则化效果。

### （五）工程策略

多模态的引入使得DMD训练极其脆弱。可改进如下：

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

由于多模态条件的引入使得 DMD 训练极其脆弱， 论文针对性地提出了三项关键改进： 优化多模态条件数据： 使用视觉大模型 （如 Qwen-lmage) 提升参考图像的质量， 并使用 Qwen2.5-VL 强化文本提示词中的动态特征描述， 以防止低质量数据引发的误差崩塌。 充分收敛的 ODE 初始化： 不同于以往文本到视频的蒸馏， 本模型必须在 ODE 阶段训练至完全收敛 （20k 步）， 以此为后续敏感的生成器一评论家博弈提供极其稳固的起占激进的优化调度： 鉴于多模态蒸馏的有效学习窗口非常短暂 （通常在几百步后开始退化 ), 研究团队将学习率翻倍， 并大幅提升教师模型的分类器引导 ℃ (G) 比例， 从而在窗口期内强制模型学会唇轨同步等高难度对齐任务。

## 四、流匹配模型中的DMD

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

目标是最小化学生生成分布 Pfake 与真实数据分布 Prea1 之间的 KL 散度。 其关于生成器参数 0 的核心梯度表达式如下： 0） voG9(z)l Vfake Ct' 其中各变量的含义如下： · z、 （0， 是输入给学生生成器的初始噪声。 Go(z) 是学生模型生成的图像 （即假样本）。 · 6、 （0， 是用于构造中间状态的参考噪声。 t e [ 0， 1 ] 是时间步， 通常从均匀分布中采样。 · = tG9(z) + （1 一 t)e 是按照最优传输 (Optimal Transport) 路径在时间步乯插值得到的中间状态。 · （， 是预训练的教师流匹配模型 （如 SD3、 Flux 等大型流匹配模型） 在该状态下预测的真实向量场， 它代表指向真实数据流形的方向。 · 是一个伴随训练的假向量场预测器 (Fake vector Field Estimator), 它负责拟合当前学生模型生成轨迹的向量场。 · 国@） 是依赖于时间的权重函数。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

要理解上述表达式， 我们需要推导流匹配中的向量场 （） 与边缘得分函数 Vxt log （） 之间的内在数学联系。 在最优传输条件流匹配 (OT-CFM) 中， 概率路径定义为从噪声到数据的直线插值： 其中 ～ （0，） 代表高斯先验， 昀 ～ Pdata 代表目标数据。 给定数据终点昀后， 的条件分布为一个高斯分布： 到司 = （“， （1 一 02 刀

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

根据条件高斯分布的性质， 边缘得分函数可以表示为条件得分的期望： Vxt logpt （） = E。。叩@恸） [Vx log （1 一乯 1 （1 一 02 对条件分布求导有： 代入期望中得到边缘得分： logpt （1 tE [ 昀 ] log （） = eTt

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在流匹配中， 目标向量场 （） 定义为路径导数 = 一 0 的条件期望。 由于一一乯 1 老一 t 1， 可以得到： 取给定的条件期望： 从中我们可以反解出数据昀的后验期望： 1 1

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

将解出的 E[CI ] 代回得分函数公式： t 細 + （1 logpt （） 一 （1 一 02 （1 一 02 1

这样，我们就找到了得分函数和流匹配速度场之间的关系。

设真实数据的得分函数值为s_real，学生数据的得分函数值为s_fake，我们知道：

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

VOCDMD E 0@“ “ 0 一 Sfake ct,t DO

而由前面的推导：

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

sreal， 0 一 5 佰ke@t, 乯）

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

VreaI@t, 0 一 t'fake@t, 啕

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

将此结果代回梯度公式， 并将链式法则 Voct = tVoGo(z) 展开， 便得到了流匹配下的 DMD 核心梯度表达式。 由转换带来的系数一上和链式法则产生的最终被统一吸收到权重函数国@） 中。

## 五、Checkpointed Self Forcing

### （一）Self Forcing的问题

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

传统的 Self Forcing 算法要求学生模型和教师模型拥有相同的上下文长度。 但在 Solaris 的设计中， 为了让学生模型从长上下文的教师模型中获益， 必须在生成学生视频时引入滑动窗口 (Sliding-window) + 1 如果在滑动窗口设置下直接进行反向传播， 会遇到严重的显存问题： 计算图冗余： 每生成一帧， 滑动窗口就会向前移动一步， 产生一个新的上下文窗口 （例如： 第 1 步是帧 1： Ls， 第 2 步是帧 2： Ls + 1) 显存爆炸： 深度学习框架 （如 Jax) 的反向传播需要将所有这些重叠的窗囗同时保留在内存中。 如果学生上下文长度为 0， 总生成步数为 0， 那么这种冗余会导致内存成本高达 0 （0 + 1

### （二）Checkpointed Self Forcing的核心思想

在训练时，放弃每一步用滑动窗口的自回归方式推理，而是并行化、用注意力掩码模拟滑动窗口。但这样就无法保证自回归性，故Checkpointed Self Forcing采用了“先不带梯度进行自回归生成，再带梯度并行训练”的方法。工作流如下：

1.无梯度的自回归展开

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这个阶段的目的是模拟推理时的自回归生成过程， 收集训练所需的 “ 历史干净上下文 " 和 " 当前带噪目标 "， 但在代码层面完全禁用梯度图的构建， 以节省极大的显存。 1 1 ． 2 ． 3 ． 全局初始化： 设定教师上下文长度 0 （即本次生成的总帧数） 和学生上下文长度 Ls （即滑动窗口的大小）。 初始化两个空列表： 0 （用于存放干净估计帧） 和 （用于存放带噪过渡帧 ), 并初始化一个空的 KV Cache。 + 1 随机采样截断步数 （5）： 在所有的去噪时间步 {tl, · ·， } 中， 均匀随机抽取一个时间步 5。 这个 5 就是本轮训练中设定的 " 停止去噪 “ 的目标时刻 （对应前文提到的）。 + 1 逐帧生成循环 (Outer Loop)： 对于要生成的每一帧犭 （从 1 到）： 注入纟屯噪声： 为第帧初始化一个纯随枧噪声叫丆 ～ （0，。 + 1 内部去噪循环 (lnner Loop)： 从时间步 T 开始， 一步步往回去噪， 直到时间步 5。 + 1 如果当前步正好等于截断步 5： 说明到达了设定的中间噪声水平。 算法会将当前的带噪状态叫存入列表中。 接着， 模型 Go 基于这个带噪状态和当前的 KV Cache, 预测出对应的干净帧祸。 将这个干净帧存入天 0 列表中。 + 1

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这个阶段的目的是模拟推理时的自回归生成过程， 收集训练所需的 “ 历史干净上下文 " 和 " 当前带噪目标 "， 但在代码层面完全禁用梯度图的构建， 以节省极大的显存。 1 1 ． 2 ． 3 ． 全局初始化： 设定教师上下文长度 0 （即本次生成的总帧数） 和学生上下文长度 Ls （即滑动窗口的大小）。 初始化两个空列表： 0 （用于存放干净估计帧） 和 （用于存放带噪过渡帧 ), 并初始化一个空的 KV Cache。 + 1 随机采样截断步数 （5）： 在所有的去噪时间步 {tl, · ·， } 中， 均匀随机抽取一个时间步 5。 这个 5 就是本轮训练中设定的 " 停止去噪 “ 的目标时刻 （对应前文提到的）。 + 1 逐帧生成循环 (Outer Loop)： 对于要生成的每一帧犭 （从 1 到）： 注入纟屯噪声： 为第帧初始化一个纯随枧噪声叫丆 ～ （0，。 + 1 内部去噪循环 (lnner Loop)： 从时间步 T 开始， 一步步往回去噪， 直到时间步 5。 + 1 如果当前步正好等于截断步 5： 说明到达了设定的中间噪声水平。 算法会将当前的带噪状态叫存入列表中。 接着， 模型 Go 基于这个带噪状态和当前的 KV Cache, 预测出对应的干净帧祸。 将这个干净帧存入天 0 列表中。 + 1

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

阶段结果： 我们得到了两个长度为 0 的序列。 一个是干净历史帧序列 0 = [， 个是带噪过渡帧序列 = [ 到 ' ]。 此时显存中没有任何用于反向传播的计算图。 另一 + 4

2.数据处理

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了准备最终的重计算， 我们需要将这些无梯度的 " 死数据 " 重新组织起来。 彻底剥离梯度： 对和 0 再次显式调用 stop-grad()， 确保它们作为纯净的常数输入进 1 ． 入下一步。 + 1 2 ． 拼接序列： 将长度为 0 的干净帧序列天 0 和长度为 0 的带噪帧序列在序列维度上拼接起来， 形成一个新的长序列 Xtn = [ 0， ]。 此时输入序列的总长度翻倍， 变为了 20。

3.掩码下的并行重计算与反向传播

接下来开始并行重计算，每个token从之前任意抽取的噪声水平开始去噪一步。为了模拟滑动窗口注意力，构建特殊的掩码，可用四个象限表示如下：

> [图片 31：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

根据算法 2， 这个掩码被分为四个象限， 严格规定了谁能看谁： + 1 右下角 （带噪 Queries 看带噪 Keys)： 带噪帧叫只能看它自己， 不允许看其他的带噪帧， 也不允许看历史的带噪帧 （呈现出一条对角线） + 1 0 左下角 （带噪 Queries 看干净 Keys)： 带噪帧叫可以看过去的干净帧， 但必须严格遵守 ` 一 s 汤一 1 因果关系和滑动窗口限制， 即只能看干净帧。 + 1 的信息， 以防发生信息穿越。 + 2 0 左上角 （干净 Queries 看干净 Keys)： 干净帧按照正常的因果滑动窗口规则， 看过去的右上角 （干净 Queries 看带噪 Keys)： 全黑， 完全禁止。 干净帧绝对不允许看到任何带噪帧

这样，显存中只需要保留一份大小为O(L_t)的计算图，就可以进行反向传播。

## 六、循环内自蒸馏（ILSD）

Google DeepMind在《ELT: Elastic Looped Transformers for Visual Generation》中提出循环内自蒸馏（ILSD），以自由调节扩散模型的去噪循环次数，让每一个中间输出都是有意义的，不至于在没达到预先设定的最大去噪步数时就输出无意义的噪声，从而适配不同硬件算力（如云端用较多去噪步数生成高清图像，端侧用较少去噪步骤数生成普通图像）：

> [图片 32：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

· 学生路径 (Student Path): 在每一次训练迭代中， 从均匀分布中随机采样一个中途循环次数教师路径 (Teacher Path)： 运行完整的最大循环次数 Lm 部表征。 Ltnt (Lmtn 孓力， < Lmax）， 并在此时提取输出。 产生最成熟、 最高保真度的内

> [图片 33：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

训练的目标不仅是让最终输出逼近真实标签， 还要让 " 中间步骤的学生 " 去模仿 " 最终完成的教师 '。 ILSD 的联合损失函数 £ s 刀融合了三项：

> [图片 34：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

· 第一项是教师 (Lmax） 针对真实数据的目标损失 (Ground-Truth Loss)。 · 第二项是学生 “） 针对真实数据的目标损失。 · 第三项是蒸饣留损失， 促使学生拉近与教师输出的距离。 意味着教师预测在这部分停止梯度传递 (stop-gradient) 是一个随训练进行从 1 线性衰减到 0 的课程学习权重。 这迫使共享参数块学会把复杂的变换压缩到更早的循环步骤中。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 为了让模型具备快速生成和自回归 （逐块生成） 的能力， 第一步需要为学生模型打下坚实的基础， 让它学会用极少的步数完成原本需要很多步的去噪过程。 工作流： 1 ． 系统首先运行庞大的教师模型， 生成完整的 N = 48 步去噪轨迹数据 {， 月 2 ． 算法从这条极长的轨迹中， 极其精简地子采样出 = 4 个关键时间步。 3 ． 因果学生生成器接收带噪潜变量和多模态条件 c （文本、 音频、 参考图像 ), 并尝试在这 4 步内直接预测出没有任何噪声的纯净视频块。 数学公式：...
- 由于多模态条件的引入使得 DMD 训练极其脆弱， 论文针对性地提出了三项关键改进： 优化多模态条件数据： 使用视觉大模型 （如 Qwen-lmage) 提升参考图像的质量， 并使用 Qwen2.5-VL 强化文本提示词中的动态特征描述， 以防止低质量数据引发的误差崩塌。 充分收敛的 ODE 初始化： 不同于以往文本到视频的蒸馏， 本模型必须在 ODE 阶段训练至完全收敛 （20k 步）， 以此为后续敏感的生成器一评论家博弈提供极其稳固的起占激进的优化调度： 鉴于多模态蒸馏的有...
- 目标是最小化学生生成分布 Pfake 与真实数据分布 Prea1 之间的 KL 散度。 其关于生成器参数 0 的核心梯度表达式如下： 0） voG9(z)l Vfake Ct' 其中各变量的含义如下： · z、 （0， 是输入给学生生成器的初始噪声。 Go(z) 是学生模型生成的图像 （即假样本）。 · 6、 （0， 是用于构造中间状态的参考噪声。 t e [ 0， 1 ] 是时间步， 通常从均匀分布中采样。 · = tG9(z) + （1 一 t)e 是按照最优传输 (Op...
- Google DeepMind在《ELT: Elastic Looped Transformers for Visual Generation》中提出循环内自蒸馏（ILSD），以自由调节扩散模型的去噪循环次数，让每一个中间输出都是有意义的，不至于在没达到预先设定的最大去噪步数时就输出无意义的噪声，从而适配不同硬件算力（如云端用较多去噪步数生成高清图像，端侧用较少去噪步骤数生成普通图像）：

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
