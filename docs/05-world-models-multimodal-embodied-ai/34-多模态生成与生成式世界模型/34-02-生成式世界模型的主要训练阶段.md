---
title: "34.2 生成式世界模型的主要训练阶段"
source_docx: "第5部分 世界模型、多模态生成与具身智能/34.多模态生成与生成式世界模型/34.2 生成式世界模型的主要训练阶段.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 34.2 生成式世界模型的主要训练阶段

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、预训练

生成式世界模型的预训练一般使用海量无监督视频（现业界还可为含音频的视频）数据，可能有或无文本或图像标签。模型可根据视频前面的状态、文本或图片提示等预测下一个状态，在过程中学习世界表征和物理规律，具体任务可分为Text-to-Video-Audio、Image-to-Video-Audio、Video-Audio-to-Video-Audio（视频序列延续）、Text-to-Video、Image-to-Video、Video-to-Video等。

其中在训练视频和音频的联合生成时，模型架构会变化如下：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在求解上述向量场时， 网络内部采用了双分支结构 (Dual-branch)。 视频的 Token 和音频的 Token 在 Transformer 的每一层不仅进行自注意力 (Self-Attention) 计算， 还会通过交叉注意力 ℃ ross-Attention) 机制进行信息交换。 1 在任意时间步乯去噪时： 1 ． 视频分支会 " 倾听 " 当前音频的节奏和语义， 从而微调下一帧人物的嘴型或动作幅度。 2 ． 音频分支会 " 观察 " 画面的物理碰撞或环境变化， 从而合成出精确同步的音效或回声。

训练过程中，针对高分辨率和长视频可增加噪声扰动比例，以提升收敛稳定性。

训练后期，可采用“课程学习”方式，视频长度逐步扩展；模型逐渐增加流偏移以增加高噪声时间步的权重，有助于在极长的时间跨度内稳定场景级别的全局结构，减少漂移；利用光流评估器挑选高动态数据，针对性增强视频的运动幅度和平滑度。

## 二、监督微调/中期训练

### （一）针对动作动态的微调

1.数据集准备

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

叙事描述 (Narrative Caption)： 作为全局语义提示 (Global Semantic Prompt)， 将环境信息、 相机运动和时间演变结合在一起。 在模型的前向传播中， 这些文本特征通过 DiT 架构中的交叉注意力层 ℃ ross Attention) 作为条件注入。 + 1 场景静态描述 (Scene-static Caption)： 刻意忽略了所有的相机运动和动作信息。 这种标注强制模型在学习阶段分离 " 画面长什么样 " 和 " 画面怎么动 “， 使得模型能够学习到精确的、 由控制信号 （而非纯文本） 驱动的物理动态规律。 + 2 密集时间描述 (Dense Temporal Caption)： 将长视频按时间区间切片并标注具体事件。 这被用来支持时间对齐训练 (Temporal Alignment Training), 帮助模型理解和预测在极细粒度下的时序动作。

密集时间描述相当于在每一个时间区间s_t的视频后，都在上下文中加入对动作a_t的描述，让模型更好地学习p(s_t+1|s_t,a_t)。

2.微调方法

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在此阶段， 模型被注入用户的动作指令以支持交互。 动作表示与注入： 将连续的相机旋转 (Plücker 嵌入） 与离散的键盘输入 （多热编码的 w、 A、 S、 D) 在通道维度拼接。 融合后的动作信号通过自适应层归一化 (AdaLN) 机制注入到 DiT 块中， 动态调节特征生成。 + 1 参数高效微调： 为了防止模型在学习交互时出现 “ 灾难性遗忘 " （即丧失原有的高保真画质）， 主 DiT 块的参数被冻结， 仅微调新添加的动作适配器层 （包含动作嵌入投影和 AdaLN 参数）

### （二）Self-Forcing（自展开训练）

预训练本质上是Off-policy的，这意味着长时间的自回归生成依然会导致误差累积。可让模型通过KV Cache读取自己生成的历史帧（和预训练读取“标准答案”的历史帧）、预测下一帧来进行条件训练，强制其学会从自身生成的误差和伪影中恢复。

### （三）针对不同视频风格的微调

在数百个基于运动类型和视觉风格精心划分的类别中，使用极高质量的人工校验数据进行微调。可以先分别调出多个专门模型，后合并，以平衡视觉保真度与提示词可控性。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在多风格的视频生成中， 如果把写实、 动漫、 3D 渲染等所有数据混在一个数据集中对单一模型进行监督微调 (SFT), 极易引发参数优化方向的冲突， 导致模型产生灾难性遗忘或风格污染。 分化训练： 团队首先在针对不同视觉风格和运动类型精心构建的多个子数据集上， 分别微调出多个 " 专门模型 (Specialized Models)" 权重融合： 由于这些模型都是从同一个预训练 Base 模型出发的， 它们的参数分布在同一个损失盆地 (Loss basin) 内。 无需增加任何额外的训练算力， 可以直接在代数层面对权重矩阵进行合并。 业界常见的开源操作包括： 权重平均 (weight Averaging)： 将多个专家模型的参数矩阵取算术平均。 球面线性插值 (SLERP): 在高维超球面上插值， 比直接平均更好地保留大矩阵的非线性几何特征。 任务算术 (Task Arithmetic)： 提取出每个专家的 " 任务向量 （专家权重减去基础权重） "， 并按比例将其叠加回基础模型中。 通过 Merge 操作， 最终的单体模型能同时 " 继承 " 不同专家的视觉保真度和提示词控制力， 且防止了过拟合。

关于球面线性插值（SLERP）：（仅适用于全量微调的大模型，不适合用LoRA微调的大模型）

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

球面线性插值 (Spherical Linear lnterpolation, 简称 SLERP) 是目前大模型 （如 LLM 或 Diffusion 扩散模型） 权重融合中最流行、 也是效果最优雅的算法之一要理解 SLERP, 我们可以先对比最简单的线性插值 (LERP, 即权重平均 / Model Soup)0 如果把模型的权重张量看作高维空间中的两个点， LERP 是直接在两点之间连一条直线。 这种做法的致命缺点是， 直线的中间部分会 " 穿过 " 球体的内部， 导致插值后的向量模长 （可以理解为权重的能量或方差） 发生严重衰减， 从而丢失模型原有的特征表达能力。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

扩散模型 （如 VAE 的潜空间或去噪过程的输入噪声） 的数学底座是建立在各向同性的标准正态分布 （0， 上的。 这个极高维的高斯分布， 在几何拓扑上就是一个完美的超球面。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

如果你用线性插值， 中间帧的噪声方差会下降 （如前文所证 ), 模型会认为这是一种 " 不合法、 被污染 " 的噪声， 从而解码出极度模糊、 结构彻底崩坏的中间帧。 只有使用 SLERP 沿着高斯超球面滑动， 才能保证每一帧的过渡噪声都完美符合 （0， 的分布要求， 从而让扩散模型逐帧去噪生成如丝般顺滑的渐变动画。

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在扩散模型中， 权重方差的衰减会直接导致网络输出的激活值幅度剧烈下降。 反映在生成的视频或图像上， 就是画面发灰、 对比度极速下降、 高频细节丢失 （如同被蒙上了一层毛玻璃 ), 这在计算机视觉中被称为 " 模型坍缩 "。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

假设我们有两个单位向量和 2， 它们之间的夹角为 QO 我们希望引入一个插值系数 e [ 0， 1 ]， 在它们构成的二维平面上寻找一个插值向量鬱@）， 使得 v(t) 也是单位向量， 且到 v(t) 的夹角为 tQ, 鬱@） 到 2 的夹角为 （1 一 t)Q0 因为鬱@） 和 2 共面， 我们可以将其表示为这两个向量的线性组合： C1t， 1 ． + C2 2 为了求解系数和， 我们利用向量的点积 （内积） 性质。 已知 2 = COS Qo

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

丿 sin((l 一 t)o 丿

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在实际的 AI 工程代码 （如广泛使用的 mergekit 工具） 中， 模型动辄包含数十亿参数的矩阵， 我们不能直接对任意矩阵套用上述公式， 需要经过一套严密的工程处理。 以下是合并模型 A （权重办） 和模型 B （权重） 的详细工作流： 形状与词表对齐 (Tensor Matching): 合并前必须扫描两者的网络结构。 如果存在形状或词 1 ． 表 (Vocab) 不匹配， 会通过注入空张量 (Empty tensors) 进行填充 (Padding), 强制对齐到相同的维度。 展平为一维向量 (Flattening)： 提取网络中对应的张量层， 并将其拉平为一维向量和 VB 2 ． 模长提取与归一化 (Normalization): 计算两者的原始欧几里得模长目 v 和 IIVBIIO 然后将 3 ． 它们除以各自的模长， 变成刚好落在单位超球面上的方向向量 4 和计算夹角余弦 (Dot Product)： 计算两者的点积 d = 且 ·， 这其实就是 cos 4 ． 5 ． 极值与共线保护 (Fallback Check)： · 在代码实现中， 通常会设置一个阈值 （例如 DOT THRESHOLD = 9995）。 如果点积极其接近 1 （意味着两个模型的这个参数层几乎是平行的， Q O), 那么 sin Q 会趋近于 0， 导致公式中出现除以零的灾难。 此时， 算法会自动退化为普通的线性插值 (LERP)。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

6 ． 球面插值计算 (Apply SLERP): 计算出确切的夹角 Q = arccos(d)， 然后根据设定的融合比例乯 （例如 0 · 5 表示各取一半 ), 代入前文推导的 SLERP 公式， 得到方向插值后的新单位向量 neWO 模长恢复 (Magnitude Scaling)： 由于我们之前把向量变成了单位向量， 现在需要把能量 “ 还 " 回去。 通常会对原始模长也进行一次线性插值 M = （1 一瞓 v + v， 然后把新 7 ． 8 ． 模长乘到上： 权重。 vneu, × 重构张量 (Reshape): 把一维的 vnew 重新折叠回原始的多维矩阵形状， 替换掉原模型中的

## 三、RL

### （一）视频生成中的RLHF

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 纟屯净视频预测 0）： 不同于语言模型按 Token 输出， 扩散模型的每次采样轨迹是连续的噪声演变。 在训练迭代中， 模型模拟完整的视频推理流水线， 由当前的去噪策略直接预测出不带噪声的生成视频。 多维奖励评估： 将预测的送入三个独立的奖励模型矩阵一一基础模型 （评估图文对齐和结构）、 运动模型 （惩罚伪影、 奖励流畅度） 以及美学模型 （从关键帧提取视觉质感评分）。 复合梯度反传： 直接将这几个奖励模型的得分进行线性组合， 通过无需 Critic 网络的 GRPO 机制 （利用同一 Prompt 下的生成组内相对优势）， 直接最大化该复合奖励， 并将梯度反传给扩散模型。 多轮动态博弈： 为了防止模型找到某种作弊手段 （Rewa rd Hacking, 例如为了获取高美学分而让画面彻底静止 ), 在生成模型和奖励模型之间执行多轮迭代学习 (Multi-round iterative learning), 从而稳步拔高人类偏好对齐的性能上限。

### （二）ABot-World中利用RL学习物理规律

高德通过两个核心组件，把优化目标从像素相似度转向物理一致性：

Proposer module：负责根据当前任务，列一份物理规则清单，说清哪些能做，哪些绝对不行。

Scorer module：对模型生成的多个结果逐帧打分。

然后用Diffusion-DPO强化合规行为，物理正确就奖励，物理错误就扣分。反复纠正下来，模型自然学会了“什么动作不违反物理”，例如能够根据输入的末端位姿和夹爪状态，推演出未来的时空动力学变化，不再只是像素层面的“看起来像”。

## 四、模型蒸馏与Self Forcing

通过以上方式训练出的模型，但在与物理世界的交互中，低延时往往非常重要，故需要更少的扩散步数等，这就需要对模型进行蒸馏。此外，有时也会用具有双向注意力的扩散模型作为教师模型，具有因果注意力的自回归模型作为学生模型，使其更具有对未来的全局视野。

此外，生成式世界模型在预训练时是以Ground Truth作为上下文训练的，这是一个Off-Policy的过程。长轨迹运行时，如果出现一点误差，就容易进入OOD区域导致崩溃，故我们要在训练时让模型适应在自身生成的轨迹作为上下文的条件下，进行生成，完成自主纠偏的过程，即Self Forcing。研究表明，Self Forcing需要运用分布匹配蒸馏（DMD）的方法才能稳定训练。

具体方法见扩散模型的蒸馏与Self forcing一节。

## 五、生成对抗优化

世界模型特别是视频生成模型要“模拟真实世界”，一种思路就是让它“模拟得更真实”。

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

对抗优化 (Adversarial Optimization)： 因为在纯 DMD 蒸馏中缺乏真实数据的直接监督， 学生容易继承教师的偏差。 为了提升生成质量， 在伪分数网络上挂载了一个 GAN 分类头刀 （·） 进行对抗训练。 其对抗目标函数分别为生成器损失 CG 与判别器损失 £ 刀： + 2 CG = 了 （1 一刀伊。 0， 〗 其中 ·） 为 Softplus 函数， 与 p(i) 分别为真实视频与合成视频分布。

## 六、SOAR：自我纠偏训练

腾讯混元团队提出的HY-SOAR让模型在去噪过程中学会自我反思和纠偏。我们知道，SFT只教模型处理“理想轨迹”，但推理时模型走的是自己的轨迹，一旦早期去噪出现偏移，后续状态就进入了训练中从未见过的区域；RL则将数据中丰富的轨迹级信息压缩成了一个标量奖励，大量可用于纠正中间步骤的信号在转化中丢失，当数据质量足够高时，瓶颈在于利用率。RL在利用率上打了折扣，SOAR的目标是把这个折扣拿回来。

SOAR的工作流：

1.对真实样本执行一步无梯度前向推理，模拟模型自身可能产生的偏离；

2.对偏离状态重加噪，构造辅助训练点；

3.以原始样本为锚点，计算解析的纠正目标。

SOAR不是要替代RL，而是为RL提供一个更稳定的起点。当前RL的一个核心挑战是基础模型本身的生成轨迹还不够稳定，直接用奖励信号驱动探索，模型容易在不稳定的区域做出过激调整，导致某个指标提升但其他维度崩塌。SOAR可以先把模型的轨迹稳定性拉到一个更高的基线，在此基础上再接入RL做偏好探索，模型就能在一个更安全的区间内进行风格调整和质量优化。

## 七、数据处理

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

高质量的训练数据是上述模型收敛的基础， 其数据清洗是一个高度自动化的过滤流水线： 1 ． 2 ． 3 ． 4 ． 镜头分割： 利用帧间差异检测或预训练检测器， 将原始长视频切分为最大 12 秒的连贯片段。 视觉遮挡修复： 结合启发式规则和目标检测模型， 识别并自适应裁剪掉水印、 字幕等噪声区语义去重： 利用内部视频表征模型提取特征向量并进行聚类。 在高度相似的簇中， 只保留美学质量得分最高的一个样本。 长尾分布重平衡： 统计视频在动作、 风格、 分辨率等维度的概率分布。 对头部冗余类别进行降采样， 对尾部类别进行概率加权， 确保联合分布的均衡。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
