---
title: "34.6 生成式世界模型的上下文工程（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/34.多模态生成与生成式世界模型/34.6 生成式世界模型的上下文工程（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 34.6 生成式世界模型的上下文工程（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、上下文的时空压缩

### （一）问题背景

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

要解决的问题： 在自回归生成长视频时， 随着历史帧数量的不断增加， Transformer (DiT) 的厶口线性甚至平方级增长， 导致显存爆炸和推理速度断崖式下降。 传统的滑动窗 Context Length 厶王口 (Sliding Wi n d (w) 会丢失长程历史信息， 而直接丢弃历史帧则会导致场景不一致。 + 2 TSCM 算法工作： · 为了在保留全局历史信息的同时降低计算量， Yume1.5 采用了一种 * * " 近密远疏 “ 的自适应压缩策略并在 DiT Block 中双管齐下进行特征融合： 步骤 1： 时空压缩 (Temporal-Spatial Compression) 对于历史帧 zc, 模型首先在时间维度上进行每 32 帧抽样 1 帧的随枧采样， 然后使用高压缩比的 Patchify 操作。 距离当前预测帧越远的历史帧， 下采样率越大： 距离一 1 到一 2 的帧： 下采样率为 （1， 2， 2）。 距离一 3 到一 6 的帧： 下采样率为 （1， 4， 4）。 距离一 7 到一 23 的帧： 下采样率为 （1， 8， 8）。 这种通过插值 DiT 中的 Patchify 权重来实现的压缩， 大幅减少了传入模型的 Token 数量 + 1 0

### （二）时间与空间压缩

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

要解决的问题： 在自回归生成长视频时， 随着历史帧数量的不断增加， Transformer (DiT) 的厶口线性甚至平方级增长， 导致显存爆炸和推理速度断崖式下降。 传统的滑动窗 Context Length 厶王口 (Sliding Wi n d (w) 会丢失长程历史信息， 而直接丢弃历史帧则会导致场景不一致。 + 2 TSCM 算法工作： · 为了在保留全局历史信息的同时降低计算量， Yume1.5 采用了一种 * * " 近密远疏 “ 的自适应压缩策略并在 DiT Block 中双管齐下进行特征融合： 步骤 1： 时空压缩 (Temporal-Spatial Compression) 对于历史帧 zc, 模型首先在时间维度上进行每 32 帧抽样 1 帧的随枧采样， 然后使用高压缩比的 Patchify 操作。 距离当前预测帧越远的历史帧， 下采样率越大： 距离一 1 到一 2 的帧： 下采样率为 （1， 2， 2）。 距离一 3 到一 6 的帧： 下采样率为 （1， 4， 4）。 距离一 7 到一 23 的帧： 下采样率为 （1， 8， 8）。 这种通过插值 DiT 中的 Patchify 权重来实现的压缩， 大幅减少了传入模型的 Token 数量 + 1 0

由于世界在时空上的近似连续性特质，这种下采样对世界模型性能的影响较小，但能大大减轻显存压力。

### （三）通道压缩与线性注意力

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

进一步地， 历史帧 zc 经过压缩率为 （8， 4， 4） 的 Patchify, 并将其通道维度压缩至 96， 得到 0 在 DiT Block 内部， 当前视频 Token zl 经过交叉注意力层后， 提取出预测帧 zl， 并与 “ “ 拼接， 得到融合特征分灬。 为了避免标准注意力机制中序列长度带来的二次方计算复杂度， 模型在此处使用了线性注意力 (Linear Attention)。 + 2

在DiT Block内部，当前视频token与在窗口Full Attention层中经过时间空间压缩直接保存的近期历史token交互，也与Linear Attention层中经过通道压缩保存的远期历史隐状态拼接融合。

## 二、KV Cache中的重锚点

自回归模型有一个通病：误差累积。生成到第100块时，它参考的是第99块和第98块；如果第99块有一点点误差，第100块就会照着变形的画面继续生成，最后导致严重漂移。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

注意力缓存 （KV Cache) 的常规逻辑： 模型在生成新帧时， 会 “ 回顾 " 之前的帧 （即查阅 KV 1 ． 缓存）。 通常， 模型倾向于关注最近的几帧以保持动作连贯。 2 ． AHIS 的核心思想： “ 厚古薄今 " · 论文设定了一个容量为 * * 5 个 Block （区块） * * 的注意力窗口。 ' 身份锚点 (SinkTokens)： 将最开始生成的、 质量最高、 身份最完美的前 3 个 Block 永久性地锁定在缓存里 （不随时间滑动而被淘汰）。 这些就像是 “ 身份证件照 "。 滚动缓存 (Rolling Tokens)： 剩下的 2 个 Block 用来存储最近刚刚生成的画面， 并且随着时间不断滑动更新， 用于保持当前嘴唇和动作的物理连贯性。 为什么叫 Anchor-Heavy （重锚点）？ 3 ． 传统的注意力下沉 (Attention Sinks, 多用于大语言模型） 通常只保留极少量的 Token （比如第一句话的首个词） 用来稳定注意力计算。 而这篇论文故意给身份锚点分配了极其不成比例的巨大权重 （3 个 Block 对比 2 个 BIock) 效果： 这强迫模型在生成每一帧时， 绝大部分的注意力都死死盯住最初始那张没有畸变的 “ 完美脸 （高保真表征） "， 从而在根本上压制了模型去模仿最近帧里那些细微的扭曲误差。 这使得数字人即使连续说话数分钟， 长相也不会发生漂移。 + 2

## 三、训练上下文模拟注入误差

在推理时，模型的上下文是自身已经生成的帧，难免有误差。如果训练时模型只学会了基于真实帧进行生成，则在推理时在出现误差时就容易进入OOD区域。为解决该问题，昆仑万维的Matrix-Game 3.0在训练上下文中就模拟注入误差，从而增强模型对真实场景下上下文误差的鲁棒性。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 分组输入： 将视频潜变量序列分为前帧 （作为历史条件） 和后 N 一帧 （作为当前需要预测的加噪目标）。 误差收集： 计算模型预测的干净估计值与真实潜变量之间的残差： 误差注入： 将残差存入误差缓冲区在训练时按照均匀分布采样标量 7， 对历史潜变量进行扰动： 条件注入与优化： 通过交叉注意力 ℃ ross-Attention) 准确注入离散的键盘动作， 通过自注意力 (Self-Attention) 注入连续的鼠标控制信号。 最终的流匹配 (Flow-matching) 优化目标为： + 1： N + 1： N VO 2

## 四、相机感知的长程记忆机制

Matrix-Game 3.0通过设计长程记忆模块，提升了在自回归的长程生成中的空间一致性与稳定性。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了保证玩家在虚拟世界中长时间漫游后 " 回头看 " 时， 场景布局和细节不发生改变， 模型引入了显式的长程记忆。 统一自注意力机制： 检索到的记忆潜变量、 近期过去帧和当前加噪帧被置于同一个注意力空间中， 由同一个 DiT 进行联合时空建模， 避免了外部记忆分支带来的特征不对齐和收敛缓慢问达。 相机感知检索与编码： 并非所有历史都有用。 模型根据相机的位姿和视场角重叠度来检索相关的记忆帧， 并使用相对 Plücker 坐标 (Plücker encoding) 进行显式的几何编码， 帮助模型准确对齐不同视角下的同一场景。 记忆通路同样采用了上述的误差注入工作流， 以减少训练和推理间的偏差。 改进的旋转位置编码 (RoPE)： 为了避免相隔很远的记忆帧和当前帧出现周期性的位置对齐 (Positional aliasing), 模型对不同注意力头引入了独立的扰动系数：

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 自回归模型有一个通病：误差累积。生成到第100块时，它参考的是第99块和第98块；如果第99块有一点点误差，第100块就会照着变形的画面继续生成，最后导致严重漂移。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
