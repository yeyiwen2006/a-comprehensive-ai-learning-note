---
title: "34.5 Diffusion Forcing与上下文分步去噪（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/34.多模态生成与生成式世界模型/34.5 Diffusion Forcing与上下文分步去噪（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 34.5 Diffusion Forcing与上下文分步去噪（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、Diffusion Forcing

### （一）问题背景

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

下一个 Token 预测模型 (Teacher Forcing)： 例如 G PT 系列。 它们支持生成可变长度的序列， 但往往在连续数据 （如视频生成） 上由于误差累积容易导致预测崩溃， 且由于是逐步确定未来的， 无法根据未来的奖励 / 目标对当前步骤进行 “ 引导 (Guidance)" 全序列扩散模型 (Full-Sequence Diffusion)： 常用于视频生成或离线强化学习规划。 它将所有时间步作为一个整体添加相同水平的噪声， 这虽然支持多步引导， 但它采用了非因果架构， 只能生成固定长度的序列， 无法像人类一样基于 “ 因果不确定性 " 来做决策。

目前大多扩散与自回归结合的模型都是分块生成，每次根据已经生成的块x_1,...,x_t，用扩散模型从噪声中生成新的一块x_t+1，这里的隐含前提是，每一块的噪声水平都是相同的。我们注意到对Token添加扩散噪声本质上是一种“部分掩码”，0噪声意味着完全无掩码，纯噪声意味着完全被掩盖，这种做法也就是相同水平的掩码。

而Diffusion Forcing则不同，为序列中的每一个Token或每一块（统一以x_1,x_2,...,x_t表示）分配独立、随机的噪声水平，并训练一个因果模型来“解掩码”。

对于扩散模型，经过Diffusion Forcing训练后也可具备自回归生成的能力。

### （二）训练阶段

Diffusion Forcing训练目标是让模型学会对具有任意且独立噪声水平的序列进行去噪。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 4 ． 5 ． 采样轨迹： 从数据集中采样出真实观测序列 （昀， · 分配独立噪声： 遍历时间步 = 1， · · ”， 为每一个独立、 均匀地采样一个噪声水平岳 e { 0， 1， …， K}O 前向加噪： 将原始 Token 根据各自的噪声水平进行前向扩散， 得到观测值。 并计算目标日， 四。 隐状态更新与噪声预测： 模型 （如 RNN 或具有因果掩码的 Transformer) 根据前一时刻的隐状态荔一 1、 当前的带噪观测和噪声水平， 预测出噪声 0 = （荔一 1， '， 岳）。 随后更新隐状态荔。 反向传播： 将所有时间步的预测噪声与真实噪声计算均方误差 (MSE Loss) 并更新网络权重

### （三）推理阶段：基于排程矩阵的控制

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在算法定义中， 扌非程矩阵是一个维度为 M × 的二维网格： 列 （丆）： 代表序列的时间步长 ()i m e steps), 即 e { 1， 2， 一 ·， }。 · 行 (M)： 代表去噪采样的迭代步骤 (Denoising steps), 索引为 m。 · 元素取值 (Kmt): 矩阵中的每一个元素 1Cm e { 0， 1， · 一， K}， 精确指定了第个 Token 在第 m 次去噪迭代时， 所必须达到的目标噪声水平。 其中 K 是纯白噪声 （完全掩盖）， 0 是干净数据 （无噪声）。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

模型在推理时， 完全遵循这个矩阵的 " 乐谱 " 来演奏。 具体执行流程如下： 1 ． 2 ． 3 ． 4 ． 5 ． 全局初始化： 算法开始时， 将整个目标序列昀初始化为独立的高斯白噪声， 此时所有 Token 的噪声水平都是。 外层循环 （按行降噪）： 沿着矩阵的行向下遍历 （例如 m 从 M 一 1 递减到 0）。 每一行代表了一轮全局的去噪刷新。 内层循环 （因果推进）： 在固定的行 m 内， 沿着时间轴 = 1， · 一， 从左到右遍历。 目标读取与单步去噪： 对于当前的 (),， 算法读取矩阵设定值左 = 1Cm “ 随后， 模型利用当前的隐状态一 1 和带有当前实际噪声的， 预测出噪声 （一 1，， 左）， 并通过 Langevin 动力学公式 （或 DDIM) 将的噪声精确降至目标层级。 因果状态更新： 利用刚刚去噪后的更新隐状态， 由于是因果架构， 这个更新后的将包含 “ 的最新信息， 并传递给下一个时间步 + 1。

### （四）基于排程矩阵K的生成范式

三种生成模式：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 全序列扩散 (Full-Sequence Diffusion) 模式如果我们要模仿传统视频扩散模型 （如 SVD 或 Sora 的某一层逻辑）， 只需让矩阵的每一全相同。 工作流呈现： 五 ／ 2 人 / 2 / 2 T ] 一分 [ 1， 2， 3 1， 2， 3 控制效果： 所有 Token 同步均匀去噪。 2 · 自回归 (Autoregressive /Teacher Forcing) 模式行元素完如果我们要模仿语言模型 （如 GPT) 逐字生成， 只需让矩阵呈现阶梯状分布， 确保前一个 Token 彻底降噪为 0 后， 再开始降噪下一个 Token。 工作流呈现： 控制效果： 每次只针对一个 Token 进行彻底去噪， 生成完毕后作为下一帧的绝对确定的历史条件。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

3 · 因果不确定性 (Causal Uncertainty / Pyramid Scheduling) 模式一 _ DF 的杀手锏这正是论文的核心创新。 它让矩阵呈现一种 * * " 金字塔 “ 或 “ 拉链 " 状分布 * *： 让 " 近期未来 " 降噪较多， 而 “ 远期未来 " 保持较高的噪声。 工作流呈现： CI， 2， 3 控制原理： 五 ／ 2 1 0 五 / 2 K]T + 宀 0 0 ／ 2 T ] 一 01， 2， 3 此时， 序列形成了一个 “ 噪声梯度 "。 当我们在预测昀时， 昀和并没有完全成型， 但它们携带了未来宏观趋势的 " 残影 （高噪声状态） "。 这种安排完美地契合了贝叶斯滤波中对于物理世界 " 未来不确定性递增 " 的建模， 并允许我们将未来目标的梯度平滑地反向传播回当下的决策点 （实现规划 Guidance)

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在传统的自回归模型中， 过去的 Token 一旦生成就固定死了， 未来的奖励无法修改过去的决定。 而在 DF 中， 由于未来的 Token 虽然还在较高的噪声状态， 但它们可以通过因果架构的隐状态荔将梯度反向传播回较早的 Token。 这意味着， 模型可以 " 预见 " 远期未来的奖励， 并在尚未完全固定的 " 现在 “ 做出路径修正。 + 3

与Transformer架构的对应：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 信息的彻底屏蔽 （对应传统 Causal Mask) 如果在扌非程矩阵中， 我们将所有未来的 Token 标记为纯高斯白噪声 （噪声水平） · 在 Transformer 的自注意力计算 e “ “ （Q， K,） = softmax(Q-C） 中。 对于过去的 Token, 当它试图用自己的 Query 去关注未来的 Key 时， 由于未来 Token 对应的特征是完全从 （0， 采样的， 它与任何有意义的模式内积 QKT 的期望都接近 0 （或随机均匀分布）。 更重要的是， 未来 Token 的 value 也是纯随机噪声。 任何对纯随机噪声的线性组合， 其结果依然是没有语义信息的随机噪声。 结论： 未来被标注为全白噪声时， 没有一丝一亳的真实信息 (Ground Truth) 会泄漏到过去。 这等效于一个物理的 Causal Mask0

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 信息的按需透传 ()F 独有的 Fractional Masking) 如果我们改变扌非程矩阵， 让未来的 Token 带有部分噪声 （例如左 = / 2） 此时， 未来的 Token 变成了 0@0 + 1 一 0。 它体内既含有真实的未来特征 0， 又包裹着噪 6。 当过去的 Token 在计算自注意力时， 它会从 V 矩阵中提取到被衰减过的未来信号。 结论： 通过控制未来 Token 的噪声比例， Transformer 不需要改变任何网络结构， 也不需要复杂的 Attention Mask 设计， 就能实现从 " 完全不因果 " 到 “ 部分因果 "， 再到 " 严格因果 " 的平滑过渡。 这就是 Diffusion Forcing 范式的优雅之处： 噪声 (Noise) 不再仅仅是需要被去除的障碍， 而是变成了调节网络感受野、 控制因果性、 以及传播规划梯度的核心控制介质。

### （五）利用蒙特卡洛引导实现规划

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在传统的全序列扩散模型中， 由于所有的 Token 必须以相同的噪声水平同步去噪， 未来和现在是被强行绑定在一起的， 因此在给定的噪声层级下， 模型缺乏明显的随机性来源来进行未来的蒙特卡洛估计。 但 DF 采用了因果架构 (Causal Architecture) 独立噪声水平。 这意味着： 未来的 Token 怎么变， 完全依赖于过去和现在的隐状态。 我们可以在不改变当前 ' 的情况下， 随意初始化未来步 （昀 + 1， （例如设为纯白噪声）， 并让模型单向地向后推演。 ．， + “） 的噪声水平

MCG在每一次去噪迭代都探索N条路，根据奖励情况决定主去噪路径怎么调。步骤如下：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

MCG 的运行机制如下： 1 ． 2 ． 3 ． 4 ． 当前时刻的观测： 智能体在环境的当前真实时间步获得了历史累积的隐状态荔一 1。 生成规划视距 (Look-ahead Window)： 模型并不会一口气规划到宇宙末日， 而是设定一个向未来的视距 HO 此时模型开始在 " 脑海 “ 中生成一个局部的未来序列 Ct:t+H。 每个去噪步的 MCG 采样： 在这个局部序列 Ct:t+H 的每一次去噪迭代 (Denoising step m) 中， 为了决定如何给当前的 ' 去噪， 模型都会在脑海中向未来 + 1 到 + H) “ 并行分叉 " 出多条带有不确定性的轨迹。 单步执行与滚动 (MPC): 经过多轮去噪， 当前 Token 昀 （即当前需要执行的动作 (t) 彻底清晰 （噪声降为 0）。 智能体在现实中执行这个动作， 环境返回真实的奖励和下一个观测 Ot + 1。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

不需要把这 N 条轨迹彻底降噪到 0， 只需要降噪到能算出奖励函数的程度即可。 计算这 N 条轨迹的预期未来奖励 R(i)， 并求平均值 （期望）：

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

我们对这个期望奖励月， 关于当前正在去噪的那个模糊的输入 '， 求亻扁导数 （梯度）： 这个梯度代表着什么？ 它代表着： " 如果我把当前稍微带有噪声的过 ' 往梯度的方向稍微挪动一占那么它在未来演化出高奖励轨迹的概率就会最大化。 占

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

现在， 我们将刚才求得的奖励梯度直接注入到这个预测中 （类似于分类器引导 Classifier Guidance 的做法） 6 然后， 使用这个被修改过的噪声己去把 ' 降噪到下一个更清晰的层级岳一 1

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在 MCG 中， 那 N 条被采样出来的未来轨迹， 在计算完梯度之后就直接被丢弃了。 它们的作用仅仅是充当 " 侦察兵 "， 为计算期望梯度 (Expected Rewa rd Gradient) 提供方差更小的无偏估计。

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

MCG 的运行机制如下： 1 ． 2 ． 3 ． 4 ． 当前时刻的观测： 智能体在环境的当前真实时间步获得了历史累积的隐状态荔一 1。 生成规划视距 (Look-ahead Window)： 模型并不会一口气规划到宇宙末日， 而是设定一个向未来的视距 HO 此时模型开始在 " 脑海 “ 中生成一个局部的未来序列 Ct:t+H。 每个去噪步的 MCG 采样： 在这个局部序列 Ct:t+H 的每一次去噪迭代 (Denoising step m) 中， 为了决定如何给当前的 ' 去噪， 模型都会在脑海中向未来 + 1 到 + H) “ 并行分叉 " 出多条带有不确定性的轨迹。 单步执行与滚动 (MPC): 经过多轮去噪， 当前 Token 昀 （即当前需要执行的动作 (t) 彻底清晰 （噪声降为 0）。 智能体在现实中执行这个动作， 环境返回真实的奖励和下一个观测 Ot + 1。

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

通过这种方式， 当前动作在从纯噪声降为 0 的漫长过程中， 每走一步， 都会受到未来预期的 " 梯度拉扯 "， 最终使得当降噪为 0 时， 它本身就已经是一条通向最优解的动作。 这种方法不仅采样效率极高， 而且生成的轨迹更加平滑且符合物理规律。

### （六）为什么传统自回归和扩散模型都无法将未来梯度传到过去，而该架构可以？

自回归：

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 必须要先有 “ 现在 "， 才能推演 “ 未来 "： 自回归模型必须把当前的 Token 完全确定下来 （例如从分布中 sample 出一个具体的值， 变成一个 hard 变量）， 才能把它作为历史条件输入进去， 从而推演出未来的 + 1。 不可导的采样阻断了梯度： 因为已经被 “ 硬确认 (Hard commitment)" 了， 当你推演出未来并计算出奖励月时， 由于中间经历了一个不可导的离散采样步骤 （或者即便使用了连续松弛， 也无法在当前步骤内撤销并重新采样 ), 你很难用平滑的梯度直接修改已经生成的。 结果： 要想在自回归模型里做长视距引导， 通常只能依赖启发式的树搜索 (Tre e Search, 如 MCTS)， 这种方法计算代价极其高昂， 且难以利用连续的梯度信息。

扩散：

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 同步去噪导致命运被绑定： 在全序列扩散中， 所有的时间步 （过去、 现在、 未来） 都被强行绑定在同一个噪声水平上同步去噪 (denoising tokens in tandem) 无法产生 “ 分支 (Branching)": 要在当前状态过下推演出多条不同的未来轨迹， 你需要一个额外的随机性来源。 但在全序列扩散中， 如果你固定了当前的带噪状态， 并且大家都在同一个噪声层级， 整个计算图就固定了， 你只能得到一条确定的、 和当前状态紧紧绑定的未来轨迹。 结果： 你算出的只是 " 这一条 " 轨迹的梯度。 由于单条轨迹的方差极大 （特别是包含纯随机噪声时 ), 这个梯度在引导时非常不稳定， 这直接导致了 Diffuser 生成的动作在时间因果上常常不一致， 甚至在实际执行时不得不抛弃生成的动作， 转而求助于手工设计的 PD 控制器。

Diffusion Forcing（DF，以近期噪声k=2，远期噪声k=K为例）：

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 突破全序列扩散的瓶颈： 解锁蒙特卡洛分支因为未来的噪声水平 （如） 远远高于当下的噪声水平 （如 2）， 这意味着未来还没有被绑定。 我们可以把当前的冻结住， 然后向未来注入成百上千个不同的纯白噪声）。 模型会以此为起点， 在脑海中把这些未来的白噪声一直降噪到层级 2 （即对齐到近期的噪声水平） 这就完美地生成了 N 条不同的未来轨迹， 从而能够计算出方差极小、 极其稳定的期望奖励梯度 (Expected Reward Gradient) V E[R]O 这是全序列扩散做不到的。 2 · 突破自回归的瓶颈： 保持当下的 “ 软可导性 " 此时， 当下的 Token 并没有像自回归模型那样被 " 硬确认 "， 它依然处于一个微弱的噪声状态 （正因为它还是一个连续的、 带有不确定性的变量， 所以上面计算出的极其精准的期望梯度， 可以直接作用于它， 平滑地修改它在这一步 Langevin 动力学中的去噪方向。

### （七）历史信息轻微加噪技巧

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在需要超长序列生成 （比如生成 ℃ 00 帧以上的视频） 时， DF 利用采样阶段的灵活性提出了一个极其聪明的技巧来防止误差累积： 为历史信息轻微加噪在生成下一帧时， DF 并不完全信任上一帧一 1 （因为它是模型自己生成的， 必定含有微小瑕疵）。 相反， DF 会保留一个带有极其微小噪声 ksmall 的上一帧隐状态 zksmall （0 < ksmall 《 K ), 并以此为条件去生成下一帧。 由于模型在训练时本身就是基于带噪输入学习的， 这使得自回归生成一直处于分布内 (ln-distribution)， 从而实现了无限长度的稳定 Rollout, 彻底解决了传统 Teacher Forcing 模型的崩溃问题。

### （八）理论可行性证明

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

从理论上， 为什么分配独立噪声水平是可行的？ 论文的附录 A 给出了一套基于证据下界 (ELBO) 的严谨数学证明。 模型实际上是在优化一个修改版的扩散 ELB00 假设前向马尔可夫过程为 q@ q@k 》 1）， 生成模型参数化为脚。 作者证明了 DF 的训练过程最大化了真实联合分布对数似然的下界。 根据定理 A.I， 对于随枧独立采样的噪声水平左 1， 其期望对数似然的 ELBO 可以写为：

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 lnpo@t 1，

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

lnpo@t， 一 1） + 乥 K + 1 + 1

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这里的核心发现是， 在隐变量一 1 完全表达力足够的情况下， 由于各个时间步 t 上的损失项是解耦的， 权重项可以被忽略或重新加权。 因此， 最终的训练目标简化为我们常见的扩散模型最小二乘形式： 2 由于损失涵盖了序列中所有可能的局部掩码扌非列， 这保证了在推理时， 模型可以用任意的时间步调度策略来采样真实数据的子序列边缘分布。

## 二、上下文分步去噪

### （一）问题背景

> [图片 26：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

近年来， 自回归 (A R) 生成因其支持流式输出、 无限扩展和实时交互， 成为长视频生成和世界模型的核心技术路线。 然而， AR 流程面临一个严峻的挑战： 分布偏移 (Distribution Drift), 表现为随着视频长度增加， 画面会出现过度饱和、 运动重复和语义漂移。 + 2 现有方法的困境： 为了保持相邻视频块 (Block) 之间的时间连贯性， 现有的 AR 方法 （如 Self-Forcing) 通常会先将前一个视频块完全去噪 （使其噪声水平 = O), 然后将其作为高度清晰的上下文， 用于生成下一个视频块。 虽然这种极其干净的上下文锚定了时间一致性， 但它同时也极大地放大了模型自身的预测误差： 模型会以极高的置信度将前几步积累的误差不断向后传播， 从而加剧了长视频的质量退 + 3 0

### （二）核心思想

> [图片 27：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这篇论文的核心洞见是： 高度干净的上下文并不是必须的。 受双向扩散模型 (Bidirectional Diffusion Models) 在共享噪声水平下对所有帧进行去噪却依然能保持连贯性的启发， 作者提出： 如果在与当前块相同的噪声水平下提供上下文， 不仅能提供足够的时间一致性信号， 还能有效抑制误差传播。 + 3

> [图片 28：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 误差分解与偏差 · 信息权衡假设在第 j 步 （从噪声水平与降噪到与 + 1） 对第 “ 个视频块 Bn 进行去噪。 表示前一个块的真实干净潜变量， 模型的预测值为 “ 一 1） 是积累的预测误差。 如果我们在噪声水平标下构建上下文 c 是 1， 其公式为：， 其中

> [图片 29：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

真实信号传播的偏差随机扰动

> [图片 30：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

从这个公式可以看出， 真实信号和传播偏差共享同一个系数 （1 一。）。 降低噪声水平 （即让上下文变得更干净） 会增加有用信号， 但同时也以同等比例放大了误差 -1）。 现有的方法使用 = 0， 使得系数变为 1， 这就导致了全量误差的无衰减传播。 + 2 2 · 时间因果性与最优噪声水平为了产生时间上连贯的视频， 上下文携带的信息量必须大于或等于当前块在第 j 步结束时所拥有的信息量。 在扩散模型中， 信噪比 SNR(t) 随着的减小单调递增。

因此，时间因果性要求对于每一步，上下文信噪比≥当前块在本步结束时的信噪比（如自回归生成中，上下文噪声为0，当前块在本步结束时的噪声也为0），上下文噪声t_c≤当前块在本步结束时的噪声t_j+1。

而我们希望在满足因果性的前提下，最大程度地衰减误差系数(1-σt_c)，则取其最小值，取等时上下文噪声＝当前块在本步结束时的噪声。

### （三）核心算法与推理工作流

> [图片 31：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

传统的 AR 就像盖楼， 第一栋楼从地基盖到顶层 （完全去噪）， 再盖第二栋。 HiAR 则是 “ 分层施工 “： 所有楼房先一起打地基 （第 1 步去噪）， 再一起盖一楼 （第 2 步去噪）， 依次类推。 假设我们要生成 3 个视频块 （BI， B2， B3）， 总去噪步数为 S = 4 步 （噪声表 > 2 > 3 > 0）。 推理序列如下： + 2 （1） （2） （3） 初始化： 生成所有块的纯噪声 1， 1， 1。 去噪步 j = 1 （目标降至乯 2）： 1 ． 2 ． 3 ． （1） 处理 BI： 无上下文。 输入， 模型预测并更新得到 2。 处理 B2： 提取上下文 2。 模型根据输入和上下文 2， 预测并更新得到 2。 （2） 模型根据输入 3） 和上下文， 预测并更新得到与 （3） 处理 B3： 提取上下文 2， 2。 （此时所有块都达到了 t2 噪声水平。 实际工程中， 步骤 2 和 3 的 KV Cache 更新可以与前向传播融合， 实现流水线加速） 2 去噪步 j = 2 （目标降至 3）： 4 ． 重复上述过程。 处理 B2 时， 使用的上下文是刚刚在上一轮生成的。

> [图片 32：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

去噪步 j 一 3 及 j = 4： 5 ． 依次执行， 直到所有块降噪至乯 4 0， 输出最终干净的视频块。 0， 0， 0

这样意味着有12步，比传统的扩散和自回归都慢。为了加速，采取两个措施：

1.反对角线并行

> [图片 33：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

论文在第 3 ． 2 节指出， 在算法 1 中， 第 j 步的第 “ 个块 Bn, 只依赖于第 j 步它前面的块 B<n， 以及它自己上一阶段 （第 j 一 1 步） 的状态。 因此， 在 N × S 的去噪网格中， 位于同一条 " 反对角线 " 上的任务是相互独立的， 可以同时计算。 + 1 具体到你的例子 （3 个块， 4 个去噪步 ), 并行流水线如下： 阶段 1： 阶段 2： 阶段 3： 阶段 4： 阶段 5： 阶段 6： 计算 BI 的第 1 步。 同时计算 B2 的第 1 步和 BI 的第 2 步。 同时计算 B3 的第 1 步、 的第 2 步和 BI 的第 3 步。 同时计算的第 2 步、 的第 3 步和 BI 的第 4 步。 同时计算 B3 的第 3 步和 B2 的第 4 步。 计算 B3 的第 4 步。 通过将每个去噪步分配给专用的进程， 总的串行等待阶段从 N × S = 12 骤降到了 N + S 一 1 = 6 个阶段。

2.KV Cache算子融合

> [图片 34：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

即使在单卡环境下无法做多进程物理并行， HiAR 也做了极具工程价值的算子融合。 在分层循环中， 单纯地更新 Bn 的 KV 缓存和去噪 Bn+1 原本是两次独立的前向传递。 + 1） 论文提出将 [c(n)， ] 在帧维度上拼接， 通过一次前向调用 (Forward Call) 就同时完成了 “ 写入上下文缓存 " 和 " 利用该缓存去噪下一个块 " 两个动作。 这种融合将每层循环的成本降至 N + 2 次传递， 最终在 4 步设置下实现了约 1 ． 8 倍的挂钟时间 (wall-clock) 加速。 ' + 4

### （四）训练算法与工作流

> [图片 35：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 A： 教师轨迹准备 (Teacher Trajectory SampIing) （： 这一步通常可以离线预计算在训练开始前准备调用强大的 Wan2.1-14B 教师模型。 1 ． 在双向注意力模式下， 让教师模型运行大量的 ODE 去噪步 （例如 50 步）。 2 ． 从这条高质量的密集轨迹中， 提取出与学生模型 4 步时间表对齐的参考检查点 3 ． ℃ heckpoints) {， 步骤 B： 学生模型前向传播 （获取两种预测） 在同一次训练迭代中， 学生模型需要走两条不同的路径来获取计算损失所需的预测值： 1 ． 2 ． 路径 1 （对应 DMD 损失）： 因果自展开。 学生模型在因果注意力下运行， 按照分层去噪的方式， 用自己的输出作为上下文进行多块 (Blocks) 展开。 这产生了学生模型的单步输出分布脚 （00D。 路径 2 （对应 FKL 损失）： 双向模式单步预测。 学生模型切换到双向注意力模式， 输入步骤 A 中教师的检查点卩 （注意， 仅限第 1 步， 即 K = 1), 预测出一个速度场。

> [图片 36：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 C： 联合计算损失与反向传播 1 ． 2 ． 3 ． 计算 £ 刀 M 刀： 根据路径 1 中学生因果自展开的分布， 与教师模型的多步输出分布计算 Reverse-KL 散度。 这迫使学生在自回归时向教师的高密度区域靠拢。 计算 £ 丆刀： 拿路径 2 中学生预测的速度场， 与教师轨迹中相邻两个检查点算出的 “ 真实速度 " 进行对比， 计算误差。 这迫使学生在双向模式下保持动态多样性 （从而间接保护了因果模式下的动态）。 合并与更新： 将两个损失按权重相加 £ = £ 刀 M 刀 + /\ £ 丆 （其中 = 0 · 1), 然后进行反向传播， 更新学生模型 Wan2.1-1.3B 的权重。

两个损失函数：

> [图片 37：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · £ 刀 M 刀： 分布匹配蒸馏损失 （“ 主线任务 "） 这是学生模型在 * * 因果自展开 ℃ ausal SeIf-RoIIout) * * 过程中计算的损失， 目的是让学生学会像老师一样进行多步去噪。 它的本质是一个 Reverse-KL （逆 KL 散度）： 物理意义： 逆 KL 散度评估的是 “ 学生分布覆盖教师分布的程度 "。 由于公式中在前， 它天生具有 * * 寻求众数 (mode-seeking) * * 的倾向。 带来的问题 （低运动捷径）： 当学生模型在长序列中自己滚动生成时， 预测复杂的运动容易出错。 为了快速拉低 £ 刀 M 刀这个损失， 模型会自发地倾向于去拟合教师分布中最安全、 最容易预测的模式一一也就是 “ 几乎不动的静态画面 “。 在分层去噪的复杂上下文中， 这种坍塌效应甚至会被放大。 + 1

> [图片 38：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · £ F 刀： 前向 KL 正则化损失 （“ 防偏护栏 "） 为了防止模型变成 " 静态图片生成器 "， 作者引入了基于轨迹采样的正则化项， 迫使模型单步输出对齐教师的真实参考轨迹。 2 物理意义： 因为目标参考值耳。 是从教师的真实分布中采样的， 优化这个均方误差 (MSE) 在数学上等价于优化一个 Forward-KL （前向 KL 散度） 目标。 解决的问题： 前向 KL 的特性是 " 覆盖面广 (mass-covering)", 它会惩罚 " 模式丢弃 (mode dropping)" 行为。 这就强迫学生模型必须去覆盖教师模型所能生成的各种丰富动态， 从而保住了视频的运动多样性。 + 1

为什么FKL使用双向注意力：（和其Off-policy有关）

> [图片 39：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这是为了防止梯度干扰 (Gradient lnterference) 物理特性上存在根本的 " 不对称性 "：。 论文发现， 这两种注意力枧制在生成图像的双向注意力 (Bidirectional) 的均匀性： 每一帧都能看到所有帧， 因此所有位置生成的帧在质量和模糊度上是均匀的。 因果注意力 (CausaD 的渐进锐化： 前面的帧只能看自己， 后面的帧能看到前面所有的帧。 随着前序上下文不断消解不确定性， 后面的帧会变得越来越锐利 （高频细节越来越多） 灾难性的后果： 教师模型生成的参考轨迹耳。 是均匀的。 如果强行在学生的因果模式下应用 £ 丆五 （即用均匀的参考去指导渐进锐化的网络 ), 这种错配的监督信号会与自回归生成路径严重冲突， 导致整体画面质量断崖式下跌。 破局之道： 作者发现了一个神奇的统计规律： 模型在双向模式下的运动动态得分， 与因果模式下的动态得分具有极强的正相关性 (pearson r = 0 ． 968） 因此， 作者将 £ 丆刀严格限制在双向模式下计算。 这样既能提供一个时空均匀的监督信号来提升全局动态， 又完全不会去污染因果模式下脆弱的高频细节生成机制。 + 2

> [图片 40：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

£ 丆五是 “ 刚性轨迹匹配 (Trajectory Matching)" 在计算 £ F 五刀时， 公式要求学生模型预测的速度场必须极其精确地拟合两个固定参考点之间的差值。 这些参考点。 丆是教师模型在双向注意力模式下提前生成好的。 因为是双向生成的， 这些参考画面在时空上是均匀的 （所有帧的清晰度一致）。 如果学生模型处于因果模式， 它的天性是 * * " 渐进锐化 “ * * （越往后的帧越清晰）。 用一套 " 均匀 " 的刚性坐标点， 去死死卡住一个天性 " 渐进锐化 " 的因果网络， 就会产生严重的物理冲突。 这就好比要求一个习惯用右手画画的人 （因果 ), 必须一比一精准临摹左手画出来的笔触 （双向轨迹 ), 结果自然是画得一团糟。 ' + 4 £ 刀 M 刀是 “ 柔性分布匹配 (Distribution Matching)" £ 刀 M 刀优化的目标是逆向 KL 散度 (Reverse-KL)， 它并不要求学生完全照搬教师的某一条具体演化路径。 在实际操作中， 它的梯度是通过一个 * * " 学习到的分数差 (learned score difference, 类似于判别器 /Critic) " 来计算的。 它只关心一个宏观问题： " 学生最终生成的这个状态， 是否处于教师模型认可的高概率密度区域 * *？ “ 只要学生生成的视频看起来像真实的 （符合教师的分布）， DMD 就不管学生是用 " 渐进锐化 " 还是什么别的手法生成的。 因此， 它不会与因果模式发生冲突。 2

> [图片 41：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

£ 丆五的输入是被强加的： 计算 £ F 时， 强行塞给学生模型的输入。 来自于教师的双向分布。 学生在因果模式下， 突然接到一个并非由因果规律产生的上下文， 这会引起严重的 “ 水土不服 "， 导致梯度信号混乱。 + 1 £ 刀 M 刀的输入是学生自己探索出来的： £ 刀 M 刀是在学生进行 " 因果自展开 ℃ ausal Se | f 一 Ro ut） " 的过程中计算的。 这意味着， 此时输入给 DMD 损失的状态， 本来就是学生模型用自己的因果注意力一步一步踏踏实实生成出来的。 由于输入状态完美契合因果模式的分布规律， 后续的 DMD 判别器自然能给出平滑且准确的梯度指导， 而不会产生错配。 + 3

从计算图角度，为什么损失可以相加：

> [图片 42：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

以下是底层的计算图 ℃ omputational Graph) 流转过程： 唯一的实体是 “ 学生网络参数俨： 虽然有两条路径， 但它们共享同一个神经网络 （即同一个 1 ． 注意力机制的权重矩阵、 同一个 MLP 投影层等）。 注意力模式 （双向 vs 因果） 仅仅是前向传播时 * * 掩码矩阵 (Attention Mask Matrix) * * 的不同输入参数。 构建计算图分支 A （主线）： 传入因果 Mask 和多步输入状态， 网络参数 0 经过一系列复杂的 2 ． 矩阵乘法， 输出结果， 计算出标量 £ 刀 M 刀。 此时， 系统在内存中保留了针对 O 的第一组梯度图。 构建计算图分支 B （支线）： 同一次迭代中， 系统将同一套网络参数 0 的注意力 Mask 切换为 3 ． 双向， 传入教师的单步检查点， 前向传播后计算出标量 £ F 刀。 此时系统构建了第二组梯度图。 4 ． 标量相加与梯度累加 (Gradient Accumulation)： 将两个标量损失相加： Tota1 Loss L DMD + @ ． 1 * L FKL。 当执行 .backward() 时， 反向传播算法会分别沿着分支 A 和分支 B 往回求导。 因为网络参数 0 是共享的， 微积分中的链式法则 ℃ hain Rule) 决定了： 进入参数 O 的总梯度， 等于分支 A 传回的梯度加上分支 B 传回的梯度。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 在传统的全序列扩散模型中， 由于所有的 Token 必须以相同的噪声水平同步去噪， 未来和现在是被强行绑定在一起的， 因此在给定的噪声层级下， 模型缺乏明显的随机性来源来进行未来的蒙特卡洛估计。 但 DF 采用了因果架构 (Causal Architecture) 独立噪声水平。 这意味着： 未来的 Token 怎么变， 完全依赖于过去和现在的隐状态。 我们可以在不改变当前 ' 的情况下， 随意初始化未来步 （昀 + 1， （例如设为纯白噪声）， 并让模型单向地向后推演。 ．...
- 1 ． 2 ． 3 ． 同步去噪导致命运被绑定： 在全序列扩散中， 所有的时间步 （过去、 现在、 未来） 都被强行绑定在同一个噪声水平上同步去噪 (denoising tokens in tandem) 无法产生 “ 分支 (Branching)": 要在当前状态过下推演出多条不同的未来轨迹， 你需要一个额外的随机性来源。 但在全序列扩散中， 如果你固定了当前的带噪状态， 并且大家都在同一个噪声层级， 整个计算图就固定了， 你只能得到一条确定的、 和当前状态紧紧绑定的未来轨...
- 步骤 A： 教师轨迹准备 (Teacher Trajectory SampIing) （： 这一步通常可以离线预计算在训练开始前准备调用强大的 Wan2.1-14B 教师模型。 1 ． 在双向注意力模式下， 让教师模型运行大量的 ODE 去噪步 （例如 50 步）。 2 ． 从这条高质量的密集轨迹中， 提取出与学生模型 4 步时间表对齐的参考检查点 3 ． ℃ heckpoints) {， 步骤 B： 学生模型前向传播 （获取两种预测） 在同一次训练迭代中， 学生模型需要走两...
- 2 · £ F 刀： 前向 KL 正则化损失 （“ 防偏护栏 "） 为了防止模型变成 " 静态图片生成器 "， 作者引入了基于轨迹采样的正则化项， 迫使模型单步输出对齐教师的真实参考轨迹。 2 物理意义： 因为目标参考值耳。 是从教师的真实分布中采样的， 优化这个均方误差 (MSE) 在数学上等价于优化一个 Forward-KL （前向 KL 散度） 目标。 解决的问题： 前向 KL 的特性是 " 覆盖面广 (mass-covering)", 它会惩罚 " 模式丢弃 (mod...
- 这是为了防止梯度干扰 (Gradient lnterference) 物理特性上存在根本的 " 不对称性 "：。 论文发现， 这两种注意力枧制在生成图像的双向注意力 (Bidirectional) 的均匀性： 每一帧都能看到所有帧， 因此所有位置生成的帧在质量和模糊度上是均匀的。 因果注意力 (CausaD 的渐进锐化： 前面的帧只能看自己， 后面的帧能看到前面所有的帧。 随着前序上下文不断消解不确定性， 后面的帧会变得越来越锐利 （高频细节越来越多） 灾难性的后果： 教师模...
- £ 丆五是 “ 刚性轨迹匹配 (Trajectory Matching)" 在计算 £ F 五刀时， 公式要求学生模型预测的速度场必须极其精确地拟合两个固定参考点之间的差值。 这些参考点。 丆是教师模型在双向注意力模式下提前生成好的。 因为是双向生成的， 这些参考画面在时空上是均匀的 （所有帧的清晰度一致）。 如果学生模型处于因果模式， 它的天性是 * * " 渐进锐化 “ * * （越往后的帧越清晰）。 用一套 " 均匀 " 的刚性坐标点， 去死死卡住一个天性 " 渐进锐化...

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
