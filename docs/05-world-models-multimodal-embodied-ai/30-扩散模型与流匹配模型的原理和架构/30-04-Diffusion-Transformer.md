---
title: "30.4 Diffusion Transformer"
source_docx: "第5部分 世界模型、多模态生成与具身智能/30.扩散模型与流匹配模型的原理和架构/30.4 Diffusion Transformer.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 30.4 Diffusion Transformer

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、核心架构与工作流

Diffusion Transformer将Transformer架构引入扩散模型，实现了与大语言模型架构的统一，也提高了可扩展性，使得生成质量可随着模型参数的扩大而提升。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 潜在空间分块 (Latent Patchification) DiT 并不直接操作像素， 而是操作 VAE （变分自编码器） 编码后的潜在特征。 给定一张输入图像 e “ × 彬 × 3， 首先通过预训练的 VAE 编码器将其映射到潜在空间， 得到潜在表示 z ]R 接着， DiT 将 z 切分为大小为 P × p 的空间块 (Patches)， 将这些块展平并经过线性投影， 转换为序列长度为 T = 伊加） × (w/p)， 维度为 d 的 Token 序列。 最后， 加上标准的可学习位置编码 (positional Embeddings)0 2 · 条件注入机制： adaLN-Zero Block 标准的扩散模型需要在每个去噪步骤中注入当前的时间步乯和条件类别 co U-Net 通常通过交叉注意力 ℃ ross-Attention) 或特征拼接来实现， 而 DiT 采用了自适应层归一化 (Adaptive Layer Normalization, adaLN)o 这是 DiT 架构中最关键的设计。 对于传入 DiT Block 的隐藏层状态九， adaLN 的操作如下： adaLN(), c) = 7(t,c)LayerNorm(h) + ß(t c) 其中， 缩放参数 7(t,c) 和平移参数 0@ c) 是通过对时间步嵌入和类别嵌入的加和应用机 (MLP) 回归直接生成的， 而不是作为可学习参数固定在网络中。 一个多层感知

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

此外， DiT 还引入了门控机制 (Dimension ScaIing)， 用于残差连接之前。 为了极大地稳定训练， 作者提出了 adaLN-zero 初始化策略： 即强制 MLP 在初始化时输出的 7@， c）、 囹@，。） 以及残差门控参数 a(t,c) 全部为零。 这意味着在训练初始阶段， 每一个 DiT BIock 都是一个严格的恒等映射 (ldentity Function)： 这保证了网络在初始状态下不会因为深层 Transformer 的高方差而崩溃。 3 · Unpatchification （去块化与预测） 在经过 N 层 DiT Block 后， Token 序列被送入最后一个标准的全连接层， 将维度扩张回 p × P × 2c （乘以 2 是因为模型需要同时预测噪声和方差）。 随后， 将一维序列重新排列 (Reshape) 回五 × × 2c 的空间网格结构， 输出给后续的损失函数计算或采样。

我们发现adaLN只能注入定长向量c，如[CLS]token。但显然一个token不够，故可以引入基于多模态双流注意力的MM-DiT：（扩散和自回归结合的模型，自回归上下文越来越长，也是运用了此方法）

先对图像流和文本流各自AdaLN（自适应层归一化）后各自计算Q、K、V，然后将图像的QKV和文本的QKV直接拼接（Concatenate）在一起，在这个拼接好的超长序列上，执行一次全局的自注意力机制，图像、文本可以内部信息融合，也可以让图像提取对应文本提示、文本根据图像更新上下文。

注意力计算完成后，将输出的超长序列重新切开，恢复成图像特征和文本特征。然后分别送入各自的残差连接和前馈神经网络（FFN），并应用AdaLN预测的门控系数。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

此外， DiT 还引入了门控机制 (Dimension ScaIing)， 用于残差连接之前。 为了极大地稳定训练， 作者提出了 adaLN-zero 初始化策略： 即强制 MLP 在初始化时输出的 7@， c）、 囹@，。） 以及残差门控参数 a(t,c) 全部为零。 这意味着在训练初始阶段， 每一个 DiT BIock 都是一个严格的恒等映射 (ldentity Function)： 这保证了网络在初始状态下不会因为深层 Transformer 的高方差而崩溃。 3 · Unpatchification （去块化与预测） 在经过 N 层 DiT Block 后， Token 序列被送入最后一个标准的全连接层， 将维度扩张回 p × P × 2c （乘以 2 是因为模型需要同时预测噪声和方差）。 随后， 将一维序列重新排列 (Reshape) 回五 × × 2c 的空间网格结构， 输出给后续的损失函数计算或采样。

4.注意力模块和自回归Transformer的不同点

对于扩散模型，在任何一个注意力层，所有的token必须同时相互计算注意力，最终也会同时输出所有token对应的向量。这就要求在显存中实打实地实例化一个N*N的注意力分数矩阵。而且如果扩散K步，就要对这个矩阵计算K次。对于视频生成模型，N=帧数*patch数，这也就是为什么很多视频生成模型采用“块内扩散、块间自回归”的方式。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

训练阶段工作流 1 ． 2 ． 3 ． 4 ． 5 ． 6 ． 7 ． 图像编码： 将原始图像输入冻结权重的 VAE 编码器， 提取潜在特征 zoo 随机加噪： 随机采样一个时间步 ～ （1， T) 和标准高斯噪声 6 ～ （0，。 根据前向公式计算得到加噪后的潜在变量荔。 块化处理 (Patchify)： 将切分为多个 Patch， 展平并加上位置编码， 形成输入序列。 条件嵌入： 将时间步和图像对应的类别标签 c 转化为条件向量 （通过 Embedding 层和 MLP)o DiT 前向传播 · · 将 Token 序列输入 N 层 DiT Block0 · 在每一层内部， 条件向量动态生成 adaLN 的缩放和平移参数， 对 Token 进行自适应归一特征还原 (Unpatchify): 将最后一层的输出序列恢复为与荔相同的空间维度形状。 损失计算与反向传播： 网络输出预测的噪声和协方差矩阵对角线元素。 计算 Lsimp1e + Lv1b， 并通过梯度下降更新 Transformer 的权重。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

推理 （生成） 阶段工作流 1 ． 2 ． 3 ． 噪声初始化： 从标准正态分布中采样一个纯噪声张量 ～ （0，， 并指定想要生成的类别标签 co 逐步去噪循环： 对于时间步 = T 一 1， ． a ． 将当前的荔切分为 Patch 序列。 ·， 1 执行以下操作： b. 将序列连同时间步 t 和类别 c 送入 DiT 网络。 c. 网络通过 adaLN-zero 机制处理上下文， 预测出当前步骤的噪声和方差习知 d. 使用预测结果， 根据 DDPM 或 DDIM 采样算法的数学公式， 计算出上一步的潜在变量荔一 1。 图像解码： 循环结束后得到去噪完毕的 zoo 将 zo 送入冻结权重的 VAE 解码器， 重建出最终的高清像素图像

## 二、训练目标

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 DiT 的训练中， 网络 O 实际上需要同时预测加入的噪声以及方差伽为了预测噪声， 使用简化的均方误差损失 (Simplified Loss)： S1mp 为了学习方差模型额外引入了完整的变分下界 (Variational Lowe r Bound, VLB) 损失。 总损失函数为两者的结合 （通常对 Lv1b 施加一个较小的权重以防止干扰主目标的优化）： 力 = Lsimp1e + Lv1b

## 三、方差的预测

### （一）为什么目标函数含预测方差项？

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在逆向去噪过程脚 （荔一 1） 中， 我们需要假设一个高斯分布： 在最初的 DDPM ()o et 2020） 中， 作者发现真正的后验分布方差存在两个理论上的极端边界 （上界和下界）： 1 ． 上界： 0 （假设真实图像完全是标准正态噪声）。 1 一老一 1 2 ． 下界： 0 一 0 （假设真实图像 zo 是完全已知且确定的）。 1 一 ät

具体解释：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

既然模型只能看到一张充满噪点的图， 那么从模型的视角来看， zo 就绝对不是一个确定的占而是一个充满无数种可能性的概率分布。 我们可以构建一个直观的思想实验： 极端情况 A 很大， 接近纟屯噪声）： 假设就像是一台完全没有信号的雪花屏电视。 模型看着这片雪花， 它能确定原本的画面是一只猫、 一只狗， 还是一辆车吗？ 完全不能。 因为有成千上万种不同的 zo 在加上大量噪声后， 都会坍缩成这同一片雪花。 此时， 模型对真实的 zo 极度不确定， 这导致它推断上一步图像的方差极大， 逼近理论上界。 极端情况 B 很小， 接近去噪尾声）： 假设是一张非常清晰的猫咪照片， 只是上面落了几粒灰尘 （微小噪声）。 模型看着这张图， 非常笃定这原本就是一只猫 （此时其他 zo 的可能性已经极小）。 此时， 模型对 zo 非常确定， 逆向去噪的方差极小， 逼近理论下界。

在我们训练的时候，我们知道真实图像就在训练集里，完全已知且确定，但模型不知道；模型在推理的时候，若对真实的z0是什么完全没有把握，则zt-1的方差取上界；若对真实的z0是什么完全确定，则zt-1的方差取下界。

上界为什么为βt：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在逆向过程中， 我们想要推导的是 q （一 1）。 根据贝叶斯公式， 我们可以将其展开为： q 圄一 1） q@ 1 q()t 一 1 囝 = q(zt) q （一 1） 是前向加噪过程， 这是完全已知的： · /V （； 荔一 1，。 关键点来了： 在不确定性最大的极端情况 （通常在很大， 接近扩散末期时）， 图像已经被破坏得面目全非。 此时如果我们假设完全不知道 zo 是什么， 那么对于荔一 1 边缘分布的最合理假设， 就是它已经退化成了一个标准正态分布 （先验分布）： q(zt-l) （0

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

q(zt-l 司 q 圄一 1） q@一 1 atZt—l q （一 1 (t) exp

配方后，根据正态分布的系数可确定方差。

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

初代 DDPM 认为， 只要总的扩散步数丆足够大 （例如 = 1000）， 每一步的变化极小， 此时和 0 几乎相等。 因此， 初代模型选择不预测方差， 而是直接将方差暴力固定为一个常数矩阵： （z） 一丆 （其中被硬编码为或） 致命痛点： 这种做法在 T = 1000 时效果很好， 但如果我们想加速采样， 比如只用 50 步 （大步长采样 ), 这两个方差边界就会产生巨大的分歧。 此时如果仍然使用固定的方差， 模型就会被强制拉入次优的概率分布， 导致生成的图像充满噪点或极其模糊。

### （二）如何预测方差？

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了解决大步长采样带来的方差不确定性， lmproved DDPM 提出 （并被 DiT 采用 ), 让神经网络自己根据当前的特征和时间步乯来动态预测最合适的方差。 1 · 方差的参数化 (Parameterization) 直接让模型输出绝对的方差数值是不稳定的。 因此， 模型被设计为输出一个插值系数向量 e [ 0， 1 严， 在对数空间内对理论的上下界进行线性插值： log， = @ log + （1 一 @ log 0： 这也是为什么在 D 江的最后一个全连接层丿特征维度会扩展到 2c。 其中 c 个通道用于预测噪声 e 另外 c 个通道正是用来预这个插值向量

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 梯度的阻断与损失函数 (Stop-Gradient and Loss) 为了训练这个动态方差， 我们必须引入完整的变分下界 (Variational Lowe r Bound, VLB) 损失：） 目御@一 1 囝由于 Lvlb 在计算时同时包含了均值脚和方差 E9, 直接优化它会导致均值预测的梯度变得极其不稳定， 反而破坏图像质量。 因此， DiT 在计算 Lvlb 时， 对均值预测施加了停止梯度 (Stop-Gradient) 操作。 这意味着 Lvlb 的反向传播梯度只用来更新方差的权重， 而完全不干扰噪声的学习。 最终的混合损失函数表示为： 力 = Lsimp1e + ÄLv1b （在 DiT 中， 通常设置 = 0 · 001 以确保方差学习不会干扰主体的噪声预测）。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
