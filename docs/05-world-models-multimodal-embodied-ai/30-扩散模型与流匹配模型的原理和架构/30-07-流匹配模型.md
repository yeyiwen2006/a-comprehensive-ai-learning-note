---
title: "30.7 流匹配模型"
source_docx: "第5部分 世界模型、多模态生成与具身智能/30.扩散模型与流匹配模型的原理和架构/30.7 流匹配模型.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 30.7 流匹配模型

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、流匹配的基本思路及与扩散模型的对比

流匹配（Flow Matching）和扩散模型都是生成模型，它们的目标都是将简单的分布（如高斯噪声，p_0）变换为复杂的数据分布（如图像或文本，p_1）。

扩散模型的思路是：像墨水在水中扩散一样，数据逐渐加噪变成噪声。生成过程则是逆转这个扩散过程，像“去噪”一样一步步还原数据。这通常被建模为随机微分方程（SDE），路径是离散、曲折的。

流匹配的思路是：希望从加噪后的分布直接得到加噪前的分布。直接定义一个向量场，这个场告诉粒子（数据点）在每一时刻t应该往哪个方向移动。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

起始状态： 在 = 0 时刻， 所有的样本都服从一个简单分布） （例如标准高斯噪声， 这就像一盘散沙）。 目标状态： 在 = 1 时刻， 样本汇聚成了我们想要生成的复杂数据分布） （例如精美的图片集合， 就像沙子堆成的城堡）。 速度场鬱@，： 在任意时刻乯 e [ 0， 1 ] 和高维空间的任意位置 £， 存在一个 " 风向标 “ 或者说 " 力场 "， 它指示了位于该位置的粒子在下一瞬间应该往哪个方向移动、 速度有多快。 常微分方程的作用： 粒子的运动轨迹随时间的变化率 （即速度）， 正是由该位置的速度场决定的。 这在数学上天然地被表达为一个常微分方程： 如果我们知道每个时刻和位置的风向 （即神经网络学到的速度场 ), 我们就必须通过积分 （求解这个 ODE) 来推导出这粒沙子最终会被吹到哪几。

我们希望让特定数据a从x_0（噪声位置，已知）以给定的速度分布（也就是“速度场”中对应位置对应时间的值）趋近x_1（有意义的位置，未知）。事实上，上述常微分方程可写成积分形式：x_1-x_0即v_t关于t从0到1积分的结果。

由于我们不关心加噪的过程经历了什么，因此我们不妨假设一个理想情况，即所有数据都沿着我们希望的速度和方向匀速直线运动。因此，我们的目标就是要学习这个速度场，使之接近我们假设的理想情况。如果我们把大量噪声点扔进这个场里，它们最终会汇聚成有意义的数据。

## 二、数学原理

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

流匹配的核心在于定义和学习这个 “ 速度场 " A. 概率路径 (Probability Path) 我们不需要像扩散模型那样模拟复杂的物理扩散过程。 我们可以直接定义一条从噪声到数据昀的路径。 最简单且最高效的方法是线性插值 (Linear lnterpolation)： 当乯 = 0 时， 一 0 + 昀， t [ 0， 1 ] （全是噪）。 当 = 1 时， = （全是数据）。 这条路径是直线的， 这对于快速生成非常重要。 B. 速度场 (velocity Field) 既然有了位置随时间的变化公式， 我们就可以算出粒子移动的速度 （对时间求导）： （t00， 1 eTt = 1 一 £ 0 这个就是条件流匹配 (Conditional Flow Matching) 的目标速度。 它告诉我们： 如果要在时间 = 1 到达昀， 当前的粒子应该以什么速度和方向移动。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

C. 训练目标 (FIow Matching Objective) 在训练时， 我们无法预知终点 （这正是我们想生成的）。 因此， 我们需要训练一个神经网络 （， 让它在只知道当前位置和时间乯的情况下， 去预测上述的目标速度。 损失函数非常简单， 就是回归 (Regression) 问题： 2 即： 让神经网络预测的 “ 流速度 " 尽可能接近真实的 " 流速度 ' ut Ct 0， 1 0

当然，实际上在图像生成等任务中，预测v时已知的信息包括当前位置x_t、时间t和条件c。

在给定终点的情况下，我们希望速度应为指向终点的常数。或许你会问，那为什么还需要用神经网络拟合呢？这是因为我们在推理的时候并不知道终点是什么，不同的扩散起点（也就对应不同“粒子”）、不同的附加条件都会对应不同的终点和不同的“理想速度”。但在巨大的高维向量场中，实际在执行特定任务时的速度（该位置的场的方向）往往不是直指向终点的匀速，流匹配模型就是尽量去在每个给定的点拟合我们想要的速度方向。

## 三、训练方法（以流匹配VLA模型为例）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

以目前最先进的最优传输流匹配 (Optimal Transport Flow Matching, 类似于丌 0 中的设计） 为例。 假设基础噪声分布为 ～ = （0，， 真实动作轨迹分布为昀 ～， 多模态条件为 c = Encoder(), 0。 模型构建了一条连接噪声与真实动作的直线概率路径： = （1 一 0 + 1。 动作生成器的目标是拟合真实的速度场 (velocity Field)， 其目标函数定义为： 2 CFM(O) = 1 一 0、 （0， 1）@0、 po @1、 内这里 （昀一） 是真实路径的恒定梯度。 相较于传统的 DDPM 扩散模型， 流匹配的 ODE 轨迹更加平滑且笔直， 能够在极少的推理步数 （甚至几步内） 生成高保真的连续动作信号。

在训练时，我们对于每一组x_0和x_1，都希望其连线上每个位置的速度尽可能满足从x_0指向x_1，且大小在数值上等于二者之差。当然事实上，由于空间中的速度场是连续的，因此只能尽量拟合上述需求。当多条连线同时汇聚于一个点时，该点速度近似等于各连线对应速度的平均值。

工作流：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 特征提取： VLM 接收当前相机观测和语言 l, 前向传播生成多模态条件向量 co 噪声初始化： 从标准正态分布中采样一段长度为丑的纯噪声轨迹 ～ （0，。 常微分方程求解 (ODE SoIving): 在时间步 e [ 0， 1 ] 之间， 动作生成器 （， t,c) 结合上下文 c 计算当前状态的梯度。 通过数值积分器 （如 Euler 法或 Heun 法） 逐步更新。 动作执行： 将 ODE 终点作为预测的动作块。 系统采用 * * 模型预测控制 (MPC/ Receding Horizon ControD * * 策略， 仅执行前个动作， 随后重新获取新的视觉观测， 进入下一轮闭环推理。

这里的时间步是连续的，但神经网络只能处理离散的t值输入。故往往采取采样的方法，如每隔0.02的时间间隔进行一次采样，用该时刻网络输出的值对时间间隔积分，让x_t“走一小步”。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 特征提取： VLM 接收当前相机观测和语言 l, 前向传播生成多模态条件向量 co 噪声初始化： 从标准正态分布中采样一段长度为丑的纯噪声轨迹 ～ （0，。 常微分方程求解 (ODE SoIving): 在时间步 e [ 0， 1 ] 之间， 动作生成器 （， t,c) 结合上下文 c 计算当前状态的梯度。 通过数值积分器 （如 Euler 法或 Heun 法） 逐步更新。 动作执行： 将 ODE 终点作为预测的动作块。 系统采用 * * 模型预测控制 (MPC/ Receding Horizon ControD * * 策略， 仅执行前个动作， 随后重新获取新的视觉观测， 进入下一轮闭环推理。

## 四、应用

1.生成高分辨率、高质量的图像，性能可与Stable Diffusion等扩散模型媲美甚至更优，且通常生成速度更快（因为轨迹更直，从而需要的采样步数更少） 。

2.用于生成连续的视频帧序列，处理高维度的时空数据。

3.用于科学计算与模拟，如模拟物理系统（如流体动力学）、分子构象生成（蛋白质 3D 结构预测）等，因为这些问题本质上就是对连续变量变化（高维空间中点的移动速度随时空的变化）的建模。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
