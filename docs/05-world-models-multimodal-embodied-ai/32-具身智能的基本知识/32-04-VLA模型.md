---
title: "32.4 VLA模型"
source_docx: "第5部分 世界模型、多模态生成与具身智能/32.具身智能的基本知识/32.4 VLA模型.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 32.4 VLA模型

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

VLA（Vision-Language-Action，视觉-语言-动作）模型正在引发一场范式革命，它打破了传统机器人“感知-规划-控制”的模块化割裂设计，将多模态大模型的语义推理能力直接映射为物理世界的底层执行动作。

## 一、核心思想

VLA模型的核心思想是“端到端”（End-to-End）的多模态深度融合。它将预训练的视觉-语言模型（VLM）作为“大脑”，把机器人的动作输出转化为一种特殊的“语言”，从而让大模型能够直接指挥物理硬件。

视觉（Vision）：通过视觉编码器（如ViT、SigLIP）将输入图像（如RGB摄像机画面流）切分为Patch，并转化为视觉Token，赋予机器人理解空间关系、物理属性的能力。

语言（Language）：接收人类的自然语言指令，结合预训练大模型积累的庞大互联网知识，提供逻辑推理与“常识”。

动作（Action）：VLA将动作词表化，将连续的机器人物理控制参数（如机械臂的关节扭矩、末端执行器的坐标）离散化，作为一种特殊的Token融入大语言模型的词表中。

## 二、自回归VLA范式

### （一）基本原理

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

假设在时间步智能体接收到视觉观测序列 0<t e 0 以及自然语言指令 I e 乙模型的目标是输出最优动作 e 儿在以 RT- 2 (Robotic Transformer 2） 和 OpenVLA 为代表的自回归 (Autoregressive) V LA 范式中， 连续动作会被离散化为一系列动作 Token (ZI,， ·，）。 例如， 一个 7 自由度 （7 一 DoF) 的机械臂动作可以表示为空间位移、 旋转和夹爪开合度。 模型的训练目标是最大化以观测和指令为条件的动作 Token 的对数似然 (Log-Likelihood)： 其中， 0 为神经网络的参数， D 为包含人类专家示范和互联网图文的混合数据集。 通过这种方式， 动作预测被完全等价转化为了类似于 ChatGPT 生成文本的 Next-token Prediction 任务。

### （二）工作流

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 多模态输入处理 (Multimodal lnput Processing): 步骤 1 · 1： 获取当前相枧的 RGB 图像仂， 通过视觉编码器 (Vision Encoder) 提取特征， 并经过 Projector （投影层） 对齐到语言模型的特征空间， 生成视觉 Token 序列。 步骤 1 · 2： 接收用户指令 I （如 " 把红色的苹果放到蓝色的碗里 “ ), 通过文本分词器生成语 Token 序列。 特征拼接与跨模态注意力计算 (Context Concatenation & Cross-modal Attention): 步骤 2 · 1： 将系统提示词、 视觉 Token 和语言 Token 按序列拼接： CSystem Prompt， lmage Tokens， Language Tokens]。 步骤 2 · 2： 送入 Transformer Decoder 骨干网络， 通过自注意力机制 (Self-Attention) 让指令语义与图像中的物理特征 （如苹果的位置、 碗的位置） 产生深度绑定 (Grounding)。 自回归动作生成 (Autoregressive Action Generation): 步骤 3 · 1： Transformer 结合上下文， 逐个预测当前步骤的动作 Token: 预测出代表 Ac 的 Token, 再将 Ac 作为已知条件预测 Ay, 依次类推， 直到输出夹爪动作 Tokeno

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

4 ． 反词表化与物理控制 (Detokenization and Execution): 步骤 4 · 1： 将生成的动作 Token 序列 (ZI, · ·，） 映射回连续的物理数值 （即解除离散化 ), 生成实际的控制指令。 步骤 4 · 2： 将发送纟合机器人的底层控制器 （如 PID 控制器） 驱动电机执行。

### （三）优缺点

自回归VLA直接复用LLM的架构，天然继承了互联网级数据的逻辑链条（Chain-of-Thought）和常识推理能力。泛化能力极强，但受限于自回归的生成速度，高频连续控制能力略显不足。

## 三、扩散/流匹配VLA范式

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这一范式的核心动机是消除 “ 动作词表化 " 带来的离散化误差。 它不再将动作强行转化为语言词表中的 Token, 而是使用生成式连续时间模型 (Diffusion Models 或 Flow Matching) 直接在连续空间中生成高维、 高频的动作轨迹 (Action Chunks)0 代表性模型： Physical lntelligence 的丌 0 (Pi-Zero)、 FO rc eV LA 等。

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

扩散 / 流匹配 V LA 通常采用 * * " VLM + 动作专家 (Action E × pert） “ * * 的解耦与重组架构： 多模态条件编码器： 继承预训练的视觉语言模型 （如 PaliGemma、 Sig 凵 P 结合 LLM 骨干）。 它负责将图像序列 0 和语言指令 I 处理为深度的上下文条件嵌入 ℃ ondition Embeddings)0 连续动作生成器 (Action Expert): 通常是一个 Diffusion Transformer （DiT) 或 U-Net0 它接收上述条件嵌入， 并通过交叉注意力机制 ℃ ross-Attention) 或拼接自注意力机制， 对纯噪声信号进行迭代去噪， 最终输出连续的物理动作块 = {， + 1，

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

以目前最先进的最优传输流匹配 (Optimal Transport Flow Matching, 类似于丌 0 中的设计） 为例。 假设基础噪声分布为 ～ = （0，， 真实动作轨迹分布为昀 ～， 多模态条件为 c = Encoder(), 0。 模型构建了一条连接噪声与真实动作的直线概率路径： = （1 一 0 + 1。 动作生成器的目标是拟合真实的速度场 (velocity Field)， 其目标函数定义为： 2 CFM(O) = 1 一 0、 （0， 1）@0、 po @1、 内这里 （昀一） 是真实路径的恒定梯度。 相较于传统的 DDPM 扩散模型， 流匹配的 ODE 轨迹更加平滑且笔直， 能够在极少的推理步数 （甚至几步内） 生成高保真的连续动作信号。

工作流：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 特征提取： VLM 接收当前相机观测和语言 l, 前向传播生成多模态条件向量 co 噪声初始化： 从标准正态分布中采样一段长度为丑的纯噪声轨迹 ～ （0，。 常微分方程求解 (ODE SoIving): 在时间步 e [ 0， 1 ] 之间， 动作生成器 （， t,c) 结合上下文 c 计算当前状态的梯度。 通过数值积分器 （如 Euler 法或 Heun 法） 逐步更新。 动作执行： 将 ODE 终点作为预测的动作块。 系统采用 * * 模型预测控制 (MPC/ Receding Horizon ControD * * 策略， 仅执行前个动作， 随后重新获取新的视觉观测， 进入下一轮闭环推理。

优缺点：动作输出不再是离散 Token，而是通过 Diffusion Model（扩散模型）或 Flow Matching 直接生成连续的动作轨迹，在处理需要极高精度和灵巧度（如折叠衣服、灵巧手操作）的任务时表现出显著优势，但在长程任务的因果关系保持上可能不如自回归。

## 四、统一表征与潜在动作范式

不同机器人的硬件形态之间（乃至与人类第一视角之间）存在差异。为了让VLA模型不被限制于特定的硬件中，而是可以利用海量无动作标签的视频进行扩展（Scaling），我们引入“抓取右上方的物体”等与具体硬件无关的统一潜在动作，而不是“右臂上抬30厘米”这样的具体机械操作。

在具身智能架构分类中，它介于分层和端到端之间，因为模型间传递的信息是可微的，梯度可以（在某些设计下）从底层直接传导回高层，或者在同一个世界模型的潜空间内完成对齐。

### （一）核心架构

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

多模态条件编码器： 继承预训练的视觉语言模型 （如 PaliGemmm Sig 凵 P 结合 LLM 骨干）。 它负责将图像序列 0 和语言指令 I 处理为深度的上下文条件嵌入 ℃ ondition Embeddings)0 · 连续动作生成器 (Action Expert): 通常是一个 Diffusion Transformer (DiT) 或 U-Net0 它接收上述条件嵌入， 并通过交叉注意力机制 ℃ ross-Attention) 或拼接自注意力机制， 对纯噪声信号进行迭代去噪， 最终输出连续的物理动作块 = {， + 1，

### （二）训练方法

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 · 训练阶段一： 潜在动作空间的无监督学习 (Latent Action Learning) 这一阶段不需要任何底层物理控制信号 （如关节扭矩 ), 模型仅通过观看海量的视频序列 （包括人类完成家务的视频、 各类不同形态机械臂的视频） 来学习。 数学推导与工作流： 模型通常引入一个基于 VQ-VAE （向量量化变分自纟扁码器） 的逆动力学模型 (lnverse Dynamics Model)。 1 ． 2 ． 3 ． 4 ． 输入与编码： 输入连续两帧视频图像 （仂， 仂 + 1）。 纟扁码器提取这两帧之间的视觉和物理状态变化 （如物体的空间位移、 形变、 交互 ), 生成连续的隐特征。 向量量化 (Quantization): 将连续隐特征映射到离散的密码本 ℃ odebook) 中， 提取出以任务为中心的 “ 潜在动作 " 荔。 代表了极其高级的运动语义 （例如 “ 向右前方伸展并闭合抓取 "), 它完全独立于具体的枧器人硬件。 解码与重建： 前向动力学模型接收当前帧仂和潜在动作， 尝试重建出下一帧图像 + 1。 损失函数： 通过重建损失和密码本正则化损失更新 VQ-VAE 网络： 一 ezt 目 2 + 0 IlEncoder(ot, 仂 + 1） 一 sg[ezt] 目 2 lot+l 一 + 1 目 + llsg[Encoder(0t, 仂 + 1 （其中丿 0 为密码本中的嵌氕向量， sg[•] 为停止梯度作。 丿

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 训练阶段二： 联合策略优化与具身对齐 (Joint Policy Optimization) 在获得强大的跨形态潜在动作空间后， 开始正式训练 VLA 大模型， 将语义与物理硬件绑定。 工作流： 1 ． 2 ． 3 ． 伪标签生成 (pseudo-labeling)： 利用阶段一训练好的 VQ-VAE 纟扁码器， 将带有语言指令的机器人视频数据集全部扫描一遍， 转化为 " 指令一图像一潜在动作 “ 的离散序列数据。 高层意图规划学习： 使用标准的大语言模型 Next-token Prediction 目标训练 VLM 主干网络。 给定图像和文本上下文， 使其输出正确的潜在动作的概率分布 （荔 》， 0。 这部分占用了绝大部分算力， 但因为它预测的是统一的， 所以能够无缝融合所有不同机器人的数据。 底层动作解码训练 (Grounding): 引入一个轻量级的形态感知解码器 (Embodiment-aware Decoder) 或联合扩散模块 （如 viLLA-X 中的联合扩散设计）。 · 将 VLM 输出的潜在动作与当前机器人的专属形态标识 (Embodiment Embedding, 例如 " 7 自由度机械臂 “ 或 “ 12 自由度灵巧手 " 特征） 进行拼接。 采用少量的 * * 包含真实底层动作标签 (Action-labeled) * * 的数据， 训练该解码器将翻译为真实的物理控制指令： = E [ 一 log P( |， 仂， embodiment—id)] CAction

其中，目前业界部分实践在高层运用自回归以适配成熟生态，底层运用扩散以降低延迟。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
