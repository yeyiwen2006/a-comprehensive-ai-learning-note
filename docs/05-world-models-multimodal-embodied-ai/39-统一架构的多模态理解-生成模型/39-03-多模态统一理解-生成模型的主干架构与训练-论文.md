---
title: "39.3 多模态统一理解-生成模型的主干架构与训练（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/39.统一架构的多模态理解-生成模型/39.3 多模态统一理解-生成模型的主干架构与训练（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 39.3 多模态统一理解-生成模型的主干架构与训练（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、主干网络的输出范式

1.文本自回归、图像扩散，但共享同一个Transformer（字节BAGEL、Meta Transfusion）

运用同一个Transformer，输出不同模态时，模型运用掩码转换，直接在两种状态中切换：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

文本输出： 延续大语言模型的 Next-Token-Prediction （自回归） 范式， 逐词输出文本。 视觉输出： 不再是自回归， 而是通过类似扩散模型 / 矫正流的去噪过程 (Diffusion Denoising), 在连续隐空间内生成分布， 最后由专门的图像 Decoder （如 VAE 解码器） 渲染成高保真像素图像。

输出文本时使用因果注意力，输出图像时对上文为因果注意力、图像内部为双向注意力。视觉输出中，token之间是并行的。模型预测流速度，然后以此算出该去噪步骤后的位置z。

2.纯自回归生成（LongCat-Next、DeepSeek Janus）

DeepSeek Janus和LongCat-Next采取纯粹的自回归生成和因果注意力。无论文本还是图像token，都被离散化为词表（不同模态有各自的词表）。在同一个Transformer中，每次都进行自回归生成：先不分模态，根据上下文预测出离散的Next token向量（Next token ID），然后再根据模态将生成的向量送进本模态的De-tokenizer，将token转化回语言、图片等形式。序列会通过特殊的控制token（如<image_start>, <image_end>, <audio_start>）来界定模态的上下文边界。

在LongCat-Next中，一个图像块由8个层级的token构成（运用残差向量量化，由整体到细节），这里每次预测一个图像块且只预测第1层token。在一张图的token序列都已经生成完毕时，先用一个自注意力层由第1层token并行预测第2-8层token（自注意力层输入为第1层token+7个可学习的嵌入向量），由双轨扩散De-tokenizer将其还原为连续像素图：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

双轨工作流 1 ． 2 ． 轨道一： 结构像素解码器 (Structural Pixel Decoder) · 操作： 接收离散视觉 Token Z, 通过高效的卷积或 Transformer 反卷积网络， 优先重构出图像的骨架、 物体边界和文本布局厶 “。 = 灬 (Z)O 公式： stT、 uct 轨道二： 扩散像素细化器 (Diffusion Pixel Refiner) · 操作： 将第一轨的输出 / 作为强条件约束， 利用轻量级扩散模型 (Diffusion Model) stT 甩进行迭代去噪， 向其中注入逼真的光影、 材质和超高频的纹理细节。 公式： 在反向去噪的第乯步， 细化器基于布局进行纹理预测： 一 1 = （， 厶 t “ 山 0 +， t)eo 最终输出高保真图像。

3.DiDA：先训练纯自回归模型，再针对图像生成用Diffusion Loss蒸馏微调（Emu3.5）

在预训练、SFT、RL时，均用统一的Decoder-only自回归训练方法进行Next token prediction：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

当模型基于当前的文本意图， 在序列中连续输出一整段 Visual Tokens, 并生成一个模态结束符后， 系统底层的 * * 视觉解码器 (VAE Decoder) 就月入。 它的工作是将这串离散的视觉代码重新 " 翻译 " 回连续的 RG B 像素网格， 从而在物理层面上渲染出中间图像状态 （例如一张画好了边界框或拼好图块的图片）。

以上训练结束后，再通过调整掩码策略，实现把图像生成从自回归改为少步骤扩散，以同时提高速度和效率。只需要加一轮自蒸馏就能让模型快速适应新策略：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 A （串行生成文本）： 逐个 Token 吐出文字。 步骤 B （并行生成图像）： 当系统识别到当前需要生成文本时， 引擎调用常规的自回归模式， 当遇到生成图像的指令时， FSM 调度器立即切换状态。 系统会一次性为整张图像分配足额的 " 噪声 Token" （例如 ℃ 24 × ℃ 24 图像对应的约 4000 个 Token) 步骤 C （离散去噪）： 模型利用 DiDA 赋予的双向注意力能力， 通过少量的几个离散去噪步骤 (Discrete Denoising Steps)， 并行地将这几千个噪声 Token 细化并还原成最终的图像 Token 步骤 D （解码器输出）： 最后， 视觉分词器的解码器 (vanilla 或 Diffusion-based) 将这些离散 Token 还原为人类可见的像素图像。

4.视觉优先的离散扩散（Muddit）

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

统一的离散扩散架构： 在同一个 Transformer 架构和同一种解码范式下处理图文双向生成。 强大的视觉先验： 与基于语言先验的常规做法不同， Muddit 采用 " 视觉优先 " 策略， 它的多模态扩散 Transformer (MM-DiT) 主干网络是使用 Meissonic （一个经过充分训练的高分辨率文本到图像离散扩散模型） 初始化的。 轻量级文本解码器： 在共享的 MM-DiT 主干之上， 仅增加一个极其轻量的线性头 (Linear Head) 作为文本解码器， 将共孚潜空间映射回文本 tokeno

相比于一般Diffusion的优点：

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

一般扩散生成模型 （及混合统一模型）： 绝大多数模型仅专注于单一模态 （如专做文生图）。 即便是一些试图统一图文的模型 （如 Transfusion 等 ), 也往往采用 " 胶水架构 "： 用自回归的 LLM 处理文本， 用连续扩散处理图像， 两者的表征空间和生成范式是分裂的。 Muddit （极致的真正统一）： 文本和图像全都被量化为了同等地位的离散 tokeno 因此， 无论是生成文本还是生成图像， Muddit 使用的是完全相同的数学公式、 同一个扩散 Transformer (MM-DiT) 主干网络以及同一套损失函数。 此外， 当前很多多模态大模型是以 " 语言先验 " 为主导 （从开源 LLM 开始微调）， 而 Muddit 则是以 * * " 视觉先验 “ * * 为主导， 其主干网络继承自 Meissonic （一个纯粹的离散文本到图像模型 ), 这賦予了它极强的空间结构感知力。

## 二、混合Transformer专家

在稠密模型下，视觉理解、生成等任务容易互相冲突，且通过IsoFLOP缩放定律分析发现，视觉更“渴望数据”，语言更“渴望参数”。因此，我们可利用MoE进行“分流”。

1.不预先定义、让各模态自然分流（LongCat-Next等）

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在联合训练时， 两种模态对资源的需求呈现出巨大的不对称性 · 通过 lsoFLOP 缩放定律分析发现， 在稠密 (Dense) 模型下， 语言模型的最佳参数规模服从 Nopt C0 ． 47， 而视觉模型为 Nopt C0 ． 37。 这意味着视觉比语言更 “ 渴望数据 " （Data · hungry)， 而语言更 “ 渴望参数 "(parameter-hungry)o 如果共孚同一套稠密参数， 注定无法同时满足两者的最佳扩展需求。 为了解决这个问题， 论文引入了混合专家架构 (Mixture-of-Experts, MoE)0 高颗粒度与稀疏性： 细粒度的专家划分 （如将专家维度缩小， 设置 G = 16） 对多模态尤为关键。 MoE 能够在不增加计算负担 (Active Compute 恒定） 的情况下大幅增加总参数量， 从而显著降低了文本困惑度并提升了图像生成质量。 自发模态特化： 路由器 (Router) 在没有人类先验干预的情况下， 自然而然地将绝大多数专家分配给了更需要参数的 “ 文本 "， 而视觉标记则与文本共享特定专家。 模型在浅层主要进行模态特化的独立处理， 在深层才进行多模态的融合抹平缩放鸿沟： 引入 MoE 后， 语言的缩放指数被拉高 （刀 C0 ． 59）， 向视觉的指数 （刀 C0 ． 64） 靠拢， 极大地缩小了两者在扩展时的需求鸿沟， 为构建万亿参数级的真实多模态基座模型铺平了道路。

2.硬性分流（字节BAGEL等）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · MoT （混合 Transformer 专家） 主干网络进入大模型主干后， 所有数据流不再像传统大模型那样挤在同一个全连接层里。 在 Transformer 的每一层中， 它巧妙地结合了 “ 共享 " 与 " 分流 “： 共享的自注意力层 (Shared SeIf-Attention)： 所有的 Token （无论是文本还是图像的两套 Token) 都在这一层进行全局的信息交互。 这就是多模态 " 统一世界观 " 的物理发生地。 QKT Z = Softmax 解耦的专家前馈层 (Decoupled Expert FFNs): 计算完全局注意力后， Token 会根据其自身模态的属性， 被硬性路由 (Routing) 到两个不同的专家网络中： 多模态理解专家 (Understanding Expert): 专门处理文本 Token 和理解型编码器传来的语义 Token, 主攻逻辑运算。 多模态生成专家 (Generation Expert)： 专门处理用于图像生成的隐变量 Token, 主攻空间与物理规律模拟。

## 三、训练方法与数据

### （一）预训练

Emu3.5：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

为了让模型学习到世界的时间连续性和因果关系， Emu3.5 的预训练极度依赖从互联网长视频中提取的 * * " 视觉一语言交错数据 " * * （总计 790 年的视频素材）。 其算法工作流分为两个阶段： 1 ． 2 ． 第一阶段： 大规模通用对齐数据组装： 将视频关键帧与语音识别 (ASR) 转录文本按时间戳对齐， 交错拼接成长度最高达 32， 768 的长序列。 优化目标： 模型在 ℃ 万亿 (IOT) 个 Token 上执行标准的交叉熵损失优化。 为了防止海量的视觉 Token 淹没文本损失， 视觉 Token 的损失权重被调整为 0 ． 5。 优化器使用 AdamW, 参数设定为 01 = 0 ． 9、 2 一 0 · 95、 6 = 1.0 × 10 一 8。 第二阶段： 高质量增强学习数据提纟屯： 继续在 3 万亿 (3T) 个高质量 Token 上训练， 引入了语义分割、 视觉描述 ℃ aptioning) 等丰富的多模态标注。 分辨率提升： 图像的动态分辨率上限从第一阶段的 512X512 提升至 ℃ 24X1024， 促使模型捕获更细粒度的视觉细节。

BAGEL：

VLM图文对、视频数据、网页数据等视频→采样约4帧→每两帧之间用VLM生成描述帧间变化的短文本→构成图像-文本-图像-文本的交错序列。这与Emu3.5直接在视频顺序帧上做NTP有本质区别。Emu3.5的视频数据保留了完整的时空连续性，而BAGEL的视频数据经过了重新结构化，变成了离散的多图交错格式，时序关系主要通过帧间文字描述来传递，而不是帧本身的密集序列。

LongCat-Next：

先训练全模态Encoder（Tokenizer）和全模态Decoder（De-tokenizer），然后在Latent Space用统一的离散Next token prediction损失训练主干网络（训练时，真实标签通过Encoder映射后，与预测在隐空间计算交叉熵；推理时上下文是主干网络自己直接预测产生的Token ID对应的Embedding）。尽管损失是统一的，但由于生成的模态是确定的且此时各模态Tokenizer、De-tokenizer均已经训练完毕，主干网络会被训练为在对应位置生成该模态抽象表征，以便下游解码为该模态内容。

需要注意的是，解耦训练的仅仅是“把输入变成离散token”的Tokenizer（准确说对于视觉只需要训练VQ-VAE，因为后续残差匹配步骤为硬编码）和“把离散tokern变成输出”的De-tokenizer。词表中每个token代表的向量为多少（Embedding层）则属于主模型训练的一部分，一个工程trick是为防止codebook collapse，使用率低的entry会不断被当前batch数据重置，确保词表的全部entry都被充分利用。

### （二）后训练

1.SFT：在高质量样本上精调，并统一各种多模态任务的指令跟随能力。

2.RL（以Emu3.5为例）

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

算法框架： 采用组相对策略优化 (Group Relative Policy Optimization, GRPO) 算法进行端到端的联合强化学习。 统一奖励系统： 构建了一个多维度的奖励函数。 例如， 文本渲染任务使用 OCR 准确率奖励， 图像生成使用美学评分和 C 凵 P 相似度奖励。 规范化处理： 为了平衡不同任务间异构的奖励分布， 所有奖励信号在反向传播前被统一归一化到 [ 1， 10 ] 的区间内， 防止模型在单一任务上出现 “ 奖励作弊 （Rewa rd Hacking)"

部分任务：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

视觉叙事 (Visual Narrative)： 能够根据上下文， 生成包含连续故事情节和高度一致角色的多图多文混合输出。 视觉引导 (Visual Guidance)： 能够理解多步过程 （如烹饪、 手工 ), 并生成图文并茂的逐步操作指南， 展示了极强的因果推理能力。 世界探索与具身操作 (Embodied Manipulation)： 在枧器人操作中， 能够将长视距任务分解为多个子任务。 给定公式： Subt =， 0 （其中为语言指令， 0 为观测序列 ), Emu3.5 能够准确预测物理规律的演变， 如机械臂抓取、 叠衣服时的形变等， 展现了作为 " 世界模型 " 的潜力。

### （三）DiDA适应（Emu特有）

首先需要注意：基于Next token prediction的自回归模型的输出步数不是一个高维向量，而是词表中各个logit的概率，无法用DMD等方法让学生模型学习教师的去噪轨迹。同时，教师模型是自回归的，学生模型是双向扩散的，也意味着无法使用自回归模型经典的蒸馏方法。

故这里我们只进行“数据自蒸馏”：让训练好的、能力登峰造极的纯AR版Emu3.5充当教师，批量生成海量的高质量图像。用自身生成的图像，作为DiDA适应的样本。这些生成的图像和对应的文本指令，构成了用于训练DiDA的“自蒸馏数据集”，教师模型的任务到此结束。接下来，系统随机采样加噪时间步，让学生模型进行一次去噪（类似扩散模型的训练），但用自回归模型的Cross Entropy Loss。

推理时去噪工作流则变为如下：

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

1 ． 2 ． 3 ． 4 ． 5 ． 第 0 步 （纟屯噪声起点）： 一次性初始化所有的视觉 Token 为 CMASK] （或者纯噪声 Token) 全局预测： 模型通过双向注意力， 一次性预测出这 4096 个位置的所有 Token 概率。 “ 末位淘汰 " 与 “ 择优录取 " （关键调度）： 模型并不会直接采用这 4096 个预测结果。 · 它会根据输出的概率分布计算置信度 (Confidence)0 按照预设的调度策略 (Schedule), 在这一步只 " 锁定 ℃ ommit 尸置信度最高的极小一部分 Token （比如 IO%), 这些 Token 在后续步骤中将变成 " 干净的上下文 “。 剩下的 90 ％ 依然保持 [MASK] 状态。 循环往复： 将这 ℃ ％ 的干净 Token 和 90 ％ 的噪声 Token 重新输入模型， 进行下一次预氵则， 再锁定下一批置信度最高的 Token （比如累积到 30 ％）。 结束： 经过 T 次这样的循环， 所有的 Token 都被逐步锁定， 最终拼凑出一张完整的高清图像

为什么后训练结束后再进行DiDA适应：

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

DiDA 的训练过程需要专门构建包含图文对和交错图文序列的 " 自蒸馏数据集 " 自蒸馏的逻辑前提是： 必须先有一个能力极强、 输出质量极高的 " 教师模型 " 来提供对齐标准和生成轨迹。 这个 " 教师模型 " 正是经历了完整 S FT 和 RL 阶段、 生成能力达到巅峰的纯自回归版 Emu3.50 如果跳过前面的步骤直接练 DiDA, 模型根本没有能力产出高质量的自身数据来指导并行解码的学习。 总结来说： 前三个阶段 （预训练、 SFT、 (L) 负责脚踏实地打造一个理解多模态世界的 “ 最强大脑 "； 而最后阶段的 DiDA, 相当于在模型出厂前， 单独为其视觉输出模块加装了一个并行加速引擎。 这种解耦设计是平衡模型极致能力与推理速度的最优解。

## 四、GRPO训练方法

### （一）UniGRPO

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

《 UniGRPO: Unified POIicy Optimization for Reasoning-Driven Visual Generation 》 是由香港中文大学和字节跳动 Seed 团队共同提出的一项研究。 该论文主要攻克的是统一多模态模型中的强化学习 (RL) 对齐难题。 研究团队采用了一种极简且高度可扩展的方法， 将 " 提示词 (Prompt) + 思考 (Thinking) -+ 图像 (lmage） " 这一推理驱动的视觉生成全过程， 严格构建为一个统一的马尔可夫决策过程 (MDP)0 在此基础上， 团队提出了 UniGRPO 框架， 利用稀疏的终端奖励， 通过组相对策略优化 (GRPO) 算法， 联合优化文本 （自回归， (R) 和图像 （流匹配， Flow Matching) 的生成策略。

1.多模态统一推理中的马尔可夫决策过程

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在强化学习视阈下， 论文将多模态交错生成无缝转化为一个序列化的 MDP M = 西，，， R)O 这里的每一个步骤要么对应文本阶段的单次 Token 预测， 要么对应图像阶段的单步去噪过程。 状态空间 (State Space) & 状态分为双阶段演进。 在文本阶段， 状态 5 = (),） 包含输入提示词 c 和所有已生成的推理 Token 巛。 步入图像阶段后， 状态 stkmg 一则拓展为包含提示词、 完整的推理文本序列当前带有噪声的图像潜变量以及当前的流时间标。 动作空间 (Action Space) 灵： 在文本阶段， 动作 aßt e V 是从大语言模型词表中采样出的 = Ctk-At e 则是模型预测的下一个流匹配步骤的去单个 Tokeno 在图像阶段， 动作 atkmg 噪潜变量。 状态转移 (Transition)： 给定动作后， 两个模态的转移都是确定性的。 文本部分将 aßt 附加到序列末尾， 图像部分将潜变量由推进至奖励机制 (Reward) R: 这是一个典型的稀疏奖励场景。 模型仅在图像潜变量被完全去噪为终端状态时， 才获得最终奖励月 00， c)o 中间所有的推理和去噪步骤奖励均设为零。

2.算法工作流

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了高效求解上述 MDP， UniGRPO 将用于大语言模型的 TextGRPO 和用于视觉模型的 FlowGRPO 进行了深度整合。 具体的算法工作流如下： 1 ． 2 ． 3 ． 4 ． 5 ． 策略初始化： 加载具备交错生成能力的统一多模态大模型基座 （本研究以经过监督微调 SFT 的 Bagel 模型为例 ), 将其记为初始策略。 文本推理采样： 针对给定的用户提示词 c, 模型调用自回归策略 （'）， 并行生成 G 个不同的推理思考链， 记作 { }i 一 1。 图像流匹配采样： 随后， 这 G 个推理链分别作为条件上下文， 引导模型通过混合 SDE-ODE 积分器策略砌 （" 《 stkmg)， 生成 G 条对应的图像去噪轨迹 { } 应计算终端视觉奖励： 针对最终生成的 G 张图像， 使用基于 lnternVL 微调的奖励模型进行评估， 衡量其视觉质量以及与用户提示词的一致性， 从而获得稀疏终端奖励。 计算组相对优势 (Advantages)： 不依赖于传统价值模型 (value Model)， 而是在生成的组内直接计算每个样本的相对优势翡。 数学公式为： 一 mean({RJ}q-1)

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

6 ． 计算文本与图像策略目标： · TextGRPO: 使用标准的 GRPO 目标函数计算文本损失、。 到@）， 内置重要性采样截断 (Clipping) 和 KL 散度惩罚。 · FlowGRPO: 为了在确定的常微分方程 (ODE) 中引入强化学习所需的探索性， 将采样转换为随机微分方程 (SDE)0 同时， 应用比例归一化 (RatioNorm) 技术对数重要性比值进行标准化， 确保截断机制不失效。 由此得出图像部分的目标函数叼 “）。

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

=。 “ + AUFlow 并通过梯度上升联合更新模型权重 O （实验中权重系数设定为 1， 以平衡文图优化）。

FlowGRPO的原理见扩散模型与流匹配模型的强化学习的相关内容。

3.CFG的移除

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

CFG (Classifier-Free Guidance) 是目前扩散模型和流匹配中最常用的推理技巧， 用于强制提升图像与提示词的契合度。 它的做法是： 在每一步去噪时， 让模型进行两次前向计算。 一次输入提示词 （有条件评估 ), 另一次不输入提示词 （无条件评估）。 然后， 算法放大这两次输出差异的向量， 强行把生成方向往提示词上 " 推 "。 为何要在训练中彻底摒弃它？ · 计算灾难： 虽然 CFG 在推理时很好用， 但在强化学习训练中却是个灾难。 如果在多条件生成 （如图像纟扁辑） 中， CFG 甚至需要每步进行三次以上的评估。 在交错生成的多轮交互中， 模型必须不断管理和分支多个上下文。 梯度迷宫： 在 RL 框架下， 这种翻倍的计算量不仅让显存和算力爆炸， 更致命的是， 它构建了一个带有大量 ' 分支 (branched)" 的复杂计算图， 让反向传播计算梯度变得极其困难和低效。 线性、 无分支的轨迹展开 UniGRPO 直接大刀阔斧地砍掉了训练中的 C FG 评估分支， 强制要求模型只做单次、 单向的有条件计算 （即线性、 无分支的展开）。 底气何在？ 论文作者认为， 既然强化学习的奖励函数本来就在评估 “ 图文对齐度 "， 那么通过最大化这个奖励， 模型就能主动把对齐能力直接 " 内化 " 到自身的神经网络权重中， 从而在根本上摆脱对 CFG 双路分支的依赖。

4.KL约束的调整

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

传统的潜空间 KL 散度陷阱为了防止模型在追求高奖励时 “ 忘掉 " 预训练的先验知识， 传统的做法是计算当前策略与参考模型之间的 KL 散度作为惩罚。 在上述的 SDE 公式中， 这个局部 KL 散度是可以被精确解析计算出来的。 时间步依赖的致命漏洞： 数学推导表明， 这个精确的 KL 散度本质上是预测速度的平方差， 但它必须要乘以一个权重： 噪声方差的倒数 1/@。 优化器的 “ 疯狂利用 "： 问题就出在这里。 在图像生成的极早期 （刚开始去噪时 ), 图像几乎全是噪声， 此时的方差极大。 这导致 1 /。 变得无限趋近于零。 聪明的 RL 优化器立刻发现了这个漏洞： 在早期阶段大幅度篡改生成轨迹， 由于权重极小， 几乎不会受到任何 KL 惩罚。 这就是所谓的 “ 法外之地 "， 它会直接导致模型产生严重的伪影和纹理崩坏 （Rewa rd hacking)。 UniGRPO 的破局之道： 无加权速度场 MSE 面对这个数学上完美但工程上存在漏洞的公式， 作者果断放弃了 KL 散度， 改为直接计算模型预测的 " 速度场 " （即向量场输出吻） 与参考模型之间的均方误差 (MSE)0 2

### （二）VLPO：输出连续隐变量的时用的GRPO变体

> [图片 21：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

标准的 GRPO 目标函数通过最大化以下公式来更新策略参数 0： 五月po@ 一 E 一乥乥 mm （，，） 街 t， clip(ri.t@）， 1 优势计算 (Advantage, Att)： 完全一致。 对于同一个问题， 模型生成一组 (Group) 回答。 根据这组回答的最终得分计算均值和标准差， 从而得到标准化的相对优势翡 t std PPO 截断 (Clipping)： 完全一致。 使用 1 一 6 和 1 + 限制单次策略更新的幅度， 防止模 KL 散度惩罚 ()L PenaIty)： 完全一致。 防止新策略偏离参考模型 (Reference Model) 太

> [图片 22：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

标准的 GRPO 目标函数通过最大化以下公式来更新策略参数 0： 五月po@ 一 E 一乥乥 mm （，，） 街 t， clip(ri.t@）， 1 优势计算 (Advantage, Att)： 完全一致。 对于同一个问题， 模型生成一组 (Group) 回答。 根据这组回答的最终得分计算均值和标准差， 从而得到标准化的相对优势翡 t std PPO 截断 (Clipping)： 完全一致。 使用 1 一 6 和 1 + 限制单次策略更新的幅度， 防止模 KL 散度惩罚 ()L PenaIty)： 完全一致。 防止新策略偏离参考模型 (Reference Model) 太

> [图片 23：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

唯一的差异： 概率比率 rit@） 的定义对于文本 Token （GRPO / 离散空间） 率相除： 因为文本有明确的词表分布， 所以比率直接是新旧概丌 0 t 丌 0 不 t 对于潜变量 （VLPO / 连续空间）： 潜变量是高维连续向量， 没有 softmax 概率。 Monet 的做氵去是假设潜变量服从高斯分布， 推导出一个等效的连续概率比率： 1 exp 2 伊 2 这里九@ 是 RoIIout 阶段收集的冻纟吉特征， 九 ‰ 是当前正在优化的网络生成的特征。 工作流总结： 在强化学习阶段， 如果某条包含潜变量的思维链算对了 (Ait > 0）， VLPO 会利用相同的 GRPO 公式框架， 通过最大化 t@）， 在欧氏空间中驱动九 ‰ 向那个带来好结果的五到显 old ． 亠 · 拢 （最小化两者之间的为 2 距离）。 这使得 MLLM 首次能够利用最终的 Rewa rd 信号来直接微调内部的连续视觉特征空间。

> [图片 24：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

唯一的差异： 概率比率 rit@） 的定义对于文本 Token （GRPO / 离散空间） 率相除： 因为文本有明确的词表分布， 所以比率直接是新旧概丌 0 t 丌 0 不 t 对于潜变量 （VLPO / 连续空间）： 潜变量是高维连续向量， 没有 softmax 概率。 Monet 的做氵去是假设潜变量服从高斯分布， 推导出一个等效的连续概率比率： 1 exp 2 伊 2 这里九@ 是 RoIIout 阶段收集的冻纟吉特征， 九 ‰ 是当前正在优化的网络生成的特征。 工作流总结： 在强化学习阶段， 如果某条包含潜变量的思维链算对了 (Ait > 0）， VLPO 会利用相同的 GRPO 公式框架， 通过最大化 t@）， 在欧氏空间中驱动九 ‰ 向那个带来好结果的五到显 old ． 亠 · 拢 （最小化两者之间的为 2 距离）。 这使得 MLLM 首次能够利用最终的 Rewa rd 信号来直接微调内部的连续视觉特征空间。

### （三）多模态统一离散化时的GRPO

LongCat-Next将所有模态统一用离散token序列处理，故均可运用GRPO。但不同模态的GRPO是分开的，视觉中8层各自的GRPO也是分开的（最后加权平均），互相不共用pi函数（因为意义不同）。

这里去掉了KL约束，但保留了GRPO中的截断约束。

多模态统一训练难度大，容易出现不稳定情况。现代大模型的RL一般先采样若干步，再按批次统一训练，若偶尔采样到一个低概率的噪声token，这条路径可能后面会因为正反馈而级联出乱码等问题，后续加入训练数据后就极易导致崩溃。为避免训练崩溃，引入两个Filter：

1.Entropy-based Filter：在RL中，少数token采样到极低概率的噪声token后，后续生成会级联出乱码，整条序列熵极高。计算同一个minibatch中所有序列的sequence-wise entropy的均值μH和标准σH，对于某条序列，若H_seq>μH+nσH，则认为该序列是outlier，直接丢弃，不参与梯度。

2.Training-Inference Difference Filter：检测rollout时的采样策略与当前训练策略之间的per-token概率差异，只要序列中任意一个token超过阈值δ，整条序列就被丢弃，防止污染训练。根本原因是硬件或数值精度不匹配导致rollout时混入了极低概率token，这些token在采样下概率很高，但在当前训练策略下概率极低。标准的importance sampling correction（如TIS、MIS）只能在token级做平均，无法处理这种极端的单token异常。

## 五、世界模型能力的涌现

> [图片 25：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

实验采用了导航世界模型 (Navigation World Model, NWM) 的设定， 创新性地将机器人的导航动作 （如平移、 旋转） 直接纟扁码为离散的自然语言文本 Token （如 dc = + L338 或 "go on the road")。 结果显示， 模型无需对架构做任何特殊修改， 只需输入上下文帧和文本指令， 就能准确预测出未来的视觉状态画面。 更重要的是， 这种物理规律的掌握主要得益于前期的海量无标注视频预训练， 真正在特定导航数据上的训练比例只需达到总数据的 1 ％ 即可让性能饱和。

也就是说，结合世界模型的多模态联合预训练激发的对语言和世界状态的基础认知是关键，在此之上，具体领域数据只需要少量就可以达到饱和性能。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 《 UniGRPO: Unified POIicy Optimization for Reasoning-Driven Visual Generation 》 是由香港中文大学和字节跳动 Seed 团队共同提出的一项研究。 该论文主要攻克的是统一多模态模型中的强化学习 (RL) 对齐难题。 研究团队采用了一种极简且高度可扩展的方法， 将 " 提示词 (Prompt) + 思考 (Thinking) -+ 图像 (lmage） " 这一推理驱动的视觉生成全过程， 严格构建为一个...
- 传统的潜空间 KL 散度陷阱为了防止模型在追求高奖励时 “ 忘掉 " 预训练的先验知识， 传统的做法是计算当前策略与参考模型之间的 KL 散度作为惩罚。 在上述的 SDE 公式中， 这个局部 KL 散度是可以被精确解析计算出来的。 时间步依赖的致命漏洞： 数学推导表明， 这个精确的 KL 散度本质上是预测速度的平方差， 但它必须要乘以一个权重： 噪声方差的倒数 1/@。 优化器的 “ 疯狂利用 "： 问题就出在这里。 在图像生成的极早期 （刚开始去噪时 ), 图像几乎全是噪声...
- 标准的 GRPO 目标函数通过最大化以下公式来更新策略参数 0： 五月po@ 一 E 一乥乥 mm （，，） 街 t， clip(ri.t@）， 1 优势计算 (Advantage, Att)： 完全一致。 对于同一个问题， 模型生成一组 (Group) 回答。 根据这组回答的最终得分计算均值和标准差， 从而得到标准化的相对优势翡 t std PPO 截断 (Clipping)： 完全一致。 使用 1 一 6 和 1 + 限制单次策略更新的幅度， 防止模 KL 散度惩罚 ()...

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
