---
title: "39.2 多模态统一理解-生成模型的输入编码（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/39.统一架构的多模态理解-生成模型/39.2 多模态统一理解-生成模型的输入编码（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 39.2 多模态统一理解-生成模型的输入编码（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、主要编码方案

对于图像等连续模态，如果像LLM一样直接离散编码，将出现问题：图像信息密度高，直接离散化会导致Codebook极其庞大；用于理解的整体语义信息和用于生成的局部信息容易出现冲突，难以取得平衡。为此，出现了一些主要的输入编码处理路线。

1.离散编码+理解生成合流编码（智源Emu、LongCat-Next、字节UniTok）

这种方案下，一般将图像转化为离散的token IDs，以适配目前通用的自回归模型架构。

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了让模型能像处理文本一样处理图像， 系统底层包含一个视觉分词器 （通常是基于 VQ-VAE 架构）。 它的作用是将连续的图像像素空间进行压缩、 量化， 并映射到一个离散的 " 视觉词表 " 中。 这样一来， 图像就被切分成了一系列离散的视觉标记 (Visual Tokens), 它们在格式和维度上与文本标记 (Text Tokens) 实现了平权。

LongCat-Next论文指出，通过增加训练数据，离散表征可以无限逼近连续表征的效果。运用离散表征的好处在于两方面：首先，可以使得图像等模态与LLM的Next token prediction完成统一；其次，与GRPO等强化学习方法天然兼容，不用像流匹配模型那样为了能进行RL而专门从ODE改为SDE。

但是核心难点在于，单一有限的离散Token词表表征容量太小。有一些解决方法：

（1）分块、分层Codebook（LongCat-Next）：模型不使用一本“大词典”，而是用8*K（K为一张图的分块数）本“分层词典”，将它们的结果组合起来。这就类似于计算机中，只要用14位0或1的二进制数组合起来，就可以表征16384种组合。

一张图像先分为若干块，然后对于每一个图像块，在经过VQ-VAE后，通过残差向量量化的方式切分token（注意残差向量量化这一步没有参数，而是硬编码取词表中匹配度最高者，词表的Embedding向量值是和主模型一起训练的）。

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

单一量化的困境： 图像的信息密度极高 （既有猫的轮廓这种语义信息， 也有毛发纹理这种高频细节）。 如果试图用一个庞大的密码本 （例如 ℃ 万个词） 来完全覆盖所有可能的图像块组合， 计算量将呈指数级爆炸， 且极难训练。 RVQ 的分层压缩思想： 为了解决这个问题， 模型不使用一本 “ 大字典 "， 而是使用 8 本较小的 “ 分层字典 " （比如每本大小为 16384）。 Leve 丨 1 (l = 1): 首先， 模型尝试用第一层密码本去匹配原始图像特征。 这第一层捕捉的是最核心、 最抽象的语义信息 （比如 “ 这是一只猫的朵 "）。 计算残差： 第一层的匹配必然不够精确， 会产生误差 （残差： 原始特征一第一层特征）。 Level 2 (l = 2）： 接着， 模型用第二层密码本去专门量化这个 " 残差 "。 第二层捕捉的是次一级的细节。 循环往复： 以此类推， 直到第 8 层 (I = 8）。 每一层都在修补上一层留下的微小误差， 捕捉越来越精细的高频纹理和边缘信息。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

单一量化的困境： 图像的信息密度极高 （既有猫的轮廓这种语义信息， 也有毛发纹理这种高频细节）。 如果试图用一个庞大的密码本 （例如 ℃ 万个词） 来完全覆盖所有可能的图像块组合， 计算量将呈指数级爆炸， 且极难训练。 RVQ 的分层压缩思想： 为了解决这个问题， 模型不使用一本 “ 大字典 "， 而是使用 8 本较小的 “ 分层字典 " （比如每本大小为 16384）。 Leve 丨 1 (l = 1): 首先， 模型尝试用第一层密码本去匹配原始图像特征。 这第一层捕捉的是最核心、 最抽象的语义信息 （比如 “ 这是一只猫的朵 "）。 计算残差： 第一层的匹配必然不够精确， 会产生误差 （残差： 原始特征一第一层特征）。 Level 2 (l = 2）： 接着， 模型用第二层密码本去专门量化这个 " 残差 "。 第二层捕捉的是次一级的细节。 循环往复： 以此类推， 直到第 8 层 (I = 8）。 每一层都在修补上一层留下的微小误差， 捕捉越来越精细的高频纹理和边缘信息。

这8个层级的向量都有各自的词表空间，分别记录从整体到局部的特征，它们通过加法融合，整体特征更多服务于理解，局部特征更多服务于生成，事实上巧妙地解决了冲突问题。

值得注意的是，消融实验发现多级embedding加和后若不重新编码，性能会下降。为此引入单层FFN，在Codebook lookup后对加和特征重新映射，防止多级信息叠加后语义坍缩。

（2）二进制Codebook（UniWeTok）：在传统的VQ-VAE或VQGAN中，Codebook是一个显式存在的可学习参数矩阵。编码器输出下一个向量时，需要和其中所有向量计算距离并取最小。图像信息丰富，需要Codebook容量极大，会导致距离计算成本极高。可以采用二值化方法，利用向量每一个维度的正负号构建Codebook，为正就量化为1，为负就量化为-1，构建了一个大小为2^d_model的Codebook。

2.离散编码+理解生成分开编码（DeepSeek Janus）

我们知道，理解和生成任务在同一个编码器中的梯度冲突很可能是因为需要的信息类型不同。因此，一个直观的方法是用不同的编码器分开编码：理解型编码器输出的编码更多服务于文本生成；生成型编码器输出的编码更多服务于视觉生成。

纯自回归的Janus在理解用CLIP/SigLIP，生成用VQ tokenizer。Janus在理解任务中只启用理解型编码器，反之亦然。

3.连续编码+理解生成分开编码（字节BAGEL、Meta Transfusion）

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

文本输入： 直接通过语言模型自带的 Embedding 层 （词表查找 ), 转化为文本 Token (Xtext 图像输入： 这张原始图片 （像素矩阵） 会被同时复制两份， 分别喂给两个视觉编码器： 喂给 ViT, 输出代表高级概念的语义 Token (XViT)0 喂给 VAE Encoder, 输出代表连续空间结构的隐变量 Token (XVAE）。

不同于Janus在理解任务中只启用理解型编码器（反之亦然），BAGEL为了更好地统一理解与生成任务，将三种Token被拼接为序列，一起输入一个统一的自注意力层。当模型准备开始生成图像时，会在自注意力层后面接一个[BOI]token，并在后面注入一定数量（如1024个）噪声token（训练时为目标图像加噪得到，测试时则直接采样白噪声），开始并行去噪、预测流速度。

若VAE编码结果的向量维度仍然大于Transformer的输入向量维度，可以用线性池化下采样或U-Net下采样方案。后者需要的参数量更大，但生成质量更高。

Janus和BAEGL论文的实验均证明，共用编码器时，加入生成任务后，模型理解指标明显下降，CE loss上升，说明共享表征时生成会挤占理解的语义容量，二者存在强烈冲突；解耦架构实验（双专家+双编码器）中，生成任务不再破坏理解性能，同时理解特征能为生成提供语义约束，让图像更贴合文本、细节更准确，实现理解与生成双向协同。

4.连续编码+理解生成合流编码（RAE）

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在过去的统一多模态模型中， " 看 " 和 " 画 " 实际上使用的是两套完全不同的特征语言视觉理解 （看）： 大语言模型 (LLM) 为了看懂图片， 通常会使用 C 凵 P 或 Sig 凵 P 这样的表征纟扁码器。 这些纟扁码器提取出来的是高维的、 富含语义的特征 （例如 1152 维）。 这就像是 LLM 的 " 视觉母语 "。 + 2 视觉生成 （画）： 传统的扩散模型为了节省算力， 使用 VAE 将像素极度压缩成低维的隐特征 （例如几十维 ), 在这个低维空间里进行去噪生成。 + 1 分裂的痛点： 因为扩散模型生成的是 VAE 低维特征， 而 LLM 只认识 C 凵 P/Sig 凵 P 的高维语义特征。 如果扩散模型刚画完一张草稿 (VAE 隐特征 ), LLM 是完全看不懂的。 传统工作流： 如果 LLM 想检查扩散模型画得对不对， 必须经过极其繁琐的步骤： 扩散模型生成 VA E 特征一 > VAE 解码器将其还原为像素图片一 > CLIP 编码器重新将像素图片编码为高维特征一 > LLM 进行评估。 这就导致模型仿佛有两个不互通的大脑。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在过去的统一多模态模型中， " 看 " 和 " 画 " 实际上使用的是两套完全不同的特征语言视觉理解 （看）： 大语言模型 (LLM) 为了看懂图片， 通常会使用 C 凵 P 或 Sig 凵 P 这样的表征纟扁码器。 这些纟扁码器提取出来的是高维的、 富含语义的特征 （例如 1152 维）。 这就像是 LLM 的 " 视觉母语 "。 + 2 视觉生成 （画）： 传统的扩散模型为了节省算力， 使用 VAE 将像素极度压缩成低维的隐特征 （例如几十维 ), 在这个低维空间里进行去噪生成。 + 1 分裂的痛点： 因为扩散模型生成的是 VAE 低维特征， 而 LLM 只认识 C 凵 P/Sig 凵 P 的高维语义特征。 如果扩散模型刚画完一张草稿 (VAE 隐特征 ), LLM 是完全看不懂的。 传统工作流： 如果 LLM 想检查扩散模型画得对不对， 必须经过极其繁琐的步骤： 扩散模型生成 VA E 特征一 > VAE 解码器将其还原为像素图片一 > CLIP 编码器重新将像素图片编码为高维特征一 > LLM 进行评估。 这就导致模型仿佛有两个不互通的大脑。

此外，过去的理解-生成统一模型常为了迁就生成任务的算力而使用低维压缩表征，无法同时满足理解所需的语义容量与生成所需的细节精度，正是引起两种任务发生冲突的重要原因。

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

RAE 的核心思路非常直接： 既然 LLM 习惯看 SigLlP 的高维语义特征， 那我们为什么不逼着扩散模型直接去生成 Sig 凵 P 的高维语义特征呢？ 所以， 在 RA E 的架构下， 它并没有取消编码， 而是彻底抛弃了 VAE 编码器， 统一只使用 Sig 凵 P-2 作为唯一的编码器。 统一的生成空间： 扩散模型不再预测 VAE 的低维特征， 而是直接在 16 × 16 × 1152 维的 Sig 凵 P-2 语义特征空间里进行去噪生成。 无缝的模态互通： 因为扩散模型生成的直接就是 Sig 凵 P-2 特征， 这恰好是 LLM 的 " 母语 "。 这意味着， 扩散模型刚在隐空间里把草稿打好， LLM 不需要经过任何像素解码和重新编码， 直接就能读取这些特征序列， 立刻判断出这张图画得符不符合要求。 RA E 解码器的作用： 只有当所有的生成和校验步骤都在这个高级语义空间里彻底完成， 系统最终需要把这张图展示到你的屏幕上时， 才会调用 RA E 解码器， 将这些特征一次性渲染翻译成真实的像素图片。 总结来说： RA E 不是回到了低级的像素空间， 而是把 " 生成 " 这件事， 强行拉升到了和 " 理解 " 同一个级别的高维语义空间。

不同于VAE编码器、解码器参数量相等，RAE采取重编码器、轻解码器的方法：

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

重编码器 (Heavy Encoder)： 直接 “ 白嫖 " 开源的、 参数量极其庞大的视觉基础模型 (Foundation Model, 例如数十亿参数的 Sig 凵 P-2、 DlNOv2 或 MAE)0 它的任务是深思熟虑地 " 看透 " 真实图像， 提取出极其高维、 富含语义的特征表示。 轻解码器 (Light Decoder)： 仅仅是一个层数很少、 参数量极小的浅层 ViT (Vision Transformer)。 因为重编码器提取的特征已经包含了完美的信息， 解码器不需要再进行复杂的语义推理， 它只负责做一个简单的 " 翻译工 ' 一一将高维特征矩阵 “ 降维打击 " 成人类肉眼可见的 RGB 像素矩阵。

由于在高维空间内扩散生成较费算力，轻解码器恰恰减轻了算力负担，起到了平衡作用。（生成图像时不需要编码器）

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

RAE 的核心思路非常直接： 既然 LLM 习惯看 SigLlP 的高维语义特征， 那我们为什么不逼着扩散模型直接去生成 Sig 凵 P 的高维语义特征呢？ 所以， 在 RA E 的架构下， 它并没有取消编码， 而是彻底抛弃了 VAE 编码器， 统一只使用 Sig 凵 P-2 作为唯一的编码器。 统一的生成空间： 扩散模型不再预测 VAE 的低维特征， 而是直接在 16 × 16 × 1152 维的 Sig 凵 P-2 语义特征空间里进行去噪生成。 无缝的模态互通： 因为扩散模型生成的直接就是 Sig 凵 P-2 特征， 这恰好是 LLM 的 " 母语 "。 这意味着， 扩散模型刚在隐空间里把草稿打好， LLM 不需要经过任何像素解码和重新编码， 直接就能读取这些特征序列， 立刻判断出这张图画得符不符合要求。 RA E 解码器的作用： 只有当所有的生成和校验步骤都在这个高级语义空间里彻底完成， 系统最终需要把这张图展示到你的屏幕上时， 才会调用 RA E 解码器， 将这些特征一次性渲染翻译成真实的像素图片。 总结来说： RA E 不是回到了低级的像素空间， 而是把 " 生成 " 这件事， 强行拉升到了和 " 理解 " 同一个级别的高维语义空间。

随规模扩展，加宽扩散头、噪声增强解码等机制几乎不再带来收益。但部分机制必须保留：

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

最初为 lmageNet 设计的 RA E 框架包含许多复杂的修改。 当扩展到数十亿参数的 T21 任务时， 论文对其进行了压力测试： 必须保留的核心机制： 维度感知噪声调度 (Dimension-aware noise scheduling)o 因为高维隐空间与低维空间特性不同， 必须根据有效数据维度 m (Token 数乘以特征维度） 对时间步进行偏移缩放。 去掉该机制会导致模型性能灾难性下降。 时间步偏移公式为： atn 1 + (a 一 1） 标其中 “ 为基准维度， a = 不再需要的复杂机制： 宽扩散头 （刀 T 刀 “）： 在小模型 （0 ． 5B 参数） 中， 加宽的扩散头能突破网络宽度瓶颈； 但在大模型 （2 ． 4B 以上） 中， 骨干网络自带的隐藏维度已经大于隐空间维度， 该机制的收益基本饱和。 噪声增强解码 (Noise-augmented decoding)： 通过给输入添加噪声来训练解码器， 仅在扩散模型训练的极早期阶段起到了正则化作用， 在模型收敛后提升微乎其微。

此外，RAE比VAE更能有效避免过拟合：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在现代 T21 模型的 " 大规模预训练 + 高质量小数据微调 “ 范式中， 过拟合是一个致命问题。 VAE 的脆弱： 实验观察到， 基于 VAE 的模型在高质量数据集上微调仅仅 64 个 Epoch 后， 就会遭遇灾难性的过拟合。 它的训练损失会迅速暴跌， 模型开始死记硬背训练样本， 丧失泛化力。 + 4 RA E 的稳健： 相反， RA E 模型展现出了惊人的稳定性。 即使微调拉长到 256 个 Epoch 甚至 512 个 Epoch, 它的性能依然保持坚挺。 论文推测， RA E 的高维且高度语义化的隐空间起到了 " 隐式正则化 " 的作用， 强迫模型学习真正的特征分布， 而不是死记硬背像素细节。 + 3

后续研究：Beyond Language Modeling表明，高维RAE表征可同时提升生成与理解性能，传统范式中“LLM主导、视觉从属”嫁接架构是导致二者难以协同的核心原因，因为该范式会迫使视觉表征向文本对齐，进而牺牲理解细粒度或生成精准度，加剧模态冲突。该工作的核心实验围绕生成与理解的双向促进关系展开，采用统一Transformer架构搭配RAE表征，在纯文本、纯视频、图像-文本对、动作视频的混合数据上从零预训练。实验及结论：

（1）双向增益实验：固定视觉token预算，增加文本token可持续提升生成质量；固定文本token预算，增加视觉token不损害语言建模效果，证明视觉与语言任务无负向冲突；

（2）泛化增益实验：20B VQA数据搭配80B通用多模态数据（视频/图像-文本/文本），VQA效果超越100B纯VQA数据训练结果，证实生成相关多模态数据可反哺视觉理解能力；

（3）范式对比实验：原生多模态联合预训练显著优于LLM嫁接视觉的范式，在理解与生成任务上全面领先，证明平等模态架构是释放双向增益的关键。

## 二、Embedding训练方法

需要说明的是，下面不同的损失常配合使用。

1.对比学习：主流视觉编码器（SigLIP、CLIP等）采用的方法，拉近图像全局特征与文本特征的距离。

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

主流的视觉纟扁码器 （如 Sig 凵 P 或 C 凵 P) 通常采用对比学习 ℃ ontrastive Learning)' 旨在拉近图像全局特征与文本特征的距离： exp(s(1,T)/T) 一 log c0月t7、 astit， e exp(s(l, 00） 这种方式虽然能实现高层语义对齐， 但会严重 " 坍缩 " 细粒度的空间和纹理信息， 导致其无法用于高质量的图像生成。 SAE 彻底摒弃了纯对比范式。 它通过引入海量的视觉 ． 语言密集监督 （包括细粒度图像描述、 视觉问答、 视觉推理等任务 ), 迫使纟扁码器在学习过程中既保留高信息密度的语义属性， 又保留用于生成的物理细节。

这种方式能实现高层语义对齐，但会丢失细节，无法用于生成。

2.重建损失：为了避免丢失细节信息，可用类似于VAE Loss，含MSE、感知损失（用预训练神经网络提取特征后计算相似度）和GAN损失。中科院、MEGVII和面壁智能的ROSS通过实验指出，重建损失能提升理解能力，文生图则不一定，因为前者梯度精细、稳定。

3.原生多模态预训练：如果从零进行主模型的预训练，可以在训练中引入各种视觉整体、细节理解和生成任务，让编码器自主学习到既保留高层语义，又保留底层细节的编码方式。

另外，对于不同分辨率的图像，可以用自适应分辨率方法，或先在固定分辨率预训练，后续微调时让模型适应不同分辨率。人脸数据和文字图像等可精调。

## 三、离散编码方案中的特殊训练方法

### （一）利用连续编码器训练离散编码器

事实上，离散编码器本身和连续编码器的区别仅仅就是在后面多了一个Codebook取近邻。故可以利用连续编码器，训练得到语义完备的连续编码器。具体方式包括：

1.蒸馏连续编码器：让离散编码器蒸馏连续编码器的输出。通过余弦相似度损失，让每一层激活值接近连续编码器对应层激活值。

对于离散Codebook，UniWeTok提出应同时在量化操作前和量化操作后蒸馏。量化后蒸馏是防止量化导致蒸馏到的信息丢失；量化前蒸馏是因为后面会提到我们强行让损失对码本离散向量的梯度变成了损失对编码器得到的连续向量的梯度，但实际上这两个梯度是不等的，只靠量化后蒸馏会导致回传的梯度不准。

2.以连续编码器作为初始权重微调。

### （二）离散化方案的其他额外损失函数

1.Commitment Loss

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

国 G 一 sg [ Q] 目 2 （承诺损失 / Commitment Loss) 含义： 是编码器输出的连续潜变量， Q 是离散化后的量化特征 （来自密码本） [ · ] 代表停止梯度 (stop-gradient) 操作。 + 2 作用： 由于在量化过程中存在信息丢失， 这个损失强迫编码器输出的连续特征主动去 " 承诺 " 或 " 靠近 “ 选定的离散特征 QO 这防止了连续特征在潜空间中随意游走， 确保量化过程的稳定性。

2.Entropy Loss

这个损失分为两部分：Token Entropy Loss要求模型在面对单个token时必须“极其果断”，不确定性越小越好；Codebook Entropy Loss损失要求模型在面对全局数据时必须“雨露均沾”，不确定性适当大，以充分利用Codebook，防止模型只利用少数token。

3.Generative-Aware Prior（GAP）

可用一个轻量级的模型作为“代理生成器”，在tokenizer训练时就施加next-token对应图像生成的监督，迫使token的分布本身对自回归生成友好。

### （三）Codebook的训练问题与STE

Codebook中的向量用随机或聚类初始化。但在训练时，往往会碰到问题：

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在 VQ 架构中， 编码器输出连续向量， 量化器通过查找码本输出离散的码本向量 zq （即 ek), 最后解码器将 zq 还原为图像。 这里的量化操作 （找最近邻） 在数学上表达为： = arg min llze 梯度的灾难在于 arg min 函数的数学性质： 它是一个分段的阶跃函数 (Ste p Function)0 如果你稍微改变一点点编码器的输出： 1 ． 大多数情况下， 它离当前的依然是最近的， 选中的索引不变， zq 也不变。 此时导数为 0。 2 ． 极少数情况下， 恰好跨过边界， 索引左发生突变。 此时导数为无穷大 （）。

因此训练时会运用直通估计器（STE），对于编码器网络的连续输出z_e，前向传播时离散编码器最终输出z_q会被赋值为Codebook里的e_k，反向传播时则让损失对编码器最终输出z_q的梯度直接被替换为对连续输出z_e的梯度。公式为：

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 15 OCR 未识别出有效文字：未识别出有效文字。]

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

结果： 解码器传回来的梯度萨， 不仅没有变成 0， 反而以 1： 1 的比例， 亳发无损地 “ 穿透 " 了量化层， 直接交给了编码器。

但这样做的后果是，L对码本向量e_k的梯度变成了0，需要引入其他机制更新码本。一般有以下两种方法：

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

机制 A： 码本损失 (Codebook Loss) _ 一基于梯度的经典做法在总损失函数中， 人为强制增加一项额外的惩罚项。 对于每次前向传播中被选中的码本向量和对应的编码器连续特征， 计算它们之间的均方误差 (MSE)： 2 = llsg[ze] 一 cod 00 原理解释： 这里的 sg[•] 代表 Stop-Gradient （停止梯度）。 这意味着在这个损失项中， 我们把编码器的输出 ‰ 当作一个 " 固定的目标锚点 "。 更新过程： 优化器在计算这个损失的梯度时， 只会去更新码本向量， 迫使它在连续空间中一步步走向 " 编码器的输出。 （： 同时为了防止编码器跑丿还会有一个对应的 Commitment L oss 忙。 器驸出向码本拢， 二者双向奔赴。 丿一腭绢码

> [图片 18：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

机制 B： 指数移动平均 (EMA, Exponential Moving Average) _ _ 更稳定的现代做法在实际训练大规模 VQ-VAE 或 VQ-GAN （包括 UniTok 的底层） 时， 使用梯度下降更新码本往往非常不稳定。 因此， 现代架构更倾向于完全放弃使用优化器更新码本， 而是采用 EMA0 · 更新过程 · 1 ． 在每个 Batch 中， 记录下有哪些连续特征 ze 被分配给了当前的码本向量。 2 ． 计算这个 Batch 中分配给的所有 ‰ 的均值 （假设记为。 3 ． 直接用移动平均公式强行更新的权重： （中 7 是衰减系数， 如 0 ． 99 龙优势： EMA 更新不需要计算梯度， 它就像是在动态地执行 " 在线 K-Means", 使得码本向量能非常平滑、 稳定地追踪编码器特征分布的变化。

> [图片 19：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

机制 C： UniTok 的特殊更新 （语义蒸馏注入） 对于 UniTok 而言， 除了上述的基础重构更新 （让码本贴近图像特征 ), 它的码本还承受着语义对齐的任务。 在 UniTok 的对比学习目标 （如 lmage-Text Contrastive Loss) 中， 被激活的离散 Token 对应的码本向量， 会与对应的文本连续向量计算余弦相似度。 这会产生额外的梯度， 强迫码本向量不仅要像原图的 Patch， 还要在连续高维空间中移动到与文本描述对齐的位置。

此外，为保证Codebook被充分利用，其中使用频率过低的token会被替换掉：

> [图片 20：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

系统会定期扫描码本， 如果发现某个向量的使用频率低于阈值， 就会直接把它抛弃， 然后从当前 Batch 的编码器特征 ze 中随机抽一个活跃的向量， 强行覆盖这个死掉的你， 让它 " 起死回生 "， 重新参与到后续的 EMA 或梯度更新中去。

### （四）涉及二进制Codebook的训练中的SigLu技巧：以UniWeTok为例

在用Binary codebook做量化时，存在两个损失的优化冲突：Token Entropy Loss倾向于把Encoder输出推向±∞，Commitment Loss希望Encoder输出贴近Codebook Entry（即±1）。用SigLu(x) = (1 - e^x) / (1 + e^x)把Encoder输出约束在 (-1, 1) 区间内。在这个约束下，两个损失在数学上等价。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
