---
title: "39.4 将视觉融入CoT的推理（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/39.统一架构的多模态理解-生成模型/39.4 将视觉融入CoT的推理（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 39.4 将视觉融入CoT的推理（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

当前的LLM主要依赖文本链式思考（CoT）进行推理，图像等模态仅仅作为文本的辅助输入，在数学和编程等抽象领域表现优异，但在需要丰富空间和物理直觉的物理世界任务中仍落后于人类。在涉及物理世界的推理任务中，视觉生成作为世界模型，能够提供比LLM更丰富的信息量和更强大的先验知识。同理，依赖复杂推理的的图像、视频生成等任务中，也需要基于LLM进行符号化、文本化的思考。人类的思考中往往同时包含了语言符号推理、空间想象，文本推理和基于图像等的世界模型在思维链中应当是互补的而非同构/冗余的。

因此，可应用原生多模态输出的模型进行微调（或重新预训练），在语言CoT与世界模型的空间推理结合，推进推理过程。下面介绍一些相关工作，如清华、字节的《Visual Generation Unlocks Human-Like Reasoning through Multimodal World Models》，智源Emu3.5等。

## 一、基本假设

1.多可观测马尔可夫决策过程

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

论文将任务中的 " 世界 " 严谨地建模为一个多可观测马尔可夫决策过程 (MOMDP), 定义为 M = 状态与观测： 底层的真实状态 e 5 通常是隐藏的， 但可以通过不同的观测视角 (Views) 来感知， 即 0 = （5） e。 动作与转移： 当采取动作 a e 灵后， 世界状态根据转移概率 5 / ～ 的 5， a) 发生演进， 并产生新的观测。

2.世界模型的两大原子能力

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

为了在 AI 中实现类似人类的心智模拟， 世界模型需要具备两种核心能力： 世界重建 (world Reconstruction)： 从部分观测中推断出完整的环境结构并合成新视角。 在生成模型中， 这体现为端到端的新视角生成： (0+n+l@师 + 2 世界模拟 (world Simulation)： 对动态过程进行建模以预测未来的观测结果。 这体现为基于当前状态和动作预测未来观测： （+ 1 ast)。 + 1

## 二、算法工作流

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 输入初始化： 接收问题 Q 和输入图像厶将图像设为推理的初始观测状态 00 交替推理与生成 （循环 H 步）： 对于推理轨迹 = （rl， 01）， 2， 02）， · ·， （，） 中的每逻辑推理： 基于累积的上下文信息生成逻辑推理步骤 0 （通常为文本 ), 这一步隐含了采取动作 a 或进行视角转换。 世界建模 （观测生成）： 调用世界模型生成新的观测。 这里有三种模式： 视觉世界建模： 为生成的图像。 文本世界建模： 为文本描述 （如坐标或符号矩阵）。 隐世界建模： 不生成显式观测 = Ø), 完全依赖模型的隐式内部表征。 输出结果： 根据生成的推理轨迹， 输出最终答案。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

关于链式思考 ℃ oT） 中循环 H 步的控制， 论文在 " 理论推导 “ 和 “ 实际模型运行 " 中采用了两种不同的处理方式： 1 ． 理论分析中的固定长度： 在论文附录的严谨数学推导部分， 为了方便建立概率模型和计算互信息， 作者将 H 定义为固定的链式思考 ℃ oT） 长度 ("where H denotes the (fixed) CoT length")。 这是一种数学上的理想化抽象。 实际运行中的自回归控制： 在模型真正的训练和推理过程中， H 并不是写死的常数。 统一多 2 ． 模态模型 (UMM) 是通过自回归 (Autoregressive) 的方式逐步生成内容的。 模型在每一步会基于当前的上下文， 自行判断是继续生成下一个推理步骤、 生成世界状态观测， 还是结束思考并输出最终答案。 例如， 在论文展示的数据示例中， 模型在完成所有逻辑推演后， 会生成 </think> 标签来主动终止循环， 并输出最终的简短答案。

r_i是文本思维链，可以认为是大脑中的逻辑思考（我们该怎么做，为什么这么做）；而o_i则是根据前文o_0到o_i-1（表示以前的状态）以及文本思维链（表示动作）生成的Next State Prediction，可以认为是在大脑中的想象、在草稿纸上画的图或结构化表格（当前事物变成了什么样）。具体如下：

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

0 (Logical Reasoning Step / 逻辑推理步）： · 定义： 代表基于当前累积上下文进行的逻辑演绎、 动作规划或解释说明。 形式： 通常是自然语言的描述， 侧重于 * * " 动作 " 或 " 原因 " * *。 示例： 在折纸任务中， 0 会是这样的句子： " 接下来， 我们撤销第一次折叠。 这是一个水平折叠 “ ． “； 在迷宫任务中， 它是： “ 我将向右进发， 直到撞到墙壁 “ ． (Observation / 观测状态）： · 定义： 代表在当前步骤下， 对世界底层状态的显式观测结果。 形式： 在文本世界建模中， 为了与普通的自然语言推理区分开， 论文将严格限定为对世界状态的显式文本化结构表达 （如具体的坐标、 符号化矩阵等）。 它侧重于 * * " 当前的静态画面是什么样 " * *。 示例： 在方块三视图任务中， 是用字符画拼接成的二维矩阵， 用 0、 x、 确表示方块的位置和颜色状态。 在迷宫任务中， 是显式的坐标点元组， 如 < p。 土 nt > 246 83</point> 在折纸任务中， 是表示网格覆盖状态和孔洞形状的二维数组。 * 等符号精

## 三、损失或目标函数

在BAGEL等多模态输出模型上进行微调，损失或目标函数如下。

1.监督微调

（1）纯自回归方案（智源Emu）：统一的交叉熵损失。

（2）自回归+图像扩散方案（清华/字节研究）

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 监督微调阶段工作流与损失函数 (SFT Loss) 在 SFT 阶段， 模型的目标是模仿数据集中交替出现的文本思考 （0） 和中间图像观测 （）。 因此， 其损失函数结合了用于文本的交叉熵损失 (Cross-entropy loss) 用于图像生成的流匹配损失 (FIow-matching IOSS) 数学公式定义如下： H+I 到文本损失项 （公式前半部分）： 针对问题 Q 和初始图像厶计算模型在当前推理前缀下， 生成每一步文本推理 0 中每一个词 ri,j 的负对数似扌吕失视觉损失项 （公式后半部分）： 对于生成的中间图像观测仂， 首先对其添加噪声得到 （其中 = tot + （1 一 t)e)。 然后， 计算模型预测的流场向量与真实的噪声方向 (e 一 0，） 之间的均方误差 (MSE), 以此来优化视觉生成能力。

这里用的是图文推理数据集，其中的样本满足：

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在这个数据集中， 每一个样本都被设计成严格的因果逻辑链： 先规划： 文本解释当前的推理瓶颈 （例如： “ 这里的路线太复杂， 我需要在地图上画出从 A 到 B 的路径来验证连通性 “）。 再操作： 紧接着插入特殊的视觉起始 Token （如 <lvision_startl> ), 随后是一串视觉 Tokens, 用于在原图上生成红色的导航线。 后总结： 视觉 Tokens 结束后， 接上特殊的视觉结束 Token （如 <lvision_endl> ), 文本继续基于刚才 " 画 " 出的线进行结论推导。 通过在这 2 ． 4 万条数据 （氵函盖拼图重组、 空间导航、 视觉搜索、 图表重聚焦四大任务） 上进行微调 (Fine-tuning)， 模型在底层的 Attention 矩阵中， 深深地建立了文本意图一 > 视觉生成触发一 > 视觉特征映射一 > 新文本结论的注意力权重依赖。

2.强化微调（RLVR）

（1）仅奖励文本token方案（清华/字节研究）

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 强化学习阶段工作流与优化目标 (RLVR Objective) 在 SFT 之后， 为了进一步提升逻辑推理的正确率， 论文引入了 RLVR 阶段。 由于很难为中间生成的图像定义准确的奖励分数， 此阶段的工作流采取了 * * " 优化文本， 正则化视觉 “ * * 的策略： 即仅使用 GRPO 算法优化文本生成部分， 而对视觉生成部分施加相对参考模型的 KL 散度约束。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

文本强化项 （公式前半部分）： 这是标准的 GRPO 裁剪目标函数。 通过计算当前策略与旧策略的比率， 并结合优势函数和裁剪操作 (Clip)， 推动模型生成能获得更高最终正确奖励的文本链式思考。 视觉正则化项 （公式后半部分）： 这部分计算的是当前正在训练的模型吻与冻结参数的 S FT 参考模型吻， “ 输出的流场之间的 L2 距离。 在连续时间扩散 ／ 流模型中， 这等价于计算两个概率分布的 KL 散度。 它强制模型在探索更好的文本推理策略时， 不要 " 灾难性地遗忘 " 在 S FT 阶段学到的生成正确物理世界图像的能力。

（2）同时奖励多模态方案（智源Emu）

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

统一奖励系统： 构建了一个多维度的奖励函数。 例如， 文本渲染任务使用 OCR 准确率奖励， 图像生成使用美学评分和 C 凵 P 相似度奖励。 规范化处理： 为了平衡不同任务间异构的奖励分布， 所有奖励信号在反向传播前被统一归一化到 [ 1， 10 ] 的区间内， 防止模型在单一任务上出现 “ 奖励作弊 （Rewa rd Hacking)"

## 四、信息论背景

1.误差分解定理

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 定理陈述令 p 为最优链式思考与答案的真实分布 （即 Oracle)， 为当前模型学习到的联合分布。 模型端到端生成答案的误差 （由 KL 散度衡量） 可以通过以下不等式进行分解：

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

> [图片 12 OCR 未识别出有效文字：未识别出有效文字。]

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

H+I

公式的后半部分代表世界建模误差，如果我们采用隐式世界建模（即不显式生成中间图像或坐标），第二项的误差降为0，但状态转移（如物理碰撞、空间旋转）都必须压缩在模型的隐状态中进行，增加了推理的复杂度和不确定性，将导致第一项（推理误差）剧烈膨胀。

2.推理不确定性降低的程度界限

> [图片 14：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

令为隐藏的真实物理世界状态。 显式的世界建模对推理不确定性 （由香农熵表示） 的降低满足以下两个性质：

> [图片 15：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

性质 1： 不确定性不增原则引入过去的观测仁 1， 不会增加当前推理步 0 的熵。 其降低量等于两者之间的条件互信息 （0@0， ro： 仁 1） 一 （和月，） = 丑@1 仁 1； 0@0， ro： 仁 1） 0

> [图片 16：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

性质 2： 不确定性降低的理论上界这种不确定性的降低量受到两个因素的严格制约 （取最小值）： （01 一 1； Ti 00， T() 汤一 1） min@（01 一 1； S 巧一 1，） （50 汤一 1， T() 汤一 1

> [图片 17：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这个 min 函数非常优雅地解释了 " 视觉优势假说 " 的适用边界： 第一项盯@； 5） 代表 “ 观测的丰富度 "。 这要求生成的观测必须能充分反映底层的真实状态。 在空间物理任务中， 纯文本很容易遭遇 " 表征瓶颈 " （难以用文字描述复杂的空间变换）， 而视觉图像具有极高的互信息量， 因此能大幅拉高这个上界。 第二项 5） 代表 “ 任务的需求度 "。 这要求观测中包含的信息， 必须是下一步推理真正用得上的。 “ 过拟合 " 的代价： 如果一个任务非常简单 （例如文中提到的迷宫任务， 只需要跟踪简单的二维坐标 ), 第二项） 的值会非常小。 此时， 即便你生成了极度高保真、 信息量巨大的视觉图像 （第一项很大 ), 整个 min 函数的结果依然很小。 这种情况下， 过于丰富的视觉细节不仅是不必要的， 反而会徒增定理 1 中的 “ 世界建模误差 "， 对整体推理产生负面影响。

## 五、基准测试

### （一）需要空间想象的文本生成推理

用高度空间性和结构性的问题，可以评估模型在需要生成或利用中间视觉图像的场景下的真实推理能力，例如字节Seed在MIRA基准测试中提供的欧几里得几何、抽象空间与逻辑谜题、物理推理、因果变换等问题。

根据目前的基准测试情况来看，纯文本架构在应对多步复杂空间视觉任务时得分均较低，存在固有缺陷；强行喂给模型一系列人工标注的中间视觉线索图像后，模型的表现大幅提升。同时有文本输出和基于世界模型的图像输出的模型具有更好的物理推理能力。这也证明了结合世界模型的推理的重要性。

但值得注意的是，ROVER等测试也指出即便多模态输出模型，也将抽象的逻辑推理转换为视觉符号表示（例如自主绘制有效的几何辅助线）上存在明显缺陷。这可能和现在世界模型主要限制在像素而非抽象空间上输出有关。

### （二）需要文本逻辑的视觉生成推理

ROVER等测试中，提出了另一种跨模态交互范式：模型是否能使用文本提示和逻辑推理链，来指导复杂的图像生成任务，提出时间变换、空间变换、因果推理、定量变换、几何原理应用等问题。

根据目前的基准测试情况来看，模型的内部推理过程和对齐度直接决定了最终图像的质量。支持文本与图像交错生成的模型，在视觉生成表现上显著优于仅支持单轮单模态输出的模型，而单纯把语言推理模型与专门的图像生成模型级联组合，是不能复现这种能力的。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 因此，可应用原生多模态输出的模型进行微调（或重新预训练），在语言CoT与世界模型的空间推理结合，推进推理过程。下面介绍一些相关工作，如清华、字节的《Visual Generation Unlocks Human-Like Reasoning through Multimodal World Models》，智源Emu3.5等。
- 2 · 强化学习阶段工作流与优化目标 (RLVR Objective) 在 SFT 之后， 为了进一步提升逻辑推理的正确率， 论文引入了 RLVR 阶段。 由于很难为中间生成的图像定义准确的奖励分数， 此阶段的工作流采取了 * * " 优化文本， 正则化视觉 “ * * 的策略： 即仅使用 GRPO 算法优化文本生成部分， 而对视觉生成部分施加相对参考模型的 KL 散度约束。
- 文本强化项 （公式前半部分）： 这是标准的 GRPO 裁剪目标函数。 通过计算当前策略与旧策略的比率， 并结合优势函数和裁剪操作 (Clip)， 推动模型生成能获得更高最终正确奖励的文本链式思考。 视觉正则化项 （公式后半部分）： 这部分计算的是当前正在训练的模型吻与冻结参数的 S FT 参考模型吻， “ 输出的流场之间的 L2 距离。 在连续时间扩散 ／ 流模型中， 这等价于计算两个概率分布的 KL 散度。 它强制模型在探索更好的文本推理策略时， 不要 " 灾难性地遗忘 "...
- 1 · 定理陈述令 p 为最优链式思考与答案的真实分布 （即 Oracle)， 为当前模型学习到的联合分布。 模型端到端生成答案的误差 （由 KL 散度衡量） 可以通过以下不等式进行分解：

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
