---
title: "31.3 每步引入可学习噪声的Online RL（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/31.扩散模型与流匹配模型的强化学习/31.3 每步引入可学习噪声的Online RL（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 31.3 每步引入可学习噪声的Online RL（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

Flow-Noise等方法在原有流匹配模型的基础上引入可学习的噪声网络，将去噪过程重塑为离散时间的MDP，从而可用传统RL算法，既解决了流匹配缺失随机性、无法计算动作概率似然的问题，又通过可学习、有方向的探索解决了高维空间盲目探索往往失败的问题。

## 一、核心思想

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

传统的流匹配是在连续时间内求解常微分方程 (ODE)， 缺乏随机性， 难以像 PPO 那样进行对数似然 (log-likelihood) 的计算和在线试错探索。 Flow-Noise 的创新在于， 它在原本确定性的去噪路径中， 主动注入了一个可学习的噪声网络， 将去噪过程强行重塑为一个离散时间的马尔可夫决策过程 (Discrete-time MDP)0 这使得模型在每一步生成时既有探索能力， 又能获得精确的动作对数似然估算， 从而完美适配传统的 Actor-Critic 在线 RL 算法。

## 二、算法原理

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

它放弃了纯确定性的 ODE 采样， 而是引入了一个可学习的噪声网络 (Learnable Noise Network), 将原本连续的去噪轨迹离散化为若干步 （步长为， 并在每一步中注入受控的随机性。 在该内部去噪 MDP 中： 状态空间 (State)： 在去噪时间步丆， 内部状态定义为当前环境观测与当前隐变量的组合： 动作空间 (Action)： 内部的 " 动作 " 即为流模型生成的下一个离散时间步的隐状态 （当丆 < 1 时） 或最终在环境中执行的物理动作 （当丆 = 1 时）： 1 At if 丆 < 1 if 丆 = 1 随机转移与可学习噪声： 从 AtT 到 A05 的转移被建模为高斯分布： 其中， 6 ～ （0， I） 是注入的标准高斯噪声， 由流匹配网络预测出的均值方向决定， 则是由可学习的噪声网络输出的标准差。

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 · 如何解决 “ 超高维加高斯噪声极易崩溃 "？ 解法： 引入可学习噪声网络 (LearnabIe Noise Network) 如果你在 1024 × 1024 × 3 的每个像素上都盲目叠加固定方差的常数高斯探索噪声， 图像必然会碎裂成纯噪声。 Flow-Noise 的做法是不使用固定的探索方差， 而是引入一个额外的神经网络参数来动态预测协方差习鼠， 0。 此时， 策略网络输出的不再是一个确定的点， 而是一个高斯分布： 自适应探索： 在生成轨迹的安全区域 （例如刚开始去噪、 只有大轮廓时 ), 网络会输出较大的方差以鼓励广泛探索； 当进入对噪声极其敏感的高频细节生成区域时， 会自动收敛输出极小的方差， 甚至趋近于 0。 维度解耦： 网络可以学习到在不重要的背景特征维度上加大探索， 而在关键的语义维度上保持克制， 从而完美避开了高维空间中的级联崩溃。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

由于每一步丆 + 丆 + 都服从已知的高斯分布， 整个去噪轨迹的联合概率密度就可以被精确分解并计算。 给定一条从丆 = 0 到丆 = 1 的离散去噪轨迹， 其总动作对数似然为每一步转移概率对数的累加： log） 一乥 (A | At ot) 因为是由参数化的高斯分布构成， 上式变得完全可计算， 从而可以直接用于 P PO 等基于策略梯度的 RL 算法的目标函数中。

## 三、工作流

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 1： 环境交互与初始化 （外部循环起点） 1 ． 智能体从仿真环境或现实世界获取当前时间步的观测状态 （如摄像头图像、 本体感受数 2 ． 在模型内部， 初始化流匹配的起点， 采样纯高斯噪声 A? ～ （0， 1)0 步骤 2： 离散化去噪过程 （内部 M D P 循环） 开启一个从丆 = 0 到丆 = 1 （步长为） 的离散去噪循环。 对于每一步丆： 1 ． 2 ． 3 ． 4 ． 构建内部状态： 组合观测与隐变量得到网络推理： 将输入 VLA 骨干网络 （如 Transformer), 预测出当前的速度场， 进而计算出期望去噪方向。 噪声评估： 辅助的可学习噪声网络根据当前状态输出标准差。 采样与记录： 采样随机噪声 e， 计算下一步隐变量 A05 = +。 · 同时， 将该步的丆 + 丆对数似然 log ()t lAt,ot) 记录到缓存中。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

步骤 3： 动作执行与奖励获取 （外部循环终点） 1 ． 当内部去噪循环到达丆 = 1 时， 得到无噪声的确定性物理动作 = A} 2 ． 将动作 at 下发给环境执行。 3 ． 观测状态更新， 并返回单步奖励 （例如在抓取任务成功时返回稀疏的 + 1 奖励）。 步骤 4： 强化学习策略更新 ()L 反向传播） 1 ． 2 ． 3 ． 4 ． 收集到一个或多个 Episode 的完整轨迹后， 计算累计优势函数 (Advantage)。 提取出步骤 2 中精确计算的联合对数似然总和 E log。 应用 PPO 算法的截断代理目标函数 (Surrogate Objective) 计算策略梯度。 更新 VLA 模型网络参数 O 以及噪声网络参数， 使得在相似观测下， 能获取高环境奖励的去噪轨迹生成概率最大化。

## 四、该方法如何解决流匹配RL“脱离最优传输后步数激增”的问题

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

2 · 如何解决 “ 脱离最优传输后步数 (NFE) 激增 "？ 解法： 一步到位的单层马尔可夫决策过程 (One-layer Discrete-time MDP) 在传统的流匹配中， 轨迹变弯曲会导致 ODE 求解器 （如欧拉法、 RK4) 在积分时产生巨大的截断误差， 从而必须将步长 At 切得极小 （即 NFE 激增）。 Flow-Noise 从根本上绕开了 ODE 求解器。 它将整个生成过程重构成了一个离散时间的 MDP0 环境的转移函数被强行定义为： Ct—At = at 这意味着， 模型不是在预测一个连续的 " 切线方向 "， 而是直接跳跃到下一个离散状态。 RL 算法 （如 PPO) 优化的是这一个个离散跳跃点的累积奖励。 既然根本不用连续微分方程来积分， 自然也就无所谓 " 曲率导致截断误差 " 的问题， 模型依然可以在较少的步数内 （例如 10 步 MDP) 稳定生成高奖励结果。

这让Flow-Noise看起来很像扩散模型，但同时又具备了流匹配模型自身去噪轨迹接近直线、速度快的优点。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文标题标记为论文笔记，但未自动发现原论文链接、arXiv/DOI、作者或年份，建议人工补充。
- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
