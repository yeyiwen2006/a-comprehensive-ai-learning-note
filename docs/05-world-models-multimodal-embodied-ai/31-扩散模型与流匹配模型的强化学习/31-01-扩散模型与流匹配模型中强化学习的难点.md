---
title: "31.1 扩散模型与流匹配模型中强化学习的难点"
source_docx: "第5部分 世界模型、多模态生成与具身智能/31.扩散模型与流匹配模型的强化学习/31.1 扩散模型与流匹配模型中强化学习的难点.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 31.1 扩散模型与流匹配模型中强化学习的难点

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

## 一、马尔可夫过程的变化

在扩散模型和流匹配模型下，马尔可夫过程的形式和纯自回归的LLM有所不同。以流匹配为例：

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

1 ． 2 ． 3 ． 4 ． 状态： 当前时间步的连续图像 / 潜变量。 动作： 模型输出的连续向量场 （一个方向）。 环境转移： 确定性的数值积分， 例如欧拉法 Ct+At = + A。 奖励： 只有积分到 = 1 得到最终图像后， 才能获得美学评分或对齐评分月 （）。

从x_0到x_1的过程，实质上是v_t关于t从0到1积分，这里v_t是一个连续函数，事实上一般是采样几十步，每次沿该时间步的速度走一小步。

## 二、扩散模型与流匹配中RL的共同困境

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

无论是通过随机微分方程 (SDE) 去噪的扩散模型， 还是通过常微分方程 (ODE) 积分的流匹配， 它们在微调时都面临着生成过程长、 动作空间维度极高的宏观挑战。 1 · 稀疏奖励下的长轨迹信度分配难题 (Credit Assignment ove r Long Trajectories)

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

原理一致性： DM 和 FM 生成一张最终图像均需要数十步的逐步迭代。 无论是预测噪声还是向量场吻， 这都是一个序列决策过程。 困境所在： RL 的奖励信号 （如美学评分、 人类偏好对齐评分月 00）） 极其稀疏， 只能在生成过程结束、 得到最终完整图像后才能获取。 由于中间没有任何即时奖励 (Dense Reward)， 旦最终图像评分很低， RL 算法极难搞清楚到底是哪一步 （比如是乯 = 0 ． 9 的大轮廓生成阶段， 还是 = 0 ． 1 的细节纹理阶段） 的神经网络输出出现了失误。 后果： 传统的优势函数估计 （如 GAE) 在这种长链条的 ODE/SDE 求解器中难以向回精准传播奖励梯度， 导致优化极其低效， 甚至出现错误的信用归因。 2 · 超高维连续动作空间的探索危机 (Exploration in High-Dimensional Continuous Space)

尤其是价值网络训练极易崩溃，这也就是基于GRPO的算法更常用的原因。

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

原理一致性： DM 和 FM 生成一张最终图像均需要数十步的逐步迭代。 无论是预测噪声还是向量场吻， 这都是一个序列决策过程。 困境所在： RL 的奖励信号 （如美学评分、 人类偏好对齐评分月 00）） 极其稀疏， 只能在生成过程结束、 得到最终完整图像后才能获取。 由于中间没有任何即时奖励 (Dense Reward)， 旦最终图像评分很低， RL 算法极难搞清楚到底是哪一步 （比如是乯 = 0 ． 9 的大轮廓生成阶段， 还是 = 0 ． 1 的细节纹理阶段） 的神经网络输出出现了失误。 后果： 传统的优势函数估计 （如 GAE) 在这种长链条的 ODE/SDE 求解器中难以向回精准传播奖励梯度， 导致优化极其低效， 甚至出现错误的信用归因。 2 · 超高维连续动作空间的探索危机 (Exploration in High-Dimensional Continuous Space)

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

原理一致性： 在大语言模型 (LLM) 的 RLHF 中， 动作空间是离散的词表 （通过调整 logits 的概率即可轻松探索）。 而在 DM 和 FM 中， 模型的 " 动作 " 是输出与图像分辨率同等大小的连续张量 （例如 1024 × 1024 × 3 的潜变量）。 困境所在： 要在如此庞大的连续高维空间中进行有效的随机探索 (Exploration), 无异于大海捞针。 如果在每一步预测的向量场或噪声上盲目叠加高斯的探索噪声， 极易导致整个微分方程的积分轨迹出现级联误差 ℃ ascading Errors), 使最终生成的图像彻底崩溃成无意义的噪后果： 为了保证生成轨迹不崩溃， 研究者只能将探索噪声的方差设置得极小， 但这又会导致模型缺乏探索能力， 迅速陷入局部最优， 这也是 DM+RL 极易引发模式崩溃 (Mode Collapse)、 丧失生成多样性的底层原因。

## 三、流匹配中RL的特殊难点

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

虽然 FM 在很多方面比 DM 更简洁高效， 但在与 RL 的结合上， 它受限于自身的底层数学假设， 面临着比扩散模型更颟手的独有理论困局。 1 · 破坏最优传输的 “ 直线特性 " 与流形脱离 (Breaking Optimal Transport Straight Lines)

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

困境所在： 流匹配 （尤其是目前主流的 OT-FM) 在预训练时的最大卖点， 是通过最优传输 (Optimal Transport) 强制对齐纯噪声昀和真实图像， 构造了一条极其简单的零曲率直线轨迹： 直线破裂： 预训练的 FM 是一个完美遵循这条直线的拟合器。 当引入 RL 时， RL 策略梯度的唯一目标是 " 最大化最终图像的奖励 "。 为了迎合奖励， RL 会强行修改中间时间步的， 这就不可避免地将 ODE 轨迹 “ 拽离 " 了原本的最优传输直线。 后果： 扩散模型原本的轨迹就是弯曲的高曲率布朗运动， 所以稍微拽偏一点尚能承受。 但对于 FM 而言一旦轨迹脱离直线， 不仅会产生图中所说的 " 偏离流形、 产生无意义图像 " 的现象， 更致命的是， 轨迹曲率的激增会导致 ODE 求解器的截断误差 (Truncation Error) 爆炸。 原本 FM 只需要 4 步积分就能出图， 被 RL 扭曲后， 必须增加到几十甚至上百步才能维持图像质量， 直接粉碎了 FM 最大的 “ 少步数采样优势 " 2 · 确定性 ODE 带来的 “ 似然度计算死锁 " (Likelihood Degeneration)

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

困境所在： 标准在线 RL （如 PPO/GRPO) 必须计算动作的对数似然度 logn(als)o DM 的逆向去噪本身就是随机高斯转移， 天然自带可计算的概率密度。 数学死锁： FM 的生成过程是纯确定性的常微分方程 (ODE)0 这意味着给定状态， 其转移到下一步 Ct-At 的概率在数学上是一个狄拉克函数 (Dirac delta function)o 对函数求 log 会导致无穷大 (Degenerate Likelihood)0 因此， 标准 RL 算法在纯粹的流匹配上是完全无法运行的。 必须强行魔改底层架构 （例如通过 ODE-to-SDE 转换人为注入探索噪声 ), 这让原本极其优雅简洁的 FM 变得异常臃肿和不稳定。

当然需要注意的是，流匹配模型在给定输入的情况下输出是固定的，不代表其输出不存在概率分布：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

在流匹配中， 我们常说的 " 去噪过程是固定的 "， 指的是给定一个具体的初始噪声 z 后， 它演化到目标数据的路径 (Trajectory) 是唯一且确定的 （因为我们求解的是常微分方程 ODE, 而非随机微分方程 SDE)O 但是， 模型具有分布， 是因为我们的输入源头是一个分布。 这在数学上被称为前推测度 (Push- forward Measure)0 想象你手里有一把沙子 （标准高斯分布的噪声 z ～ （0，）， 你将沙子撒在一个有特定风向 （向量场）） 的传送带上。 虽然每一粒沙子被风吹动的轨迹是完全确定、 固定的， 但因为一开始沙子落在各个位置的概率不同 （高斯分布的密度不同 ), 当它们最终被吹到终点时， 就会自然而然地堆积成一个新的形状一一这就是模型生成的数据分布）。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
