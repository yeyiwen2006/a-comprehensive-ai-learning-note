---
title: "31.2 Offline RL转化为加权监督学习（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/31.扩散模型与流匹配模型的强化学习/31.2 Offline RL转化为加权监督学习（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 31.2 Offline RL转化为加权监督学习（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、RLVR转化为逼近最优策略的加权监督学习

### （一）核心思想

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

该方法的核心思想是 * * " 避开在线探索的难题， 将 RL 转化为加权的监督学习 " * *。 · 它首先定义了一个代理散度刀丆 M， 试图让当前模型在流匹配损失空间内逼近理论最优策略丌。 由于无法直接从丌 * 采样， 它利用重要性采样 (lmportance Sampling), 引入了静态离线数据集 D， 并赋予权重茌@， a) exp(A/ß), 即优势越大的动作， 权重越高。 点睛之笔在于最后的 “ 极设定 "： 通过让折扣因子 7 + 1 并给予极端负奖励， 优势函数被极化。 权重 （0， a) 变成了二值 { 0， 1 }。 这在数学上等价于直接丢弃所有 " 失败 " 的轨迹， 只保留 " 成功 " 的轨迹进行监督学习。

### （二）数学表达

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在标准的正则化强化学习框架下， 我们的目标是在最大化奖励的同时， 约束新策略砌不要偏离参考策略丌 7 可太远。 其目标函数定义为： 根据数学推导， 这个目标函数存在一个理论上的最优策略闭式解丌 ''可@ a 丌 * （司丌，。 氟 a@） exp 0 这意味着， 最优策略倾向于在参考策略的基础上， 给予那些具有高优势函数 (Advantage Function, > 0） 的动作更高的概率

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

因为这个理论最优策略丌 * 通常极其复杂， 无法直接用一个有限参数的神经网络表示， 所以我们需要进行策略投影 (policy Projection) 一即找一个参数化的策略， 让它尽可能地去拟合丌 *。 在传统的优势加权回归 (AWR) 算法中， 拟合的手段是最小化丌 * 和之间的 KL 散度： 俨 = arg min Eo、 刀人 （丌 (• 0 〗 砌 · 0 展开 KL 散度后， 这等价于最大化加权对数似然 (weighted Log-likelihood)： E[w(), a) log 丌和@〗。 问题就在这里： 我们的 v LA 模型是用流匹配目标 £ 丆 M 训练的， 它根本算不出确切的 log 丌国司。 这条传统路线走不通了。

替代散度法：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

DFM(T*(•Io), 砌@司） E、 丌气 ．@） [CFM （0， a 通俗解释： 这个公式的物理意义是： “ 假设我现在能从理论最优策略丌 * 中抽取出完美的动作样本 a， 那么我就用模型自带的流匹配损失函数 CFM， 强迫我的神经网络砌去学习、 生成这些完美的动作。 " 这就巧妙地避开了计算概率分布的要求， 直接在流匹配的损失空间里衡量两个策略的 " 距离 "

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

有了新的散度刀丆 M， 策略投影步骤就变成了： 俨 = arg min E E 但我们在现实中显然无法真的从未知的理论最优策略丌 * 中采样。 因此， 论文采用了离线强化学习中标准的 * * 重要性采样 (lmportance Sampling) * * 技巧： 我们用手头收集到的固定数据集 D 中的样本来代替丌 * 中的样本， 并给这些样本乘以一个权重 w(), a)， 权重正比于指数化的优势函数 exp(A/ß)。 推导到这一步， 目标函数变为了： 俨 arg min E 最后， 为了极致的简化和易于扩展， 论文做了一个极端设定： 将折扣因子 7 设为接近 1， 并给失败的轨迹分配极大的负奖励。 这导致权重 w(), a) 退化成了二值权重 （成功的动作权重为 1， 失败的为 0）。 这也就是论文中方程 （4） 最终用于策略微调的监督学习目标函数的由来。

## 二、DPO for Diffusion

### （一）数学表达

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第一步： RLHF 目标的重参数化在标准的带 KL 惩罚的 RL 框架下， 我们希望微调一个策略 （扩散模型） （叫 c）， 使其在给定条件 prompt c 时， 生成的图像能最大化隐含的奖励 r(), c)， 同时不能偏离预训练的参考模型 Pref@IC) 太远： max - [r@, c 〗 一 (PO@IC) IIPref@IC)) 其中是控制亻扁离程度的超参数。 这个优化问题存在一个理论上的闭式最优解 (Optimal Policy)： 1 —Pref(ClC) exp —r(c,c p*@lc) 其中 Z 是配分函数。 D PO 的核心魔法就在于对上式进行移项， 反解出奖励函数 r@, c)： p*@lc) r@, c) = log + log Z Pref@IC)

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在偏好学习中， 我们通常假设人类偏好服从 Bradley-Terry (BT) 模型。 即对于给定的 prompt c, 人类认为图像 (winner, 获胜者） 比 (loser， 失败者） 更好的概率为： 伊 C T C 其中是 Sigmoid 函数。 现在， 将第一步反解出的 r@.c) 代入 BT 模型中。 由于是计算差值， 常数项 log Z 被完美消去了： P9@w c) = 伊 log P9 （c 一 0 log。 f （c 这就是大语言模型 (LLM) 中标准 DPO 的目标函数。 但扩散模型卡在了一个致命问题上： 扩散模型的精确对数似然 logpo(clc) 在数学上是难以计算的 (lntractable)o

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

第二步： 代入 Bradley-Terry 偏好模型在偏好学习中， 我们通常假设人类偏好服从 Bradley-Terry (BT) 模型。 即对于给定的 prompt c, 人类认为图像 (winner, 获胜者） 比 (loser， 失败者） 更好的概率为： T C 其中是 Sigmoid 函数。 现在， 将第一步反解出的 r@.c) 代入 BT 模型中。 由于是计算差值， 常数项 log Z 被完美消去了： P9@w c) = 伊 log P9 （c 一 0 log。 f （c 这就是大语言模型 (LLM) 中标准 DPO 的目标函数。 但扩散模型卡在了一个致命问题上： 扩散模型的精确对数似然 logpo@lc) 在数学上是难以计算的 (lntractable)o 第三步： 扩散模型的 ELBO 替换 Diffusion-DPO 的最大贡献， 就是用 * * 证据下界 (ELBO, Evidence Lower Bound) * * 来近似这个难以计算的对数似在扩散模型中， 极大化对数似然等价于极小化去噪预测误差 (MSE 损失）。 令 £ c, 为模型在时间步的去噪损失 （以预氵则噪声 e 为例）：

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

（C,） 因为似然与去噪损失成负相关， 我们可以做如下近似： log 脚@lc) -Et,e 0@c, + c 因此， 对数似然的比值可以被替换为去噪损失的差值： po@lc) log

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

这个公式的物理直觉非常清晰： 如果参考模型和当前模型在 “ 好图佃刁 " 上的损失差， 大于它们在 “ 坏图佃 l) " 上的损失差， 我们就施加奖励； 反之则施加惩罚。

在流匹配中同理：

> [图片 11：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在流匹配中， 对数似然依然难以精确计算， 但我们可以用向量场拟合的 MSE 损失来替代扩散模型中的噪声预测损失。 只需将公式中的 C9 替换为我们在 DMD 中讨论过的流匹配目标： c5M@ c, 0 = 1 一 £ 0 2 只要将这个向量场误差代入 Diffusion-DPO 的框架中 （这在近期文献中被称为 Flow-DPO), 你就可以利用离线偏好对来直接微调流匹配模型， 而完全不需要涉足在线 RL 求解 ODE 的泥潭。

### （二）工作流

> [图片 12：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

阶段 1： 数据准备 1 ． 构建或收集一个静态离线偏好数据集 DO 数据集中包含大量的元组 (),。，）， 即 " 文本提示词 "、 " 人类 / A | 偏好的好图 "、 " 人类 / A | 淘汰的坏图 "。 2 ． 准备预训练好的基础扩散模型作为参考模型 Oref, 并将其权重、 车纤 3 ． 初始化一个架构完全相同的策略模型 O （即我们要训练的模型， 通常从 Oref 初始化）。 阶段 2： 训练循环 (Batch 级别） 对于每一个训练 Step, 执行以下操作： 1 ． 2 ． 3 ． 4 ． 采样与加噪： 从数据集中采样一个 Batch 的 (),，）。 随机采样时间步 ～ （0， T)， 并采样高斯噪声 6 ～ （0，。 构建中间状态： 利用前向扩散过程的公式， 分别纟合好图和坏图加噪到时间步仁 · t · eTl t ． + 1 一 0z6 计算参考模型的误差 ()o Gradient)： 将加噪后的好图、 坏图连同和 c 输入冻结的参考模型 Oref, 计算它们预测噪声与真实噪声 e 的均方误差： Cref@w) 和 Cref(Cl)0 计算策略模型的误差 (With Gradient)： 将同样的数据输入正在训练的策略模型 O, 计算其

> [图片 13：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

5 ． 反向传播更新： 将上述四个误差标量代入 CDPO-Diff(0) 公式中。 通过深度学习框架 （如 PyTorch) 的自动求导机制计算关于 0 的梯度， 并使用优化器 （如 Adamw) 更新模型权重。

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 在标准的正则化强化学习框架下， 我们的目标是在最大化奖励的同时， 约束新策略砌不要偏离参考策略丌 7 可太远。 其目标函数定义为： 根据数学推导， 这个目标函数存在一个理论上的最优策略闭式解丌 ''可@ a 丌 * （司丌，。 氟 a@） exp 0 这意味着， 最优策略倾向于在参考策略的基础上， 给予那些具有高优势函数 (Advantage Function, > 0） 的动作更高的概率
- 第一步： RLHF 目标的重参数化在标准的带 KL 惩罚的 RL 框架下， 我们希望微调一个策略 （扩散模型） （叫 c）， 使其在给定条件 prompt c 时， 生成的图像能最大化隐含的奖励 r(), c)， 同时不能偏离预训练的参考模型 Pref@IC) 太远： max - [r@, c 〗 一 (PO@IC) IIPref@IC)) 其中是控制亻扁离程度的超参数。 这个优化问题存在一个理论上的闭式最优解 (Optimal Policy)： 1 —Pref(ClC) e...
- 这个公式的物理直觉非常清晰： 如果参考模型和当前模型在 “ 好图佃刁 " 上的损失差， 大于它们在 “ 坏图佃 l) " 上的损失差， 我们就施加奖励； 反之则施加惩罚。
- 阶段 1： 数据准备 1 ． 构建或收集一个静态离线偏好数据集 DO 数据集中包含大量的元组 (),。，）， 即 " 文本提示词 "、 " 人类 / A | 偏好的好图 "、 " 人类 / A | 淘汰的坏图 "。 2 ． 准备预训练好的基础扩散模型作为参考模型 Oref, 并将其权重、 车纤 3 ． 初始化一个架构完全相同的策略模型 O （即我们要训练的模型， 通常从 Oref 初始化）。 阶段 2： 训练循环 (Batch 级别） 对于每一个训练 Step, 执行以下操作...

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
