---
title: "31.4 FlowGRPO：化ODE为SDE（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/31.扩散模型与流匹配模型的强化学习/31.4 FlowGRPO：化ODE为SDE（论文）.docx"
status: "auto-converted"
ocr: "auto-generated, needs human review"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 31.4 FlowGRPO：化ODE为SDE（论文）

> 本文由本地 Word 原稿自动转换而来。图片中的文字由 OCR 自动识别，可能存在识别错误，欢迎提交 Issue 修正。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、从ODE到SDE的变换

> [图片 1：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

从常微分方程 (ODE) 到随机微分方程 (SDE) 的转换 · 背景原理： 在标准的流匹配 (Flow Matching) 或扩散模型中， 图像生成被建模为一个确定性的 ODEO 这意味着， 只要你给定一个初始的纯噪声， 模型去噪生成图像的轨迹就是唯一确定、 不可改变的。 RL 的困境与解法： 强化学习 (RL) 的本质是 “ 探索与利用 (Exploration and Exploitation)"0 如果轨迹是确定的， 模型就无法尝试新的生成路径， 自然也就无从得知哪种路径能获得更高的奖励。 因此， FlowGRPO 将这个确定的 ODE 转化为了 SDEO 通过在每一步去噪过程中引入由控制的随枧噪声扰动， 算法赋予了模型偏离既定路线去 “ 试错 " 的能力。 有了这种随机探索性， RL 才能发挥作用。

具体来说，FlowGRPO将流匹配中的常微分方程（ODE）：

> [图片 2：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

无损转换为了一个随机微分方程 (SDE)： 吻 （， + —g(t)2Vx logpt） + g(t)dwt 数学奇迹： 这里的 g(t)dwt 就是注入的探索噪声。 奇妙的是， 它同时在漂移项 (Drift term) 一个与分数 (Score) 相关的修正项如@） 2V， logpt(c)o 根据福克一普朗克方程中加入了 (Fokker-Planck Equation), 这两者产生的影响会完美抵消。

转化为了随机微分方程：

> [图片 3：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

dct 画@， 0 +g(t)V10gpt@t 〗 + 2g （加 “

仔细观察，可以发现加入的项来自扩散随机加噪的随机梯度朗之万动力学表达式：

> [图片 4：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

dct = V logp@t)dt + uclwt

其中第二项为探索噪声，第一项为保证x关于t的边缘分布不变的修正项，避免模型遇到OOD情形。

乘的系数g(t)表示布朗运动的剧烈程度，是一个预定义好的函数。

## 二、比例归一化与截断机制的修复

> [图片 5：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

比例归一化 (RatioNorm) 与截断机制的修复 · 背景原理： 在 PPO 或 GRPO 等强化学习算法中， 为了保证训练的稳定性， 必须使用 " 截断机制 (Clipping)"0 它通过计算新旧策略的 " 重要性比值 (lmportance Ratio) " 0@）， 强制限制模型每次参数更新的幅度， 防止模型因为某次偶然的高奖励而彻底破坏原本的生成能力。

> [图片 6：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在流匹配的 SDE 采样中， 每一步的去噪转移概率是一个高斯分布。 设定当前步的时间间隔为 At， 噪声方差为 At0 新策略 （当前正在优化的模型） 和旧策略 （收集数据时的参考模型） 的概率密度函数可以表示为： 2 p(c) exp 20 《 A 其中， 和况分别是新旧策略预测的均值 （即向量场的漂移项）。 重要性比值 (lmportance Ratio) 定义为新旧策略的概率之比 rtk@） = 9 （一 At)。 为了方便推 P901d(Xtk—At) 导， 我们取自然对数： log rtk@） = 1 1 IIctk-At 一 2 + 202 At 2 伊 2 At 关键点来了： 在强化学习中， 训练数据的轨迹是由旧策略。 ／ 采样生成的。 因此它必然满足： Ctk—At = + 02 At · 6

> [图片 7：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

在流匹配的 SDE 采样中， 每一步的去噪转移概率是一个高斯分布。 设定当前步的时间间隔为 At， 噪声方差为 At0 新策略 （当前正在优化的模型） 和旧策略 （收集数据时的参考模型） 的概率密度函数可以表示为： 2 p(c) exp 20 《 A 其中， 和况分别是新旧策略预测的均值 （即向量场的漂移项）。 重要性比值 (lmportance Ratio) 定义为新旧策略的概率之比 rtk@） = 9 （一 At)。 为了方便推 P901d(Xtk—At) 导， 我们取自然对数： log rtk@） = 1 1 IIctk-At 一 2 + 202 At 2 伊 2 At 关键点来了： 在强化学习中， 训练数据的轨迹是由旧策略。 ／ 采样生成的。 因此它必然满足： Ctk—At = + 02 At · 6

> [图片 8：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

这里 e ～ （0， 是标准高斯噪声。 将这个 Ctk-At 代入对数公式， 并定义新旧策略的均值差为 = 一， 公式展开并化简后得到： 目 A@2 log 0@） = 2 2 At At 对上述公式求高斯噪声 6 的数学期望， 由于 E[e] = 0， 第二项被消去： 目 A@2 2 《 At 因为目 A 川 》 2 > 0 （只要策略更新了， 均值就会有差异）， 所以对数重要性比值的期望严格小于 0。

> [图片 9：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

截断失效的危机： 在流匹配模型中， 由于概率密度的特性， 这个重要性比值的分布会系统性地向左偏移 （平均值小于 1), 并且在不同的时间步长上表现出极不一致的方差。 这导致预设的截断边界 （比如 [ 1 一 e, 1 + 司） 形同虚设， 无法有效约束模型那些过于自信的错误更新， 进而引发严重的 " 奖励黑客 (Reward Hacking)" 现象， 即模型钻空子获得高分但生成一堆无意义的乱码。 RatioNorm 的解法： 为了修复这个问题， 论文引入了 RatioNorm 技术。 该技术对数重要性比值进行标准化处理， 强行将其分布重新居中到零附近。 这样一来， 截断边界就能再次精准地 " 卡 " 住过大的策略更新， 确保了最终导出的图像目标函数 “，@） 能够平稳收目 A （钰上川 2 log 薦為@） = At log 0為@） + 202 At

> [图片 10：原 Word 此处有图片；为避免版权风险，开源版暂不上传图片。]

**图片文字 OCR（自动识别，待校对；数学公式必须人工核对）：**

> [公式校对警告] 这段 OCR 文本疑似包含数学公式、上下标、希腊字母或高密度符号。请不要直接把自动识别结果视为可靠公式。

截断失效的危机： 在流匹配模型中， 由于概率密度的特性， 这个重要性比值的分布会系统性地向左偏移 （平均值小于 1), 并且在不同的时间步长上表现出极不一致的方差。 这导致预设的截断边界 （比如 [ 1 一 e, 1 + 司） 形同虚设， 无法有效约束模型那些过于自信的错误更新， 进而引发严重的 " 奖励黑客 (Reward Hacking)" 现象， 即模型钻空子获得高分但生成一堆无意义的乱码。 RatioNorm 的解法： 为了修复这个问题， 论文引入了 RatioNorm 技术。 该技术对数重要性比值进行标准化处理， 强行将其分布重新居中到零附近。 这样一来， 截断边界就能再次精准地 " 卡 " 住过大的策略更新， 确保了最终导出的图像目标函数 “，@） 能够平稳收目 A （钰上川 2 log 薦為@） = At log 0為@） + 202 At

## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 在流匹配的 SDE 采样中， 每一步的去噪转移概率是一个高斯分布。 设定当前步的时间间隔为 At， 噪声方差为 At0 新策略 （当前正在优化的模型） 和旧策略 （收集数据时的参考模型） 的概率密度函数可以表示为： 2 p(c) exp 20 《 A 其中， 和况分别是新旧策略预测的均值 （即向量场的漂移项）。 重要性比值 (lmportance Ratio) 定义为新旧策略的概率之比 rtk@） = 9 （一 At)。 为了方便推 P901d(Xtk—At) 导， 我们取自...
- 这里 e ～ （0， 是标准高斯噪声。 将这个 Ctk-At 代入对数公式， 并定义新旧策略的均值差为 = 一， 公式展开并化简后得到： 目 A@2 log 0@） = 2 2 At At 对上述公式求高斯噪声 6 的数学期望， 由于 E[e] = 0， 第二项被消去： 目 A@2 2 《 At 因为目 A 川 》 2 > 0 （只要策略更新了， 均值就会有差异）， 所以对数重要性比值的期望严格小于 0。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
